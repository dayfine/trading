; A2 note: the `weinstein.*` deps below are the established backtest exception
; surface (.claude/rules/qc-structural-authority.md A2 allow-lists analysis/
; imports into trading/trading/ under trading/trading/backtest/**). This library
; reads the snapshot warehouse and reuses the PRODUCTION stage classifier rather
; than reimplementing one; it defines no strategy behaviour of its own.

(library
 (name monster_scan_lib)
 (libraries
  core
  status
  types
  trading.data_panel.snapshot
  weinstein.snapshot_pipeline
  weinstein.stage
  weinstein.types)
 (preprocess
  (pps ppx_compare ppx_deriving.eq ppx_deriving.show ppx_let ppx_sexp_conv)))
