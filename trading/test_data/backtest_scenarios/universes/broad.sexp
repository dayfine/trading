;; Broad universe sentinel — tells the runner to fall back to the full
;; sector map loaded from [data/sectors.csv] (pre-migration behaviour).
;;
;; Used by goldens under [goldens-broad/] for nightly/GHA-only scale runs.
;; See dev/plans/backtest-scale-optimization-2026-04-17.md §Step 1.
Full_sector_map
