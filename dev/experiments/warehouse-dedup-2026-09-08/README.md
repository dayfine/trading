# Warehouse dedupe rebuild — `_v10dedup` (2026-09-08, #2730 ask 3)

Rebuild of the three vintage warehouses (`top-3000` 2000 / 2009 / 2019, the `_v7mark` supersets from `warehouse-rebuild-2026-09-06`) with two changes:

1. **Rename-twin pass armed** (`-dedupe-rename-twins -twin-basis returns`, PR #2733): the `_v7mark` warehouses carried twin pairs (NLS/BFX, BB/BBRY, AABA/YHOO, DOC/HCP_old, AORT/CRY_old, AZN/AZN_old, LANC/MZTI, HPT/SVC …) and every wider-stop arm funded both legs — up to +$890k of a single arm's "edge" was one instrument counted twice (`stop-width-by-state-2026-09-08`).
2. **MEL quarantined** (#2732): a ~$170k/share mis-scaled series with a few $8–12 bars; the map arms bought 1–2 "shares" at $175k and lost $172k–$343k on the 12.2 bar.

Script: `rebuild4.sh` (pinned worktree `sweep-dedup`, container-exclusive, sequential; per-vintage `rename_twin_report_<v>_v10.txt` + `terminal_runs_<v>_v10.csv` land in `results/`).

Then: re-run the item-3 null + map (salts 0–2) on `_v10dedup` 2000, gated by `validator_diff.exe` on V6 (ask 2), and re-base the record band on it (V6 must read 0 on every cell).

## Run log

(pending — launch only when no cell and no agent is live; each vintage is a full 3,000-symbol build.)
- 00:18–01:03 PT 09-09: **2000 vintage built** — `/tmp/snap_top3000_2000_v10dedup`, 2907 snaps = 2907 manifest lines (from 2999 on `_v7mark`: −91 twin symbols −1 MEL), run tree c70c39c23, wall 45 min alone in the container. `results/rename_twin_report_2000_v10.txt`: **83 groups / 91 symbols dropped** (basis=returns, match ≥ 0.95, overlap ≥ 100 d); every pair the item-3 V6 reads named is in it — BFX←NLS, BB←BBRY, AABA←YHOO, AORT←CRY_old, DXC←CSC, UPBD←RCII, MZTI←LANC, SVC←HPT, AZN←AZN_old, DOC←HCP_old, LGTY←AMSWA, TXNM←PNM, PRDO←CECO_old; 7 groups have ≥ 2 twins (e.g. AVNT←AVNT_old+POL_old). `SGP_old1` is NOT a twin (it stays in the manifest, SGP itself is not in the 2000 superset) — the 2010 `Per_position` event on that name in the map arms is a real series, not a duplicate.
- `results/terminal_runs_2000_v10.csv` (69 rows): stray_bar 13 → dropped, stub_tail 16 → truncated, long_low_tail 27 + high_price_tail 1 → kept, and **prefix_misscale 12 → kept**: AGR AKR BKNG DRL GEG_old HPC LAN PEGX SBER SGY SWD TEK_old — the MEL class (a mis-scaled prefix, e.g. AGR at $73,567 through 2006-07-03 then $33.94; BKNG and PEGX at the 999999.9999 sentinel; SBER $107,000) detected by the terminal-runs pass and left in the store. Same series were in `_v7mark`, so the item-3 pairs share them; only MEL was hand-quarantined. Follow-up under #2732: the store-level guard should quarantine this class, not just report it.
- 01:03 PT: the script aborted on 2009 (`expected to drop exactly 1 MEL line … dropped 0` — MEL is only in the 2000 superset); check relaxed to per-vintage expected counts (1/0/0) and the rebuild relaunched — 2000 SKIPs on its manifest, 2009 building from 01:03:49.
- 01:04–02:10 PT: **2009 and 2019 built.** 2009: 2687 snaps = manifest (2886 on `_v9gap` − 199 twin symbols; `rename_twin_report_2009_v10.txt` 184 groups — the gap-filled superset carries far more ticker-change history than the 2000 one: AAL←LCC, ANDV←TSO, AGN←WPI, AMRX←IPXL, AGR←UIL …), wall 43 min. 2019: 2749 snaps = manifest (2936 − 187; 170 groups: AAMI←BSIG, ALTM←LTHM, ARQ←ADES, ATCO←SSW …), wall 23 min. **REBUILD DONE 02:10.** Terminal-runs classes: 2009 = stray 11 / stub 21 / long_low 18 / high_price 1 / **prefix_misscale 12 kept** (AKR BHRB BYDDY CGE CMG GEG_old IBDRY LAN LJPC MILGF NPPXF WLL); 2019 = stub 30 / long_low 9 / **prefix_misscale 6 kept** (BHRB CMG HTV LHC-UN TNM TNT). CMG and BHRB recur across vintages — the split-adjusted-prefix shape (#2732 lead, same comment).
- 02:12 PT: item-3 re-run launched on `_v10dedup` 2000 from `sweep-item3v10` (main 969637974): lane A `a0:0 a1:1 a0:2 r0:0`, lane B `a1:0 a0:1 a1:2` (`stop-width-by-state-2026-09-08/chain-v10.sh`); two concurrent 26y cells, 3.5 GB. The `_v9gap` 2009 / 2019 warehouses and `_v7mark` stay until `_v10dedup` cells validate (V6 = 0 expected on every cell).
