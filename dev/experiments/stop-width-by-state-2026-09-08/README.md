# Item 3 surface — per-macro-state initial stop width (2026-09-08)

Mechanism: `initial_stop_buffer_by_macro_state` (#2718, default-off axis; plan `dev/plans/stop-width-by-macro-state-2026-09-06.md`). Question: does sizing the FALLBACK initial stop by the five-state breadth read (#2685) beat the record's flat 4% on the clean 2000-vintage basis? Motivation: the 09-04 yearly review's per-state P&L (Deteriorating entries −$604k, Recovering +$627k) and the 09-05 width surface (12% wins at 26y on the OLD basis — every pre-#2672 width read must be re-measured).

## Arms (salt 0 first; salts 1–2 on anything that moves)

All three = the record spec `rec26y-new` (clean `_v7mark` 2000 warehouse, record convention, 2000-01-01..2026-06-26, build main e7dde095a in `sweep-item3`) + `((macro_config ((breadth_direction ((enabled true))))))` so all five states can fire:

| arm | per-state map | why |
|---|---|---|
| `r0-record-asis` | the record spec UNCHANGED, breadth read OFF | **build-paired comparator** (QC #2720 D2): the record at the current build; also re-bases the record itself on the post-#2709 build (expected: 263.16% / 707 / 0.385 / 42.37 if #2709 is inert on this warehouse — it need not be; #2705 is inert by construction) |
| `a0-breadth-on-null` | EMPTY (every slot unset → scalar 1.0 = 4%) | **null control**: the breadth read is documented as purely additive; this cell must reproduce the build-paired comparator `r0-record-asis` digit-for-digit or the read is not inert (the 09-07 record cell, 263.16% / 707 / Sharpe 0.385 / DD 42.37, ran at b48537469 — BEFORE #2709, which cancels marker-day tickets and so can act on a marker-carrying warehouse like `_v7mark`; #2705 only retired an already-off guard and is inert — so it is not a build-paired null) |
| `a1-map-neutral8` | bullish/recovering 0.9167 (12%), neutral 0.96 (8%), deteriorating/bearish 1.0 (4%) | the plan's candidate map, Neutral at the low end |
| `a2-map-neutral10` | same with neutral 0.94 (10%) | Neutral at the high end |

Comparator: the record band (`project_record_rebase_2026_09_07`: 263% median, 181–562% across salts, maxDD 42–45%). Read per `mechanism-validation-rigor.md`: distribution (salts), the join (`symbol|entry_date`) to decompose shared vs arm-only P&L, per-state entry counts (the breadth state at entry is in `trade_audit.sexp`), and the exit mix (stop whipsaws vs rotation). A map that helps only through one monster is a lottery, not a lever (`project_stop_anchor_surface_is_dds`).

Universe discipline: top-3000-2000 (broad) — a measurement surface, not sp500.

## Run log

- 08:12 PT: lanes launched (`chain.sh A a0 a2`, `chain.sh B a1`), logs `/tmp/item3-run/chain-{A,B}.log`, artifacts `/tmp/sweeps/item3/`. Two concurrent 26y cells (~4.0 GB of 7.75) — above `container-capacity-scheduling.md`'s one-at-a-time guidance; no agents are dispatched while they run.
- QC #2720 rework: `r0-record-asis` added as the build-paired comparator (queued in lane B after a1); the null's expected trade count corrected to 707 (723 was the superseded 09-03 record); per-state entry counts need a date join on `cascade_summary.breadth_state` (per-Friday), not a per-trade field; the map only ever WIDENS (Deteriorating/Bearish 1.0 = the record scalar).
- 11:01 PT: **a0 (null) = the 09-07 record cell digit-for-digit** — 263.15542634794929% / 707 / Sharpe 0.38537854586575881 / DD 42.371840829902126, V16 PASS / V17 PASS, wall 10,144 s. Two conclusions in one read: the five-state breadth read is inert when no consumer is armed (as `breadth_direction.mli` contracts), AND the build moved from b48537469 to e7dde095a (#2705, #2708, #2709) without touching this cell — so the r0 build-paired comparator (QC #2720 D2) is no longer needed at salt 0 and its lane-B waiter was cancelled; lane B goes to salts instead. a2 started 11:01 in lane A.
- 11:26 PT: **a1 (map, Neutral 8%) salt 0 = 692.29% / 784 trades / Sharpe 0.548 / DD 37.88**, V16 PASS / V17 PASS, wall 11,625 s — vs a0/record 263.16 / 707 / 0.385 / 42.37. One draw, outside the record band's top (562%), so the SHAPE is the read, not the level. Join (`symbol|entry_date`, `results/a{0,1}-*-trades.csv`): 359 shared trades earn +$1.10M → +$2.32M (the same entries, wider stop: CLS 2023-06-20 −$8k → +$979k is +$987k of it; BPT +$159k, CAR +$156k, SLAB +$123k, NVDA +$102k; the largest shared loss WNC 2003 +$271k → +$148k); a0-only 348 trades +$1.34M (LOGI +$354k, BBWI +$333k lost) vs a1-only 425 trades +$2.97M (AEIS 2025 +$604k, NLS +$589k and BFX +$585k both 2020-04-22 `extension_stop`, KTOS +$497k, MOS 2006 +$445k, KLIC, WMB, BBRY). Realised $2.44M → $5.29M. Exit mix: `stop_loss` 450 → 377, `laggard_rotation` 242 → 387, `extension_stop` 3 → 7, `delisted` 7 → 2; force liquidations 2 → 4 (events listed in `results/a{0,1}-*-force_liquidations.sexp`; reviewed below). Same shape as the 09-05 width surface: widening cuts whipsaws and hands exits to rotation, and the level is carried by the known monsters (AEIS, MOS = the record's salt-1 pair; NLS/BFX 2020; CLS 2023) — a tail lever. Per-state entry counts still to do (date join on the per-Friday `cascade_summary.breadth_state`). **Salt-1 pair launched 11:41 in lane B** (`SALT=1 chain.sh B a1 a0`; a0-s1 is the build-inertness control against the record's s1 cell 561.61%); a2 (Neutral 10%) running in lane A since 11:01.
