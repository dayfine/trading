# Item 3 surface — per-macro-state initial stop width (2026-09-08)

Mechanism: `initial_stop_buffer_by_macro_state` (#2718, default-off axis; plan `dev/plans/stop-width-by-macro-state-2026-09-06.md`). Question: does sizing the FALLBACK initial stop by the five-state breadth read (#2685) beat the record's flat 4% on the clean 2000-vintage basis? Motivation: the 09-04 yearly review's per-state P&L (Deteriorating entries −$604k, Recovering +$627k) and the 09-05 width surface (12% wins at 26y on the OLD basis — every pre-#2672 width read must be re-measured).

## Arms (salt 0 first; salts 1–2 on anything that moves)

All three = the record spec `rec26y-new` (clean `_v7mark` 2000 warehouse, record convention, 2000-01-01..2026-06-26, build main e7dde095a in `sweep-item3`) + `((macro_config ((breadth_direction ((enabled true))))))` so all five states can fire:

| arm | per-state map | why |
|---|---|---|
| `r0-record-asis` | the record spec UNCHANGED, breadth read OFF | **build-paired comparator** (QC #2720 D2): the record at the current build; also re-bases the record itself on the post-#2709 build (expected: 263.16% / 707 / 0.385 / 42.37 if #2709 is inert on this warehouse — it need not be; #2705 is inert by construction) |
| `a0-breadth-on-null` | EMPTY (every slot unset → scalar 1.0 = 4%) | **null control**: the breadth read is documented as purely additive; this cell must reproduce the build-paired comparator `r0-record-asis` digit-for-digit or the read is not inert (the 09-07 record cell, 263.16% / 707 / Sharpe 0.385 / DD 42.37, ran at b48537469 — BEFORE #2709, which cancels marker-day tickets and so can act on a marker-carrying warehouse like `_v7mark`; #2705 only retired an already-off guard and is inert — so it is not a build-paired null) |
| `a1-map-neutral8` | bullish/recovering 0.9167 (12%), neutral 0.96 (8%), deteriorating/bearish 1.0 (4%) | the plan's candidate map, Neutral at the low end |
| `a2-map-neutral10` | same with neutral 0.94 (10%) | Neutral at the high end |

Comparator: the record band (`project_record_rebase_2026_09_07`: 263% median, 181–562% across salts, maxDD 42–45%). Read per `mechanism-validation-rigor.md`: distribution (salts), the join (`symbol|entry_date`) to decompose shared vs arm-only P&L, per-state entry counts (the breadth state at entry is in `trade_audit.sexp`), and the exit mix (stop whipsaws vs rotation). A map that helps only through one monster is a lottery, not a lever (`project_stop_anchor_surface_is_dds`).

Universe discipline: top-3000-2000 (broad) — a measurement surface, not sp500.

## Run log

- 08:12 PT: lanes launched (`chain.sh A a0 a2`, `chain.sh B a1`), logs `/tmp/item3-run/chain-{A,B}.log`, artifacts `/tmp/sweeps/item3/`. Two concurrent 26y cells (~4.0 GB of 7.75) — above `container-capacity-scheduling.md`'s one-at-a-time guidance; no agents are dispatched while they run.
- QC #2720 rework: `r0-record-asis` added as the build-paired comparator (queued in lane B after a1); the null's expected trade count corrected to 707 (723 was the superseded 09-03 record); per-state entry counts need a date join on `cascade_summary.breadth_state` (per-Friday), not a per-trade field; the map only ever WIDENS (Deteriorating/Bearish 1.0 = the record scalar).
- 11:01 PT: **a0 (null) = the 09-07 record cell digit-for-digit** — 263.15542634794929% / 707 / Sharpe 0.38537854586575881 / DD 42.371840829902126, V16 PASS / V17 PASS, wall 10,144 s. Two conclusions in one read: the five-state breadth read is inert when no consumer is armed (as `breadth_direction.mli` contracts), AND the build moved from b48537469 to e7dde095a (#2705, #2708, #2709) without touching this cell — so the r0 build-paired comparator (QC #2720 D2) is no longer needed at salt 0 and its lane-B waiter was cancelled; lane B goes to salts instead. a2 started 11:01 in lane A.
- 11:26 PT: **a1 (map, Neutral 8%) salt 0 = 692.29% / 784 trades / Sharpe 0.548 / DD 37.88**, V16 PASS / V17 PASS, wall 11,625 s — vs a0/record 263.16 / 707 / 0.385 / 42.37. One draw, outside the record band's top (562%), so the SHAPE is the read, not the level. Join (`symbol|entry_date`, `results/a{0,1}-*-trades.csv`): 359 shared trades earn +$1.10M → +$2.32M (the same entries, wider stop: CLS 2023-06-20 −$8k → +$979k is +$987k of it; BPT +$159k, CAR +$156k, SLAB +$123k, NVDA +$102k; the largest shared loss WNC 2003 +$271k → +$148k); a0-only 348 trades +$1.34M (LOGI +$354k, BBWI +$333k lost) vs a1-only 425 trades +$2.97M (AEIS 2025 +$604k, NLS +$589k and BFX +$585k both 2020-04-22 `extension_stop`, KTOS +$497k, MOS 2006 +$445k, KLIC, WMB, BBRY). Realised $2.44M → $5.29M. Exit mix: `stop_loss` 450 → 377, `laggard_rotation` 242 → 387, `extension_stop` 3 → 7, `delisted` 7 → 2; force liquidations 2 → 4 (events listed in `results/a{0,1}-*-force_liquidations.sexp`; reviewed below). Same shape as the 09-05 width surface: widening cuts whipsaws and hands exits to rotation, and the level is carried by the known monsters (AEIS, MOS = the record's salt-1 pair; NLS/BFX 2020; CLS 2023) — a tail lever. Per-state entry counts still to do (date join on the per-Friday `cascade_summary.breadth_state`). **Salt-1 pair launched 11:41 in lane B** (`SALT=1 chain.sh B a1 a0`; a0-s1 is the build-inertness control against the record's s1 cell 561.61%); a2 (Neutral 10%) running in lane A since 11:01.
- **Quality flags reviewed (force liquidations, `Per_position` breaker):** a0 = 2 events (GERN 2012-09-10 −$61k; AWRE 2014-07-25 −$87k); a1 = 4 events (SGP_old1 2010-09-20 −$102k — a synthetic `_old1` rename twin, i.e. the twin-detector's split copy of SGP, so a data-quality note as well as a gap; FOSL 2012-05-08 −$85k; AWRE 2014-07-25 −$76k; IMMR 2018-08-03 −$79k). All are single-day gaps through the stop on real news days (GERN trial failure, FOSL and AWRE and IMMR earnings collapses), the same class as the APLS event on the `_v9gap` cells — the breaker doing its job, not data. The map adds two because a wider stop leaves more room for a gap to open below it; the count stays in the range the record itself carries. `trades.csv` labels them `stop_loss` (the labelling nit filed on #2717 stands).
- **QC #2728 rework (12:20 PT) — three corrections that change the read:**
  1. **V6 twin regression on a1.** `results/a1-map-neutral8-s0-validator.sexp.md`: `V6 INVARIANT 6 violations` (BFX/NLS, BB/BBRY, AABA/YHOO, DOC/HCP_old, AORT/CRY_old, AZN/AZN_old) vs `V6 PASS` (3 residual) on a0 and on the 09-07 record. The `_v7mark` warehouse still carries twin pairs (the 2000-vintage rebuild ran `build_snapshots.exe` without a dedupe pass), and on the map arm BOTH entries of a pair get funded — a0 never entered the twin partners at all (zero rows for NLS/BFX, BB/BBRY, YHOO, AORT, HCP_old, AZN in `a0-…-trades.csv`), so the mechanism is sizing/funding (the map's smaller tickets — AABA 5,353 sh vs 5,844 — let both twins clear the cash gate; 784 vs 707 trades), not a stop-out of one twin. **NLS/BFX and BB/BBRY are one instrument each** (bar-for-bar identical OHLC in the store; same entry date, same share count, same exit) — the "monsters" list above counted NLS and BFX twice. Duplicated excess ≈ **$764k = 27% of the realised delta**. Data-fix item queued as #2730: dedupe twins at build (`-dedupe-rename-twins` exists on `build_scenario_snapshots.exe`; the vintage rebuild path needs it too) before any salted read of this surface is quoted.
  2. **Concentration.** The +429pp is a $4.29M final-equity delta; the six named movers = **$3.71M = 86% of it** and 130% of the realised delta — **excluding them the map is realised-NEGATIVE vs the null**. Only 66% of the +429pp is realised; **34% is mark-to-market on 9 open positions** vs the null's 3. So the salt-0 level is a handful of tail trades, two of them double-counted twins.
  3. **AWRE 2014-07-25 is a corporate action, not a news gap.** The store's `close/adjusted_close` ratio is a constant 0.736 through 07-24 and exactly 1.000 from 07-25 — a ≈$1.75/share distribution on a $6.63 close, matching the 6.63 → 4.93 open. The simulator runs unadjusted OHLC, so the position gapped through its stop on a distribution the data does not carry as an event. GERN, FOSL and IMMR verified genuine (ratio constant across their events). "The breaker doing its job, not data" therefore holds for three of the four a1 events and for one of the two a0 events (GERN; a0's other event IS the same AWRE distribution), not for AWRE — a data-handling limitation (special dividends) worth its own quality flag.
  Net read after the corrections: the map's salt-0 level is inflated by twin duplication and carried by six trades; what survives as a *shape* is the exit-mix shift (stop_loss 450 → 377, rotation 242 → 387) and the maxDD improvement (42.4 → 37.9) — re-read on a deduped warehouse and across salts before any of it is quoted as a lever.
- 14:34 PT: **a2 (map, Neutral 10%) salt 0 = 946.24% / 806 trades / Sharpe 0.621 / DD 35.20**, V16 PASS / V17 PASS, wall 12,761 s (`results/a2-map-neutral10-s0-*`). One draw, same caveats as a1 — **never quote 946**. Read, same join (`symbol|entry_date`, vs a0): 375 shared trades +$1.28M → +$2.02M (WSM 2023-09-25 +$446k, BPT +$321k, BBWI +$308k, NVDA 2020 +$208k, INFY +$176k, CAR +$171k; WNC 2003 −$122k); a0-only 332 trades +$1.17M (LOGI +$354k, IPIXQ +$256k lost) vs a2-only 431 trades +$3.82M (NOVT 2016 +$759k, **NLS +$750k and BFX +$746k = one instrument, twin double-count**, KTOS 2025 +$553k, KLIC 2020 +$294k, CLFD +$236k, BB +$223k, UTHR +$216k). Realised $2.44M → $5.84M (+$3.40M); **unrealised $0.32M → $3.80M (+$3.48M) on 9 open positions vs 3 — half the +683pp is mark-to-market, and two open names are $2.67M of it: VIAV 2025-10-02 (12.98 → 47.63, +$1.57M) and KLAC 2025-06-17 (90.08 → 248.64 on the split-safe basis, 10:1 split 2026-06-12, +$1.10M)**; ALKS +$325k, NOK +$210k, MSM +$182k. a1 also holds KLAC (+$1.20M) but not VIAV — the a1/a2 join is 469 shared / 315 a1-only / 337 a2-only, so Neutral 8% vs 10% re-draws ~40% of the trade list (the 2% step is inside the path-dependence noise; salts, not a2-vs-a1, are the read). Exit mix vs a0: `stop_loss` 450 → 363, `laggard_rotation` 242 → 420, `extension_stop` 3 → 6, `stage3_force_exit` 3 → 7. Initial-width buckets (from `stop_initial_distance_pct`): a2 = 12% × 586 / 10% × 172 / <5% × 40 (a1 = 12% × 586 / 8% × 139 / <5% × 38) — **the map is "12% almost everywhere"**: Bullish+Recovering fire on ~73% of entries, Neutral ~20%, Deteriorating+Bearish ~5%, so the Neutral slot moves ~1/5 of tickets and the 12% slot is the lever. V6: `3 violations` — BFX/NLS 2020-04-22 (+$746k duplicated), LANC/MZTI 2006-09-15 (−$25k dup), HPT/SVC 2013-05-20 (−$18k dup); net twin excess ≈ **+$703k = 21% of the realised delta** (a0 holds MZTI alone, never the pairs). Force liquidations 3: SGP_old1 2010-09-20 (−$98k, rename-twin copy), AWRE 2014-07-25 (−$80k, the special-distribution case above), and **MEL 2017-02-08 −$172k: a corrupt series** — `data/M/L/MEL/data.csv` runs 2008–2018 at ~$60k–$177k per "share" on 2,353 of 2,427 bars with a handful of $8–12 bars mixed in (2014-07, 2017-02-08 = 12.2, 2018-06); the strategy bought ONE share at $175,002 (a real-sized ticket) and the 12.2 bar gapped it to −99.99%. a0 also traded MEL once (2017-01-30, −$11k `stop_loss` on 02-06) — a store defect in the 2000 vintage, not a mechanism read (issue #2732). Net: same shape as a1 (whipsaws → rotation, maxDD 42.4 → 35.2), same inflation (twins + ~10 tail trades + 2 open MTM names ≈ the whole level).
- 15:45 PT: **a1 (map, Neutral 8%) salt 1 = 768.96% / 816 trades / Sharpe 0.559 / DD 31.10**, V16 PASS / V17 PASS, wall 14,617 s (slowed by lane A + a builder; `results/a1-map-neutral8-s1-*`) vs the record's salt-1 cell (`warehouse-rebuild-2026-09-06/results/rec26y-new-s1-v7-*`: **561.61% / 705 trades** / 0.52 / 45.15 — the handoff's "755" was a typo, the artifact says 705). Join (`symbol|entry_date`): 363 shared trades +$271k (CLS 2023-06-20 +$1.01M again, WSM +$401k, CAR +$192k; MOS 2006-10-30 −$367k — the record's monster is *smaller* with the wider stop, KR −$232k, CMA-WS −$225k); record-only 342 trades +$2.35M lost (AEIS 2025 +$973k, LOGI +$619k, BBWI +$609k, CLB, CHDN, UTHR); a1-only 453 trades +$2.64M (**BFX +$699k and NLS +$696k = one instrument**, KTOS +$563k, BPT +$453k, UPBD +$211k, **BB +$210k and BBRY +$209k = one instrument**, CLFD, THC, KEP; worst GEL −$176k, ING −$125k). Realised $4.97M → $5.53M (**+$561k**); **unrealised $0.87M → $2.34M (+$1.47M) = 72% of the +207pp is mark-to-market, and VIAV 2025-10-02 alone is +$1.57M of it** (the same open name that carries a2-s0; the record s1 holds ARW +$507k instead). V6: **5 violations** (AABA/YHOO, BFX/NLS, AORT/CRY_old, BB/BBRY, AZN/AZN_old) vs the record s1's 2 — net twin excess ≈ **+$890k (BFX/NLS +$697k, BB/BBRY +$209k, the rest ±$20k) = 159% of the realised delta: ex-twins the map is realised-NEGATIVE vs the record at salt 1 too** (≈ −$330k), the same sign as the salt-0 ex-movers read. Exit mix: `stop_loss` 455 → 384, `laggard_rotation` 239 → 409, `extension_stop` 2 → 7, `liquidity_exit` 1 → 5. Width buckets: 12% × 626 / 8% × 131 / 10% × 15 / <5% × 44. Force liquidations 5: SGP_old1 2010 (−$106k, rename-twin copy), FOSL 2012 (−$86k), AWRE 2014-07-25 (−$76k, distribution), BPL 2014-09-25 (−$73k, genuine: `close/adjusted_close` constant 1.674 through the 25.18 → 18.25 eight-day slide), **MEL 2017-02-08 −$343k (TWO "shares" at $171,666; #2732)** — the corrupt series costs this arm −$343k outright. **Salt-1 verdict on the map = same anatomy as salt 0:** the level is MTM on one open name plus twin double-counting; the realised edge is absent-to-negative once twins are removed; what is salt-robust is the exit-mix shift and the **maxDD improvement (45.1 → 31.1 at s1; 42.4 → 37.9 / 35.2 at s0)** — exactly the shape clock-52 had (`project_clock52_promoted`: the maxDD win was the one salt-robust property). a0-s1 (the build-inertness control vs 561.61 / 705) started 15:45 in lane B; a1-s2 running in lane A since 14:35.
