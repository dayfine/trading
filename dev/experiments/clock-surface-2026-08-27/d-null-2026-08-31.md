# Cell-D null (#2600): the −39.1pp was the FLOOR, not noise

Closes the one residual qc-behavioral recorded on PR #2587 (review
5063323764): "no cell-D null was run, so the −39.1pp return figure is
uninterpretable in magnitude." It is now interpreted, and the answer is
adverse. clockD-{0,52} (top-1000 PIT-2000, 2000-01-01→2026-06-26, clockA
config) at salts {1,2}, one build (`5dc61da07`, same as every #2587 arm),
`run-d-null.sh`, artifacts in `results/goldens/clockD-*`.

## The paired table (salt 0 from the #2587 rework, committed then)

| salt | D-0 (null) ret% / sharpe / maxDD | D-52 ret% / sharpe / maxDD | Δ ret | Δ sharpe | Δ maxDD |
|---|---|---|---:|---:|---:|
| 0 | 410.60 / 0.4759 / 30.13 | 371.50 / 0.4958 / 23.29 | −39.1 | +0.020 | −6.84 |
| 1 | 609.39 / 0.5594 / 32.83 | 214.34 / 0.3887 / 26.06 | **−395.0** | −0.171 | −6.77 |
| 2 | 503.86 / 0.5234 / 33.12 | 193.46 / 0.3651 / 25.15 | **−310.4** | −0.158 | −7.97 |

## Reading

- **The return loss at top-1000 is real, consistent-sign, and large.** All
  three paired deltas negative; salt 0 — the one cell the promotion
  discussion saw — was the SMALLEST by an order of magnitude. Contrast
  cell B, where pairing collapsed a 15pp arm-level swing into a stable
  +7pp effect: here pairing does not rescue the value, it convicts it.
- **The s0 "sharpe win" was a salt artifact.** 52 loses sharpe at s1/s2 by
  ~0.16-0.17 — far outside anything the s0 +0.020 suggested.
- **maxDD is the one composition-robust win**: −6.8/−6.8/−8.0pp here,
  matching the direction in every other grid cell (A/B/C). The clock is a
  genuine drawdown-reducer everywhere tested.
- **Mechanism hypothesis** (consistent with [[project_edge_is_the_fat_tail]]
  and the cell-E lesson that breadth is THE lever): a >52-week resting
  ticket that eventually fills is disproportionately a crash-recovery
  monster. At top-3000 a cancelled ticket's slot refills from a deep
  candidate pool, so the clock's freed-capital reshuffle nets positive; at
  top-1000 the pool is shallow — the cancel forfeits the monster and the
  replacement is worse. The clock taxes the tail exactly where the tail
  has no substitutes. (Hypothesis — the trade-level join across the six D
  arms would confirm; not run tonight.)

## Verdict implication — decision needed (ASKABLE, user)

Under `promotion-confirmation.md`'s own rule ("PROMOTE only if … never
badly dominated in ANY cell"), cell D with its null now shows 52 **badly
dominated on return** (up to −395pp) and losing sharpe 2/3 at one broad
breadth tier. Had this table existed on 2026-08-28, value-52 would not
have cleared the grid; ACCEPT(mechanism) stands, but the promoted VALUE is
composition-sensitive: robust risk reduction, breadth-dependent return
cost.

Mitigating context, why this is not a #2384-style emergency: the #2587
paired-golden table proved the flip's default-path blast radius is EMPTY —
`sim_entry_trigger_at_suggested` defaults false, so no default-path
golden, no live behavior, and no CI surface changes with the clock's
value. The default only binds behind trigger-at-E arming
(record-convention experiment lineage). Nothing on main is currently
mispriced by keeping 52; every future trigger-at-E experiment, however,
inherits a knob value that loses 300pp+ of return at top-1000.

Options for the user:
1. **Revert the default to 0** (grid discipline: no robust value → no
   promotion). One-line config PR; blast radius empty by the same
   argument.
2. **Keep 52** (canonical top-3000 basis wins on all metrics at A; accept
   the documented breadth sensitivity; record-convention specs pin the
   knob explicitly anyway).
3. **Keep 52 + require record-convention specs to pin the knob
   explicitly** (make the sensitivity un-inheritable where it bites).

The ledger entry should be amended with this table either way.
