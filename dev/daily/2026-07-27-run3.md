# Status — 2026-07-27 [run 3]

Run timestamp: 2026-07-27T11:28:59Z– (GHA orchestrator run 30262098532)
Run ID: 2026-07-27-run-3

**Mode:** FULL PASS — **main was RED on arrival and is green now. 4 PRs merged, 1 held at the
rework cap. 18 subagent sessions, 12 QC verdicts, 2 rework loops. A four-run mystery got its
controlled experiment, and a lost orchestrator run's orphaned work was adopted and finished.**

## Headline

- **Main was red when I started, for exactly the failure run 2 held a fix for.** CI run
  30261052917 on `50112fb9` failed `dune runtest` with four `Error:` lines, all
  `Tuner.Bayesian_opt:11`/`:17`. PR #2113 — QC-APPROVED on both gates in run 2 but held because
  GitHub had never created a check-suite for its branch — **was the fix**. The hold resolved on
  its own: the branch's merge commit `e04332d1` triggered both required workflows normally.
  Merged as `757abdf2`; **main CI is green again.** Run 2 was right to hold, and right that
  holding cost little.
- **The OpenBLAS story now has a controlled experiment, not an inference.** Run 2 named the
  missing evidence precisely: *"no green run recorded its CPU."* This run supplies it. I ran
  `dune runtest --force` on **the same commit `50112fb9`, in the same container image**, on this
  runner — an **AMD EPYC 9V74** — and got **exit 0 across the full suite**, while GHA CI on that
  identical commit went **red**. Same code, same image, different CPU, opposite result. That
  eliminates "code regression" and "cache artifact" as explanations and confirms the run-2 root
  cause. **Red: `Intel Xeon 6973P-C` (3 occurrences). Green: `AMD EPYC 9V74` (first green run
  with a recorded CPU).**
- **An entire orchestrator run was lost, and I found its orphan.** Run `30250031315` (08:28Z,
  between run 2 and me) dispatched a `feat-weinstein` agent for the `record_fill` CLI, the agent
  pushed **the plan and nothing else**, and the run ended at 23 minutes without writing a daily
  summary. Nothing recorded that it happened; the only trace was a plan-only draft PR #2117 whose
  body credited a run ID that appears in no summary. I adopted it and had it implemented rather
  than re-planning from scratch.
- **The two-gate QC design earned its keep twice on #2123, and then the rework cap earned its
  keep too.** qc-structural independently re-enumerated every `dev/audit/` consumer and correctly
  confirmed the author's suffix-glob claim — then qc-behavioral found a *different* regression the
  glob analysis cannot see: `consecutive_rework_count`'s **ordering** was wrong, reproduced in
  **both directions** on scratch repos. The rework fixed that with a genuinely better design than
  the one I suggested — and **introduced a worse defect than the one it fixed**: under
  `set -euo pipefail`, `grep` exiting 1 on a legacy record kills the script, so the intended
  default-to-zero is unreachable dead code. **0 of 76 committed records carry the new field**, and
  the crash fires *only* on the `NEEDS_REWORK` path — silently destroying exactly the records that
  feed the `>= 3` escalation signal the fix existed to protect. Two iterations, two new defects:
  I stopped at the cap rather than pushing a third under time pressure. **#2123 is not merged, so
  main is untouched.**
- **A QC agent ran `pkill -f 'dune'` and disrupted three sibling agents**, then said so
  unprompted in its return value. The self-report is why I could attribute the fallout correctly
  instead of treating three stalled writers as three separate bugs.

## Step 1b drift cross-reference

Run 2 filed a `[medium]` that `ls -t dev/daily/*.md | head -1` is meaningless in a fresh CI
checkout (every file carries the same mtime) and gave the one-line fix. **I used the fix**
(`git log -1 --format=%cI -- <file>`, sorted) and it correctly returned `2026-07-27-run2.md`.
First run in which that trap did not have to be discovered mid-flight.

Run 2's `Pending work` rows, re-verified against live GitHub state:

| run-2 row | run-2 state | verified now | classification |
|---|---|---|---|
| cleanup #2112 | auto-merged | merged `19b8c8a2` | confirmed |
| simulation #2115 | auto-merged | merged `5ff998d9` | confirmed |
| weekly-snapshot #2114 | auto-merged | merged `9e9f0ad5` | confirmed |
| backtest-infra #2113 | **blocked (CI)** | **CI ran on the merge commit; merged `757abdf2` this run** | resolved |
| harness | blocked, no dispatchable T1/T3 | **2 of the 4 new items were dispatchable** (see below) | corrected |
| cleanup backlog | 8 open, top = 6th citation defect | dispatched → #2121 merged `6676ff8e` | picked up |

**One correction to run 2, not a drift warning.** Run 2 recorded harness as "blocked — 4 new
items filed, 3 of them need a `workflow`-scoped PAT". Re-reading them, **H-AUDIT-COLLISION
needs no PAT at all** (it is a plain shell script under `trading/devtools/checks/`), and it
described *live, recurring data loss*. It was dispatched this run and is #2123. H-QC-SCALE is
likewise PAT-free. Only H-BLAS and H-CI-DISPATCH are genuinely PAT-blocked.

**No `[drift]` warnings.** Fifth clean cross-reference in a row.

## Step 1c carried-`[critical]` verification

Run 2 carried **no `[critical]`**. Re-verified the standing items:

1. **LAPACKE / `Tuner.Bayesian_opt`** → **fixed and merged.** Was red on main at checkout;
   `757abdf2` closes it. Root cause confirmed by controlled experiment (§Health Scan).
2. **`[high]` #2113 cannot merge — GitHub created no check-suite** → **resolved.** The merge
   commit triggered both workflows. Root cause never diagnosed; the watchdog filed in
   `harness.md` (H-CI-DISPATCH) is still worth building.
3. **`[high]` auto-merge is neither sufficient nor safe** → carried, still real, **no victim
   this run**: every PR had `auto_merge = None` and I merged each explicitly after re-verifying
   all three gates in the same step.
4. **`[medium]` QC agents cannot write `dev/reviews/`** → mitigated again by my writing them.
5. **`[medium]` `record_qc_audit.sh` filename collision** → **being fixed this run** (#2123).
   I worked around it meanwhile by writing run-suffixed feature names.
6. **`[medium]` Step 1b `ls -t` ordering** → **fix applied** (above).

**No still-real `[critical]` items.** Main is green on `757abdf2` and again after `6676ff8e`.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-infra | **auto-merged** | fix/lapacke-gp-cholesky | #2113 | **MERGED `757abdf2`** — un-redded main; 5-run saga closed |
| (deps) | **auto-merged** | deps/opam-weekly | #2118 | **MERGED `950462e0`** — weekly opam bump |
| cleanup | **auto-merged** | cleanup/stop-types-citation | #2121 | **MERGED `6676ff8e`** — 6th citation defect; 2 more candidates found |
| weekly-snapshot | **auto-merged** | feat/record-fill-cli | #2117 | **MERGED `72e593e2`** — orphan adopted, implemented, reworked; both gates APPROVED |
| harness | **rework cap hit (2/2)** | harness/audit-filename-collision | #2123 | rework 1 fixed the ordering bug but **introduced a worse one**; NOT merged, escalated for next run |
| ops-data | not dispatched | — | — | `EODHD_API_KEY` absent; `data-gaps.md` unchanged since #814 (2026-05-03) |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-infra | — | **merged** | #2113. No re-QC needed: code byte-identical through two merges; only `dev/status/cleanup.md` differed and its resolution was verified correct |
| weekly-snapshot | feat-weinstein | completed | #2117. **Adopted the orphaned plan** from lost run 30250031315; 10 mutations, and caught 3 of its own as false-reds |
| weekly-snapshot | qc-structural | **NEEDS_REWORK** 3/5 | verified all 6 declared plan deviations sound; one P6 Result-discard |
| weekly-snapshot | qc-behavioral | **NEEDS_REWORK** 3/5 | cash conservation + `>=` boundary verified genuinely pinned; **3 mutations survived** |
| weekly-snapshot | feat-weinstein | rework 1/2 **completed** | closed all 5 findings with binding-live mutations; **no implementation logic changed** (lib files byte-identical) |
| weekly-snapshot | qc-structural | **APPROVED** 4/5 | delta `25e1965b..a007d550`; P6 CLOSED; new tests themselves pattern-clean |
| weekly-snapshot | qc-behavioral | **APPROVED** 4/5 | delta re-review; **re-ran 3 of 5 mutations itself**; F2.1–F2.4 all CLOSED |
| weekly-snapshot | — | **merged** | #2117 → `72e593e2`; I resolved a `cleanup.md` conflict by hand and verified all 7 code files byte-identical to the QC'd tip |
| cleanup | code-health | completed | #2121. Verified §5.2 from primary source; ruled out §5.1 and §5.3 **by name** |
| cleanup | qc-structural | **APPROVED** 5/5 | behaviour-free confirmed; REST-derived file list quoted per the new A3 rule |
| cleanup | qc-behavioral | **APPROVED** 4/5 | re-derived independently; **partially refuted** the author's exhaustiveness claim; found 2 more candidates |
| cleanup | — | **merged** | #2121 → `6676ff8e`; `stop_types.mli` verified byte-identical through the branch update |
| harness | harness-maintainer | completed | #2123. Chose branch-in-the-middle so existing suffix globs keep working; verified consumers |
| harness | qc-structural | **APPROVED** 4/5 | independently re-enumerated both consumers; confirmed the glob claim |
| harness | qc-behavioral | **NEEDS_REWORK** 2/5 | **found the ordering regression structural could not see**; reproduced both directions |
| harness | harness-maintainer | rework 1/2 **completed** | F1/F2/F3 addressed; **rejected the suggested `ls -1t` fix for a better one** |
| harness | qc-behavioral | **NEEDS_REWORK** 2/5 | delta re-review; F2+F3 CLOSED, **F1 OPEN — the fix introduced a crash on legacy records** |
| harness | — | **cap hit, escalated** | rework 2/2 not dispatched; PR stays open, unmerged. Main unaffected |
| (deps) | qc-structural | **APPROVED** 5/5 | #2118 |
| (deps) | qc-behavioral | **APPROVED** 5/5 | #2118. Established the file is a **drift tripwire, not a lockfile** — correcting 2 prior reviews |
| (deps) | — | **merged** | #2118 → `950462e0` after branch update + three-gate re-verify |
| ops-data | ops-data | skipped | `EODHD_API_KEY` re-verified **absent**; `data-gaps.md` unchanged since 2026-05-03 — sentinel unchanged |

## Feature Progress

### backtest-infra — #2113, the five-run saga, closed

Run 2 did the hard work: root-caused the failure to a distro OpenBLAS that mis-detects the
runner CPU as `Cooperlake` and dispatches wrong AVX-512 kernels for n>=33, and — critically —
**rejected the cheap fix** (switching `uplo`) that would have turned both failing tests green
while silently corrupting every GP posterior. It then correctly refused to merge because
GitHub's gate 1 was unsatisfiable.

My job was narrower and I kept it that way: confirm the gates still hold, and merge.

- **The CI hold self-resolved.** The `e04332d1` merge commit triggered both required workflows.
  Whatever suppressed check-suite creation across three SHAs, a PR open, a close/reopen and a
  PAT push, a merge commit was not subject to it. Root cause unknown; H-CI-DISPATCH stands.
- **The verdicts carried, and I checked rather than assumed.** All five reviewed code files are
  **byte-identical** between the reviewed tip `569790e9` and the merged tip. Only
  `dev/status/cleanup.md` differed, because #2112 had landed an entry in it — and the PR's *net*
  contribution to that file is exactly the same semantic edit as reviewed, now layered on
  #2112's. Same rigor run 2 applied to #2114.
- **The CI green is non-vacuous.** The job log shows `./test_bayesian_opt.exe` and
  `./test_bayesian_opt_cholesky.exe` actually executing, zero `Error:` lines. This mattered:
  the *other* green build-and-test I looked at this run (PR #2120's, which merged 13 minutes
  before main went red) contains **zero** `Tuner.Bayesian_opt` lines at all — that target was
  served from cache and never ran. **A green PR check is not evidence the suite ran.**

### weekly-snapshot — #2117, an orphan adopted

The lost run's agent left a 195-line plan and no code. Rather than re-plan, I briefed a fresh
agent to treat the existing plan as the contract, and to update it rather than deviate silently.
It recorded six deviations in a new §6 — and both reviewers verified all six as sound, including
the one that matters most for code health: the nesting linter failed three functions on the
first draft, and it fixed them **by extracting `_check_*` helpers, not by adding a marker or
bumping a limit**.

Two things stand out in the review exchange:

**The author audited its own evidence.** It reported 10 mutations, 10 red — then noticed three
were red only because the mutation left a binding unused and the *build* failed, re-ran those
three with binding-preserving mutations, and reported the correction. qc-behavioral spot-checked
all three and confirmed they hold.

**qc-behavioral then found what the author's own mutation testing missed.** Three mutations
**survived** 20/20 green — three `.mli`-documented guards with no fence. The serious one:
`--trim-shares -10 --trim-price 200` would **mint 10 shares and move 2,000 of cash**. The
reviewer named it as the **#2059 phantom-position class** — the bug that cost this project five
runs to find — appearing in the one operation this feature exists to make safe. The
implementations are correct; the fences are missing. That is a NEEDS_REWORK worth having.

### cleanup — #2121, the sixth citation defect

Fixed to the family's established bar: §5.2 verified from the primary source, with §5.1
(">15% risk", an entry-risk cap) and §5.3 ("4-6%", the *trader*-method stop size) ruled out by
name as different quantities.

qc-behavioral re-derived the citation independently rather than auditing the author's chain,
enumerating **all 13** `%`-bearing lines plus spelled-out forms. Two results worth keeping:

- It checked the **second** site separately, because `support_floor_lookback_bars` documents a
  *lookback window*, not a depth — and confirmed the citation attaches narrowly to the shared
  depth threshold, with the sharing **real in code** (`weinstein_stops.ml:75`/`:82` pass both
  into the same `Support_floor` call), not merely asserted in prose.
- It **partially refuted** the author's "no other percentage-qualified pullback language"
  claim: two lines were missed (volume contracting 75%+, ~50% of breakdowns pulling back).
  Neither is a *depth*, so the conclusion stands — but this matters, because the family's
  planned next step is a linter, and **a naive `%`-adjacency heuristic will hit both**. The
  linter needs a depth-vs-quantity discriminator.

It also found **two more candidates** (`stop_types.mli:36` and `:114` cite a *named* section,
not a `§N.N`), correctly out of scope here, and correctly noted the planned linter must first
decide whether a named `§` counts.

## QC Status

- backtest-infra / **#2113: APPROVED** — structural 4/5 + behavioral 5/5 @ `569790e9` (run 2),
  carried to the merged tip on verified byte-identity. Audit
  `dev/audit/2026-07-27-backtest-infra-run3.json`.
- deps / **#2118: APPROVED** — structural 5/5 + behavioral 5/5 @ `a5c66128`, 0 rework.
  Audit `dev/audit/2026-07-27-deps-opam-weekly-2118.json`. Record:
  `dev/reviews/deps-opam-weekly-2118.md`.
- cleanup / **#2121: APPROVED** — structural 5/5 + behavioral 4/5 @ `1fbb08dd`, 0 rework.
  Audit `dev/audit/2026-07-27-cleanup-run3.json`. Record: `dev/reviews/cleanup.md`.
- weekly-snapshot / **#2117: NEEDS_REWORK** — structural 3/5 + behavioral 3/5 @ `25e1965b`;
  rework 1/2 dispatched. Record: `dev/reviews/weekly-snapshot.md`.
- harness / **#2123: NEEDS_REWORK (behavioral)** — structural 4/5 APPROVED, behavioral 2/5 @
  `fd41ed6a`; rework 1/2 dispatched. Record: `dev/reviews/harness.md`.
- **Declared deviations:** (a) qc-structural and qc-behavioral ran **concurrently** rather than
  gated (Step 5 Stage 2) — it paid off again: behavioral produced the decisive finding on three
  of four PRs. (b) Writers ran in parallel in per-agent `git worktree`s.
- All `dev/reviews/*.md` written by me; QC agents still cannot write them.

## Budget

- Budget cap: $200.00 (`merge-policy.json`; Max-20x flat rate — a runaway backstop, not a real budget)
- Target utilization: 60%–95%
- Subagent sessions: **18** (7 writer sessions incl. 3 resumes, 11 QC sessions incl. 3 delta
  re-reviews)
- Measured subagent tokens: **2,171,000**

  | Agent | Role | Outcome | Subagent tokens |
  |---|---|---|---|
  | feat-weinstein | #2117 record_fill CLI | completed | 178,630 |
  | code-health | #2121 citation (2 sessions) | completed | 210,255 |
  | harness-maintainer | #2123 audit filename (2 sessions) | completed | 313,119 |
  | qc-structural | #2118 | APPROVED 5/5 | 65,478 |
  | qc-behavioral | #2118 | APPROVED 5/5 | 125,581 |
  | qc-structural | #2121 | APPROVED 5/5 | 65,404 |
  | qc-behavioral | #2121 | APPROVED 4/5 | 90,859 |
  | qc-structural | #2117 | NEEDS_REWORK 3/5 | 87,816 |
  | qc-behavioral | #2117 | NEEDS_REWORK 3/5 | 123,369 |
  | qc-structural | #2123 | APPROVED 4/5 | 82,026 |
  | qc-behavioral | #2123 | NEEDS_REWORK 2/5 | 114,942 |
| harness-maintainer | #2123 rework 1 (2 sessions) | completed | 334,524 |
| qc-behavioral | #2123 delta re-review | NEEDS_REWORK 2/5 | 94,406 |
| feat-weinstein | #2117 rework 1 | completed | 110,239 |
| qc-structural | #2117 delta re-review | APPROVED 4/5 | 77,774 |
| qc-behavioral | #2117 delta re-review | APPROVED 4/5 | 97,893 |

- Any subagent killed mid-flight: **No** — but three were **disrupted** by a sibling's
  `pkill -f dune` (see §Escalations), costing two of them a full cold rebuild.
- Total (estimated, Sonnet pricing per `model_prices`): **~$25–35** / $200 (~13–18%)
- Utilization assessment: **UNDER_TARGET on cost; not under-worked.** Every eligible track was
  dispatched, three PRs merged, two full rework loops opened. **Nothing was skipped for budget
  or queue-depth reasons.**
- **What actually capped throughput:** CI latency and base churn, unchanged from runs 1 and 2.
  Every merge this run required a branch update because a sibling landed underneath it, and each
  update costs a fresh ~12–18 min `build-and-test`. Four such cycles. Writer parallelism is not
  the constraint.
- Scope reduced due to budget: **No.**

## Health Scan

Full report: `dev/health/2026-07-27-fast-run3.md`. Gates run before any dispatch.

- On main `50112fb9`: `dune build` **0**, `dune runtest --force` **0**, `status_file_integrity`
  **0**, `index_size_linter` **0**, `no_python_check` **0**, fmt clean, magic numbers clean.
- **GHA CI on that same commit: exit 1**, four `Error:` lines, all `Tuner.Bayesian_opt`.
  That contradiction is the point — see §Headline. CPU recorded per run 2's recommendation:
  **AMD EPYC 9V74**.
- `--force` was used deliberately: run 2 caught itself accepting a cached exit 0 as evidence,
  and I found a second instance of the same trap in the wild this run (#2120's green CI never
  ran the tuner target).
- Step 6.2's documented path in `lead-orchestrator.md` is still wrong (6th run): the correct
  relative path under `run-in-env.sh` is `devtools/checks/…`. Used the corrected form.

## Follow-up Queue

- **41** open `[ ]` items across `dev/status/*.md` — Step 2b threshold (10) exceeded, recorded.
  No maintenance slot substituted: the dispatched work **was** defect closure.
- Largest concentrations: `harness.md` (14), `cleanup.md` (11), `tuning-methods.md` (4),
  `all-eligible.md` (3), `tuning.md` / `trade-audit.md` / `data-foundations.md` (2 each).
- Filed this run: two further citation candidates (`stop_types.mli:36`, `:114`); the
  `%`-adjacency discriminator requirement for the planned citation linter;
  `Live_portfolio.header` schema-drift (`LINTER_CANDIDATE`); the `adjust --stop-price`
  lowered-stop policy question (deferred to 4c.b).

## Integration Queue

- **#2113 — MERGED `757abdf2`.** Three gates re-verified in the same step as the merge; byte-identity checked across two merges. **Un-redded main.**
- **#2118 — MERGED `950462e0`.** Same, after a branch update.
- **#2121 — MERGED `6676ff8e`.** Same, after a branch update; both its own and #2113's `cleanup.md` entries verified present on the merged tip.
- **#2117 — MERGED `72e593e2`.** Both gates APPROVED after rework 1. The branch went
  `mergeable_state: dirty` on a `dev/status/cleanup.md` §Backlog conflict (this PR added an entry
  while #2113 and #2121 flipped others); I resolved it by keeping this PR's new entry and taking
  main's **resolved** `[x]` version of the one it duplicated, then verified all **seven** reviewed
  code files byte-identical to the QC-approved tip before merging.
- **#2123 — OPEN, held at the rework cap (2/2).** Structural APPROVED; behavioral NEEDS_REWORK
  twice. **Not merged, so main is unaffected.** Remaining work is one line plus two hardening
  nits — see §Escalations.

## Current Milestone Target

Anchored to `dev/notes/next-session-priorities-2026-07-27.md`:

- **P0 — Picks Phase C + #2103.** Closed in runs 1–2.
- **Next weekly-snapshot item (4c.a, `record_fill` CLI)** is #2117 — implemented, in rework.
  4c.b (the trailing-stop **state machine**, the one that changes what the report *says*)
  remains the substantive next piece.
- `docs/design/weinstein-trading-system-v2.md` still has **no `Current milestone:` line**, so
  `check_11_linter_expiry.sh` still cannot evaluate any milestone-pinned exception. Oldest open
  cleanup item; needs a human-chosen value. **Seventh run carrying this.**

## Dependency Unlocks

- **#2113 unblocks the tuner suite on affected runners** and removes the repo's only live
  Owl-linalg exposure. The wider OpenBLAS defect is untouched — it is a distro binary — but
  in-repo blast radius is now nil (the `Owl.Linalg.S` aliases in `analysis/technical/trend/lib/`
  are dead: declared, never called).
- **#2123 unblocks trustworthy QC audit history.** Until it lands, every same-day second review
  of a track destroys the first record — I worked around it this run by writing run-suffixed
  feature names, which is a mitigation, not a fix.

## Escalations

- **[rework-cap] harness / #2123 reached 2 rework iterations without converging, and each
  iteration introduced a new defect.** Iteration 0 fixed the filename collision but broke
  `consecutive_rework_count` **ordering**. Iteration 1 fixed the ordering with a genuinely better
  design than the one I suggested — and introduced a **crash on legacy records**: under
  `set -euo pipefail`, `grep` exiting 1 on a record without the new `recorded_at_ns` field kills
  the script, making the intended default-to-zero unreachable dead code. **0 of 76 committed
  records carry the field**, and the crash fires *only* on the `NEEDS_REWORK` path — so it
  silently destroys exactly the records feeding the `>= 3` escalation signal the fix existed to
  protect. **I stopped at the cap rather than pushing a third iteration under time pressure**,
  because "each attempt introduces a new defect in the same file" is precisely the pattern the cap
  exists to catch. **#2123 is not merged; main is unaffected.** The remaining work is small and
  fully diagnosed: a one-line `|| true` (verified working by the reviewer), plus quoting/validating
  `WRITE_AUDIT_RECORDED_AT_NS` (currently `=oops` yields malformed JSON in a committed artefact)
  and replacing GNU-only `date -u +%s%N`. Next run resumes from `dev/reviews/harness.md`.

- **[high] A QC agent ran `pkill -f 'dune'` and disrupted three concurrently-running sibling
  agents.** The reviewer on #2118 used it to reclaim disk during a 3.8 GB cold build. It killed
  its own waiters and the in-flight builds of `ws-audit`, `ws-citation` and `ws-recordfill`.
  **It reported this itself, unprompted**, which is the only reason I could attribute two
  stalled writers correctly instead of chasing three phantom bugs. Cost: two full cold rebuilds.
  **Fix:** the agent definitions and dispatch prompts should forbid broad process kills outright
  (`pkill -f`, `killall`) and require killing by PID. I added that prohibition to every prompt
  after the incident. It belongs in `.claude/rules/worktree-isolation.md` alongside the existing
  isolation contract, because "your worktree is isolated" is exactly the assumption that makes a
  global `pkill` feel safe.

- **[high] Two of three writer agents ended their turn without committing, in the documented
  failure mode.** Both backgrounded their final `dune runtest` and stopped "waiting for the
  notification" — the precise behaviour `.claude/rules/worktree-isolation.md` §"Finish Protocol"
  exists to prevent ("never end a turn with an un-pushed commit… run the final verification in
  the foreground"). Their work was intact on disk and I recovered both by resuming them with the
  rule quoted, but a less careful run would have lost a full agent-session each. **This is the
  second consecutive run in which the Finish Protocol was the operative failure surface** (run 2
  reported zero violations after three in three days, so this is a regression). Both dispatch
  prompts **already contained** the foreground requirement in bold — so restating it is not
  working. Recommend the protocol move from prose into something structural.

- **[medium] QC verdict delivery is drifting off the documented channel, and it broke my
  parser.** `.claude/rules/pr-merge-gates.md` specifies verdicts as **PR review comments**.
  This run: one agent posted an **issue comment** instead (#2117 structural), and without the
  `## Verdict` / `## Quality Score` blocks in parseable form — my REST query over
  `/pulls/N/reviews` found nothing and I had to fall back to `/issues/N/comments` and then to
  the agent's return value. `record_qc_audit.sh --pr-number` reads the review channel, so a
  verdict delivered this way is **invisible to the audit path**. Fix: the QC agent definitions
  should show the exact `POST /pulls/{n}/reviews` curl and state that an issue comment is not a
  valid delivery.

- **[medium] `dev/lib/run-in-env.sh` is a live footgun for worktree dispatch, still undocumented
  in the agent files.** It hardcodes `${GITHUB_WORKSPACE}/trading`, so an agent invoking it from
  `/tmp/ws-*` silently builds a **sibling agent's** checkout. Run 2 found this and put it in its
  summary; it is still not in any agent definition, so I had to hand-write the warning into all
  nine dispatch prompts again. That is the second run of manual mitigation for a one-paragraph
  doc fix.

- **[medium] `.claude/agents/lead-orchestrator.md` is unwritable by agents, and now carries two
  known-stale sections.** The #2123 author tried to update the audit-filename documentation
  (`:1361`, `:1415`) and was blocked by the harness as "sensitive" — correctly, but nothing
  routes the fix anywhere. Separately, **Step 6.2's documented `run-in-env.sh` path has now been
  wrong for six consecutive runs**. Both are orchestrator-owned; I am recording them rather than
  editing the file mid-run so the doc change lands with #2123's behaviour change.

- **[medium] Every merge this run required a branch update, and each costs a full CI cycle.**
  Four PRs, four `update-branch` calls, four ~12–18 min `build-and-test` runs — because siblings
  keep landing underneath each other. This is now the dominant throughput cost (writers are not
  the constraint). Worth considering whether the required-checks `strict: true` setting needs to
  be `strict` for docs-only and status-only diffs.

- **[info] The green-CI-can-be-vacuous trap has a second confirmed instance.** PR #2120's
  `build-and-test` was green with **zero** `Tuner.Bayesian_opt` lines in the log — cached target,
  never ran. It merged; the same content on main then ran the suite cold and went red 13 minutes
  later. Whenever a CI green is load-bearing for a merge decision, the log should be checked for
  the specific target actually executing. I did this for #2113.

- **[info] `GH_TOKEN` still lacks `issues: write`** (carried, 8th run). Cost this run: still
  cannot close #2059 (fixed by code in run 2), nor file the OpenBLAS finding as an issue. Also
  lacks `administration: read`, so branch protection is unreadable — which the standing
  auto-merge question turns on.

- **[info] A writer briefly edited the shared parent checkout instead of its worktree**, caught
  it itself, reverted with `git checkout --` before staging anything, verified clean, and redid
  the work correctly. No contamination reached the shared tree. Recorded because it is the second
  worktree-discipline near-miss in two runs, and both were self-caught.

## Questions for You

1. **The Finish Protocol is not holding, and prose is not fixing it.** Two of three writers
   stopped before committing this run despite the requirement being in bold in their briefs.
   Options I can see: (a) a `Stop` hook that refuses to end an agent turn with un-pushed commits
   on a `feat/`/`harness/`/`cleanup/` branch; (b) the orchestrator polling worktrees and
   auto-resuming, which is what I did by hand today and which worked reliably. I can build (b)
   without new permissions. Want me to?

2. **May I have `issues: write`?** Eighth run asking. #2059 is fixed by code and still open;
   the OpenBLAS finding has no issue anyone will see.

3. **The OpenBLAS guard still needs a `workflow`-scoped PAT.** The controlled experiment this
   run removes the last doubt about the diagnosis. Two lines (`OPENBLAS_CORETYPE` + a numerical
   smoke check) would end the whole class. It blocks H-BLAS, H-CI-DISPATCH and #1636.

4. **Auto-merge: four runs now with no victim.** Every PR had `auto_merge = None` and I merged
   each explicitly after re-verifying all three gates in the same step — which again handled
   four branch-staleness cases cleanly. I suggest we settle this as policy and I stop carrying
   the `[high]`. Say the word.

## Per-run links

- [run 1] dev/daily/2026-07-27.md
- [run 2] dev/daily/2026-07-27-run2.md
- [run 3] dev/daily/2026-07-27-run3.md
- [lost] orchestrator run 30250031315 (08:28Z) produced no summary; its orphaned work is #2117

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
