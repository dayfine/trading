# Status — 2026-07-11 [run 4]
Run timestamp: 2026-07-11T19:45:00Z
Run ID: 2026-07-11-run-4

**Mode:** RECONCILE + HEALTH (full pass — Step 0.5 fast-exit did NOT fire:
Condition 2 fails because **#1926 merged since run-3 and touched
`dev/status/floor-quality.md`**, a real track-state change to reconcile). No
orchestrator dispatch / QC / merge: every queued feature track (S1-S7) is
deep-warehouse EODHD-data-gated (`EODHD_API_KEY` absent), the harness backlog is
all milestone/human-gated, cleanup is drained, and there are 0 open PRs.

## Headline

- **Main HEAD `9aec60a6` → `e086ec5f`.** The only track-state inflow since run-3
  is **#1926 `feat/realism-defaults-flip` MERGED** (S0: USER-MANDATED realism
  basis-change — `min_entry_dollar_adv` 0→$1M and `stale_exit_after_days`
  None→5, both default-on). This is exactly what run-3 predicted: the draft it
  flagged DO-NOT-MERGE was cleared + merged by the maintainer LOCAL after its
  re-measures checklist emptied. It touched config, goldens, the ledger, and
  `dev/status/floor-quality.md` (realism flip now recorded under Completed).
- **0 open PRs.** The draft is gone (merged); nothing else is in flight.
- **Main GREEN**: `build-and-test`, `perf-tier1-smoke`, `golden-custom-universe`
  `success` on `e086ec5f`; `golden-sp500-5y` in_progress (long golden). Required
  merge-gate checks (`build-and-test` + `perf-tier1-smoke`) both green.
- **Correction to the last ~18 runs: `dune` IS available in the GHA runner** via
  `eval $(opam env)` (dune 3.24.0). Prior runs wrongly reported "dune absent"
  and skipped the Step 6.1 build gate. **This run RAN the full gate locally:
  `dune build` exit 0 AND `dune runtest` exit 0** (full OUnit suite + all
  dune-wired linters). Main-baseline health is now verified directly, not just
  inferred from CI.

The remaining structural throttle is narrower than run-3 stated: it is **data +
QC-env**, not build. The feature/experiment queue (S1-S7) needs the broad-universe
EODHD warehouse (`EODHD_API_KEY` absent), and a GHA-authored PR still cannot pass
the 3-gate merge (qc-structural/qc-behavioral need the docker `trading-1-dev`
worktree-isolation env, absent here). But the runner CAN now build and test —
worth noting for any future non-data harness/cleanup dispatch.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| S0 — realism-defaults flip | **DONE (merged #1926)** | — | — | landed as the new measurement basis; floor-quality comparators stay TOTAL-RETURN |
| S1 — AXTI exit verification | blocked (deep-warehouse / maintainer LOCAL) | — | — | rebuild warehouse to 2026-06-26 (~31 min), re-run honest-tradeable end 2026-06-26, observe trailing-stop-exit vs still-holding |
| S2 — extension-episode event screen | blocked (deep-warehouse / maintainer LOCAL) | — | — | paired high-trail exit screen (10/15/20/25%) on close/MA≥2.5-3× episodes; per `mechanism-validation-rigor` (distributions, not means) |
| S3 — trader-preset bundle test | blocked (deep-warehouse / maintainer LOCAL) | — | — | build missing default-off dials → deep WF-CV trader bundle vs investor baseline (W3) |
| S5 — floor-quality P1b step-3 lens screen | blocked (deep-warehouse / maintainer LOCAL) | — | — | sleeve lens vs TR-SPY 2000-2026: per-crash-episode table, interventions, whipsaw dist |
| S6 — grind-weeks exposure PR | blocked (LOCAL QC/merge) | — | — | `weeks_below_ma_slow_grind` as strategy config (default 8 = no-op), deep surface {2,4,6,8} |
| S7 — faithful per-week universes (M6.6) | blocked (design + human-gated) | — | — | universe staleness is the #1 capture loss; design snapshots-per-screen-date + runner seam, then cost — may need own plan doc |
| short-side-strategy | blocked (LOCAL/data-gated) | — | — | short-leg regime-P&L decomposition (Thread C) |
| weekly-snapshot / decision-audit (M6.6) | blocked (human-gated) | — | — | live weekly cycle: DATA_SOURCE + cron + alerts (human decision) |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | awaiting #1327 enable-commit (carried ≥30 runs) |
| harness (ci.yml ENOSPC fix) | blocked (human: workflow-PAT) | — | — | `[~]` in harness.md; exact YAML in commit `4cc7d08` / PR #1636 |

(S4 hold-exit promotion remains CLOSED — #1925 4y-fold sensitivity showed the
dominance was a fold-horizon artifact.)

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| (all feat tracks S1-S7) | — | skipped | deep-warehouse EODHD-data-gated (`EODHD_API_KEY` absent → cannot build a broad-universe warehouse) + LOCAL-only 3-gate QC/merge (docker `trading-1-dev` worktree-isolation env absent). `dune` is present but data + QC-env are not. Not dispatchable in GHA. |
| harness | — | skipped | no dispatchable `harness/*` item: T1 all `[x]`; open `[ ]` = T2-B/C/D (milestone-gated), T3-C (superseded), T3-H (low-pri T1-N-gated), T4 (human/automation-only); lone `[~]` (ci.yml ENOSPC #1636) human-PAT-blocked (needs `workflow` scope). harness.md unchanged since run-3. |
| code-health | — | skipped | `cleanup.md` §Backlog drained (only the template placeholder line, 0 real `[ ]`); latest deep scan 2026-07-06 had 0 actionable `[medium]`/`[high]`; unchanged since run-3; next weekly deep ~2026-07-13. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `data-gaps.md` sentinel unchanged (Last updated 2026-05-03, #814). Remaining gaps need human input. |
| _index.md | — | reconciled | advanced Last-updated header to run-4, HEAD `e086ec5f`; recorded #1926 merged (S0 done) + the dune-availability correction. All per-track rows verified current (only #1926 landed since run-3; floor-quality row's next-task still accurate = S5 step-3 lens). status_file_integrity exit 0; index_size_linter exit 0 (8862/20480 bytes). |
| (QC) | — | not run | 0 open PRs (draft #1926 merged); no READY_FOR_REVIEW branch. QC also needs the LOCAL docker `trading-1-dev` + jj `isolation:worktree` env, absent in GHA. |
| (merge gate) | — | not run | 0 open PRs, no APPROVED track → nothing to auto-merge (Step 6.5). |

## Feature Progress

### S0 — realism-defaults flip (floor-quality track) — MERGED
- Done: #1926 landed the realism basis-change (entry-gate $1M ADV + stale-exit
  5d default-on). Recorded in `dev/status/floor-quality.md` Completed. Goldens
  re-pinned; ledger `2026-07-10-realism-defaults-flip`.
- Next (LOCAL): floor-quality P1b step-3 lens screen vs TR-SPY (S5).

### (no other orchestrator-runnable feature code in GHA)
- S1-S7 remain deep-warehouse + long-run + LOCAL-only (data + QC-env gated).

## QC Status
- (none this run — 0 open PRs; QC agents require the LOCAL docker `trading-1-dev`
  + jj `isolation:worktree` env per `qc-{structural,behavioral}-authority.md`,
  absent in the GHA runner.)

## Budget
- Budget cap: $200 (runaway backstop, from `merge-policy.json`; Max-20x
  subscription — $ figures are API-equivalent estimates, not billed).
- Target utilization: 60–95%.
- Subagents spawned: **0** (no dispatchable feature/harness/ops/cleanup work; 0
  open PRs → no QC/merge).
- Per-subagent breakdown: (none)
- Any subagent killed mid-flight: No.
- Total (estimated): ~$0.6 (orchestrator state-read + open-PR/main-CI inspection
  + compare-since-run-3 + **local `dune build` + `dune runtest` gate** +
  status-integrity + index-size linter + `_index.md` reconcile + health report +
  summary) / $200 (~0.3%). Measured cost:
  `dev/budget/2026-07-11-<run>.json` written by the GHA post-run step after this
  session exits (not present now).
- Utilization assessment: **UNDER_TARGET** — reason: **all-work-gated /
  covered-LOCALLY**. No track skipped for budget or queue-depth (Step 3.75b: a
  track is either eligible or not; none is GHA-eligible — data-gated /
  LOCAL-only-QC / human-gated).
- Scope reduced due to budget: No.

## Data Operations
- ops-data skipped — `EODHD_API_KEY` absent in GHA; `data-gaps.md` unchanged
  since #814 (2026-05-03). Remaining ADL / sector / historical-survivorship gaps
  require human input.

## Harness Work
- None dispatched. T1 all `[x]`; open `[ ]` = T2 (milestone-gated), T3-C
  (superseded), T3-H (low-pri T1-N-gated), T4 (human/automation-only). Lone `[~]`
  = ci.yml ENOSPC fix (#1636, correct diff in commit `4cc7d08`), human-PAT-blocked
  (needs `workflow`-scoped token).

## Health Scan
- Result: **CLEAN** (report: `dev/health/2026-07-11-fast-run4.md`).
- Main build: **PASSING** — `dune build` exit 0 (local, this run).
- Main test+lint suite: **PASSING** — `dune runtest` exit 0 (local, this run;
  full OUnit suite + `fmt_check` + `linter_magic_numbers` + `fn_length` +
  `nesting` + `mli_coverage` + `status_file_integrity`, zero `^FAIL:` lines).
  The `find: ... No such file or directory` lines are the known benign
  sandbox-race artifact (RUNTEST_EXIT=0).
- GitHub CI on HEAD `e086ec5f`: `build-and-test`, `perf-tier1-smoke`,
  `golden-custom-universe` all `success`; `golden-sp500-5y` in_progress;
  `orchestrator` in_progress (this run).
- Status file integrity: **PASSING** (`status_file_integrity.sh` exit 0).
- Index size: **PASSING** (`index_size_linter.sh` exit 0 — 8862/20480 bytes).
- Weekly deep scan: not due — last `dev/health/2026-07-06-deep.md`; next ~2026-07-13.

## Follow-up Queue
- Code-health §Backlog drained (only the template placeholder, 0 real `[ ]`).
  Total actionable followups below threshold (10); no maintenance pass triggered
  (Step 2b).

## Integration Queue
- Empty — 0 open PRs. #1926 (S0 realism flip) merged this window; nothing else
  in flight. Nothing awaiting review or GHA-mergeable.

## Current Milestone Target
- M6.6 — live weekly cycle — blocked on a human decision (live `DATA_SOURCE` +
  cron + alerts). Backtest/experiment tracks data-gated on the broad-universe
  EODHD warehouse. Active LOCAL frontier: realism flip DONE (#1926, S0) → AXTI
  exit verify (S1) → extension-episode screen (S2); floor-quality P1b step-3 lens
  screen (S5). S7 (faithful per-week universes) is the #1 capture-loss fix — may
  deserve its own plan doc.

## Dependency Unlocks
- **#1926 (S0 realism flip) merged** — the realism basis-change is now the
  default measurement convention for all downstream floor-quality / experiment
  comparators. No track was *blocked* on it (it was a basis change, not a
  primitive), but every future backtest now runs on the honest-tradeable basis.

## Step 1b drift cross-reference
- Prior summary = run-3 (`2026-07-11-run3.md`). Its `## Pending work` rows were
  all `blocked` / `in-flight (maintainer LOCAL)` / draft — none
  orchestrator-`dispatched` — so no `[drift]` warning is possible from that table.
- Inflow to main since run-3: **#1926** (a REAL feature merge — NOT an exempt
  `ops: daily orchestrator summary` commit). It advanced S0 exactly as run-3
  predicted (maintainer LOCAL clears the draft checklist + merges). A predicted,
  legitimate track-state advance is not drift. **No `[drift]` warnings.**
- **Step 1c carried-critical re-verification:** run-3 carried **no** build-red
  `[critical]` (only `[info]`s + the M6.6 `[decision-needed]`). Re-verified main
  unambiguously GREEN — and this run went further than run-3 could: ran the full
  local gate (`dune build` + `dune runtest` both exit 0) in addition to CI
  (`build-and-test` + `perf-tier1-smoke` `success` on `e086ec5f`);
  status_file_integrity exit 0; index_size_linter exit 0. **No still-real
  build-red `[critical]`; none emitted.**

## Escalations
- [info] Carried-forward verification: no still-real `[critical]` items; main
  unambiguously green — verified locally (`dune build` + `dune runtest` exit 0)
  AND via CI (`build-and-test`, `perf-tier1-smoke`, `golden-custom-universe`
  `success` on `e086ec5f`).
- [info] **Correction — `dune` is available in the GHA runner** (via
  `eval $(opam env)`, dune 3.24.0). The last ~18 runs' "dune absent" claim was
  false. Implication: the Step 6.1 build gate can and should run every GHA run
  (it did this run), and any *non-data* harness/cleanup work could in principle
  be built/tested here. The remaining GHA gaps are (a) `EODHD_API_KEY` + a
  warehouse, and (b) the docker `trading-1-dev` QC-isolation env for the 3-gate
  merge — not build.
- [info] **Structural throttle #1 — `EODHD_API_KEY` GHA secret + a broad-universe
  / deep-history / regime-diverse warehouse.** Its absence is why every
  feature/experiment track (S1-S7) is data-gated and GHA dispatches 0 feature
  code. NOT a work-availability problem: the maintainer's LOCAL sessions are
  productive (07-11: #1925 sensitivity + #1926 realism flip merged).
- [info] **Structural throttle #2 — no QC-merge env in GHA.** The runner has no
  docker `trading-1-dev`, so the 3-gate merge (qc-structural + qc-behavioral
  APPROVED, both requiring `isolation:worktree`) cannot run here. Even with data,
  GHA cannot land a feature PR through QC.
- [decision-needed] **M6.6 live cycle** (carried) — needs a human decision: live
  `DATA_SOURCE` wiring + scheduling cron + alerting. Backtest is proven; this is
  the gate to running the strategy for real. NOT a build-gate item — main green.
- [info] **Grant the orchestrator token `workflow` (+ `issues:write`,
  `actions:write`) scope** (carried) — would unblock the ci.yml ENOSPC fix
  (#1636, exact YAML + commit `4cc7d08` staged).
- [info] **`tuning` M2 (qNEHVI) awaiting maintainer enable-commit** (#1327,
  carried ≥30 runs).

## Questions for You
1. **EODHD access** — can the `EODHD_API_KEY` secret be added to the GHA
   orchestrator and a broad-universe / deep-history warehouse be built? With
   `dune` now confirmed available in the runner, EODHD data is the single biggest
   remaining unblock for autonomous feature/experiment work. Absent it, GHA runs
   stay reconcile+health-only while the real work runs LOCAL.
2. **M6.6 live cycle** — ready to make the live-pipeline decision (DATA_SOURCE +
   cron + alerts), or keep it parked?
3. **workflow-scoped PAT** — grant it so the orchestrator can land the staged
   ci.yml ENOSPC fix (#1636)? Low-risk, one-time.
4. **GHA orchestrator cadence** — given the sustained LOCAL-only reality (four
   reconcile passes today), is the 2-slot/day cron still worth the passes, or
   throttle further until the EODHD / QC-env unblocks land? (Counter-point: the
   build gate now runs here, so each pass does verify main-baseline health.)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)

## Per-run links
- run-1 -> `2026-07-11.md`
- run-2 -> `2026-07-11-run2.md`
- run-3 -> `2026-07-11-run3.md`
- run-4 -> `2026-07-11-run4.md`
