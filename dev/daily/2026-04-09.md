# Status — 2026-04-09

## Feature Progress

### simulation/Slice 2 [MERGED]
- Done today: Full Slice 2 rework — cash-as-position via `Portfolio_view.t`
  - `Portfolio_view.t = { cash; positions }` type + `portfolio_value` utility (3 tests)
  - STRATEGY interface: `~positions` → `~portfolio:Portfolio_view.t` (all 3 strategies + all callers updated)
  - Simulator constructs `Portfolio_view.t` from `Portfolio.current_cash` + position map
  - Weinstein strategy derives portfolio value via `Portfolio_view.portfolio_value` for position sizing
  - Bar accumulation, MA direction (from `Stage.classify`), simulation date (bar date, not `Date.today`)
  - 3 new tests: idempotent accumulation, multi-day accumulation, bar date on transitions
  - Smoke tests extended: `hist_start` 2022-01-01 (100+ weekly bars warmup)
- Merged PRs: #237, #242, #240, #241 (4-commit stacked PR)
- Closed PRs: #232, #233, #234 (original `?portfolio_value` approach — replaced)

### data/fetch-universe [PR #243]
- New script: `analysis/scripts/fetch_universe/fetch_universe.exe`
- Fetches from EODHD exchange-symbol-list endpoint, populates name + exchange
- Filters to Common Stock + ETF (24,529 instruments)
- Sector/industry empty — EODHD fundamentals endpoint requires higher API tier

## Harness Work

### T1-Q quality score [MERGED — PR #235]
- Added `## Quality Score` (1–5 rubric) to `qc-behavioral.md` output template
- All Tier 1 harness items now complete

## Data Operations

### Index data fetched
- GSPC.INDX (S&P 500): 24,684 bars — OK
- GDAXI.INDX (DAX): 11,838 bars — OK
- N225.INDX (Nikkei 225): 15,735 bars — OK
- FTSE.INDX: Not on EODHD (returns `[]`); `ISF.LSE` ETF is a possible proxy

### Stale data refresh
- FAILED: `fetch_symbols.exe` requires `-symbols` flag (not optional). Refreshing all 37K symbols requires either making the flag optional or reading from universe.sexp. Deferred.

### A-D breadth
- `ADV.NYSE` / `DEC.NYSE`: "Ticker Not Found" on EODHD. Search returns nothing. Not available on this platform. `ad_bars:[]` degrades gracefully.

## Design

### Milestone integration tests plan [MERGED — PR #236]
- `docs/design/milestone-integration-tests.md`: M1-M7 test specs
- Tests live alongside components (not in a central directory)
- M1-M4 can start now, M5 after Slice 3, M6-M7 after P5/P7
- Assigned to feat-weinstein agent

### Architecture decisions
- **Cash as position**: Modeled via `Portfolio_view.t = { cash; positions }` — clean type, no synthetic positions
- **STRATEGY interface**: `~portfolio:Portfolio_view.t` replaces `~positions`, no optional params
- Decision recorded in PR #232 comment (4 options evaluated, chose type-based approach)

## Follow-up Queue
- simulation: Slice 3 — screener-aware synthetic test data for trade assertions
- simulation: `prior_stage` accumulation per symbol for Stage1→Stage2 transition detection
- data: make `fetch_symbols.exe -symbols` optional (read from universe.sexp) for bulk refresh
- data: FTSE proxy decision (ISF.LSE ETF or skip)
- data: A-D breadth — needs alternative data source (not on EODHD)
- data: sector/industry metadata — needs EODHD fundamentals tier upgrade
- milestone tests: M1+M2 implementation (next agent run)

## Integration Queue
- PR #243: `fetch_universe.exe` — ready for review

## Current Milestone Target
M4/M5 — Position Management / Historical Backtesting
- M4 components complete (stops, portfolio_risk, order_gen, trading_state)
- M5 simulation Slice 2 done, Slice 3 needed for trade assertions
- Milestone integration tests planned but not yet implemented

## Escalations
- None

## Questions for You
1. FTSE.INDX not on EODHD — use ISF.LSE (ETF proxy) or skip entirely?
2. Should we upgrade EODHD API tier for fundamentals (sector/industry data)?
3. Stale data (ends 2025-05-16): need to add "fetch all from universe" mode to fetch_symbols.exe — prioritize?
