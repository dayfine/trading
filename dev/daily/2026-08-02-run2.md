# Status — 2026-08-02 [run 2]

Run timestamp: 2026-08-02T12:45Z– (GHA orchestrator run 30748251970)
Run ID: 2026-08-02-run-2

**Mode:** IN PROGRESS — provisional summary, written up-front on purpose.

## Why this file exists before the run finished

`A-SUMMARY-STALE-FALLBACK`: a truncated run is indistinguishable from a no-op and
loses its summary, its cost record, and any PR it stranded. This run writes the
summary and opens its PR in the first minutes and rewrites the file in place at
Step 7.

**If this file still says `IN PROGRESS`, the run was truncated** — treat the
sections below as the state at truncation. The 07-30-run2 and 08-02-run1 files
are both frozen in exactly this state; see §Escalations.

## Headline: run 1 dispatched, and the PRs still did not move

Run 1 (this morning, run 30738894584) correctly diagnosed the 3-day stall and
dispatched both stranded tracks. Its summary merged as #2177. **But neither
branch advanced.** Verified first-hand this run against the GitHub REST API:

| PR | Branch | Tip SHA | Last push | Commits above main |
|---|---|---|---|---|
| #2169 | `harness/audit-atomic-write` | `993a1437` | **2026-07-30T13:22Z** | 1 |
| #2166 | `cleanup/adjusted-basis-sync-pin` | `fa8c1595` | **2026-07-29T16:55Z** | 1 (claim commit only) |

So the count is now **nine** orchestrator runs — 07-29 → 08-02 — across which
these two PRs were the literal "next task" on their index rows and neither
landed. Run 1's dispatch is not visible in either branch's history; its own
summary froze at `IN PROGRESS` with both agents "in flight", which is the
signature of a truncated run (`A-SUMMARY-STALE-FALLBACK` again, third instance).

This run re-dispatches both, with the failure mode fed back into the briefs.

## Established before dispatch (verified this run, not assumed)

- **Main is GREEN** at `d451f470` (#2177): `build-and-test` SUCCESS,
  `perf-tier1-smoke` SUCCESS, `golden-sp500-5y` SUCCESS,
  `golden-custom-universe` SUCCESS. (`golden-sp500-15y` `cancelled` =
  concurrency cancel, not a failure; `orchestrator` `in_progress` = this run.)
- **Step 0.5 did NOT fast-exit, and not because of the escape hatch.** Two
  conditions failed on their merits: **Condition 1** — `cleanup` tip `fa8c1595`
  ≠ its `Reviewed SHA: d75d8b54` — and **Condition 3** — Step 1b emitted drift.
  Worth recording: the vacuity bug was *not* load-bearing this run, because the
  queue is non-empty. It bites exactly when the queue is empty.
- **Step 1c — `A-FASTEXIT-VACUOUS` re-verified STILL REAL, by direct test.**
  I attempted the documented 6-line patch to `.claude/agents/lead-orchestrator.md`
  with the edit tool and was refused for lack of write permission. This is no
  longer an inference from a prior summary — it is a first-hand negative result
  this run. Twelfth consecutive run.
- **Toolchain re-verified.** `gh` **ABSENT**, `docker` **ABSENT**, `jj` 0.39.0
  present, `dune` 3.24.1 **works** via `bash dev/lib/run-in-env.sh dune …` —
  `bash`, not `sh` (`sh` dies on `set: Illegal option -o pipefail`). Also:
  `git` needs `safe.directory` set before any command works in this container.
  All four facts pre-supplied in both briefs so neither agent re-derives them.
- **`ops-data` skipped, correctly.** `dev/notes/data-gaps.md` is unchanged since
  #814 (long before the last run), and `EODHD_API_KEY` is absent from this
  environment. Both the sentinel check and the precondition say skip.

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | harness-maintainer | in flight | rework #2169 — qc-behavioral N1 (same-filesystem temp invariant untested); mutation-proof required |
| cleanup | code-health | in flight | implement #2166 for real — branch is a claim commit only, 4 days |
| ops-data | — | skipped | data-gaps.md unchanged since #814; EODHD_API_KEY absent |

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | dispatched | harness/audit-atomic-write | #2169 | rework N1 → re-QC → merge |
| cleanup | dispatched | cleanup/adjusted-basis-sync-pin | #2166 | implement pin → QC → merge |

## QC Status

- **harness (#2169)** — structural **APPROVED** (quality 4/5, all gates exit 0,
  22/22 scenarios) at `993a1437`; behavioral **NEEDS_REWORK** (quality 2/5, CP4
  FAIL, one item N1). Rework iteration 1 of 2 dispatched this run.
- **cleanup (#2166)** — `Reviewed SHA` in `dev/reviews/cleanup.md` is `d75d8b54`,
  which is not this branch; there is nothing to review yet (claim commit only).

Both briefs carry an explicit **mutation-verification requirement**: add the
assertion, then break the thing it guards and prove the assertion goes red. The
behavioral review on #2169 found N1 precisely because the prior suite was
false-green at 22/22 with the invariant broken — repeating that would be the
second bite at the same apple.

## Escalations

- **[critical] `A-FASTEXIT-VACUOUS` — re-verified by direct test this run, still
  unfixable by this agent.** Step 0.5's Condition 1 is universally quantified
  over tracks with open PRs, so **zero** open PRs passes it *vacuously* — yet
  zero open PRs is maximum dispatch capacity, the opposite of a saturated queue.
  The fix is a ~6-line spec edit to `.claude/agents/lead-orchestrator.md`, **not**
  a workflow file, so it is **not** blocked on the `workflow`-scoped PAT. It is
  blocked solely on this agent's write permission — I attempted the edit this run
  and was refused. **Twelfth consecutive run.** Measured cost: $43.53 across four
  runs on 07-29, plus four no-op runs on 07-31/08-01 (four orphaned
  `ops/budget-*` branches, `$0.0000` each, no summaries), plus two finished PRs
  now idle five days.

  Exact patch, ready to paste — in §"Four conditions for no-op exit", replace the
  Condition 1 code block with:

  ```
  open_pr_tracks = [tracks with N > 0 open PRs]

  # Non-vacuity guard FIRST — an empty queue is max capacity, not saturation.
  IF len(open_pr_tracks) == 0: CONDITION_1 = FAIL   # → full pass, do NOT fast-exit

  FOR each track in open_pr_tracks:
    tip_sha = SHA of newest open PR
    last_review_sha = "Reviewed SHA:" line from dev/reviews/<track>.md (or "" if absent)
    IF tip_sha != last_review_sha: CONDITION_1 = FAIL
  ```

- **[high] `A-SUMMARY-STALE-FALLBACK` — carried, still unfixed, now with a third
  live instance.** The `ls -t dev/daily/*.md` cross-day fallback in
  `.github/workflows/orchestrator.yml` lets a run with no summary silently
  inherit a prior day's, report success, escape the *Fail on escalations* gate,
  and orphan its cost record. `2026-07-30-run2.md` and now `2026-08-02.md` are
  both frozen mid-run. **Genuinely blocked on the `workflow`-scoped token**
  (unlike `A-FASTEXIT-VACUOUS`).

- **[high] The weekly live-picks run has now missed a full cycle.** Verified:
  newest record in `dev/weekly-picks/` is **2026-07-24** (`ab53f8a6f/`, #2150).
  The 07-31 cycle never ran; 08-02 is past it, so that week is gone rather than
  late. P0 of `next-session-priorities-2026-07-30.md` with a pinned recipe. Not
  orchestrator-dispatchable — needs EODHD, maintainer-local. Next slot 08-07.

- **[info] The promoted bundle's relative margin is uncertified on the honest
  basis.** `dev/backtest/DEEP_RESULTS.md:41-46` states it: the re-pin records the
  honest *level* of the promoted config, not a re-certification of the *promotion
  decision*. Default-on since #2047 on a basis since shown to flatter by −322pp
  return / +6.8pp MaxDD (#2156). Under `.claude/rules/promotion-confirmation.md`
  that is a live default whose justifying evidence is no longer certified. ~7–8h
  of grid; explicitly a user call.

- **[info] EODHD absent in GHA — 4th week.** ~12 data-gated track rows. Combined
  with the fast-exit defect this is one decision, not two: provision the key, or
  formally re-scope the WF-CV tracks build/plan-only so the cron stops idling.

- **[resolved] `support-floor-stops` ownership.** Run 1's `[info]` is fixed —
  the index row now reads `IN_PROGRESS | feat-weinstein` with both follow-ups
  named. Not carried forward.

## Budget

- Budget cap: $200.00 (`dev/config/merge-policy.json`); target band 60–95%.
- Subagents dispatched: 2 (harness-maintainer, code-health) + QC to follow.
- Auth is a Max-20x flat-rate subscription; the cap is a runaway-dispatch
  backstop, not a real budget. Measured cost lands in
  `dev/budget/2026-08-02-30748251970.json` post-run.
- Eligible-work availability, not budget, remains the binding constraint.

## Questions for You

Priority order, all carried and still unanswered:

1. **Grant this agent write access to `.claude/agents/lead-orchestrator.md`** (or
   apply the 6-line patch above yourself). Twelve runs deep, and this run
   confirmed the block by direct test rather than by inference. Highest-leverage
   unblock available: it stops a twice-daily no-op loop and needs **no** new
   token scope.
2. **The `workflow`-scoped token.** Seven filed defects share it;
   `A-SUMMARY-STALE-FALLBACK` is the highest-value — it is why two of the last
   three summaries are frozen mid-sentence.
3. **The weekly picks cycle has now slipped a full week.** Next slot 08-07.
4. **Decide the bundle-vs-alternatives honest-margin grid** — spend the ~7–8h, or
   record in `DEEP_RESULTS.md` that the caveat is knowingly accepted.
5. `gh` in the image vs converting the remaining call sites to REST.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
