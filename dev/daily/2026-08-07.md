# Status — 2026-08-07

Run timestamp: 2026-08-07T07:46Z–11:4xZ (GHA orchestrator run 31159010863)
Run ID: 2026-08-07-run-1
Mode: FULL (Step 0.5 skipped on the empty-queue precondition)

## Headline

**A day's worth of merged test code had reached `main` without ever passing a review gate, and nobody knew — because it arrived inside an auto-merged budget-record PR.** Finding that was worth more than either of the two PRs I shipped.

| PR | What | Outcome |
|---|---|---|
| **#2231** | H-AUDIT-TEST-DISABLE-COUNT-TAUTOLOGICAL + H-WRITE-AUDIT-REPO-ROOT-NOT-REDIRECTABLE | CI green · structural 5/5 → 5/5 · behavioral **2/5 → 4/5** · **merged `25d07cfc`** (1 rework) |
| **#2232** | support-floor-stops B5 + B6 — panel-path `Empty_window`, named undefined inert fraction | CI green · structural 5/5 · behavioral **5/5** · **merged `b54f8880`** (0 reworks) |
| **#2228** | recovered `cleanup/segmentation-default-params-pin` | **closed as superseded — zero net diff** (see below) |

### The finding

The 2026-08-06 summary recorded the `cleanup` track as `skipped — deferred for wall clock`, second run running, and flagged it `[high]`. That was wrong twice over.

`git ls-remote` showed a branch nobody had mentioned: `cleanup/segmentation-default-params-pin`, pushed **2026-08-06 13:14Z** — after the 08-06 summary was written — carrying a complete, well-formed 31-line fix for backlog finding F2, with **no PR ever opened**. Classic H-JJ-JST-BROKEN-GHA: the agent did the work, couldn't open a PR, and the run that dispatched it never reached its Step 4.5 fallback. I opened it as **#2228** under that fallback, rebased it onto main, and put it through QC.

qc-behavioral's first move was to diff it against `main`. **It was empty.**

```
$ git diff origin/main bfc02ac5 --stat        (nothing)
$ git rev-parse bfc02ac5^{tree}      -> 91cc015898c4ed323741dc15ed455be962dba2b3
$ git rev-parse origin/main^{tree}   -> 91cc015898c4ed323741dc15ed455be962dba2b3
```

I verified this independently before acting on it. The branch commit is *not* an ancestor of main, yet main carries byte-identical copies of both files. The content had arrived via **`5abfaf58` = #2224, `ops(budget): record 2026-08-06-31103636022 ($9.7172)`** — an auto-merged ops PR whose body reads *"Additive observational data only"* and whose diff is:

```
A  dev/budget/2026-08-06-31103636022.json
M  dev/status/cleanup.md
M  trading/analysis/technical/trend/test/dune
M  trading/analysis/technical/trend/test/segmentation_test.ml
```

**Test code rode into `main` inside a PR that declared itself data-only, bypassing both QC gates.** The mechanism is the shared-tree churn 08-06 escalated — the budget commit was built from a working tree that still held the code-health agent's changes, so `git add` swept them in. 08-06 saw that churn *delete* orchestrator files; this is the same hazard running the other direction, and the other direction is worse, because deletion is loud and silent addition is not.

**qc-structural reviewed the branch in isolation and reported "touches exactly 2 files."** It never diffed against main, so the PR read as ordinary. Only the behavioral pass caught it. That asymmetry is the reusable lesson.

The content is fine — and #2228's review is now the gate it never got. qc-behavioral mutation-tested every claim against both the pre- and post-fix fixtures, and **disclosed and corrected a false-green in its own first harness** before reporting:

| mutation | pre-fix fixture | post-fix fixture |
|---|---|---|
| `penalty_weight` 0.2 → 0.02 | **GREEN** | **RED** |
| `trend_bonus_weight` 0.5 → 5.0 | **GREEN** | **RED** |
| `min_r_squared` 0.9 → 0.85 (unanticipated by F2) | **GREEN** | **RED** |
| `max_segments` 10 → 12 (unanticipated by F2) | **GREEN** | **RED** |

The argmax blind spot was real, the pin closes it, and "pins all 13 fields" is compiler-enforced by OCaml record-literal exhaustivity rather than aspirational. **No revert warranted.** #2228 closed as superseded; F2 marked `[x]` with the whole trail recorded in `dev/status/cleanup.md`.

## Established before dispatch (measured, not assumed)

- **Main GREEN by direct measurement** at `73826b7a`: `dune build` exit 0. GitHub CI green on the same SHA across `build-and-test`, `perf-tier1-smoke`, `golden-sp500-5y`, `golden-custom-universe`. Re-measured post-merge at `b54f8880`: `dune build` exit 0, `dune runtest` **exit 0**, zero `FAIL:` lines.
- **Step 0.5 did NOT fast-exit, for the right reason.** The only open PR was #2227, human-authored and 2 minutes old — **zero orchestrator-dispatched PRs**, the opposite of saturated. The non-empty-queue precondition from #2180 skipped the four conditions and forced a full pass. Third consecutive run it has fired correctly, and this run is the strongest argument for it yet: a fast-exit would have left the stranded branch stranded.
- **Toolchain, unchanged:** `gh` ABSENT, `docker` ABSENT, `dune` not on PATH, `jj`/`jst` present but BROKEN. Working: `curl`, `jq`, `python3`, `git`, `bash`, `dev/lib/run-in-env.sh dune …`. All six agent sessions were pre-warned, plus the `/__w/trading/trading` vs `/workspaces/trading-1/trading` trap and the "do not background your build" prohibition. **All six landed their files correctly, none attempted `jst`, none stalled on a background monitor.**
- **Pre-flighted every deliverable's existence before dispatching.** All three survived: `record_qc_audit_test.sh:1287` still read `(( seen25 == ${#HOOK_DISABLE_VALUES[@]} ))`, `write_audit.sh:154-165` still walked up before consulting `$REPO_ROOT`, and `test_panel_callbacks.ml` pinned `Flag_off`/`Raw_fallback`/`Adjusted` but **not** `Empty_window` while `test_support_floor.ml:1027` did.

## Step 1b drift cross-reference

- **`[drift]` — the `cleanup` row.** 08-06 recorded `blocked … deferred for wall clock, 2nd run running`. In fact a `code-health` agent completed and pushed the work at 13:14Z and no PR was opened; no summary was ever written for that second run. See Headline.
- **`[drift]` — follow-ups the 08-06 summary says it filed, which never reached the status files.** `H-AUDIT-TEST-DISABLE-COUNT-TAUTOLOGICAL` was absent from `harness.md`, and **B5 and B6 were absent from `support-floor-stops.md`**. The other three harness items did land. This is the shared-tree churn 08-06 escalated, and it means **the daily summary was the only surviving record of three findings**. All three are now properly filed — B5/B6 by #2232, the harness item by #2231.
- **Step 1c — no still-real `[critical]` items.** 08-06 carried none tagged `[critical]`, and main was verified green by direct measurement. Nothing escalates to `[critical]`.
- **Stale branch, no PR ever:** `harness/rework-streak-escalation` (2026-08-05) contains **only** a `[~]` marker commit on `harness.md` and no work. An abandoned dispatch. Main is unaffected (the marker never merged). Flagged for deletion.

## Dispatched this run

Writers ran **strictly sequentially** (shared-tree constraint); QC gate-sequenced. **Eight agent sessions, 12 dispatches** counting resumes (2 writers + 6 QC sessions; 4 of the dispatches were resumes of an existing session rather than fresh spawns).

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| cleanup | — | **recovered** | orphan branch found by `git ls-remote`; PR #2228 opened via Step 4.5 |
| cleanup | qc-structural | **APPROVED (5)** | reviewed the branch in isolation; **did not diff against main** — see Escalations |
| cleanup | qc-behavioral | **NEEDS_REWORK (2)** | **found the zero net diff + the #2224 sweep-in**; verified content correct by mutation anyway |
| harness | harness-maintainer | completed | both findings; grepped every caller before choosing fix shape (a) |
| harness | qc-structural | **APPROVED (5)** | re-derived all three of the author's mutations independently |
| harness | qc-behavioral | **NEEDS_REWORK (2)** | **B1 — the walk-up branch was deletable with the suite 31/31 green** |
| harness | harness-maintainer | rework 1 | scenario 27b; corrected its own rationale after behavioral measured it false |
| harness | qc-structural | **APPROVED (5)** | delta pass; reproduced the 27b mutation |
| harness | qc-behavioral | **APPROVED (4)** | 13 probes; constructed 4 *subtler* walk-up regressions to test the contract |
| support-floor-stops | feat-weinstein | completed | B5 + B6; **withheld** a test it judged green by construction, and said so |
| support-floor-stops | qc-structural | APPROVED → **held** → **APPROVED (5)** | first pass cited the author's numbers; I refused it and required re-derivation |
| support-floor-stops | qc-behavioral | **APPROVED (5)** | ran a mutation the author never claimed; **dropped a premise of mine as false** |
| ops-data | — | skipped | `data-gaps.md` unchanged since #814 (2026-05-03); `EODHD_API_KEY` absent |
| health-scanner | — | not dispatched | fast scan is deterministic (Step 6); deep scan is a weekly cron |

**Rework budget used: 1 of 2 on harness, 0 of 2 on support-floor-stops.** Neither hit the escalation path.

## Pending work

| Track | State | Branch | PR | Next step |
|---|---|---|---|---|
| harness | auto-merged | harness/audit-test-count-and-repo-root | **#2231** | merged `25d07cfc` — all 3 gates green |
| support-floor-stops | auto-merged | feat/split-safe-empty-window-panel | **#2232** | merged `b54f8880` — all 3 gates green |
| cleanup | closed | cleanup/segmentation-default-params-pin | **#2228** | closed as superseded; F2 `[x]`; branch deletable |
| simulation | blocked | — | — | fill-model / E-anchored arc — **in flight locally**, fenced |
| screener | blocked | — | — | local-top-E arms — **in flight locally**, fenced |

## Feature Progress

### harness [MERGED #2231]

- **A coverage claim that could not detect its own erosion.** Scenarios 25a/26a compared the loop's iteration count against `${#HOOK_DISABLE_VALUES[@]}` — the same array's own length — so shrinking the array to `(0)` shipped **30/30 green** while silently dropping five of the six documented "off" spellings. Since #2221's entire deliverable was the *narrowed* disable contract, that is precisely the wrong pin. Worse, the pass message hardcoded the literal list, so a shrunk array printed `all 1 documented 'off' spellings (0 false no yes true '')` — green and self-contradicting. Fixed with an independent `HOOK_DISABLE_EXPECTED_COUNT=6` plus a `_disable_values_repr()` that derives the printed list from the array.
- **The reviewer's own words on why that repr is load-bearing:** shrinking now prints `for all 6 documented 'off' spellings ('0'); exercised 1`, which puts the five lost spellings next to the expected count — so it reads "you dropped five," not "lower the constant." The tautology moved from agreement-by-construction to agreement-by-two-coordinated-edits, which is the standard escape, and the residual is named in the code.
- **The rework finding is the one I'd keep.** qc-behavioral deleted the entire `.git`/`.claude` walk-up block from `_repo_root()` and the suite came back **31/31 green**. Every caller in the test file — *including the new scenario 27* — sets `REPO_ROOT`, so no test reached the second branch. And that branch is the **only one production uses** (`lead-orchestrator.md` L1414 invokes `write_audit.sh` with `REPO_ROOT` unset). *A PR whose entire subject is reordering two branches pinned one and left the other deletable undetected.*
- **The `env -u REPO_ROOT` guard in the fix is not boilerplate — it was measured.** With it removed and the suite run under an ambient `REPO_ROOT`, scenario 27b not only fails, it **writes a record into the live `dev/audit/` (104 → 105 records)** — reproducing the exact litter bug the whole item was filed for, inside the test meant to pin it.
- **The fix shape was chosen on evidence, not preference.** The author grepped all 39 call sites first, found every one already sets `REPO_ROOT` to a fixture that also carries a `.claude` sentinel — so override and walk-up already agreed everywhere exercised — and surfaced an independent argument: the shared `repo_root()` in `_check_lib.sh:53`, which most other check scripts source, **already** checks `$REPO_ROOT` first. `write_audit.sh`'s bespoke copy had the precedence backwards relative to in-repo convention, independent of the finding.
- **A rationale retracted on measurement.** The author had scoped the `record_qc_audit.sh` sibling out because "the two scripts' `REPO_ROOT` usage is independently overridable." qc-behavioral measured that false: `record_qc_audit.sh:102` plainly reassigns the inherited **exported** `REPO_ROOT` with its own walk-up result, and the export attribute survives — so the sibling's bug propagates *through* this PR's new precedence. The reviewer **upheld the scope call** and faulted only the reason; the author verified the mechanism with its own two-script repro and corrected the text rather than softening it.
- Counts: **31 assertions / 27 scenarios → 32 / 28.**

### support-floor-stops [MERGED #2232]

- **B5 — the track's documented recurring defect class, caught for the third time.** `Empty_window` (added by #2220) was pinned on the bar-list path and not the panel path, which had siblings for the other three states. "Covered on one path, unexercised on the other" was #2213's B3 and #2220's B1. The production trigger is live: `Snapshot_bar_views.daily_view_for` returns `empty_daily_view` on three routes, including "no raw-close rows for the symbol" — real for a newly-listed or snapshot-missing symbol.
- **The B5 measurement was run in both directions, which is what makes it a finding rather than an assertion.** Removing the `Empty_window` branch from `_scan_basis` gives 25 cases / 1 failure *with* the new test; with `test_panel_callbacks.ml` reverted to `25d07cfc`, the same mutation gives **24 cases / 0 failures / exit 0**. The panel path was genuinely *blind*, not merely under-asserted.
- **The test reaches the empty view the way production does**, and qc-behavioral verified the distinction is real rather than rhetorical: of the three routes to `empty_daily_view`, the test's `lookback:90` and in-calendar `as_of` exclude two by construction, so route 3 (snapshot-missing symbol) is what fires.
- **B6 — an undefined metric made unrepresentable rather than documented-around.** The inert fraction is `Raw_fallback / (Raw_fallback + Adjusted)` with `Empty_window` correctly excluded from both terms — which means an all-`Empty_window` arm has denominator zero, and a `0/0` rendered as a blank is indistinguishable from "this column was never wired up." Those two states call for opposite responses. New pure module `Split_safe_metric` returns `Inert_fraction of float | Not_exercised of tally`, so a consumer must pattern-match. `Not_exercised` carries the tally because the whole complaint was that a blank cell can't say why: `flag_off > 0` is a wiring alarm, `empty_window > 0` with `flag_off = 0` is a coverage problem, all-zero is an empty population.
- **qc-behavioral checked that tri-state was pinned rather than merely asserted** — the highest-value check in the review — and it was: each reading corresponds to a distinct literal tally asserted in a test, with distinguishability asserted *directly as a pair* rather than inferred.
- **The writer withheld a test and was right to.** It declined a panel sibling of the bar-list `split_safe_empty_window_stop_matches_flag_off`, reasoning that with `n_days = 0` every accessor returns `None`, so `_rescaled` and the untouched bundle are observationally identical and no mutation could redden it. Both reviewers independently confirmed the short-circuit at `support_floor.ml:187` makes that correct. **It would have been coverage-shaped but green by construction — the exact defect #2220's B2 was about.** That is the standard propagating.
- **A domain question I raised, checked and dropped by the reviewer.** I asked whether `Empty_window` was hiding a real hazard — a stop computed from no data. qc-behavioral traced it: the resulting stop is the pre-existing buffer fallback, and `floor_is_structural → false` records it as `Buffer_fallback`, so two independent audit columns both say "no floor data." The system never claims a structural floor it doesn't have. It reported the premise as false and **declined to manufacture a finding from it.**
- Counts: `test_panel_callbacks` 24 → **25**; `test_split_safe_metric` **0 → 8** (new module).

## QC Status

- **harness (#2231)** — structural 5/5, behavioral NEEDS_REWORK 2/5 → rework 1 → structural 5/5, behavioral **APPROVED 4/5**. Merged `25d07cfc`. Rework: **1 of 2**.
- **support-floor-stops (#2232)** — structural 5/5 (after being held and re-run), behavioral **APPROVED 5/5**. Merged `b54f8880`. Rework: **0 of 2**.
- **cleanup (#2228)** — structural APPROVED 5/5, behavioral **NEEDS_REWORK 2/5** (zero net diff). Closed as superseded, not reworked: there is nothing to rework in an empty diff, and the reviewer's own recommendation was to close and record the sweep-in. No audit record written — the PR is the record.
- Audit records: `dev/audit/2026-08-07-harness-audit-test-count-and-repo-root-harness.json` (quality 4) and `dev/audit/2026-08-07-feat-split-safe-empty-window-panel-support-floor-stops.json` (quality 5), both `0644`, both `consecutive_rework_count=0`.
- All **six** QC reviews posted to their PRs via REST on receipt. `gh` is absent so agents cannot post their own; **the PR is the durable record, not the file** — see Escalations for why that distinction earned its keep today.

## Health Scan

`dev/health/2026-08-07-fast.md`. Measured post-merge at `b54f8880`: build **PASSING** (exit 0), tests **PASSING** (exit 0, zero `FAIL:` lines), status-file integrity **PASSING** (exit 0), index-size linter **PASSING** (exit 0). Result: **CLEAN**.

**The index-size linter caught one of my own Step 5.5 edits, and the documented invocation would have missed it.** My reconciled `support-floor-stops` row came to 264 chars against a 250 limit. Run the way `lead-orchestrator.md` documents — `… | tail -5; echo "exit=$?"` — it printed the `FAIL:` line **and `exit=0`**, because `$?` reads `tail`'s status. Re-run with the output redirected instead of piped, it correctly reported failure; I shortened the row and it went green. **Second consecutive run in which these linters caught a defect in the orchestrator's own output rather than an agent's, and the second in which the documented snippet would have missed it.**

## Follow-up Queue

Total open `- [ ]` items across `dev/status/*.md`: **57** (threshold 10). As 08-06's H-FOLLOWUP-THRESHOLD-RETUNE established, this count is **crossed by construction** — ~10 are T2/T3/T4 roadmap items in `harness.md` that Step 2c explicitly declares undispatchable — and should not be read as debt growth. I did not convert a feature slot to a maintenance pass on this signal; one of three slots was already maintenance.

**Ten items filed this run, all from QC findings, all non-blocking.**

**Into `dev/status/harness.md`** (from qc-behavioral on #2231):
- **H-AUDIT-27B-CUMULATIVE-COUNT** (R1) — 27b's `walkup_count27b == 1` is a *cumulative* count on a fixture shared with 27. Robust today (probe J2 shows a precedence revert reddens both rather than false-greening; J1 shows 27b doesn't depend on 27 *running*), but sensitive to any future scenario writing into `WALKUP_ROOT`. Fix: snapshot-and-delta, or its own fixture.
- **H-AUDIT-27B-LEAKS-UNDER-PWD-REGRESSION** (R2) — under a `$PWD`-resolving walk-up regression, 27b goes red *and* leaves a stray record in the live `dev/audit/`. Fix: `cd` to a scratch dir for the invocation.
- **H-AUDIT-WALKUP-SENTINEL-NARROWING-UNPINNED** (R3) — narrowing the sentinel to `.claude` only isn't caught, since the fixture carries only `.claude`. Low value; K1/K3/K4 *are* all caught, so 27b closes the contract.
- Plus, filed by the writer: **H-RECORD-QC-AUDIT-REPO-ROOT-SIBLING**, **H-AUDIT-TEST-DISABLE-COUNT-PAD-DIRECTION-WORDING**, **H-AUDIT-HOOK-DISABLE-COUNT-DOCSTRING-DRIFT** (the two `ACCEPTED SPELLING:` blocks in `write_audit.sh` enumerate six and three values respectively — they already disagree with each other).

**Into `dev/status/support-floor-stops.md`** (from qc-behavioral on #2232): **R1** (`tally_of_bases` order-independence documented but unpinned — a test would be green by construction, filed only for map completeness), **R2** (`empty_tally` pinned transitively; a naming nit), **R3** (the §Follow-ups strikethrough could let a skimmer infer more closure than exists — suggest "(reduction only; rendering remains F6)"), **R4** (F6 still says "the shell count is sufficient to qualify a surface", which is now stale advice — that is exactly the ad-hoc path producing the `0/0` blank B6 forbids).

Carried, unactioned: **F3** (still the highest-value item on support-floor-stops), F1, F2, F6, F7, F8, F9; H-AUDIT-ATOMICITY-PARTIAL-PIN, H-AUDIT-CHMOD-FAIL-CLOSED, H-REWORK-STREAK-ESCALATION-UNTESTED, H-JJ-JST-BROKEN-GHA, H-BLAS, H-CI-DISPATCH. **H-AUDIT-TEST-DISABLE-COUNT-TAUTOLOGICAL, H-WRITE-AUDIT-REPO-ROOT-NOT-REDIRECTABLE, cleanup F2, B5 and B6 were closed.**

## Integration Queue

| PR | State | CI | structural | behavioral |
|---|---|---|---|---|
| #2231 | **MERGED `25d07cfc`** | green | APPROVED 5/5 | APPROVED 4/5 |
| #2232 | **MERGED `b54f8880`** | green | APPROVED 5/5 | APPROVED 5/5 |
| #2228 | **CLOSED** (superseded, zero net diff) | green | APPROVED 5/5 | NEEDS_REWORK 2/5 |
| #2227 | **OPEN — yours** | green | skipped (docs-only) | skipped (docs-only) |
| #2229 | **OPEN — yours** | — | — | V12 stop-distance gate-consistency invariant (Workstream A) |
| #2230 | **OPEN — yours** | — | — | faithfulness / sensibility evaluation harness (Workstream B) |

Both merges were clean; no `update-branch` was needed, so the merged tips *are* the SHAs the QC agents reviewed. `.claude/rules/pr-merge-gates.md` satisfied to the letter.

**You opened #2229 and #2230 while this run was in flight** — Workstreams A and B of `dev/plans/fill-model-faithfulness-2026-08-07.md`, exactly the scope your plan assigned to yourself. That is the fence working as intended in both directions: I stayed off the entry path and `Post_run_validator`, and you landed them without colliding with anything I dispatched. I have not reviewed or merged either.

**#2227 is left for you deliberately.** It is docs-only and CI-green, so it qualifies for the two-gate path — but you opened it two minutes before this run started and its plan describes work you scoped to yourself. Merging your PR mid-session would be presumptuous. Say the word and I'll merge it next run.

## Current Milestone Target

M5. No milestone-gated harness items unblocked this run (T2-B/T2-C/T2-D remain gated).

## Dependency Unlocks

None new. One **sequencing consequence** advances: with B5 and B6 closed, the `split_safe_floors` axis now reports its inert fraction on **both** paths, and an all-`Empty_window` arm is distinguishable from an unwired column. Per 08-06's question 3, the two caveats I raised then are now closed. What remains before a promotion decision under `.claude/rules/promotion-confirmation.md` is **F6** (render the fraction) plus a surface that actually reports it. F7 and F9 both *understate* how much of a run the flag touched, so neither can make an inert arm look active — but both must be named when the number is read.

## Budget

- Cap $200.00 (`merge-policy.json`); target band 60–95%. Auth is a Max-20x flat-rate subscription, so the cap is a runaway-dispatch backstop rather than a real spend limit.
- Subagent sessions: **8** (2 writers, 6 QC), **12 dispatches** counting resumes. ~1.22M subagent tokens.
- **Resuming agents rather than re-spawning them again paid in quality, not just cost.** Every re-review continued an agent with its context intact, so reviewers re-ran *their own* mutations instead of rediscovering the file — behavioral re-ran its own B1 probe on the delta and confirmed it now reddens. Delta passes took 2–6 minutes against 3–22 for initial ones.
- **Wall clock, not budget, was the binding constraint** — ~4h, almost all serial agent time. Writers cannot safely overlap in this shared tree and QC agents check out branches, so the pipeline is inherently serial.
- **Utilization: UNDER_TARGET on cost, AT CAPACITY on wall clock.** These disagree routinely now and the wall clock is the real limit. Recording the honest version rather than the flattering one.

## Escalations

- **[high] An `ops(budget)` PR carried test code into `main`, bypassing both QC gates — and it is the auto-merge path that did it.** #2224's body says *"Additive observational data only"*; its diff contains `segmentation_test.ml` (+31), a `dune` change, and a status edit. The Step 8a bundling step builds the budget commit from the shared working tree, so whatever an agent left there gets swept in. **Two mechanical guards would each have caught this deterministically:** (a) a check that a PR whose title matches `ops(budget)` / `ops: daily orchestrator summary` touches **no** `*.ml` / `dune` file; (b) an "empty diff vs base" check at PR-open time, which would have flagged #2228 before a reviewer spent a session on it. I cannot add either — both live under `.claude/**` or `.github/**`. **This is my top ask this run.**

- **[high] qc-structural reviewed a PR in isolation and missed a zero net diff; qc-behavioral caught it.** Structural's protocol reads the branch diff (`git diff origin/main...HEAD`, three-dot) which showed three files and looked normal. The two-dot `git diff origin/main HEAD` was empty. **A structural reviewer should diff both ways** — the three-dot form answers "what did this branch author?" and the two-dot answers "what would merging change?", and only the second one gates a merge. Cheap, mechanical, and it would have saved a full QC session. Also under `.claude/**`.

- **[high] qc-structural truncated `dev/reviews/harness.md` twice in one run** — 880 lines → 53 on its first pass, then again on its delta pass, silently dropping the entire behavioral section. I rebuilt it from backup both times and the file is intact at 1175 lines, but **this only worked because I now back up every orchestrator-owned file to `/tmp` before each agent touches the tree.** 08-06 asked whether QC agents should write to the PR only and skip the file; after today I'd answer **yes**. I mitigated it mid-run by instructing later QC agents to return their body and not touch the file at all, and that worked cleanly. The nine reviews on the PRs never needed rebuilding.

- **[high] `.claude/**` is not writable — 7th consecutive run, now blocking 7 items.** Claude Code's built-in self-modification guard, not the workflow token. Blocked: the Step 4 working-tree-isolation claim (still actively wrong), the Step 6.2 path bug **and its companion `$?`-through-a-pipe bug** (both demonstrated live again today — see Health Scan), the `/workspaces/trading-1` purge, H-JJ-JST-BROKEN-GHA's fix, folding the "do not background your build" prohibition into the agent definitions (**now 19 of 19 across four runs**, entirely dependent on my pasting it into every brief), the two-way-diff rule above, and instructing QC agents not to write review files.

- **[high] `jj`/`jst` remain non-functional in GHA (H-JJ-JST-BROKEN-GHA) — and this run shows the real cost.** Both writers again needed my Step 4.5 REST fallback, and all nine QC reviews needed REST posting. **The stranded `cleanup/` branch is this bug's compound interest**: an agent did good work, couldn't open a PR, the run ended, and the work sat invisible for a day until a `git ls-remote` happened to surface it — by which point it had already leaked into main through an unrelated PR. Cheapest fix is still (c): delete the `jst` instructions from the GHA path and document the REST fallback as the sole mechanism.

- **[high] `A-SUMMARY-STALE-FALLBACK` — carried, still unfixed.** `.github/workflows/orchestrator.yml:332` still has the cross-day `ls -t dev/daily/*.md` fallback, so a run with no summary silently inherits a prior day's and reports success. Blocked on a `workflow`-scoped token.

- **[medium] A QC agent cited the author's own measurements as verification; I held the review and required re-derivation.** qc-structural's first pass on #2232 filled its Measurement Summary from `dev/status/support-floor-stops.md` — the author's own recorded numbers — which confirms the status file is internally consistent and nothing else. I sent it back; it re-ran MT5 in both directions and MB1, reproduced every number, and answered the two questions it had skipped. **The verdict was unchanged but previously unearned.** Worth naming because approving a measurement-standard PR on unverified measurements is the same defect one level up, in the gate itself.

- **[medium] A discrepancy in `dune runtest`'s exit code that I could not resolve and would like verified.** On the MT5 mutation the writer reported **exit 1**; qc-structural reported **exit 0** with the same 1 failing case, remarking "dune doesn't fail on test failure, only build error." Both agree on the failure count, so nothing was mis-gated today, and my own full `dune runtest` on green main returned 0. But if `dune runtest` can return 0 with a failing test, **every exit-code gate in this pipeline is unsound** — and if it can't, one of the two agents misread a pipeline's status, which is the `$?`-through-a-pipe trap again. Cheap to settle deliberately; I'd rather not leave it ambiguous.

- **[info] Stale branch for cleanup:** `harness/rework-streak-escalation` (2026-08-05) holds only a `[~]` marker commit and no work — an abandoned dispatch, never PR'd. Main is unaffected. Also `cleanup/segmentation-default-params-pin` and the two merged branches are now deletable.

- **[info] The fence held.** Per `.claude/rules/gha-local-coordination.md` I treated the fill-model / E-anchored arc as yours and dispatched nothing on `simulation` or `screener`. I read `dev/plans/fill-model-faithfulness-2026-08-07.md` off #2227's branch purely to compute the fence, and named the fenced files explicitly in the feat-weinstein brief (`entry_audit_helpers.ml`, `screener.ml`, `Post_run_validator`, `validator_row_checks.ml`). **The agent respected it and reported doing so** — it declined to touch F8 (`Entry_audit_capture.classify_stop_floor_kind`, a dormant #2167-shaped trap) specifically because it sits adjacent to `entry_audit_helpers.ml`. F8 stays open.

- **[info] Agents did NOT stall on background monitors — 0 of 12 dispatches**, extending 0-of-12 (08-06), 0-of-15 (08-05), 0-of-4 (08-04). The only change remains the explicit prohibition plus "blocking for 10–20 minutes is expected and correct" at the top of every brief.

- **[info] `trading/compile_commands.json` is a tracked build artifact** that `dune build` regenerates with host-specific opam paths, so it dirties the tree on every build. The feat-weinstein agent had to revert it twice; it is already on the cleanup backlog as `tracked_build_artifact`. A small, real papercut for every agent working here.

- **[info] EODHD absent in GHA — 10th week.** ~12 data-gated track rows unchanged.

- **[info] Carried-forward verification: no still-real `[critical]` items**; main green by direct measurement (exit 0 at `73826b7a` pre-dispatch, `dune build` + `dune runtest` both exit 0 at `b54f8880` post-merge).

## Questions for You

1. **Add the ops-PR content guard — this is the one that matters.** A check that a PR titled `ops(budget)` / `ops: daily orchestrator summary` touches no `*.ml` or `dune` file would have caught #2224 deterministically. Today's instance was benign (the swept-in code is good, and it now has a review). The next one might not be, and nothing would tell us. Pairs with an "empty diff vs base" check at PR-open time. Both need `.claude/**` or `.github/**` access I don't have.

2. **Grant write access to `.claude/**`, or apply the seven queued fixes yourself.** Asked seven runs running. Two of them stopped being theoretical again today: the Step 6.2 `$?`-through-a-pipe bug printed `FAIL:` **and** `exit=0` on a real over-length row I had just written, and the missing two-way-diff rule is why a zero-diff PR reached behavioral review.

3. **Should QC agents stop writing `dev/reviews/*.md` entirely?** You raised the shape of this on 08-06. Today qc-structural destroyed 880 lines of that file twice in one run. Mid-run I switched to "return the body, don't touch the file" and it worked cleanly. The PR already is the durable record. I'd like to make that the rule.

4. **Settle the `dune runtest` exit-code question** (Escalations, medium). Two agents reported different exit codes for the same mutation with the same failure count. Nothing was mis-gated, but the whole pipeline gates on exit codes and I don't want to assume.

5. **F3 is again my recommended first feature pick next run** — `trade_audit_report_bin.ml` reading raw where the domain wants adjusted, the G14 pathology relocated to the reporting layer. I deferred it today because it needs a golden re-pin *and* sits in the trade-audit reporting area your fill-model arc is active in. If you'd rather I stay out of that neighbourhood entirely until the arc lands, say so and I'll take F6 or F8 instead.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
