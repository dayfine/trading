# Status — 2026-06-13

Run timestamp: 2026-06-13T02:15Z (GHA orchestrator run 27453251622)
Run ID: 2026-06-13-run-1

**Mode:** FULL PASS / **0 DISPATCH.** Main is GREEN on `d4ea7bb2`. Since the
06-12 summary (#1552) the maintainer shipped #1554–#1556 overnight, including the
**P0 root-fix surface**: a default-off `suppress_warmup_trading` flag (#1555,
`dev/plans/warmup-trading-flag-2026-06-12.md`) that addresses the deepest 06-12
forensics finding (the strategy trades during the 210-day warmup), plus #1556 (revert
cash-floor-rejected exit fills so stops retry) and #1554 (handoff rename). The one
open PR — maintainer-authored **#1558** (wire `Fold_health.check_divergence` into the
runner, #1557 item 1) — is **CI-RED on the file-length linter** (`runner.ml` and
`simulator.ml` both 504 > 500 hard limit), a single mechanical blocker on a
deliberately-structured core PR. The orchestrator did **not** push onto that active
maintainer branch (collision/presumption on core files for a 5-min extraction);
surfaced with the exact fix instead. **Every other forward item is data-gated
(EODHD absent in GHA; top-3000 PIT warehouses maintainer-local) or human-fork-gated
(warmup-flag surface test = maintainer judgment on re-pinning goldens).** No
GHA-eligible, non-colliding dispatch existed → 0 dispatch. **HEAD at run start = run
end: `d4ea7bb2`** (no orchestrator merge this run).

## Step 0 rampup check (deterministic)

Main is **GREEN**. CI on `d4ea7bb2` (#1555 tip): `build-and-test` SUCCESS,
`perf-tier1-smoke` SUCCESS, `golden-sp500-5y` SUCCESS, `golden-custom-universe`
SUCCESS, `watchdog` skipped, `orchestrator` = this run. **0 open `ci-red` issues.**
Local build gate: `dune build` exit 0 + full `dune runtest` exit 0 on `d4ea7bb2`,
`0` `^FAIL:` lines (live evidence in §Health Scan). Proceeded.

## Step 0.5 saturated-queue check

**Evaluated — NOT a no-op exit (full pass).** Condition 2 FAILS hard: the
maintainer merged #1554/#1555/#1556 since the prior summary (#1552), touching
`dev/status/backtest-infra.md`, `dev/status/simulation.md`, and adding
`dev/plans/warmup-trading-flag-2026-06-12.md` — real status drift. → full pass.
(Condition 1: one open PR #1558, tip `2e1fccdd`, no QC review yet → not a clean
"all-reviewed" queue either.)

## Step 1b drift cross-reference

Prior summary `2026-06-12` `## Pending work` rows were all `blocked` /
`auto-merged` — **no `dispatched` / `awaiting-merge` orchestrator row left
hanging → no `[drift]` warnings.** The #1554–#1556 merges + #1558 PR are
maintainer work, not orchestrator drift.

- **Resolved between runs:** the prior `[critical-decision-needed]` **P0
  warmup-trading semantics** is now partially addressed — the maintainer landed
  the default-off `suppress_warmup_trading` flag (#1555). The *semantics decision*
  (does warmup=indicators-only win, requiring every golden re-pinned) is still a
  maintainer fork, but the flag surface now exists.

## Step 1c carried-`[critical]` verification

Prior summary `2026-06-12` carried **no bare `[critical]`** (two
`[critical-decision-needed]` — P0 warmup + M6.6 live cycle — plus several
`[info]`). Re-verified main **GREEN** this run (`dune build` + `dune runtest`
exit 0 on `d4ea7bb2`; full CI green on the identical SHA; 0 open `ci-red` issues).

**[info] Carried-forward verification: no still-real bare `[critical]` items;
main GREEN on `d4ea7bb2` (build + runtest exit 0, 0 `^FAIL:`).** The
`[critical-decision-needed]` items are re-listed under §Escalations (none trips
the gate-fail).

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-infra (fold-health wiring) | blocked (maintainer PR, CI-red) | feat/fold-health-runner-wiring | #1558 | Sole blocker = file-length linter: `runner.ml` 504 + `simulator.ml` 504 (>500 hard limit). Extract a cohesive helper from each (no behavior change) to get under 500, then re-run CI. Maintainer-owned (deliberately-structured core PR, #1557 item 1). |
| P0 warmup-trading semantics | blocked (maintainer fork) | — | #1555 (flag landed) | Default-off `suppress_warmup_trading` flag now exists (#1555). Decide whether warmup=indicators-only wins → test as a surface (experiment-flag-discipline), expect EVERY pinned baseline to move, re-pin goldens behind the flag. Maintainer judgment; blocks the definitive matrix re-run. |
| P2 definitive matrix + policy universe | blocked (data-gated; on P0) | — | — | Emit composition-policy universe artifact ($-volume wired #1542), re-run both matrices with min-window guard + fold-health + honest warmup semantics. Gated on the P0 decision + maintainer-local top-3000 PIT warehouses; `EODHD_API_KEY` absent in GHA. |
| short-side economic-floor experiment | blocked (experiment/data) | — | #1551 (axis landed) | `short_min_price` axis landed 06-12 (#1551). Next: WF-CV the axis (0.0 vs ~17.0) before any default flip; fix Phase-2 `margin_call` `TriggerExit` dedup bug. |
| `enable_short_side` default flip (true→false) | blocked (needs ledger ACCEPT) | — | — | Forbidden without a ledger ACCEPT + confirmation grid (experiment-flag-discipline R3 / promotion-confirmation). |
| stage-accuracy / experiment-platform | blocked (data-gated) | — | — | Broad-universe WF-CV re-run of the cascade-selection inversion (#1509 merged); force_exit_off grid REJECTED (#1503). top-3000 run dirs maintainer-local. |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | Not yet enabled for GHA dispatch (precedent #1327). |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-infra | — | skipped | Open maintainer PR #1558 in flight (CI-red on file-length); did not collide with the active core branch. Surfaced exact fix as escalation. |
| short-side-strategy / stage-accuracy / experiment-platform | — | skipped | Forward strategy/experiment work data-gated (EODHD absent in GHA; top-3000 PIT warehouses maintainer-local) or needs ledger ACCEPT. |
| P0 warmup surface test | — | skipped | Maintainer fork: deciding warmup semantics + re-pinning every golden is human judgment, not autonomous. Flag (#1555) just landed. |
| harness | — | skipped | No eligible T1/T3 item. Tier 1 fully `[x]`; open items T2 (milestone-gated), T3-H (low-pri). `harness.md` unchanged since 2026-05-22. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged since 2026-05-03 (ADL needs human input; sector metadata needs live HTTP exceeding session time). |
| code-health | — | skipped | `dev/status/cleanup.md` §Backlog empty (only template placeholder); deterministic Step 6 surfaced no `[medium]`/`[high]` (build + runtest clean). File-length trend logged in §Health Scan, not auto-refactored (core files, maintainer-active). |

## Feature Progress

### backtest-infra — Fold_health runner wiring (maintainer #1558) — BLOCKED on CI
The maintainer's #1558 wires `Fold_health.check_divergence` into the backtest
runner (follow-up to #1556, which landed the pure `Stuck_held_positions` finding
but descoped the runner wiring). It adds `fold_health_runner.ml/.mli` (a new
module), feeds the two position counts from the runner + simulator, and adds a
136-line test. The build fails **only** on the file-length linter — the +13/+20
line additions push `runner.ml`→504 and `simulator.ml`→504, both over the 500
declared-large hard limit. No test/behavioral failures. Sole, mechanical blocker.

### Strategy program — surface unchanged
No edge-seeking strategy work this run (single-dial surface remains empirically
exhausted; the redirect to tail-preserving levers + coherent presets needs the
human fork). The P0 warmup flag (#1555) is the most significant overnight change —
a default-off mechanism that, when the semantics decision is made, will shift
every backtest baseline.

## QC Status

| Track | Structural | Behavioral | Overall | Quality | PR |
|-------|-----------|-----------|---------|---------|----|
| (none dispatched) | — | — | — | — | — |

No orchestrator QC this run. Open maintainer PR #1558 was **not** QC'd — its CI is
red (file-length), so it is not mergeable regardless of QC; pushing a QC/rework
commit onto the maintainer's active core branch was declined (collision/presumption).

## Budget

- Budget cap: $50.00 (`dev/config/merge-policy.json`); target 60–80% ($30–$40).
  `auto_merge_enabled: true`.
- Subagents spawned this run: **0** (no eligible non-colliding dispatch).
- Any subagent killed mid-flight: **No**.
- Orchestrator overhead: 1 `dune build` (exit 0) + 1 full `dune runtest` (exit 0)
  on `d4ea7bb2`, ~12 GitHub REST/log calls (open PRs, main + PR check-runs, PR
  #1558 detail + files + reviews, failing-job log fetch, ci-red issues), Step 5.5
  reconcile (`_index.md` header + backtest-infra row), health report, this summary.
- **Estimated cost this run (estimated):** ~$1–2 (orchestrator state-read +
  build/runtest + REST; no subagents). Measured figure lands post-run in
  `dev/budget/2026-06-13-${GITHUB_RUN_ID}.json` (GHA capture step).
- Utilization assessment: **UNDER_TARGET** (~$1–2 / $50 ≈ 2–4%). **All-work-done:**
  every GHA-eligible forward item is data-gated, maintainer-fork-gated, or collides
  with the in-flight maintainer PR #1558. **No eligible track was withheld for
  budget/queue-depth reasons.**
- Scope reduced due to budget: No.

## Data Operations

(omitted — ops-data not dispatched; `EODHD_API_KEY` absent in GHA;
`dev/notes/data-gaps.md` unchanged since 2026-05-03.)

## Harness Work

(omitted — harness-maintainer not dispatched; no eligible T1/T3 item.)

## Health Scan

Deterministic Step 6 checks (`dev/health/2026-06-13-fast.md`):

- **Result: CLEAN** (main)
- Build gate (6.1): `dune build` exit 0 + full `dune runtest` exit 0 on
  `d4ea7bb2`; `0` `^FAIL:` lines. (`find: .../_build/.sandbox/...` + `File "."`
  lines are benign dune-sandbox / workspace-root notices.)
- Status integrity (6.2): covered by the green `dune runtest`.
- Index-size linter (post Step 5.5 reconcile): exit 0 (6015/20480 bytes; longest
  row 237 ≤ 250).
- **File-length trend watch (leading indicator):** `all_eligible_runner.ml` 495,
  `price_path.ml` 498, `runner.ml` 491, `portfolio.ml` 489, `simulator.ml` 485 —
  all under the 500 limit on main (PASS) but creeping. PR #1558 already pushed
  `runner.ml`/`simulator.ml` over (504 each). Recommend a human-scheduled proactive
  extraction pass; the orchestrator did NOT auto-refactor these (core modules the
  maintainer is actively editing — collision risk).

## Follow-up Queue

- **#1558 file-length unblock** (maintainer): extract a helper from `runner.ml`
  and `simulator.ml` (both 504 → <500); no behavior change.
- short-side economic-floor experiment: WF-CV the `short_min_price` axis (0.0 vs
  ~17.0) before any default flip; Phase-2 `margin_call` `TriggerExit` dedup bug
  (short-side-strategy.md Finding A) still open.
- P0 warmup-trading semantics decision (maintainer/human): flag (#1555) landed;
  decide indicators-only vs running-start, re-pin goldens behind the flag.
- `enable_short_side` default flip — needs ledger evidence (R3), not autonomous.
- Data-gated: P2 matrix, cascade-inversion broad-universe re-run, dot-com/GFC
  regime read — all need maintainer-local top-3000 PIT warehouses + GHA
  `EODHD_API_KEY`.

## Integration Queue

- **Empty.** Zero orchestrator PRs this run. The one open PR (#1558) is
  maintainer-authored and CI-red (file-length) — not an orchestrator merge
  candidate.

## Current Milestone Target

- **P0 warmup-trading semantics** — flag surface landed (#1555); the
  semantics-decision + golden re-pin is the maintainer fork that unblocks the
  definitive matrix.
- **Strategy lever surface — empirically exhausted** for edge-seeking; redirect to
  tail-preserving levers + coherent presets needs the human fork.
- **Next capability milestone (unowned):** M6.6 live cycle.

## Dependency Unlocks

- `suppress_warmup_trading` flag (#1555) now available as a backtest config
  surface — unblocks the P0 comparison run once the maintainer decides semantics +
  agrees to re-pin goldens.

## Escalations

(All `[info]` / `[critical-decision-needed]` — **no bare `[critical]`.** Main is
GREEN; the deterministic Step 6 build gate exited 0 with live evidence in §Health
Scan on `d4ea7bb2`, and the one red signal (PR #1558) is confined to that PR's
branch, not main — so no `[critical]` build/linter assertion is warranted.)

- [info] **Carried-forward verification: no still-real bare `[critical]` items;
  main GREEN** (`dune build` + `dune runtest` exit 0 on `d4ea7bb2`; full CI green;
  0 open `ci-red` issues).
- [high] **Maintainer PR #1558 blocked on a trivial file-length overage.** Sole CI
  failure is `FAIL: file length linter`: `trading/backtest/lib/runner.ml` 504 and
  `trading/simulation/lib/simulator.ml` 504, both over the 500 declared-large hard
  limit (#1558 adds +13 / +20 net lines respectively). No test/behavioral failures.
  Fix = extract one cohesive helper block from each file (no behavior change) →
  under 500 → re-run CI. The orchestrator did **not** push onto the active
  maintainer branch (deliberately-structured core PR, #1557 item 1; collision +
  presumption risk outweighs a ~5-min extraction). Not a bare `[critical]` — main
  is green; the red is confined to the PR.
- [info] **File-length trend on main** (code-health-discipline leading indicator):
  `price_path.ml` 498, `all_eligible_runner.ml` 495, `runner.ml` 491,
  `portfolio.ml` 489, `simulator.ml` 485 — all under 500 (PASS) but close. A small
  additive PR to any will break CI (as #1558 just demonstrated). Recommend a
  human-scheduled extraction pass; these are maintainer-active core modules so the
  orchestrator did not speculatively refactor passing files.
- [critical-decision-needed] **P0 warmup-trading semantics** (carried; flag now
  landed #1555) — decide warmup=indicators-only (re-pin every golden behind the
  flag) vs intentional running-start (segregate inherited positions). Core-backtest
  + golden-repin fork → maintainer-owned; blocks the definitive matrix. Not a bare
  `[critical]`.
- [info] **`enable_short_side` default is still `true`.** Flipping the global
  default needs a ledger ACCEPT + confirmation grid; the `short_min_price` axis
  (#1551) is the disciplined evidence path.
- [info] **Broad-universe / broad-PIT data is the single highest-value unblock —
  still GHA DATA-GATED.** Adding `EODHD_API_KEY` as a GHA secret + staging the deep
  top-3000 PIT snapshot warehouse would let the orchestrator run the P0 comparison,
  the P2 matrix, and the cascade-inversion broad-universe validation autonomously.
  (carried.)
- [info] **trade-forensics LOW items held** pending human opt-in (post-exit capture
  ratio / auto-stage_chart render). GHA-eligible OCaml reporting tooling. (carried.)
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (carried).
- [info] **15y split-day adjustment regression on `weinstein_strategy.ml`**
  (carried) — #998 root-caused; fix pending; touches core → scope decision.
- [info] **Orchestrator GH token scope gap + no `gh`/`jst` in GHA** (carried) —
  PRs/merges/reviews done via `curl` REST + GraphQL; token lacks `actions:write` +
  `issues:write`. No occurrence this run (no dispatch / merge).
- [critical-decision-needed] **M6.6 live cycle** (carried) — live `DATA_SOURCE` +
  cron + alerts + trading-state durability; the next capability milestone, unowned.
  Not a bare `[critical]`.

## Questions for You

1. **#1558 file-length unblock** — the maintainer PR is red only on file-length
   (`runner.ml`/`simulator.ml` both 504 > 500). Do you want to fix the extraction
   yourself (it's your deliberately-structured PR), or should a future orchestrator
   run push a code-health extraction commit onto the branch? (The orchestrator
   design permits the latter via Step 5a, but declined this run to avoid colliding
   with your active core branch.)
2. **P0 warmup-trading semantics** — the flag (#1555) landed default-off. Decide
   (a) warmup = indicators-only (re-pin every golden), or (b) intentional
   running-start (segregate inherited positions). Core-backtest fork; blocks the
   definitive matrix.
3. **EODHD_API_KEY in GHA** (carried — still the top concrete unblock). Adding it +
   staging the deep top-3000 PIT warehouse would let the orchestrator run the P0
   comparison + P2 matrix + cascade-inversion validation autonomously.
4. **Proactive file-length extraction pass?** Five core/orchestration modules are
   within 2–15 lines of the 500 hard limit on main. Worth a scheduled cleanup
   before the next additive PR breaks CI?
5. **M6.6 live cycle** (carried) — schedule it, or keep deferred?
6. **Grant the orchestrator token `actions:write` + `issues:write`?** (carried).

## Per-run links

- [run 1] `dev/daily/2026-06-13.md` (this file) — FULL PASS / 0 DISPATCH. Main
  GREEN on `d4ea7bb2`. Maintainer shipped #1554–#1556 + warmup flag #1555
  overnight; open maintainer PR #1558 CI-red on file-length (runner.ml +
  simulator.ml both 504 > 500) — surfaced for the maintainer, not collided with.
  All other forward work data-gated / human-fork-gated. Health CLEAN. Reconciled
  `_index.md`. 0 subagents (~$1–2 estimated).

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
