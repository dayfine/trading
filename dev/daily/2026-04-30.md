# Status — 2026-04-30
Run timestamp: 2026-04-30T08:05:00Z
Run ID: 2026-04-30-run-1

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-perf | dispatched | feat/backtest-g6-decade-nondeterminism | #703 | QC APPROVED (structural + behavioral, quality 5) — awaiting human merge. Investigation note + forward-guard regression test only; suspected primary leak site (`trading/orders/lib/create_order.ml:_generate_order_id`) flagged for follow-up dispatch (core module, out of scope for feat-backtest). |
| short-side-strategy | blocked | — | — | sp500-2019-2023 short-side validation rerun is local-only (needs 491-symbol universe). G7 (#702) closed the last gap in the G1–G7 chain; rerun is the next concrete step but cannot run in GHA. Maintainer-local action item. |
| short-side-strategy | blocked | — | — | G4 long-side extension + portfolio-peak threshold (per `dev/notes/session-followups-2026-04-29-evening.md` §2). Cross-cutting; A1 expected. Touches core `Trading_portfolio` — needs feat-weinstein or core-owner agent. Deferred this run on budget margin + "let G4 land first then extend" sequencing. |
| simulation | blocked | — | — | Split-day work fully wrapped. M5 walk-forward + tuner catch-all items remain. Maintainer-local sp500 baseline rerun is the documented follow-up. |
| backtest-perf | blocked | — | — | §Decision items (4 human/QC sign-off questions); tier-4 first manual `workflow_dispatch` for canonical baseline (local-only); N≥5000 release-gate stays P1 awaiting daily-snapshot streaming. |
| hybrid-tier | blocked | — | — | Option 1 engine-pool DONE 2026-04-28. Option 2 daily-snapshot streaming P1 future work (~3,000 LOC across 5-8 PRs per `dev/plans/daily-snapshot-streaming-2026-04-27.md`). |
| harness | blocked | — | — | Backlog drained. Yesterday-evening session followups §4 (run-in-env.sh fallback warn) and §5 (qc-structural gh pr view enforcement) closed via maintainer-driven PRs #699 and #700 today. |
| orchestrator-automation | blocked | — | — | Phase 2 pending. Carried [info]s: H3 false-positive on advisory linter text (not yet fixed); review-file persistence gap (review-file write persisted on this run, no recurrence). Carried orchestrator-merge-race [info] from 2026-04-29 run-2 did NOT recur this run. |
| cleanup | blocked | — | — | Backlog still empty. Bin-file refactor candidate (`optimal_strategy.ml` 484 LOC) carried from 2026-04-29 run-1. |
| cost-tracking | blocked | — | — | Run-1 measured cost lands in `dev/budget/2026-04-30-<run_id>.json` post-GHA-capture. |
| ops-data | skipped | — | — | `dev/notes/data-gaps.md` unchanged since 2026-04-14 (sentinel rule). |
| trade-audit | — | — | — | MERGED. No work this run. |
| optimal-strategy | — | — | — | MERGED. All planned phases landed 2026-04-28..29 (PR-1..PR-5 + macro-trend wiring + lib extraction). |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-perf | feat-backtest | completed | G6 investigation + forward-guard regression test on `feat/backtest-g6-decade-nondeterminism`. PR #703 opened (4 files, +536/-3). Identified suspected primary leak site at `trading/orders/lib/create_order.ml:_generate_order_id` (timestamp-prefixed IDs → Hashtbl bucket order in `manager.ml` → fill iteration order → metric divergence on long-horizon × broad-universe scenarios). Did NOT apply fix — out-of-scope (core module per A1 watch-list). |
| backtest-perf | qc-structural | APPROVED | All hard gates PASS; P6 test-pattern sub-rules PASS; A1/A2/A3 PASS. PR file list verified via `gh pr view` REST shape per harness PR #700. Review at `dev/reviews/backtest-perf.md` (Reviewed SHA `e39bda57…`). |
| backtest-perf | qc-behavioral | APPROVED | Quality Score: 5. Pure infra/investigation/test PR per `qc-behavioral-authority.md` skip rule — S*/L*/C*/T* rows all NA. CP1–CP4 all PASS (PR-body claims map to committed tests; isolation property pinned via element-wise `equal_trade`; suspected leak narrative spot-verified against source). |
| backtest-perf | — | audit written | `dev/audit/2026-04-30-backtest-perf.json` (overall=APPROVED, structural=APPROVED, behavioral=APPROVED, quality=5, consecutive_rework_count=0). |
| backtest-perf | — | draft→ready flip | NA — agent opened DRAFT then marked ready-for-review at session end before QC ran. PR was already non-draft when reviewed. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since 2026-04-14 (sentinel skip per Step 2d). EODHD_API_KEY also unset in GHA env. |
| harness | — | skipped | No dispatchable T1/T3 items (backlog drained). PRs #699 + #700 today were maintainer-driven, not orchestrator-dispatched. |
| cleanup (Step 2e) | — | skipped | §Backlog empty AND no new `[medium]`/`[high]` findings this run. |

## Feature Progress

### backtest-perf [IN_PROGRESS — PR #703 awaiting merge]

- **Maintainer activity since 2026-04-29 run-2 summary commit (13:27Z) → this run (08:05Z):** 28 PRs merged in a heavy cycle. Headlines:
  - **Short-side gap chain G1–G7 fully closed**: G5 audit harness (#689), G2 metrics short pairing (#690), G1 short-stop diagnosis + minimal fix (#691), test-determinism baseline regen post-G2 (#692), G3 cash floor on shorts (#694), G4 force-liquidation policy (#695), G7 short position sizing (#702). The full chain from `dev/notes/short-side-gaps-2026-04-29.md` + `dev/notes/sp500-shortside-rerun-blocked-g7-2026-04-30.md` is now landed on main.
  - **Split-day work fully wrapped**: #678 (strategy-side `Position.t` sync), #679 (verification harness), #680 (Weinstein stop_states rescale), #682 (short-side flag for sp500 long-only mitigation). Combined with the 2026-04-28 redesign chain (#658/#662/#664/#667), the 97.69% phantom MaxDD trail is structurally closed.
  - **Optimal-strategy track wraps**: #672 (lib extraction), #673 (run-2 follow-up B fix), #676 (macro read side), #677 (release-report integration), #684 (status wrap + N=1000×10y RSS), #672 (lib extraction). Track flipped to MERGED.
  - **Goldens-broad N=1000 baselines pinned long-only**: #683 (universe_cap sexp option), #687 (canonical baselines for 4 cells: bull-crash, covid, six-year, decade). `decade-2014-2023` showed 145 vs 135 trades across run modes — surfaced as G6 (the dispatch target this run).
  - **N=1000×30y capacity validation**: #697 confirmed peak 6.3 GB / wall 32m — clean for the 8 GB ubuntu-latest ceiling.
  - **Harness fixes**: #675 (bundle-budget checkout), #685 (run-in-env docker probe + worktree path), #686 (worktree auto-cleanup + SessionStart hook), #699 (run-in-env stderr WARN), #700 (qc-structural mandate `gh pr view` for PR file enumeration — closes 2026-04-29 evening session followups §5).
  - **Docs hygiene + new gap notes**: #688 (evening followups), #693 (data availability audit), #696 (historical universe membership gap), #698 (QC reviews 695-696-697), #701 (G7 finding from sp500 rerun).

- **This run dispatched**: feat-backtest on G6 (cross-cell metric divergence on `goldens-broad/decade-2014-2023`). The agent:
  - Wrote `dev/notes/g6-decade-nondeterminism-investigation-2026-04-30.md` (268 LOC) — full code-path audit of `scenario_runner.ml`'s fork-per-cell flow.
  - Added `trading/trading/backtest/scenarios/test/test_scenario_runner_isolation.ml` (215 LOC) — three sub-tests pinning `round_trips` bit-equality and `final_portfolio_value` within 1e-9 across "target standalone" vs "target after a different scenario" run modes, on GHA-sized fixture pair (`panel-golden-2019-full` + `tiered-loader-parity`).
  - Updated `dev/status/backtest-perf.md` §Completed.
  - **Did NOT apply a fix** — suspected primary leak site is `trading/orders/lib/create_order.ml:_generate_order_id` (timestamp-prefixed IDs → Hashtbl bucket order in `trading/orders/lib/manager.ml` → `process_orders` iteration order → fill order under cash-floor / sizing edges → divergent metrics on long-horizon × broad-universe). That site is in the A1 core-module watch-list (`trading/trading/orders/`), out of scope for feat-backtest. Agent followed the dispatch contract and stopped with a §Findings summary.

- **Test reproduction caveat**: the test passes on the GHA-sized fixture pair (22 symbols × 15 months ≈ 1300 cell-Fridays). The bug requires the ~520k cell-Friday surface that only the decade × N=1000 cell exhibits. Test is therefore a **forward guard** — it pins the property `metric_record(single) == metric_record(batch)` which holds today on small windows and would catch any regression that breaks isolation more aggressively.

- **QC**: structural + behavioral both APPROVED, quality score 5. No findings.

### simulation [IN_PROGRESS — split-day fully wrapped]

- **Status file refreshed** this run: `## Status` heading no longer claims `fix/split-day-broker-model-debug` is a DRAFT PR (that PR merged as #678 yesterday, ~19h before this run). `## In Progress` block consolidated: split-day broker-model + Weinstein stop-state rescale + sp500 short-side flag mitigation all listed as wrapped with PR numbers; M5 walk-forward + tuner remains the residual.
- Track stays IN_PROGRESS for M5 work and the local sp500 baseline rerun.

### short-side-strategy [MERGED — G7 closure today]

- G7 (`feat/g7-short-position-sizing`, PR #702) merged 2026-04-30T00:59:21Z. The leaking surface was `Portfolio_risk.compute_position_size` (risk-budget formula `shares = floor(dollar_risk / |entry - stop|)` was unbounded; `max_long/short_exposure_pct` knobs existed but were only consumed by `Portfolio_risk.check_limits`, which is **never called from the live entry pipeline**). Fix: added `~side` parameter to `compute_position_size`; final share count is now `min(risk_based_shares, exposure_capped_shares)`.
- The `enable_short_side = false` override stays in place on `goldens-sp500/sp500-2019-2023.sexp`. The validation rerun with shorts re-enabled is the next concrete step but **local-only** (491-symbol universe data not available in GHA). When the rerun lands and shows G7 sufficiency, override is reverted, `expected` ranges re-pinned against shorts-on numbers, and G5 added as the standing regression gate.
- Pending non-rerun follow-ups: (a) G4 long-side extension + portfolio-peak threshold per `dev/notes/session-followups-2026-04-29-evening.md` §2; (b) covid-recovery 20.81% WR investigation deferred per §3 until baseline lands.

### harness [IN_PROGRESS]

- Two maintainer-driven harness PRs landed this morning, both addressing yesterday-evening session followups:
  - **#699** `run-in-env.sh` stderr WARN on docker-inspect fallback + header docstring sync — closes session-followups §4.
  - **#700** qc-structural mandate `gh pr view --json files` for canonical PR file enumeration — closes session-followups §5 (the qc-structural false-positive pattern from PRs #677 and #687, where the agent walked git ancestry and miscounted cumulative commits' files instead of the PR's actual diff).
- The qc-structural agent on this run's PR #703 used the mandated `gh pr view` shape (visible in its return summary: "PR file list verified via `gh pr view 703 --json files`") — first run after the PR #700 update, behaviour confirmed.
- Backlog still drained on T1/T3.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| backtest-perf | #703 (open) | APPROVED | APPROVED | 5 | dev/audit/2026-04-30-backtest-perf.json (consecutive_rework_count=0) |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched: 3 (1 feat-backtest + 1 qc-structural + 1 qc-behavioral)
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | feat-backtest (G6 investigation) | sonnet | completed | ~230K | est. ~$3.50 |
  | qc-structural (PR #703) | sonnet | completed | ~72K | est. ~$1.20 |
  | qc-behavioral (PR #703) | sonnet | completed | ~77K | est. ~$1.30 |
- Any subagent killed mid-flight: No.

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-04-30-<run_id>.json` post-GHA-capture (estimate not visible during this session).

- Total (estimated): ~$6 for subagents + ~$1 orchestrator overhead = **~$7 of $50 cap (~14%)**.
- Utilization assessment: **UNDER_TARGET** (<60%). All eligible tracks dispatched; no remaining actionable GHA-runnable work this window. Status: "all-work-done" — sp500 rerun is local-only, G4 extension deferred on sequencing (let G4 land then extend), G6 fix dispatch deferred on core-module ownership (orders is A1-flagged; needs feat-weinstein or core-owner agent which the orchestrator doesn't currently dispatch).
- Scope reduced due to budget: No — under-target due to actionable-work-floor, not budget pressure. Yesterday's cumulative spend (~$45) gave way to today's fresh quota; the orchestrator deliberately scoped tight given the heavy maintainer-driven cycle that just landed (28 PRs in 19h).

## Data Operations

(ops-data did not run — `dev/notes/data-gaps.md` unchanged since 2026-04-14, EODHD_API_KEY unset.)

## Harness Work

(harness-maintainer did not run this run — backlog has no dispatchable T1/T3 items. PRs #699 + #700 — both maintainer-driven harness PRs addressing yesterday-evening session followups — were not orchestrator-dispatched.)

## Health Scan

From `dev/health/2026-04-30-fast.md` (deterministic per Step 6):
- `dune build && dune runtest` exit **0**.
- `status_file_integrity.sh` exit **0** after this run's `simulation.md` schema fix (the `## Status` heading was prose-formatted; reformatted to bare-keyword + descriptive paragraph below).
- Advisory linter `linter_magic_numbers.sh` continues to emit 3 false-positives on docstring-embedded numerals (`stops_runner.ml:76,77.49`, `optimal_strategy_runner.ml:671`); none gate (exit 0). Carry as [info].
- Result: **CLEAN** (build gate).

## Follow-up Queue

(Top-level — total open follow-up items across all tracks: ~7.)

- **G6 fix dispatch** (NEW from this run): `trading/orders/lib/create_order.ml:_generate_order_id` mints order IDs with a `Time_ns_unix.now()` ns-precision prefix; downstream `Hashtbl.fold` iteration order in `trading/orders/lib/manager.ml` varies by bucket placement of those keys, producing divergent `process_orders` order across cells with different prior wall-clock state. Out of scope for feat-backtest this run; needs feat-weinstein or core-owner agent. The forward-guard test in PR #703 pins the property post-fix.
- **G4 long-side extension + portfolio-peak threshold** (carried, ex `dev/notes/session-followups-2026-04-29-evening.md` §2): the merged G4 force-liquidation policy is short-side defensive; long-side gaps revealed by goldens-broad baselines (decade 94% MaxDD with +1582% return) suggest the same machinery should fire on long-side unrealized loss + portfolio-peak floor. Cross-cutting; A1 expected. Touches core `Trading_portfolio`. Deferred this run.
- **sp500 short-side validation rerun** (post-G7): local-only (needs 491-symbol universe). Action: maintainer reruns + reports metrics; orchestrator picks up baseline re-pinning afterwards.
- **covid-recovery 20.81% WR investigation** (deferred until short-side baseline lands per session-followups §3).
- **qc-structural recurring H3 false-positive on advisory linter text** (carried from 2026-04-29 run-1+run-2): not yet fixed; pattern persists. Did not trigger this run because PR #703 had no advisory linter output near the diff surface.
- **qc-structural review-file persistence gap** (carried from 2026-04-29 run-1+run-2): the structural agent reported writing the review file in run-1 of yesterday but the file was unchanged. Persisted normally on this run's PR #703 (review-file write confirmed). Worth one more confirming dispatch before declaring fixed.
- **Bin-file refactor candidate** (carried from 2026-04-29): `trading/trading/backtest/optimal/bin/optimal_strategy.ml` (484 LOC monolithic). Append to `dev/status/cleanup.md` §Backlog by next health-scan dispatch.

## Integration Queue

- **#703** `feat(backtest): G6 cross-cell isolation forward-guard test + investigation note` — feat-backtest. 4 files, +536/-3. structural + behavioral both APPROVED, quality score 5. Awaiting human merge.

(Auto-merge remains disabled (`auto_merge_enabled: false`) for feature PRs; this orchestrator's run-1 summary PR auto-merges per Step 8a.)

## Current Milestone Target

M5 (backtest infrastructure complete + tunable) — incremental progress this run:
- ✅ **G1–G7 short-side gap chain fully closed** (PRs #689–#695 + #702). The systematic short-side bug surface that surfaced in 2026-04-29's sp500 rerun (-144.5% return / 245.8% MaxDD) is structurally addressed across stop direction, metrics pairing, cash floor, force-liquidation policy, audit harness, and position sizing.
- ✅ **Split-day broker-model fully wrapped** (#678 / #679 / #680 / #682 in addition to the 2026-04-28 redesign chain). Cross-side `Position.t` consistency between broker and strategy view pinned with regression tests.
- ✅ **Goldens-broad long-only baselines pinned at N=1000** for 4 cells (#687).
- ✅ **N=1000×30y capacity validated** at 6.3 GB peak / 32m wall (#697) — clean for the 8 GB ubuntu-latest ceiling.
- ✅ **G6 forward-guard test** (this run, PR #703) — bit-equality of metric records across single-cell vs batch run modes is now pinned. Suspected primary leak diagnosed but fix deferred (out-of-scope).
- ⏭ G6 fix dispatch (orders module — needs core-owner) is the next sequential dispatch.
- ⏭ sp500 short-side baseline rerun (local-only) is the next maintainer action.
- ⏭ G4 long-side extension + portfolio-peak threshold remains queued.
- ⏭ Daily-snapshot streaming (Option 2 of hybrid-tier) for N≥5000 release-gate stays P1.

## Dependency Unlocks

- **#702 unblocks sp500 short-side rerun**: G7 (compute_position_size now caps shares by `max_short_exposure_pct`) closes the last short-side gap surfaced in `dev/notes/sp500-shortside-rerun-blocked-g7-2026-04-30.md`. Maintainer can now re-enable shorts on `goldens-sp500/sp500-2019-2023.sexp` (drop the `enable_short_side false` override) and rerun to validate.
- **#700 unblocks qc-structural file-list accuracy**: the mandate to use `gh pr view --json files` (not git-log walks) closes the PR-scope confusion that mis-flagged PRs #677 and #687. PR #703's qc-structural review used the new shape cleanly — first observation post-mandate, behaviour confirmed.

## Escalations

[info] **G6 fix surface is in `trading/orders/lib/create_order.ml` (core module)** (NEW this run). The forward-guard test is in flight as PR #703; the fix itself was deferred because the leaking module is on the A1 core-module watch-list. Concretely: `_generate_order_id` mints IDs with a ns-precision wall-clock prefix; downstream `Hashtbl.fold` iteration in `manager.ml`'s `list_orders` (called from `process_orders`) returns those orders in bucket-placement order, which varies by ID. Different prior wall-clock state across cells → different bucket placements → different fill order → metric divergence on long-horizon × broad-universe scenarios. Two design choices for the fix: (a) replace the timestamp prefix with a deterministic per-process counter (cheapest, but loses the "human-readable timestamp in ID" property); (b) replace `Hashtbl` with a sortable structure (Map.t) and have `list_orders` return in deterministic order regardless of ID. Owner: feat-weinstein or core-owner agent. Process question: which feat-agent should own this? Today's orchestrator does not have a distinct "core-owner" agent; feat-weinstein has historically owned `trading/trading/portfolio/`-adjacent changes via A1 flag review.

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried from 2026-04-29 run-1+run-2). Permanent fix still pending: tighten the agent's prompt to explicitly cite Step 6.1 of `lead-orchestrator.md` ("trust only the exit code, not stdout `FAIL:` strings") and require running `dune runtest 2>&1 | tail -3 ; echo "exit=$?"`. Did not trigger this run because PR #703's diff surface had no advisory linter output local to the diff.

[info] **qc-structural review-file persistence gap** (carried from 2026-04-29 run-1+run-2). Did not recur this run — `dev/reviews/backtest-perf.md` was correctly persisted by the structural agent (confirmed file modification visible during dispatch), and the behavioral agent appended without overwriting. Worth one more confirming dispatch before declaring fixed.

[info] **Bin-file refactor candidate** (carried from 2026-04-29): `trading/trading/backtest/optimal/bin/optimal_strategy.ml` (484 LOC monolithic — but per #672 lib extraction this is now ~35 LOC). The original 484-LOC concern may be obsolete; revisit when next deep scan emits a new finding.

[info] **Cumulative day spend low (~14% of $50 cap)**. UNDER_TARGET assessment per Step 3.75c. All eligible tracks dispatched; no remaining actionable GHA-runnable work this window (sp500 rerun is local-only, G4 extension deferred on sequencing, G6 fix needs core-owner). This is "all-work-done" rather than "scope reduced" — yesterday's heavy maintainer-driven cycle (28 PRs in 19h) consumed most of the actionable-work-now surface; the queue is naturally light today.

## Step 1b drift cross-reference

Most recent prior summary `dev/daily/2026-04-29-run2.md` (committed 2026-04-29T13:27:39-04:00 = 13:27Z via PR #674, merged ~13:27Z). Pending work table parsed:

- `optimal-strategy: awaiting-merge #673`: merged 13:17Z (within minutes of run-2 commit). Not drift — normal merge cadence.
- All other tracks were `blocked` or `merged` in run-2's table; no dispatched-but-unmerged followups to track.

No `[drift]` warnings emitted. The 28 PRs that landed between 2026-04-29 13:27Z and this run's 08:05Z (~19h elapsed) were all maintainer-driven feature work or docs hygiene, not orchestrator-dispatched work waiting on agent return. The dispatch system's idempotency guarantees held cleanly (no double-dispatch, no orphan branches).

## Step 1c carried-forward critical verification

Run-2 of 2026-04-29 carried zero `[critical]` items into today (all `[info]`). Run-1 of today carries forward zero `[critical]` items. Build gate re-verified clean (exit 0); status integrity also clean post this run's `simulation.md` schema fix.

## Per-run links

- [run 1] dev/daily/2026-04-30.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
