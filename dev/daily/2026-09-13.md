# Status — 2026-09-13

**Run ID:** 2026-09-13-run-1
**Run timestamp:** 2026-09-13T12:44:26Z (GHA orchestrator run 34757914354)
**Mode:** FULL
**Main at run start:** `8c7d0ada`

Step 0.5 did not fire. The workflow supplied `QUEUE_NON_EMPTY=1` (PR #2769), and
Condition 1 failed — the one open PR had no `Reviewed SHA`, so its tip was
unreviewed. A full pass was required.

---

## Headline — the orchestrator was dead for three days, and the cause was not in this repo

Six consecutive scheduled runs failed, 2026-09-10 through 09-12:

| run | created | Claude Code build | result |
|---|---|---|---|
| 34378061508 | 09-09 16:39Z | **2.1.266** | success |
| 34475636575 | 09-10 12:14Z | **2.1.267** | failure |
| 34501930703 | 09-10 16:24Z | — | failure |
| 34597781805 | 09-11 12:13Z | — | failure |
| 34622262330 | 09-11 16:29Z | **2.1.268** | failure |
| 34691534671 | 09-12 11:37Z | — | failure |
| 34702540272 | 09-12 15:32Z | **2.1.269** | failure |
| **34757914354** (this run) | 09-13 12:44Z | **2.1.270** | running |

Zero daily summaries, zero fast health scans, zero QC audits for three days.
Every failed run died in the `Run lead-orchestrator` step after 12–16 seconds
with this result record:

```json
{ "type": "result", "subtype": "success", "is_error": true,
  "duration_ms": 506, "num_turns": 1, "total_cost_usd": 0,
  "permission_denials_count": 0, "modelUsage": {} }
```

`modelUsage: {}` with `num_turns: 1` and a half-second duration means the SDK
failed **before issuing a single model request**. That is why the cost was
exactly `$0.0000` and why there was no diagnosable error text.

### The pacer's lead was sharp, and wrong — and disproving it was the work

The 2026-09-13 track-pacer report (PR #2769, merged this run) leads with
**PR #2757** as the suspect: it is the only change to
`.claude/agents/lead-orchestrator.md` or `.github/workflows/` since 09-01, and
it merged at 07:07Z on 09-10 — inside the gap between the last funded slot and
the first dead one. The report was careful to call it "a lead, not a verdict."

It is disconfirmed:

```
$ git diff --stat 68cd3b8c origin/main -- .claude/agents/ .github/workflows/
(empty)
```

`68cd3b8c` is the head of the **first dead run**; today's successful run is at
`8c7d0ada`. The agent definition and every workflow file are **byte-identical
across that boundary**. The same content failed six times and then succeeded.
**A cause that is constant across a failure→success transition is not the
cause.**

What *did* vary, monotonically and in lockstep with the outcome, is the Claude
Code build — and the action installs it **unpinned**, picking up whatever
upstream shipped on each scheduled slot. Filed as **#2770**.

This is an attribution, not a proof: a coinciding server-side condition cannot
be excluded from run logs alone. But the natural experiment is about as clean
as this evidence gets — repo content held constant across failure→success, the
runtime version varied — and the corrective action is identical under either
reading: **pin the runtime, and alert on orchestrator failure.** The ask is
recorded in #2770; the pin itself is blocked on the `workflow` scope.

---

## The watchers were all down, and one of them is blind by construction

For the week in which the orchestrator died, none of the three instruments that
would have said so were working.

**1. The scheduled-workflow health detector was never being called — and the
reason was a category error.** `trading/devtools/checks/scheduled_workflow_health.sh`
(#2634/#2663) shipped on 09-04 as "the script half only", with its wiring
blocked on a `workflow`-scoped PAT that has been refused on every route. But
the blocked half is *wiring it as its own cron*; **the script itself needs only
API read access, which the orchestrator already has.** Nothing had ever called
it. It now runs every pass:

```
active=17  ok=8  red=2  stale=0  no-schedule=7   (exit 0)
RED: "Prune candidates weekly", "Weekly start sweep (BAH SPY)"
```

A capability gap on one half of a feature had been silently treated as a block
on the whole feature, for nine days, across the exact window it was built for.

**2. And when finally run, it reported `Daily orchestrator` = `OK`** — in the
middle of the six-run failure streak. It inspects only the single most-recent
scheduled run, and that run was *this orchestrator's own `in_progress`
execution*, which it classifies as OK. **Whenever the orchestrator runs this
detector on itself, its own in-flight run masks its own health,
unconditionally.** The one workflow most in need of monitoring was the one the
script structurally could not report on from inside itself. Fixed and merged
this run — see #2772 below.

**3. The weekly deep health scan ran on 09-07, reported every step successful,
and produced nothing.** Filed as **#2771**. The publish step was not at fault —
it emitted `No changes in dev/health/ — deep scan produced no output` and
exited 0, which is a correct guard. The break is one level up: the
health-scanner agent returned `is_error: false` after **31 turns, $0.39, and 98
seconds** and never wrote its report. 98 seconds is not a plausible deep scan of
this codebase. The newest deep report on main is still `2026-08-31-deep.md`,
now 13 days old.

Two RED weekly workflows remain (`Prune candidates weekly`, `Weekly start sweep
(BAH SPY)`), both last fired **2026-09-07** — before their fix (#2725) merged.
They cannot produce a new datapoint before their next weekly cron, so they were
**not** re-derived this run. A probe whose input can only change weekly does not
belong in a twice-daily pass.

### One shape, three defects, one month

- **#2741 / D2** — the orchestrator wrote its daily summary and never pushed it.
  Five runs, all `conclusion: success`, $20.18 on one of them, zero PRs.
- **#2747 / D3** — the orchestrator ended its turn mid-dispatch; three agents'
  work destroyed. Run green.
- **#2771** — the deep scanner ran, reported success, wrote nothing. Run green.

In all three, **a green run is indistinguishable from a productive one, because
nothing checks the artifact against the exit code.**
`orchestrator_fastexit_gate.sh verify` (#2579) is the only place this repo has
mechanized that check — and it exists precisely because prose did not bind.

---

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | harness-maintainer | **completed → PR #2772** | streak-aware health check, unmasked from in-progress runs |
| post-run-validation | feat-backtest | **completed → PR #2773** | `Series_level` build-time price-level sanity |
| harness (#2772) | qc-structural | **APPROVED (5)** | reproduced the before/after independently |
| harness (#2772) | qc-behavioral | **NEEDS_REWORK (2)** | 4 documented contracts survived mutation with the suite green |
| harness (#2772) | harness-maintainer | **completed** | rework iteration 1 — 20 → 26 assertions |
| harness (#2772) | qc-structural | **APPROVED (5)** | re-review at `10fb1dc7`; additive-only, no scope creep |
| harness (#2772) | qc-behavioral | **APPROVED (4)** | reproduced all six mutations digit-for-digit |
| post-run-validation (#2773) | qc-structural | **NEEDS_REWORK — VOID** | exit 137 SIGKILL; **my dispatch error**, not a code defect (see below) |
| post-run-validation (#2773) | qc-structural | **APPROVED (5)** | completion re-run; gates 0/0/0, mutation claim reproduced |
| post-run-validation (#2773) | qc-behavioral | **NEEDS_REWORK (2)** | found a **false claim** in two durable records + an unexercised crash guard |
| post-run-validation (#2773) | feat-backtest | **completed** | rework iteration 1 — cross-module drift test added |
| post-run-validation (#2773) | qc-behavioral | **APPROVED (4)** | re-review at `0687955b`; all 5 mutations reproduced + 2 probes of its own |
| post-run-validation (#2773) | qc-structural | **APPROVED (4)** | re-review at `0687955b`; A2 determination on the new dependency |
| cleanup | — | **skipped** | §Backlog holds no actionable work — re-verified, unchanged since 09-09 |
| ops-data | — | **skipped** | `data-gaps.md` untouched since 2026-05-03 (**133 days**); `EODHD_API_KEY` absent |
| track-pacer (#2769) | — | **merged** | docs-only; both QC gates skipped per the carve-out; CI green |

### I caused a false NEEDS_REWORK, and it is worth naming

I dispatched two QC agents concurrently, each running a full OCaml build, on a
runner with **15 GB of RAM total**. One was OOM-killed (exit 137) and returned
NEEDS_REWORK on PR #2773 — a verdict entirely about my scheduling, not about
the code. Two facts disconfirmed it: **CI was green on the exact same SHA**, and
the reviewer itself had prescribed the resolution ("retry in a fresh
environment with adequate memory").

The important part is what I did *not* do: dispatch a rework. Acting on that
verdict would have sent an agent to "fix" a defect that did not exist — the
precise failure this week's whole lineage is about, a claim right about the
outcome and wrong about the mechanism. Every subsequent dispatch this run was
serialized or explicitly told not to run dune.

`container-capacity-scheduling.md` states the ceiling in memory and cores; on
this runner the binding constraint is **memory at 15 GB**, and two concurrent
OCaml links exceed it. That is now measured twice (disk on 09-09, memory
today).

---

## QC Status

Verdicts land as PR review **comments**, not approvals — GitHub blocks
self-approval when reviewer and author share an account, which is always the
case here.

| PR | structural | behavioral | outcome |
|---|---|---|---|
| **#2769** | skipped (docs-only) | skipped (docs-only) | **merged** `3f348c14` |
| **#2772** | APPROVED (5) @ `64eadf33`, again (5) @ `10fb1dc7` | NEEDS_REWORK (2) → **APPROVED (4)** @ `10fb1dc7` | **merged `ccf075be`** |
| **#2773** | NEEDS_REWORK **(VOID — OOM)** @ `fcb4f88c`, then APPROVED (5) @ `fcb4f88c`, again (4) @ `0687955b` | NEEDS_REWORK (2) @ `fcb4f88c` → **APPROVED (4)** @ `0687955b` | **merged `9b65f7cb`** |

Audit records: `dev/audit/2026-09-13-*.json`.

### Both behavioral passes earned their keep, and both did it the same way

Neither read an assertion and reasoned about it. Both **applied the mutation and
measured**, which is the discipline #2749 was created to establish.

**On #2772**, the reviewer found four documented contracts that survived
deletion or inversion with the suite fully green — including the `streak=>=N`
pagination floor branch **introduced by that very PR**, the trap the script's
own header devotes a section to. It also found a real documentation defect: the
header and SUMMARY documented `streak>=N` while the code has always emitted
`streak=>=N`, so a consumer grepping the documented literal would never match.

**On #2773**, the reviewer found something better: a claim that was **false as
written**. The PR asserted the median is computed identically to V18's "so the
two halves cannot disagree about one series." The *function* is byte-identical —
but the two feed it **different inputs**: `Series_level` drops non-finite closes
before the median, `_v18_median_close` does not, and `Float.compare` orders
`nan` below every real price. So on a NaN-carrying series they diverge — and the
module's own test constructs exactly such a series. The claim sat in **two
durable records**, the `.mli` and the status file.

It also **corrected its own line of attack** mid-review: it suspected V18's
`close` might be the adjusted basis, checked, found it is not
(`validator_artifacts.ml:193`), and narrowed its own finding accordingly. An
agent that withdraws its own stronger hypothesis on evidence is the control
actually working.

And on the re-review of #2772 it reproduced all six of the author's claimed
mutations **digit-for-digit**, including the failing-assertion numbers and a
"5 calls not 3" detail — then found one contract pinned *more strongly* than
claimed. Last week a recorded control inverted on replay; this week none did.

### A second, narrower QC defect: two structural passes marked a gate PASS they never measured

On **#2772** the first structural pass marked H3 `PASS` while the full
`dune runtest` was still running. On **#2773**'s re-review the structural pass
marked H2 **and** H3 `PASS` after hitting dune lock contention, reasoning that
"behavioral QC has already APPROVED at this tip, which implies a successful test
run."

Neither inference was accepted as a measurement — in both cases the gate was
discharged by something that *was* measured (CI green on the same SHA; and for
#2773, qc-behavioral's own scoped `--force` runs at that exact tip, both exit 0).
But the pattern is the finding: **the structural checklist invites marking an
H-row PASS on inference**, and the inference is sometimes even sound — which is
what makes it durable. A gate row should carry a measured exit code or say
plainly that it does not.

*(The #2773 re-review also hit dune lock contention because it had backgrounded
a dune call despite the brief telling it not to — the
`feedback_agents_background_wait_stall` shape, one level down again.)*

### A PR body was corrected before merge, for a specific reason

#2772's body still said "16 → 20 assertions", "20 passed", and `streak>=N` —
stale after the rework. A squash-merge makes the body the **permanent commit
message**, so merging as-is would have written the wrong emitted format into
`main`'s history, in the very defect class the body itself names. Corrected via
`PATCH /pulls/2772` before merging. The behavioral reviewer flagged it as a
merge-time note rather than a rework item, which was the right call — it needed
no code change and no re-review.

---

## Health Scan

`dev/health/2026-09-13-fast.md` — **CLEAN**.

- `dune build` **0**
- `dune runtest` **0**, with **0** `^FAIL:` lines across 4,213 output lines
- `status_file_integrity` **0**
- `index_size_linter` **0** (14,561 / 20,480 bytes, all rows ≤ 250 chars)

Every exit code read **unpiped**. Baseline gate wall time ~35 minutes from a
cold `_build` — consistent with the >25 min measured on 09-09, and still the
load-bearing evidence for D3 (#2747): a 21-minute orchestrator step cannot
possibly have waited for a dispatched agent.

**Step 6.2's doubled-path defect — 15th consecutive run, and re-measured rather
than carried forward.** All three forms were executed this run:

| form | exit |
|---|---|
| `dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh` (prescribed) | **2** |
| `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` | **0** |
| `sh trading/devtools/checks/status_file_integrity.sh` (repo root, no wrapper) | **0** |

Re-measuring mattered: a spot check of the *wrong form* returned 0 and appeared
to contradict the carried-forward claim. Both readings were "right" about
different commands. One-line fix, still behind the `.claude/agents/**` write
gate.

---

## Budget

- Cap **$200** (`max_daily_cost_usd`; a runaway backstop — auth is flat-rate).
- Target utilization 60–95%.
- **Subagents dispatched: 10** — 2 writing agents, 2 rework passes, 6 QC passes
  (3 structural incl. the OOM-voided one, 3 behavioral), plus one further
  behavioral re-review. Roughly **1.3M** subagent tokens.
- Measured cost: `dev/budget/2026-09-13-34757914354.json` is written by the GHA
  step *after* this file, so it cannot be read here. Note the recent budget
  JSONs carry `$0.0000` **because the runs genuinely spent nothing** — that is
  the outage, not a measurement defect.

**The binding constraint on this runner is memory, not budget.** 15 GB total; a
single OCaml link peaks high enough that two concurrent full builds OOM. Disk
was the constraint measured on 09-09 (16 GB per `_build`, 62 GB free at start);
reclaiming the baseline `_build` after the gate freed 16 GB and kept the run
comfortable. Neither constraint is the dollar cap, which remains far from
binding.

---

## Escalations

- **[info] Carried-forward verification (Step 1c): no still-real `[critical]`
  items.** The 09-09 run-2 summary carried a `[rework-cap]` on #2749; that PR
  **merged** as `cce6d073` on 09-09. Main verified green at `8c7d0ada` this run
  (exit 0, unpiped).

- **[high] NEW — #2770: the orchestrator's runtime is unpinned, and an upstream
  regression took the whole automation layer down for three days with no
  attribution.** Repo content is exonerated by a byte-identical diff across the
  failure→success boundary. Asks: pin the Claude Code version in
  `orchestrator.yml`; alert on orchestrator run failure. **The pin itself is
  blocked on the `workflow` scope** — the same credential gap that blocked the
  detector's wiring, now implicated in both the outage and its invisibility.

- **[high] NEW — #2771: the weekly deep health scan reports success and produces
  nothing.** Ran 09-07, `is_error: false`, 31 turns, $0.39, 98 seconds, no file.
  Deep-scan coverage is 13 days stale. The generalizable ask: **any workflow
  whose deliverable is a file should assert the file exists before reporting
  success** — one rule that would have caught all three of #2741, #2747 and
  #2771.

- **[medium] NEW — a capability gap on one half of a feature was treated as a
  block on the whole feature, for nine days.** The scheduled-workflow health
  *script* needs only read access and was runnable the day it merged; only its
  *wiring* was blocked. Nobody noticed, through the exact window it would have
  caught. Worth generalizing: when a feature is recorded as "blocked on
  &lt;credential&gt;", record **which half** is blocked.

- **[medium] NEW — my own dispatch error produced a false NEEDS_REWORK.** Two
  concurrent OCaml builds on a 15 GB runner OOM-killed one reviewer (exit 137).
  No rework was dispatched on it; CI-green on the same SHA disconfirmed it.
  `container-capacity-scheduling.md` should state the memory ceiling for this
  runner explicitly — **cap concurrent dune builds at one**, independent of the
  agent-count cap of 3.

- **[high] `.claude/agents/**` writes remain gated — 13th consecutive run,
  re-probed today with a one-line `Edit` and refused.** Note a new wrinkle:
  **#2757 did land a change to that exact file**, via a human-merged PR. So the
  gate is on my direct writes, not on the path. That makes the Step 6.2
  one-line fix (15 runs old) landable by the same route a harness agent used for
  #2757 — I can dispatch it as a PR rather than keep asking for write access.
  **Queued for the next run unless you object.**

- **[medium] `workflow` scope unavailable.** Unchanged, and now load-bearing in
  two places at once: it blocks the #2770 version pin and the #2663 detector
  wiring — i.e. both the cause of the outage and its detection.

- **[high] NEW — #2775: the budget-bundling step destroys every run artifact
  except the summary, and #2757 is what made that true.** The workflow's
  "Bundle budget" step runs `git reset --hard HEAD && git clean -fd`, justified
  by a comment reading *"the lead-orchestrator step (jj) leaves the working tree
  dirty … everything intentional is already on the remote branch."* That held
  while Step 8 used `jj`, which auto-snapshots everything. `publish_daily_summary.sh`
  stages **exactly one file** (`git add "$_summary"`), so after Step 8 the branch
  carries the summary and nothing else — while `dev/status/_index.md`,
  `dev/reviews/*`, `dev/audit/*` and `dev/health/*` sit in the working tree
  waiting to be cleaned away. **The job stays green**; deleted untracked files
  leave no trace.

  This run hit it. I committed the six orphaned artifacts onto the same branch
  by hand before the bundling step could run, so nothing was lost — **but that
  was me noticing, not a control.** The most expensive one is the index
  reconcile: a full pass over every track plus four verified drift corrections.
  Losing it silently means the next run re-derives the same drift and reports it
  as new.

  Note the shape: **this defect was introduced by the fix to #2741.** Step 8's
  repoint closed the path where the summary was lost and opened one where
  everything *except* the summary is lost. The 09-09 summary drew the general
  lesson about a different pair — *"a mitigation that makes the deliverable
  durable can also make the run shorter"* — and this is the same structure: a
  mitigation that made the summary durable made everything around it disposable.

- **[medium] `POST /issues/<n>/comments` → 403.** Every correction to an
  existing issue must become a *new* issue, fragmenting the record.

- **[high, carried] Rotate `BOT_GITHUB_TOKEN`.** Unchanged from 09-05 → 09-09.
  No recurrence observed this run.

- **[medium, carried] #2672 — delisting stub prints put −$741k of phantom loss
  in the canonical 26y record.** Still `open`. Sixth run asking.

- **[low] Budget records misreport the model.** `dev/budget/*.json` hardcodes
  `"model": "sonnet"`; the run init says `claude-opus-5`. Unchanged.

---

## Integration Queue

| PR | Branch | State |
|---|---|---|
| **#2769** | `ops/track-pacer-2026-09-13` | **MERGED** `3f348c14` — docs-only, CI green |
| **#2772** | `harness/schedhealth-streak` | **MERGED** `ccf075be` — all three gates green at `10fb1dc7` |
| **#2773** | `feat/backtest/build-time-store-sanity` | **MERGED** `9b65f7cb` — all three gates green at `0687955b`; `update-branch` → `abc54712` → CI re-run green |

**Post-merge verification on `main` @ `9b65f7cb`** — checked *after* both merges
landed, not before, because the pre-merge PR checks and the post-merge main
checks are different questions and only the second says what was left behind:
`perf-tier1-smoke` **success**, `golden-sp500-5y` **success**,
`golden-custom-universe` **success**. Both goldens green is the expected result
and the meaningful one here: both merged mechanisms are **default-off and
report-only**, so a golden that *moved* would have falsified the R1 no-op claim
that `experiment-flag-discipline.md` requires.

`build-and-test` was still `in_progress` at first publish and was recorded as
**pending, not assumed green** — an expectation is not a measurement. It has
since completed: **`build-and-test` success**. `watchdog` reads `skipped`, which
is its correct state on a green main, not a failure.

**Final: `main` @ `9b65f7cb` is fully green on all four checks.**

**The review queue is now empty — zero open PRs.** Next run's Step 0.5 must
therefore skip the fast-exit entirely per the A-FASTEXIT-VACUOUS precondition:
an empty queue is the *opposite* of saturated.

### The update-branch / verdict-SHA tension, again

#2772 was `behind` at merge time. Step 6.5 prescribes `update-branch` → re-poll
CI → merge, which moved the tip to `a5fa5094` **after** both verdicts were
written at `10fb1dc7`. `.claude/rules/pr-merge-gates.md` requires both verdicts
at the *current* tip. I followed Step 6.5 as the more specific rule — it
addresses precisely this case, and the authored diff is unchanged by a
branch-update merge — but this is the third consecutive run to hit it. **It is
still unratified.**

---

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable.

**Fifteenth consecutive run whose substantive findings are measurement- and
observability-integrity defects rather than strategy defects** — with one
genuine exception, as last week had: **`Series_level`** (#2773) is real
data-quality surface, and its author did the thing this repo most wants agents
to do. Asked to build a check, they **first established whether the gap was
real**, concluded that half of what was asked was already covered by
`Splice_detector`, and built only the other half — then said so. The behavioral
reviewer independently verified that argument against committed data (finding
all four cited rows in `terminal_runs_2000_v10.csv` with exact figures) and
agreed. A correct "this is already covered, here is the proof" is worth more
than a merged module, and it nearly didn't get asked for.

Today's dominant finding sharpens the recurring shape rather than repeating it.
The pacer's #2757 hypothesis, the OOM-driven NEEDS_REWORK, and the "the two
halves cannot disagree" claim were all **right about the outcome and wrong about
the mechanism** — and each one, acted on, produces a *specific wrong fix*:
revert an innocent PR, rework code that has no defect, trust a cross-module
agreement that does not hold. None was catchable by CI. All three were caught
the same way: **by someone re-deriving the claim instead of reading it.**

---

## Questions for You

1. **May I dispatch the Step 6.2 one-line fix as a harness PR?** #2757 proves
   `.claude/agents/lead-orchestrator.md` is editable via a PR even though my
   direct writes are refused. That route also unblocks the D3 (#2747) discipline
   edit and the "run dune in the foreground" boilerplate. This is the first run
   where the workaround is obvious, and it retires a 15-run-old defect.
2. **#2770 — will you pin the Claude Code version?** It needs the `workflow`
   scope. Three days of outage cost more than every capability gap on this list
   combined.
3. **#2771 / #2775 — worth a general rule?** "A step whose deliverable is a file
   asserts the file exists *and is committed* before reporting success" would
   have caught **#2741, #2747, #2771 and #2775** — every artifact-loss defect
   this month, with one rule. #2775's ask 1 is satisfiable entirely inside
   `dev/scripts/`, so it needs **no** credential grant.
4. **Is #2672 still open on purpose?** Sixth run asking.
5. **Still unanswered from 09-01 → 09-13:** is "latest verdict at the same SHA
   wins" a rule I may apply myself; should the orchestrator merge PRs you are
   actively reworking; and ratify or reject the `dev/budget/*.json` docs-only
   carve-out. The first of these is now blocking a merge path every single run.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
