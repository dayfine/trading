# Status — 2026-07-06 [run 2]
Run timestamp: 2026-07-06T12:16:31Z
Run ID: 2026-07-06-run-2

**Mode:** QC + MERGE pass. Two open PRs appeared since run-1 (which reported 0). Auto-merged the
routine weekly deps bump (#1866); ran the full QC pipeline on the new `cash_reserve_pct` strategy
feature (#1867) → **qc-behavioral NEEDS_REWORK** (one real correctness/contract gap) — verdict
posted as a PR review comment; **no rework agent dispatched** because the human is actively
iterating on that branch.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| cash-reserve | needs-rework (human-owned, in flight) | feat/cash-reserve-pct | #1867 | address behavioral finding (combined short-sleeve+reserve under-honoring + docstring) — human iterating; re-QC next run when tip stabilizes |
| (weekly opam deps) | auto-merged | deps/opam-weekly | #1866 | MERGED (ebbeb11c); branch pruned |
| simulation | blocked (data-gated) | — | — | stale-exit grid via WF-CV — needs deep/regime-diverse EODHD warehouse |
| stage-accuracy / spy-only / short-side / backtest-* / experiment-platform | blocked (LOCAL/data-gated) | — | — | broad/regime-diverse WF-CV on EODHD-gated warehouses |
| weekly-snapshot / decision-audit (M6.6) | blocked (human-gated) | — | — | large-warehouse multi-week sweep; live weekly cycle human-gated |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | awaiting #1327 enable-commit (carried ≥12 runs) |
| harness (ci.yml ENOSPC fix) | blocked (human: workflow-PAT) | — | — | `[~]` in harness.md; exact YAML in #1636 body |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| cash-reserve | qc-structural | APPROVED | gates on 2071c1f9 pass (fmt/build/scoped runtest); A1–A3 clean; flag-discipline R1 default-off + R2 axis-searchable PASS, R3 NA |
| cash-reserve | qc-behavioral | NEEDS_REWORK | Q3; spine intact (W1), faithful dial (W2); one CP4/CP1 finding — reserve under-honored when short_sleeve_fraction+cash_reserve_pct both >0 |
| cash-reserve | — | no rework dispatch | human actively pushing `fix(review):` commits mid-run (tip 2071c1f9→57ffa666); rework agent would collide → verdict posted as PR comment instead |
| (opam deps) | orchestrator (inline) | auto-merged | #1866 — trivial lock-file bump, CI fully green; structural APPROVED inline (CI = full linter gate), behavioral NA (precedent #1313); merged squash, branch deleted |
| harness | — | skipped | no `harness/*` PRs; T1 all `[x]`; open T3-C superseded, T3-H low-priority + gated on T1-N golden stability |
| code-health | — | skipped | `cleanup.md` §Backlog drained (only template placeholder); no new `[medium]`/`[high]` findings |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `data-gaps.md` unchanged since #814 (2026-05-03) → sentinel skip |
| (all feat tracks) | — | skipped | data-gated (EODHD key absent) / human-gated / LOCAL; no GHA-runnable feature code work |

## Feature Progress

### cash-reserve [NEEDS_REWORK — human-owned, in flight]
- Done today: full QC pipeline. **qc-structural APPROVED** — a clean, default-off (`[@sexp.default 0.0]`)
  capital-reserve sizing knob on the Weinstein strategy config, searchable as a `Variant_matrix`
  axis via `Overlay_validator` (flag-discipline R1/R2 satisfied). **qc-behavioral NEEDS_REWORK
  (Q3)** — one real finding: the `.mli` docstring claims the reserve is charged exactly once
  across the reserved-short-sleeve path, but on trace `short_budget = portfolio_value *
  short_sleeve_fraction` uses the **full** portfolio value (not the reserve-reduced `spendable`),
  so when `spendable < short_budget` the reserve is silently under-honored and total deployment
  exceeds `spendable`. No test exercises both knobs > 0. Required fix: combined-knob test +
  docstring reconciliation. Default 0.0 → cannot affect any current backtest/live result.
- In progress: human is iterating (`fix(review):` commits landing minute-by-minute).
- Blocked: No — awaiting the human's fix; re-QC next run once the tip stabilizes.

### Everything else [IN_PROGRESS, blocked]
- No feature code runnable in GHA: every open track is data-gated (EODHD absent), human-gated,
  or a LOCAL maintainer session. No feat-agent dispatched.

## QC Status
- cash-reserve (#1867): **NEEDS_REWORK (behavioral)** — see `dev/reviews/cash-reserve.md`; audit
  `dev/audit/2026-07-06-cash-reserve.json` (consecutive_rework_count=1, below the 3-run escalation).
- deps-opam-weekly (#1866): APPROVED (structural) / NA (behavioral) — merged; `dev/reviews/deps-opam-weekly-1866.md`.

## Budget
- Budget cap: $200 (backstop, from merge-policy.json; Max-20x subscription — figures are
  API-equivalent estimates, not billed).
- Target utilization: 60–95%.
- Subagents spawned: **2** (qc-structural + qc-behavioral, both on #1867; isolation:worktree).
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | qc-structural (#1867) | inherited | completed | ~77k | ~$0.7 |
  | qc-behavioral (#1867) | inherited | completed | ~98k | ~$0.9 |
- Any subagent killed mid-flight: No.
- Total (estimated): ~$2.6 (2 QC agents + ~$1 orchestrator overhead) / $200 (~1.3%).
- Utilization assessment: **UNDER_TARGET** — reason: **all-work-done**. The only GHA-runnable
  work was QC on the two open PRs (done) + one auto-merge; every feature track remains
  data/human/LOCAL-gated. No track skipped for budget/queue reasons. Deps chore did not warrant a
  separate agent (handled inline).
- Scope reduced due to budget: No.

## Data Operations
- ops-data skipped — `data-gaps.md` unchanged since #814 (2026-05-03) and `EODHD_API_KEY` absent
  in the GHA runner. Highest-leverage unblock remains the EODHD secret + broad-universe warehouse
  (see Escalations).

## Harness Work
- None dispatched. T1 all `[x]`; the lone `[~]` (ci.yml ENOSPC fix #1636) is blocked on a human
  `workflow`-scoped PAT; open T3 items are superseded (T3-C) or low-priority + gated on T1-N
  golden-scenario stability (T3-H). No `harness/*` PR in flight.

## Health Scan
- Result: CLEAN. (`dev/health/2026-07-06-run2-fast.md`)
- Main build: PASSING (`dune build` exit 0).
- Status file integrity: PASSING (exit 0).
- Index size linter: PASSING (exit 0, 8340/20480 bytes, all rows ≤ 250 chars).
- Deep scan (weekly GHA cron): latest `dev/health/2026-06-22-deep.md`; not dispatched here.

## Follow-up Queue
- No open `[ ]` follow-up items across status files (below the maintenance threshold of 10).

## Integration Queue
- **#1867 cash-reserve** — NEEDS_REWORK (behavioral); NOT mergeable. Human-owned + iterating.
  Re-QC next run when the tip stabilizes and the behavioral finding is addressed.
- #1866 deps — merged this run (3 gates green).

## Current Milestone Target
- M6.6 — live weekly cycle — blocked on a human decision (live `DATA_SOURCE` + cron + alerts).
  Analysis/backtest tracks are otherwise data-gated on the broad-universe EODHD warehouse.

## Dependency Unlocks
- None this run.

## Step 1b drift cross-reference
- Prior summary (2026-07-06, run-1, #1863) `## Pending work` rows were all `blocked` / `reconciled`
  — no `dispatched`/`awaiting-merge` rows → no classic `[drift]` warnings.
- **New since run-1:** two human PRs opened (#1866 deps 09:28Z, #1867 cash-reserve 10:39Z) — run-1
  reported 0 open PRs; these are new inflow, not drift. Both handled this run.
- Step 1c carried-`[critical]` re-verification: the only carried critical is the M6.6 live-cycle
  **human decision** (not a build-red assertion). Re-verified main GREEN via CI success on the
  pre-run HEAD (build-and-test + perf-tier1-smoke SUCCESS) and local `dune build` exit 0 — **no
  still-real build-red `[critical]`**; none emitted.

## Escalations
- [critical-decision-needed] **M6.6 live cycle** (carried) — the live weekly pipeline needs a human
  decision: live `DATA_SOURCE` wiring + scheduling cron + alerting. Backtest is proven; this is the
  gate to running the strategy for real. (Not a build-gate item — main is green.)
- [info] **`cash_reserve_pct` (#1867) has a real short-sleeve reserve bug** — when
  `short_sleeve_fraction>0` and `cash_reserve_pct>0`, the reserve is under-honored (short_budget is
  computed off full portfolio_value, not the reserve-reduced spendable). Default 0.0 masks it today.
  Posted on the PR; the human is fixing. No orchestrator action needed unless it stalls.
- [info] **Single highest-leverage human unblock: `EODHD_API_KEY` GHA secret** + a broad-universe
  (top-3000 / deep-history / regime-diverse) warehouse. Its absence is why ~every feature track is
  "data-gated" and the orchestrator dispatches 0 feature code most runs.
- [info] **Grant the orchestrator token `workflow` (+ `issues:write`, `actions:write`) scope**
  (carried) — would unblock the ci.yml ENOSPC fix (#1636, exact YAML staged).
- [info] **`tuning` M2 (qNEHVI) awaiting maintainer enable-commit** (#1327, carried ≥12 runs).

## Questions for You
1. **#1867 cash-reserve** — the behavioral QC flagged a real short-sleeve reserve under-honoring
   bug (details on the PR). Want the orchestrator to dispatch a rework agent once you pause, or are
   you handling it inline? (No agent dispatched this run to avoid colliding with your pushes.)
2. **EODHD access** — can the `EODHD_API_KEY` secret be added to the GHA orchestrator and a
   broad-universe warehouse be built? Single biggest unblock for autonomous feature/experiment work.
3. **M6.6 live cycle** — ready to make the live-pipeline decision (DATA_SOURCE + cron + alerts), or
   keep it parked?

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)

## Per-run links
- run-1 -> `2026-07-06.md`
- run-2 -> `2026-07-06-run2.md`
