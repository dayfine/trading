# Status — 2026-08-21

Run timestamp: 2026-08-21T07:35Z (GHA orchestrator run 32458970760)
Run ID: 2026-08-21-run-1
Mode: FULL

> **A merge API call returned `"Pull Request successfully merged"` for a merge I
> did not perform.** #2455 had already been merged by a concurrent actor nine
> minutes earlier; my `PUT /merge` with an explicit `sha` returned success and the
> correct merge commit anyway. Had I logged "I merged #2455" on that response —
> which is exactly what the merge step is written to do — the record would have
> been false. §"The merge I did not make".

## Headline

**The queue emptied, and almost none of it was me.** Three PRs were open at start;
two are merged, one is correctly held. But a **concurrent QC pipeline was running
the whole time** — it reviewed, reworked, and merged alongside me on the same two
PRs. I duplicated its work on #2455 and can prove neither of us needed to.

**Every claim I was most confident about was wrong, and agents caught all three.**
The `pr_gate_status.sh` defect does *not* "print an empty table and exit 0" as
#2432 and two summaries state — it crashes. The Weinstein book is *not* "available
locally" as `.claude/rules/book-as-authority.md` asserts — the path is macOS-only
and this runner is Linux. And I could not attach either correction to the issue it
belongs on, because this token can **file** an issue but not **comment** on one.

**A QC agent posted a review that no gate reader can parse** — 4,570 characters,
**zero real newlines, 43 literal `\n`**. It rendered as a wall of text and an
anchored `^#+ Verdict` regex — *precisely what `pr_gate_status.sh` uses* — found no
match. That is a **third** distinct way a QC agent can fail to deliver, and it
landed on the PR that fixes the gate reader. §"A verdict the gate could not read".

**The `unclear` branch I dispatched this morning fired three times before lunch.**
#2452 and #2455 (twice) each ended with disagreeing verdicts at a single SHA. One
of them proved the *naive* version of the fix would have been wrong.

## Established before dispatch (measured this run, not inherited)

- **Main GREEN at `87f659a4`** — `build-and-test`, `perf-tier1-smoke`,
  `golden-sp500-5y`, `golden-custom-universe` all **completed/success**. Zero open
  `ci-red` issues.
- **Four shell gates run natively, output redirected not piped, exit codes read
  directly:** `status_file_integrity` **0**, `index_size_linter` **0**,
  `no_python_check` **0**, `tracked_artifact_linter` **0**.
- **Queue at start: three open PRs** — #2433 (`do-not-merge`), #2452, #2455.
- **Toolchain, re-probed not inherited:** `gh` **ABSENT**, `docker` **ABSENT**,
  `dune` works via `eval $(opam env)` but is **not on the default PATH**, `git`
  **2.34.1**, `jj`/`jst` present but non-functional (`H-JJ-JST-BROKEN-GHA`),
  `curl`/`jq`/`python3` fine. **`EODHD_API_KEY` absent.**

## Step 0.5 / 1b / 1c

**Fast-exit declined** on the first-run-of-day escape hatch (no `2026-08-21*.md`
existed, most recent summary was 08-20). Condition 1 would independently have
failed — no open PR had `tip_SHA == Reviewed_SHA`.

**Step 1b drift: NONE.** Every PR the prior run listed as pending has landed:
#2430 → `646710e0`, #2434 → `bf9fbbcd`, plus #2426/#2423/#2431 confirmed merged.
Notably **#2434 merged on its own** — the #2429 stranding the prior run predicted
did not recur this time. That is one observation, not a refutation of #2429.

**Step 1c: no still-real `[critical]` items.** The prior run carried none, and main
measured green. **No `[critical]` is emitted for this** — a green verification must
never fire the "Fail on escalations" gate.

## The merge I did not make

I took #2455 through the full gate discipline: synced it (`ec0ac9f4`), waited out
CI, ran structural, ran behavioral, adjudicated a verdict conflict, re-verified CI
immediately before merging, and merged **with an explicit `sha`** — the protection
the prior run identified as the difference between its two stories.

The response:

```json
{"merged": true, "sha": "af12af115b24", "message": "Pull Request successfully merged"}
```

**But `merged_at` is `08:08:21Z`, and I issued that call at roughly `08:17`.**

The reconstruction is not a guess; my own poll loop captured the transition:

| poll (UTC) | `mergeable_state` | reviews |
|---|---|---|
| 08:07:02 | `clean` | 6 |
| 08:07:44 | `clean` | 6 |
| **08:08:25** | **`unknown`** | 7 |

A closed PR reports `mergeable_state: unknown` — verified against three controls
(#2430, #2452, #2455 all `closed/merged/unknown`). The flip lands **four seconds**
after `merged_at`. My poll printed tip, state and review count but **not `state`**,
so I never saw it close.

So: the concurrent actor merged #2455 at 08:08:21Z; my PUT nine minutes later
returned a success-shaped response for an already-merged PR, carrying the correct
merge commit — which is exactly what a genuine merge looks like.

**Two consequences worth separating.**

1. **The outcome was right.** All three gates were genuinely green at `47417aa4`,
   my adjudication was correct, and the explicit `sha` guaranteed that if anything
   *had* merged, it merged the tip I reviewed.
2. **The attribution would have been false.** `"merged": true` is not evidence that
   *you* merged it. Per **#2428** every actor authenticates as `dayfine`, so
   `merged_by` cannot disambiguate either. This is the same shape as the 08-20
   phantom review: **a success-shaped response that is not proof of the work.**

Cheap control, symmetric with the review-by-id check: **read `state`/`merged`
immediately before the merge call, and treat a `merged_at` that precedes your
request as "someone else did this" rather than logging authorship.**

## The concurrent pipeline, quantified

It was not a background presence — it did most of the work:

| time (UTC) | actor | event |
|---|---|---|
| 07:14–07:27 | concurrent | structural + behavioral + re-verdict on #2452; structural + behavioral on #2455 |
| 07:24:34 | concurrent | `ee7e6498` rework iteration 1 on #2455 |
| 07:38 | **me** | synced #2455 → `ec0ac9f4` |
| 07:49:47 | **me** | structural APPROVED @ `ec0ac9f4` |
| 07:54:26 | concurrent | `47417aa4` rework iteration 2 — **re-stales my verdict** |
| 07:54:42 | concurrent | behavioral NEEDS_REWORK (B5) @ `ec0ac9f4` |
| 08:06:13 | concurrent | structural APPROVED @ `47417aa4` |
| 08:07:54 | concurrent | behavioral APPROVED @ `47417aa4` |
| **08:08:21** | concurrent | **#2455 merged** |
| 08:07–08:13 | **me** | behavioral posts NEEDS_REWORK then a superseding APPROVED @ `47417aa4` — **to an already-merged PR** |
| 08:08:33 | concurrent | **#2452 merged** |

My entire #2455 pipeline — two agents, ~29 minutes — was **duplicated work on a PR
another pipeline was already driving to completion**. It produced one thing of
value: my behavioral reviewer independently found **B5**, a mis-paired control
(`novolconf`'s mean-hold comparator quoted `nobasestop`'s 3.6d instead of arc's
4.4d, moving two config axes at once). The concurrent actor fixed the same defect
~15 minutes earlier, so even that was a duplicate.

This is the **third, fourth and fifth** confirmed instance of #2432's concurrent
pipeline. It is no longer an anomaly; it is the normal operating condition, and the
orchestrator has no way to detect it before dispatching. **A claim marker — a label
or comment convention — would let the two stand down for each other.** That remains
Question 2 from the prior run, now with a measured cost attached.

## Adjudicating three verdicts at one SHA

At `47417aa4`, #2455 carried **APPROVED, NEEDS_REWORK, and APPROVED** on the
behavioral gate. Under `main`'s current logic that reads a clean `ok`. I refused
recency and read the bodies:

- The **NEEDS_REWORK** (`4991176348`) declares `Reviewed SHA: ec0ac9f4` in its own
  first line. It reviewed the **previous** commit; GitHub pinned it to the tip
  because the branch moved mid-review. Its sole finding (B5) is **exactly** what
  `47417aa4` fixes — I read that 4-line patch myself (`3.6` → `4.4` in a note and a
  fixture description).
- The **superseding APPROVED** (`4991192866`) cites the prior review by id, explains
  the race, and quotes the diff.
- A **second, independent** behavioral APPROVED (`4991145734`) agrees at the tip.

Adjudication took about two minutes. **That is the case for `unclear` over
auto-resolution**: the machine cannot tell these apart, but a human-legible
`ADJUDICATE` costs two minutes and a wrong `ok` costs a #2384.

## What I dispatched the gate-reader fix on, and what it found

The prior run named `pr_gate_status.sh` "the single most actionable item" and asked
(Question 1) whether to dispatch it. I did — **#2456**, structural **APPROVED**.

I gave the agent a live counter-example rather than just the issue: **#2452's**
later APPROVED at an unchanged SHA was a **legitimate** resolution — a PR-body-only
finding (so the tip correctly never moved), with the reviewer citing its own prior
review by id and re-verifying each claim. **A naive "any NEEDS_REWORK poisons the
SHA" rule would have false-blocked a genuinely approved PR.** The agent built a
fixture of that shape, confirmed it is mechanically indistinguishable from the
#2423 blind-supersede shape, and adopted `unclear` — the same fail-safe #2425 chose
intra-body.

**The agent then corrected the issue's own framing.** Running unmodified
`origin/main` in this exact `gh`-absent environment:

- **no args** → `set -e` trips on the failed command substitution: **exit 127**,
  raw `gh: not found`, header **never printed**;
- **explicit PR numbers** → header and separator print (that path needs no `gh`),
  *then* it crashes — a **misleading partial table**.

So "prints an empty table and exits 0" — in #2432 and two daily summaries — is
**wrong in the way that matters**. "Exits 0" implies a caller could be fooled into
reading the gate as passed; the real hazard is the partial table. Recorded in the
script header, `harness.md`, and on #2456 rather than silently superseded.

## A verdict the gate could not read

#2456's structural review posted **successfully** — HTTP 200, correct `commit_id`,
verified by id. And it was **unparseable**:

```
len(body)        = 4570
real newlines    = 0
literal "\n"     = 43
anchored ^#+ Verdict match → NO MATCH
```

The agent hand-escaped the JSON instead of letting the serializer do it. The body
rendered as one wall of text, and the regex `pr_gate_status.sh` uses to find a
verdict returns nothing — so the gate reads **no verdict at all** from a review
whose author reported APPROVED.

I repaired it via `PUT /pulls/{n}/reviews/{id}` — **encoding only, not one word of
content** (43 literal escapes → 43 real newlines; the word-count delta of 632 → 661
is purely `\n\n`-glued tokens separating). Verdict now reads `APPROVED` to an
anchored parser.

**Three distinct QC delivery failures are now on record**, and they degrade in
detectability: a **stall** announces itself and leaves work on disk; a **phantom
review** announces success and leaves nothing; a **malformed body** announces
success, leaves a real artifact, and is invisible to both the human (who sees text)
and the machine (which sees no verdict). Only the third can pass a by-id check.

The control is now in the behavioral brief: build the payload with `json.dump`, then
GET it back and assert `body.count("\n") > 0`, `body.count("\\n") == 0`, and an
anchored `^#+ Verdict` match.

## Where I was wrong

1. **"The script prints an empty table and exits 0."** It crashes — exit 127, or a
   partial table then a crash. Inherited from #2432 and repeated in two summaries;
   corrected by live measurement.
2. **"The book is available locally."** `.claude/rules/book-as-authority.md` gives
   `/Users/difan/Downloads/...`. This runner is Linux; **`/Users` does not exist**
   and a filesystem-wide search finds nothing. I verified this myself rather than
   accept the agent's report. Filed as **#2457**.
3. **"Sync #2455, then #2452."** Correct in principle, wasted in practice — both
   were already being driven to merge by another pipeline.
4. **I assumed my merge succeeded because the API said so.** §"The merge I did not
   make".

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| arc-readiness (#2455) | qc-structural | **APPROVED** (4) | @ `ec0ac9f4`; review `4990996692` verified by id; confirmed my "sync touched no PR files" claim |
| harness (gate reader) | harness-maintainer | **pushed** `b7c147d9` → **#2456** | fixed both #2432 defects; **corrected the issue's own framing by live measurement**; 26 → 39 assertions |
| arc-readiness (#2455) | qc-behavioral | **APPROVED** (4) | found **B5** independently; posted NEEDS_REWORK then superseded it at the tip; corrected me three times |
| harness (#2456) | qc-structural | **APPROVED** (4) | 39/39 assertions, `dash -n` clean, no `#2421`/`#2425` regression — **but posted an unparseable body**; repaired dispatcher-side |
| harness (#2456) | qc-behavioral | in flight at run end | CP1–CP4 + Rule 0 on both backends |
| #2452 | — | **merged by concurrent actor** | `ec6f06ee` at 08:08:33Z |
| #2455 | — | **merged by concurrent actor** | `af12af11` at 08:08:21Z; my later PUT returned success anyway |
| #2433 | — | **HOLD** | `do-not-merge` label — Rule 0 hard hold; rework cap 2/2 reached |
| ops-data | — | skipped | `data-gaps.md` unchanged since #814 (2026-05-03); `EODHD_API_KEY` absent |
| code-health | — | not dispatched | QC contended for the container all run (`container-capacity-scheduling.md`); a choice, recorded below |
| health-scanner | — | not dispatched | fast scan is deterministic (Step 6); deep scan is a weekly cron |

## QC Status

| PR | structural | behavioral | merged |
|---|---|---|---|
| #2455 | APPROVED @ `ec0ac9f4` (mine), APPROVED @ `47417aa4` (concurrent) | APPROVED @ `47417aa4` ×2, plus one superseded NEEDS_REWORK | `af12af11` |
| #2452 | APPROVED @ `017945f1` | NEEDS_REWORK → **legitimately** superseded by APPROVED, same SHA | `ec6f06ee` |
| #2456 | **APPROVED** @ `7b51d6e5` (body repaired) | in flight | — |
| #2433 | APPROVED @ `6cea0ebe` | **NEEDS_REWORK** (cap 2/2) | — **held** |

## Pending work

| Track | State | Branch | PR | Next step |
|---|---|---|---|---|
| harness (gate reader) | dispatched | `harness/gate-reader-2432` | **#2456** | behavioral verdict in flight; CI green, structural APPROVED — merge when the third gate lands |
| experiments (rt-freshness) | **blocked** | `exp/rt-freshness` | **#2433** | `do-not-merge` + rework cap 2/2. **Human decision required** — B8 is a quantifier overstatement propagated into a durable memory file |
| arc-readiness | merged | — | #2452, #2455 | — |
| ops (this run) | awaiting-merge | `ops/daily-2026-08-21` | — | this summary + health report + index reconcile |

## Health Scan

`dev/health/2026-08-21-fast.md` — **CLEAN**. Four shell gates exit 0; CI green on
`main@87f659a4`.

The build gate rests on **CI plus four QC agents** that ran `dune build @fmt` /
`dune build` / `dune runtest` to **exit 0** natively on supersets of `main`. **A
local full `dune runtest` was deliberately not launched** — two to three agents
were building for most of the run on a 4-core/15 GB box, and a prior run OOM-killed
this runner (exit 137, silently) doing exactly that.

Carried trap, still live: `dune runtest` output contains **two `FAIL:` strings that
are not failures**, inside `OK:` lines of the `sete_diagnostics_check` meta-linter.
`grep -c 'FAIL:'` → 2 on a green tree; `grep -c '^FAIL:'` → 0.

## Budget

- Cap **$200.00** (`merge-policy.json`), target band 60–95%. Auth is a Max-20x
  flat-rate subscription, so the cap is a runaway-dispatch backstop, not a spend
  limit.
- **5 dispatches** (1 harness writer, 4 QC passes). Prior run measured **$61.87 for
  12 dispatches ≈ $5.16/dispatch**, so this run estimates **≈ $26 ≈ 13%** —
  **UNDER_TARGET**. Tagged *estimated*; `dev/budget/2026-08-21-32458970760.json`
  carries the measured figure and **supersedes this number if they disagree**.
- **Under-target is real, and this time partly wasted rather than merely idle.**
  Two of the five dispatches (~40% of spend) duplicated a concurrent pipeline on
  #2455. The remaining headroom was not filled because **QC outranks fill-loop
  work** when both contend for the container, and QC contended all run.
- `code-health` was not dispatched: the top `cleanup.md` item is still the unanswered
  **test-file-length policy decision** (human-gated, seven runs), and the next
  actionable one (odoc population, 26 sites across 11 files) needs a forced
  `dune build @fmt` — the operation that OOM-killed a prior runner — while agents
  were building.

## Escalations

*(carried items re-verified by execution, not copied; new items marked NEW)*

- **[info] Carried-forward verification: no still-real `[critical]` items.** Main
  green at `87f659a4`; zero open `ci-red` issues; four shell gates exit 0.

- **NEW [high] A merge API call returned success for a merge it did not perform.**
  #2455's `merged_at` (08:08:21Z) precedes my `PUT /merge` by ~9 minutes; the
  response was `{"merged": true, ...}` with the correct merge commit. Logging
  authorship from that response records a falsehood, and per #2428 `merged_by`
  cannot disambiguate. **Control: read `state`/`merged` immediately before merging
  and treat a prior `merged_at` as someone else's work.**

- **NEW [high] A QC agent posted a review that no gate reader can parse.** 4,570
  chars, **0 real newlines, 43 literal `\n`**; anchored `^#+ Verdict` → no match.
  Passes a by-id existence check, so the 08-20 control does **not** catch it. Third
  distinct QC-delivery failure mode, and the least detectable. Repaired
  dispatcher-side (encoding only).

- **NEW [high] `book-as-authority.md` points every GHA reviewer at a path that
  cannot exist** (**#2457**). Verified: `/Users` absent on this Linux runner,
  filesystem search empty. Book faithfulness is the project's own "one validation
  axis with no tooling", and its tier-3 step — writing resolved answers back into
  the reference — **cannot run in GHA at all**. Fails silently: the agent falls back
  to the distilled reference and returns a normal-looking verdict. Needs a
  `.claude/**` edit → blocked per #2428.

- **NEW [medium] This token can file an issue but cannot comment on one.** Measured:
  create issue **201**, create PR review **201**, comment on a **PR** **201**,
  comment on an existing **issue** **403**, PATCH an issue **403**. **The
  orchestrator can open a tracked item but can never correct or follow up on it** —
  a structural gap in the audit trail for a project whose practice is exactly that.
  Today's #2432 correction had to be attached to PR #2456 instead.

- **[high] The concurrent QC pipeline is now the normal condition, not an anomaly**
  (**#2432**). Three further instances today; it merged both PRs I was working.
  ~40% of this run's spend was duplicated effort. A claim marker (label or comment
  convention) would let the two stand down for each other.

- **[high] `lead-orchestrator.md` still contains the mechanism that caused the
  −40.91pp regression** (**#2428**). `grep -c 'do-not-merge'` → **0**; Step 5
  Stage 3 still flips drafts to ready. Re-confirmed: `.claude/**` is push-OK but
  **edit-blocked at the tool-permission layer**. Needs a human.

- **[high] `golden-runs-sp500-15y` has never completed since 2026-05-17** (**#2427**).
  Unchanged. The only run spanning a pre-2009 macro regime, which
  `promotion-confirmation.md` **requires** in any confirmation grid. Needs
  `workflow` scope.

- **[high] The daily-summary auto-merge polls 10 min for a ~13 min check** (**#2429**).
  Still present at `orchestrator.yml:448`. **Did not bite this run** — #2434 merged
  on its own — which is one observation, not a refutation.

- **[high] The `ops(budget)` review bypass — sixteenth filing.** `orchestrator.yml:482`
  still cuts the fallback branch from the runner's HEAD rather than `origin/main`,
  then auto-merges. Needs `workflow` scope.

- **[high] `A-SUMMARY-STALE-FALLBACK` (`:334`) — twelfth filing.** Still present.

- **[high] Strategy tracks remain structurally undispatchable from GHA.** `docker`
  absent; the live strategy work is container backtests. As on 08-16 through 08-20.

- **[high] Step 4's GHA dispatch shape is still wrong in the spec** — it prescribes
  `git checkout -b` in the **shared** checkout while permitting concurrent
  dispatches. Hand-pasted the `git worktree` correction into all five briefs again.
  Part of **#2428**.

- **[medium] `H-JJ-JST-BROKEN-GHA` confirmed again.** git 2.34.1 vs jj's 2.41 floor.
  **#2456 was opened by the `curl` REST fallback** because the agent could not open
  its own PR.

- **[medium] Two project rules contradict each other on how QC agents build**
  (**#2386**, open). `qc-*-authority.md` mandates `docker exec trading-1-dev`, which
  does not exist here. All five briefs overrode it by hand.

- **[info] EODHD absent in GHA.** `data-gaps.md` untouched since #814 (2026-05-03),
  **110 days ≈ 15.7 weeks**. ops-data correctly sentinel-skipped. *(The prior
  "22nd/23rd week" figures were a per-run counter, not elapsed time.)*

## Follow-up Queue

- **#2433 needs a human.** Rework cap 2/2 with `do-not-merge`. B8: *"an order of
  magnitude more headroom on every leg"* / *"4–15× headroom on every leg"* — the
  MaxDD leg is **1.8–2.4×**, which the PR's own table prints correctly three lines
  above; and *"13–134× apart by metric"* where win rate is **1.19×**. Both
  propagated into a **durable memory file** that future sessions read without the
  correcting table. Everything else in that PR verified exact.
- **Two non-blocking nits on the merged #2455**: rework iteration 1 silently deleted
  a "cohort is confounded" caveat, and a comment says a short level "lands exactly
  on 157.00" when it is 156.9992 (the assertion itself is exact).
- **LINTER_CANDIDATE from #2455**: findings B2, B3 and B5 were all prose figures
  disagreeing with `actual.sexp` files **in the same directory** — mechanically
  checkable, and it recurred three times in one PR.
- `dev/status/cleanup.md` — 7 open items; the top one remains the unanswered
  test-file-length policy decision.

## Integration Queue

**#2456 is the only mergeable PR, pending its behavioral verdict.** CI green at
`7b51d6e5`, structural APPROVED at the same tip.

**#2433 is correctly NOT merged** — `do-not-merge` is a Rule 0 hard hold that
outranks all three gates, and it is at rework cap.

**Final state:** `main` at **`ec6f06ee`** with #2455 and #2452 both landed.
`perf-tier1-smoke` **success**; `build-and-test`, `golden-sp500-5y` and
`golden-custom-universe` were still **in_progress** at run end on that tip — the
next run should confirm they went green rather than assume it.

## Current Milestone Target

Unchanged and still unreachable from GHA — the live strategy work is container
backtests and `docker` is absent. #2427 continues to make the macro-regime
confirmation-grid cell that `promotion-confirmation.md` requires **unobtainable by
any path**.

## Dependency Unlocks

- **#2456, once merged, closes the loop this run opened by hand three times.** The
  `unclear`/`ADJUDICATE` branch is not speculative: #2452 and #2455 (twice) each
  produced disagreeing verdicts at one SHA today, and #2455's would read a clean
  `ok` under `main`'s current logic.
- **#2456 also makes `pr_gate_status.sh` runnable in this container for the first
  time**, via the curl backend — the repo's own gate reader has been unusable in
  the environment the cron actually runs in.

## Questions for You

1. **Who is running the second QC pipeline, and can it get a claim marker?** This
   is now the highest-cost open question, not a curiosity: it reviewed, reworked and
   merged both PRs today, and ~40% of my spend duplicated it. A label
   (`qc-claimed-by:<actor>`) or a comment convention would let whichever arrives
   second stand down. Carried from the prior run's Question 2, now measured.
2. **`.claude/agents/*.md` and `.claude/rules/*.md` need a human** (#2428, #2457).
   Two fully-specified fixes are blocked at the tool-permission layer: the
   `do-not-merge`/draft-hold mechanism, and the book-path claim that silently
   disables faithfulness verification in GHA. An agent cannot land either, and I
   would not want it to route around the block.
3. **Should the orchestrator's token get `issues: write` for comments?** It can file
   an issue but not correct one. Every follow-up finding has to be attached to a PR
   instead of the issue it belongs to.
4. **#2433 needs your call.** Rework cap 2/2, `do-not-merge`, one blocking finding
   (B8) that is a real overstatement in a durable memory file. Its underlying
   experiment work verified exact on every number a reviewer could reach.
5. **The 15y golden (#2427) — profile or raise the cap?** Unchanged from the prior
   run; my inclination is still to profile, since #844 found the 15y run 37× slower
   per cycle than the 5y with O(t²) suspected.
6. **Test-file length limits** — unchanged, still blocking the top `cleanup.md`
   item, unanswered across eight runs. **401** `*/test/*.ml` files, **154** over 300
   lines, **70** over 500, largest `test_screener.ml` at **2687**.

## What the next run should know

1. **Check `state`/`merged` immediately before merging, not just CI and verdicts.**
   A `PUT /merge` returns `"merged": true` for an already-merged PR, so the response
   is not evidence you did it.
2. **Verify a review is *parseable*, not merely present.** By-id existence was last
   run's control and it passed a review with zero real newlines. Assert real
   newlines, no literal `\n`, and an anchored `^#+ Verdict` match.
3. **Poll `state` in any watch loop.** Mine printed tip, mergeable_state and review
   count, and so missed the PR closing under it for nine minutes. `mergeable_state`
   flipping to `unknown` is the tell.
4. **Look for a concurrent pipeline before dispatching QC.** Recent reviews or
   `fix(review):` commits within the last ~30 minutes mean another actor owns the
   PR. Dispatching anyway costs a full pipeline and changes nothing.
5. **A verdict pinned to the tip may have read a different commit.** GitHub pins a
   review to the tip at post time, not the SHA the reviewer read. Always compare the
   `Reviewed SHA:` line in the **body** against `commit_id`.
6. **The book is not available in GHA** (#2457). Do not brief a behavioral agent to
   primary-verify from source here; ask it to flag what it could not resolve.
7. **You can file an issue but not comment on one.** Plan corrections onto a PR.
8. **Give reviewers a live counter-example, not just the issue.** The #2452 shape is
   what stopped #2456 from shipping a rule that would have false-blocked approved
   PRs.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
