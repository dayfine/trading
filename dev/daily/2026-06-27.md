# Status — 2026-06-27

Run timestamp: 2026-06-27T02:29Z (GHA orchestrator run 28275182617)
Run ID: 2026-06-27-run-1

**Mode:** ACTIVE RE-QC PASS — **2 QC subagents dispatched, 0 merges, 0 feat dispatches.**
The maintainer opened **PR #1760** (`feat/liquidity-realism-overlay`, ready) after
run 2 — a default-off liquidity-realism overlay addressing the ELCO illiquid-short
−48% NAV crash from the short-realism forensics (#1759). Its tip carried **new
commits past a prior local QC**, so per Step 1.5 the orchestrator re-QC'd it:
structural re-APPROVED, behavioral **re-NEEDS_REWORK** (a real regression — the
round-1 fix reopened a sibling double-exit gap). Verdict + exact fix posted to the
PR; **not merged** (gate fails on NEEDS_REWORK; PR also `behind` main + CI in-flight),
**not auto-reworked** (maintainer's actively-worked branch).

**HEAD:** `014db4ca` (`docs(short-realism): deep margin off/on acceptance — premise
refuted`, #1759). One docs-only commit since run 2's summary (#1758) — touches
`dev/notes/short-realism-*` + 4 golden longshort-margin sexp scenarios; no
`dev/status/*` track drift.

## Step 0 rampup check (deterministic)

- **Main CI GREEN** on `014db4ca`: build-and-test + perf-tier1-smoke +
  golden-custom-universe + golden-sp500-5y all SUCCESS (the in-progress
  "orchestrator" entry is this run).
- **0 open `ci-red` issues** — no hard stop.

## Step 0.5 saturated-queue check — NOT no-op (Condition 1 FAIL)

First-run-of-day; evaluated anyway. **Condition 1 FAILS**: PR #1760 is open with
`tip_sha=44e1811b` ≠ its reviewed SHA `573bd3f3` (new rework commits since the prior
QC). That alone makes this an active run → re-QC dispatched. (Conditions 2/4 — no
non-summary `dev/status/*`/harness/cleanup change since run 2 — held; Condition 3 —
no `[drift]`.) The 0-open-PR state of run 2 no longer holds.

## Step 1b drift cross-reference

Run-2's `## Pending work` rows were all `blocked`. No `dispatched`/`awaiting-merge`
rows → **no `[drift]` warnings.** The one new commit (#1759) is docs/sexp only.

## Step 1c carried-`[critical]` verification

Run 2 carried **no bare `[critical]`** (only `[info]` + the standing
`[critical-decision-needed]` M6.6, which does not trip the gate). Live re-verify:
main CI GREEN on `014db4ca`; `status_file_integrity.sh` exit 0; `index_size_linter.sh`
exit 0; `no_python_check.sh` exit 0; 0 open ci-red issues.

- [info] Carried-forward verification: no still-real bare `[critical]`; main GREEN on
  `014db4ca`; status-integrity + index-size + no-python exit 0 live.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| short-side-strategy / liquidity-realism overlay | re-QC NEEDS_REWORK (PR stays open/ready) | feat/liquidity-realism-overlay | #1760 | MAINTAINER: apply the posted fix — make the liquidity skip set `stop∪stage3∪laggard∪force_liq` (thread `stage3_exited_ids`/`laggard_exited_ids` into `_run_liquidity_special_exit`), correct the `special_exits.ml` docstring, add a stage3/laggard+illiquid collision test. Then update-branch (it's `behind` #1759) + CI-green → re-QC → merge. |
| short-side-realism (P0 plan #1757) | resolved/refuted (#1759) | — | — | #1759 ran the deep margin off/on acceptance and **refuted the premise** (margin model already shipped #859); the ELCO crash root-cause is illiquidity, not leverage → the liquidity overlay #1760 is the actual fix. Plan effectively superseded. |
| concentration (broad matrix / grid) | tabled by maintainer (#1757) | — | — | Tabled until shorts legible. Long-only 0.30 re-pin (#1753) merged. |
| barbell-overlay (weight cert) | blocked (human mandate) | — | — | Tooling complete (#1683/#1689/#1697/#1742, default-off). Next: confirmation-grid weight cert — needs human weight mandate. |
| rolling-start-lens / stage-accuracy | blocked (LOCAL/data-gated) | — | — | Regime-diverse / broad-universe WF-CV re-runs on LOCAL warehouses (EODHD-gated). |
| weekly-snapshot (M6.6 live cycle) | blocked (human-gated) | — | — | Generator/baseline/C2 test MERGED; live cycle (DATA_SOURCE/cron/alerts/state) unowned + human-gated. |
| cash-floor-correctness | blocked (human/data-gated) | — | — | NS1 done; NS2 short-proceeds human-gated; NS4 DD-validation data-gated. |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | Awaiting the #1327 enable-commit. |
| data-foundations (Phase 1.1 verify) | blocked (parked — EODHD tier 403) | — | — | 3 blocking-refactor checkboxes NOT_RUN; active path human-gated on ADR $-vol policy. |
| harness (ci.yml ENOSPC fix) | blocked (human: workflow-PAT) | — | — | `[~]` in harness.md; exact YAML in #1636 body; needs a `workflow`-scoped PAT. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| short-side-strategy (#1760) | qc-structural | APPROVED (8/10) | Re-QC at tip `44e1811b`. Hard gates exit 0; all linters green; A1 FLAG (Special_exits re-export/extraction) non-blocking. |
| short-side-strategy (#1760) | qc-behavioral | NEEDS_REWORK (3/10) | Re-QC at tip `44e1811b`. Prior force-liq finding RESOLVED + well-tested, but skip-set restructuring REGRESSED stage3/laggard collision coverage (CP1/CP4). Verdict + exact fix posted as PR review comments. |
| short-side-strategy (#1760) | — | not merged | Step 6.5 gate fails on behavioral NEEDS_REWORK; PR also `behind` main + `build-and-test` in-progress. |
| short-side-strategy (#1760) | — | not auto-reworked | Maintainer's actively-worked branch (last rework ~7h before run) — collision-avoidance (`gha-local-coordination.md`); the fix is mechanical + fully specified in the posted review for the maintainer to apply. |
| concentration | — | skipped | Tabled by maintainer (#1757). |
| harness | — | skipped | Tier-1 all `[x]`; lone `[~]` (ci.yml ENOSPC) is `workflow`-token-blocked. |
| code-health | — | skipped | `cleanup.md` §Backlog drained; 2026-06-22 deep scan 0 critical/high/medium; unchanged. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged → sentinel skip. |
| all other feature tracks | — | skipped | Data-/human-/LOCAL-gated (see Pending work). |

## Feature Progress

### PR #1760 — liquidity-realism overlay (re-QC: NEEDS_REWORK) ⚠️
Default-off, Weinstein-faithful risk overlay (held-degradation exit + entry gate)
fixing the ELCO illiquid-short −48% NAV crash. Prior local QC at `573bd3f3`:
structural-APPROVED + behavioral-NEEDS_REWORK over a force-liq skip-guard gap.
Maintainer pushed a rework to `44e1811b` (`fix(review): include force-liq ids in
liquidity-exit skip set` + ocamlformat).

**Re-QC at `44e1811b`:**
- **Structural re-APPROVED (8/10).** Full `dune build`/`runtest` exit 0; fn_length,
  magic_numbers, nesting, mli_coverage all green; P6 test patterns clean; A1 FLAG
  (the special-exit orchestration was cleanly *extracted* into `special_exits.ml` +
  a `Special_exits` re-export) is non-blocking.
- **Behavioral re-NEEDS_REWORK (3/10).** The force-liq half is genuinely fixed and
  pinned by a real discriminating integration test (`Special_exits.run` with a short
  breaching both force-liq and the liquidity floor, asserting exactly one
  `TriggerExit`, verified to FAIL pre-fix). **But** the rework replaced the
  pre-existing skip set `stop∪stage3∪laggard` with an internally-computed
  `trigger_exit_ids_of force_exit_ts ∪ stop` — and by the call site
  `stage3`/`laggard` ids have been *filtered out of* `force_exit_ts` (they are
  returned as separate channels). `Transition_assembly.assemble_output` concatenates
  the exit channels with **no per-position dedup**, so a Stage-3- or laggard-exiting
  position that is *also* illiquid on the same Friday now receives a duplicate
  `TriggerExit` — the identical double-exit crash class the rework documents.
  Confirmed by direct source trace (`special_exits.ml:92-96,142-165`;
  `transition_assembly.ml:44-51`). The docstring also now misstates `force_exit_ts`
  contents (claims it carries stage3/laggard — it does not). **Exact fix posted to
  the PR:** union all four exited-id sets, fix the docstring, add a stage3/laggard
  collision test.

This is the "verify before acting" guard working in the other direction: the
behavioral finding was a strong claim reversing a merge, so it was traced to source
(call-site filtering + the concatenation with no dedup) and **confirmed real** before
the NEEDS_REWORK was posted on the maintainer's PR.

## QC Status

- **short-side-strategy / #1760:** overall_qc **NEEDS_REWORK (behavioral)** at
  `44e1811b`. Structural APPROVED (8/10), behavioral NEEDS_REWORK (3/10). Reviews
  posted as PR review comments (ids 4583752926 + 4583752957). Audit:
  `dev/audit/2026-06-27-liquidity-realism-overlay.json` (consecutive_rework_count=1).

## Budget

- Budget cap: **$200.00** (`merge-policy.json` — runaway-dispatch backstop on a flat-
  rate Max subscription; the $ figure does not bill). `auto_merge_enabled: true`;
  `target_utilization` band 60–95%.
- Subagents spawned: **2** (qc-structural + qc-behavioral; both reused the pre-built
  tip worktree to avoid redundant cold builds).
  - qc-structural: ~83k subagent tokens.
  - qc-behavioral: ~91k subagent tokens.
- Estimated cost this run (estimated, not measured): **~$2–3** — 2 QC subagents +
  orchestrator state-read + 1 cold build of the PR tip + REST polling + 3 linter runs
  + reviews/audit/index/summary writes. No feat-agent, no merge.
- Measured cost: `dev/budget/2026-06-27-28275182617.json` is written by the GHA
  "Capture run cost" step *after* this orchestrator exits → not present now.
- Utilization assessment: **UNDER_TARGET (<2% of the $200 backstop)**, classified
  **"all-work-done"**: the one open PR was re-QC'd; every other dispatchable item is
  data-/human-/LOCAL-gated. Nothing skipped for budget.
- Killed mid-flight: No. Scope reduced due to budget: No.

## Data Operations

- ops-data did not run. `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md`
  unchanged → sentinel skip. ADL gap remains human-gated.

## Health Scan

(From `dev/health/2026-06-27-run1-fast.md` + deterministic Step 6 checks)
- **Result: CLEAN.**
- Build gate (6.1): CI authoritative — main GREEN on `014db4ca`. PR tip `44e1811b`:
  full `dune build` + `dune runtest` exit 0 (all linters green) as a local cross-check.
- Status integrity (6.2): `status_file_integrity.sh` exit 0.
- Index size: `index_size_linter.sh` exit 0 (8559/20480 bytes; reconcile row trimmed
  from 252→<250 chars to satisfy the cap).
- No-python: exit 0.
- Deep scan: latest 2026-06-22 (#1706) — 0 critical/high/medium; unchanged.

## Follow-up Queue

Total open followup items across status files: **~44** (carried; > 10 threshold).
Not actionable for a maintenance dispatch — all on data-gated / human-gated / LOCAL
tracks.

## Integration Queue

- **#1760** is the only open PR. **Blocked from merge** by the behavioral
  NEEDS_REWORK (and it is `behind` main + CI in-progress). Returns to the queue once
  the maintainer applies the posted fix, updates the branch, and CI goes green — then
  the next run re-QCs and (if green) auto-merges.

## Current Milestone Target

- **Liquidity-realism overlay (#1760):** the real fix for the ELCO illiquid-short
  crash (the short-realism P0's premise was refuted by #1759 — the margin model was
  already shipped; the crash was illiquidity, not leverage). One mechanical rework
  away from mergeable. Default-off on merge; arming + a WF-CV surface is the
  downstream experiment.
- **Single highest-leverage human unblock for GHA autonomy (unchanged):** add
  `EODHD_API_KEY` as a GHA secret + stage the deep top-3000 PIT warehouse — converts
  ≥5 data-gated LOCAL-only tracks into GHA-autonomous work.

## Dependency Unlocks

- None this run (0 merges). The liquidity overlay, once merged, becomes a new default-
  off experiment axis (`liquidity_config.min_hold_dollar_adv` / `min_entry_dollar_adv`).

## Escalations

(All `[info]`/`[qc-rework]` plus the standing `[critical-decision-needed]` M6.6 —
**no bare `[critical]`.** Main GREEN; linters exit 0 live; 0 open ci-red issues.)

- [qc-rework] **PR #1760 re-QC = NEEDS_REWORK (behavioral, confirmed by source
  trace).** The maintainer's round-1 fix (force-liq skip) is correct, but it
  *regressed* Stage-3/laggard collision coverage and shipped a docstring that
  misstates `force_exit_ts`. **The orchestrator posted the verdict + exact fix to the
  PR but did NOT auto-rework** (maintainer's actively-worked branch — collision
  avoidance) and did NOT merge (gate fails). **Maintainer action:** make the liquidity
  skip set `stop∪stage3∪laggard∪force_liq` (thread the already-computed
  `stage3_exited_ids`/`laggard_exited_ids` into `_run_liquidity_special_exit`), fix
  the `special_exits.ml` docstring, add a stage3/laggard+illiquid collision test;
  then update-branch (it's `behind` #1759) + CI-green. *Note: this is the 2nd
  NEEDS_REWORK on #1760 and the 2nd appearance of the same double-exit crash class
  (force-liq → now stage3/laggard) — worth a careful skip-set audit so a 3rd channel
  isn't missed.*
- [info] **Barbell default-flip / light-floor weight cert (0.30-0.40)** — fully
  enabled by tooling but NOT done; requires a confirmation-grid ACCEPT + the human's
  weight mandate. (carried)
- [info] **ci.yml ENOSPC fix is `workflow`-token-blocked and NOT yet applied**
  (carried). Exact YAML in PR #1636's body + `harness.md` (`[~]`). **Human action:
  apply with a `workflow`-scoped PAT.**
- [info] **Grant the orchestrator token `workflow` + `issues:write` (+
  `actions:write`)** (carried) — would let the orchestrator push the ci.yml fix, close
  stale ci-red issues, and re-run flaky CI itself.
- [info] **Broad-universe / deep PIT warehouse in GHA — highest-leverage GHA *data*
  unblock, still DATA-GATED** (carried; ≥5 tracks gate on it). Add `EODHD_API_KEY` as
  a GHA secret + stage the deep top-3000 PIT warehouse.
- [info] **data-foundations Phase 1.1 verify checkboxes are parked, not pending**
  (carried) — `NOT_RUN (blocked on tier 403)`.
- [info] **Followup accumulation = ~44 (> 10 threshold)** but all on gated/LOCAL
  tracks → no actionable maintenance dispatch.
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (#1327, carried).
- [critical-decision-needed] **M6.6 live cycle** (carried) — live `DATA_SOURCE` + cron
  + alerts + trading-state durability; the next capability milestone, unowned +
  human-gated. Generator (#1588) + first baseline (#1598) + C2 test (#1596) all MERGED.
  Not a bare `[critical]` (does not trip the gate-fail).

## Questions for You

1. **PR #1760 rework — apply yourself or have the orchestrator dispatch it?** The fix
   is mechanical and fully specified in the posted review (union all four exited-id
   sets + docstring + collision test). The orchestrator held off auto-reworking
   because it's your actively-worked branch. Say the word and the next run can dispatch
   a feat-agent to push the fix (a 2-commit stack on your PR) instead.
2. **EODHD_API_KEY in GHA + stage the deep top-3000 PIT warehouse?** Still the one
   change that converts the broad-universe lenses (≥5 tracks) into GHA-autonomous work.
3. **Barbell weight mandate** — light floor 0.30-0.40 vs heavier 0.50-0.70 — to queue
   the confirmation-grid weight cert.
4. **Grant the orchestrator token `workflow` + `issues:write` + `actions:write`** so
   the ci.yml ENOSPC fix push + red-main recovery + stale ci-red cleanup are one API call.
5. **M6.6 live cycle green-light?** Generator + first baseline + C2 test all MERGED.
6. **Resolve tuning M2 qNEHVI enable-commit (#1327)?**

## Per-run links

- [run 1] `dev/daily/2026-06-27.md` (this file) — ACTIVE RE-QC PASS, **2 QC subagents.**
  Re-QC'd maintainer PR #1760 at tip `44e1811b`: structural APPROVED (8/10), behavioral
  NEEDS_REWORK (3/10) — confirmed a real stage3/laggard double-exit regression by source
  trace; posted verdict + exact fix; not merged, not auto-reworked. Health CLEAN, main
  GREEN on `014db4ca`, 0 open ci-red issues.

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
