# Status — 2026-09-09 [run 2]

**Run ID:** 2026-09-09-run-2
**Run timestamp:** 2026-09-09T16:39Z– (GHA orchestrator run 34378061508)
**Mode:** FULL
**Main at run start:** `df7922fb`

Step 0.5 did not fire: the workflow supplied **`QUEUE_NON_EMPTY=0`**, and per the
precondition an empty queue is the *opposite* of saturated. A full pass was
mandatory. Confirmed independently: `GET /pulls?state=open` returns **0**.

---

## Headline — run 1 dispatched three agents this morning and lost all three, and the cause is the fix run 1 adopted

Run 1 (`34350513426`, 12:21Z) recorded three dispatches: harness on #2729, harness
on #2643, feat-backtest on #2732. Its `## Dispatched this run` table marked all
three **"in flight"** with the note *"(agent results pending at the time of this
first publish; updated below)"*. It was never updated. **Nothing from any of them
reached origin** — `GET /branches` returns 16 branches and not one is theirs.

The step timings say why:

| step | start | end | duration |
|---|---|---|---|
| 7 — **Run lead-orchestrator** | 12:25:40Z | **12:46:37Z** | **20m 57s** |
| 10 — bundle budget + auto-merge | 12:46:37Z | 13:14:44Z | 28m 07s |

The orchestrator's own step lasted **21 minutes**. Three writing agents cannot be
dispatched, build, run `dune runtest`, and return inside that. The corroborating
measurement is in this run: **my baseline `dune build && dune runtest` on this
same runner has been running for over 25 minutes** — one verification gate alone
outlasts run 1's entire orchestrator step. The agents were still working when the
process ended, and the ephemeral container took them with it. The job then spent
28 minutes — longer than the orchestrator ran — polling an auto-merge.

### The uncomfortable part: this was caused by run 1's own mitigation

Run 1 opens by explaining that it published its summary **incrementally**, citing
the 09-08 run's conclusion verbatim: *"the general fix is publish incrementally
and treat the artifact as the deliverable, not make the final step more
reliable."* That is a correct fix for the five-lost-summaries defect (#2741 D2),
and it worked — you are able to read run 1's summary because of it.

But *"treat the artifact as the deliverable"* is only true when the artifact is
the deliverable. On a dispatch run it is **not**: the deliverable is the agents'
PRs, and the summary is a report *about* them. Having published, run 1 had
produced its stated deliverable and stopped.

**A mitigation that makes the deliverable durable can also make the run shorter.**
Five runs lost the report while doing the work; run 1 saved the report and lost
the work. The two failures look opposite and share a root: both treat one
end-of-run artifact as the thing that must survive, instead of asking what the run
was actually for.

The correction is not to stop publishing early — it is *"publish early **and** stay
until the agents return."* This run does both: this file was published before the
agents finished, and the run does not end until they are back and their branches
are on origin. **No `in flight` row will be left standing in the table below.**

### Two distinct defects now share one lineage

- **D2 (#2741)** — Step 8's jj path loses the **artifact**. Fix: repoint Step 8.
  Still blocked (§Escalations).
- **D3 (new, this run)** — the orchestrator ending its turn mid-dispatch loses the
  **work**. No issue exists for it. This one is not blocked on any access grant —
  it is a discipline the orchestrator can hold today, and this run holds it.

---

## Correction — the cleanup backlog has *no* actionable items, and disk was never the reason

Both the 09-08 and 09-09-run-1 summaries declined to dispatch `code-health` "on
disk grounds," and both named the same top item:

> the `test_gap` single-conjunct pin in `test_entry_fill_retry_e2e.ml`

I read all four `- [ ]` entries in `dev/status/cleanup.md`. **None is dispatchable
work:**

| # | entry | what it actually is |
|---|---|---|
| 1 | `linter_coverage` — test files exempt from file-length limits | a **policy decision** ("either the exemption is intentional… or test files should carry a higher explicit limit"), explicitly needing a decision before a linter change |
| 2 | `record_size` — `segmentation_params` at 13 fields | **archive**: *"Not actioned… Recorded so the trend is visible if a third hoist is ever proposed"* |
| 3 | `test_gap` — the named "top actionable item" | **archive**: its own text says *"Not a defect (the conjunct is present and does its job); recorded so a future edit does not delete it as redundant"* |
| 4 | `<finding_type>: <path/to/file.ml>` | the fenced illustrative template, labelled **"ILLUSTRATIVE TEMPLATE, not a live backlog item"** |

So the honest answer is **"§Backlog is empty of work"** — Step 2e's own documented
skip condition — not "declined for disk." The distinction matters because the two
have opposite implications: the disk framing says *this work is queued and
starving*, and generated a standing escalation ("two consecutive declines is how
an item never happens"). It was never starving. There is nothing there.

This is #2742's category error — archive entries counted as pending work —
reproduced in a second file, and it had already propagated into an escalation.

**The correction cuts both ways, though.** My first pass counted 4 open items;
the deep-scan checker counts the template as **excluded** (`FOLLOWUP_EXCLUDED=9`).
`#2675`'s fence-aware exclusion is working exactly as designed on the one case I
naively got wrong. The checker was right and the grep was wrong — worth recording,
because three summaries in a row have criticised that counter, and on this specific
sub-case it beat a hand read.

---

## The three RED weekly workflows: nothing new, and asking again was the error

Same three RED as 09-05, 09-08 and this morning: `Prune candidates weekly`,
`Weekly track pacer`, `Weekly start sweep (BAH SPY)`.

**None has fired since run 1 examined them four hours ago, and none can.** They are
weekly crons — Sun 06:00, Mon 14:00, Mon 16:17 UTC. Today is Wednesday. The next
possible new datapoint is 2026-09-13.

Run 1 already established the right reading (*"a red row that predates the fix is
not evidence the fix failed"*) and did the genuinely useful thing: it ran
`prune_candidates.sh` directly, got exit 0, and established that #2725's fix works
without waiting for Monday. There is nothing left for this run to learn, and I
re-derived the same three rows with a full workflow-listing API sweep to find that
out.

The transferable point is about **check cadence, not check content**: a probe whose
input can only change weekly does not belong in a twice-daily pass. Re-running it
manufactures the appearance of a persistent problem out of an unchanged one — which
is most of why this row has occupied four consecutive summaries.

---

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness (#2643) | harness-maintainer | **completed → PR #2748** | `goldens_affected_check` nested-new-field FP; suite 21 → 23 |
| harness (#2729) | harness-maintainer | **completed → PR #2749** | mutant disagreement reconciled; jj rationale corrected per #2741 |
| post-run-validation (#2732 ask 2) | feat-backtest | **completed → PR #2750** | V18 store-level implausible-series check, 17 tests |
| harness (#2748) | qc-structural | *in flight* | dispatched after the writing agents freed their worktrees |
| harness (#2749) | qc-structural | *in flight* | |
| post-run-validation (#2750) | qc-structural | *in flight* | |
| cleanup | — | **skipped** | §Backlog contains no actionable work (see §Correction) — *not* a disk decline |
| ops-data | — | **skipped** | `data-gaps.md` untouched since 2026-05-03 (#814) — **129 days**; `EODHD_API_KEY` absent |

All three writing dispatches are run 1's lost work, recovered. Each brief carried
an explicit plain-`git` push instruction (`jst`/`gh` are unusable here) and an
explicit "do not open the PR — the orchestrator does it via REST" instruction; I
opened all three PRs from this side.

**Two of the three agents stalled the same way, and it is worth naming.** Both
harness agents backgrounded a long `dune` call, ended their turn "waiting for the
monitor notification," and stopped — the documented
`feedback_agents_background_wait_stall` shape. Subagents do not receive
background-completion events. Both recovered on a single nudge telling them to
block in the foreground with `timeout: 600000`. **This is the agent-level twin of
D3**: an agent that ends its turn waiting for an event it cannot receive is doing,
one level down, exactly what run 1's orchestrator did. The dispatch briefs for the
QC wave say so explicitly, and no QC agent has stalled.

## What the agents found

**#2729 is answered, and the answer is that neither QC pass was wrong.** The two
passes on #2721 ran **disjoint mutation sets** against the same tip SHA — the
interactive pass ran 3 mutations (none of them the content-losing one) and rested
its CP3 PASS on *reading* an assertion rather than testing a mutation against it;
the orchestrator's pass ran 28 and did try it. Not a contradiction about one
mutant. And the mutant no longer survives: reconstructed against current main it
gives **exit 1, 42/52**, led by the check literally named *"the pushed branch
carries the summary CONTENT, not just the branch name."* **PR #2727 — merged
before #2729 was filed — had already closed it.** The issue was answered before it
was written; #2749 records the evidence so nobody re-derives it.

The control matters as much as the result: the interactive pass's own mutation
still gives **51/52**, which is what distinguishes "the harness discriminates"
from "the harness is uniformly red."

**V18 flags BRK.A, deliberately, and the test pins it that way.** Nothing
separates a mis-mapped listing from an expensive share class using the price
series alone, so the check is an `Expectation` that asks a human rather than an
`Invariant` that fails a run. The agent flagged its own judgment call for review:
a narrower rule (large max/min ratio *alongside* the high median — MEL spans
20,000×, BRK.A ~1.2×) would exclude BRK.A, but exceeds the ask. Correct call to
surface rather than silently take.

## QC Status

*(in flight — three `qc-structural` passes dispatched; behavioral follows on any
that APPROVE. Verdicts land as PR review comments, since GitHub blocks
self-approval when reviewer and author share an account.)*

## Health Scan

`dev/health/2026-09-09-run2-fast.md` — **CLEAN**. `dune build` **0**,
`dune runtest` **0** with **0** `^FAIL:` lines across 4,187 output lines,
`status_file_integrity` **0**, `index_size_linter` OK (13,945 / 20,480 bytes,
all rows ≤ 250 chars). Every exit code read unpiped.

**Step 6.2's doubled-path defect — 14th consecutive run.** The prescribed
`dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`
exits **2** (`No such file`) because the wrapper has already `cd`'d into
`trading/`. Both working forms are recorded in the health report. One-line fix,
behind the `.claude/agents/**` write gate.

Also recorded there, because it is load-bearing evidence for D3: **the baseline
gate takes over 25 minutes on this runner.** Run 1's entire orchestrator step was
20m 57s.

## Budget

- Cap: **$200** (`max_daily_cost_usd`; a runaway backstop — auth is flat-rate).
- Subagents dispatched: **3** writing agents.
- **Disk is the binding constraint and it forced a staged dispatch this run.**
  Measured: baseline `trading/_build` reaches **15 GB**; each agent worktree
  reaches roughly the same once built; the runner started at **62 GB** free.
  15 + 3×15 = 60 GB against 62 GB is not a margin. So two agents went out first
  and the third waited for the baseline `_build` to be reclaimed after the gate —
  rather than dispatching three and discovering the ceiling as an ENOSPC.
  `container-capacity-scheduling.md` states the ceiling in memory and cores; on
  this runner it is **disk**, and it is now twice-measured.

## Escalations

- **[info] Carried-forward verification (Step 1c): no still-real `[critical]`
  items.** Run 1 carried none.

- **[medium] Withdrawing an escalation I inherited: "cleanup declined two runs
  running on disk grounds."** It is not a real queue-starvation item — the queue
  is empty of work (§Correction). Recording the withdrawal rather than silently
  dropping it, since it ran for two summaries.

- **[high] `.claude/agents/**` writes remain gated — 12th consecutive run,
  re-probed this run and refused.** I attempted the concrete Step 8 repoint (swap
  the `jj bookmark set` / `jj git push` pair for
  `dev/scripts/publish_daily_summary.sh publish`) and the write was refused. The
  replacement is merged, tested (52/52), and still uncalled. I published this
  summary by invoking that script directly, which is the workaround, not the fix.

- **[high] NEW — D3: the orchestrator can end its turn mid-dispatch and silently
  destroy every agent's work. Filed as issue #2747.** Measured above. Unlike D2
  this needs **no** access grant — the fix is that a dispatch run must not
  conclude while any agent is in flight, and any summary published early must be
  updated before the run ends. The issue asks for a **mechanical** backstop as
  well as the prose: fail the job if a merged summary's `## Dispatched this run`
  table still contains a `pending` / `in flight` cell, in the same spirit as
  `orchestrator_fastexit_gate.sh verify` (#2579). Prose did not bind for the
  fast-exit precondition and will not bind here — this run held the discipline by
  hand, which is not a control.

- **[medium] NEW — the same stall shape one level down, in subagents.** Two of
  three writing agents ended their turn waiting on a background `dune` call whose
  completion event a subagent cannot receive
  (`feedback_agents_background_wait_stall`). Both recovered on one nudge. Cheap
  to prevent: every dispatch brief should carry "run dune in the foreground with
  `timeout: 600000`; do not background it; do not use a Monitor." Mine now do,
  and the QC wave has not stalled. Worth folding into the agent definitions
  themselves — which is, again, behind the `.claude/agents/**` gate.

- **[medium] `workflow` scope unavailable.** Unchanged. Blocks #2653, #2723,
  #2634's wiring half, and the git≥2.41 image upgrade that would close D1
  (`jst submit`).

- **[medium] `actions: write` absent** (`POST /actions/workflows/*/dispatches`
  → 403, measured run 1). Not re-probed this run — see §"three RED weekly
  workflows" on cadence.

- **[medium] `POST /issues/<n>/comments` → 403.** Every correction to an existing
  issue must become a *new* issue, fragmenting the record.

- **[high, carried] Rotate `BOT_GITHUB_TOKEN`.** Unchanged from 09-05/09-08/09-09
  run 1. No recurrence observed this run.

- **[medium, carried] #2672 — delisting stub prints put −$741k of phantom loss in
  the canonical 26y record.** Still `open`. Fifth run asking.

- **[low] Budget records misreport the model.** `dev/budget/*.json` hardcodes
  `"model": "sonnet"`; the run init says `claude-opus-5`. Unchanged. Also: the
  four most recent budget JSONs carry no `total_cost_usd` key at all, so the
  measured-cost path in Step 7 has nothing to read.

## Integration Queue

| PR | Branch | State |
|---|---|---|
| **#2746** | `ops/daily-2026-09-09-run2` | this summary — CI green (3/3); the workflow's bundling step owns the merge |
| **#2748** | `harness/goldens-nested-new-field` | open, qc-structural in flight |
| **#2749** | `harness/publisher-mutant-reconcile` | open, qc-structural in flight |
| **#2750** | `feat/backtest/v18-store-sanity` | open, qc-structural in flight |

Nothing is auto-merged this run: the three-gate contract needs CI + both QC
verdicts at the current tip, and behavioral has not run. Any PR still short of
three gates when this run ends is a normal handoff — the next run opens with
`QUEUE_NON_EMPTY > 0` and picks up the pipeline where it stands, which is the
state Step 0.5 is designed for.

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable.
**Fourteenth consecutive run** whose substantive findings are measurement- and
observability-integrity defects rather than strategy defects. Today's is the
sharpest form yet: the defect was *introduced by the previous run's fix to the
previous defect*, and it destroyed more value than the defect it fixed.

## Questions for You

1. **Grant write access to `.claude/agents/**`?** Twelfth ask. The exact edit is
   drafted and refused; the replacement script is merged and unused.
2. **Close #2729?** It was answered before it was filed — #2727 already killed
   the mutant, and #2749 carries the reproduction (42/52 with the killing check
   named) plus the diagnosis of why the two passes disagreed. I cannot comment on
   issues, so I cannot close it with an explanation attached.
3. **Is #2672 still open on purpose?** Fifth run asking.
4. **Still unanswered from 09-01 → 09-09:** is "latest verdict at the same SHA
   wins" a rule I may apply myself; should the orchestrator merge PRs you are
   actively reworking; and ratify or reject the `dev/budget/*.json` docs-only
   carve-out.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
