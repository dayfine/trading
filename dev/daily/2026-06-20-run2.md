# Status — 2026-06-20 [run 2]

Run timestamp: 2026-06-20T05:50Z (GHA orchestrator run 27861908373)
Run ID: 2026-06-20-run-2

**Mode:** NO-DISPATCH full pass. **One new open PR since run 1** — #1662
(`feat/vol-scaled-stop`, maintainer `dayfine`, default-off ATR stop-distance floor),
created 03:10Z and **last touched 05:49:28Z — ~13 seconds before this run's PR check**,
with `build-and-test` CI still `in_progress` and the branch `behind` main. The maintainer
is demonstrably active on this exact lever right now (it was a documented next-session
lever in #1658), so the orchestrator **diagnosed + escalated** rather than
racing/QC'ing/auto-merging the hot human branch — the same disciplined disposition run 2
yesterday applied to #1659, which then self-merged cleanly. **0 subagents.**

**HEAD:** `1db67325` (`ops: daily orchestrator summary 2026-06-20 (#1661)`), advanced from
run-1's `f245b1fe` by the run-1 summary merge #1661. Main CI GREEN on HEAD.

## Step 0 rampup check (deterministic)

- Main CI GREEN: on `f245b1fe` the `orchestrator` workflow completed SUCCESS and
  build-and-test + perf-tier1-smoke + golden-sp500-5y + golden-custom-universe all SUCCESS;
  on HEAD `1db67325` build-and-test + perf-tier1-smoke + both goldens SUCCESS
  (`orchestrator` in_progress = THIS run; `watchdog` skipped).
- **0 open `ci-red` issues.** No hard stop, no fix-PR needed.

## Step 0.5 saturated-queue check

**Full pass, not a no-op fast-exit.** This is run 2 (a prior summary exists today —
2026-06-20.md run 1), so the first-run-of-day escape hatch does NOT apply, and
**Condition 1 FAILS:** there is now 1 open PR (#1662) whose tip `5162b59e` has no matching
`Reviewed SHA` (never QC'd; 0 reviews). A failed condition ⇒ full pass. (C2/C3/C4 are moot
once C1 fails: no `dev/status/*.md` changed since the run-1 summary commit `1db67325` — that
commit only touched `_index.md` + daily/health/budget; no `[drift]`; harness.md/cleanup.md
unchanged.)

## Step 1b drift cross-reference

Run-1's `## Pending work` rows were all `blocked` (data/human/LOCAL-gated) — no
`dispatched`/`awaiting-merge` rows. Between run 1 and now: the run-1 summary #1661 merged,
and the maintainer pushed #1662 (open). #1662 is **not** an orchestrator dispatch whose
status file failed to update → **no `[drift]` warnings.** This is the maintainer's active
LOCAL workstream (the vol-scaled-stop lever from #1658), classified, not drift.

## Step 1c carried-`[critical]` verification

Run 1 carried **no bare `[critical]`** (only `[info]` + the standing
`[critical-decision-needed]` M6.6 live-cycle, which does NOT trip the gate). Re-verified
main healthy: `status_file_integrity.sh` exit 0, `index_size_linter.sh` exit 0, 0 open
ci-red issues, main CI GREEN on `f245b1fe`/`1db67325`. No still-real bare `[critical]` to
carry.

- **[info] Carried-forward verification: no still-real bare `[critical]` items; main GREEN
  (build-and-test + perf-tier1-smoke + both goldens SUCCESS on `f245b1fe`; status-integrity +
  index-size exit 0 live).**

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| vol-scaled stop (stop-quality lever) | blocked (CI in_progress; maintainer-active hot branch) | feat/vol-scaled-stop | #1662 | Author lets CI finish + rebases (`behind` main); default-off, bit-identical baseline. Next run QCs + merges if green, or the maintainer self-merges (per #1659 precedent). |
| short-side-strategy (reserved short sleeve) | blocked (LOCAL, default-off) | — | — | #1659 MERGED. Next: screen sleeve fractions w/ decision-grading lens → WF-CV → grid before any default flip. `[non-blocking]`, LOCAL/data-gated. |
| decision-grading lens (autopsy) | blocked (LOCAL) | — | — | Phases 1-5 + insurance decomposition MERGED. Next analysis runs LOCAL against LOCAL warehouses. |
| weekly-close stop (axis) | blocked (LOCAL, default-off) | — | — | #1655 landed default-off; #1657 records a REJECT screen. WF-CV surface run is LOCAL/data-gated. |
| rolling-start-lens / experiment-platform | blocked (LOCAL/data-gated) | — | — | t3k factor-lens matrix SHIPPED; next = a *tradeable* regime/DD proxy — LOCAL validation. |
| backtest-perf / backtest-infra | blocked (data-gated, LOCAL) | — | — | Regime-diverse confirmation cells + composition-policy P2 matrix — needs LOCAL warehouses. |
| stage-accuracy | blocked (data-gated, LOCAL) | — | — | Broad-universe WF-CV re-run gates on the LOCAL top-3000 matrix. |
| weekly-snapshot (M6.6 live cycle) | blocked (human-gated) | — | — | Generator/baseline/C2 test MERGED; live cycle unowned + human-gated. |
| cash-floor-correctness | blocked (human/data-gated) | — | — | NS1 done; NS2 short-proceeds human-gated; NS4 DD-validation data-gated. |
| margin / long-short (Initiative B, Phase 5) | blocked (human-gated, LOCAL) | — | — | Touches core Portfolio (A1 flag); default-off keeps goldens bit-equal; human-supervised. |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | Awaiting the #1327 enable-commit. |
| data-foundations (Phase 1.1 verify) | blocked (parked — EODHD tier 403) | — | — | 3 open blocking-refactor checkboxes are all `NOT_RUN (blocked on tier 403)`; Phase 1.1 parked per Option B pivot. Active path (IWV scrape) human-gated on ADR $-vol policy. |
| harness (ci.yml ENOSPC fix) | blocked (human: workflow-PAT) | — | — | `[~]` in harness.md; exact YAML in #1636 body; NOT applied. Needs a `workflow`-scoped PAT. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| vol-scaled stop | — | skipped (QC + rework + merge) | PR #1662 `build-and-test` CI still `in_progress` (merge gate cannot complete regardless — pr-merge-gates.md forbids merge on pending CI); branch `mergeable_state: behind`; **updated 05:49:28Z, ~13s before the PR check** ⇒ maintainer actively mid-session on this exact hot lever. Per `gha-local-coordination.md` (collision avoidance) + the #1659 precedent, do NOT race/QC/duplicate a just-touched hot human branch — QC against a moving tip is instantly stale and a worktree `jj edit` would contend with the human's force-pushes. Defer QC to next run once CI settles + branch rebases (or the maintainer self-merges, as with #1659). |
| all other feature tracks | — | skipped | Every track data-/human-/LOCAL-gated or parked (see Pending work). |
| harness | — | skipped | Tier-1 all `[x]`; T2 milestone-gated (M5/M6/M7); T3-C superseded, T3-H low-priority/T1-N-gated; T4-* human-request-only. Lone `[~]` (ci.yml ENOSPC fix) is `workflow`-token-blocked. |
| code-health | — | skipped | `cleanup.md` §Backlog only the template placeholder `[ ]` open; latest deep scan (2026-06-15) 0 `[medium]`/`[high]`; run-2 fast report CLEAN. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged since 2026-05-03 (#814) → sentinel skip. |

## Feature Progress

No feature code shipped by the orchestrator this run. The on-main headline since run 1 is
only the run-1 summary merge (#1661). The single new open work item is the maintainer's
hot lever:

### vol-scaled stop — PR #1662 "vol-scaled (ATR) installed-stop distance" (default-off) [CI in_progress]
- **What:** adds a **default-off** `vol_scaled_stop_atr_mult` (+ `vol_scaled_stop_atr_period`,
  default 14) to `Weinstein_stops.config`. When `> 0.0`, the minimum installed-stop distance
  becomes `Float.max(installed_stop_min_pct, mult * ATR/entry_price)` — volatile names get a
  structurally wider stop floor to cut whipsaw at its source. `<= 0.0` is an **exact no-op**
  (every golden replays bit-identically; experiment-flag-discipline R1). The 15%
  `max_stop_distance_pct` reject cap still applies *after* the widened floor.
- **New primitive:** `Weinstein_stops.Vol_scaled_stop.effective_min_stop_distance_pct` (ATR via
  the existing `analysis/technical/indicators/atr` lib); degenerate inputs fall back to the base
  floor (never narrower). Wired per-candidate in `weinstein_strategy_screening.entries_from_candidates`.
- **Why:** the decision-grading lens showed the live stop is whipsaw-dominated and the live buffer
  is a *fixed* 8% regardless of a name's volatility. Weekly-close (#1655, a looser *trigger*) was
  REJECTED because it held real breakdowns deeper; this lever attacks whipsaw at a different point
  (the *distance floor*) without that flaw. 14 files, +397/-4. Test: `test_vol_scaled_stop.ml`.
- **Status (CI):** perf-tier1-smoke SUCCESS, build-and-test **in_progress** at run time;
  `mergeable_state: behind` (HEAD advanced past it via #1661). 0 reviews.
- **Why the orchestrator did not QC/merge it:** CI is still running (merge gate can't complete),
  the branch is behind + just-touched (updated ~13s before the PR check), and the maintainer is
  clearly mid-session on this exact branch. Low-regret call: diagnose precisely, leave the branch
  untouched, defer QC to next run once CI settles and the branch rebases.

## QC Status

- vol-scaled stop (#1662): **PENDING** — not run this cycle (CI `build-and-test` in_progress;
  branch behind + maintainer-active). Will QC on the next run once CI is green and the branch is
  stable, unless the maintainer self-merges first (per the #1659 precedent).
- No other open PRs to review.

## Budget

- Budget cap: **$200.00** (`merge-policy.json` — runaway-dispatch backstop on a Max-20x flat-rate
  subscription; the $ figure does not bill). `auto_merge_enabled: true`; `target_utilization` band 60–95%.
- Subagents spawned: **0**.
- Estimated cost this run (estimated, not measured): **~$0.90** — orchestrator state reads,
  CI/PR/issue REST polling, PR #1662 diagnosis, two deterministic linters, health-report + summary writes.
- Measured cost: `dev/budget/2026-06-20-27861908373.json` is written by the GHA "Capture run cost"
  step *after* this orchestrator exits, so it is not present during this session.
- Utilization assessment: **UNDER_TARGET — "all-work-done."** No eligible non-gated dispatch surface
  (the one open PR is CI-blocked + maintainer-active; all other tracks gated; maintenance backlogs
  drained). Nothing skipped for budget. Low $ is by design (flat-rate subscription; the cap is a
  backstop, not a fill target).
- Killed mid-flight: No. Scope reduced due to budget: No.

## Data Operations

- ops-data did not run. `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged since
  2026-05-03 (#814). The ADL gap remains human-gated. No actionable gap.

## Health Scan

(From `dev/health/2026-06-20-run2-fast.md` + deterministic Step 6 checks)
- **Result: CLEAN.**
- Build gate (6.1): CI authoritative — main GREEN on `f245b1fe`/`1db67325` (build-and-test +
  perf-tier1-smoke + both goldens SUCCESS).
- Status integrity (6.2): `status_file_integrity.sh` exit 0; `index_size_linter.sh` exit 0
  (7450/20480 bytes, all rows ≤ 250).
- No new `[medium]`/`[high]` findings since the 2026-06-15 deep scan (0 critical; Action required: NO).
- The PR #1662 CI-in_progress state is a PR-side condition, NOT a main-branch health finding.

## Follow-up Queue

Total open followup items across status files: **~43** (carried; > 10 threshold). **Not actionable
for a maintenance dispatch** — they live in data-gated / human-gated / LOCAL in-flight tracks. No
maintenance cycle swap warranted.

## Integration Queue

- **#1662 (vol-scaled stop, default-off)** — NOT mergeable yet: `build-and-test` CI in_progress +
  `mergeable_state: behind`. Needs CI green + a branch rebase (the maintainer typically self-merges
  these; #1659 precedent). Held this run; maintainer-active hot branch. QC deferred to next run.

## Current Milestone Target

- **GHA-eligible non-gated *feature* surface remains drained.** The orchestrator's value this run was
  detection-and-diagnosis: it caught #1662's pre-merge state within ~2.7 h of push (and ~13s after the
  maintainer's latest touch), pinned the blocker to CI-still-running + branch-behind on a hot human
  branch, confirmed it is default-off / bit-identical baseline, re-verified main health, and recorded
  the headline. Durable strategy work is the maintainer's LOCAL/active workstream.
- **Single highest-leverage human unblock (unchanged):** add `EODHD_API_KEY` as a GHA secret + stage
  the deep top-3000 PIT warehouse — converts ≥5 data-gated tracks into GHA-autonomous work.

## Dependency Unlocks

- None new this run. #1662 (vol-scaled stop) is itself a disciplined stop-quality follow-up to the
  whipsaw diagnosis from the decision-grading lens; it adds a default-off stop-distance axis once it
  lands (LOCAL/data-gated screen → WF-CV → grid before any default flip).

## Escalations

(All `[info]` except the standing `[critical-decision-needed]` M6.6 — **no bare `[critical]`.** Main
GREEN; linters exit 0 live; 0 open ci-red issues. The #1662 CI-in_progress state is PR-side, not a
main-health `[critical]`.)

- [info] **PR #1662 CI `build-and-test` in_progress + branch `behind`** (NEW). Default-off ATR
  stop-distance floor (bit-identical baseline). Disposition: let CI finish + rebase; the maintainer
  is actively mid-session and self-merges these (#1659 precedent), so the orchestrator did not race
  the hot branch. Next run QCs + merges if it's still open and green.
- [info] **ci.yml ENOSPC fix is `workflow`-token-blocked and NOT yet applied** (carried). #1634 is
  closed (dormant flake), but the fix remains unapplied. Exact two-step YAML is in PR #1636's body +
  `dev/status/harness.md` (`[~]`). **Human action: apply with a `workflow`-scoped PAT** (low urgency).
- [info] **Grant the orchestrator token `workflow` + `issues:write` (+ `actions:write`)** (carried) —
  would let the orchestrator push the ci.yml fix, close stale ci-red issues, and re-run flaky CI itself.
- [info] **Broad-universe / deep PIT warehouse in GHA — highest-leverage GHA *data* unblock, still
  DATA-GATED** (carried; ≥5 tracks gate on it). Add `EODHD_API_KEY` as a GHA secret + stage the deep
  top-3000 PIT warehouse.
- [info] **data-foundations Phase 1.1 verify checkboxes are parked, not pending** (carried). The 3 open
  `[ ]` items under its `## Blocking Refactors` are all `NOT_RUN (blocked on tier 403)` — they need the
  EODHD Fundamentals tier we don't buy (Option B pivot). They block nothing active; the live path is the
  IWV scrape (human-gated on the ADR $-vol policy artifact). Not a dispatch blocker.
- [info] **Design-doc drift accumulating (advisory)** (carried) — the 2026-06-15 deep scan flags
  directories not mentioned in their design/plan docs; "Action required: NO."
- [info] **Followup accumulation = ~43 (> 10 threshold)** but all on gated/LOCAL tracks → no actionable
  maintenance dispatch.
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (#1327, carried).
- [info] **`enable_short_side` default still `true`** — flip needs a ledger ACCEPT + confirmation grid
  (carried).
- [critical-decision-needed] **M6.6 live cycle** (carried) — live `DATA_SOURCE` + cron + alerts +
  trading-state durability; the next capability milestone, unowned + human-gated. Generator (#1588) +
  first baseline (#1598) + C2 test (#1596) all MERGED. Not a bare `[critical]` (does not trip the
  gate-fail).

## Questions for You

(Carried from run 1; item 9 replaced by the live #1662 question; others unchanged.)

1. **EODHD_API_KEY in GHA + stage the deep top-3000 PIT warehouse?** The single change that converts
   ≥5 LOCAL-only data-gated tracks into GHA-autonomous work.
2. **Snapshot-format-v2 S5 + v1-removal cleanup:** LOCAL session, or leave deferred (S4 captures the win)?
3. **Regime-gated deploy rule research:** green-light a LOCAL session to validate a *tradeable*
   macro/breadth proxy?
4. **M6.6 live cycle green-light?** Generator + first baseline + C2 test all MERGED.
5. **Grant the orchestrator token `workflow` + `issues:write` + `actions:write`** so CI-fix push,
   red-main recovery, and stale ci-red cleanup are one API call.
6. **Resolve tuning M2 qNEHVI enable-commit (#1327)?**
7. **Green-light Initiative B (margin / long-short) Phase-5 in a human-supervised session?**
8. **Weekly-close stop axis (#1655):** #1657 records a REJECT screen — keep as a default-off axis, or
   run its WF-CV surface in a LOCAL session?
9. **(NEW) PR #1662 (vol-scaled ATR stop, default-off):** want the next orchestrator run to QC +
   auto-merge it once CI is green + rebased (default-off, bit-identical baseline), or will you
   self-merge it as part of your LOCAL workstream (as you did with #1659)? And: green-light a LOCAL
   session to screen the ATR-mult values (decision-grading lens → WF-CV → grid) toward a possible
   default flip?

## Per-run links

- [run 1] `dev/daily/2026-06-20.md` — NO-DISPATCH full pass, first run of day. Main advanced
  `8065c7d9 → f245b1fe`: maintainer `dune fmt`-fixed + self-merged #1659 (short-sleeve, default-off)
  + landed run-2-yesterday summary #1660. 0 open PRs. Main CI GREEN. 0 subagents.
- [run 2] `dev/daily/2026-06-20-run2.md` (this file) — NO-DISPATCH full pass. New open PR #1662
  (vol-scaled ATR stop, default-off) CI build-and-test in_progress + branch behind; maintainer-active
  hot branch (updated ~13s before the PR check), diagnosed + escalated, not reworked/QC'd/merged. Main
  GREEN on `1db67325`; 0 ci-red. Health CLEAN (linters exit 0). 0 subagents (~$0.90).

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
