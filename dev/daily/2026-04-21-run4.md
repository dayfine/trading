# Status — 2026-04-21 [run 4]

Run timestamp: 2026-04-21T20:55:39Z
Run ID: 2026-04-21-run-4

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-scale | awaiting-merge | feat/backtest-scale-3g | #484 | Re-APPROVED run-4 after review-follow-up commit `db925c5a` (F2 partial fix — threads `benchmark_symbol=GSPC.INDX` into Tiered `Bar_loader`). Structural + behavioral both green (Quality Score 3). F2 narrowed: benchmark-symbol divergence closed, but `Summary_compute` still emits `None` for every universe symbol on every Friday, so Tiered remains observationally inert on the parity scenario — G5 FLAG downgraded, not resolved; must be cleared before Tiered is flipped to default. Awaiting human merge. |
| harness | awaiting-merge | harness/gha-cost-tracking | #483 | Re-APPROVED run-4 after force-push rebase onto post-#487 main (new tip `a3075010d3a0`; the old `792b5b09` Reviewed-SHA anchor was invalidated). POSIX-sh fixes intact; `dash -n` + `bash -n` clean on both scripts; all hard gates pass. Structural APPROVED (Quality Score 4); behavioral N/A (utility scripts, no domain logic). Awaiting human merge. |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution for scrapers, golden re-runs, cross-feature QC) — human-only per status file. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-scale | qc-structural | APPROVED | Re-review after `db925c5a` (F2 partial fix). H1/H2/H3 all PASS on tip; new tip = `db925c5a4c56`. Prior APPROVED verdict at `6d690819` stands unchanged at the structural level. |
| backtest-scale | qc-behavioral | APPROVED | Quality Score 3 (unchanged). F2 status: partial fix. Benchmark-symbol divergence closed; remaining gap inside `Summary_compute.compute_values` keeps Tiered observationally inert on parity — gate before default-flip. audit written: `dev/audit/2026-04-21-backtest-scale.json` (quality_score=3). |
| harness | qc-structural | APPROVED | Re-review after force-push rebase to `a3075010d3a0`. POSIX-sh fixes from prior `792b5b09` all preserved; `dash -n` + `bash -n` clean on `dev/lib/budget_rollup.sh` and `trading/devtools/checks/budget_rollup_check.sh`. All hard gates PASS. Behavioral N/A. audit written: `dev/audit/2026-04-21-harness.json` (quality_score=4). |
| — | qc draft→ready flip | no-op | Both PRs already `draft=false` at run start (flipped in prior runs). GraphQL mutation not invoked. |
| backtest-scale (next increment) | — | skipped | 3h (nightly A/B) is a post-merge follow-on per plan `dev/plans/backtest-tiered-loader-2026-04-19.md` §Increments — requires 3g to LAND on main, not just branch-exist. Fresh-stack ineligible. |
| harness (backlog) | — | skipped | Non-plan-first track, N=1 open PR (#483). Step 1.5 strict-skip applies (carried from run-1..run-3). T1-N data-gated / T3-H / T4 + POSIX-sh-linter follow-up remain gated on #483 merge. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since 2026-04-14; no new data gaps surfaced. |
| cleanup | code-health | skipped | `dev/status/cleanup.md` §Backlog empty; today's health scans all CLEAN; no candidate to dispatch. |
| — | health-scanner | skipped | Step 6 fast scan is deterministic (build gate + status-integrity); deep scan is weekly GHA cron — not dispatched here. |

## Feature Progress

### backtest-scale [READY_FOR_REVIEW]
- Done today (this run): re-QC on new commit `db925c5a` (F2 partial fix — threads primary index as `benchmark_symbol` into Tiered `Bar_loader`). Re-APPROVED structural + behavioral (Quality Score 3).
- In progress: #484 awaiting human merge.
- Blocked: No — throttled by human-review queue.

### harness [READY_FOR_REVIEW]
- Done today (this run): re-QC after branch force-pushed/rebased onto post-#487 main (new tip `a3075010d3a0`). POSIX-sh fixes all intact. Re-APPROVED structural (Quality Score 4); behavioral N/A.
- In progress: #483 awaiting human merge.
- Blocked: No — throttled by human-review queue.

### orchestrator-automation [IN_PROGRESS]
- Done today: nothing (Phase 2 human-gated).
- Blocked: Yes — Phase 2 requires empirical runtime tests + secrets/GitHub App scope that the orchestrator cannot perform autonomously.

### cleanup [IN_PROGRESS]
- Done today: nothing — backlog empty.
- Blocked: No. Awaits next health-scan finding.

## QC Status

- **backtest-scale (3g / #484):** APPROVED (structural + behavioral re-review at `db925c5a4c56`, Quality Score 3). See `dev/reviews/backtest-scale.md` §"Structural Checklist — backtest-scale 3g re-review" + §"Behavioral Checklist — backtest-scale 3g re-review". Audit: `dev/audit/2026-04-21-backtest-scale.json`.
- **harness (gha-cost-tracking / #483):** APPROVED (structural re-review at `a3075010d3a0`, Quality Score 4; behavioral N/A). See `dev/reviews/harness.md` §"Structural Checklist — harness gha-cost-tracking re-review (PR #483 at tip a3075010d3a0, post-rebase)". Audit: `dev/audit/2026-04-21-harness.json`.
- All other tracks: MERGED or BLOCKED — no fresh QC this run.

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% (from `dev/config/merge-policy.json`)
- Subagents spawned: 3 total (2 qc-structural parallel + 1 qc-behavioral sequential)
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | qc-structural (backtest-scale re-review) | haiku | completed | ~69k | ~$1.5 |
  | qc-structural (harness re-review) | haiku | completed | ~56k | ~$1.2 |
  | qc-behavioral (backtest-scale re-review) | sonnet | completed | ~69k | ~$2.0 |
- Orchestrator overhead: ~$1.00 (state reads, Step 0.5 check, Step 6 health gates, index reconcile, summary write, push + PR + auto-merge curl calls)
- Any subagent killed mid-flight: No
- Budget utilization: ~$5.7 / $50.00 (~11%)
- Utilization assessment: UNDER_TARGET (<60%) — all-work-done. The two open PRs (#483, #484) are the entire eligible dispatch surface; both had new commits since last QC so re-QC was the correct dispatch. No other tracks eligible — harness backlog non-plan-first strict-skip, cleanup backlog empty, ops-data skipped, 3h post-merge-only. Not a scope-reduction problem — correctly idling on a near-saturated queue with surgical re-QC work.
- Scope reduced due to budget: No

## Data Operations

(skipped — `dev/notes/data-gaps.md` unchanged since 2026-04-14; no new gaps surfaced)

## Harness Work

(no dispatch — open PR #483 on the harness track; Step 1.5 non-plan-first strict-skip keeps backlog gated until it resolves; run-4 re-QC was on that same open PR after a force-push rebase, not a new harness dispatch)

## Health Scan

(From `dev/health/2026-04-21-fast-run4.md`)
- Result: CLEAN
- Critical items: none
- Build gate: `dune build` exit 0, `dune runtest` exit 0 on main@origin tip `3ecff21` (post-#488 merge — "harness: fix three orchestrator self-sabotage bugs").
- Status-file integrity: exit 0.

## Follow-up Queue

- **backtest-scale:**
  - F1 (carried, unchanged from run-1): 3g's $0.01 tolerance is tighter than plan Resolutions #1 (`max($1.00, 0.001%)`); post-merge follow-up.
  - **F2/G5 (narrowed this run):** the `benchmark_symbol` plumbing is now correct, so the divergence is no longer symptom + cause — the remaining symptom is that the parity test reports `Summary=0 Full=0` because `Summary_compute.compute_values` returns `None` for every universe symbol on every Friday. One of `ma_30w` / `atr_14` / `rs_line` / `stage_heuristic` is missing required input. Must be fully diagnosed and fixed before Tiered is flipped to default (otherwise default-flip is a silent-pass regression). Track as a stacked follow-up PR after #484 lands.
  - F3 (carried): `Tiered_runner._promote_universe_metadata` hard-failwiths on missing CSVs where Legacy silently skips — align in same follow-up.
  - 3h (nightly A/B comparison) — next increment after #484 merges.
- **harness (carried, unchanged from run-2):**
  - [carried] POSIX shell portability linter — the original `harness_gap` flagged in run-1's audit. Still a worthwhile preventative gate; the cost of the run-1 → run-2 rework cycle would have been caught at commit time. Wire `dash -n` smoke or `shellcheck` over `trading/devtools/checks/*.sh` + `dev/lib/*.sh` into `dune runtest`.
  - [carried] T3-C superseded cleanup, T3-H commit-level QC, T1-N golden scenarios (blocked on data purchase), Tier 4 end-state items.
  - [carried] `dev/status/backtest-scale.md` §Next Steps has stale lines describing 3f-part3a/3f-part3b as "Ready for QC" (they merged as #477/#478); low-priority cleanup.
- **simulation (carried):** 6 open items unchanged (volume dilution, Stage.classify O(n), TODOs for price-cache-data-source / stoplimit-orders / monthly-cadence / bar-granularity; simulation loop performance profiling).
- **short-side-strategy (carried):** bear-window backtest regression, full short cascade, Ch.11 behavioural spot-check.
- **orchestrator-automation (carried):** Phase 2 background execution (human-only).
- **cleanup:** backlog empty; latest fast scans + deep scan have no new medium/high findings.
- Total open follow-up items across tracks: ~23 (unchanged from run-3; F2 refined from "observability" to "Summary_compute correctness" but still one item).

## Integration Queue

- **PR #484** — backtest-scale 3g parity acceptance test (APPROVED structural + behavioral, Quality Score 3; re-APPROVED after `db925c5a` F2 partial fix). Merges the tiered-loader merge gate. Post-merge follow-up PR (Summary_compute root cause) is now better-scoped.
- **PR #483** — harness gha-cost-tracking (APPROVED structural after rebase; behavioral N/A, Quality Score 4). POSIX-sh fixes survived the rebase; all hard gates pass.

## Current Milestone Target

M5 — backtest infrastructure + parameter tuning. Tiered-loader track (step 3 of the M5 backtest-scale plan) completes with PR #484's merge — 3g is the parity-acceptance gate. 3h (nightly A/B comparison) is the track-closing increment; after that, M5 work shifts to walk-forward parameter sweeps (T2-B/T2-C).

## Dependency Unlocks

- None this run. Both PRs re-APPROVED but awaiting human merge.
- Pending: once #484 merges → 3h (nightly A/B) becomes dispatchable AND a `Summary_compute` post-merge follow-up PR to fully resolve G5/F2 is unblocked. Once #483 merges → harness backlog surface opens; POSIX-sh linter follow-up is the highest-leverage candidate (the run-1 → run-2 rework cycle is now a concrete case study for its ROI).

## Escalations

- [info] **Carried-forward verification (Step 1c).** Run-3 had no still-real `[critical]` items. Re-verified this run by running `dune build && dune runtest` directly on main@origin tip `3ecff21` (post-#488 merge): exit 0, exit 0. No new `[critical]` escalations. No carry-forward.
- [info] **Saturated-queue fast-exit (Step 0.5) did NOT fire this run.** Condition 1 failed — both open PRs had new commits since last QC (PR #484 new commit `db925c5a`; PR #483 force-pushed to `a3075010d3a0`). The full pass correctly produced re-QC dispatches in response. Exactly the case Step 0.5 is designed to NOT skip.
- [info] **G5/F2 is narrower than it was, but still a gate on the Tiered default-flip.** This run's re-QC pinned down the scope: the benchmark-symbol plumbing is fixed; the residual symptom is that `Summary_compute.compute_values` returns `None` for every universe symbol on every Friday on the parity scenario. The parity test still passes because both Legacy and Tiered fall through to the simulator's pre-loaded bar cache — parity holds for the WRONG reason. Do not flip Tiered to default until the Summary-compute root cause is identified and fixed. Track as a stacked follow-up PR after #484 merges.
- [info] **Run-3 summary missed that PRs had new commits.** Reading the run-3 summary file, §Pending work said both PRs "tip == Reviewed_SHA" with no outstanding QC. That was accurate at run-3 time but humans reading it in isolation might have assumed the PRs were ready-to-merge without action. Each run's summary is a snapshot; the human should treat the most recent summary as authoritative. No process change recommended — just a note for readers.

## Questions for You

1. **PR #484 merge.** (Carried from run-1 Q1, run-2 Q2, run-3 Q1 — now re-qualified.) With the F2 partial fix in `db925c5a`, is APPROVED (Quality Score 3) + the narrowed G5 FLAG (now localized to `Summary_compute.compute_values` returning `None`) acceptable as a merge signal? The parity test is sound AS A GATE OF "don't ship if outputs diverge" — it correctly passes when both paths produce the same output, even if both outputs are trivially `Summary=0 Full=0`. Merging is safe because the Legacy path is the default and Legacy behavior is unchanged. Recommended: merge, then land a stacked follow-up PR that diagnoses and fixes `Summary_compute` before any PR that flips the `loader_strategy` default to `Tiered`.
2. **PR #483 merge.** (Carried from run-2 Q1, run-3 Q2 — now re-qualified.) Post-rebase re-APPROVED (Quality Score 4). POSIX-sh fixes survived. No new friction. Ready to merge.
3. (Carried from run-6 Q1 / run-1 Q3 / run-2 Q3 / run-3 Q3, still unanswered.) Should Step 1.5 relax "skip when N > 0 open PRs on a non-plan-first track" for `harness` — today the open PR #483 blocks T1-N/T3-H/T4 + POSIX-sh-linter candidate dispatch. Option (a) keep strict (current behavior — worked fine today); option (b) scope-aware carve-out so distinct T-items can run in parallel. Low urgency.
4. (Carried from run-3 Q3 / run-6 Q2 / run-1 Q4 / run-2 Q4 / run-3 Q4, still unanswered.) Is Phase 2 of `orchestrator-automation` still worth keeping as a status-indexed IN_PROGRESS track, or collapse to §Potential until you have bandwidth to spec it?

---
## Your Response

(Edit this section. Run dev/run.sh after editing to start the next session.)
