# Status — 2026-04-21
Run timestamp: 2026-04-21T06:45:00Z
Run ID: 2026-04-21-run-1

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-scale | awaiting-merge | feat/backtest-scale-3g | #484 | 3g parity acceptance test (tiered-loader merge gate) — APPROVED structural + behavioral (Quality Score 3). F1/F2 behavioral FLAGs (tolerance tighter than plan spec; observationally-inert Tiered wrapper on this scenario due to SPY/GSPC.INDX benchmark divergence) are non-blocking for this PR but must be cleared by a post-merge follow-up before Tiered is promoted to default. Awaiting human merge. |
| harness | dispatched | harness/gha-cost-tracking | #483 | Rework — POSIX-sh compatibility fixes in `trading/devtools/checks/budget_rollup_check.sh` and `dev/lib/budget_rollup.sh`. Dune invokes these with `/bin/sh` (not bash). Required: change shebang to `#!/bin/sh`, replace `set -euo pipefail` with `set -eu`, replace `${BASH_SOURCE[0]}` with `$0`, replace `<<<` here-string with pipe, replace `[[ ]]` with POSIX `[ ]`, replace bash arrays with space-separated strings or tempfile. Full fix list in dev/reviews/harness.md §"NEEDS_REWORK Items". |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution for scrapers, golden re-runs, cross-feature QC) — human-only per status file. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-scale | feat-backtest | completed | 3g parity acceptance test — opened PR #484 at tip 6d69081; 203-line test file + 14 synthetic OHLCV fixtures + 2 scenario sexp files; escalated two Tiered-path divergences to §Follow-up (non-blocking: Bar_loader SPY vs Runner GSPC.INDX benchmark; _promote_universe_metadata hard-fails on missing CSVs vs Legacy skip). |
| backtest-scale | qc-structural | APPROVED | All hard gates pass. 13-item checklist all PASS/NA. No production code modified; test harness only. Reviewed SHA 6d690819. |
| backtest-scale | qc-behavioral | APPROVED | Quality Score 3/5. Test faithfully implements plan §3g spec. F1/F2 observability FLAGs (tolerance too tight vs plan Resolutions #1; Tiered wrapper observationally inert on scenario due to benchmark_symbol divergence) are non-blocking but must be cleared post-merge before Tiered becomes default. flipped draft→ready. Audit: quality_score=3, consecutive_rework_count=0. |
| harness | qc-structural | NEEDS_REWORK | H3 FAIL — `dune runtest` exit non-zero on `budget_rollup_check.sh` due to bash-only syntax (`#!/usr/bin/env bash`, `set -euo pipefail`, `${BASH_SOURCE[0]}`, `<<<`, `[[ ]]`, bash arrays). Dune invokes with `/bin/sh`. Behavioral QC SKIPPED per Step 5 gate policy. Reviewed SHA d1ba14a3. Audit: harness_gap="POSIX shell portability lint check absent"; consecutive_rework_count=1. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since 2026-04-14; no new gaps surfaced. |
| cleanup | code-health | skipped | `dev/status/cleanup.md` §Backlog empty; latest fast scan (today) is CLEAN; no candidate to dispatch. |
| harness (backlog) | harness-maintainer | skipped | Open PR #483 already in flight on the only harness track with work-in-progress; Step 1.5 defers additional harness dispatch until #483 resolves. T1-N/T3-H/T4 items remain gated. |
| — | health-scanner | skipped | Fast scan is deterministic (Step 6.1/6.2). Deep scan is weekly GHA cron; not dispatched here. |

## Feature Progress

### backtest-scale [READY_FOR_REVIEW]
- Done today: Dispatched feat-backtest on 3g (the parity-acceptance merge gate) after overnight merges (#473 + #477/#478 landing 3f-part3) unblocked the plan. Agent shipped the test, 14 synthetic fixtures, and 2 scenario files; opened PR #484; QC'd both gates green; PR flipped draft→ready.
- In progress: #484 awaiting human merge.
- Blocked: No. After #484 lands: 3h (nightly A/B comparison) follows; behavioral FLAGs F1/F2 become a small post-merge follow-up PR (relax parity tolerance to plan's `max($1.00, 0.001%)` and plumb `benchmark_symbol` from `Weinstein_strategy.config.indices.primary` into `Bar_loader.create`).

### harness [NEEDS_REWORK]
- Done today: Dispatched qc-structural on the existing open PR #483 (gha-cost-tracking). Review returned NEEDS_REWORK on H3 (build gate fails when dune invokes `budget_rollup_check.sh` via `/bin/sh`). Review body written to dev/reviews/harness.md with full fix list; audit record logs `harness_gap: "POSIX shell portability lint check absent"` — a candidate future linter.
- In progress: Rework pending (POSIX-sh fixes in 2 scripts).
- Blocked: No — fix is scoped and well-documented.

### orchestrator-automation [IN_PROGRESS]
- Done today: Nothing (Phase 2 human-gated per status file).
- In progress: —
- Blocked: Yes — Phase 2 requires empirical runtime tests + secrets/GitHub App scope that the orchestrator cannot perform autonomously.

### cleanup [IN_PROGRESS]
- Done today: Nothing — backlog empty.
- In progress: —
- Blocked: No. Awaits next health-scan finding.

## QC Status
- backtest-scale (3g / #484): APPROVED (structural + behavioral, Quality Score 3) — dev/reviews/backtest-scale.md §"Structural Checklist — backtest-scale 3g"/§"Behavioral QC — backtest-scale 3g"; dev/audit/2026-04-21-backtest-scale.json.
- harness (gha-cost-tracking / #483): NEEDS_REWORK (structural) — dev/reviews/harness.md §"Structural Checklist — harness gha-cost-tracking (PR #483)"; dev/audit/2026-04-21-harness.json; behavioral not run (Step 5 gate).
- All other tracks: MERGED or BLOCKED — no fresh QC this run.

## Budget
- Budget cap: $50.00 (from merge-policy.json)
- Target utilization: 60%–80% (from merge-policy.json)
- Subagents spawned: 4 total (1 feat-backtest, 2 qc-structural, 1 qc-behavioral)
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | feat-backtest (3g) | sonnet | completed | ~50k in / ~20k out | ~$5–7 |
  | qc-structural (#484) | haiku (post-#482 switch) | completed | ~40k in / ~10k out | ~$1 |
  | qc-structural (#483) | haiku | completed | ~58k in / ~10k out | ~$1 |
  | qc-behavioral (#484) | sonnet | completed (Q=3) | ~116k in / ~10k out | ~$5–7 |
- Orchestrator overhead: ~$2 (state reads, audit writes, index reconcile, review rebuild from stash, health checks, summary write)
- Any subagent killed mid-flight: No
- Budget utilization: ~$14–18 / $50 (~28–36%)
- Utilization assessment: UNDER_TARGET (<60%) — the only other eligible dispatch lane was a harness-backlog pick, which Step 1.5 correctly skipped while PR #483 was the live harness PR. ops-data and cleanup had nothing to do. Not a scope-reduction problem; the eligible surface this run was two PRs, both of which got full QC treatment.
- Scope reduced due to budget: No

## Data Operations
(skipped — `dev/notes/data-gaps.md` unchanged since 2026-04-14)

## Harness Work
(no dispatch of new harness items — #483 already open; Step 1.5 defers further harness dispatch until that PR resolves)

## Health Scan
(From `dev/health/2026-04-21-fast.md`)
- Result: CLEAN
- Critical items: none
- Build gate: `dune runtest` exit 0 on main@origin tip 9b6bc95 (post-merge-wave baseline).
- Status file integrity: exit 0.
- Note: `Tiered_strategy_wrapper: Bar_loader.promote error (continuing):` stderr lines during `test_runner_tiered_cycle` are expected fixture-missing log output, not a failure — the wrapper's "continuing:" recovery path is exercised intentionally. This is the same phenomenon behavioral QC flagged as F2 on #484 (benchmark_symbol divergence); it's a documented follow-up, not a health regression.

## Follow-up Queue
- backtest-scale (NEW this run):
  - F1: 3g's $0.01 tolerance is tighter than plan Resolutions #1 (`max($1.00, 0.001%)`); fix in the post-merge follow-up PR that also lands the benchmark_symbol plumbing.
  - F2: `Bar_loader.create` hardcodes `benchmark_symbol="SPY"` while `Runner` uses `GSPC.INDX`, so every Summary promote on the parity scenario fails silently and the Tiered path is observationally inert. Post-merge fix: plumb `benchmark_symbol` from `Weinstein_strategy.config.indices.primary` into `Bar_loader.create`. Must be resolved before Tiered is flipped to default.
  - F3 (carried): `Tiered_runner._promote_universe_metadata` hard-failwiths on missing CSVs where Legacy silently skips — align behavior in the same post-merge follow-up.
  - 3h (nightly A/B comparison) — next increment after #484 merges.
- harness (NEW this run):
  - [critical for #483] Rewrite `budget_rollup.sh` + `budget_rollup_check.sh` to POSIX sh. Fix list in dev/reviews/harness.md §"NEEDS_REWORK Items".
  - [candidate] POSIX shell portability linter — surface gap logged in audit record as `harness_gap`. Either shellcheck-style rule or `dash -n` smoke gate over `trading/devtools/checks/*.sh`.
  - (carried) T3-C superseded cleanup, T3-H commit-level QC, T1-N golden scenarios (blocked on data purchase), Tier 4 end-state items; pre-existing nesting linter refactors (fetch_universe.ml, test_data_loader.ml); latent `linter_file_length` dune dep-graph gap.
- simulation (carried): 6 open items unchanged (volume dilution, Stage.classify O(n), TODOs for price-cache-data-source / stoplimit-orders / monthly-cadence / bar-granularity).
- short-side-strategy (carried): bear-window backtest regression, full short cascade, Ch.11 behavioural spot-check.
- orchestrator-automation (carried): Phase 2 background execution (human-only).
- cleanup: backlog empty; latest fast scan has no new medium/high findings.
- Total open follow-up items across tracks: ~23 (up from ~19 at run-6; +3 backtest-scale post-merge items, +1 POSIX-sh linter candidate, offset by nothing resolved).

## Integration Queue
- PR #484 — backtest-scale 3g parity acceptance test (APPROVED structural + behavioral Quality Score 3). Merges the tiered-loader merge gate and sets up 3h. Behavioral FLAGs F1/F2 are followed up in a post-merge PR, not blocking this one.
- PR #483 — harness gha-cost-tracking (NEEDS_REWORK). Awaiting POSIX-sh rework.

## Current Milestone Target
M5 — backtest infrastructure + parameter tuning. The tiered-loader track (step 3 of the M5 backtest-scale plan) completes with PR #484's merge — the parity acceptance test is the gate. 3h (nightly A/B comparison) is the track-closing increment; after that, M5 work shifts to walk-forward parameter sweeps (T2-B/T2-C).

## Dependency Unlocks
- 3f-part3 landed overnight (PR #478) + Tiered_runner extraction (PR #477) → unblocked 3g dispatch this run.
- T3-F rule-promotion landed (PR #473) → dune checks for the enforced rule registry are now live; latent `linter_file_length` dep-graph gap can be fixed via a follow-up using the promotion path.
- Once #484 merges → 3h (nightly A/B) becomes dispatchable.

## Escalations
- [critical] **Carried-forward verification (Step 1c):** All prior `[critical]` items (run-6 carried "review queue as the throttle" as `[info]`, not `[critical]`; no other critical items in the last 3 summaries). Main baseline verified green this run (`dune runtest` exit 0, evidence in dev/health/2026-04-21-fast.md). No new `[critical]` escalations.
- [high] **harness #483 NEEDS_REWORK — POSIX-sh violations.** Two new shell scripts under `trading/devtools/checks/` and `dev/lib/` use bash-only syntax but are invoked by dune via `/bin/sh`. H3 fails with `set: Illegal option -o pipefail` at `budget_rollup_check.sh:15`. Evidence: qc-structural report in dev/reviews/harness.md §"NEEDS_REWORK Items". Fix is scoped (shebang + 6 syntax replacements in two files). Recording `harness_gap: "POSIX shell portability lint check absent"` in dev/audit/2026-04-21-harness.json for future linter candidacy.
- [info] **backtest-scale #484 behavioral FLAGs (non-blocking).** F1: hard-coded $0.01 tolerance tighter than plan spec. F2: Tiered path observationally inert on the parity scenario due to `Bar_loader.create` defaulting `benchmark_symbol="SPY"` while `Runner` uses `"GSPC.INDX"` — every Summary promote fails silently, so parity holds trivially through the simulator's pre-loaded bar cache rather than through the tiered path. Both documented as post-merge follow-ups in dev/status/backtest-scale.md §Follow-up. Must be resolved before Tiered is flipped to default. Quality Score is 3/5 primarily because the test's pass signal is currently too permissive on the very divergence it's meant to pin.
- [info] **Overnight merge wave cleared the review queue.** Between 06:23 and 10:06 UTC on 2026-04-21, 7 PRs merged (#473, #477, #478, #479, #480, #481, #482) plus daily-summary #476. Run-6's "review queue as the throttle" complaint is resolved — orchestrator is back to productive dispatch. No drift from run-6 — all status files accurately reflect the post-merge state.
- [info] **Stash recovery during review-file reconstruction.** A side effect of dispatching two foreground QC agents with `git checkout` operations on the feat branch: when the orchestrator returned to main for the summary, residual feat-branch state produced a merge conflict on `dev/status/backtest-scale.md`. Recovered cleanly by reading qc-structural's review body back from `git stash@{1}` (feat-agent stashed its work before QC ran) and rebuilding the reviews file from that source. No content lost. Mechanism to tighten for future runs: have QC agents work on detached-HEAD checkouts or in dedicated QC worktrees, so the orchestrator's own working tree is never modified.
- [info] **qc-structural extraction fallback inaccuracy.** `record_qc_audit.sh` auto-extraction for PR #483 pulled the wrong verdicts (structural=NEEDS_REWORK but behavioral=NEEDS_REWORK and overall=APPROVED) because the current review file retains the APPROVED verdict lines from prior run-6 reviews kept below the current section. Worked around by calling `write_audit.sh` directly with explicit flags. Tightening candidate: update `record_qc_audit.sh` to only parse the leading `## Structural Checklist —` / `# Behavioral QC —` sections whose Reviewed SHA matches the branch's tip, OR have qc-agents write a machine-readable `structural_qc:` / `behavioral_qc:` / `overall_qc:` field at the top of the review file (similar to Reviewed SHA) rather than relying on `## Verdict` block scanning.

## Questions for You
1. **PR #484 merge order.** Recommend merging #484 first (closes the tiered-loader merge gate, unlocks 3h). Is the behavioral Quality Score 3 + F1/F2 flag acceptable as a merge signal given the follow-ups are documented? (Framing: the test's correctness is sound; the flagged concern is that the pass signal is currently too permissive — not that the test or code is wrong.)
2. **PR #483 disposition.** Options: (a) orchestrator dispatches a scoped rework next run to fix the POSIX-sh violations (simple, mechanical, low-risk); (b) leave it for the human or harness-maintainer to pick up manually; (c) close and re-open as a smaller-scope PR. The fix is small and the failure mode is clear — recommend (a).
3. (Carried from run-6 Q1, still unanswered.) Confirm whether Step 1.5 should relax "skip when N > 0 open PRs on a non-plan-first track" for `harness` — today the open PR #483 blocks all other harness dispatch. Option (a) keep strict; option (b) add scope-aware carve-out so distinct T-items can run in parallel.
4. (Carried from run-3 Q3.) Is Phase 2 of `orchestrator-automation` still worth keeping as a status-indexed IN_PROGRESS track, or collapse to §Potential until you have bandwidth to spec it?

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
