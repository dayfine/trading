# Status — 2026-06-02 [run 1]

**Run ID:** 2026-06-02-run-1
**Run timestamp:** 2026-06-02T14:42Z (GHA `GITHUB_RUN_ID=26827049466`)
**Mode:** FULL PASS / NO DISPATCH

**HEAD at run start:** `6451b5c3` (`chore(scripts): export-memory.sh + curate agent-memory snapshot` #1432).
**HEAD at run end:** `6451b5c3` (unchanged — no orchestrator-shipped PRs this run).
**Run delta:** 0 code/docs PRs from orchestrator dispatch. The 2026-06-02 maintainer session absorbed every orchestrator-eligible surface ahead of this run via 11 PRs (#1421–#1432).

## Step 0 rampup check (deterministic)

Main CI status via REST `GET /repos/dayfine/trading/actions/runs?branch=main`. Latest completed CI runs at run start:

- `CI` (build-and-test) — completed/SUCCESS on `bad96990` (2026-06-02T14:13Z)
- `perf-tier1` — completed/SUCCESS on `bad96990` + `6451b5c3` (2026-06-02T14:36Z)
- `CI` — in_progress on `6451b5c3` (the maintainer's last merge, just landed)
- `golden-runs-*` — in_progress on `6451b5c3` (advisory, not required for merge)

Main GREEN at run start. No `ci-red`-labelled open issues (`gh issue list --label ci-red --state open` returns 0). Proceeded to Step 1.

## Step 0.5 saturated-queue check

**Not a no-op exit.** Condition 2 FAILED — `dev/status/screener.md` was modified by PR #1428 (`e31dd1d6`, `feat(screener): min_price liquidity floor (default-off)`) between the prior summary commit (`eb329a0b` ops/daily-2026-06-01-run3) and this run. Per Step 0.5 Condition 2: "no `dev/status/*.md` modified since prior summary's timestamp (excluding orchestrator summary commits)" — fail forces full pass.

Other conditions for completeness:
- **Condition 1** PASS — 0 open PRs at run start; trivially no `tip_sha != last_review_sha` violations.
- **Condition 3** PASS — no `[drift]` warnings from Step 1b.
- **Condition 4** PASS — `dev/status/harness.md` + `dev/status/cleanup.md` unchanged since prior summary.

## Step 1b drift cross-reference

Prior `dev/daily/2026-06-01-run3.md` `## Pending work` table had two rows:

- **spy-only-reference auto-merged** via #1419 at `b051bb02` — verified merged on `main` (`git log --oneline | grep b051bb02` returns the same SHA, in the ancestry of `6451b5c3`). State matches summary.
- **(ops/daily-2026-06-01-run3 summary) dispatched** via `ops/daily-2026-06-01-run3` branch — verified merged on `main` as commit `eb329a0b` via PR #1420 (`git log --grep "ops: daily orchestrator summary 2026-06-01-run3"` returns the squash-merge commit). State matches summary.

No [drift] warnings.

## Step 1c carried-`[critical]` verification

Prior run-3 summary carried no bare `[critical]` items (the entries were `[critical-decision-needed]` for M6.6 + several `[info]`s). Nothing to re-verify in the "live-evidence" sense.

[info] Carried-forward verification: no still-real bare `[critical]` items; main GREEN on `6451b5c3` at run start (CI workflow conclusion SUCCESS on latest completed run for `bad96990`).

## Step 1.5 PR-open dispatch guard

Open PR count at run start: **0**. No re-QC or stacked-dispatch decisions to make.

## Step 2 dispatch decisions

The 2026-06-02 maintainer session shipped 11 PRs absorbing every orchestrator-eligible surface that existed at the prior run's end. The newly-landed surface is documented in this run's `_index.md` preamble. Per-track decisions:

| Track | Decision | Reason |
|-------|----------|--------|
| spy-only-reference | skipped | P0 (barbell-on-stocks NAV blend) needs multi-hour backtest of SPY-only deep rerun + production deep curve; per priorities-doc ramp-up reminder "Serialize backtests vs jj agents" + RAM constraint ("510-sym production run ~3.5GB (bull) / ~4.5GB (deep)") this is **NOT orchestrator-friendly from GHA**; maintainer-local-session work. |
| experiment-platform | skipped | Lever-#1 (broad-universe test) DATA-GATED on `EODHD_API_KEY` (length 0 in GHA runtime); lever-#2 (regime/breadth entry throttle) — `neutral_blocks_longs` axis already shipped #1410 and tested FRAGILE; remaining candidates (breadth gate, trend-quality filter) need a cleaner motivating signal before build. |
| weinstein (P2 price-action-confirmation Stage 3 fix) | skipped | Per priorities-doc P2: "scan more symbols/eras with `stage_chart` to confirm the false-Stage-3 pattern generalizes" — visual confirmation is a maintainer-local prereq (the n=1 SPY-2005-2010 chart is the only existing evidence); implementing the config dial on speculation would risk the same single-window-overfit failure the experiment-platform exists to prevent. |
| weinstein (P1 few-feature carrier comparison) | skipped | Build-heavy multi-PR work: adapt `Sector_rotation_weinstein` to consume scenario universe (currently hardcodes 11 SPDR ETFs) + sweep K=10/20/30 + add sector cap. Self-contained surface but requires running backtests against PIT S&P 500 universes to validate — same backtest-serialization constraint as P0. |
| backtest-perf | skipped | Outstanding items are local-only (tier-4 workflow_dispatch needs broad universe; F.3 retirement gated on snapshot-mode-default running uneventfully for several weeks; V1/V2/V3 verification follow-ups are local-only sequencing per `dev/plans/snapshot-engine-phase-f-2026-05-03.md`). |
| tuning | skipped | M1 5/5 OCaml deliverables MERGED; M2 (qNEHVI) not yet enabled for GHA dispatch per established precedent #1327 — maintainer authors `enable T*.x for GHA dispatch` commit first. |
| simulation | skipped | All in-flight surfaces (#916 / #920) MERGED 2026-05-08; remaining is sp500-2019-2023 baseline rerun (local-only). |
| short-side-strategy | skipped | Phase 2 transition bug FIXED via #1274; Phase 3 verdict locked (margin-edge negligible in 3 of 4 bear windows; `margin_config.enabled = false` default). Track in steady state — no immediate next action. |
| data-foundations | skipped | Only remaining gap: bars-retention recovery decision (synthesize vs accept loss for SCTY/MNK/LB) — vendor-shaped decision, not orchestrator-dispatchable. |
| ops-data | skipped (no API key, no gap change) | `EODHD_API_KEY` length 0 in GHA runtime; `dev/notes/data-gaps.md` substantively unchanged since 2026-05-03 (only data-pipeline-automation-track refresh). |
| harness | skipped (no T1/T3 eligible) | T1 fully checked off; T2-B/C/D milestone-gated (M5/M7/M6); T3-C superseded; T3-H low-priority gated on T1-N goldens stability; T4-* not auto-dispatched. |
| code-health | skipped (empty backlog, no new findings) | `dev/status/cleanup.md` §Backlog has no `[ ]` items (all completed/`[x]`); deterministic Step 6 build gate clean — no new linter regressions to add to backlog. |

**Loose ends from priorities-doc 2026-06-03** (also documented in the index preamble):

- **STALE GOLDEN re-pin**: `goldens-sp500-historical/sp500-2010-2026.sexp` bands assume pre-GSPC-fix 341%; the priorities-doc cites corrected ~237%/0.44 Calmar. The golden's prior 2026-05-30 re-baseline measurement (311.9%/Calmar 0.35) differs from the new 237%/0.44 — re-pinning requires running the 510-sym 15y backtest in-container to get the full metric set (sharpe, sortino, ulcer, trades, win_rate, hold), only 3 of ~11 metrics are present in the priorities-doc summary. Same "Serialize backtests vs jj agents" constraint as P0 → deferred to maintainer-local session. Priorities-doc itself notes: "It's local-only / not in CI, so it didn't surface — local goldens drift silently". Not blocking.
- **`stage_chart` tool**: priorities-doc says "committed this session (see git log) OR on a branch — verify it landed". Run-this check: `find . -name 'stage_chart*'` returns nothing on `main`; the tool is NOT committed. Documented for maintainer to commit-or-recreate next session.
- **Scratch cleanup**: verified absent on main (`_pit_*`, `dev/backtest/scenarios-2026-06-02-*`, `/tmp/*.png /tmp/*.svg`, `stage_spy_gfc.png` all gone). Already clean.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| spy-only-reference | blocked | — | — | P0 barbell-on-stocks NAV blend (cheap post-hoc; needs SPY-only deep rerun + production deep curve); P1 few-feature carrier comparison (build-heavy, adapt Sector_rotation to scenario universe + K=10/20/30 + sector cap); P2 stage-classifier price-action-confirmation fix (gated on `stage_chart` visual generalization); P3 widen to mid/small-cap with `min_price` #1428 + ADV floor. All four require maintainer-local backtest runs or visual analysis. |
| experiment-platform | blocked | — | — | Lever-#1 (broad-universe) DATA-GATED on `EODHD_API_KEY`; lever-#2 (regime/breadth throttle) awaits cleaner motivating signal (neutral_blocks_longs FRAGILE per #1412). |
| (ops/daily-2026-06-02 summary) | dispatched | ops/daily-2026-06-02 | (to be opened in Step 8) | Orchestrator's own summary PR; GHA bundling step amends + auto-merges with the budget JSON. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| spy-only-reference | — | skipped (backtest-required) | P0/P1/P2/P3 all need maintainer-local backtest or visual analysis per priorities-doc 2026-06-03 |
| experiment-platform | — | skipped (data-gated / signal-pending) | Lever #1 needs EODHD key; lever #2 needs motivating signal |
| backtest-perf | — | skipped (local-only) | All open work is local-only sequencing or maintainer-decision |
| simulation | — | skipped (local-only) | Remaining is sp500-2019-2023 baseline rerun (local) |
| tuning | — | skipped (gated on maintainer enable) | M2 not yet enabled for GHA dispatch (precedent #1327) |
| short-side-strategy | — | skipped (steady state) | Track wrapped post-#1274 / #1262 |
| data-foundations | — | skipped (vendor-decision) | Bars-retention recovery is maintainer call |
| ops-data | — | skipped (no API key, no gap change) | `EODHD_API_KEY` length 0; `dev/notes/data-gaps.md` substantively unchanged |
| harness | — | skipped (no T1/T3 eligible) | T1 done; T2-* milestone-gated; T3-H low-pri/gated; T4-* not auto-dispatched |
| code-health | — | skipped (empty backlog) | §Backlog has no `[ ]` items; build gate clean — no new findings to add |

## Feature Progress

### experiment-platform [IN_PROGRESS]

- Done since prior summary (maintainer-shipped 2026-06-02):
  - #1421 sector-rotation K-ladder result + layer attribution (bull+deep)
  - #1422 macro-gate dial on sector-rotation (default-off `enable_macro_gate`)
  - #1423 macro-gate result — cuts sector-k3 DD + raises Calmar in both regimes
  - #1424 barbell blend CONFIRMED on ETF lab — floor DD + engine return compose
  - #1426 barbell weight sweep — 70/30 is the robust optimum
  - #1427 autopsy gap accounting — 2026-05-29 cures REJECTED; gains came from selection + deployment + downside control
  - **#1428 `feat(screener): min_price liquidity floor (default-off)`** — unblocks P3 widen-to-mid/small-cap
- In progress: none (orchestrator-eligible surface)
- Blocked: lever-#1 broad-universe test on `EODHD_API_KEY`; lever-#2 candidates on motivating-signal availability

### spy-only-reference [IN_PROGRESS]

- Done since prior summary (maintainer-shipped 2026-06-02):
  - K-ladder + macro-gate + barbell-on-ETFs all SHIPPED (#1421/#1422/#1423/#1424/#1426)
  - **Headline FLIP** on individual stocks: selection transfers as RETURN engine, not Calmar winner; SPY-only index timing beats stock-picking on Calmar in both regimes (bull 0.48 vs 0.44; deep 0.35 vs 0.25); production produces 918% on deep window vs SPY-only's 420% but pays in DD (37% vs 18.8%); the bankable edge is the drawdown floor
- In progress: P0-P3 outlined in priorities-doc 2026-06-03 but all require maintainer-local backtest runs or visual analysis
- Blocked: backtest serialization vs jj agents; RAM constraints on 510-sym runs in GHA

### Other tracks

- **screener** [MERGED] — #1428 min_price liquidity floor (default-off) shipped; gates on setup price; default 0.0 preserves all goldens; default-off feature addition does not flip status.
- **harness / cleanup / orchestrator-automation / cost-model / experiments / etc.** — all unchanged this run.

## QC Status

| Track | Verdict | Quality | PR |
|-------|---------|---------|----|
| (no QC dispatches this run; no PRs awaiting review) | — | — | — |

## Budget

- Budget cap: $50.00 (`dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30–$40)
- Subagents spawned this run: **0**
- Per-subagent breakdown: n/a
- Any subagent killed mid-flight: No
- **Estimated cost this run (estimated):** ~$0.50–1.00 (read-only orchestrator state-collection + `dune build && dune runtest` invocation + multiple REST API calls). Final figure from `dev/budget/2026-06-02-${GITHUB_RUN_ID}.json` will be written post-run by the GHA "Capture run cost" step.
- **Measured cost (from GHA execution file):** `dev/budget/2026-06-02-${GITHUB_RUN_ID}.json` written post-run.
- Utilization assessment: **UNDER_TARGET** (~$1 / $50 ≈ 2%). All-work-done — every orchestrator-eligible surface was absorbed by the 2026-06-02 maintainer session; no tracks were silently skipped, all skip reasons are documented and follow Step 1.5 / Step 2 rules.
- Scope reduced due to budget: No.

## Data Operations

(omitted — ops-data not dispatched; `EODHD_API_KEY` length 0; `dev/notes/data-gaps.md` substantively unchanged since 2026-05-03)

## Harness Work

(omitted — harness-maintainer not dispatched; backlog has only milestone-gated + low-priority items)

## Health Scan

Deterministic Step 6 checks (per `.claude/agents/lead-orchestrator.md` §Step 6 — agentic fast scan retired):

- **Result: CLEAN**
- **Build gate** (Step 6.1) — `dev/lib/run-in-env.sh dune build && dune runtest` exit **0** on HEAD `6451b5c3` at run start (live invocation). The standard pre-merge linters (`fmt_check`, `linter_magic_numbers`, `nesting_linter`, `file_length_linter`, `fn_length_linter`, `linter_mli_coverage`, `status_file_integrity_linter`, `no_python_check`) all pass via `dune runtest`.
- **Status file integrity** (Step 6.2) — `trading/devtools/checks/status_file_integrity.sh` exit **0** (all `dev/status/*.md` files have required fields). Sanity-checked again post-`_index.md` edit (this run's reconcile preamble).
- Fast report written: `dev/health/2026-06-02-fast.md`.
- Most recent deep scan: `dev/health/2026-05-25-deep.md` (unchanged).

## Follow-up Queue

- spy-only-reference: from priorities-doc 2026-06-03 — informational, not blocking:
  - **STALE GOLDEN re-pin** loose end (local-only; not in CI; deferred to maintainer)
  - **`stage_chart` tool** verify-and-commit loose end (`analysis/scripts/stage_chart/` not on main as of `6451b5c3`)

## Integration Queue

(empty — no PRs in QC-approved-awaiting-merge state)

## Current Milestone Target

Per `dev/notes/next-session-priorities-2026-06-03.md` (the live priorities doc, #1429), `docs/design/weinstein-trading-system-v2.md`, and the 2026-06-02 maintainer session findings:

- **The headline this session — selection transfers as a RETURN engine, not a Calmar winner.** The ETF lab (sector rotation + macro gate + barbell) was a feature-attribution lab; pointed at individual stocks on a clean PIT S&P 500 (`universes/sp500-historical/sp500-{2000,2010}-01-01.sexp`, Wiki replay, survivor-bias-free, ~full bar coverage), the production strategy delivers **918% on the deep 2000-2026 window vs SPY-only's 420%** — but pays in drawdown (**37% vs 18.8%**). On the LOCKED Calmar objective, **SPY-only index-timing wins both regimes** (bull 0.48 vs 0.44; deep 0.35 vs 0.25). Return vs risk-adjusted is a mandate choice; the **barbell (floor + engine, ~70/30) is how you get both**.
- **Three corrections this session** (priorities-doc §Three corrections): (1) deployment ≠ what I claimed (SPY-only already ~67-78% in-market; rotation adds only +9-12pp); (2) GSPC golden drift — pinned `sp500-2010-2026` = 341%/Calmar 0.52 was measured pre-#1380/#1383, corrected number is 237%/0.44; golden bands now STALE; (3) dividend basis — strategy trades raw close, fair raw BAH bull 534% / deep 394%.
- **Autopsy gap accounting** (#1427): the 2026-05-29 autopsy gap (+1557 late_reentry / +1176 false-stage3 / +505 late-admission) was reproduced exactly; its prescribed cures (hysteresis / exit-timing / early-admission) all FAILED walk-forward CV → ~0% closed via its own mechanisms. The gains we banked came from **selection + deployment + downside control** — axes the per-symbol autopsy can't measure.
- **Stage-classifier visual diagnostic (NEW capability)** — `analysis/scripts/stage_chart/` built mid-session, n=1 SPY-2005-2010 chart visually CONFIRMS the false Stage-3 flips mid-advance. Principled fix redirect: **price-action confirmation (don't call Stage 3 until price actually crosses below the MA)** — NOT the weeks-based hysteresis WF-CV rejected. Verify-and-commit pending.
- **P0-P3 forward** (priorities-doc): P0 barbell-on-stocks NAV blend; P1 few-feature carrier comparison; P2 stage-classifier price-action-confirmation fix; P3 widen to mid/small-cap with #1428 `min_price` + ADV floor. All require maintainer-local-session work (backtest serialization + visual analysis + build-heavy multi-PR scope).

## Dependency Unlocks

- **#1428 `min_price` liquidity floor unblocks P3 widen-to-mid/small-cap** — the broad top-1000/3000 PIT-universe test was previously penny-stock-flattered; with `min_price ∈ {1, 5, 10}` settable per scenario, the bankable broad-universe test becomes runnable once the universe data is fetched.
- **Macro-gate dial on sector-rotation (#1422)** is now an axis — pairs with K-ladder + barbell to form the testbed for lever-#2 candidates if a cleaner motivating signal appears.
- No "Interface stable: YES" flips this run (no PR opened by orchestrator).

## Escalations

(All `[info]` or `[critical-decision-needed]` — no bare `[critical]` items that would trigger the gate-fail.)

- [critical-decision-needed] **M6.6 live cycle decisions** (carried, 15th run) — 4 decisions still needed per `dev/notes/next-session-priorities-2026-05-23.md` §P4. Not a bare `[critical]`; does not trigger the gate-fail. M6.6 dispatch unblocked once these land.

- [info] **Strategic direction is settled; selection ≠ Calmar winner; barbell is the synthesis** (UPDATED this run from priorities-doc 2026-06-03) — the through-line completed by the 2026-06-02 session is "selection transfers as a return engine, not a risk-adjusted winner" + "the barbell (floor + engine, ~70/30) is how you reconcile return-mandate with drawdown-mandate". The orchestrator continues to have a clean forward plan, though all P0-P3 items require maintainer-local backtest/visual work (not orchestrator-friendly from GHA per the ramp-up reminder "Serialize backtests vs jj agents").

- [info] **STALE GOLDEN re-pin loose end** (NEW this run) — `goldens-sp500-historical/sp500-2010-2026.sexp` bands cite the pre-GSPC-fix 341% baseline; corrected ~237%. Re-pinning needs running the 510-sym 15y backtest in-container to get the full metric set (only 3/11 metrics are in the priorities-doc summary). Not blocking CI (tier-3 local-only golden, not in `perf-tier1-smoke`). Maintainer-local-session item; the priorities doc itself notes "local goldens drift silently; consider a periodic local re-pin sweep".

- [info] **`stage_chart` tool verify-and-commit loose end** (NEW this run) — `find . -name 'stage_chart*'` returns 0 hits on `main` HEAD `6451b5c3`; per priorities-doc 2026-06-03 it was built mid-session and "committed this session OR on a branch — verify it landed". It did NOT land on main. Either re-create or recover from a local-only branch.

- [info] **EODHD_API_KEY still missing from GHA orchestrator runtime** (carried, 7th run) — `${#EODHD_API_KEY}` is 0. P1/P3 broad-universe bankability gates remain DATA-GATED on this. Unblock: add `EODHD_API_KEY` as a GitHub Actions secret + plumb through `.github/workflows/orchestrator.yml`. Maintainer-decision-required.

- [info] **`gh` CLI not installed in orchestrator container** (carried) — REST-API-only flow continues to work for PR querying / open / merge. The `record_qc_audit.sh --pr-number` gap (carried) reappears whenever QC runs in GHA; this run did no QC so the gap was not exercised.

- [info] **Stale ci-watchdog issues #1280 + #1264 + #1320** (carried, 23rd consecutive run) — orchestrator token still returns 403 on `issues:write`. Human triage required.

- [info] **15y split-day adjustment regression on `weinstein_strategy.ml`** (carried) — Investigation #998 root-caused; fix work pending. Touches core `weinstein_strategy.ml`; surfaced as a feat-weinstein scope-decision item.

- [info] **shares-outstanding fundamentals source decision** (carried) — vendor decision needed; dormant.

- [info] **PR-D'c status-file checkbox stale** (carried, low priority) — `dev/status/orchestrator-automation.md` still shows `- [ ] **PR-D'c — Remove dual-write of QC verdicts to dev/reviews/ files.**` even though PR #1332 landed PR-D'c on 2026-05-27. Cosmetic.

- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (carried, 11th run) — Per `dev/status/tuning.md`: "M2 is the next milestone per plan; not yet enabled for GHA dispatch (precedent #1327)."

- [info] **Build gate runs locally this run** (carried positive) — Step 6.1 deterministic build gate exercised via `dev/lib/run-in-env.sh dune build && dune runtest` in this run, exit 0. Status-file-integrity exit 0. Both verified live.

## Questions for You

1. **Backtest dispatch from GHA — is there a path to making P0 / P1 orchestrator-eligible?** The priorities-doc ramp-up reminder says "Serialize backtests vs jj agents" and the RAM constraint says "510-sym production run ~3.5GB (bull) / ~4.5GB (deep)". GHA orchestrator container should have ~7-8 GB RAM. Two possible paths to enable orchestrator dispatch of small-to-medium backtests: (a) add a `--max-symbols` cap (e.g. 50) on a separate dispatch surface that exercises the strategy on a subset; (b) lift the "no jj agents while backtests run" constraint by having the orchestrator NOT dispatch other agents during a multi-hour backtest fold. Worth exploring? Or are all these items inherently maintainer-local?

2. **STALE GOLDEN re-pin policy** — local goldens drift silently (not in CI). Worth wiring `goldens-sp500-historical/*.sexp` into a weekly cron (analogous to `health-deep-weekly.yml`) that runs the full set and surfaces drift? Or stays a manual maintainer-rep-in cadence?

3. **EODHD_API_KEY GHA secret** — recurring `[info]` across 7 runs. Either add the secret, or formalize "broad-universe data fetch is a permanent human-session task" so the orchestrator can stop flagging it.

## Per-run links

- [run 1] `dev/daily/2026-06-02.md` (this file) — FULL PASS / NO DISPATCH; 0 orchestrator-shipped PRs; main GREEN throughout; Health CLEAN; ~$0.50–1.00 estimated spend; 11 maintainer-shipped PRs (#1421-#1432) absorbed every orchestrator-eligible surface ahead of this run.

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
