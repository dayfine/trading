# Status — 2026-05-02

**Run ID:** 2026-05-02-run-1
**Generated:** 2026-05-02T08:08:29Z
**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 2 fails:
many `dev/status/*.md` files modified since prior summary commit, plus
4 new track files added; 25 maintainer-driven PRs landed since #731
merged at 2026-05-01T16:50Z).

## Saturated-queue check result

- **Condition 1** PASS — 0 open PRs across the repo (all of yesterday's
  ops summary PRs merged; no feature-track PRs awaiting review).
- **Condition 2** FAIL — `dev/status/_index.md`, `simulation.md`,
  `experiments.md`, `weekly-snapshot.md`, `data-foundations.md`,
  `tuning.md` all modified since prior summary commit (the four new
  tracks were added by today's manual planning session). 25 commits on
  main since #731.
- **Condition 3** N/A — Step 1b ran; no `[drift]` warnings emitted.
- **Condition 4** N/A — `harness.md` and `cleanup.md` unchanged on main
  since prior summary, but Condition 2 already failed.

Decision: full pass. State has changed materially since prior run.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| experiments | blocked | — | — | M5.2d distributional/antifragility (~250 LOC), M5.2e per-trade context (~300 LOC), M5.4 E1/E3/E4 — owner unset; needs scope assignment to `feat-backtest` or new feat-agent definition |
| weekly-snapshot | blocked | — | — | M6.4 PR-4 (NVDA 10:1), M6.4 PR-5 (KO dividend), M6.5 weekly report renderer — owner unset |
| data-foundations | blocked | — | — | M7.0 Synth-v2 (~800 LOC), Synth-v3 (~1000 LOC), EODHD multi-market (5 markets), Norgate ingest (vendor signup) — owner unset |
| tuning | blocked | — | — | T-A grid_search (~400 LOC) — blocked on `experiments` M5.2e and `data-foundations` Norgate; objective metrics now available via #762 |
| backtest-perf | blocked | — | — | Tier-4 first manual `workflow_dispatch` for canonical baseline (local-only), N≥5000 release-gate stays P1 (waiting on daily-snapshot streaming). Status file §Open work block stale — most named branches merged. |
| simulation | blocked | — | — | P0 RED CI [RESOLVED] via #752; M5 walk-forward + tuner items remain. sp500-2019-2023 baseline rerun is local-only. Status file body still references the unresolved P0 — refresh deferred. |
| short-side-strategy | blocked | — | — | sp500-2019-2023 baseline rerun (local-only). All G6/G14/G15/G4 items RESOLVED today. |
| hybrid-tier | blocked | — | — | Option 2 daily-snapshot streaming (~3000 LOC across 5–8 PRs per `dev/plans/daily-snapshot-streaming-2026-04-27.md`); plan-first; not fill-window. |
| harness | blocked | — | — | Backlog drained: T1 done, T2 milestone-gated, T3-C superseded, T3-H low-priority, T4-* future end-state. No T1/T3 dispatchable items. |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution for scrapers, golden re-runs, cross-feature QC) — empirical tests pending. |
| cleanup | blocked | — | — | §Backlog empty; no new `[medium]`/`[high]` findings from deterministic Step 6. |
| cost-tracking | blocked | — | — | Run-1 measured cost will land in `dev/budget/2026-05-02-<run_id>.json` post-GHA-capture. |
| ops-data | skipped | — | — | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel rule); EODHD_API_KEY also unset in GHA env. |
| optimal-strategy | — | — | — | MERGED. #747–#750 landed today as point fixes on a wrapped track; no further dispatch. |
| screener | — | — | — | MERGED. |
| trade-audit | — | — | — | MERGED. |
| backtest-infra | — | — | — | MERGED. |
| backtest-scale | — | — | — | MERGED. |
| data-panels | — | — | — | MERGED. |
| data-layer | — | — | — | MERGED. |
| portfolio-stops | — | — | — | MERGED. |
| strategy-wiring | — | — | — | MERGED. |
| sector-data | — | — | — | MERGED. |
| support-floor-stops | — | — | — | MERGED. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| — | — | none | No eligible feat-*/harness/ops-data/code-health work this run. The four new tracks (`experiments`, `weekly-snapshot`, `data-foundations`, `tuning`) lack feat-agent ownership; per-track plans need refresh against today's merged work before agent dispatch is safe. The maintainer drove 25 PRs since the prior summary; this run's value is reconciliation + drift surfacing. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel skip per Step 2d). EODHD_API_KEY unset. |
| harness | — | skipped | No dispatchable T1/T3 items (backlog drained). |
| cleanup (Step 2e) | — | skipped | §Backlog empty AND no new `[medium]`/`[high]` findings from deterministic Step 6. |

## Feature Progress

### experiments [PLANNED — major progress today, no orchestrator dispatch]

Track was created in today's manual planning session as PLANNED. By the
end of the same day, four of its planned PRs have already merged:

- **M5.2a** — `feat(backtest): experiment-runner overrides + comparison
  + smoke catalog` (#756). The `--override` + `--baseline` + `--smoke`
  flags + the `comparison.{sexp,md}` writer.
- **M5.2b** — `feat(metrics): trade aggregates + return basics catalog`
  (#758). win_rate, avg_win/loss, profit_factor, expectancy,
  max_consecutive_*.
- **M5.2c** — `feat(metrics): risk-adjusted + drawdown analytics
  catalog` (#762). Sharpe, Sortino, Calmar, MAR, ulcer/pain index.
  Note: also rename UnrealizedPnl→OpenPositionsValue + add corrected
  UnrealizedPnl via #741 (precursor cleanup).
- **M5.4 E2** — `feat(stage): segmentation-driven stage classifier`
  (#754). The `stage_method = MaSlope | Segmentation` enum.

Pending in plan: M5.2d distributional/antifragility, M5.2e per-trade
context, M5.4 E1 short on/off, M5.4 E3 stop-buffer sweep, M5.4 E4
scoring-weight sweep. Status file `## Blocked on` line is stale —
claims M5.1 still red, but M5.1 hardening landed today.

### weekly-snapshot [PLANNED — major progress today, no orchestrator dispatch]

Same shape as `experiments` — track is PLANNED but six of its planned
PRs merged today:

- **M6.1** — `feat(snapshot): weekly snapshot writer/reader` (#753).
- **M6.2** — `feat(snapshot): forward-trace renderer` (#757).
- **M6.3** — `feat(weinstein/snapshot): cross-version pick diff` (#764).
- **M6.4 PR-1** — AAPL 4:1 split/div verification harness (#759).
- **M6.4 PR-2** — TSLA 5:1 split-replay scenario (#761).
- **M6.4 PR-3** — GOOG 2022-07-18 20:1 split scenario (#763).

Pending: M6.4 PR-4 (NVDA 10:1), PR-5 (KO dividend), M6.5 weekly report
renderer. M6.6 (live cycle) deferred. Status file `## Blocked on` line
stale.

### data-foundations [PLANNED — first PR merged today, owner unset]

- **M7.0 Track 3 Synth-v1 block bootstrap** — `feat(synthetic): block
  bootstrap synth-v1` (#755). The first PR in the data-foundations plan.

Pending: Synth-v2 (HMM regime layer), Synth-v3 (multi-symbol factor
model), EODHD multi-market expansion, Norgate ingest (waiting on user
signup). Status file `## Blocked on` line says only the first PR was
unblocked — that one shipped today.

### tuning [PLANNED — no work this run, partial unblock]

`tuning` was blocked on `experiments` M5.2 metrics catalog. M5.2c
(#762) provides the Sharpe/Calmar/Sortino objectives needed for T-A
grid_search. Track stays blocked on per-trade context (M5.2e, not
landed) for ML features. T-A grid_search (~400 LOC) is the smallest
unblock once owner is assigned.

### simulation [IN_PROGRESS]

P0 RED CI from earlier today is RESOLVED:

- `test_split_day_stop_exit:1:post_split_exit_no_orphan_equity` now
  PASSES on `main` HEAD `ad0083e`. Verified via
  `dune runtest --force trading/simulation/test/` — exit 0; the three
  `test_split_day_stop_exit.exe` cases all PASS.
- The fix landed via PR #752: `fix(simulation): unique test_data dir
  per split_day_stop_exit sub-test`. Test isolation issue, not a logic
  bug.

Status file `## Status` body still references the unresolved P0 (it
was filed earlier today before the fix landed). Reconcile deferred —
the maintainer is the right author; the orchestrator's `_index.md`
row is updated to reflect the resolution.

M5 walk-forward + tuner are still nominally next; same dispatch
prereq as the new tracks (owner / scope decision).

### short-side-strategy [MERGED — long-standing follow-ups all landed today]

The four [info] items carried for 4+ consecutive runs are RESOLVED:

- **G6** — `fix(orders): G6 - deterministic order ID generation`
  (#735). `_generate_order_id` no longer uses ns-precision timestamp
  prefix.
- **G14** — `fix(weinstein): G14 — entry_price = current close +
  lookback truncation at split boundary` (#736). Matches Option 1
  from `dev/notes/g14-deep-dive-2026-05-01.md`.
- **G15 step 1** — `feat(force-liquidation): G15 step 1 — asymmetric
  per-position thresholds (long 25%, short 15%)` (#737).
- **G15 step 2** — `feat(weinstein): G15 step 2 — aggregate
  short-notional cap at entry-decision time` (#739).
- **G15 step 3** — `feat(weinstein): G15 step 3 — pre-entry stop-width
  rejection + sizing-uses-installed-stop` (#740).
- **G4 long-side extension surface** absorbed by `portfolio_risk`:
  - `feat(portfolio_risk): add max_position_pct=0.20 per-position cap`
    (#744)
  - `fix(portfolio_risk): clamp position cap >= 0 (handle negative
    portfolio_value with shorts)` (#745)
  - `feat(portfolio_risk): split max_position_pct into long/short
    asymmetric caps` (#746)

Status file `## Status` body still lists these as pending; refresh
deferred.

### backtest-perf [IN_PROGRESS — no work this run]

No new backtest-perf PRs since the prior summary. Status file `## Open
work` block remains stale — most named branches have merged in prior
cycles. Refresh deferred until next dispatched-on-this-track run.

### harness [IN_PROGRESS — no work this run]

Backlog drained. No maintainer-driven harness PRs since prior summary.
Recurring [info] items (qc-structural H3 false-positive on advisory
linter text; review-file persistence gap) carried but not exercised.

### orchestrator-automation [IN_PROGRESS — no work this run]

Phase 1 live. Phase 2 (background execution) pending empirical tests.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| (none this run — no PRs awaiting QC; no dispatches) | | | | | |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **0**
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | (none — no subagent dispatches) | — | — | — | — |
- Any subagent killed mid-flight: NA.

**Measured cost (from GHA execution file):** Will land in
`dev/budget/2026-05-02-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$1 of $50 cap (~2%) — orchestrator overhead only
  (Read/Edit/Bash/curl tool calls).
- Utilization assessment: **UNDER_TARGET** (<60%) — actionable-work-floor.
  Status: "all-work-done" within current scope — no eligible track was
  skipped for cost or queue-depth reasons. The under-target shape this
  run reflects:
  - 25 PRs landed maintainer-driven in the past 15 hours, draining the
    actionable backlog faster than the orchestrator can dispatch on
    stale plans.
  - The four new tracks created today need feat-agent ownership before
    orchestrator can safely dispatch (per `dev/decisions.md` 2026-04-16
    scope-extension precedent).
- Scope reduced due to budget: No.

## Data Operations

(ops-data did not run — `dev/notes/data-gaps.md` substantively unchanged
since 2026-04-14, EODHD_API_KEY unset.)

## Harness Work

(harness-maintainer did not run this run — backlog drained; no
dispatchable T1/T3 items.)

## Health Scan

From `dev/health/2026-05-02-fast.md` (deterministic per Step 6):

- `dune build` exit **0** (PASSING).
- `dune runtest --force` exit **0** (PASSING; full test suite green,
  including the previously-flagged
  `test_split_day_stop_exit:1:post_split_exit_no_orphan_equity`).
- `status_file_integrity.sh` (standalone): exit **0** with `FAIL:` text
  on stdout — 8 advisory failures on the four new tracks created today:
  - `data-foundations.md`, `experiments.md`, `tuning.md`,
    `weekly-snapshot.md` all use `## Status\nPLANNED` (not in the enum)
    and `## Interface stable\nNO — track is brand-new.` (not one of
    `YES|NO|N/A`).
  - The integrity test wired into `dune runtest` does not gate on these.
- Result: **CLEAN** (build gate); 8 schema drifts surfaced for the
  next harness slack window.

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~5
(materially down from ~9 last run; G14/G15/G6/G4 closed).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): with
  G14 (#736), G15 (#737/#739/#740), and portfolio_risk caps
  (#744/#745/#746) all landed, the post-fix sp500-2019-2023 metrics
  should be re-captured. Local-only (491-symbol universe).
- **covid-recovery 20.81% WR investigation** (carried; deferred until
  short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**:
  carried, not exercised this run (no QC).
- **qc-structural review-file persistence gap**: carried, not exercised
  this run.
- **Schema drift from maintainer-driven new tracks** (NEW 2026-05-02):
  the four new track files use `PLANNED` (not in linter enum) and
  `NO — track is brand-new.` (not in `YES|NO|N/A`). Either grow the
  linter enum to include `PLANNED` and accept a free-form trailing
  clause on `Interface stable`, OR edit the four files to use
  `PENDING` + `N/A`. Pre-decision; surfacing for the next harness
  slack window.

## Integration Queue

(none — no PRs open across the repo; this run's `ops/daily-2026-05-02`
branch becomes the only open PR after Step 8.)

## Current Milestone Target

The milestone landscape was reshaped today (2026-05-02) by the
maintainer's planning session: M5/M6/M7 are now decomposed into
sub-milestones M5.1–M5.5, M6.1–M6.6, M7.0–M7.2. Today's progress:

- **M5.1 foundation hardening** — DONE. G6/G14/G15/G4 all landed (see
  `short-side-strategy` row); `simulation` P0 RED CI fixed via #752.
- **M5.2 experiment infrastructure** — partial. M5.2a/b/c MERGED; M5.2d
  + M5.2e pending.
- **M5.4 mechanical experiments** — partial. M5.4 E2 (segmentation
  Stage classifier) MERGED; E1, E3, E4 pending.
- **M6.1–M6.3** — DONE.
- **M6.4 split/div verification harness** — partial. 3 of 5 scenarios
  MERGED.
- **M6.5 weekly report renderer** — pending.
- **M7.0 Track 3 Synth-v1** — DONE.
- **M7.0 Track 1 (Norgate) / Track 2 (multi-market) / Track 3 v2/v3**
  — pending.

The shape of "what's next" depends on a feat-agent ownership decision
for the new tracks (see §Escalations).

## Dependency Unlocks

- **`tuning` partially unblocked** — `experiments` M5.2c (#762) lands
  the Sharpe/Calmar/Sortino metrics catalog that T-A grid_search needs
  as its objective surface. Still blocked on M5.2e per-trade context
  for T-C ML features.

## Escalations

[info] **Track ownership decision pending for the four new tracks
created today**. `experiments`, `weekly-snapshot`, `data-foundations`,
and `tuning` were created in today's manual planning session as
PLANNED. By end of day, several of their planned PRs have already
merged maintainer-driven. The orchestrator cannot safely dispatch on
the remaining backlog without an explicit feat-agent assignment in
`.claude/agents/`. Two natural fits:

  - extend `feat-backtest` scope to absorb `experiments` (M5.2 + M5.4)
    and `tuning` (M5.5 + M7.1) — these are direct successors to its
    current backtest-infra/backtest-scale scope;
  - extend `feat-weinstein` scope to absorb `weekly-snapshot` (M6.1–M6.5)
    — the snapshot subsystem lives under `weinstein/snapshot/`;
  - `data-foundations` straddles `feat-backtest` (synth + streaming) and
    `ops-data` (Norgate, EODHD multi-market).

  Per `dev/decisions.md` 2026-04-16 §"Reopen feat-weinstein for
  support-floor-stops", scope-extension is a documented precedent and
  needs a human decision before the orchestrator dispatches. Surfacing
  as `[info]` because the maintainer is actively driving merges; this
  is a coordination question, not a blocker on green main.

[info] **Per-track status files for the new tracks are stale against
today's merges**. `experiments.md`, `weekly-snapshot.md`,
`data-foundations.md`, and `tuning.md` all claim "Blocked on M5.1
foundation hardening (CI red)" but M5.1 is done and the listed first
PRs have merged in three of four cases. Refresh would be ~30 LOC
across the four files — deferred to maintainer or the next dispatched
run on these tracks. Index row reconciliation done in this run's
PR (Step 5.5).

[info] **`simulation.md` body still references the resolved P0 RED CI**.
The status file was edited earlier today to add the P0 row; PR #752
landed the fix in the same window. Body still reads as if RED;
`_index.md` row reconciled in this run's PR. Refresh deferred.

[info] **`short-side-strategy.md` body still lists G14/G15/G6/G4 as
pending**. All landed today; status file body stale. Refresh deferred
to next dispatched-on-this-track run; `_index.md` row reconciled.

[info] **Schema drift from maintainer-driven new tracks** (NEW). The
four new track files use `## Status\nPLANNED` (not in the
`status_file_integrity.sh` enum
`IN_PROGRESS|READY_FOR_REVIEW|APPROVED|MERGED|BLOCKED|PENDING`) and
`## Interface stable\nNO — track is brand-new.` (not one of
`YES|NO|N/A`). 8 advisory FAILs on stdout, exit 0. Two fixes possible:
(a) grow the linter enum to include `PLANNED` and accept a free-form
trailing clause on `Interface stable`; (b) edit the four files to use
`PENDING` + `N/A`. Pre-decision; harness backlog candidate.

[info] **qc-structural recurring H3 false-positive on advisory linter
text** (carried 6+ consecutive runs). Not exercised this run (no QC).

[info] **qc-structural review-file persistence gap** (carried 6+
consecutive runs). Not exercised this run.

[info] **Cumulative day spend low (~2% of $50 cap)**. Same shape as the
prior several runs: maintainer-driven cycles drain the actionable
backlog between orchestrator slots. The 25 PRs that landed in the past
15 hours include all four long-running [info] items from prior
summaries (G6, G14, G15, G4 long-side extension) — the orchestrator
quite literally has nothing left to dispatch on those surfaces.
Surfacing as `[info]` (not `[critical]` or `[high]`) because it
reflects an actionable-work-floor, not a budget-skip decision.

## Step 1b drift cross-reference

Most recent prior summary on local FS: `dev/daily/2026-05-01-run2.md`
(committed via PR #731 at 2026-05-01T16:50Z; merged to main via #731
auto-merge).

Run-2's `## Pending work` table parsed:
- All rows were classified as `blocked`, `skipped`, or `— (MERGED)` —
  no `dispatched`/`awaiting-merge` rows to verify.

Status file commits on main since prior summary commit
(PREV_ISO ≈ 2026-05-01T16:50Z, excluding `ops:` summary commits): 25
non-summary commits. Status file changes:

- `dev/status/_index.md` — multiple updates as PRs landed.
- `dev/status/simulation.md` — added P0 RED CI row earlier today (since
  resolved by #752).
- `dev/status/experiments.md`, `weekly-snapshot.md`,
  `data-foundations.md`, `tuning.md` — NEW files added today.
- `dev/status/short-side-strategy.md` — touched in last commit
  references but body stale.

No `[drift]` warnings emitted (no track had a "dispatched" row in the
prior summary that would need verification).

## Step 1c carried-forward critical verification

Run-2 of yesterday carried zero `[critical]` items into this run (all
`[info]`). Build + integrity gates re-verified clean (build exit 0;
runtest exit 0; integrity standalone exit 0 with 8 advisory FAILs on
new tracks — see §Health Scan).

[info] Carried-forward verification: no still-real `[critical]` items;
main green (build + runtest exit 0 on `ad0083e`).

All four `[info]` items carried for 4+ consecutive runs — G6 fix
process question, G4 long-side extension, G14 split-adjustment, G15
short-side risk control — are RESOLVED on main today (PRs
#735/#736/#737/#739/#740 + #744/#745/#746). Per Step 1c, these are
NOT carried forward into this run's escalations. New `[info]`s
filed reflect today's drift (track ownership, stale per-track files).

## Per-run links

- [run 1] dev/daily/2026-05-02.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
