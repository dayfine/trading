# Status — 2026-08-27

**Run ID:** 2026-08-27-run-1
**Run timestamp:** 2026-08-27T15:58Z–~19:0xZ (GHA orchestrator run 33090715656)
**Mode:** FULL
**Main:** `8230d67e` → `0d9122c8` → **`f875fe64`** (green: `dune build` 0, `dune runtest` 0, 0 `^FAIL:` lines)

> **Filename note.** An earlier orchestrator run fired today (`33063628087`,
> 10:34Z) and took the no-op fast-exit, so it pushed no summary and this is the
> first `dev/daily/2026-08-27*.md` file — hence the un-suffixed name, per Step 7's
> count-existing-files rule. That earlier run is itself a finding; see
> **Escalations → #2579**.

## Headline

**Two PRs opened, both merged, and both substantive QC findings were about
guards that do not guard.**

**#2581** (`0d9122c8`) closed a coverage hole where breaking `Cancel_handler.
handle_rejected_trades`' deliberate step-5 revert asymmetry left **416/416 tests
green**. **#2580** (`f875fe64`) closed #2576 — the 7th copy of the fused-digit
GNU-`time` RSS bug — plus the structural blind spot behind it.

The run's most valuable output came from #2580's first behavioral pass. The PR
replaced a variable-name-keyed guard with a shape-based one and proved it by
mutation. The reviewer went one level up and asked whether *the suite* pins that
property:

> I re-scoped assertion 6's regex to `rss_path.*tr -d '\n'`, i.e. literally the
> #2576 blind spot: **12 passed, 0 failed, exit 0.** Reducing it to a regex
> matching nothing gave the same green.

The guard swept only the **live repo, which is clean**, so its passing state
carried no information about whether the detector still worked. **The PR had
reproduced the exact failure mode it was fixing, one level up** — a guard that
can be silently reverted to the defect it closes with nothing going red, which
is the #2553 → #2559 → #2576 lineage in miniature. The rework pins it against a
fixture root; both mutations now exit 1.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | auto-merged | `harness/2576-rss-seventh-copy` | #2580 | merged `f875fe64`; next queued item is **#2567** (silent-null effectiveness linter) |
| cleanup | auto-merged | `cleanup/cancel-handler-revert-asymmetry-pin` | #2581 | merged `0d9122c8`; sibling single-conjunct finding left open by design |
| all others | not dispatched | — | — | see §Dispatch decisions |

**Zero open PRs at run end.**

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | harness-maintainer | completed | #2576: ported 7th call site + shape-based assertion 6; RED/GREEN/RED with a novel 3rd var name |
| cleanup | code-health | completed | contract 9 fixture; mutation-proven RED, 127 LOC (under the 200 cap) |
| harness | qc-structural | APPROVED (5) | #2580 @ `6fd4521f`; independent **4th** var-name probe (`$my_memory_kb`) confirmed shape-based |
| cleanup | qc-structural | APPROVED (5) | #2581 @ `063e0239`; reproduced the mutation independently |
| cleanup | qc-behavioral | APPROVED (5) | #2581; 5-mutation × 3-conjunct matrix (see below) |
| harness | qc-behavioral | **NEEDS_REWORK (2)** | #2580 @ `6fd4521f`; the headline finding — guard pinned by nothing |
| harness | harness-maintainer (Rework 1/2) | completed | added assertions 7 + 8; both mutations now RED |
| harness | qc-structural (re-review) | APPROVED (5) | #2580 @ `e29e4c71`; reproduced all 3 mutations |
| harness | qc-behavioral (re-review) | **NEEDS_REWORK (3)** | docs-only: stale PR body (R1) + `harness.md` contradicting its own script header (R2) |
| harness | qc-behavioral (final) | APPROVED (5) | #2580 @ `90085f9d`; R1/R2 closed, prose-only delta independently verified |
| ops-data | — | skipped | `data-gaps.md` unchanged **115 days**; `EODHD_API_KEY` absent in GHA |

**10 agent dispatches, 0 stalls, 0 lost work.** Never more than 2 concurrent —
the 08-26 run measured load 8.4 with two agents on this 4-core runner, so 2 was
treated as the working cap rather than the documented 3.

Rework iteration 2 was done **dispatcher-side** (a 7-line prose fix + a PR-body
rewrite), not by an agent — `pr-gate-loop.md`'s "finishing it from the dispatcher
side is faster than a re-dispatch" applied cleanly to a two-line contradiction.

## The QC findings, in detail

**#2581 — the reviewer proved the test pins the *contract*, not one mutant.**
Five independent break-directions, all compiling clean; four caught:

| mutation | result |
|---|---|
| step 5 ← retry-filtered remainder (the target) | RED |
| step 5 ← `[]` | RED |
| step 2 ← full list (asymmetry collapsed the *other* way) | RED |
| step 5 ← pre-cancel positions map | RED |
| reorder: revert before apply | GREEN — **correct**, the `.mli` says that order is incidental |

It then ran a per-conjunct deletion matrix and found that for the target mutation
alone the test rests on exactly **one** conjunct — the same fragility shape as the
sibling finding already on file — but that the other two conjuncts are each the
sole guard on a *different* `.mli` claim. So none is deletable, and it recorded
that explicitly so a future editor probing only one mutation doesn't prune them.

**#2580 — three durable records disagreed about one PR.** Beyond the unpinned
guard, the reviewer found the PR body still claiming `12 passed, 0 failed` after
the rework, and `dev/status/harness.md` still asserting a multi-line bug "is not
caught" while the script header it summarizes had been corrected to say the
opposite. It proved the latter with `git diff` — the line was never touched, even
though the rework note claimed both follow-ups were fixed. Since the repo
squash-merges, the PR body *is* the durable commit message, so R1 was not cosmetic.

## Corrections and checks on my own work

- I asked the final reviewer to **independently verify my own claim** that the
  `e29e4c71..90085f9d` delta was prose-only and that carrying structural's
  APPROVED forward was therefore sound — the one judgment this run where I had a
  throughput incentive to be wrong. It confirmed: single `M dev/status/harness.md`,
  +7/−2, inside the docs-only allowlist in `pr-merge-gates.md`.
- The reviewer flagged that my rewritten mutation table was labelled one commit
  back (`e29e4c71` vs the tip). Fixed before merge, since the body becomes the
  commit message.
- `git merge-base --is-ancestor` reported **every** squash-merged branch as
  "NOT-merged". Used the PR `merged` field instead; the naive check would have
  produced a wrong call on branch cleanup.

## Health Scan

`dev/health/2026-08-27-fast.md` — **CLEAN**. `dune build` 0, `dune runtest` 0
(0 `^FAIL:` lines), `status_file_integrity` 0, `index_size_linter` 0. CI on
`8230d67e` was independently green on all six checks, **including all three
golden workflows** — `golden-sp500-15y` was still in progress at the close of
2026-08-26 and has since completed success, resolving that open `[info]`.

One measurement artifact recorded there, not a finding: the orchestrator spec's
Step 6.2 snippet invokes `sh trading/devtools/checks/<script>.sh`, which returns
**exit 2** ("cannot open file") because `run-in-env.sh` already cds to the inner
`trading/`. The correct path is `devtools/checks/<script>.sh`. A 2 here reads
exactly like a check failure at a glance — worth correcting in the spec.

## Budget

- Cap: $200 (`max_daily_cost_usd` — a runaway backstop; auth is flat-rate Max-20x)
- Target utilization: 60–95%
- Subagents dispatched: **10**, all completed, **none killed mid-flight**
- Measured subagent tokens: **1,407,014** across the 10 agents (sum of reported
  per-agent totals, not an estimate)
- Estimated cost: **~$5–9** including orchestrator overhead
- **Utilization assessment: UNDER_TARGET (~4%)**, and — as on 08-26 — this is
  *not* a dispatch failure to correct. The binding constraint is the **4-core
  runner**, not quota. The #2580 QC loop (behavioral → rework → structural →
  behavioral → fix → behavioral) is inherently **serial**: each stage consumes
  the previous stage's output. Raising utilization here means a bigger runner or
  more independent tracks, not more concurrent agents.
- Scope reduced due to budget: **No.** Scope was reduced by *dispatchability* —
  see below.

## Dispatch decisions — why the other tracks did not run

Not queue-depth reasoning; each has a named, checked blocker:

- **#2539** (SC2012/SC2086/SC2010 burn-down) — **structurally undispatchable**,
  now measured rather than assumed. All 10 occurrences are in
  `.github/workflows/*.yml`, and this token gets **403** on workflow-file writes
  (control below). Same blocker as **H-BLAS**.
- **#2394** (long cascade RS phase) — its own precondition re-verified false this
  run: `min_rs_normalized` still defaults to `0.0` (`screener.ml:79`,
  `screener.mli:347`) and **zero** specs under `trading/test_data/` or `dev/` arm
  it. The attribution it would add remains unobservable.
- **#2567** (silent-null effectiveness linter) — eligible and **next in queue**;
  displaced by the #2580 rework loop, which outranks new work per
  `container-capacity-scheduling.md` §"QC outranks backtests". Design work
  (linter vs. qc-structural row) that wants plan-first.
- **#2405 / #2489 / #2408 / #2406 / #1729** — container-exclusive or human-gated.
  `docker` does not exist in this runtime; #2489 is an ASKABLE decision.
- **#2547** — still needs flake-rate accumulation, not a code fix.

## Data Operations

ops-data **not dispatched**. `dev/notes/data-gaps.md` is unchanged since
2026-05-03 (**115 days**) and `EODHD_API_KEY` is absent in GHA. Both remaining
gaps need a human first: EODData free-tier registration to check whether
`INDEX:ADVN`/`INDEX:DECL` exist as absolute counts, and a cost/license call on
Pinnacle Data. **10th consecutive run reporting this.**

## Escalations

- **[info] Carried-forward verification: no still-real `[critical]` items.** Main
  green at `8230d67e` (`dune runtest` exit 0, 0 `^FAIL:`), and green again after
  both merges. Every open item from 2026-08-26 was **re-verified by API, not
  copied**: #2559 closed (#2572), #2380 closed, #2555 closed unmerged
  (superseded by #2561), `golden-sp500-15y` now success.

- **NEW [high] #2579 — the `A-FASTEXIT-VACUOUS` fix does not bind, and failed
  silently on a run reporting `success`.** Today's earlier run (`33063628087`,
  $7.96) took the no-op fast-exit on an **empty** queue. Both of the fast-exit's
  own gates said "full pass": zero open PRs for the entire window containing it,
  and **4** non-summary commits touching `dev/status/` since the prior summary
  (`2cddc010`, `87d3fa06`, `1180aa6d`, `42c66a29`). The item is marked
  **FIXED 2026-08-02** in `dev/status/orchestrator-automation.md:731`, and the
  precondition block *is* present in the agent file — but the remedy is **prose in
  an agent file**, so it binds only if the agent applies it that run. Suggested
  backstop in the issue: have the workflow's existing `detect no-op` step refuse
  a `NO-OP` summary when the open-PR count is 0, i.e. move the precondition into
  automation vocabulary. **Blocked from self-fixing** — it edits
  `.github/workflows/orchestrator.yml`.

- **[high] → RESOLVED-AS-MEASURED: the `workflow`-scope question.** Previously
  inferred from a `workflow_dispatch` 403. Now **measured with a control**:
  `PUT /contents/.github/workflows/goldens-affected.yml` on a throwaway branch →
  **403 "Resource not accessible by personal access token"**; an identical
  `PUT` to `dev/notes/data-gaps.md` on the *same branch* → **200**. So the block
  is **path-specific to `.github/workflows/`**, not a general write failure.
  Probe branch deleted, `main` untouched throughout. This is what makes #2539 and
  H-BLAS structurally undispatchable here, and it sharpens 08-26's ask: granting
  this one permission is still the highest-leverage change available.

- **[medium] Issue writes remain create-only — re-verified, not copied.** A live
  `POST /issues/2556/comments` returned **403**. The token can create issues
  (#2579 landed), PATCH pull requests, write non-workflow files, and create/delete
  refs — but cannot comment on or update issues. Consequence this run: **#2556
  appears closeable and I could not say so on the issue.** `Positive_declining` is
  live behind `Rs.config.enable_positive_declining` (default-off) via #2563
  (`1180aa6d`), with consumer coverage in `test_screener.ml`. Recorded here
  instead.

- **NEW [medium] The QC audit trail loses rework history in GHA.**
  `record_qc_audit.sh --pr-number` **refuses to run without `gh`** (correctly — it
  declines to silently fall back to a review file that may belong to another PR),
  and `gh` is absent here. The documented fallback, `write_audit.sh`, takes only
  final verdicts. So #2580's record reads `consecutive_rework_count=0` despite
  going APPROVED → NEEDS_REWORK → APPROVED → NEEDS_REWORK → APPROVED across three
  SHAs. The `>= 3 consecutive rework` escalation this data feeds cannot fire on
  GHA-run PRs. Step 5 Stage 4 tells the orchestrator to *prefer* `--pr-number`
  mode, which can never work in this runtime.

- **NEW [low] A garbled SHA reached a durable record.** qc-structural's re-review
  body spelled `e29e4c71a99f3e2e3a7b8d9e0f1a2b3c4d5e6f7g` — not valid hex
  (trailing `g`). The API-level `commit_id` was correct so the gate held, but
  `dev/scripts/pr_gate_status.sh` reads the **body** for the SHA sentinel, so a
  garbled prose SHA can desync staleness detection. Cheap guard: validate the
  sentinel is 40 hex chars.

- **[low] `mergeable_state=behind` did not block the squash merge** of #2580,
  which suggests "require branches up to date" is **not** enforced on this path —
  worth reconciling with the 08-26 note that `strict: true` has been set since
  2026-05-09. Merged content was verified independently: main's only intervening
  change was #2581 (a simulation test), disjoint from #2580's harness scripts.

- **[info] `H-JJ-JST-BROKEN-GHA` confirmed again.** Both PRs were opened by the
  Step 4.5 curl REST fallback; no dispatched agent can open its own PR. All 10
  agents were briefed around the absence of `gh`, `docker`, `jj`, and `jst`.

- **[info] Branch hygiene.** Deleted 4 merged leftovers (`exp/rep-trade-audit-2489`,
  `feat/rs-positive-declining`, two `ops/budget-*`) plus both merged feature
  branches. Left alone: `experiments/clock-surface-2405` (parked #2405 arc) and
  two `wip/*` salvage branches (4 days old — under the 14-day rule). None has ever
  had a PR.

## Follow-up Queue

- **#2567** — next queued harness item; wants plan-first (the issue offers two
  enforcement designs: a linter over `_*_config_for` adapters, or a
  qc-structural row).
- **From #2580 (assertion 8):** its pass condition is `[ ! -s hits ]`, satisfied by
  any breakage producing no hits; it is non-vacuous only because assertion 7 is the
  positive control. Legitimate pairing, recorded not blocked.
- **From #2580:** an 8th call site that inlines the *correct* `tail -n 1 | tr -d`
  without sourcing the helper would be safe-but-duplicated and is not caught — a
  duplication concern, not a correctness one.
- **From #2581 (`harness_gap: LINTER_CANDIDATE`):** a scoped mutation harness for
  `simulation/lib/`. **Three consecutive reviews** here have derived their value
  from a hand-rolled mutant sweep; that is a tool wanting to exist.
- **From #2581:** the sibling single-conjunct finding in
  `test_entry_fill_retry_e2e.ml` remains open by design.
- `dev/scripts/launch_sweep.sh:195` still carries the `echo | grep -q` +
  `pipefail` shape that #2564/#2573 fixed elsewhere (non-gate script; verified
  still present at line ~195 under `set -euo pipefail` at line 52).
- `dev/status/cleanup.md` — top item is still the test-file-length **policy
  decision**, human-gated across **thirteen** runs.

## Integration Queue

- **#2581** → **MERGED `0d9122c8`.** Three gates green, verdicts both pinned to
  the tip, hold re-checked immediately before the merge call (`pr-merge-gates.md`
  Rule 0): no label, not draft, no labels touched.
- **#2580** → **MERGED `f875fe64`.** CI green at `90085f9d`; qc-behavioral
  APPROVED at that exact tip; qc-structural's APPROVED carried from `e29e4c71`
  on a prose-only delta **independently verified by the behavioral reviewer**,
  not on my own assertion. Hold re-checked immediately before merge.

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable. As on
08-26, both substantive findings this run were **measurement-integrity** defects
rather than strategy defects — a test suite that stayed 416/416 green with a real
asymmetry broken, and a guard whose passing state carried no information. Neither
would have been caught by any existing check, and both were found by mutation.

## Dependency Unlocks

None new. #2576 closed by #2580; #2556 appears closeable (see Escalations).

## Questions for You

1. **Grant the token write access to `.github/workflows/`?** Now measured
   precisely rather than inferred (403 on workflow paths, 200 on everything else,
   same branch and API call). It unblocks **#2539**, **H-BLAS**, and — per 08-26's
   ask — automatic generation of the paired postsubmit table that
   `config-default-blast-radius.md` demands. Still the highest-leverage permission
   change available.
2. **#2579 — fix the no-op fast-exit in the workflow, or keep it as agent prose?**
   I recommend the workflow-step backstop: prose has now demonstrably failed to
   bind once, silently, on a run that reported success. I cannot make this change
   (see Q1).
3. **#2567 next?** It is the top eligible item, and it is the *third* instance of
   the silent-null class. It wants a design decision first (linter vs.
   qc-structural row) — happy to dispatch it plan-first next run unless you'd
   rather scope it yourself.
4. **#2556 can be closed** — implemented by #2563, verified on main. I lack the
   permission to close or comment on it.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
