# Status — 2026-05-07

**Run timestamp:** 2026-05-07T08:09:58Z (run start) — 2026-05-07T08:50:00Z (summary write, approx)
**Run ID:** 2026-05-07-run-1
**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 2 FAIL: 19 non-summary status-touching commits on main since prior summary commit at 2026-05-06T13:41:55Z, including #898 (status refresh), #903 (velocity LOC), #905 (velocity_report.sh + OCaml test/source split)).

## Saturated-queue check result

- **Condition 1** N/A — open PRs at run start: 0 (the pre-dispatch state had nothing to QC).
- **Condition 2** FAIL — 19 non-summary commits on main since `PREV_ISO=2026-05-06T13:41:55+02:00`, including 3+ status-file-touching commits (#898 status refresh, #903 + #905 harness updates).
- **Condition 3** N/A — Step 1b ran; no `[drift]` warnings.
- **Condition 4** N/A (Condition 2 already failed).

Decision: full pass. **1 feat-backtest agent dispatched** on T-B Bayesian-opt CLI binary + QC pipeline on resulting PR.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| tuning | awaiting-merge | feat/backtest-tuning-bayesian-opt-cli | #914 | T-B CLI binary (`bayesian_runner.exe`); QC APPROVED q=4; CI green at QC time. After merge: 81-cell flagship sweep on `screening.weights.*` (local-only); smoke-scenario sanity-check (local-only); T-C ML/HMM (Norgate-blocked). |
| all-eligible | merged-this-cycle | — | — | PR-2 CLI exe MERGED via #901; min_grade gate MERGED via #908; Friday-breakout dedup MERGED via #904. Pending: PR-3 wiring into release-report pipeline; Stage-1→2 fixture; extract shared scaffolding. |
| harness | merged-this-cycle | — | — | #895 branch protection on `main` MERGED (closes #885 — was carried 2 runs as human-action item); #905 velocity_report.sh MERGED. Backlog drained. |
| backtest-perf | merged-this-cycle | — | — | #897 perf tier4-broad-1y SCALE cell MERGED. V1/V2/V3 verifications local-only. |
| simulation | merged-this-cycle | — | — | #911 fix degenerate actual.sexp on crash + dedup adjust+exit collisions MERGED. M5 walk-forward + tuner; sp500 baseline rerun (local-only). |
| short-side-strategy | merged-this-cycle | — | — | #909 laggard rotation MERGED (issue #887). #902 Stage-3 force-exit detector MERGED (issue #872). #910 capital-recycling combined sweep MERGED. #913 15y-cell-e Sharpe 0.94 5y→15y generalisation MERGED. sp500 baseline rerun (local-only). |
| experiments | not dispatched (no eligible item) | — | — | M5.4 sweep harnesses landed; sweep RUNS local-only. CLI binaries deferred. |
| data-foundations | not dispatched | — | — | M5.3 streaming Phase F COMPLETE. Pending: M7.0 Synth-v3 (~1000 LOC, large — would need plan-first); Norgate (vendor-blocked); EODHD multi-market resolver already landed via #772. |
| weekly-snapshot | merged | — | — | M6.6 live cycle DEFERRED. |
| orchestrator-automation | blocked (Phase 2 pending) | — | — | Phase 2 background-execution items pending empirical tests. |
| cleanup | blocked (no findings) | — | — | Backlog empty. No new `[medium]`/`[high]` findings from Step 6 deterministic checks. |
| cost-tracking | blocked | — | — | Today's measured cost will land in `dev/budget/2026-05-07-<run_id>.json` post-GHA-capture. |
| ops-data | not dispatched | — | — | data-gaps.md substantively unchanged since 2026-05-03 maintainer cycle; sectors.csv 21 days old (refresh threshold >30 days). |
| (all other tracks merged) | — | — | — | optimal-strategy / data-panels / hybrid-tier / trade-audit / backtest-infra / backtest-scale / data-layer / portfolio-stops / sector-data / strategy-wiring / support-floor-stops / screener — no change this run. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| tuning | feat-backtest | completed | Branch `feat/backtest-tuning-bayesian-opt-cli` (tip `826e90f8`); 11 files; +879/-8 LOC across 4 modules + 1 binary + tests; 11/11 new unit tests pass; 72/72 in tuner/ pass. PR #914 opened ready-for-review. Wires `Tuner.Bayesian_opt.suggest_next/observe` to a `Backtest.Runner.run_backtest`-backed evaluator; sexp-driven param spec (bounds + acquisition + objective + budget); writes `dev/tuning/<name>/{bo_log.csv, best.sexp, convergence.md}`. Smoke-scenario sanity-check intentionally deferred (mirrors PR #893's deferral; documented in PR body + status file). Determinism property pinned (same seed → byte-identical bo_log.csv via runner's full output path). Convergence pinned on 1D parabola (best_x within 0.5 of 3.0 with budget=20). |
| tuning-pr914 | qc-structural | APPROVED | All H1-H3, P1-P6, A1-A3 PASS. Build/runtest gates clean on tip `826e90f8`. Zero modifications to `tuner/lib/` (lib stays stable per PR #817 → #914 split). Review file written correctly to canonical path `/__w/trading/trading/dev/reviews/tuning-pr914.md` (no path-typo this run). |
| tuning-pr914 | qc-behavioral | APPROVED quality_score 4 | CP1-CP4 all PASS. Eleven tests pin every new `.mli` contract (spec parser × 5, evaluator cache-miss × 1, runner three-artefact emission × 1, mkdir-p × 1, parabola convergence × 1, determinism × 1, malformed-input Failure × 1). D1-D8 plan decisions inherited from lib unchanged. Domain checklist NA (pure infra / CLI wiring; no Weinstein domain logic). Notes: minor LOC overage (+879 vs claimed ≤500; +871 net) reflects T-B's heavier output-format surface (3 artefacts × tests) compared to T-A; well within ordinary tolerance. |
| tuning-pr914 | (orchestrator) | flipped draft→ready (no-op — agent opened ready); audit written | `dev/audit/2026-05-07-tuning-pr914.json` (overall_qc APPROVED q=4; consecutive_rework_count=0). |
| ops-data | — | not dispatched | data-gaps.md substantively unchanged since 2026-05-03 maintainer cycle; sectors.csv 21 days old (threshold >30 days). |
| harness | — | not dispatched | Backlog drained — #895 (branch protection) MERGED this cycle (closes the human-action item carried for 2 runs). #905 velocity_report.sh + OCaml test/source split MERGED. No remaining T1/T3 actionable items. |
| cleanup (Step 2e) | — | not dispatched | Backlog empty AND no new `[medium]`/`[high]` findings from Step 6 deterministic checks. |
| code-health | — | not dispatched | Same as cleanup — backlog empty. |

## Feature Progress

### tuning [IN_PROGRESS — T-B CLI binary dispatched]

T-A grid_search lib + 24 tests landed via PR #805 (2026-05-03). T-A CLI binary landed via PR #893 (2026-05-06). T-B Bayesian-opt lib + 30 tests landed via PR #817 (2026-05-04). All `.mli` surfaces stable.

**Today's orchestrator dispatch on T-B CLI binary**: feat-backtest on PR #914 (`feat/backtest-tuning-bayesian-opt-cli`):

- New `bin/bayesian_runner.ml` (98 LOC) + 3 supporting modules: `bayesian_runner_spec.{ml,mli}` (sexp parser; bounds + acquisition + objective + budget record), `bayesian_runner_evaluator.{ml,mli}` (real-backtest wiring via `Backtest.Runner.run_backtest`; mean-aggregates over scenarios), `bayesian_runner_runner.{ml,mli}` (drives BO ask/tell loop for `total_budget` iters; emits `bo_log.csv`, `best.sexp`, `convergence.md`).
- Module split deliberate: `_runner` is a pure plumbing layer that `_evaluator` plugs into, so unit tests drive `run_and_write` against a stub closure (e.g., 1D parabola) without spinning up a real backtest. Mirrors PR #893's split.
- Spec sexp shape: record `((bounds (...)) (acquisition <variant>) (initial_random N) (total_budget N) (seed (Some n)|None) (n_acquisition_candidates (Some n)|None) (objective <variant>) (scenarios (...)))` with `[@@sexp.allow_extra_fields]` for forward compat.
- 11/11 unit tests pin: spec parsing (simple Expected_improvement; UCB + Composite + n_acquisition_candidates=Some + seed=None; malformed Failure), `to_grid_objective` round-trip (4 simple variants + Composite via parse), `to_acquisition` round-trip (EI + UCB), `to_bo_config` field propagation (4 fields), evaluator cache-miss `Failure`, `run_and_write` three-artefact emission (CSV size_is 21 = 1 header + 20 data rows for budget=20), `mkdir -p` semantics for nested out-dirs, parabola convergence (best_x lt 0.5 from 3.0 with budget=20), determinism (same seed → byte-identical bo_log.csv via full output path).
- Smoke-scenario sanity-check deferred: lightest tier-2 smoke is 5-10 min full-universe; too heavy for the session budget. Documented in PR body + `dev/status/tuning.md`. Recommended local-verification before the 81-cell flagship sweep + before T-B convergence cross-check vs T-A grid result.

### Capital-recycling sprint (maintainer-driven, 19 PRs since prior summary)

Sprint headline: **15y baseline Sharpe 0.94 with Cell E (Stage3+Laggard combined)** — generalises 5y → 15y per #913. Trade chain:
- #902 Stage-3 force-exit detector (issue #872 capital recycling)
- #909 laggard rotation lighten-up before stop-out (issue #887)
- #908 all-eligible min_grade quality gate + grade sweep (issue #870)
- #899/#901 all-eligible PR-1 lib + PR-2 CLI MERGED (sister to optimal-strategy; raw signal alpha measure)
- #904 all-eligible dedup consecutive Friday breakouts
- #911 fix degenerate actual.sexp on crash + dedup adjust+exit collisions (debugging stability)
- #906 / #910 / #913 capital-recycling experiments (Stage3 force-exit, combined Stage3+Laggard 5-cell sweep, 15y cell E)
- #907 Position generic StrategySignal exit_reason variant — A1 follow-up to #902 generalising the exit_reason carry-over
- #895 GitHub branch protection on `main` (required status checks: build-and-test + perf-tier1-smoke; closes #885 — carried 2 runs)
- #897 perf tier4-broad-1y mechanic-validation SCALE cell (backtest-perf)
- #905 harness velocity_report.sh + OCaml test/source split

Track-level cross-cuts:
- **short-side-strategy**: #902 / #909 / #913 capital-recycling chain — strategy-side core feature work, all merged this sprint.
- **all-eligible**: PR-2 CLI MERGED via #901; #908 + #904 polished. PR-3 (release-report pipeline wiring) remains the open follow-up.
- **harness**: #895 branch protection closes the carried [info] from 2 prior runs.
- **simulation**: #911 stabilisation fix.
- **backtest-perf**: #897 SCALE cell add.

### experiments / data-foundations / backtest-perf [no work this run]

Sweep RUNS for M5.4 E3/E4 are local-only. M7.0 Synth-v3 (~1000 LOC) too large without plan-first triggers. EODHD multi-market resolver already shipped via #772 (verified — `analysis/data/sources/eodhd/lib/exchange_resolver.{ml,mli}` carries all 6 markets with parsing/currency/calendar; 21 + 5 + 6 + 15 tests pass). Norgate vendor-blocked. backtest-perf V1/V2/V3 verifications + tier-4 manual workflow_dispatch are local-only.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| tuning (T-B CLI binary) | #914 (open, not draft, mergeable_state=blocked at run start; CI green per `check-runs` at QC time: `build-and-test` success + `perf-tier1-smoke` success) | APPROVED | APPROVED | 4 | `dev/audit/2026-05-07-tuning-pr914.json` (consecutive_rework_count=0) |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **3** (1 feat-backtest + 1 qc-structural + 1 qc-behavioral)
- Per-subagent breakdown:
  | Agent | Model | Status | Tool uses | Tokens |
  |-------|-------|--------|-----------|--------|
  | feat-backtest (PR #914) | (default) | completed (~16 min, 108 tool uses) | 108 | 191K |
  | qc-structural (PR #914) | (default) | completed (~31 min, 51 tool uses) | 51 | 75K |
  | qc-behavioral (PR #914) | (default) | completed (~3.4 min, 25 tool uses) | 25 | 82K |
- Any subagent killed mid-flight: NO.

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-05-07-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$3-5 of $50 cap (~6-10%) — orchestrator overhead (Read/Edit/Bash/curl + 1× `dune build && dune runtest --force` pre-dispatch + 1× `dune build && dune runtest --force` post-dispatch + 2× `status_file_integrity.sh` invocations) + 3 subagent dispatches.
- Utilization assessment: **UNDER_TARGET** (<60%) — ~6-10% of cap. Eligible-work analysis: only 1 dispatchable feat track without breaching scope rules (T-B CLI binary). All-work-done: yes — remaining IN_PROGRESS tracks were either local-only (backtest-perf V1/V2/V3, simulation baseline rerun, experiments E3/E4 sweeps), too large without plan-first (Synth-v3 ~1000 LOC), human-action already completed (harness #895 branch protection closed itself), or vendor-blocked (Norgate / T-C ML). EODHD multi-market resolver already shipped via #772; no incremental work to dispatch on data-foundations.
- Scope reduced due to budget: NO.

## Data Operations

(ops-data did not run.)

`data-gaps.md` substantively unchanged since 2026-05-03 maintainer cycle. `data/sectors.csv` is 21 days old (refreshed 2026-04-16); refresh recommended at >30 days. Not yet stale; not dispatched.

## Harness Work

(harness-maintainer did not run this orchestrator session.)

#895 branch protection on `main` MERGED this cycle (closes #885 — carried for 2 runs as human-action). Required status checks now enforced: `build-and-test` + `perf-tier1-smoke`; `enforce_admins: true`; no force-pushes; no deletions; `strict: true`. #905 harness velocity_report.sh + OCaml test/source split MERGED.

## Health Scan

From deterministic Step 6 checks:

- `dune build` exit **0** (PASSING) on HEAD `bb72fe5`.
- `dune runtest --force` exit **0** (PASSING) on HEAD `bb72fe5` — verified live this session pre-dispatch + post-dispatch.
- `status_file_integrity.sh` (direct invocation) exit **1** with 7 advisory FAILs — `all-eligible.md` (×2 — `## Status` trailing clause + missing `## Interface stable`), `hybrid-tier.md`, `orchestrator-automation.md`, `tuning.md`, `weekly-snapshot.md` (×2). Same 5 carried-forward FAILs from prior runs PLUS one new (`all-eligible.md`'s schema not aligned). When invoked via `dune runtest`, the script's exit is not propagated to dune's exit code (verified — `dune build @runtest --force` exits 0). Per orchestrator policy: gate is dune exit code only.
- Result: **CLEAN** (build/runtest green; advisory integrity warnings carried; one new violation introduced by `all-eligible.md` schema gap).

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~7 (carried + 1 new).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): local-only.
- **covid-recovery 20.81% WR investigation** (carried; deferred until short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**: NOT exercised this run (qc-structural exited cleanly with proper exit-code-only H3 check; agent explicitly noted "linter failures in unrelated modules are unchanged"). 14+ consecutive runs with sporadic exercise; recommendation re-iterated: tighten qc-structural agent definition to enforce exit-code-only H3 verdict.
- **qc-structural review-file persistence gap**: NOT exercised this run (agent wrote correctly to canonical `/__w/trading/...` path on first try). 14+ consecutive runs with sporadic exercise; recommendation re-iterated: anchor write path with `realpath` or `git rev-parse --show-toplevel`.
- **Schema drift from maintainer-driven status updates** (carried 10 runs, advisory only — but **count up from 5 → 7 this run** because `all-eligible.md` is now in the FAIL list). Pre-decision: grow `status_file_integrity.sh` enum vs. enforce strict prefix-only. Harness backlog candidate.
- **magic_numbers linter false-positives on docstring text** (carried 9 runs, not exercised this run). Linter-side fix candidate; harness backlog.
- **Smoke-scenario sanity-check on T-B CLI binary** (NEW 2026-05-07): the agent's session deferred this; recommended local-verification before T-B convergence cross-check vs the T-A grid sweep result. Quick verification: pick `bull-2019h2.sexp`, run `bayesian_runner.exe` with `total_budget=10` against a 2-dim spec, confirm output files materialise.
- **PR-3 release-report pipeline wiring for `all_eligible_runner`** (carried from prior all-eligible status file); deferred per status file but listed in Open work.
- **3 side-issues from #853 (carried from priorities note)**: equity_curve.csv truncation; MRO zombie position; progress.sexp current_equity discrepancy. Not yet filed as separate issues per `dev/notes/next-session-priorities-2026-05-05.md` §Priority 5.

## Integration Queue

(Features with overall_qc APPROVED — ready to merge to main pending human decision)

- **#914 (tuning / T-B CLI binary)** — `feat/backtest-tuning-bayesian-opt-cli` @ tip SHA `826e90f8abee4d2fbb5020baa2cafdc23afcc857`. mergeable_state=blocked at run start (CI in flight); CI green per `/check-runs` at QC time (`build-and-test` success + `perf-tier1-smoke` success); not draft. qc-structural APPROVED; qc-behavioral APPROVED q=4; audit `dev/audit/2026-05-07-tuning-pr914.json` written. **Recommended merge action**: human reviewer can merge cleanly once branch-protection re-checks pass on the latest CI. Wires shipped lib (`Tuner.Bayesian_opt`, PR #817) to a `Backtest.Runner.run_backtest`-backed evaluator. Smoke-scenario sanity-check deferred (documented; quick local-verify owner: human reviewer or next session).

## Current Milestone Target

After this run's dispatch:

- **M5.3 daily-snapshot streaming** — Phase F COMPLETE 2026-05-06. `Bar_panels` retired end-to-end via #875/#876/#877. The full streaming pipeline is the production runtime path.
- **M5.4 mechanical experiments** — partial. E1 / E2 / E3-harness / E4-harness MERGED; sweep RUNS local-only.
- **M5.5 parameter tuning** — T-A grid_search lib MERGED; T-A CLI binary MERGED; **T-B Bayesian-opt lib MERGED**; **T-B CLI binary in flight via #914** (this run's dispatch). Pending after #914: 81-cell flagship sweep on `screening.weights.*`; T-C ML/HMM (Norgate-blocked).
- **M6 weekly-snapshot verification** — DONE on M6.1-M6.5 content; M6.6 live cycle DEFERRED.
- **M7.0 Track 1 Norgate** — pending (vendor signup blocker).
- **M7.0 Track 2 multi-market** — DONE (#772 — verified this run that resolver carries all 6 markets with parsing/currency/calendar/tests; track is genuinely complete despite data-foundations.md still listing it as "small, parallel pending").
- **M7.0 Track 3 Synth-v1 + v2** — DONE; **Synth-v3 pending** (~1000 LOC).
- **Wiki+EODHD interim historical universe** — PR-A/B/C/D ALL MERGED.
- **Tier-4 release-gate** — scaffolding (#810) + SCALE (#811) + Daily_panels O(log N) (#845) + tier4-broad-1y (#897) MERGED; manual `workflow_dispatch` is local-only.
- **CI golden-runs Phase 1** — LIVE 2026-05-06 (#873). Postsubmit baseline on every main push.
- **GitHub branch protection on main** — LIVE 2026-05-07 (#895 — required status checks `build-and-test` + `perf-tier1-smoke`, `enforce_admins: true`).

**Capital-recycling experiments** — Sprint headline: **15y SP500 Sharpe 0.94 with Cell E (Stage3+Laggard combined)** — generalises 5y → 15y per #913. Strategy-level work all merged: #902 Stage-3 force-exit, #909 laggard rotation, #907 generic exit_reason variant.

## Dependency Unlocks

- **#914 (T-B CLI binary) awaiting merge unblocks**: T-B Bayesian-opt convergence cross-check vs T-A grid result (the M5.5 acceptance criterion in `m5-experiments-roadmap-2026-05-02.md` §M5.5 T-B). After #914 lands + smoke-sanity-check passes locally, the convergence experiment is dispatchable.
- **#895 branch protection MERGED unblocks**: hard CI gate on PR merge (was bypassable before; closes #885 / harness follow-up).

## Escalations

(All `[info]`-tagged; no `[critical]` items raised this run. Live verification of main on HEAD `bb72fe5` shows `dune build && dune runtest --force` exit 0.)

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried 14+ consecutive runs; NOT exercised this run — qc-structural's H3 row passed cleanly with `dune build && dune runtest` exit 0 and explicit "pre-existing linter failures in unrelated modules are unchanged" notation). Recommendation re-iterated: tighten qc-structural agent definition to enforce exit-code-only H3 verdict; never derive H3 FAIL from finding-name text alone.

[info] **qc-structural review-file persistence gap** (carried 14+ consecutive runs; NOT exercised this run — agent wrote correctly to canonical `/__w/trading/trading/dev/reviews/tuning-pr914.md` on first try). Recommendation re-iterated: anchor write path with `realpath` or `git rev-parse --show-toplevel`.

[info] **Schema drift from maintainer-driven status updates** (carried 10 runs, advisory only — count up from 5 → 7 this run because `all-eligible.md` schema not aligned). Pre-decision: grow `status_file_integrity.sh` enum vs. enforce strict prefix-only. Harness backlog candidate. The new track files created in 2026-05 (`all-eligible.md`, `tuning.md`, `data-foundations.md`, `experiments.md`, `weekly-snapshot.md`) all use free-form trailing clauses on `## Status` and `## Interface stable`, suggesting the strict-prefix schema is the wrong long-term target.

[info] **magic_numbers linter false-positives on docstring text** (carried 9 runs, not exercised this run). Linter-side fix candidate; harness backlog.

[info] **3 side-issues from #853 not yet filed as issues** (carried from `dev/notes/next-session-priorities-2026-05-05.md` §Priority 5): equity_curve.csv truncation at 2010-11-16; MRO zombie position (in trades.csv AND open_positions.csv); progress.sexp current_equity vs summary.sexp final_portfolio_value 10× discrepancy. Recommendation: file as separate GitHub issues so they don't get lost.

[info] **Carried-forward verification (Step 1c)**: no still-real `[critical]` items; main green (exit 0 on `bb72fe5` for both `dune build` and `dune runtest --force` pre-dispatch + post-dispatch).

## Step 1b drift cross-reference

Most recent prior summary on local FS / on main: `dev/daily/2026-05-06.md` (run-1; committed via PR #894 at 2026-05-06T13:41:55+02:00; merged to main as `cfc2b65`).

Run-1's `## Pending work` table parsed for `dispatched`/`awaiting-merge` rows:
- `tuning (T-A CLI binary)` — `awaiting-merge` (PR #893, tip `bc944151`). Status today: **MERGED** as commit `4de4f1d`. Normal lag, not drift.
- `screener (`min_score_override`)` — `awaiting-merge` (PR #892, maintainer-driven). Status today: **MERGED** as commit `52fa6b4`. Normal lag, not drift.

All other rows were `not dispatched`, `merged`, or `blocked` — no `dispatched` rows that would need verification today.

Status file commits on main since prior summary commit (PREV_ISO 2026-05-06T13:41:55+02:00, excluding `ops:` summary commits):
- #898 (status refresh post #892/#893)
- #903 + #905 (harness/velocity additions touching status)

All maintainer-driven; no agent-driven status drift.

No `[drift]` warnings emitted.

## Step 1c carried-forward critical verification

Run-1 carried zero `[critical]` items into today (run-1's §Escalations were all `[info]`-tagged). Re-verified live this session on the new HEAD `bb72fe5`:

```
$ cd /__w/trading/trading/trading && eval $(opam env) && dune build && dune runtest --force
exit=0
```

Both exit 0 on pre-dispatch HEAD `bb72fe5` and post-dispatch HEAD (same — no merge during this run). **No `[critical]` items to carry forward.**

[info] Carried-forward verification: no still-real `[critical]` items; main green on `bb72fe5`.

All carried-forward `[info]` items from prior runs re-carried into today's §Escalations as observed.

## Per-run links

- [run 1] `dev/daily/2026-05-07.md` (this file — feat-backtest on T-B CLI binary; QC pipeline APPROVED q=4; audit written)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
