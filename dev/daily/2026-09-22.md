# Status — 2026-09-22

**Run ID:** 2026-09-22-run1
**Run timestamp:** 2026-09-22T12:45Z (GHA orchestrator run 35728381545)
**Mode:** FULL
**Main at run start:** `94a2e378` — green (`dune build` 0, `dune runtest` 0, zero `^FAIL:` lines). Main after this run’s merges: **`4afa2e44`**.

> **Skeleton written at Step 1, before any dispatch, and updated in place** —
> third consecutive run applying the hand-mitigation for the lost-summary defect
> (#2741 class). Two runs (09-16 `35096884441`, 09-20 `35510571160`) exited green
> and wrote no summary at all. A stale artifact beats an absent one.

---

## Step 0.5 — fast-exit skipped

`QUEUE_NON_EMPTY=8` (workflow-computed). **Condition 1 FAILS outright**: all eight
open PRs carry **zero reviews**, so `tip_sha != last_review_sha` on every one of
them. The queue is not saturated — it is entirely *unreviewed*. Full pass.

(First-run-of-day escape hatch also applies: no `2026-09-22*.md` existed at run
start.)

## The headline: there was a second run on 09-21, it cost $10.89, and it left nothing

`scheduled_workflow_health.sh` reported `Daily orchestrator` **RED, streak=1**,
`newest_completed_run_id=35636902726`. That is a run I had no record of — the
09-21 summary on `main` is run 1 (`35610789642`). Run `35636902726` fired at the
second cron slot (18:12Z) and **failed at step 10, "Locate daily summary +
detect no-op"**, with both mechanical backstops firing at once:

```
##[error]A-FASTEXIT-VACUOUS (issue #2803): dev/daily/2026-09-21-run2.md declares FULL
  mode but no open-or-merged PR exists for branch ops/daily-2026-09-21-run2, and the
  summary is not present on origin/main. The daily summary was NOT published
##[error]A-FASTEXIT-VACUOUS (issue #2810): ... ## Dispatched this run claims a dispatch
  with no artifact to show for it
##[error]  unresolved writing-agent row: harness (#2886)	harness-maintainer	*(in flight)*
##[error]  unresolved writing-agent row: harness (#2887)	harness-maintainer	*(in flight)*
```

Verified independently: `git log origin/main -- dev/daily/2026-09-21-run2.md` is
**empty**, and the file does not exist in the checkout. The narrative is gone.
The **budget record survived** via the `A-NOOP-BUDGET-ORPHAN` fallback path
(`dev/budget/2026-09-21-35636902726.json`), which is how the price is known:
**`total_cost_usd` = $10.89, for zero durable output.**

Three things worth separating, because they are easy to collapse into one:

1. **The backstops worked.** #2803 and #2810 both fired, named the branch, named
   the two unresolved rows, and failed the job loudly rather than exiting green.
   This is the first run I can point to where the "green run indistinguishable
   from a productive one" gate actually caught a live instance in the wild.
2. **The write-early mitigation is necessary but not sufficient.** Run 2 *did*
   write a summary — it died at **publication**, not authorship. Writing the
   skeleton at Step 1 protects against the run ending before Step 7; it does
   nothing for a Step 8 that fails to push. Both halves need the fix.
3. **The two claimed dispatches (#2886, #2887) produced nothing** — and #2886 has
   since been fixed anyway, by **Codex** (PR #2908, in QC this run), not by the
   orchestrator. #2887 remains open and I re-measured it still-broken below.

## Step 1b — drift cross-reference

Prior summary on `main` is `dev/daily/2026-09-21.md` (run 1). Its two merges
verified on `main`: **#2884 → `9b9751f5`**, **#2888 → `c62ec6df`**. Both present.

**One `[drift]` warning**, and it is the run-2 hole above: the 09-21 run-2
summary recorded `harness (#2886)` and `harness (#2887)` as dispatched
`*(in flight)*` and neither produced a branch, a PR, or a status-file edit. The
row is unresolvable rather than merely stale, because the summary asserting it
never reached `main`.

**Index drift found and fixed in Step 5.5:** the `backtest-perf` row still
carried `#2888` in its Open PR column; #2888 merged yesterday.

> Note on the "newest file" hazard (#2850): I did **not** use `ls -t` anywhere.
> `actions/checkout` stamps every tracked file with one identical mtime, so
> `ls -t | head -1` returns the *oldest* file. All newest-file lookups this run
> used `sort | tail -1` or `git log`.

## Step 1c — carried-forward `[critical]` verification

The 09-21 summary carried **no `[critical]` escalations**. Nothing to re-verify.

`[info]` Carried-forward verification: no still-real `[critical]` items. Recorded
explicitly so a later reader can tell "checked, nothing wrong" from "not checked"
— a `[critical]` here would fire the "Fail on escalations" gate on a false
premise, which is the whole point of Step 1c.

**#2887 re-measured, still live.** The one carried-forward defect I could test
directly, and it reproduces in one call:

```
$ sh dev/scripts/orchestrator_fastexit_gate.sh prior_summary_iso
2026-09-14T12:50:15-07:00
```

The true prior summary is **2026-09-21**. It returned **2026-09-14** — a
seven-day error, in the *widening* (safe) direction, exactly as #2887 predicts.
Carried forward as `[high]`, not `[critical]`: the error is conservative today,
and it only becomes dangerous once the write-early change lands (then defect (a),
the missing exclude-arg, baselines the window against the run's own file).

## The queue: eight open PRs, zero reviews on any of them

This is the inverse of yesterday, which ended with an empty track queue and
called it "maximum dispatch capacity for the next run". That capacity was taken —
but almost entirely by **Codex and by the maintainer's local session**, not by
orchestrator dispatch. Of the eight, three are Codex-authored (`author/codex`),
three are maintainer harness/experiment work, two are workflow-generated ops
artifacts. Nothing was waiting on a feat-agent.

So the run's whole job was the **gate loop**, which `.claude/rules/pr-gate-loop.md`
already says is the default work item ahead of new features. I treated it that way.

| PR | branch | class | disposition |
|---|---|---|---|
| **#2898** | `ops/health-deep-2026-09-21` | **docs-only** (1 × `.md`) | **MERGED** `c2ff9923` — CI is its only gate |
| **#2903** | `codex/2847-prune-rest` | code | **MERGED** `4afa2e44` — both gates APPROVED |
| **#2908** | `codex/2886-meta-payload` | code | structural APPROVED, behavioral **NEEDS_REWORK** → reworked in-run |
| #2909 | `codex/2899-diagnostic-flag` | code | deferred — capacity |
| #2906 | `harness/results-only-lane` | code | deferred — capacity **+ conflicts with #2908** (see below) |
| #2907 | `harness/codex-review-budget` | code | deferred — capacity |
| #2900 | `exp/top-of-funnel-results` | code+results | deferred — **moving target**, see below |
| #2893 | `ops/weekly-start-sweep-2026-09-21` | mixed | deferred — its own body routes it to a human decision |

### Two deferrals that are not "queue depth" reasoning

Step 1.5 explicitly forbids skipping on queue-depth or reviewer-load grounds, so
both of these are stated against something checkable:

- **#2900 is mid-flight, not merely unreviewed.** Three commits, the newest at
  **08:42Z today**, and the maintainer's own `next-session-priorities-2026-09-22.md`
  P0 step 4 says the PR still needs **salts 1 and 2**, the ledger entry, and the
  README §Verdict. The newest commit carries **s0 only** (`t1-topn-40 s0 — 237.4 %
  vs null 457.0 %`). Reviewing it now would pin a verdict to a SHA that is
  guaranteed to move. It is also `behind`.
- **#2893's PR body asks for a human decision**, in those words: *"Advisory:
  review and merge (or close if uninteresting)."* It is a workflow-generated
  weekly snapshot (+286/−294 on one report and its `.sexp`), not orchestrator
  track work. Note it is **mixed**, not docs-only — the `.sexp` is outside the
  `pr-merge-gates.md` docs-only allowlist — so it would need the full pair.

### A collision the next run needs to know about

**#2906 and #2908 both modify `dev/scripts/pr_gate_status.sh`** (+38/−0 and
+43/−5). Whichever lands first forces the other to rebase. Merging #2908 first is
the right order — it is the smaller diff and it fixes a defect that is **live on
main right now**; #2906 adds a new lane on top. Recorded so the next session does
not discover it as a surprise conflict.

## Dispatched this run

Cap set **mechanically**, not by judgement — `dispatch_disk_guard.sh` was
consulted before every dispatch decision:

```
$ sh dev/scripts/dispatch_disk_guard.sh 2
OK: 61GB free meets the 52GB floor for 2 agent(s); dispatch fits       (exit 0)
$ sh dev/scripts/dispatch_disk_guard.sh 3
REFUSE: 61GB free is below the 68GB floor for 3 agent(s)               (exit 1)
```

**2 concurrent, sequenced in waves.** Each brief carried `flock /tmp/dune.lock`
and "run dune in the FOREGROUND with `timeout: 600000`" — this runner is
memory-bound at 15 GB with a *silent* OOM, and the log-wait form of that guard is
itself a trap a subagent cannot escape.

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| prune-rest (#2903) | qc-structural | **APPROVED** (5) | @ `90aa26b0`; REST block pattern-faithful to #2842 |
| gate-reader (#2908) | qc-structural | **APPROVED** (5) | @ `5a504652`; 140k-char fixture confirmed to exist |
| prune-rest (#2903) | qc-behavioral | **APPROVED** (5) | @ `90aa26b0`; **17 mutations, 17 RED** |
| gate-reader (#2908) | qc-behavioral | **NEEDS_REWORK** (2) | @ `5a504652`; **14 mutations, 8 RED, 4 survived** — CP4 FAIL |
| prune-rest (#2903) | — | **merged** | `4afa2e44`, Step 6.5 auto-merge after `update-branch` → `26988fc0` |
| health-deep (#2898) | — | **merged** | `c2ff9923`, docs-only lane — **no QC agents spent** |
| gate-reader (#2908) | harness-maintainer | **completed** | rework iter 1 → `b53fa651`; +22 lines, test-only; CI green 3/3 |
| gate-reader (#2908) | qc-structural | re-review @ `b53fa651` | dispatched after rework |
| cleanup | — | skipped | new backlog entry appended (below); no slot free to dispatch it |
| ops-data | — | skipped | 7th consecutive run. `EODHD_API_KEY` unset; `data-gaps.md` untouched since 2026-05-03 |
| feature tracks | — | none eligible | every IN_PROGRESS row is LOCAL-fenced, data-gated, or human-gated on an R3 flip |

**Worktree reuse was the lever that made two waves fit.** The behavioral pass
re-used the structural pass's worktree at the same SHA rather than getting a
fresh one — saving ~32 GB and two cold OCaml builds on a runner where the disk
guard refuses a third agent at 61 GB free. Each worktree was reclaimed the moment
its agent finished, never batched (batching took run `33962894987` from 69 % to
ENOSPC).

## QC Status

| Track | PR | tip | CI | structural | behavioral | outcome |
|---|---|---|---|---|---|---|
| prune-rest | #2903 | `90aa26b0` | pass (3/3) | **APPROVED** (5) | **APPROVED** (5) | **auto-merged** `4afa2e44` |
| gate-reader | #2908 | `5a504652` | pass (3/3) | **APPROVED** (5) | **NEEDS_REWORK** (2) | rework iter 1 |
| health-deep | #2898 | `981b1aff` | pass (3/3) | skip (docs-only) | skip (docs-only) | **merged** `c2ff9923` |

Both reviews posted as `COMMENTED` (self-approval is blocked when reviewer and
author share an account — the verdict lives in the body), pinned to the full
40-char tip SHA, under the correct first headings (`## Structural QC` /
`## Behavioral QC`) so the gate reader attributes them correctly (#2837).

## The #2908 rework, and a dispatcher error worth recording

The behavioral finding was genuinely good, and worth restating because it is the
kind a reading-based review does not produce. The PR ships **two independent**
metadata-failure detectors, and its nine new checks exercise only the first: all
three failure fixtures (`http`, `invalid`, `empty`) make `_pr_meta` exit nonzero,
so the four-conjunct `jq -e` **schema** validator never fires in any test.
Deleting that predicate outright left all 127 checks green.

The reviewer then showed why that matters rather than asserting it: a stub
returning **HTTP 200 / valid JSON / `head.sha: null`** renders, with the guard
removed, as

```
501 pass none none none dispatch qc-structural     (exit 0)
```

— a broken reader presenting as *"CI green, no reviews yet"*. That is #2886's own
failure mode, reproduced inside the PR that fixes #2886. It also noted that this
script already has a wired, gating mutation harness
(`pr_gate_status_mutation_test.sh`) whose header records that hand-verification
of this suite **"has been WRONG TWICE IN THE SAME DIRECTION"** — and that the PR
added two guards and zero rows to it, so the harness was green only because it
did not know they existed.

### Where I got it wrong

Mid-rework I inspected the pushed commit by grepping for `run_mutation q` — the
row naming the agent's *working draft* used — got `0`, and told the agent the
durable half of the fix was missing. It was not. `b53fa651` already adds three
rows doing exactly that job under different names: **`meta_sha`** (drops the
`headRefOid` `length > 0` conjunct), **`meta_row`** (removes the ERROR
diagnostic), **`meta_exit`** (forces `exit 0`).

I keyed on *my agent's naming* instead of on *what the rows do*, and sent it
chasing a phantom gap. The agent had already reconciled correctly — discarding
its duplicate rather than landing two copies of the same three mutations under
two naming schemes — so no harm reached the branch, but the instruction was
wrong and I corrected it in-flight.

This is the **fourth consecutive run** in which the record shows a dispatcher
instruction being corrected rather than an agent's work being corrected. The
09-21 summary noted the third and called it out as a pattern; it is still the
pattern, and the common shape is **me asserting the state of a file from a narrow
grep instead of reading it**. Same family as the `| tail` exit-status bug this
repo keeps re-finding: a cheap proxy substituted for the actual measurement.

### The rework, verified dispatcher-side

The rework agent then stalled **twice** on a background wait — the
`feedback_agents_background_wait_stall` trap, despite the brief warning against
it explicitly both times. Rather than re-dispatch, I finished the verification
myself, which `.claude/rules/pr-gate-loop.md` §"Dispatcher-side recovery"
prescribes precisely for this case ("finishing it from the dispatcher side is
faster than a re-dispatch and cannot lose the work"). The work was already
complete and pushed; only the measurement was outstanding.

```
$ sh dev/scripts/pr_gate_status_test.sh
OK: pr_gate_status -- 129 tests clean.                                    (exit 0, was 127)

$ sh trading/devtools/checks/pr_gate_status_mutation_test.sh
ok   meta_sha  (killed, as pinned): drop the metadata headRefOid validation
ok   meta_row  (killed, as pinned): remove the explicit metadata ERROR-row diagnostic
ok   meta_exit (killed, as pinned): discard the nonzero exit status after metadata failures
OK: 22 mutation(s) match pin (17 killed / 4 live survivor / 1 equivalent).  (exit 0, was 19)
```

Note what `meta_sha` being **killed** actually establishes: it *is* the
reviewer's non-vacuity mutation — dropping the `headRefOid` conjunct — now
**wired into a gating harness** rather than demonstrated by hand once. The
finding is not merely fixed; the fix is self-verifying against future edits,
which is the durable half and the thing the harness's own header ("hand
verification of this suite has been WRONG TWICE IN THE SAME DIRECTION") exists
to insist on.

CI at `b53fa651`: `build-and-test`, `perf-tier1-smoke`, `goldens-affected` — all
COMPLETED SUCCESS.

**#2908 still needs both QC verdicts re-posted at `b53fa651` before it can
merge**, because `pr-merge-gates.md` requires both at the *current* tip and the
rework invalidated verdicts pinned to `5a504652`. That re-review is in flight;
whatever its outcome, I did **not** merge on the strength of my own
dispatcher-side measurements — those are evidence for the reviewer, not a
substitute for the gate.

## Health Scan

Full report: `dev/health/2026-09-22-fast.md`. **Result: CLEAN. Critical items:
none.**

```
BUILD_EXIT=0
RUNTEST_EXIT=0
```

`^FAIL:` lines: **zero**. The gate is the exit code, but both agree here, so
there is nothing to interpret. Both gates ran behind `flock /tmp/dune.lock` so
they could not race the dispatched agents — on a 15 GB memory-bound runner a
silent OOM is indistinguishable from a real failure.

Standalone checks, run without dune, exit codes read **unpiped**:

```
OK: all dev/status/*.md files have required fields.                                   exit 0
OK: index_size_linter -- dev/status/_index.md within limits (16468/20480, rows <=250) exit 0
OK: no-python check -- no *.py files found.                                            exit 0
```

`index_size_linter` was re-run **after** the Step 5.5 index edit. The 09-21 run
read this exact check as green through a `| tail` pipeline when it was actually
exit 1 (a 276-char row against a 250 cap), and the 09-20 run hit the same linter
on the same row. Reading it unpiped, after the edit rather than before, is the
cheap fix for a mistake this file has now recorded twice.

**Deep scan** (`dev/health/2026-09-21-deep.md`, merged to `main` this run via
#2898): **0 critical, 10 warnings, 170 info**. Nine of the ten warnings are
expired linter exceptions — see §Follow-up Queue.

## Scheduled workflows

FINDINGS (exit 1): RED/STALE workflows and newest_completed_run_id below.

    scheduled_workflow_health: repo=dayfine/trading stale_hours=216 runs_per_workflow=10
    NO-SCHEDULE	CI Main Watchdog	(no scheduled runs observed)
    NO-SCHEDULE	CI	(no scheduled runs observed)
    OK	Dependency freshness check	run_id=35615199970 status=completed conclusion=success created_at=2026-09-21T14:53:29Z age_hours=21 streak=0 in_progress=0 newest_completed_run_id=35615199970
    NO-SCHEDULE	golden-runs-custom-universe	(no scheduled runs observed)
    OK	golden-runs-sp500-15y	run_id=35620280513 status=completed conclusion=success created_at=2026-09-21T15:38:11Z age_hours=21 streak=0 in_progress=0 newest_completed_run_id=35620280513
    NO-SCHEDULE	golden-runs-sp500-5y	(no scheduled runs observed)
    NO-SCHEDULE	goldens-affected	(no scheduled runs observed)
    OK	Weekly deep health scan	run_id=35648266280 status=completed conclusion=success created_at=2026-09-21T19:59:38Z age_hours=16 streak=0 in_progress=0 newest_completed_run_id=35648266280
    OK	Build CI image	run_id=35600171824 status=completed conclusion=success created_at=2026-09-21T12:32:56Z age_hours=24 streak=0 in_progress=0 newest_completed_run_id=35600171824
    NO-SCHEDULE	IWV Scrape (one-shot)	(no scheduled runs observed)
    RED	Daily orchestrator	run_id=35728381545 status=in_progress conclusion=null created_at=2026-09-22T12:38:14Z age_hours=n/a streak=1 in_progress=1 newest_completed_run_id=35636902726
    OK	perf-nightly	run_id=35711088426 status=completed conclusion=success created_at=2026-09-22T09:33:45Z age_hours=3 streak=0 in_progress=0 newest_completed_run_id=35711088426
    NO-SCHEDULE	perf-tier1	(no scheduled runs observed)
    OK	perf-weekly	run_id=35605439371 status=completed conclusion=success created_at=2026-09-21T13:24:45Z age_hours=23 streak=0 in_progress=0 newest_completed_run_id=35605439371
    RED	Prune candidates weekly	run_id=35650912772 status=completed conclusion=failure created_at=2026-09-21T20:24:56Z age_hours=n/a streak=5 in_progress=0 newest_completed_run_id=35650912772
    OK	Weekly track pacer	run_id=35506154608 status=completed conclusion=success created_at=2026-09-20T10:49:59Z age_hours=49 streak=0 in_progress=0 newest_completed_run_id=35506154608
    OK	Weekly start sweep (BAH SPY)	run_id=35641922377 status=completed conclusion=success created_at=2026-09-21T18:59:10Z age_hours=17 streak=0 in_progress=0 newest_completed_run_id=35641922377
    SUMMARY: active=17 (1 page(s) fetched, full pagination -- a real total, not a floor) ok=8 red=2 stale=0 no-schedule=7 unobservable=0 -- red/stale/ok/unobservable are each computed from up to 10 most-recent scheduled runs per workflow (not just the newest); red includes a per-workflow failure-streak count (see each RED line's streak=N, or streak=>=N when the fetched page was exhausted before the streak closed)
    SUMMARY: RED workflows: Daily orchestrator(streak=1), Prune candidates weekly(streak=5)

### Both of the 09-21 summary's open workflow questions are now answered

That summary's Question 5 asked whether `weekly-start-sweep`'s post-#2842 cron
would prove the REST fix. **It did.** Run `35641922377` at 2026-09-21T18:59Z:

| workflow | 09-21 reading | today |
|---|---|---|
| `Weekly start sweep (BAH SPY)` | **RED**, `streak=>=10` | **OK**, `streak=0` |
| `Prune candidates weekly` | RED, `streak=4` | RED, **`streak=5`** |

The sweep's streak of ≥10 was correctly diagnosed last run as **stale, not live**
— every datapoint predated the fix — and the first post-fix datapoint confirms it.
It also produced a real PR (**#2893**), which is in today's queue.

The prune workflow went the other way and **failed again** (`35650912772`,
2026-09-21T20:24Z), taking the streak 4 → 5, exactly as predicted at source. That
is the workflow **#2903 fixes, and #2903 merged this run.** Verified on `main`
after the merge rather than assumed:

```
$ git show origin/main:.github/workflows/prune-candidates-weekly.yml | grep -n 'gh \|api.github.com'
131:          # REST publication: gh is absent from the CI image (#2847).
141:            "https://api.github.com/repos/${REPO}/pulls")
```

The only surviving `gh` token is inside a **comment**. The exit-127 root cause is
gone from `main`. Its next Sunday cron is the datapoint that closes #2847 — the
same shape of confirmation the sweep just supplied, so the expectation is
well-grounded rather than hopeful.

**`Daily orchestrator` RED streak=1** is the 09-21 run-2 failure dissected in the
headline. It is a *correctly* failed job, not a broken workflow.

## Budget

- Cap: **$200** (`max_daily_cost_usd`). Target band **60–95 %**.
- Standing caveat in `merge-policy.json`: auth is a flat-rate Max-20x
  subscription, so this figure is a **runaway-dispatch backstop, not a bill**.

**Cost: deliberately not stated as a measured figure.** The authoritative number
is written *after* this session ends, by the workflow's cost-capture step, into
`dev/budget/2026-09-22-35728381545.json`. I cannot read it from inside the run,
and converting token counts into dollars would be exactly the unlabelled estimate
this repo keeps objecting to. For calibration, yesterday's two runs measured
**$58.17** (run 1, 7 agent sessions) and **$10.89** (run 2, which produced
nothing durable).

**The binding constraint was disk again, and it shaped the whole run.**

| moment | free | guard verdict |
|---|---:|---|
| run start | 61 GB | 2 OK, **3 REFUSED** (68 GB floor) |
| both QC agents live | 29 GB | **1 REFUSED** (36 GB floor) |
| after reclaiming `wt-2903` | 45 GB | 1 OK, **2 REFUSED** (52 GB floor) |

Two things followed from this, both mechanical rather than judgement:

1. **Worktree reuse.** Each behavioral pass re-used its structural pass's
   worktree at the same SHA. That saved ~32 GB and two cold OCaml builds, and is
   what let a four-agent QC programme run on a machine the guard caps at two.
2. **Strict serialisation after the rework.** At 45 GB the guard refuses a second
   concurrent agent, so re-QC ran sequentially rather than as a pair. That is the
   difference between clearing three PRs and clearing five.

Utilization assessment: **resource-bound, not budget-bound.** The fill-loop had
nothing to add — at no point was there a spare eligible dispatch the disk guard
would have permitted. Five PRs were deferred, and **capacity is the honest reason
for three of them** (#2909, #2906, #2907); the other two (#2900, #2893) have
specific non-capacity reasons stated above.

## Escalations

**`[critical]` The 09-21 second cron slot lost its entire summary and $10.89 —
and the mechanical backstop is the only reason we know.** Run `35636902726`
wrote `dev/daily/2026-09-21-run2.md`, never published it (no
`ops/daily-2026-09-21-run2` PR, absent from `origin/main`), and claimed two
dispatches (#2886, #2887) that produced no branch and no PR. The
`A-FASTEXIT-VACUOUS` verify step failed the job on both grounds (#2803 and
#2810). This is tagged `[critical]` because it is a **measured, this-cycle loss
of a dispatch slot**, verified two independent ways (`git log origin/main` empty
for the file; `dev/budget/2026-09-21-35636902726.json` records the cost with no
matching summary) — not a carried-forward assertion.

The lesson that is *new*, and that the last three summaries did not have:
**write-early is not enough.** Run 2 authored a summary and still lost it,
because it died at **publication** (Step 8), not authorship (Step 7). The
standing ask below needs a third component.

**`[high]` `.claude/agents/**` writes remain refused — 21st consecutive run**,
and the blocked fix is now a *three*-part edit that must land in order:

1. **#2887 first** — `_prior_summary_path()` (a) returns the caller's own summary
   when the exclude-arg is omitted and (b) still uses `ls -t | head -1`. Measured
   again today, one call: `prior_summary_iso` returned **`2026-09-14`** when the
   true prior summary is **`2026-09-21`** — a seven-day error. Today it errs in
   the *widening*, safe direction, which is precisely why nobody noticed; after
   write-early lands, defect (a) flips it to the narrowing, unsafe direction.
2. **Then write-the-skeleton-at-Step-1** — applied by hand three runs running now,
   and the summary survived each time.
3. **Then a publication-failure guard**, which is the new part. Run 2 shows Step 8
   can drop the artifact after a perfectly good summary exists. `publish_daily_summary.sh`
   already fails loudly per step; what is missing is the orchestrator treating a
   failed publish as a run-ending escalation rather than a logged warning.

**`[high]` #2887 re-measured and still live.** See above — carried forward from
09-21 with a fresh measurement rather than a citation, per Step 1c.

**`[info]` Capability measurement: the orchestrator token CAN merge a PR that
modifies `.github/workflows/**`.** #2903 changes
`.github/workflows/prune-candidates-weekly.yml` and the REST `PUT /merge`
returned `merged=true` (`4afa2e44`). The standing `_index.md` note records the
`workflow` scope as unavailable **by push and by contents API** — both still
true, and both re-measured as recently as 09-04. This refines rather than
contradicts it: **merging is a third route, and it works.** Worth knowing,
because it means workflow fixes authored elsewhere (Codex, local session) can be
landed by the orchestrator even though it cannot write them itself. The
structural reviewer flagged this as a possible blocker; it was not.

**`[info]` Two weekly workflows: one confirmed fixed, one fixed-and-awaiting-cron.**
`weekly-start-sweep` went RED(≥10) → **OK** on its first post-#2842 run,
retroactively confirming last run's "stale streak, not live regression" call.
`prune-candidates-weekly` failed again (streak 4 → 5) and its fix merged today;
its next Sunday cron is the closing datapoint for #2847.

**`[info]` #2906 and #2908 both edit `dev/scripts/pr_gate_status.sh`.** Whichever
lands first forces the other to rebase. #2908 should go first — smaller, and it
fixes a defect live on `main`.

**`[info]` The gate reader was still broken for me this whole run.** `sh dev/scripts/pr_gate_status.sh`
rendered 5 of 8 PRs and then died — `jq: Argument list too long`, exit 126 —
omitting #2900, #2898 and #2893 entirely. I read those three from REST directly.
This is #2886, whose fix is #2908; it is not yet on `main`.

## Integration Queue

- **#2898 (weekly deep health scan)** — **MERGED** `c2ff9923`. Docs-only lane per
  `.claude/rules/pr-merge-gates.md`: the diff is a single `.md` file, so both QC
  gates are skipped and CI is the only gate. Rule 0 hold check clear, re-checked
  immediately before the merge PUT. It came back `behind`, so `update-branch` ran
  and CI re-ran green on the new tip before merging. **Zero agent sessions spent**
  — worth stating, because the docs-only carve-out is exactly what made room for
  four QC agents on a runner capped at two concurrent.

- **#2903 (prune-candidates REST publication)** — **MERGED** `4afa2e44`, via Step
  6.5 auto-merge. Rule 0 hold check CLEAR and **re-checked a second time
  immediately before the merge PUT**.

  The pre-merge read came back **`behind`**, so `update-branch` ran and moved the
  tip `90aa26b0` → `26988fc0` — **out from under QC verdicts pinned to
  `90aa26b0`**. I measured the delta rather than assuming it was harmless:

  ```
  $ git diff --name-only 90aa26b0 26988fc0 -- <the 4 reviewed files>
  (empty)
  $ git diff --name-only 90aa26b0 26988fc0        # what the update pulled in
  .claude/agents/qc-behavioral.md
  .claude/rules/session-rampup.md
  dev/health/2026-09-21-deep.md
  dev/notes/next-session-priorities-2026-09-22.md
  dev/status/backtest-perf.md
  ```

  Five main-side files, **intersection with the four reviewed files: 0**, and
  every reviewed file byte-identical across the move. CI then re-ran green
  (`build-and-test`, `perf-tier1-smoke`, `goldens-affected`) before the merge.
  Stating the general rule again because it keeps mattering: **`update-branch`
  silently invalidates SHA-pinned QC verdicts, and the only honest way through is
  to show the delta is disjoint, not to assume it.**

  Note one of those five files is `dev/health/2026-09-21-deep.md` — i.e. the
  update pulled in **my own #2898 merge from twenty minutes earlier**. Merges
  inside one run perturb each other's branches; the disjointness proof is what
  keeps that from being a problem.

## Follow-up Queue

Repo-wide open `- [ ]` items in `dev/status/*.md`: **62** (unchanged from 09-21).
Per-file maximum: **26** (`harness.md`). Files over the threshold of 4: **3** —
`harness.md` (26), `trade-audit.md` (9), `orchestrator-automation.md` (7).

`FOLLOWUP_OVER_THRESHOLD = 3 > 0`. The 09-21 run recorded itself as the 3rd run
in the maintenance cycle and fired the gate, so this run is cycle position 1 and
**Step 2b does not fire**. It is satisfied incidentally regardless: every PR this
run touched (#2903, #2908, and the deferred #2906/#2907/#2909) is harness-backlog
work against the largest file.

### Step 2e — one new cleanup entry, appended and deliberately not dispatched

`dev/status/cleanup.md` §Backlog has carried a standing banner since 2026-09-09
saying it contains **no dispatchable work** — all remaining `[ ]` entries are
self-described archive/policy records, and that banner explicitly says *"do not
re-derive this every run; re-audit only when a new entry is appended."*

Today the freshly-merged deep scan (#2898) appends one. Its §Warnings carry
**9 expired linter exceptions** past their `review_at` date:

- **7 × `tracked_artifact`**, `review_at: 2026-09-09` — three `dev/backtest/scenarios-2026-04-14-*/` and four `dev/experiments/*/` directories.
- **2 × `sexp_default_drift`**, `review_at: 2026-09-20` — `t.short_borrow_rate_tiers` and `t.short_maintenance_tiers` in `trading/trading/portfolio/lib/margin_config.mli`.

This is genuinely new and genuinely actionable ("retire or re-annotate" is
squarely in `code-health`'s scope), so I appended it — which re-arms Step 2e for
the next run. **It is distinct from the debt the exceptions describe**:
`H-TRACKED-SCENARIO-DEBT` owns deleting the 49 tracked-but-gitignored files; this
item owns only the stale annotations. The `sexp_default_drift` pair looks like a
real fix rather than a re-annotation — `margin_config.ml` already carries
`[@sexp.default []]` on both fields and `default_config` agrees, so the gap is
purely that the `.mli` documents the attribute on 2 of ~8 fields and not these.

**Not dispatched today for a mechanical reason**: every slot went to QC on an
eight-PR queue with zero reviews, and the disk guard caps this runner at 2
concurrent agents. Recorded in the backlog entry itself so the next run does not
have to re-derive it.

## Questions for You

1. **The `.claude/agents/lead-orchestrator.md` fix is now a three-part edit, and
   yesterday's version of this ask was incomplete.** 09-21 asked for #2887 +
   write-early, in that order. Run 2 that same evening shows why that is not
   enough: it **did** write a summary and still lost it, because it died at
   **publication**, not authorship — and it burned **$10.89** doing so. So the
   ask is now (a) **#2887 first**, (b) write-the-skeleton-at-Step-1, (c) **treat a
   failed Step 8 publish as a run-ending escalation rather than a logged
   warning**. `.claude/agents/**` writes have been refused for **21 consecutive
   runs**, so no agent can make any of the three.
2. **#2906 vs #2908 will conflict — confirm the order.** Both edit
   `dev/scripts/pr_gate_status.sh`. I would land **#2908 first** (smaller, and it
   fixes a defect live on `main` that broke the gate reader for me this entire
   run), then rebase #2906's new lane on top. Say if you want it the other way.
3. **#2900 — do you want the orchestrator to QC it, or is it yours?** I deferred
   it because it is visibly mid-flight (three commits, newest 08:42Z today,
   carrying **salt 0 only**) and your own P0 says salts 1–2, the ledger entry and
   the README §Verdict are still to come. Reviewing now would pin verdicts to a
   SHA guaranteed to move. Happy to take it the moment the results commit is
   complete — just say so.
4. **Disk is still the binding constraint, and it decided the shape of this run.**
   The guard refused 3 agents at 61 GB and refused a *second* at 45 GB. I got
   four QC passes out of a two-agent budget only by **reusing each structural
   pass's worktree for its behavioral pass** — worth ~32 GB and two cold builds.
   That trick is now load-bearing rather than an optimisation. A larger runner, a
   shallower checkout, or a shared `_build` would convert directly into PRs
   cleared per run: today it cost three deferred PRs (#2909, #2906, #2907).
5. **#2893 (weekly-start sweep snapshot) is waiting on you by its own request** —
   its body says *"Advisory: review and merge (or close if uninteresting)."* It is
   mixed (a `.md` plus a `.sexp`), so it is not eligible for the docs-only lane
   and would cost a full QC pair. Tell me if these should instead get a standing
   rule — they are workflow-generated weekly artifacts, and the same question will
   arrive every Monday.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
