# Status — 2026-03-24

## Feature Progress

### data-layer  [NEEDS_REWORK]
- Done today: EODHD client extended (period, fundamentals, get_index_symbols); DATA_SOURCE module type finalized; Live_source, Historical_source, Synthetic_source all implemented; 23 total tests passing
- In progress: Nothing — agent considered it complete
- Interface stable: **Yes** ← screener was unblocked mid-session
- Blocked: No
- Recent commits:
  - `ffb9eab` Extend EODHD client with period, fundamentals, and index symbols
  - `3212342` Add DataSource module type for Weinstein data abstraction layer
  - `1f19ea1` Implement DataSource implementations: Live, Historical, Synthetic

### portfolio-stops  [IN_PROGRESS]
- Done today: `weinstein.types` (9 tests), `weinstein.stops` stop state machine (23 tests), `weinstein.portfolio_risk` position sizing + limit checking (16 tests) — 48 total tests, all passing
- In progress: Nothing committed yet for next modules
- Interface stable: **Yes** (Portfolio_manager.mli finalized)
- Blocked: No
- Next steps: `trading_state` (JSON persistence), `weinstein_order_gen` (order generation from candidates + stop events)
- Recent commits:
  - `79b3e43` Add Weinstein domain types module
  - `dba00d1` Add Weinstein stops skeleton
  - `bc0eb3b` Add Weinstein stops tests (23 pass) + format
  - `e51d3e3` Add Portfolio_risk module skeleton
  - `dacec51` Add Portfolio_risk tests (16 pass)

### screener  [NEEDS_REWORK]
- Done today: All 9 modules implemented from scratch (SMA/WMA, stage classifier, RS analyzer, volume analyzer, resistance mapper, stock_analysis, cascade screener, macro analyzer, sector analyzer) — 87 tests, all passing
- Note: **Built on top of feat/data-layer** — must merge data-layer first
- Interface stable: **Yes**
- Blocked: Waiting on data-layer merge before screener can merge
- Recent commits:
  - `72bfd70` Add SMA and weighted MA indicators
  - `5ef73fe` Add Weinstein shared types and Stage Classifier
  - `c9486c7` Add Relative Strength (RS) analyzer
  - `ff04167` Add Volume analyzer and Resistance mapper
  - `8030a13` Add Stock Analyzer and Cascade Screener
  - `62e9ba6` Add Macro Analyzer and Sector Analyzer

### simulation  [WAITING]
- Done today: Nothing — all dependencies not yet APPROVED
- Blocked on: data-layer (NEEDS_REWORK), screener (NEEDS_REWORK), portfolio-stops (IN_PROGRESS)

---

## QC Status

| Feature         | Status                                                  |
|-----------------|----------------------------------------------------------|
| data-layer      | ⚠️ NEEDS_REWORK — see `dev/reviews/data-layer.md`       |
| portfolio-stops | ⏳ PENDING (still IN_PROGRESS, not yet submitted)        |
| screener        | ⚠️ NEEDS_REWORK — see `dev/reviews/screener.md`         |
| simulation      | — (not started)                                          |

---

## QC Findings Summary

### data-layer — 2 blockers

1. **Misplaced doc comment** in `http_client.mli` — the cadence comment is attached to `end_date` instead of `period`. Correctness bug in public interface documentation.
2. **Cache-write error silently discarded** in `live_source.ml` line 44 — `ignore (_save_bars_to_cache ...)` drops the `Result.t`. A disk-full or permissions error causes a silent cache miss with no diagnostic.

**Should fix:** Duplicated `_load_universe` across live/historical source files; no Breakout pattern test; no Live_source unit tests; tests use raw OUnit2 instead of Matchers library.

### screener — 4 blockers

1. **Magic numbers in `screener.ml` not in config** — `1.005` (entry buffer), `0.92`/`1.08` (stop factors), grade cutoffs `85/70/55/40/25` — all tunable parameters that need to be in config for backtesting to work.
2. **Magic numbers in `macro.ml` not in config** — A-D lookback (26), momentum window (200), NH-NL lookback (13), threshold values — all inline.
3. **Magic numbers in `stock_analysis.ml` not in config** — breakout scan window (8), base detection offsets.
4. **Tests use raw OUnit2** instead of the project Matchers library (`assert_that`, `is_ok_and_holds`, etc.) — required by CLAUDE.md.

**Should fix:** Dead branch in `rs.ml` `_classify_rs_trend`; score weights in `sector.ml` not in config; missing `breakout/` module explanation from design doc.

---

## Integration Queue
_(None — no features APPROVED yet)_

---

## Current Milestone Target
**M1 — Data + Screener Foundation** — requires: data-layer APPROVED + screener APPROVED

Progress: Both features are implemented and tests pass, but QC has found rework items. Once both complete rework, they can be approved and merged.

---

## Dependency Unlocks

- **data-layer Interface stable: YES** → screener agent was unblocked and ran successfully today (full pipeline implemented in one session!)
- **portfolio-stops Interface stable: YES** → one of three simulation dependencies is now met
- **screener Interface stable: YES** → simulation now has 2 of 3 dependencies at interface-stable
- **Simulation** remains blocked until data-layer + portfolio-stops + screener all reach APPROVED

---

## Session Accomplishments

Today was a strong first session. Starting from zero, the team:
- Delivered a complete data abstraction layer (DATA_SOURCE + 3 implementations)
- Delivered 3 portfolio/stops modules (domain types, stop state machine, position sizing)
- Delivered a complete 9-module Weinstein analysis pipeline (screener, macro, sector, RS, volume, resistance, stage classifier)
- Ran QC on both READY_FOR_REVIEW features and produced detailed review reports
- Total: ~183 tests written, all passing

The blockers are all fixable in one more session per feature.

---

## Questions for You

1. **data-layer rework priority**: The two blockers are a doc comment misplacement and a silently-ignored cache error. Should the data-layer agent fix these tomorrow, or should we accept the "should fix" items too (duplicate `_load_universe`, add Breakout test, migrate tests to Matchers)? Recommend: fix all blockers + should-fix items in one pass to avoid another rework cycle.

2. **screener rework scope**: The 4 blockers all involve moving magic numbers into config records. This is mechanical but potentially touches many files. Should the agent fix all blockers + should-fix (dead branch, missing sector config, OUnit2→Matchers migration) in a single rework session, or prioritize only the blockers?

3. **screener depends on data-layer branch**: The screener was built on top of `feat/data-layer` (not `main`), which means `feat/screener` may have `feat/data-layer` commits in its history. When you merge, merge data-layer to main first, then rebase screener on main before merging. Please confirm this is acceptable — or should the screener agent rebase onto main now?

4. **portfolio-stops next session**: Should the agent continue to `trading_state` + `weinstein_order_gen` tomorrow, or wait for data-layer to merge first so portfolio-stops can depend on the stable data types?

---

## Your Response
_(Edit this section. Run `dev/run.sh` after editing to start the next session.)_

<!--
Suggested response (delete/modify as needed):

1. data-layer rework: Fix all blockers + all should-fix items in one pass.
2. screener rework: Fix all 4 blockers + should-fix items (dead branch, OUnit2→Matchers) in one pass.
3. Screener rebase: Acceptable — merge data-layer first, then screener agent should rebase feat/screener onto main before next PR.
4. portfolio-stops: Continue to trading_state + weinstein_order_gen tomorrow (no dependency on data-layer types needed yet).
-->
