# Status — 2026-03-24

## Feature Progress

### data-layer  [IN_PROGRESS]
- **Done today:**
  - Extended EODHD HTTP client with `period` param (Daily/Weekly/Monthly), `fundamentals` type,
    `get_fundamentals`, and `get_index_symbols` API calls
  - Updated all existing call sites to pass explicit `period` field
  - Created `analysis/weinstein/data_source/` package with three implementations:
    - `DATA_SOURCE` module type — the seam for live/historical/synthetic modes
    - `Live_source` — EODHD API + local CSV cache
    - `Historical_source` — cache replay with strict no-lookahead enforcement (backtest integrity)
    - `Synthetic_source` — deterministic programmatic generation (for tests)
  - Tests for all new EODHD client functions and all three data source implementations
  - Added `ppx_deriving.show/eq` to eodhd dune config for new derived types
  - Test fixture JSON files: `get_fundamentals.json`, `get_index_symbols.json`
- **In progress:** Build verification in Docker (agent could not run Docker in worktree env)
- **Interface stable: YES** ✅ — `DATA_SOURCE` module type is finalized
- **Blocked:** Docker build unverified — needs `dune build && dune runtest` before READY_FOR_REVIEW
- **Recent commits:**
  - `062e3bb` Add period param to EODHD client and Weinstein data_source module
  - `2d47675` Add test fixture data for fundamentals and index symbols
  - `70f9839` Fix eodhd dune for ppx_deriving and add status file
  - `9f91693` Add ppx_deriving dependency to eodhd dune-project
  - `f3b3563` Update data-layer status with all recent commits

### portfolio-stops  [IN_PROGRESS]
- **Done today:**
  - Created `trading/analysis/weinstein/` package root with `dune-project`
  - `weinstein.types` — `stage` (Stage1–4), `ma_slope` (Rising/Flat/Falling), `grade` (A/B/C)
    with `[@@deriving show, eq]`
  - `weinstein.stops` — Trailing stop state machine (Initial → Trailing → Tightened):
    round-number nudge, correction-cycle detection (8%+ pullback), Stage-3 tightening,
    all thresholds in config — 17 test cases
  - `weinstein.portfolio_risk` — Fixed-risk position sizing, portfolio snapshot,
    exposure/cash/sector/risk limit checks — 14 test cases
  - `weinstein.portfolio_manager` — Stateful integration: wraps `Portfolio.t` + `Position.t`
    (not modified) + stop states; `update`, `apply_transition`, `log_trade`; `.mli` finalized
- **In progress:** Build verification in Docker; `trading_state` persistence (JSON) not yet started
- **Interface stable: YES** ✅ — `portfolio_manager.mli` is the contract for Simulation
- **Blocked:** Docker build unverified; known potential issues in status file
  (Position.id field name, StopLoss constructor fields, `[@@deriving show]` on Date.t fields)
- **Recent commits:**
  - `f393900` Add Weinstein analysis modules: types, stops, portfolio_risk
  - `3542e7f` Add Portfolio Manager module integrating stops and risk management
  - `3ee0db5` Fix portfolio_manager: add time_ns_unix dep, remove show from portfolio_update

### screener  [READY_FOR_REVIEW]
- **Done today (unblocked mid-session):**
  - 9 modules built under `analysis/weinstein/`: types, sma, stage, rs, volume,
    resistance, stock_analysis, sector, screener
  - Full Weinstein cascade filter: macro gate → sector filter → additive scoring → grade
  - Stage classifier with flat-MA disambiguation, late Stage2 detection, transition events
  - Mansfield RS normalization vs benchmark
  - Volume confirmation: breakout strength (2x threshold) and pullback contraction (75%)
  - Overhead resistance grading: Virgin/Clean/Moderate/Heavy
  - All functions pure; all thresholds configurable; all modules have .mli + tests
- **Interface stable: YES** ✅ — unlocks simulation
- **Blocked:** Docker build unverified (no Docker in worktree env) — QC agent verifying now
- **Recent commits:**
  - `7b634c1` Add Weinstein analysis pipeline: types, SMA, stage classifier, RS, volume, resistance, screener

### simulation  [RUNNING]
- All three dependencies now Interface stable: YES → agent launched end of session
- Agent still running at time of summary
- Status will be updated when agent completes

---

## QC Status
- data-layer: ⏳ PENDING — not yet READY_FOR_REVIEW (Docker build unverified)
- portfolio-stops: ⏳ PENDING — not yet READY_FOR_REVIEW (Docker build unverified)
- screener: 🔄 IN REVIEW — QC agent running now
- simulation: — (just started)

---

## Integration Queue
_(No features approved for merge yet — QC reviews in progress.)_

---

## Current Milestone Target
**M1 — Data + Screening pipeline** — requires: data-layer ✅ + screener ✅ + QC APPROVED

Progress: Both interfaces finalized, screener logic complete. QC review underway.
Pending: Docker build verification across all three feature branches.

---

## Dependency Unlocks (today)
- **data-layer → Interface stable: YES** → unblocked screener (launched + completed today)
- **portfolio-stops → Interface stable: YES** → noted for simulation
- **screener → Interface stable: YES** → unblocked simulation (launched end of session)

All four tracks were active in today's session.

---

## Build Verification Gap (important)
All three feature agents (data-layer, portfolio-stops, screener) completed substantive work but
**could not run `dune build && dune runtest`** because Docker is not accessible in worktree
agent environments (worktrees live at `/home/user/trading/.claude/worktrees/agent-X/` but the
container mounts `/workspaces/trading-1/`).

This is the #1 workflow risk: code is written but not compiled. The QC agent for screener
will attempt Docker build. Results will determine if this is fixable in-session or needs
a workflow change.

---

## Questions for You
1. **Docker in worktrees (critical):** Agents in isolated worktrees cannot reach the
   `trading-1-dev` container because the container mount path is `/workspaces/trading-1/`
   but worktrees are at `/home/user/trading/.claude/worktrees/agent-X/`. Options:
   - (a) Run feature agents **without** worktree isolation (on `feat/*` branches directly)
     — Docker works but agents share the same filesystem
   - (b) Mount the worktree path into the container before spawning the agent
   - (c) Have agents commit their code and then run builds via a separate Docker-enabled step
   **Recommendation: Option (a) — remove `isolation: worktree` from feature agents.**

2. **Build verification priority:** Tomorrow's data-layer and portfolio-stops agents should
   start by running `dune build && dune runtest` and fixing any errors before new features.
   Confirm this priority.

3. **trading_state persistence:** The portfolio-stops design includes JSON state persistence
   (not yet built). Is this required before READY_FOR_REVIEW, or can it be a follow-up task?

4. **Types module collision:** Both screener and portfolio-stops created modules under
   `trading/analysis/weinstein/`. They may have overlapping type definitions (e.g., `stage`
   defined in both `weinstein.types` and `screener.types`). The next session should reconcile
   these into a single shared types module.

---

## Your Response
_(Edit this section. Run `dev/run.sh` after editing to start the next session.)_

```
[Write your answers/decisions here]
```
