# Status — 2026-06-11

Run timestamp: 2026-06-11T02:23Z (GHA orchestrator run 27318921570)
Run ID: 2026-06-11-run-1

**Mode:** FULL PASS / **0 DISPATCH** — first run of the day (full-pass escape
hatch). The one open feature PR (#1528, harvest-rotate Step 2) is **maintainer-
in-flight**: the maintainer self-ran structural QC minutes ago, verdicted
**NEEDS_REWORK** (CI `build-and-test` red purely on `dune fmt` docstring
rewrap), and named the exact fix — so it is neither auto-mergeable (CI red) nor
an orchestrator dispatch target (re-QC redundant; reworking it collides with the
active maintainer session on the same files). All forward strategy increments
remain broad-PIT **data-gated** in GHA (no `EODHD_API_KEY`, no top-3000 PIT
warehouse). Main **GREEN**. Reconciled one stale `_index.md` row (#1509 merged).
**HEAD at run start (= run end):** `53822998`.

## Step 0 rampup check (deterministic)

Main is **GREEN**. CI on `53822998`: `CI` (build-and-test) SUCCESS,
`perf-tier1` SUCCESS, `golden-runs-sp500-5y` SUCCESS,
`golden-runs-custom-universe` SUCCESS, watchdog skipped. **0 open `ci-red`
issues** (none surfaced). Local build gate: `dune build` exit 0 + full
`dune runtest` exit 0, `0` `^FAIL:` lines (live evidence in §Health Scan).
Proceeded.

## Step 0.5 saturated-queue check

**Evaluated — NOT a no-op (full pass).** First run of 2026-06-11 → the
first-run-of-day escape hatch governs (a full pass keeps the consolidation base
non-empty). Independently, state changed materially since the prior summary:
the maintainer landed #1517–#1527 (harvest-rotate validation corrected to
NO-BUILD, Step 1 partial-exit #1525 merged, rigor rule + screen-rigor skill) and
opened **PR #1528** (harvest-rotate Step 2) — so Condition 1 (open PR with no
prior orchestrator review) would fail regardless.

## Step 1b drift cross-reference

Prior summary `2026-06-10` `## Pending work` rows were all
`blocked (data-gated / maintainer-* / human-fork)` — **no `dispatched` /
`awaiting-merge` row left hanging → no `[drift]` warnings.** The maintainer's
#1517–#1528 are independent forward progress on the (GHA-data-gated) strategy
program, classified per Step 1b as expected, not drift.

- **Resolved between runs:** prior `stage-accuracy` row carried `Open PR #1509`
  (cascade-inversion validation). #1509 **merged** 2026-06-10T02:13Z — index row
  reconciled (Open-PR cleared, next-task refreshed) in Step 5.5.

## Step 1c carried-`[critical]` verification

Prior summary carried **no bare `[critical]`** (only one
`[critical-decision-needed]` M6.6 + several `[info]`). Re-verified main **GREEN**
this run (`dune build` + `dune runtest` exit 0 on `53822998`; full CI green on
the identical SHA; 0 open `ci-red` issues).

**[info] Carried-forward verification: no still-real bare `[critical]` items;
main GREEN on `53822998` (build + runtest exit 0).** The
`[critical-decision-needed]` M6.6 + `[info]` items are re-listed under
§Escalations (none trips the gate-fail).

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harvest-rotate (Step 2) | blocked (maintainer-in-flight; CI red on fmt) | feat/harvest-rotate | #1528 | Maintainer-authored + self-reviewed NEEDS_REWORK (H1: `dune fmt` docstring rewrap → `build-and-test` FAILURE). Fix = run `dune fmt` + commit; then behavioral QC can proceed. Default-off mechanism (experiment-flag R1/R2/R3 all PASS in self-review). Held — active maintainer session on these exact files. |
| stage-accuracy / experiment-platform | blocked (data-gated) | — | — | **P0:** broad-universe WF-CV re-run of the cascade-selection inversion (#1509 docs merged; the actual top-3000 run dirs are maintainer-local `/tmp`). force_exit_off grid REJECTED (#1503). |
| harvest-rotate (Steps 3–5) | blocked (data-gated) | — | — | Step 3 Variant_matrix axis wiring (GHA-eligible, but gated behind Step 2 #1528 landing) → Step 4 decomposed WF-CV on top-3000 (~4h, data-gated) → Step 5 ledger. Expected REJECT per `project_edge_is_the_fat_tail`; deliverable is the decomposed *why*. |
| trade-forensics tooling | blocked (maintainer-in-flight, low urgency) | — | — | **P1:** PR-3 post-exit capture ratio + PR-4 auto-render stage_chart. **P2:** MFE/MAE harness gap (sim step-stream never populates excursions) — collides with #1528 (which explicitly defers partial-exit MFE/MAE capture) + active forensics work; held. |
| strategy direction | blocked (human fork) | — | — | The single-dial surface is mined out (cascade-reweight, laggard, force-exit, ma-hold, hysteresis, continuation, **harvest-rotate now NO-BUILD at screen**). Redirect: tail-preserving levers (breadth, entry quality, holding discipline) + coherent presets as wholes — needs the human fork decision. |
| backtest-perf / simulation | blocked (data-gated / deferred) | — | — | worst-vol-decile prototype regime-tag-gated; stale-exit promotion grid + multi-start E2E broad-PIT data-gated. |
| data-foundations | blocked (maintainer-decision) | — | — | READY_FOR_REVIEW — bars-retention recovery (synthesize vs accept SCTY/MNK/LB zero-bar loss) or close the track. |
| tuning | blocked (maintainer-enablement) | — | — | M2 (qNEHVI) not yet enabled for GHA dispatch (precedent #1327). |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harvest-rotate (Step 2, #1528) | — | skipped | Maintainer-authored, ready-for-review, **CI `build-and-test` FAILURE** (H1 `dune fmt` only), maintainer **self-reviewed NEEDS_REWORK** with the fix named. Cannot auto-merge (CI red, 3-gate hard stop). Re-running structural QC = redundant (maintainer just did it, minutes ago). Reworking it = concurrent-edit collision with the active maintainer session on `harvest_rotate_*` / `weinstein_strategy.ml`. Behavioral QC explicitly gated on the fmt fix (per the self-review) → not run. Left to the maintainer's own flow. |
| stage-accuracy / experiment-platform / harvest-rotate Steps 4–5 / backtest-perf / simulation / tuning | — | skipped | Forward increments broad-PIT data-gated (deep top-3000 PIT OHLCV + `EODHD_API_KEY` absent in GHA; warehouse maintainer-local `/tmp`), deferred-by-design, or human-fork-gated. |
| trade-forensics (P1 PR-3/PR-4, P2 MFE/MAE) | — | skipped | Maintainer-in-flight on the exact subsystem; #1528 explicitly defers partial-exit MFE/MAE capture (`Exit_audit_capture.emit_exit_audit` no-op on `TriggerPartialExit`). Dispatching now = competing PR on files under concurrent development. Low urgency (P1) per the 06-11 priorities doc. NOT a queue-depth skip — surfaced as an [info] decision item. |
| harness | — | skipped | No eligible T1/T3 item — Tier 1 drained (all `[x]`); only open items are T2 (milestone-gated: M5/M7/before-M6), T3-C (superseded), T3-H (low-pri, gated on T1-N golden scenarios), T4 (human-request-only). `harness.md` unchanged since 2026-05-22. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged. ADL needs human input (free-tier registration / vendor eval); sector-metadata needs live HTTP. |
| code-health | — | skipped | `dev/status/cleanup.md` §Backlog has no real open `[ ]` item (only the template placeholder); deterministic Step 6 surfaced no `[medium]`/`[high]` (build + runtest clean, fmt + magic-numbers OK). |

## Feature Progress

### Strategy program — maintainer forward progress, no orchestrator dispatch
Since the last orchestrator run (#1517–#1528, maintainer-local), two pages
turned:

1. **harvest-and-rotate (the prior P0) was screened NO-BUILD — and the writeup
   was self-corrected.** The first writeup overclaimed ("REJECTED, decisively");
   after review (new rule `.claude/rules/mechanism-validation-rigor.md`), the
   honest finding is weaker and differently shaped: the realizable per-event test
   (n=373 cash-blocked decisions) is a **coin flip** (median −0.12%, C beats P
   49.9%) with a mild fat-LEFT-tail cost to rotating — *no obvious free lunch,
   don't prioritize a build*, NOT a rigorous rejection. (#1521–#1524 docs.)
2. **The user greenlit the rigorous test anyway** (mechanism as a default-off
   surface → WF-CV, to get the decomposed *why*). **Step 1** (core
   `TriggerPartialExit` transition) **merged via #1525**. **Step 2** (the
   harvest-rotate dial, default-off) is open as **PR #1528** — 12 files, +708/-7,
   new `Harvest_rotate_runner` + wiring + 13-case test, all behind
   `enable_harvest_rotate=false` (bit-identical default). CI red only on
   `dune fmt`; maintainer self-review NEEDS_REWORK. Steps 3–5 (axis wiring,
   decomposed WF-CV on top-3000, ledger) follow; the WF-CV is data-gated in GHA.

**All forward validation requires broad-PIT WF-CV or maintainer-local run
warehouses → GHA data-gated.** The orchestrator cannot advance P0 or the
harvest-rotate WF-CV without `EODHD_API_KEY` + deep PIT snapshots in GHA.

## QC Status

| Track | Structural | Behavioral | Overall | Quality | PR |
|-------|-----------|-----------|---------|---------|----|
| harvest-rotate | NEEDS_REWORK (maintainer self-review, not orchestrator) | not run (gated on fmt fix) | NEEDS_REWORK | — | #1528 — maintainer-authored + self-QC'd; out of the orchestrator's QC/merge lane (active in-flight). |

No **orchestrator-dispatched** feature PR was open or READY_FOR_REVIEW this run.

## Budget

- Budget cap: $50.00 (`dev/config/merge-policy.json`); target 60–80% ($30–$40).
  `auto_merge_enabled: true`.
- Subagents spawned this run: **0** (no eligible dispatch).
- Per-subagent breakdown: n/a (none spawned).
- Orchestrator overhead only: 1 `dune build` (exit 0) + 1 full `dune runtest`
  (exit 0) on `53822998`, index-size linter, ~10 GitHub REST calls (HEAD
  check-runs, open PRs, PR #1528 detail/files/reviews/check-runs, #1509 state,
  recent merges, ci-red issues), `_index.md` reconcile (1 row + header), health
  report, this summary.
- Any subagent killed mid-flight: **No** (none spawned).
- **Estimated cost this run (estimated):** ~$1–2 (state-read + 2 build/test
  cycles + REST + summary; no subagents). Measured figure lands post-run in
  `dev/budget/2026-06-11-${GITHUB_RUN_ID}.json` (GHA capture step).
- Utilization assessment: **UNDER_TARGET** (~$1–2 / $50 ≈ 3%). **All eligible
  work data-gated / maintainer-in-flight / human-fork-gated — no remaining work
  this window**; no eligible track was withheld for budget reasons. The single
  highest-value lever (P0 + harvest-rotate WF-CV) is GHA data-gated; see
  §Escalations for the unblock (`EODHD_API_KEY` GHA secret + top-3000 warehouse).
- Scope reduced due to budget: No.

## Data Operations

(omitted — ops-data not dispatched; `EODHD_API_KEY` absent in GHA;
`dev/notes/data-gaps.md` unchanged)

## Harness Work

(omitted — harness-maintainer not dispatched; no eligible T1/T3 item)

## Health Scan

Deterministic Step 6 checks (`dev/health/2026-06-11-fast.md`):

- **Result: CLEAN**
- Build gate (6.1): `dune build` exit 0 + full `dune runtest` exit 0 on
  `53822998`; `0` `^FAIL:` lines. The "No dune-project file in directory ."
  warning is the benign dune-workspace-root notice (workspace root carries
  `dune-workspace`, sub-projects carry `dune-project`), not a failure.
- Status integrity (6.2): covered by `dune runtest` (wired into
  `devtools/checks/dune`), exit 0.
- Index-size linter (post Step 5.5 reconcile): exit 0 (5333/20480 bytes, all
  rows ≤ 250 chars; longest 226).

## Follow-up Queue

- harvest-rotate: maintainer to run `dune fmt` + commit on #1528 (clears CI),
  then Steps 3–5 (axis wiring → decomposed WF-CV on top-3000 → ledger).
- stage-accuracy / experiment-platform: broad-universe WF-CV re-run of the
  cascade-selection inversion (data-gated).
- trade-forensics: PR-3 capture ratio + PR-4 chart render (P1, low urgency);
  MFE/MAE harness gap (P2, small + well-scoped, but collides with #1528).
- backtest-perf / simulation: worst-vol-decile prototype (regime-tag-gated);
  multi-start E2E `run` (data-gated); snapshot-loader windowed/mmap decode.
- data-foundations: bars-retention recovery decision or close — READY_FOR_REVIEW.

## Integration Queue

- **#1528** `feat(weinstein): harvest-rotate dial — trim Stage2-late winners via
  TriggerPartialExit (default-off)` (`feat/harvest-rotate`) — maintainer-authored
  code PR, **CI `build-and-test` FAILURE** (H1 `dune fmt` only; `perf-tier1-smoke`
  SUCCESS), maintainer self-reviewed **NEEDS_REWORK**. Out of the orchestrator's
  Step 6.5 lane (active maintainer in-flight; not auto-mergeable while CI red).
  Unblock: maintainer runs `dune fmt` + commits.
- **No orchestrator feature PR is awaiting merge** — none dispatched this run.

## Current Milestone Target

- **Strategy lever (in-flight via the maintainer):** the harvest-rotate rigorous
  test (default-off surface → decomposed WF-CV). Step 1 merged (#1525), Step 2
  open (#1528). Expected REJECT per `project_edge_is_the_fat_tail`; the value is
  the WF-CV-grade *why* (structural-tax vs timing vs picks). The broader thesis
  stands: the single-dial position-management surface is mined out; the edge is
  the fat-tailed let-winners-run behaviour, so future search should bias toward
  tail-*preserving* levers + coherent presets as wholes.
- **Methodology infrastructure (extended):** new `mechanism-validation-rigor.md`
  rule + `screen-rigor` skill codify the 7-check screen discipline (the
  harvest-rotate overclaim that prompted them).
- **Core-perf (blocked):** snapshot-loader windowed/mmap decode — maintainer-local.

## Dependency Unlocks

- No "Interface stable: YES" flips this run (no dispatch).

## Escalations

(All `[info]` / `[critical-decision-needed]` — no bare `[critical]`. Main is
GREEN; the deterministic Step 6 build gate exited 0 with live evidence in
§Health Scan on `53822998`, so no `[critical]` build/linter assertion is
warranted.)

- [info] **Carried-forward verification: no still-real bare `[critical]` items;
  main GREEN** (`dune build` + `dune runtest` exit 0 on `53822998`; full CI green
  on the identical SHA; 0 open `ci-red` issues).
- [info] **PR #1528 CI red on `dune fmt` only — maintainer's lane.** The
  maintainer's self-review already named the fix (run `dune fmt` + commit). No
  orchestrator action: re-QC is redundant, and reworking the PR would collide with
  the active maintainer session on `harvest_rotate_*` / `weinstein_strategy.ml`.
  Flagged so it is visible, not because the orchestrator should touch it.
- [info] **Broad-universe / broad-PIT data is the single highest-value unblock —
  still GHA DATA-GATED.** Adding `EODHD_API_KEY` as a GHA secret + plumbing it
  through `.github/workflows/orchestrator.yml`, and staging the deep top-3000 PIT
  snapshot warehouse (currently maintainer-local on container `/tmp`), would let
  the orchestrator run the **P0 cascade-inversion broad-universe validation** and
  the **harvest-rotate Step-4 decomposed WF-CV** autonomously. The GHA data gate is
  the sole remaining barrier to autonomous strategy-program progress. (carried,
  ~21st run.)
- [info] **Hand harvest-rotate Steps 3–5 or trade-forensics PR-3 to the
  orchestrator?** Step 3 (Variant_matrix axis wiring for `harvest_rotate_enabled`)
  and PR-3 (post-exit capture ratio) are genuine GHA-eligible OCaml tooling. Both
  are held this run to avoid colliding with the maintainer's active session on the
  same files (#1528 + `trade_audit_*`). If you want either parallelized once #1528
  lands, say so and the orchestrator dispatches next run. NOT a queue-depth skip.
- [info] **The single-dial strategy-mechanism surface is exhausted** (cascade-
  reweight, laggard, ma-hold, force-exit-off all WF-CV-REJECTED; harvest-rotate now
  NO-BUILD at the cheap screen). Per the 06-11 priorities doc +
  `.claude/rules/weinstein-faithful-core.md`, the redirect is tail-preserving
  levers (breadth, entry quality, holding discipline) + coherent presets as wholes,
  not a 7th dial. Needs the human **fork decision** before dispatch.
- [info] **Orchestrator GH token scope gap** (carried) — bot token can create/
  merge PRs + post PR reviews but lacks `actions:write` + `issues:write`. Granting
  these would let the orchestrator re-run transient-failed CI + manage `ci-red`
  watchdog issues. (No occurrence this run — main green; 0 open `ci-red` issues.)
- [info] **data-foundations READY_FOR_REVIEW** — maintainer to decide bars-
  retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close the track.
- [info] **`gh`/`jst` not available in the GHA env** (carried) — orchestrator
  creates/merges PRs + posts QC reviews via `curl` REST. No dispatch this run, so
  only the daily-summary PR exercises it.
- [info] **15y split-day adjustment regression on `weinstein_strategy.ml`**
  (carried) — Investigation #998 root-caused; fix pending; touches core →
  feat-weinstein scope-decision.
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (carried)
  — `tuning.md`: M2 not yet enabled for GHA dispatch (precedent #1327).
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~29th run) —
  live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the
  M6.1–M6.5 verification harness is done; live wiring is the next capability
  milestone and is currently unowned/unscheduled. Not a bare `[critical]` (does
  not trip the gate-fail).

## Questions for You

1. **EODHD_API_KEY in GHA** (carried — still the top concrete unblock). Adding
   this secret + staging the deep top-3000 PIT snapshot warehouse would let the
   orchestrator run the **P0 cascade-inversion broad-universe validation** and the
   **harvest-rotate Step-4 decomposed WF-CV** autonomously. Without it, the entire
   strategy program stays maintainer-local.
2. **Parallelize harvest-rotate Step 3 / trade-forensics PR-3 once #1528 lands?**
   Both are GHA-eligible OCaml tooling, held this run only to avoid colliding with
   your active session. Say the word and the orchestrator picks one up next run.
3. **Resolve the strategy fork?** The single-dial surface is mined out (now incl.
   harvest-rotate NO-BUILD at screen). The redirect is tail-preserving levers +
   coherent presets as wholes — needs your fork call before dispatch.
4. **data-foundations** — READY_FOR_REVIEW. Action the bars-retention recovery or
   close the track?
5. **M6.6 live cycle** (carried) — the next capability milestone (live
   DATA_SOURCE + cron + alerts + trading-state durability) is unowned/unscheduled.
   Schedule it, or keep deferred?
6. **Grant the orchestrator token `actions:write` + `issues:write`?** (carried) —
   would let it self-heal infra-flake red main + manage `ci-red` watchdog issues.
   No occurrence this run (main green), but the gap stands.

## Per-run links

- [run 1] `dev/daily/2026-06-11.md` (this file) — FULL PASS / **0 DISPATCH**.
  harvest-rotate Step 2 (#1528) is maintainer-in-flight, CI red on `dune fmt`,
  self-reviewed NEEDS_REWORK — out of the orchestrator's QC/merge lane. All
  forward strategy increments broad-PIT data-gated / maintainer-in-flight /
  human-fork-gated; no orchestrator feature PR awaited QC. Reconciled 1 stale
  `_index.md` row (#1509 merged). Main GREEN (`53822998`, build + runtest exit 0,
  full CI green); Health CLEAN. 0 subagents; ~$1–2 estimated.

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
