# Status — 2026-05-07 [run 2]

**Run timestamp:** 2026-05-07T13:00:00Z (run start) — 2026-05-07T13:30:00Z (summary write, approx)
**Run ID:** 2026-05-07-run-2
**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 1 FAIL: open PR #916 `feat/equity-curve-truncation-fix` has tip `2e7986d2` with no review file; Conditions 2/3/4 PASS — no commits on main since run-1's summary commit at 11:34:09 +0200, no drift warnings, harness/cleanup unchanged).

## Saturated-queue check result

- **Condition 1** FAIL — PR #916 (opened 2026-05-07T12:50:39Z by maintainer, ~10 min before this run) has `tip_sha=2e7986d2` and no `dev/reviews/<track>.md` review file; `last_review_sha=""` → tip != reviewed.
- **Condition 2** PASS — 0 non-summary commits on `main` since `PREV_ISO=2026-05-07T11:34:09+02:00` (run-1's summary commit `b244866` is HEAD; nothing has merged since).
- **Condition 3** PASS — Step 1b ran; no `[drift]` warnings (tuning `awaiting-merge` at #914 with same tip SHA `826e90f8` is normal pre-merge lag, not drift).
- **Condition 4** PASS — `dev/status/harness.md` and `dev/status/cleanup.md` unchanged since run-1 summary commit.

Decision: full pass for state inspection + Step 5.5 reconcile, but **NO agents dispatched** — see §Dispatched this run for per-track skip rationale.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| tuning | awaiting-merge | feat/backtest-tuning-bayesian-opt-cli | #914 | T-B CLI binary; QC APPROVED q=4 in run-1 (tip `826e90f8` unchanged); CI green at QC time. `mergeable_state` is `behind` (run-1 summary commit advanced main). After merge: 81-cell flagship sweep on `screening.weights.*` (local-only); smoke-scenario sanity-check (local-only); T-C ML/HMM (Norgate-blocked). |
| simulation | skipped (in-flight, human-driven) | feat/equity-curve-truncation-fix | #916 | Maintainer-authored P1 fix from `dev/notes/next-session-priorities-2026-05-07.md` — equity-curve truncation root-caused to ANDV→MPC merger leaving the strategy unable to price held positions; introduces `Market_data_adapter.get_previous_bar` forward-fill, authoritative `step_result.had_market_bars` field, new `Stale_hold` detector, and `Summary.stale_held_symbols`. CI `build-and-test` FAILING (annotation `Process completed with exit code 1` at .github line 37); `perf-tier1-smoke` success. Human is iterating; orchestrator did not auto-QC. |
| backtest-perf | not dispatched (local-only) | — | — | V1/V2/V3 verifications local-only; tier-4 manual `workflow_dispatch` local-only. |
| experiments | not dispatched (local-only) | — | — | M5.4 E3 stop-buffer sweep run + report; E4 scoring-weight sweep run + report. Both local-only ~5×2h each. |
| data-foundations | not dispatched (large + vendor-blocked) | — | — | M5.3 streaming Phase F COMPLETE 2026-05-06. M7.0 Synth-v3 (~1000 LOC, would need plan-first). Norgate vendor-blocked. EODHD multi-market resolver already shipped (#772). |
| all-eligible | not dispatched (deferred follow-ups) | — | — | PR-3 release-report pipeline wiring (deferred per status file); Stage-1→2 fixture; extract shared scaffolding. No plan file for PR-3. |
| harness | skipped (backlog drained) | — | — | Tier 1 fully checked off; Tier 2 milestone-gated; Tier 3 mostly drained (T3-C superseded, T3-H low-priority); Tier 4 not auto-dispatched per orchestrator policy. |
| cleanup | skipped (backlog empty) | — | — | §Backlog empty; no new `[medium]`/`[high]` findings from Step 6 deterministic checks. |
| ops-data | skipped (no-change) | — | — | `dev/notes/data-gaps.md` substantively unchanged since 2026-05-03 maintainer cycle; `data/sectors.csv` 21 days old (refresh threshold >30 days); `EODHD_API_KEY` unset in this runtime. |
| orchestrator-automation | blocked (Phase 2 pending) | — | — | Phase 2 background-execution items pending empirical tests. |
| cost-tracking | passive | — | — | Today's run-2 measured cost will land in `dev/budget/2026-05-07-run<id>.json` post-GHA-capture. |
| (all other tracks merged) | — | — | — | optimal-strategy / data-panels / hybrid-tier / trade-audit / backtest-infra / backtest-scale / data-layer / portfolio-stops / sector-data / strategy-wiring / support-floor-stops / screener / weekly-snapshot / short-side-strategy — no change since run-1. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| tuning | — | skipped | open PR #914 in flight, no new commits since run-1 QC (tip `826e90f8` unchanged; `dev/reviews/tuning-pr914.md` Reviewed SHA matches). Awaiting human merge. |
| simulation | — | skipped | open PR #916 in flight, maintainer-authored, CI `build-and-test` FAILING with `exit code 1`. Human is actively iterating on the fix; orchestrator does not auto-QC non-orchestrator-dispatched human PRs in active iteration with CI failing. (Cost reasoning: qc-structural's H2 hard gate would deterministically NEEDS_REWORK on the same CI signal already visible to the human; spend has near-zero marginal value.) Surfaced in §Pending work + `_index.md` simulation row reconcile. |
| backtest-perf | — | skipped | V1/V2/V3 verifications local-only (need broad universe data not present in GHA); tier-4 manual workflow_dispatch local-only. Not dispatchable here. |
| experiments | — | skipped | M5.4 E3/E4 sweep runs local-only (~5×2h each, full universe). |
| data-foundations | — | skipped | M7.0 Synth-v3 (~1000 LOC) too large without plan-first; Norgate vendor-blocked; EODHD multi-market already shipped (#772). |
| all-eligible | — | skipped | PR-3 release-report pipeline wiring is "deferred" per status file; no plan file exists; would touch the release-report pipeline (cross-cutting; would need plan-first per Step 3.5 trigger 2). Not dispatched. |
| harness | — | skipped | Tier 1 fully checked off; Tier 2 milestone-gated; Tier 3 drained (T3-C superseded, T3-H low-priority); Tier 4 not auto-dispatched. |
| cleanup (Step 2e) | — | skipped | §Backlog empty; no new `[medium]`/`[high]` findings from Step 6 deterministic checks (build/runtest exit 0; status_file_integrity advisory FAILs unchanged at 7). |
| ops-data | — | skipped | `data-gaps.md` substantively unchanged since 2026-05-03; sectors.csv 21 days old (threshold >30); `EODHD_API_KEY` unset. |

## Feature Progress

### tuning [IN_PROGRESS — T-B CLI binary awaiting human merge]

No change since run-1: PR #914 (`feat/backtest-tuning-bayesian-opt-cli`, tip `826e90f8`) is APPROVED q=4 by both qc-structural and qc-behavioral; audit `dev/audit/2026-05-07-tuning-pr914.json` written. `mergeable_state=behind` because run-1's own summary commit `b244866` advanced `main` (#915 merged). Sits in §Integration Queue for human merge or rebase.

### simulation [IN_PROGRESS — maintainer-driven fix in flight]

Maintainer opened PR #916 (`feat/equity-curve-truncation-fix`) at 2026-05-07T12:50:39Z, ~10 minutes before this orchestrator run. The PR addresses P1 from `dev/notes/next-session-priorities-2026-05-07.md` (the priorities note itself was not committed to main as of HEAD `b244866`):

- **Root cause framed in PR body**: ANDV (Andeavor) merged into MPC on 2018-10-01; the strategy held ANDV through the merger and never closed it. From 2018-09-29 onward the simulator could not price the held position, so `_compute_portfolio_value` errored and the fallback returned just `current_cash`; `Backtest.Runner.is_trading_day` flagged `value=cash` with positions open as a non-trading day, dropping every subsequent step from the filtered steps list; `equity_curve.csv` (built from filtered steps) ended at the day before the merger; `Sharpe`/`MaxDrawdown`/`CAGR` distorted via `Metric_computer_utils.is_trading_day_step`. `trades.csv` was unaffected.
- **Fix surface (4 layers)**: (A) forward-fill valuation via `Market_data_adapter.get_previous_bar` for any held symbol without today's bar; (B) authoritative `step_result.had_market_bars : bool` trading-day signal replacing the brittle value-vs-cash heuristic; (C) new `Trading_simulation.Stale_hold` module — pure detector + log + sexp roundtrip, configurable `stale_hold_policy` (default K=5 days); (D) `Summary.stale_held_symbols : string list` distinct stale-flagged symbols across the run.
- **Diff**: 30 files modified, +407/-150 LOC; new files `simulation/lib/stale_hold.{ml,mli}` (+222), modified `simulator.{ml,mli}`, `runner.{ml,mli}`, `metric_computer_utils.{ml,mli}`, plus test wiring across 11 test files.
- **CI**: `perf-tier1-smoke` PASSED (build smoke completes, runtime parity holds against the BAH-SPY scenario); `build-and-test` FAILED with `Process completed with exit code 1` annotation at the workflow file. The split CI status (one job pass, one fail) is suggestive — the failure is most likely in either `dune runtest` of the new `Stale_hold` tests, the `test_runner_filter` rewrite (74/-58 LOC), or `test_metrics` (+13 LOC); without annotation detail the orchestrator can't differentiate.
- **Out of scope (per PR body)**: active force-close on cash-merger / stock-merger semantics — requires structured M&A vendor data; tracked separately.

The orchestrator's role here is to surface the in-flight PR in §Pending work and the `_index.md` reconcile and stay out of the human's way. Re-QC on a future orchestrator run is the right time, after the human has fixed CI and the tip stabilises.

### Capital-recycling sprint follow-up (no new work this run)

The 19-PR sprint that closed on 2026-05-06→07 is fully merged; the remaining diagnostic surface (P1 equity-curve truncation, P2 MRO zombie position, P3 progress.sexp current_equity 10× discrepancy listed in `dev/notes/next-session-priorities-2026-05-05.md` §Priority 5) is now being driven by the maintainer via #916 (P1) and direct work on the other items.

### experiments / data-foundations / backtest-perf / all-eligible [no work this run]

All sweep RUNS for M5.4 E3/E4 remain local-only. M7.0 Synth-v3 (~1000 LOC) too large without plan-first triggers. EODHD multi-market resolver already shipped via #772. Norgate vendor-blocked. backtest-perf V1/V2/V3 verifications + tier-4 manual workflow_dispatch are local-only. all-eligible PR-3 (release-report pipeline wiring) remains a "deferred" follow-up without a plan file.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| tuning (T-B CLI binary) | #914 (open, not draft, mergeable_state=`behind`; CI green at last check: `build-and-test` success + `perf-tier1-smoke` success) | APPROVED (run-1) | APPROVED (run-1) | 4 | `dev/audit/2026-05-07-tuning-pr914.json` (carried from run-1; no re-QC this run since tip unchanged) |
| simulation (equity-curve truncation fix) | #916 (open, not draft, mergeable=true, CI `build-and-test` FAILURE) | not run | not run | — | not written — orchestrator did not dispatch QC on human-driven PR with failing CI |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **0**
- Per-subagent breakdown:
  | Agent | Model | Status | Tool uses | Tokens |
  |-------|-------|--------|-----------|--------|
  | (none) | — | — | — | — |
- Any subagent killed mid-flight: NO (none dispatched).

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-05-07-run<id>.json` post-GHA-capture step.

- Total (estimated): ~$1 of $50 cap (~2%) — orchestrator overhead only (Read/Edit/Bash/curl + 1× `dune build && dune runtest --force` health gate + 1× `status_file_integrity.sh`).
- Utilization assessment: **UNDER_TARGET** (<60%) — ~2% of cap. Eligible-work analysis: zero dispatchable feat tracks this run. All non-merged IN_PROGRESS tracks are either (a) in flight with an open PR (tuning #914, simulation #916), (b) local-only (backtest-perf V1/V2/V3, experiments E3/E4, simulation baseline rerun), (c) too large without plan-first (data-foundations Synth-v3 ~1000 LOC), (d) vendor-blocked (Norgate / T-C ML), or (e) deferred follow-ups without plan files (all-eligible PR-3). Harness backlog drained; cleanup backlog empty; ops-data inputs unchanged. Cumulative day spend across run-1 + run-2: ~6-10% of cap (run-1's 3 subagents drove most of it).
- Scope reduced due to budget: NO.

## Data Operations

(ops-data did not run.)

`data-gaps.md` substantively unchanged since 2026-05-03 maintainer cycle. `data/sectors.csv` is 21 days old (refreshed 2026-04-16); refresh recommended at >30 days. `EODHD_API_KEY` unset in this runtime regardless. Not dispatched.

## Harness Work

(harness-maintainer did not run this orchestrator session.)

Backlog drained per run-1 summary: T1 fully checked off; T2 milestone-gated (T2-B/C/D require future M5/M6/M7 milestones); T3 mostly drained (T3-C superseded by T1-E baseline, T3-H low-priority "spawn qc-structural on individual commits"); T4 not auto-dispatched. No new actionable items since run-1.

## Health Scan

From deterministic Step 6 checks on HEAD `b244866`:

- `dune build` exit **0** (PASSING) — verified live this session.
- `dune runtest --force` exit **0** (PASSING) — verified live this session.
- `status_file_integrity.sh` (direct invocation) exit **0** with **7 advisory FAILs** printed but not propagating exit code — same 7 lines as run-1: `all-eligible.md` ×2 (`## Status` trailing clause `IN_PROGRESS (PR-2 ready for review; PR-1 merged via #899)` + missing `## Interface stable`), `hybrid-tier.md` (`## Status` trailing clause), `orchestrator-automation.md` (`## Status` trailing clause), `tuning.md` (`## Status` trailing clause), `weekly-snapshot.md` ×2 (`## Status` says PLANNED + `## Interface stable` value `NO — track is brand-new.`). When invoked via `dune runtest`, the script's exit is not propagated to dune's exit code (verified — `dune runtest --force` exits 0). Per orchestrator policy: gate is dune exit code only.
- Result: **CLEAN** (build/runtest green; advisory integrity warnings unchanged from run-1, no new violations).

Note: `all-eligible.md`'s `## Status` trailing clause now reads "IN_PROGRESS (PR-2 ready for review; PR-1 merged via #899)" — the "PR-2 ready for review" suffix is stale (PR #901 MERGED 2026-05-06 per run-1 summary). The schema-drift `[info]` carried from run-1 covers this; not a new violation.

## Follow-up Queue

Total open follow-up items across all tracks: ~7-8 (carried; no new this run).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): local-only.
- **covid-recovery 20.81% WR investigation** (carried; deferred until short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**: NOT exercised this run (no QC dispatched). 14+ consecutive runs with sporadic exercise; recommendation re-iterated: tighten qc-structural agent definition to enforce exit-code-only H3 verdict.
- **qc-structural review-file persistence gap**: NOT exercised this run (no QC dispatched). 14+ consecutive runs with sporadic exercise; recommendation re-iterated: anchor write path with `realpath` or `git rev-parse --show-toplevel`.
- **Schema drift from maintainer-driven status updates** (carried 11 runs, advisory only — count steady at 7 from run-1; no new violations this run). Pre-decision: grow `status_file_integrity.sh` enum vs. enforce strict prefix-only. Harness backlog candidate.
- **magic_numbers linter false-positives on docstring text** (carried 10 runs, not exercised this run). Linter-side fix candidate; harness backlog.
- **Smoke-scenario sanity-check on T-B CLI binary** (carried from run-1 §Follow-up; agent's session deferred this; recommended local-verification before T-B convergence cross-check vs the T-A grid sweep result).
- **PR-3 release-report pipeline wiring for `all_eligible_runner`** (carried).
- **3 side-issues from #853 (carried from priorities note)**: equity_curve.csv truncation **NOW BEING ADDRESSED via #916**; MRO zombie position; progress.sexp current_equity discrepancy. P1 (equity-curve truncation) is in flight via maintainer's #916 — once that lands, only MRO zombie + progress.sexp discrepancy remain.

## Integration Queue

(Features with overall_qc APPROVED — ready to merge to main pending human decision)

- **#914 (tuning / T-B CLI binary)** — `feat/backtest-tuning-bayesian-opt-cli` @ tip SHA `826e90f8abee4d2fbb5020baa2cafdc23afcc857`. Carried from run-1: `mergeable_state=behind` (run-1's own summary commit advanced main); CI green at QC time (`build-and-test` success + `perf-tier1-smoke` success); not draft. qc-structural APPROVED; qc-behavioral APPROVED q=4; audit `dev/audit/2026-05-07-tuning-pr914.json` written. **Recommended merge action**: human reviewer can merge cleanly after a no-op rebase onto current main. Wires shipped lib (`Tuner.Bayesian_opt`, PR #817) to a `Backtest.Runner.run_backtest`-backed evaluator. Smoke-scenario sanity-check deferred (documented; quick local-verify owner: human reviewer or next session).

## Current Milestone Target

No change since run-1.

- **M5.3 daily-snapshot streaming** — Phase F COMPLETE 2026-05-06. `Bar_panels` retired end-to-end via #875/#876/#877.
- **M5.4 mechanical experiments** — partial. E1 / E2 / E3-harness / E4-harness MERGED; sweep RUNS local-only.
- **M5.5 parameter tuning** — T-A grid_search lib MERGED (#805); T-A CLI binary MERGED (#893); T-B Bayesian-opt lib MERGED (#817); **T-B CLI binary in §Integration Queue via #914**. Pending after merge: 81-cell flagship sweep on `screening.weights.*`; T-C ML/HMM (Norgate-blocked).
- **M6 weekly-snapshot verification** — DONE on M6.1-M6.5 content; M6.6 live cycle DEFERRED.
- **M7.0 Track 1 Norgate** — pending (vendor signup blocker).
- **M7.0 Track 2 multi-market** — DONE (#772).
- **M7.0 Track 3 Synth-v1 + v2** — DONE; Synth-v3 pending (~1000 LOC).
- **Wiki+EODHD interim historical universe** — PR-A/B/C/D ALL MERGED.
- **Tier-4 release-gate** — scaffolding (#810) + SCALE (#811) + Daily_panels O(log N) (#845) + tier4-broad-1y (#897) MERGED; manual `workflow_dispatch` is local-only.
- **CI golden-runs Phase 1** — LIVE 2026-05-06 (#873).
- **GitHub branch protection on main** — LIVE 2026-05-07 (#895).

## Dependency Unlocks

(No new unlocks this run — main is unchanged from run-1.)

- **#914 (T-B CLI binary) awaiting merge unblocks**: T-B Bayesian-opt convergence cross-check vs T-A grid result.
- **#916 (equity-curve truncation fix) awaiting CI-fix + merge unblocks**: meaningful 16y Cell E summary metrics; clean Sharpe / MaxDD / CAGR on cell-E baseline; potential follow-on for the other two side-issues from #853 (MRO zombie + progress.sexp 10× discrepancy).

## Escalations

(All `[info]`-tagged; no `[critical]` items raised this run. Live verification of main on HEAD `b244866` shows `dune build && dune runtest --force` exit 0.)

[info] **PR #916 build-and-test CI failure** (NEW 2026-05-07 run-2): maintainer-authored equity-curve truncation fix has CI `build-and-test` job failing with `Process completed with exit code 1` annotation at .github line 37. The split CI status (`perf-tier1-smoke` success, `build-and-test` failure) is suggestive — the failing job runs `dune runtest`, which means a test in the new `Stale_hold` suite, the rewritten `test_runner_filter`, or the modified `test_metrics` is most likely the failing site. Annotation detail not exposed by GH API. Not orchestrator-actionable; surfacing for visibility. Action item: human reviewer to fix CI; orchestrator will re-evaluate on next run if the tip stabilises with green CI.

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried 14+ consecutive runs; NOT exercised this run — no QC dispatched). Recommendation re-iterated: tighten qc-structural agent definition to enforce exit-code-only H3 verdict; never derive H3 FAIL from finding-name text alone.

[info] **qc-structural review-file persistence gap** (carried 14+ consecutive runs; NOT exercised this run — no QC dispatched). Recommendation re-iterated: anchor write path with `realpath` or `git rev-parse --show-toplevel`.

[info] **Schema drift from maintainer-driven status updates** (carried 11 runs, advisory only — count steady at 7 from run-1; no new violations this run). Pre-decision: grow `status_file_integrity.sh` enum vs. enforce strict prefix-only. Harness backlog candidate.

[info] **magic_numbers linter false-positives on docstring text** (carried 10 runs, not exercised this run). Linter-side fix candidate; harness backlog.

[info] **3 side-issues from #853 not yet filed as issues** (carried; equity-curve truncation is now in flight via #916 — once that lands, only MRO zombie position + progress.sexp current_equity 10× discrepancy remain). Recommendation: file remaining 2 as separate GitHub issues so they don't get lost.

[info] **Carried-forward verification (Step 1c)**: no still-real `[critical]` items; main green (exit 0 on `b244866` for both `dune build` and `dune runtest --force`). Step 1c verified all run-1 escalations were `[info]`-tagged; nothing to carry forward as `[critical]`.

## Step 1b drift cross-reference

Most recent prior summary on local FS / on main: `dev/daily/2026-05-07.md` (run-1; committed via PR #915 at 2026-05-07T11:34:09+02:00; merged to main as `b244866`).

Run-1's `## Pending work` table parsed for `dispatched`/`awaiting-merge` rows:
- `tuning` — `awaiting-merge` (PR #914, tip `826e90f8`). Status today: PR still open at the same tip SHA `826e90f8`. `mergeable_state=behind` because run-1's own summary commit advanced main. Normal pre-merge lag, **not drift**.

All other rows in run-1 were `merged-this-cycle`, `not dispatched`, or `blocked` — no `dispatched` rows that would need verification today.

Status file commits on main since prior summary commit (`PREV_ISO=2026-05-07T11:34:09+02:00`, excluding `ops:` summary commits): **0**. The current HEAD `b244866` IS the run-1 summary commit; nothing has merged since.

No `[drift]` warnings emitted.

## Step 1c carried-forward critical verification

Run-1 carried zero `[critical]` items into today (run-1's §Escalations were all `[info]`-tagged). Re-verified live this session on the current HEAD `b244866`:

```
$ cd /__w/trading/trading/trading && eval $(opam env) && dune build && dune runtest --force
exit=0
```

Both exit 0 on HEAD `b244866`. **No `[critical]` items to carry forward.**

[info] Carried-forward verification: no still-real `[critical]` items; main green on `b244866`.

All carried-forward `[info]` items from prior runs re-carried into today's §Escalations as observed (no exercise this run since no agents dispatched).

## Per-run links

- [run 1] `dev/daily/2026-05-07.md` (feat-backtest on T-B CLI binary; QC pipeline APPROVED q=4; audit written)
- [run 2] `dev/daily/2026-05-07-run2.md` (this file — no agents dispatched; state inspection + `_index.md` reconcile only; PR #916 surfaced as in-flight maintainer-driven simulation work)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
