# Status — 2026-08-16

Run timestamp: 2026-08-16T07:32Z (GHA orchestrator run 31933953820)
Run ID: 2026-08-16-run-1
Mode: FULL

> **Checkpointed mid-run, deliberately** — same reason as 08-12 through 08-15.
> Both prior runs ended in workflow `failure`. This file was written and pushed
> before any dispatched agent returned, then updated in place.

## Headline

**The last run's rework commit was written, and then thrown away.** GHA run
31884644256 failed at step 9 with `fatal: 'ops/daily-2026-08-15-run2' is already
checked out at '/tmp/ops-summary'` — and two lines above that error, the log
records the runner's HEAD as `594b4b92 fix(review): address QC rework iteration 1
(#2338) — gate the ## Verdict parsers on FILE_MODE`. **That commit exists nowhere
else.** `harness/audit-gh-stderr-gate` on origin is still `e0aa62d7`. The work was
done, committed, never pushed, and destroyed with the runner.
§"The rework that was written and thrown away".

**Two independent defects, both visible in that one log tail, both one line.**
The worktree collision that stranded #2336 is a fixed path with no cleanup; and
the same line that proves the lost commit also gives the `ops(budget)` bypass its
second, independent confirmation. §"Two one-line defects in a five-line log tail".

**The queue was seven PRs deep, every one CI-green, and three of them had never
been reviewed at all.** That is the largest open queue any recent run has opened
with, and it is the run's actual work. §"Dispatched this run".

## Established before dispatch (measured this run, not assumed)

- **Main GREEN at `a2ad930c`.** Check-runs on that exact SHA: `build-and-test`
  **success**, `perf-tier1-smoke` **success**, `golden-custom-universe`
  **success**. (`golden-sp500-5y` and `orchestrator` in progress — not failures.)
  Zero open `ci-red` watchdog issues.
- **Shell-only gates, run by me, all exit 0:** `status_file_integrity.sh` **0**,
  `index_size_linter.sh` **0** (10505/20480 bytes), `no_python_check.sh` **0**.
- **Queue: 7 open PRs, all seven CI-green** on their tips — the deepest opening
  queue in recent runs. Three carried zero reviews.
- **Toolchain unchanged — fourteenth consecutive run carrying these by hand:**
  `gh` **ABSENT**, `docker` **ABSENT**, `dune` not on PATH, `jj`/`jst` present but
  **BROKEN**. Working: `git`, `curl`, `jq`, `python3`, `bash`,
  `dev/lib/run-in-env.sh`. Plus the `git config --global --add safe.directory`
  dance before the first `git` call.
- **Runner capacity, measured:** 4 cores, 15 GB RAM, 64 GB disk free at start.

## Step 0.5 — fast-exit declined, deterministically

Queue non-empty (7 PRs), so the vacuous-pass precondition did not fire.
**Condition 1 FAILED** on three separate tracks — #2347, #2348, #2349 and #2350
all had **no `Reviewed SHA` at all** against a live tip. **Condition 2 also
FAILED**: `dev/status/harness.md` and others moved on main since the prior merged
summary. Full pass, and not a close call.

## The rework that was written and thrown away

Run 2 on 08-15 recorded `#2338 rework 1/2` as **dispatched, in flight** and exited.
I went to re-dispatch it and checked the prior run's job log first. From
`actions/jobs/95012054360/logs`, the last five meaningful lines of the run:

```
Found ops/daily-* PR #2336 on branch ops/daily-2026-08-15-run2; bundling budget JSON.
HEAD is now at 594b4b92 fix(review): address QC rework iteration 1 (#2338) — gate the ## Verdict parsers on FILE_MODE
Removing dev/audit/2026-08-15-harness-audit-gh-stderr-gate-harness.json
From https://github.com/dayfine/trading
 * branch              ops/daily-2026-08-15-run2 -> FETCH_HEAD
fatal: 'ops/daily-2026-08-15-run2' is already checked out at '/tmp/ops-summary'
##[error]Process completed with exit code 128.
```

The rework agent **finished its job**. It addressed the reviewer's finding, and
took the optional-but-better branch of it — the reviewer had offered "narrow the
comment" as the minimum fix and "gate `:375`/`:384`/`:431` on `FILE_MODE`" as the
author's-call full fix; the commit subject says it took the full fix. Then the run
died before anything pushed it.

**Why this is not recoverable, and why that is the finding.**
`.claude/rules/pr-gate-loop.md` §"Dispatcher-side recovery" tells a dispatcher not
to re-dispatch reflexively — a stalled agent's commit usually still sits in its
workspace, and finishing it from the dispatcher side (`jj bookmark set` →
`jj git push`) is faster and cannot lose the work. **That recipe assumes a
persistent workspace.** On a GHA runner there is none: the container is torn down
with the job, and `jj` is broken here anyway. The recovery path the rules describe
does not exist in this environment, so an unpushed commit is not "stalled", it is
**gone**.

The mitigation is not a better recovery procedure, it is a stricter push
discipline — which `.claude/rules/worktree-isolation.md` §"Finish Protocol"
already states ("Never end a turn with an un-pushed commit… a green local build
that was never pushed is lost work"). The agent appears to have honoured commit
discipline and not push discipline. **I have re-dispatched the work with the lost
commit's own subject line quoted in the brief**, so the second attempt starts from
the first one's conclusion rather than from the reviewer's finding.

## The `ops(budget)` bypass: from "un-reviewed code" to "a feature that never had a PR"

Eight prior runs filed this escalation. Run 1 on 08-15 root-caused it to
`orchestrator.yml:482` cutting the fallback branch from the runner's dirty HEAD.
I did not set out to work on it, but a routine branch-inventory sweep produced the
sharpest instance yet, so it is recorded here.

The inventory started with a **wrong method, which is itself the finding**. I first
classified origin's 18 branches with `git merge-base --is-ancestor <tip> origin/main`
and got **"unmerged" for all 18** — including ones I had merged myself minutes
earlier. The repo **squash-merges**, so a merged branch's tip is never an ancestor
of main. That is the same trap the 08-14 run fell into from the other direction,
and the 08-15 correction already named the fix: **only the PR record answers this
question.** Re-running against `GET /pulls?state=all` classified all 18 correctly
in one call.

It also surfaced one branch that no commit-graph test would have flagged:

```
feat/split-safe-inert-report-row     NO PR FOUND
```

That branch has **no pull request, open or closed**. Its content is nonetheless on
`main`. `git show --stat fff8075e`:

```
fff8075e ops(budget): record 2026-08-07-31178553959 ($21.3693) (#2235)

 dev/budget/2026-08-07-31178553959.json          |  28 +++
 dev/status/support-floor-stops.md               | 110 ++++++--
 .../backtest/test/test_trade_audit_report.ml    | 206 +++++++++++++++-
 .../trade_audit_report/trade_audit_report.ml    |  63 ++++-
 .../trade_audit_report/trade_audit_report.mli   |  31 ++-
 5 files changed, 419 insertions(+), 19 deletions(-)
```

**419 insertions of production code and tests, on main, under a title announcing a
JSON cost record, with zero reviews — and the feature branch it came from never had
a PR at all.** Prior filings established that un-reviewed code rides along on
`ops(budget)` PRs. This is the end state that follows from it: a feature whose
author presumably expected to open a PR later found its code already merged, so no
PR was ever opened and no review ever happened. Nothing in the repo records that
this code was never reviewed — the branch just looks stale.

The one-line fix is unchanged and still blocked on `.github/workflows/**`:
`git fetch origin main && git checkout -B "ops/budget-…" origin/main`.

## Two one-line defects in a five-line log tail

**1. The bundling step collides with a worktree the orchestrator leaves behind.**
`fatal: '<branch>' is already checked out at '/tmp/ops-summary'`. Step 9 wants to
check out the daily-summary branch at a **fixed path**, and the orchestrator's own
Step 8 had already checked that branch out. Git refuses a second checkout of one
branch; `set -e` turns the refusal into exit 128. Consequences, both observed:
**#2336 was stranded open** (it is still open as I write this — I am merging it
this run), and the budget JSON for run 31884644256 never landed.

Fix is a `git worktree remove` / `--force` on the fixed path before the add, or a
per-run unique path. Blocked on `.github/workflows/**` (`workflow`-scoped PAT).
**I have worked around it on my side** by removing my summary worktree before
exit, so the branch is checked out nowhere when step 9 runs — but that is a
convention this run happens to follow, not a guarantee, and the next orchestrator
that forgets will strand its own summary the same way.

**2. The `ops(budget)` bypass gets its second independent confirmation, for free.**
The 08-15 run 1 summary root-caused the review bypass to `orchestrator.yml:482`
cutting the fallback branch from the runner's **dirty HEAD** instead of
`origin/main`, and predicted that pre-existing agent commits ride along into the
budget PR. Line 2 of that tail is exactly the predicted state: at bundling time
the runner's HEAD is **an agent's feature commit**, not `main`. That is the
mechanism, caught in the act, from a log that was written for an unrelated reason.
The one-line fix stands unchanged:
`git fetch origin main && git checkout -B "ops/budget-…" origin/main`.

## Health Scan

`dev/health/2026-08-16-fast.md` — see file. Shell-only gates all exit 0
(`status_file_integrity.sh`, `index_size_linter.sh`, `no_python_check.sh`), and
`build-and-test` + `perf-tier1-smoke` are **success** on main's tip `a2ad930c`.

**Step 6.1's local `dune build`/`runtest` was deliberately not run by me.** Three
QC agents were building concurrently on a 4-core / 64 GB box, each `_build` ~14 GB;
a fourth cold build would have contended for cores and disk with the run's actual
work, and — per the flaky-test entry in `dev/status/cleanup.md`, which now has
**load** as its confirmed correlate — would have raised the odds of a spurious red
rather than lowered them. CI on main's exact tip is the stronger evidence and it
is green. Recorded as a deliberate substitution, not an omission.

## The capacity ceiling on this box is 2 build-heavy agents, not 3

`.claude/rules/container-capacity-scheduling.md` caps concurrent agents at 3. That
number is calibrated for the local ~8-core devcontainer. **This GHA runner has 4
cores**, and I dispatched three build-heavy QC agents into it at once. One starved:
it spent 20+ minutes unable to complete `dune build`, reported the stall as "dune
lock contention", and returned **NEEDS_REWORK on an environment failure** for a PR
it had otherwise found clean.

I inspected the live runner rather than accepting the diagnosis:

```
/tmp/qc-2349/trading/_build        15G, holds the only _build/.lock
/__w/trading/trading/trading/_build   absent
~/.cache/dune                      28K
load average                       10.43  (4 cores)
```

Each agent had its **own** worktree and its **own** `_build`, so there was no shared
lock to contend for and the shared dune cache is negligible. The mechanism is plain
**CPU starvation** — a cold OCaml build here is 10–20 minutes uncontended, and three
of them do not degrade gracefully. The distinction matters because "lock contention"
suggests a serialization fix and "starvation" correctly says *dispatch fewer*.

Two corrections applied immediately, both of which worked:

1. **A public annotation on #2348** recording that its NEEDS_REWORK is an ENVFAIL,
   not review feedback, with the measurements above — so the author does not act on
   it and a later reader is not misled by two verdicts at one SHA.
2. **Reclassifying dispatches as build-heavy vs shell-only.** The two rework agents
   this run touch only shell scripts; they were dispatched with an explicit
   instruction *not* to run `dune build`, and both ran happily alongside two cold
   builds. **The real cap is 2 build-heavy agents; shell-only agents are close to
   free.** That is a more useful rule than a flat count of 3, and it is what let this
   run keep four agents in flight without a second starvation.


## The three-gate loop paid for itself twice, within minutes

Two substantial PRs went from **zero reviews** to **merged** during this run, and
in both cases the maintainer merged within minutes of the second gate landing:

| PR | behavioral APPROVED | merged | gap |
|---|---|---|---|
| #2349 TTL knob split | 07:56:10 | 07:59:09 | **3 min** |
| #2348 ticket-cancel observability | ~08:12 | 08:13:42 | **~2 min** |

Neither merge was mine — `dayfine` merged both. That is the loop working exactly
as `.claude/rules/pr-gate-loop.md` intends: **the gates were the bottleneck, not
the merge decision.** Worth recording because prior runs have framed queue depth as
a review-pacing problem; here it was purely a QC-throughput problem, and clearing
the QC cleared the queue the same hour.

These were also the maintainer's **own #1 and #2 queue items** —
`dev/notes/next-session-priorities-2026-08-16.md` grades defect **F** (artifacts
can't answer basic questions → #2348) and **C+E** (one knob arms re-screen + clock
→ #2349) as the top two. The run did not have to guess at priority; reviewing the
open queue in PR order happened to land on it.

## Two QC findings worth reading even if you skip the rest

**#2339 — an escalation that could never clear.** The rework-streak consumer
scanned `dev/audit/*.json` for the **maximum** `consecutive_rework_count` per
feature, never reading `recorded_at_ns` or `overall_qc`. That collides with
`write_audit.sh`'s own contract, *"If APPROVED, the streak resets to 0."*
qc-behavioral built the divergence fixture (count=4 NEEDS_REWORK, then count=0
APPROVED) and measured the check firing on a feature the writer had already marked
resolved. Since audit records are committed and never pruned, **the mechanism's
first real firing would also have been its first permanent false escalation** —
and `harness` was the likely first victim, with 23 records sharing that slug. Blast
radius today was zero (all 121 real records max at 1), which is exactly why it
needed a fixture rather than a bug report. Fixed to latest-by-timestamp.

**#2338 — a comment that was still false after being fixed.** Iteration 1 gated
the three leaking `## Verdict` parsers and added a scenario that pins them
exactly. The author then left the `:335-347` comment unchanged, reasoning the fix
made its universal claim literally true. qc-behavioral enumerated **all ten**
`REVIEW_FILE` reads and found one still ungated — the SHA extractor at `:497-498`
— and measured it: a PR-mode record picks up `FOREIGNSHA99` from an unrelated
file, against `""` in the control. Because `write_audit.sh` uses `--sha` as the
identity key for rework counting, that is a silent mis-key of the very counter
#2339 above exists to drive. Iteration 2 was therefore **comment-only**: replace
the universal claim with a closed list of four sites and name the residual as
`H-AUDIT-SHA-FILE-LEAK`.

**This is the third PR this week rejected for the same defect class** (#2327 took
three iterations, #2338 two). The pattern is stable enough to state as a rule:
*when a comment asserts a universal property over a set of call sites, the review
should enumerate the set.* Both times, enumeration found the exception in one
grep. That is a cheaper check than the argument it replaces.

## A wrong-base diff in a QC review, caught before it mattered

qc-structural's A3 row on #2352 states the PR *"consolidated
`enable_entry_ticket_rescreen` + `entry_order_max_rest_weeks` into
`entry_order_ttl_weeks` — schema unification."* **It did no such thing.** The
branch was cut at `81759a22`, which predates #2349 (merged this morning); the old
field name on that branch is its **stale base**, not a change it makes. The agent
diffed against current `main` instead of the merge-base and read the difference as
intentional.

I checked rather than carried it:

```
merge-base                         81759a22   (predates #2349)
#2352's real .mli diff             only the stop_width_mode docstring, ~line 1462+
TTL field region                   ~1392-1461, untouched by #2352
git merge-tree <mb> <branch> main  0 conflict markers
```

So there is **no schema regression** — #2349's split survives the merge intact —
but the A3 row was evaluated against the wrong diff, and the misreading ran in the
*approving* direction, which is the harder kind to notice. `.claude/rules/qc-structural-authority.md`
already warns about A3 methodology (PR #687's git-log ancestry walk); this is the
same error reached by a different route, and worth adding to that row's guidance:
**diff against the merge-base, not against `main`.** I passed the correction into
the behavioral brief so the second reviewer did not inherit the premise.

## Dispatched this run

**19 dispatches: 3 writer sessions (all reworks), 16 QC passes.** **Seven PRs merged.**

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| ops | — | **merged** | **#2336 → `c26e97a1`** — prior run's stranded summary, recovered |
| ops | — | **merged** | **#2347 → `cade675d`** — weekly track pacer; docs-only, QC skipped per merge-gates |
| strategy | — | **merged** | **#2350 → `4b353105`** — defect-A refutation; docs-only |
| backtest | qc-structural | **ENVFAIL** | #2348 @ `cec629ec` — starved out; annotated non-authoritative, re-dispatched |
| simulation | qc-structural | **APPROVED** (5) | #2349 @ `04b92026` — all three gates exit 0 |
| harness | qc-behavioral | **NEEDS_REWORK** (2) | #2339 @ `15b2a121` — resolved streak can never clear |
| harness | harness-maintainer | completed | #2338 rework 1/2 → `e3878b5c`; full fix + scenario 38; 55/0 → 56/0 |
| harness | harness-maintainer | completed | #2339 rework 1/2 → `b3c65c07`; latest-by-timestamp + featureF fixture |
| simulation | qc-behavioral | **APPROVED** (4) | #2349 — W1/W2 verified against the book; §4.7 quote confirmed verbatim |
| backtest | qc-structural | **APPROVED** (5) | #2348 re-run — all three gates exit 0 with a build slot to itself |
| harness | qc-structural | **APPROVED** | #2338 @ `e3878b5c`; reproduced 56/0 + the scenario-38 mutation |
| simulation | qc-structural | **APPROVED** (5) | #2352 @ `6a108b42` — R1/R2 verified; A3 row used the wrong base (see above) |
| backtest | qc-behavioral | **APPROVED** (4) | #2348 — A1 PASS: dune deps unchanged is the decisive evidence |
| harness | qc-structural | **APPROVED** (5) | #2339 @ `b3c65c07`; tie-break determinism probed across 3 orderings |
| harness | qc-behavioral | **NEEDS_REWORK** (2) | #2338 @ `e3878b5c` — SHA extractor still leaks; `FOREIGNSHA99` measured |
| harness | harness-maintainer | completed | #2338 rework 2/2 → `ecafe950`; comment-only, `H-AUDIT-SHA-FILE-LEAK` filed |
| simulation | — | **merged** | **#2349 → by maintainer**, 3 min after behavioral APPROVED |
| backtest | — | **merged** | **#2348 → by maintainer**, ~2 min after behavioral APPROVED |
| harness | qc-behavioral | **NEEDS_REWORK** (2) | #2339 @ `b3c65c07` — a surviving mutant on the untested `recorded_at_ns` default branch |
| simulation | qc-behavioral | **NEEDS_REWORK** (2) | #2352 — W2 FAIL: the mode's headline size-vs-rank distinction does not exist |
| harness | qc-structural | **APPROVED** (5) | #2338 @ `ecafe950` — confirmed comment-only delta, suite 56/0 |
| harness | qc-behavioral | **APPROVED** (4) | #2338 @ `ecafe950` — re-enumerated all 6 functional reads; 1:1 with the comment's list |
| harness | — | **auto-merged** | **#2338 → `b602123c`**, all three gates green at `ecafe950` |
| ops-data | — | skipped | `data-gaps.md` unchanged since #814 (2026-05-03); `EODHD_API_KEY` absent — 18th week |
| code-health | — | skipped | top cleanup item is a human policy decision (test-file length limits) |
| health-scanner | — | not dispatched | fast scan is deterministic (Step 6, CLEAN); deep scan is a weekly cron |
| feat-backtest / feat-weinstein | — | not dispatchable | strategy P0s are multi-hour container backtests; `docker` absent |

## Pending work

| Track | State | Branch | PR | Next step |
|---|---|---|---|---|
| harness | **auto-merged** | `harness/audit-gh-stderr-gate` | **#2338** → `b602123c` | — (residual filed as `H-AUDIT-SHA-FILE-LEAK`) |
| harness | blocked | `harness/rework-streak-escalation` | **#2339** | behavioral NEEDS_REWORK at `b3c65c07`; **rework 1 of 2 used** — one iteration left |
| simulation | blocked | `feat/stop-width-demote` | **#2352** | behavioral NEEDS_REWORK at `6a108b42`; **0 of 2 reworks used** |
| ops | awaiting-merge | `ops/daily-2026-08-16-final` | **#2356** | this summary + index reconcile + 5 audit records |

## Budget

- Cap **$200.00** (`merge-policy.json`); target band **60–95%** ($120–190). Auth is a
  Max-20x flat-rate subscription, so the cap is a runaway-dispatch backstop, not a
  spend limit — the config says so explicitly.
- **19 dispatches** × ~$7.84 (last measured per-dispatch rate,
  `dev/budget/2026-08-14-31781463085.json`: $109.72 / 14) ≈ **$149**, plus ~$1
  orchestrator overhead. Utilization ≈ **75%** — **IN_TARGET**, and the first run
  in a week to reach the band.
- **What changed versus the last two runs**, both of which were UNDER_TARGET at
  ~47% and ~36%: those runs were constrained by *file contention* within a narrow
  harness backlog. This run had a **seven-PR queue spanning four independent file
  families**, so the binding constraint moved to runner CPU — and once dispatches
  were split into build-heavy (cap 2) and shell-only (effectively free), four
  agents ran concurrently without a second starvation.
- Measured cost lands in `dev/budget/2026-08-16-31933953820.json` via the GHA
  capture step after exit. Note the 08-15 run-1 record has `total_cost_usd: null`
  (7 of 337 records are null; the previous six are all Apr–May), so if this one is
  also null it is the second in two days and worth a look.

## The two rejections that closed the run, and why both are worth the rework

Both landed after the last merge, so neither is in the shipped total — but they are
the run's two sharpest technical results.

**#2339 — a surviving mutant, measured against live data.** My brief asked whether
the new tie-break was reachable. The reviewer answered that (it is: `write_audit.sh`
falls back to **whole-second** resolution on any platform without GNU `date +%s%N`,
so same-`ns` records for one feature are ordinary, not legacy-only — the shipped
comment's "only possible for legacy records" over-claims) and then found something
better by mutating the exact line the rework added:

```
-  [ -z "$streak_recorded_at" ] && streak_recorded_at=0
+  [ -z "$streak_recorded_at" ] && streak_recorded_at=9999999999999999999
```

**The committed change-detector still exits 0 — the mutant survives, zero assertions
redden.** Every fixture passes an explicit timestamp, so the documented
default-to-oldest branch is never executed. And it is not hypothetical: of the **121
real `dev/audit/` records, 121 carry the count but only 40 carry `recorded_at_ns`** —
**81 (67%) hit exactly that branch**, with 10 features tied at the default. The scan
declares `(universe)` and reads that directory for real. Required fix is
test-and-comment only.

**#2352 — the faithfulness argument rests on a distinction that does not exist.**
The PR's case for `Demote_over_max` is that it is closer to §5.1 than `Size_down`
*because* it costs rank rather than size: "keeps size, gives up rank" versus "keeps
rank, gives up size". The reviewer verified the book quote (`weinstein-book-reference.md`
L303 — *"If stop requires >15% risk from entry → prefer other candidates"*, genuinely
comparative, W1 PASS) and confirmed the `overhead_supply` precedent is real in code,
not just prose. Then it checked the sizing:

> Both route an admitted candidate through the same `_size_and_build_entry`, and
> `compute_position_size` takes no `sized_down` input. `sized_down` reaches only the
> trace string and `entry_meta`.

**The two modes produce identical position sizes.** `entry_audit_capture.ml` says so
outright — *"total risk-to-stop stays `risk_per_trade_pct * portfolio_value` **either
way**"* — and the PR's own test file concedes it at L245-246. The test pins a **tag**
difference (`Admit` vs `Admit_sized_down`) that the docstring reads as a **size**
difference. W2 FAIL, and most likely a docstring correction rather than a code change.

**What these two have in common** is what the whole gate loop is for: in both cases
the code is close to right and the *claim about it* is wrong, and in both cases the
reviewer settled it by **running something** — a mutation, a `compute_position_size`
call-site check — rather than by arguing from the source. That is the third and
fourth instance this run of the same lesson, after #2338 and #2327 before it.

## Escalations

*(carried items re-verified this run, not copied; new items marked NEW)*

- **[info] Carried-forward verification: no still-real `[critical]` items.** Main
  green on `a2ad930c` at run start — `build-and-test` and `perf-tier1-smoke` both
  **success**; `status_file_integrity.sh`, `index_size_linter.sh` and
  `no_python_check.sh` all exit 0 locally; zero open `ci-red` watchdog issues. Two
  independent QC agents additionally measured full `dune build` **exit 0** on PR
  branches cut from main.

- **NEW [high] An agent's completed work was destroyed because the GHA runner is
  ephemeral.** Run 31884644256's job log records `594b4b92 fix(review): address QC
  rework iteration 1 (#2338)` as the runner's HEAD; that commit is on no remote and
  the branch never moved. `.claude/rules/pr-gate-loop.md` §"Dispatcher-side
  recovery" instructs a dispatcher **not** to re-dispatch reflexively because a
  stalled agent's commit usually survives in its workspace — **that recipe assumes
  a persistent workspace and is simply false in GHA.** The rule should say so, and
  the agent-facing fix is push-after-every-commit (already in
  `.claude/rules/worktree-isolation.md` §"Finish Protocol", evidently not
  followed). I re-dispatched with the lost commit's own subject quoted in the
  brief; the second attempt reached the same conclusion in one iteration. Blocked
  on `.claude/**`.

- **NEW [high] The daily-summary bundling step fails on a fixed worktree path.**
  `fatal: 'ops/daily-2026-08-15-run2' is already checked out at '/tmp/ops-summary'`
  → exit 128 under `set -e`. That is why **#2336 sat stranded** and why run
  31884644256 is recorded as `failure` despite doing useful work. Fix: `git
  worktree remove --force` on the fixed path before the add, or a per-run unique
  path. **I worked around it by keeping the main checkout on `main` and leaving the
  summary branch checked out nowhere** — a convention, not a guarantee. Needs
  `.github/workflows/**`.

- **[high] The `ops(budget)` review bypass — now with its worst observed
  instance.** Root cause unchanged (`orchestrator.yml:482` cuts the fallback branch
  from the runner's dirty HEAD). New this run: `feat/split-safe-inert-report-row`
  has **no PR at all**, yet `git show --stat fff8075e` shows its content on main —
  **419 insertions** of `trade_audit_report.ml/.mli` plus tests, merged under
  `ops(budget): record 2026-08-07-… ($21.3693)` with **zero reviews**. A feature
  branch whose code landed without ever having a pull request. The one-line fix is
  unchanged: `git fetch origin main && git checkout -B "ops/budget-…" origin/main`.
  Needs `.github/workflows/**`.

- **NEW [medium] The concurrency cap in `container-capacity-scheduling.md` is
  calibrated for the wrong machine.** It says 3 agents; that is right for the local
  ~8-core devcontainer and wrong for this **4-core** GHA runner, where three cold
  OCaml builds starve. Measured: one `_build` per worktree (no shared lock), 28K
  shared dune cache, load average 10.4. **The useful distinction is build-heavy
  (cap 2) vs shell-only (effectively free)** — with that split, four agents ran
  concurrently all run with no second starvation. Worth writing into the rule as a
  per-machine cap rather than a constant. Blocked on `.claude/**`.

- **NEW [medium] A QC agent diffed against `main` instead of the merge-base.**
  #2352's A3 row described a stale base as an intentional schema change (details
  above). No harm done — I verified the merge is clean — but the misreading ran in
  the approving direction. `.claude/rules/qc-structural-authority.md`'s A3 row
  already warns against git-log ancestry walks; it should also say **diff against
  the merge-base**. Blocked on `.claude/**`.

- **NEW [medium] `H-AUDIT-SHA-FILE-LEAK`** — filed on `dev/status/harness.md` this
  run. `record_qc_audit.sh:497-498` reads `REVIEW_FILE` unconditionally in PR mode,
  so a record's `sha` can come from an unrelated `dev/reviews/` file
  (`FOREIGNSHA99` measured, vs `""` control). `write_audit.sh` uses `--sha` as the
  identity key for `consecutive_rework_count`, so the consequence is a mis-keyed
  streak. **Pre-existing on main**, deliberately left out of #2338's scope.

- **[high] The strategy tracks remain structurally undispatchable from GHA.**
  Unchanged: `docker` absent, and the live P0s are multi-hour container backtests.
  **But this run is a partial counter-example worth noting:** three of the six PRs
  merged were strategy/backtest work, reviewed here and merged by the maintainer.
  The orchestrator cannot *run* the backtests, but it can and did clear the review
  gate for strategy code. That is a smaller gap than "ships harness work only".

- **[high] Step 4's GHA dispatch shape is still wrong in the spec** — it prescribes
  `git checkout -b` in the **shared** checkout for parallel agents. Hand-pasted the
  `git worktree` correction into all 16 briefs again. Blocked on `.claude/**`.

- **[high] The "never background your build" instruction is still unfollowable.**
  Bash force-backgrounds at ~120 s; a cold `dune build` here is 10–20 min. The
  detached-with-sentinel pattern was hand-pasted into every brief again — and the
  one agent that hit trouble is the one that waited on an event instead. Blocked on
  `.claude/**`.

- **[high] Step 0.5 Condition 1 still cannot distinguish "reviewed" from
  "finished".** Did not bind this run (three PRs had no `Reviewed SHA` at all, so
  Condition 1 failed loudly and correctly), but the defect is unchanged. Blocked on
  `.claude/**`.

- **[high] `A-SUMMARY-STALE-FALLBACK` is still open** (`orchestrator.yml:334`).
  Sixth filing. Needs `.github/workflows/**`.

- **[info] `.claude/**` is refused by the Write/Edit tools, not by the filesystem.**
  Re-confirmed framing from 08-15 run 2: the shell can write it; the tool guard
  refuses. I did not route around it — that guard is a product safety boundary, and
  finding a gap in it is not authorization to edit my own agent definition mid-run.
  Six deferred fixes are one `sed` away from any local session.

- **[info] EODHD absent in GHA — 18th week.** `dev/notes/data-gaps.md` untouched
  since #814 (2026-05-03); ~12 data-gated track rows unchanged.

## Questions for You

1. **The `ops(budget)` bypass now has a named victim — apply the one-line fix?**
   `feat/split-safe-inert-report-row` put 419 lines on main with no PR and no
   review. This has been filed nine times; it is the only escalation that has put
   un-reviewed production code on `main`, and it is a one-line change to
   `orchestrator.yml:482` that I cannot make.
2. **Two workflow-level defects now strand orchestrator output.** The
   `/tmp/ops-summary` collision (this run's diagnosis) and
   `A-SUMMARY-STALE-FALLBACK`. Both need a `workflow`-scoped PAT. Is that PAT
   obtainable, or should these be closed as won't-fix so runs stop re-filing them?
3. **Should the capacity rule become per-machine?** The measured answer for this
   runner is *2 build-heavy, unlimited shell-only*. I can carry it by hand in
   briefs indefinitely, but it belongs in
   `.claude/rules/container-capacity-scheduling.md`.
4. **Test-file length limits — still needs your decision**, unchanged and still
   blocking the top `cleanup.md` item. Measured: **401** `*/test/*.ml` files, **154**
   over 300 lines, **70** over 500, largest `test_screener.ml` at **2687**.
5. **Was merging #2349 and #2348 yourself the workflow you want?** Both merges
   happened 2–3 minutes after the second gate landed, which suggests you were
   watching. If you would rather the orchestrator auto-merged on full-green, Step
   6.5 already does that and `auto_merge_enabled` is on — say the word and I will
   stop leaving them for you.

## What the next run should know

1. **Two PRs open, both with a live NEEDS_REWORK and a precise required fix.**
   **#2339** at `b3c65c07` — rework **1 of 2 used**, one iteration left; the fix is
   test-and-comment only (a fixture that omits `recorded_at_ns`, a fire-direction
   fixture, and correcting the "only legacy records" reachability claim). **#2352**
   at `6a108b42` — **0 of 2 used**; four findings the reviewer judged clearable in a
   single iteration, B1 most likely a docstring correction. Both structurals are
   APPROVED at those tips, so a rework only needs structural re-run if it touches code.
2. **`H-AUDIT-SHA-FILE-LEAK` is now the cheapest real harness item.** Filed this run
   with its measurement (`FOREIGNSHA99` vs `""`), pre-existing on main, deliberately
   out of #2338's scope. Gating `:497-498` on `FILE_MODE` is the one-line fix. Note
   the reviewer's nit that the `~:497-498` line reference in the shipped comment is
   already stale (the extractor sits at `:506-507` after iteration 2's +9 comment
   lines) and disagrees with the status file's `:493-499` — fix the references in
   the same PR.
3. **Split dispatches into build-heavy and shell-only before choosing a batch
   size.** Two cold OCaml builds is this runner's ceiling; shell-only agents cost
   nothing. That single distinction was the difference between this run's 16
   dispatches and the previous run's 9.
4. **When a comment asserts a property over a set of call sites, enumerate the
   set.** Three PRs this week were rejected for a comment claiming more than the
   code delivered, and both times enumeration found the exception in one `grep` —
   cheaper than the argument it replaces.
5. **Diff against the merge-base, not against `main`.** A QC agent read a stale base
   as an intentional schema change this run. Same family as the `git ls-remote` vs
   PR-record trap below.
6. **`git merge-base --is-ancestor` cannot tell you whether a branch is merged
   here** — this repo squash-merges, so it reports "unmerged" for everything. I got
   18/18 wrong before switching to `GET /pulls?state=all`, which is one call and is
   correct. Same lesson the 08-15 run recorded for a different question.
7. **Leave the main checkout on `main` and clean at exit.** It makes the
   `ops/budget-*` fallback branch harmless if it fires, and it keeps the daily
   branch checked out nowhere so the bundling step's fixed-path worktree can
   succeed.
