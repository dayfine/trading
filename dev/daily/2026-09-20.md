# Status — 2026-09-20

**Run ID:** 2026-09-20-run1
**Run timestamp:** 2026-09-20T16:12Z (GHA orchestrator run 35521718664)
**Mode:** FULL
**Main at run start:** `883ef4b9` — green (last CI on `d59ad4be`: `build-and-test`,
`perf-tier1-smoke`, `golden-sp500-5y`, `golden-custom-universe` all SUCCESS)

> **Written early, mid-run, and updated in place** — deliberately. See §The finding.
> An artifact that exists and is stale beats one that never existed.

---

## The finding: the "no summary written" defect fired again today, third instance

Run **`35510571160`** (12:25Z today) exited with the agent step
(`Run lead-orchestrator`) **green**, burned **$10.72**, and produced **no
`dev/daily/2026-09-20*.md` at all**. The job then failed at *Locate daily summary
+ detect no-op* — the mechanical backstop doing exactly its job, at the cost of
a whole dispatch slot.

Step-level evidence from `GET /actions/runs/35510571160/jobs`:

| # | step | conclusion |
|---|------|-----------|
| 8 | Run lead-orchestrator | **success** |
| 9 | Capture run cost from execution file | success |
| 10 | Locate daily summary + detect no-op | **failure** |
| 11 | Bundle budget into daily summary and auto-merge | success |
| 12 | Fail on escalations | skipped |

This is the **third** instance of the repo's own named open defect class — *a
green run is indistinguishable from a productive one* (#2741, #2747, #2771,
#2803, #2810) — applied to the orchestrator itself:

- 09-16 run `35096884441` — success, 56 turns, **$12.68**, no summary.
- 09-20 run `35510571160` — success, **$10.72**, no summary.
- (this run, mitigated by writing the file first)

The step-11 detail is worth noting: *Bundle budget into daily summary and
auto-merge* reported **success** on a run with no summary to bundle into. It
opened standalone PR **#2873** (`ops/budget-2026-09-20-35510571160`, $10.7182)
instead — which is why that PR is sitting open as the only entry in the queue.
A step whose name says "bundle into daily summary" succeeding when there is no
daily summary is the same defect class one level down.

**Mitigation adopted this run, not a fix:** this file was written to disk before
any agent returned, then updated in place. The real fix is upstream in
`.claude/agents/lead-orchestrator.md` (write the summary skeleton at Step 1, not
Step 7) — and `.claude/agents/**` writes remain **refused** in this runtime
(19th consecutive run). Escalated below.

---

## Step 0.5 — skipped on the first-run-of-day escape hatch

Prior summary is `dev/daily/2026-09-16.md`, **4 days old** (> 24 h), so the
documented escape hatch fires and the fast-exit is skipped without evaluating
the four conditions. Independently, the queue would not have supported a no-op
anyway: `QUEUE_NON_EMPTY=1`, and that one PR is #2873, an ops-budget
housekeeping PR carrying no track work — i.e. the dispatch queue is effectively
**empty**, which is maximum capacity, the opposite of saturation.

The 09-17 → 09-19 gap (six consecutive cron slots at exactly `$0.0000`) is
consistent with the account-level weekly rate limit that
`dev/notes/next-session-priorities-2026-09-20.md` records for the local session
("rate-limit gap 09-16 19:35 → 09-20 00:00"), not with a repo fault. Today's
weekly track pacer reaches the same conclusion and correctly labels it a lead
rather than a verdict.

---

## Step 1b — drift cross-reference

Prior summary (09-16) listed `#2851`, `#2852`, `#2853` as harness PRs. All three
**merged on 09-16**. No `[drift]` warnings: no track was recorded as dispatched
with an un-updated status file.

Carried item resolved between runs: the 09-16 run's open question about
qc-structural on #2851 ("no verdict within the run window") is moot — the PR
merged.

## Step 1c — carried-forward `[critical]` verification

The 09-16 summary carried no `[critical]` escalations. Nothing to re-verify.
`[info]` Carried-forward verification: no still-real `[critical]` items.

---

## Dispatched this run

Cap set **mechanically**, not by reasoning:

```
$ sh dev/scripts/dispatch_disk_guard.sh 2
OK: dispatch_disk_guard -- 59GB free meets the 52GB floor for 2 agent(s); dispatch fits   (exit 0)
$ sh dev/scripts/dispatch_disk_guard.sh 3
REFUSE: dispatch_disk_guard -- 59GB free is below the 68GB floor for 3 agent(s)           (exit 1)
```

Each agent got its **own `git worktree`** (`/__w/trading/wt-postrunval`,
`/__w/trading/wt-audit27b`) created before dispatch, per the #2470 rule — never
a bare `git checkout -b` in the shared checkout. Both briefs carry an explicit
**one-dune-in-flight** guard (`flock /tmp/dune.lock`, gated on the
orchestrator's own build gate finishing) because this runner is memory-bound at
15 GB and the OOM is silent.

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | harness-maintainer | **PR #2874** | Both scenario-27b residuals from #2231 closed, mutation-verified. CI green. |
| post-run-validation | feat-backtest | **PR #2875** | `Series_level` wired into `Build_runner`, report-only, default-off. |
| harness | qc-structural | APPROVED (5) | @ `11f79ab6` |
| harness | qc-behavioral | APPROVED (5) | @ `11f79ab6`; 2 non-blocking residuals filed |
| harness | — | **merged** | `960bebc5`, Step 6.5 auto-merge |
| post-run-validation | qc-structural | APPROVED (4) | @ `1b6d9031` |
| post-run-validation | qc-behavioral | **NEEDS_REWORK** (2) | @ `1b6d9031`; CP1 + CP3 unpinned |
| post-run-validation | feat-backtest | rework iter 1 | → `e5db6988` |
| post-run-validation | qc-structural | APPROVED (5) | re-review @ `e5db6988` |
| post-run-validation | qc-behavioral | APPROVED (4) | re-review @ `e5db6988`; 3 residuals filed |
| post-run-validation | — | **merged** | `f1549820`, after `update-branch` → `bba9d56b` |
| cleanup | — | skipped | §Backlog has no dispatchable work (re-audited 09-16 and 09-09). |
| ops-data | — | skipped | 5th consecutive run. `EODHD_API_KEY` unset on this runner. |
| feature tracks (other) | — | none eligible | Every other IN_PROGRESS row is LOCAL-fenced, data-gated, or human-gated. |

### #2874 — harness: two test-hygiene residuals, closed with mutation evidence

`H-AUDIT-27B-CUMULATIVE-COUNT` and `H-AUDIT-27B-LEAKS-UNDER-PWD-REGRESSION`
(both filed by qc-behavioral on #2231). 2 files, +43/−9, three commits.

The brief demanded mutation evidence rather than "the suite still passes" —
which proves nothing on a test-hygiene fix, since it passed before the work
started. What came back:

- **Item 1** — 27b now snapshots the `WALKUP_ROOT/dev/audit` count immediately
  before its own call and asserts a **delta of 1**. Under a simulated future
  scenario writing into the shared fixture first, the old cumulative form
  **mis-FAILs** (`got walkup_count=2`) while the delta form correctly passes
  (`before=1, after=2, delta=1`).
- **Item 2** — 27b's invocation now runs in an empty scratch cwd, inside the
  `$(...)` subshell. Under probe K4 (walk-up resolving from `$PWD`, mutated on a
  `/tmp` **copy**, never the real script): before, `rc=0` and the live
  `dev/audit/` went 195 → **196** (the leak); after, `rc=1` +
  `FAIL: could not locate repo root`, delta 0, `dev/audit/` still 195.

The second half is the one that mattered: **the fix had to keep 27b red under
K4**, not merely stop the leak. A change that stops the leak by stopping the
detection would be a regression wearing a fix's clothes. It stayed red.

Counts unchanged either side (76 assertions / 76 labels / 63 `# Scenario`
headers / 64 distinct numbers; `76 passed, 0 failed`). The agent also refreshed
`harness.md`'s `## Last updated:` — today's track pacer had flagged it as **30
days stale across 46 commits**, the most-actively-edited status file in the
program.

### #2875 — post-run-validation: arming a pure module without touching it

`Series_level` existed and was tested; nothing called it. Now `Build_runner`
classifies each symbol's **stored** series (post splice-cut, post tail-rule)
behind `-detect-series-level`, writing a `series_level.csv` sidecar. Zero lines
of `series_level.{ml,mli}` changed.

Two things in the agent's own report are worth keeping:

1. **It departed from the named precedent, and said why.** The brief told it to
   mirror `-detect-splices`. It mirrored that *contract* (default-off switch,
   CSV named for the pass, report-only) but took `Twin_pass`' *module boundary*
   instead — a new `Level_pass` module holding the two impure halves (the
   `Core.Command` flag block, the sidecar write) — because this pass runs inside
   `Build_runner.build` beside `Series_tail`, so both builders must share one
   `params`, unlike the splice pre-pass that lives in a single CLI shell. It put
   the reasoning in the PR body and the module docstring so it reads as a
   decision rather than drift. Following a precedent's shape where the shape
   does not fit would have been the worse outcome.
2. **It shipped a wrong number and corrected it unprompted.** It first claimed
   the wiring added ~20 lines to `build_runner.ml`; measured, it is **+42 (729 →
   771)**, and it fixed the figure in both the status file and the PR body in a
   follow-up commit — noting that nothing was *gated* on the claim, since
   `linter_file_length.sh` covers `*/lib/*.ml` only. An ungated wrong number is
   exactly the kind that survives, so self-correcting it is the useful part.

It also reports that the **full** `dune runtest` caught two violations a scoped
`dune runtest <dir>` had missed (`build` at 52 lines; 4 over-nested test cases),
both fixed by refactoring — no limit bumped, no `@large-module` marker added,
per `code-health-discipline.md`. qc-structural is verifying that claim.

### The stall I caused, and the correction

Both briefs told the agents to gate every `dune` call on
`until grep -q DONE /tmp/buildgate.log; do sleep 20; done`. That was my error:
**a subagent never receives the event that would end such a wait.** The
harness-maintainer hit it, correctly declined to busy-loop, and returned early
having "stopped to wait for the background notification" — the repo's known
`feedback_agents_background_wait_stall` failure mode, induced by the dispatcher
rather than found in the wild.

Recovered by `SendMessage` to both agents (context intact, no re-dispatch, no
work lost): drop the log wait, keep the `flock /tmp/dune.lock` serialization,
run every dune call in the **foreground** with `timeout: 600000`. The
harness-maintainer then noted — correctly — that **my correction was itself
already stale**: by the time it arrived, it had pushed past the SHA I quoted and
had already opened #2874 on its own. Both facts are recorded because the second
one is the check on the first: an agent that reports the dispatcher's own
message as out of date is doing the job right.

The intent behind the instruction was sound (one dune in flight on a 15 GB
memory-bound runner, where OOM is silent). `flock` alone achieves it; the log
wait added nothing but a trap.

## QC Status

| Track | PR | tip | CI | structural | behavioral | outcome |
|---|---|---|---|---|---|---|
| harness | #2874 | `11f79ab6` | pass | **APPROVED** (5) | **APPROVED** (5) | **auto-merged** `960bebc5` |
| post-run-validation | #2875 | `1b6d9031` | pass | **APPROVED** (4) | **NEEDS_REWORK** (2) | reworked → `e5db6988` |
| post-run-validation | #2875 | `e5db6988` | pass | **APPROVED** (5) | **APPROVED** (4) | **auto-merged** `f1549820` |

Both PRs merged this run. The dev→review→rework→re-review→merge loop closed
**inside a single run** for #2875 — which is what Step 5a's intra-run rework
exists for: waiting a scheduler interval on two mechanical contract findings
would have cost a full day.

Read back independently by the gate script rather than from my own notes:

```
$ sh dev/scripts/pr_gate_status.sh 2874 2875
PR     CI       STRUCT   BEHAV    CODEX   NEXT-ACTION
2874   pass     ok       ok       none    MERGE
2875   pass     ok       rework   none    rework (behavioral findings)
```

Both structural reviews posted to their PRs as `COMMENTED` reviews pinned to the
full 40-char tip SHA (self-approval is blocked when reviewer and author share an
account — the verdict lives in the body, which is what `pr_gate_status.sh`
reads). Checklists carried to `dev/reviews/harness.md` and
`dev/reviews/post-run-validation.md`, each with `Reviewed SHA:` as line 1.

**#2874 structural — APPROVED, quality 5.** All three gates exit 0 (76
scenarios, 76 passed). Verified independently of the author's report: the delta
snapshot is taken **before** the call (line 2007) and computed after (2016–17),
so it is immune by construction to a future scenario writing into
`WALKUP_ROOT`; the scratch `cd` is confined to the `$(...)` substitution (2010)
and cannot leak to the outer script; and **detection is preserved** — 27b still
hard-errors under a `$PWD`-based walk-up. `write_audit.sh` and
`record_qc_audit.sh` have zero diff.

**#2875 structural — APPROVED, quality 4.** All three gates exit 0; CI
subsequently went green on all three checks (the reviewer re-checked
`build-and-test` as instructed rather than relying on the dispatch-time
snapshot). R1 verified via the armed-vs-unarmed `payload_md5` equality test; R2
verified — `level_config` is a real `Build_runner.build` parameter defaulting to
`Series_level.Config.default` (`enabled=false`), routed through `Level_pass`,
with all three flags exposed. A2 parsed from the `(libraries ...)` s-expressions
rather than grepped (the `scripts.level_pass` addition lands in
`backtest/snapshot_warehouse/dune`, and `backtest/**` may consume Tier 2). The
author's self-corrected +42 (729 → 771) figure checks out.

### #2874 behavioral — APPROVED, quality 5, and it re-derived rather than trusted

The reviewer re-ran the K4 probe itself instead of accepting the author's
numbers, on a `/tmp` copy of `write_audit.sh`:

```
PRE-fix:   FAIL: scenario 27b — ... got rc=0, walkup_count=0
           live dev/audit/ 195 → 198, incl. 27b's own record
POST-fix:  FAIL: scenario 27b — ... (before=0, after=0, delta=0); got rc=1
           + FAIL: could not locate repo root
           live dev/audit/ 195 → 197, 27b's record ABSENT
```

So the fix **kept the red** — the detection now fires as a hard `rc=1` rather
than as a wrong-placement count — and the leak is gone. The specific failure
this PR could have had (removing the leak by removing the detection) is not
present.

It filed two non-blocking residuals, both `LINTER_CANDIDATE`:

- **R2874-A** — the `$PWD`-leak *class* is closed for 27b but **still open for
  28b (`:2153`) and 30c (`:2247`)**, which is what the residual 195 → 197
  accounts for. The PR body had already disclosed this; the reviewer confirmed
  and narrowed it, and corrected one detail — the body called them "separate
  `H-AUDIT-*` residuals", but no such residual existed until this entry.
- **R2874-B** — 27b's two guard claims are pinned by PR-body prose, not by a
  committed test, even though this same file already carries a committed
  mutation harness (19 mutants, 14 killed) as precedent.

The reviewer scored 5 rather than 4 specifically because the PR body volunteered
the limits of its own result — disclosure is what let the review *confirm* the
sibling leaks rather than *discover* them.

### #2875 behavioral — NEEDS_REWORK, quality 2, two unpinned contracts

Both findings are of the same shape: an `.mli` docstring claims something the
suite does not pin. **The code may well be right — that is not the point.** An
unpinned contract regresses silently.

**Finding 1 (CP1) — the "report-only" identity assertion is not whole-value.**
The `.snap`-payload half is sound; the manifest half is not. The reviewer
mutated the pass to touch **a manifest entry only** —
`active_through = if Option.is_some level then None else …`, silently clearing
the **delisting marker** for every flagged symbol:

```
Ran: 9 tests in: 0.15 seconds.
EXIT=0          ← green
```

Cause: `_payload_md5s` projects each entry to `(symbol, payload_md5)`, dropping
`path` / `byte_size` / `csv_mtime` / `active_through` — while `level_pass.mli:20`
claims bit-identity "byte for byte in every `.snap` **and in the manifest**" and
`build_runner.mli:240` claims "**no manifest entry changed**." A ~3-line fix:
`file_metadata` already derives `equal`, so compare whole records with `path`
normalised to its basename.

**Finding 2 (CP3) — the ordering claim is unpinned.** Hoisting `_classify_level`
above **both** `_cut_splice` and `_clean_tail` leaves the entire suite green
(exit 0): the fixtures are built so neither rule edits them and `splice_cuts` is
never passed, so no case can distinguish the orderings. Classifying the *pre*-cut
series would report on bars that never reach the warehouse — a wrong report that
looks like a right one.

What the reviewer **verified**, so the rework leaves it alone: the empty-report
case (header alone, not a missing file); both tuning knobs live; and the flag
genuinely reaching the detector — all three sub-mutations reproduced, and
deleting the `level_config` read **fails to compile** (warning 69
`unused-field`), which the reviewer rightly called stronger pinning than a test.
That last point matters in context: the two most recent mechanism PRs on
adjacent tracks (#2862, #2863) each took a rework for exactly the wiring being
unpinned. Here the wiring is pinned; the gaps are elsewhere. Progress, not a
repeat.

### The rework (iteration 1) — and the reviewer's mutation that was inert

Test-only, 9 → 12 cases, no change to `series_level.{ml,mli}` or
`build_runner.{ml,mli}`. Both findings addressed with before/after mutation
output:

| mutation | pre-rework | reworked |
|---|---|---|
| manifest-only harm (clears the delisting marker) | 9 tests **OK, exit 0** | `arming the level pass changed a manifest entry` — **exit 1** |
| hoist `_classify_level` above `_cut_splice` **and** `_clean_tail` | 9 tests **OK, exit 0** | 2 failures — **exit 1** |

Finding 1 was fixed by comparing **whole `file_metadata` records** (derived
`equal`, `path` normalised to basename, sorted by derived `compare`) instead of
the `(symbol, payload_md5)` projection. Finding 2 by plumbing `?splice_cuts`
into the test helpers and adding `SPLICED` (with an **uncut control arm**, so
"no row" cannot pass vacuously) and `STRAY` (asserts the exact
`whole_window,21` row that only the post-tail-rule series produces).

**The part worth keeping** is what the author found while doing it. The
reviewer's *literal* Finding-1 mutation site is an **equivalent mutant** — it is
green even against the reworked suite. Mutating `active_through` at
`_build_one_symbol:288` only touches `_active_through_of_bars`, which reads the
**bar-level** `Daily_price.active_through`, and no CSV/EODHD path populates that
field (`build_runner.ml:168-172` says so outright), so it is `None` on every CSV
fixture with or without the mutation. The marker that actually reaches the
manifest is *derived* later, in `_entry_with_derived_marker`.

And the blindness ran deeper than the projection: **every fixture ran to the
universe's end and already carried `active_through = None`**, so no mutation of
that field could have reddened anything regardless of what the test compared.
Fixing only the projection would have produced a test that looks whole-value and
still cannot see the harm. The author added a `GONE` fixture (21 bars, ending 9
days before the others, past the default `survivor_tolerance_days` of 7) so one
*flagged* symbol carries a real marker.

So the reviewer's *conclusion* was right — the projection dropped
`active_through` and the harm is real — while the *demonstration* was inert, and
the author said so plainly rather than quietly pinning the site they were
pointed at. That is the second time this run an agent corrected the instruction
it was given (the first being harness-maintainer on my own stale
`SendMessage`), and both corrections were load-bearing.

One thing the author correctly could **not** do from the runner: the PR body's
"Precedent followed" section still carried the superseded "~20 lines" figure
(`gh` is unusable, and the text exists only in the PR body, not in the repo). I
patched it via REST — safe here because the SHA had already moved and no review
existed at the new tip, so this created no second verdict at one SHA
(`H-GATE-TWO-VERDICTS-ONE-SHA`).

### One residual the structural pass surfaced

`build_runner.ml` is now **771 lines, over the repo's 500-line hard limit — and
nothing gates it.** `linter_file_length.sh` scopes to `*/lib/*.ml`, and this
file lives under `trading/analysis/scripts/build_snapshots/`. No limit was
bumped and no `@large-module` marker was added, because none was needed: the
file is simply outside the linter's field of view, and was already at 729 on
`main` before this PR.

That is a **coverage gap, not this PR's defect** — and it is the more
interesting half. Asked to classify rather than fix it, qc-behavioral measured
the whole class: **nine `.ml` files outside `*/lib/*` exceed the 500-line hard
limit unobserved**, two of them at more than double it —

| file | lines | | file | lines |
|---|---:|---|---|---:|
| `release_report.ml` | 1005 | | `scenario_runner.ml` | 675 |
| `trade_audit_ratings.ml` | 1001 | | `sexp_default_drift_linter.ml` | 674 |
| `build_runner.ml` | 771 | | `selection_trace.ml` | 662 |
| `trade_audit_report.ml` | 691 | | `shiller_weinstein_decades.ml` | 626 |
| | | | `backtest_runner.ml` | 522 |

No limit was bumped and no `@large-module` marker was added on any of them,
because none was ever required — **the linter simply never looked.** That is the
failure mode worth naming: a guard reporting green on a set it does not cover is
indistinguishable from the set being clean. It is the same shape as this run's
headline finding one level down, and `linter_file_length.sh`'s own header
already records a prior scope-widening episode (2026-08-14), so this is the
second time the pattern proved too narrow.

**Filed as issue #2876** with the measured list and the fix shape — widen the
`find` to `*/bin/*.ml` + `*/scripts/*/*.ml`, then decide per file between
extraction, a justified marker, and a dated exception. Both halves belong
together: widening alone just relocates the problem into a red CI.

Neither PR is flipped out of draft yet. Per `pr-merge-gates.md` both need CI +
qc-structural + qc-behavioral APPROVED **at the current tip**; neither is
docs-only (each changes a `.sh` or `.ml`), so the docs-only carve-out does not
apply.

## Health Scan

Full report: `dev/health/2026-09-20-fast.md`. **Result: CLEAN. Critical items:
none.**

```
BUILD_EXIT=0
RUNTEST_EXIT=1   ← see below; NOT a main-baseline failure
FAIL: index_size_linter — table rows exceed 250 chars in dev/status/_index.md
```

Exactly one `^FAIL:` line, and it was **this run's own Step 5.5 reconcile** —
the `screener` row I had just rewritten came to 281 chars against a 250 cap. The
row did not exist on `main` until this run created it. Shortened, then
re-verified by running the offending check alone rather than re-paying a full
`dune runtest` while agents held the runner:

```
OK: index_size_linter — dev/status/_index.md within limits (15657/20480 bytes, all rows <= 250)   exit 0
OK: all dev/status/*.md files have required fields.                                               exit 0
```

Two notes for whoever reads this next:

- The linter **caught the orchestrator's own edit before it reached a PR**,
  which is the cheapest possible moment. It exists because reconcile cells
  accrete run history; this is the first run that gave it something to fire on.
- `RUNTEST_EXIT=1` here **must not be read as "main is red."** Main's evidence is
  independent — last CI on `d59ad4be` passed all four checks. Reporting a
  working-tree failure as a baseline failure is precisely the stale-`[critical]`
  cascade Step 1c exists to prevent, so it is not escalated as one.

Status-file integrity: PASSING (exit 0).

## Scheduled workflows

FINDINGS (exit 1): RED/STALE workflows and newest_completed_run_id below.

    scheduled_workflow_health: repo=dayfine/trading stale_hours=216 runs_per_workflow=10
    NO-SCHEDULE	CI Main Watchdog	(no scheduled runs observed)
    NO-SCHEDULE	CI	(no scheduled runs observed)
    OK	Dependency freshness check	run_id=34857850208 status=completed conclusion=success created_at=2026-09-14T14:46:50Z age_hours=145 streak=0 in_progress=0 newest_completed_run_id=34857850208
    NO-SCHEDULE	golden-runs-custom-universe	(no scheduled runs observed)
    OK	golden-runs-sp500-15y	run_id=35513485696 status=completed conclusion=success created_at=2026-09-20T13:26:02Z age_hours=2 streak=0 in_progress=0 newest_completed_run_id=35513485696
    NO-SCHEDULE	golden-runs-sp500-5y	(no scheduled runs observed)
    NO-SCHEDULE	goldens-affected	(no scheduled runs observed)
    OK	Weekly deep health scan	run_id=34889371218 status=completed conclusion=success created_at=2026-09-14T19:50:58Z age_hours=140 streak=0 in_progress=0 newest_completed_run_id=34889371218
    OK	Build CI image	run_id=34843421567 status=completed conclusion=success created_at=2026-09-14T12:26:19Z age_hours=147 streak=0 in_progress=0 newest_completed_run_id=34843421567
    NO-SCHEDULE	IWV Scrape (one-shot)	(no scheduled runs observed)
    RED	Daily orchestrator	run_id=35521718664 status=in_progress conclusion=null created_at=2026-09-20T16:07:34Z age_hours=n/a streak=>=9 in_progress=1 newest_completed_run_id=35510571160
    OK	perf-nightly	run_id=35502558041 status=completed conclusion=success created_at=2026-09-20T09:31:44Z age_hours=6 streak=0 in_progress=0 newest_completed_run_id=35502558041
    NO-SCHEDULE	perf-tier1	(no scheduled runs observed)
    OK	perf-weekly	run_id=34849086820 status=completed conclusion=success created_at=2026-09-14T13:24:52Z age_hours=146 streak=0 in_progress=0 newest_completed_run_id=34849086820
    RED	Prune candidates weekly	run_id=34892029810 status=completed conclusion=failure created_at=2026-09-14T20:17:37Z age_hours=n/a streak=4 in_progress=0 newest_completed_run_id=34892029810
    OK	Weekly track pacer	run_id=35506154608 status=completed conclusion=success created_at=2026-09-20T10:49:59Z age_hours=5 streak=0 in_progress=0 newest_completed_run_id=35506154608
    RED	Weekly start sweep (BAH SPY)	run_id=34884001539 status=completed conclusion=failure created_at=2026-09-14T18:57:58Z age_hours=n/a streak=>=10 in_progress=0 newest_completed_run_id=34884001539
    SUMMARY: active=17 (1 page(s) fetched, full pagination -- a real total, not a floor) ok=7 red=3 stale=0 no-schedule=7 unobservable=0 -- red/stale/ok/unobservable are each computed from up to 10 most-recent scheduled runs per workflow (not just the newest); red includes a per-workflow failure-streak count (see each RED line's streak=N, or streak=>=N when the fetched page was exhausted before the streak closed)
    SUMMARY: RED workflows: Daily orchestrator(streak=>=9), Prune candidates weekly(streak=4), Weekly start sweep (BAH SPY)(streak=>=10)

### Reading the three REDs — they are three different things

The check reports one status per workflow; the causes are not comparable, so
they are separated here rather than escalated as one alarm.

**1. `Daily orchestrator`, streak >= 9 — a composite, not one fault.** Every
scheduled run from 09-16 12:37Z to 09-20 12:25Z is `failure`, but from at least
three distinct causes:

| run | date | cause |
|---|---|---|
| `35096884441` | 09-16 12:37Z | agent step green, **no summary written**; gate failed (and validated a 2-day-old file, #2850) |
| `35125114953` | 09-16 16:57Z | **step 8 `Run lead-orchestrator` itself failed** — even though this run's summary was written and merged as #2854 |
| `35222092194` … `35453227622` | 09-17 → 09-19 (6 runs) | `$0.0000` — no model call; account-level weekly rate limit (see §Step 0.5) |
| `35510571160` | 09-20 12:25Z | agent step green, **no summary written**; gate failed |

Worth stating plainly: **run `35125114953` is the one the 09-16 summary
describes as its own successful FULL run.** The summary landed, the index
reconcile landed, and the *job* still went red at the agent step. A run can
therefore be productive and red at once — which means the red streak is not by
itself evidence that nothing shipped, and the `$0.0000` runs are not
interchangeable with the no-summary ones.

**2. `Prune candidates weekly`, streak 4 — known, filed, unchanged.** Fails at
`Push branch and open PR`. Confirmed at source this run:
`prune-candidates-weekly.yml` still calls **`gh pr create`**, and `gh` is absent
from this image by design (exit 127). This is **issue #2847**, already open — no
duplicate filed. The fix pattern is already proven in the sibling workflow
(#2842 moved `weekly-start-sweep.yml` to REST). No new scheduled run has fired
since 09-14, so the streak is unchanged at 4, not worsening. The analysis itself
is not being lost — the report is produced; only the PR fails (a report landed
manually as #2859 on 09-16).

**3. `Weekly start sweep (BAH SPY)`, streak >= 10 — the streak is stale, not
current.** Same step, same `gh pr create` cause, going back to at least
2026-07-13 (every scheduled run, 10 weeks). **But #2842 merged 09-16 and the
newest failure is 09-14** — so every datapoint in this streak *predates the
fix*. The next cron (**2026-09-21**) is the first real test. Reporting this as a
live regression would be wrong; reporting it as green would also be wrong. It is
untested.

Both (2) and (3) match the `_index.md` header's standing note verbatim, which
this run re-measured rather than copied forward — it is accurate.

## Budget

- Cap: **$200** (`max_daily_cost_usd`). Target band **60–95%**.
- Subagent sessions: **9** — 2 implementation, 3 qc-structural, 3
  qc-behavioral, 1 rework. (7 distinct agents; #2875 took a full second QC pair
  after its rework.)

**Measured** (subagent token totals, reported per agent — these are real counts,
not estimates):

| agent | tokens | tool calls | wall |
|---|---:|---:|---:|
| feat-backtest (#2875) | 266,138 | 114 | 37 m |
| harness-maintainer (#2874) | 172,942 | 61 | 25 m |
| qc-behavioral (#2875) | 161,698 | 50 | 8 m |
| qc-behavioral (#2874) | 138,039 | 34 | 8 m |
| qc-structural (#2875) | 115,884 | 50 | 25 m |
| qc-structural (#2874) | 93,722 | 23 | 27 m |
| feat-backtest rework (#2875) | 201,661 | 77 | 13 m |
| qc-behavioral re-review (#2875) | 161,577 | 46 | 6 m |
| qc-structural re-review (#2875) | 103,605 | 36 | 8 m |
| **total, 9 agent sessions** | **~1.41 M** | **491** | |

**Cost: not stated as a measured figure here.** The authoritative number is
written *after* this session ends, by the workflow's cost-capture step into
`dev/budget/2026-09-20-35521718664.json` — I cannot read it from inside the run,
and inventing a dollar figure from the token counts above would be exactly the
kind of unlabelled estimate this summary spends its first section complaining
about. For scale only: the two failed runs today and on 09-16 cost **$10.72**
and **$12.68** for 56-turn sessions that dispatched nothing; this run dispatched
seven agents and merged a PR, so expect meaningfully more.

Note the standing caveat in `merge-policy.json`: auth is a flat-rate Max-20x
subscription, so `max_daily_cost_usd` is a **runaway-dispatch backstop**, not a
bill. The real constraints today were not cost:

- **Disk**, which bound hardest. `dispatch_disk_guard.sh` was called before
  every dispatch decision and **refused 3 agents** at the outset (59 GB free vs
  a 68 GB floor) and later **refused even 2** at 45 GB — at which point QC
  waited rather than proceeding. Each worktree is ~16 GB; free space tracked
  61 → 29 → 46 GB as worktrees were created and reclaimed **individually, the
  moment each agent finished**, never batched.
- **Memory**, at 15 GB with a silent OOM. Handled by `flock /tmp/dune.lock`
  rather than by hoping.

Utilization assessment: **UNDER_TARGET on cost, at ceiling on disk.** These are
not in conflict — the run was resource-bound, not budget-bound, and no eligible
track was skipped for cost. The fill-loop had nothing to add: at no point was
there a third eligible dispatch that the disk guard would have permitted.

## Escalations

**`[info]` Step 1c carried-forward verification: no still-real `[critical]`
items.** The 09-16 summary carried none; main is green (`dune build` exit 0, and
CI green on `d59ad4be`). Recorded explicitly so a later reader can tell "checked,
nothing wrong" from "not checked".

**`[high]` The orchestrator's own summary-writing step failed again today, for
the third time.** Run `35510571160` (12:25Z) exited the agent step green at
$10.72 and wrote no summary; the verify gate failed the job, as designed. Same
shape as 09-16's `35096884441` ($12.68). The mechanical backstop is working —
**the thing it backstops is not.** The real fix is upstream in
`.claude/agents/lead-orchestrator.md`: write the summary skeleton at Step 1
rather than Step 7, which is what this run did by hand. **`.claude/agents/**`
direct writes remain refused in this runtime (19th consecutive run)**, so the
orchestrator still cannot fix its own definition. That refusal is now the single
highest-leverage blocked capability in the program: the defect is understood, the
fix is one paragraph, and no agent can apply it. **A human edit to that file
would retire a defect class that has now cost four dispatch slots.**

**`[info]` Two weekly workflows remain RED, for one shared cause, with different
urgency.** Both die at `Push branch and open PR` on `gh pr create` (exit 127;
`gh` is absent from the image by design):

- **`prune-candidates-weekly`** — streak 4, confirmed at source this run
  (`prune-candidates-weekly.yml` still calls `gh pr create`). Already **issue
  #2847**; no duplicate filed. The fix pattern is proven next door in #2842.
  The analysis is not lost — only the PR; a report landed by hand as #2859.
- **`weekly-start-sweep`** — streak >= 10, but **every datapoint predates its
  fix** (#2842 merged 09-16; newest failure 09-14). The **2026-09-21 cron is
  the first real test.** Do not read this one as a live regression.

Both are blocked on the same missing capability: `workflow` scope is
unavailable by push, by contents API, and by workflow-dispatch (403 on all
three routes, with a 201 control).

**`[info]` Filed issue #2876** — `linter_file_length.sh` scopes to `*/lib/*.ml`,
leaving **nine `.ml` files over the 500-line hard limit unobserved** (two at
>2×). Surfaced by QC on #2875; measured by qc-behavioral. Not that PR's defect.

**`[info]` `post-run-validation` #2875 is at rework iteration 1 of 2.** If the
rework does not clear both findings within this run, the PR stays draft and the
track carries into the next run with the findings as pre-flight context. This is
the *first* rework on this track, so the 3-consecutive-run design-problem trigger
is not near.

**`[info]` Dispatcher self-correction, recorded so it is not repeated.** I put a
`until grep -q DONE /tmp/buildgate.log` wait into both initial briefs. A subagent
cannot receive the event that ends such a wait; one agent stalled on it. `flock`
alone achieves the intended serialization. **Every brief after that point used
`flock` + explicit "run dune in the FOREGROUND with `timeout: 600000`", and no
agent stalled again.** The four later dispatches confirm the corrected form
works.

## Integration Queue

- **#2874 (harness)** — **MERGED** `960bebc5` this run, via Step 6.5 auto-merge:
  Rule 0 hold check CLEAR (no `do-not-merge` label, no `convert_to_draft` after
  first review), CI re-verified at the tip immediately before acting, draft
  flipped via the GraphQL `markPullRequestReadyForReview` mutation
  (`isDraft: false` confirmed), hold **re-checked a second time** immediately
  before the merge PUT, `mergeable_state: clean`, squash-merged.
- **#2875 (post-run-validation)** — **MERGED** `f1549820`, after one rework and
  a full re-review at the new tip. Same Rule 0 double-hold-check, and one extra
  step worth recording: the pre-merge read came back **`behind`** (because
  #2874 had just landed), so Step 6.5's `update-branch` ran, moving the tip to
  `bba9d56b`.

  An update-branch **moves the SHA out from under the QC verdicts**, which are
  pinned to `e5db6988`. Rather than assert that this is harmless, I measured it:

  ```
  $ git diff --stat e5db6988 bba9d56b
   dev/status/harness.md                           |  6 ++--
   trading/devtools/checks/record_qc_audit_test.sh | 46 +++++++++++++++---
  $ git log --oneline e5db6988..bba9d56b --no-merges
   960bebc5  harness(audit-test): ... (#2874)
  ```

  The update pulled in **exactly #2874** and nothing else, and its two files are
  **disjoint** from every file #2875 touches (`build_snapshots/**`,
  `dev/status/post-run-validation.md`). So the reviewed content is byte-identical
  to what both reviewers approved. CI then re-ran green on `bba9d56b` and the
  merge proceeded. Worth flagging generally: **`update-branch` silently
  invalidates SHA-pinned QC verdicts**, and the only honest way through is to
  show the delta is disjoint, not to assume it.
- **#2873 (`ops/budget-2026-09-20-35510571160`)** — **still open. I tried to
  clear it and did not succeed.** It is the orphaned budget record from this
  morning's failed run, opened standalone because there was no daily summary to
  bundle into: a single added `dev/budget/*.json`, matching the convention all
  six prior budget PRs followed. It was `behind`, so I ran `update-branch`
  (new tip `279f86e3`) and waited ~18 minutes across two polls;
  `build-and-test` was still `in_progress` and `mergeable_state` never left
  `blocked`, so I stopped rather than keep burning the run on housekeeping.
  **Nothing is wrong with it — it just needs its CI to finish.** It should
  merge cleanly on the next attempt.

  Worth clearing, though, and not only for tidiness: a lingering **non-track**
  PR makes the next run's `QUEUE_NON_EMPTY` read "queue non-empty" while no
  track work is actually in flight — the exact vacuous-saturation ambiguity the
  #2579 precondition exists to prevent. It fired that way today: the computed
  fact said `QUEUE_NON_EMPTY=1`, and the one PR was this.

**Track-work queue: empty.** Both dispatched PRs merged; nothing of substance is
waiting on review. That is maximum dispatch capacity for the next run, not
saturation — and the next run should read it that way.

## Follow-up Queue

Repo-wide open `- [ ]` items in `dev/status/*.md`: **64**. Per-file maximum:
**28** (`harness.md`). Files over the threshold of 4: **3** — `harness.md` (28),
`trade-audit.md` (9), `orchestrator-automation.md` (7).

Step 2b's maintenance gate is satisfied in substance rather than by ceremony:
this run's harness dispatch *was* a maintenance pass — it closed two items off
the largest backlog and refreshed that file's `## Last updated:`, which today's
track pacer had flagged as 30 days stale across 46 commits. `orchestrator-
automation`'s 7 items are all blocked on `workflow` scope; `trade-audit`'s 9 are
feat-backtest-owned and none is the top of its track's queue.

## Audit records

Written by `write_audit.sh` with `--sha` passed on every call — `gh` is absent
here, so the GHA path always takes the fallback, and the fallback is exactly
where omitting `--sha` degrades the rework-collision guard.

```
dev/audit/2026-09-20-harness-audit-27b-residuals-harness.json                         (consecutive_rework_count=0)
dev/audit/2026-09-20-feat-series-level-build-runner-prev<ts>-post-run-validation.json (NEEDS_REWORK @1b6d9031)
dev/audit/2026-09-20-feat-series-level-build-runner-post-run-validation.json          (APPROVED @e5db6988, count reset to 0)
```

The middle line is the guard working as documented: the second write for the
same branch **on the same date** did not clobber the first — it preserved it
under `-prev<ts>-` because the two calls carried different `--sha`. Had `--sha`
been omitted, the NEEDS_REWORK record would have been destroyed and the
`>= 3`-consecutive-rework human-escalation trigger would have been unreachable
for this branch today. A rare case of a documented hazard being exercised and
observably not firing.

## Dependency Unlocks

- **`post-run-validation`'s next step is now purely operational.** `Series_level`
  was the last unwired detector on the track; with #2875 merged, the remaining
  work is "arm `-detect-series-level` on the next warehouse rebuild and read the
  sidecar" — no code gate. It pairs naturally with `rename-twin-dedup`'s pending
  item (arm #2862's twin guards on a PIT rebuild): **both now want the same
  rebuild**, so one operational pass would discharge two tracks.
- Expect `Mixed_scale` rows (PEGX/CGE/TNT/HTV) and possibly **zero**
  `Whole_window` rows on that first armed report. Per the status file, an empty
  report is the **expected** result, not broken wiring — now pinned by a test.

## Current Milestone Target

**PIT top-3000 universe migration** (`dev/plans/pit-universe-migration-2026-09-14.md`)
— steps 1–6 MERGED, maintainer-led and LOCAL; orchestrator dispatch stays off it.
The live experiment is the pre-registered `v1-index-veto` arm running locally
(salts 0/1/2, ~6.3 h each). Nothing in this run touches either.

## Questions for You

1. **Will you edit `.claude/agents/lead-orchestrator.md` to write the summary
   skeleton at Step 1 instead of Step 7?** This run did it by hand and the
   summary survived; the two runs that did not (09-16, 09-20 12:25Z) lost their
   whole slot plus ~$23. `.claude/agents/**` writes have been refused for 19
   consecutive runs, so no agent can make this one-paragraph fix. It is the
   highest-leverage blocked item in the program right now.
2. **#2847 (`prune-candidates-weekly` still on `gh pr create`)** needs the
   `workflow` scope that is 403 on all three routes. The REST fix is already
   proven in #2842. One human push retires a 4-week-dead weekly job.
3. **#2876 (file-length linter scoped to `*/lib/*.ml`)** — nine files over the
   hard limit, unobserved. Widening the scope is mechanical; deciding
   extraction-vs-marker-vs-exception for each of the nine is the judgement call.
   Want that dispatched as harness work next run, or do you want to set the
   per-file policy first?
4. **`weekly-start-sweep`'s 09-21 cron is the first test of #2842.** If it goes
   green, its streak of 10 was historical and the item closes itself. Worth a
   glance tomorrow.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
