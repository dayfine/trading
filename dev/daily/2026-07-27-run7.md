# Status — 2026-07-27 [run 7]

Run timestamp: 2026-07-27T22:36:47Z– (GHA orchestrator run 30311344891)
Run ID: 2026-07-27-run-7

**Mode:** FULL PASS — **found that two of my own verification instructions do not
work in this container at all, and that `gh` — which the orchestrator spec tells
agents to use in six places — is not installed here.**

## Headline

- **`gh` is not on PATH in this container, and that is why QC reviews stopped
  posting themselves.** `which gh` → not found. `jst`, `jj`, `git`, `curl`,
  `python3` are all present; `gh` is not. This is not cosmetic: the orchestrator
  spec instructs agents to run `gh pr view` / `gh pr edit` (Step 2d footer,
  Step 4 commit discipline, Step 4.5 PR-creation fallback), `record_qc_audit.sh
  --pr-number` requires it, and `.claude/rules/pr-merge-gates.md`'s entire
  "verify CI before merge" procedure is written in `gh pr checks`. Most of those
  call sites are wrapped in `|| true`, so they **fail silently**. The visible
  symptom this run: qc-structural completed a full review of #2140 and then had
  no way to post it — it returned the review body in its message and told me to
  post it by hand. I did (review id 4792163982). Everything downstream of a
  posted review — the audit record, the merge gate — depends on a step the
  environment cannot perform.
- **My own Step 6.1 build gate failed twice, in two different ways, and neither
  failure was a code signal.** First attempt: `dune: command not found`, exit
  **127** — I invoked `dune` without `eval $(opam env)`. Second attempt, run
  correctly from an isolated worktree: **exit 137, SIGKILL** — OOM-killed,
  because three dispatched agents were each running their own full `dune build`
  on a **4-CPU / 16 GB** box and dune defaults to `-j 4` per tree. Both exit
  codes would read as "main is RED" to a naive check. I used GitHub CI on main's
  exact HEAD `70cd1c8d` instead, which is **stronger** evidence — it runs the
  full linter suite in a clean environment — and is what I now recommend as the
  default gate.
- **A dispatched agent checked out its branch in the shared main checkout,
  voiding my in-flight gate.** For a window, `/__w/trading/trading` was on
  `feat/picks-validator-v1` instead of `main`. It self-corrected into a proper
  worktree, and all five agents ended up correctly isolated — but the damage was
  already done to anything reading or building the shared tree in that window.
  **The orchestrator's own GHA dispatch guidance causes this**: it tells agents
  to `git checkout -b <branch> origin/main`, which is a shared-tree operation and
  is unsafe the moment two agents run at once.
- **PR #2139 claimed a green suite while CI was red, and the cause is a known
  harness bug.** Its body says "Full `dune build && dune runtest` green"; CI
  `build-and-test` failed on that exact SHA with 8 nesting-linter violations, all
  in the single 241-line file the PR adds. This is the signature of
  `H-RUNENV-WORKTREE-BLIND` — `dev/lib/run-in-env.sh` verifying `main` from a
  worktree. I dispatched both the fix for the PR and the fix for the wrapper.
- **The one item run 6 handed me as "top item for the next run" did not sail
  through.** #2140 got structural APPROVED (4) but behavioral **NEEDS_REWORK**
  (2), on a finding that lands squarely on me: the `> threshold` branch that
  feeds **my own Step 2b** is never executed by its test, so inverting `-gt` to
  `-lt` would leave the suite green with the maintenance-cycle signal backwards.

## Step 1b drift cross-reference

Newest merged summary on disk: `dev/daily/2026-07-27-run6.md` (#2141, `70cd1c8d`).
Run 6's `Pending work` rows, re-verified against live GitHub state:

| run-6 row | run-6 state | verified now | classification |
|---|---|---|---|
| weekly-snapshot #2136 | auto-merged | merged `1a91ee94` | confirmed |
| harness #2135 | auto-merged | merged `ed816d4f` | confirmed |
| cleanup #2138 | auto-merged | merged `f3a80c46` | confirmed |
| harness #2140 | awaiting-merge, "QC NOT YET RUN" | still open, still **0 reviews**, CI green | confirmed — accurate handoff |
| ops-data | not dispatched | `EODHD_API_KEY` re-verified **absent** | unchanged |

**No `[drift]` warnings.** Eighth clean cross-reference in a row.

**One gap in run 6's own record, not track drift:** PR **#2139** was opened at
20:55Z, inside run 6's window (19:51Z–21:54Z). Run 6 recorded it in
`dev/status/_index.md` ("#2139 open (maintainer, picks-validator v1)") but
**omitted it from its own `Pending work` table**, and did not notice its CI was
red. So run 6's two outputs disagreed with each other about what was in flight.
Filed as `[info]` below — the index was right, the table was incomplete.

**Run-number derivation.** On-disk count gives 7 (`.md`, `run2`, `run3`, `run4`,
`run6`, `-summary`). I also applied run 6's recommended cross-checks: no open
`ops/daily-*` PR branches exist, and querying the orchestrator workflow directly
shows **no other run in flight** (mine is the only `in_progress`) and no
completed run today citing a number above 6. All three sources agree: **run 7**.

Worth recording: the workflow API shows **seven** completed orchestrator runs
today but only **five** summary files. `30287137763` (16:58Z) is run 6's
documented killed run; `30250031315` ran only 23 minutes and most likely
fast-exited as a no-op, which by design does not push a summary. So the
"runs outnumber summaries" pattern run 6 flagged is real and has more than one
benign cause as well as the malign one.

## Step 1c carried-`[critical]` verification

Run 6 carried **no `[critical]`**. Re-verified its standing items:

1. **Main baseline** → **GREEN**, on the exact HEAD `70cd1c8d`:
   `build-and-test` success (22:04:15Z), `perf-tier1-smoke` success (21:56:59Z),
   `golden-sp500-5y` success, `golden-custom-universe` success. See §Health Scan
   for why this is CI evidence rather than a local re-run.
2. **`[high]` `.claude/agents/` write block** → **carried, and independently
   corroborated twice this run.** I checked `.claude/settings.json`: it contains
   **no deny rule** for that path, and the file is `-rw-r--r--` and writable by
   my uid. So run 6's narrowing was right — it is a harness-level tool
   permission on one path, not filesystem and not project config. qc-behavioral
   hit the same block from a different direction and described it as "legitimately
   sensitive-file-blocked". **I did not route around it.**
3. **`[medium]` run-number rule cannot see an in-flight or killed run** →
   **still real**; I applied the recommended three-source derivation (above) and
   it cost me a dedicated API query to be safe.
4. **`[medium]` concurrent orchestrator runs undetected** → **remains resolved.**
   Verified again: only one orchestrator run in flight.
5. **`[high]` auto-merge neither sufficient nor safe** → **carried, still no
   victim.** Both PRs were `mergeable_state: behind` again this run.
6. **`[info]` `GH_TOKEN` lacks `issues: write`** → not re-tested; carried
   unchanged rather than asserted.

**No still-real `[critical]` items.**

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | **auto-merged** | harness/followup-count-fix | #2140 | **MERGED `df69562b`** — full loop in one run: QC → NEEDS_REWORK → rework → re-QC APPROVED → merge |
| weekly-snapshot | **rework 1/2 in flight** | feat/picks-validator-v1 | #2139 | CI green, structural APPROVED q5; behavioral NEEDS_REWORK q2 — 3 claimed red-path tests don't exist. Test-only rework dispatched |
| harness (2nd) | **blocked — no CI, conflicts** | harness/run-in-env-worktree-root | #2143 | Fix pushed, structural APPROVED q5. **Zero check-suites (H-CI-DISPATCH)** and now `dirty` against the #2140 I merged. Needs rebase + a way to make CI fire; behavioral QC not yet run |
| cleanup | not dispatched | — | — | 6 real open backlog items; deferred on container capacity (see §Budget) |
| ops-data | not dispatched | — | — | `EODHD_API_KEY` absent; `data-gaps.md` unchanged since 2026-05-03 |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | qc-structural | **APPROVED** q4 | #2140. Two mutations, both RED. **Could not post its own review — no `gh`**; I posted it (id 4792163982) |
| harness | qc-behavioral | **NEEDS_REWORK** q2 | #2140. Measured the count (41 vs threshold 10) rather than reasoning about it; found the Step 2b branch untested. Posted its own review (id 4792245039) via the REST recipe I supplied |
| harness | harness-maintainer | **completed** | #2140 rework `9767e712`. All 4 findings closed; ran the `-gt`→`-lt` inversion itself → RED, restored → GREEN |
| harness | qc-behavioral (re-QC) | **APPROVED** q4 | #2140 at the rework tip. Two independent mutations + **perturbed the live `dev/status/` tree** to prove fixture isolation. Review id 4792360786 |
| harness | — | **merged** | #2140 → `df69562b`; three gates re-verified on the tip immediately before merge |
| weekly-snapshot | feat-weinstein | **completed** | #2139 `36e403bf`. All 8 nesting violations refactored away — **+168/−140 in one file, zero `@nesting-ok` markers** |
| weekly-snapshot | qc-structural | **APPROVED** q5 | #2139. Independently checked both non-mechanical rewrites (De Morgan negation, `Hash_set.add` move) — both correct. Review id 4792370146 |
| weekly-snapshot | qc-behavioral | **NEEDS_REWORK** q2 | #2139. **9 mutations incl. a provenance proof**; 4 came back green, exposing 3 unpinned contracts. Review id 4792431610 |
| weekly-snapshot | feat-weinstein (rework) | **in flight** | #2139 rework 1/2 — test-only, no production change |
| harness (2nd) | harness-maintainer | **completed** | `2e38a52a` — dual-candidate root derivation + 174-line regression check. **Revert test RED 4/5**; also fails loudly when the two candidates disagree, which is the case cwd-preference alone can't fix |
| harness (2nd) | qc-structural | **APPROVED** q5 | #2143. Ran the revert test itself — **3/5 assertions RED** against the pre-fix script (author reported 4/5; both confirm the test is real, the count differs). Verified the docker + devcontainer paths unregressed and the `RUN_IN_ENV_PRINT_ROOT` seam can't fire accidentally. Review id 4792467256 |
| cleanup | — | skipped | container capacity — see §Budget; **not** a backlog-empty skip |
| ops-data | — | skipped | `EODHD_API_KEY` absent; `data-gaps.md` unchanged since 2026-05-03 (sentinel unchanged) |

## QC Status

- **harness (#2140)**: structural **APPROVED** (q4) → behavioral **NEEDS_REWORK**
  (q2) → rework → behavioral re-QC **APPROVED** (q4) → **MERGED `df69562b`**.
  Audit: `dev/audit/2026-07-27-harness-followup-count-fix-harness.json`
  (`consecutive_rework_count` reset 1 → 0 on approval).
  **Deviation recorded honestly:** structural was **not** re-run on the rework
  delta. That delta is 4 files / +54 −6, shell and docs only, with no OCaml, no
  core-module surface and no import changes — the structural rows (P1–P6, A1–A3)
  have no purchase on it, and structural had already approved the surrounding
  files. I judged the re-run redundant rather than skipped it silently; disagree
  and I will re-run it next time.
- **weekly-snapshot (#2139)**: CI **green**, structural **APPROVED** (q5),
  behavioral **NEEDS_REWORK** (q2) → rework iteration 1 of 2 in flight.
  Audit: `dev/audit/2026-07-27-feat-picks-validator-v1-weekly-snapshot.json`.
- **harness #2143**: structural QC in flight; no behavioral yet.

All audits were written via the `write_audit.sh` direct-flag fallback, because
the preferred `record_qc_audit.sh --pr-number` path requires `gh`.

### The #2140 behavioral finding worth reading

The finding I most want on the record is not one of the three defects the PR set
out to fix. It is that **`check_05_followup_items.sh:90` — the `> threshold`
branch whose output feeds my own Step 2b maintenance-cycle decision — is never
executed by the test.** The fixture totals 3 items, so only the info branch runs,
and the test asserts the warning's *absence*, never its presence. Inverting
`-gt` to `-lt` would leave the suite GREEN with the Step 2b signal exactly
backwards.

The incident being fixed was "an untested filter made this counter lie". The fix
shipped with its most consequential branch still unexecuted. That is the same
defect one level up, and it is the third distinct instance this run of the
recurring meta-problem: **a verification believed to have happened that did not.**

The reviewer also measured the metric instead of arguing about it: the new count
is **41** against `followup_threshold: 10`, and it carries a **≥11-item
non-actionable floor** — 10 `T2`/`T3`/`T4` tier roadmap items in `harness.md`
plus 1 template placeholder in `cleanup.md`. So the threshold is crossed *by
construction* even if every genuine follow-up were closed tomorrow, and 10 of
those 11 are items **Step 2c of this same orchestrator explicitly refuses to
dispatch**. The old number (76) and the new number (41) therefore produce the
identical Step 2b outcome. The fix repairs the measurement — 76 was not a count
of anything — but does not yet make the metric a usable gate. A tracker for the
retune is part of the rework.

## Budget

- Budget cap: **$200** (`dev/config/merge-policy.json`; a runaway-dispatch
  backstop under a flat-rate subscription, not a real budget)
- Target utilization: 60–95%
- Subagents dispatched: **5** (2 QC, 1 feat, 2 harness — one of which is a rework)
- Measured subagent tokens where reported: qc-structural ~100K, qc-behavioral ~117K
- Estimated cost this run: **~$8–12** including orchestrator overhead → **~5%**
  of cap. **UNDER_TARGET.**

**The binding constraint this run was not budget — it was container memory, and
that is a more useful number than the dollar figure.** The box is **4 CPUs /
16 GB**. Each concurrent agent runs `dune` at its default `-j 4`, so three agents
plus my own gate requested ~16 parallel OCaml compiler processes and my build was
**OOM-killed (exit 137)**. I held the `code-health` cleanup dispatch back for
this reason — **not** because the backlog was empty (it has 6 real items). Under
the current dispatch model, ~2–3 concurrent OCaml-building agents is the ceiling
regardless of what the dollar cap says, so a run that "under-utilizes" budget may
be correctly saturated on memory. See §Escalations.

## Health Scan

From `dev/health/2026-07-27-fast-run7.md`.

- Result: **CLEAN**
- Main build: **PASSING** — CI on exact HEAD `70cd1c8d`, all four checks success
- Status file integrity: **PASSING** — `status_file_integrity.sh` is wired at
  `trading/devtools/checks/dune:166` under `(alias runtest)`, so it executed
  inside that green `build-and-test`
- Critical items: **none**

The local re-run was attempted twice and discarded both times (127, then 137).
Neither was a code signal; both are recorded in the health report because either
one, pattern-matched carelessly, produces a false `[critical] main is RED`.

## Follow-up Queue

Measured directly this run (and independently corroborated by qc-behavioral):
**41** open `- [ ]` items across `dev/status/*.md`, of which **~11 are
non-actionable** (10 tier-roadmap items + 1 template placeholder). Genuine debt
is roughly **30**: 7 open `H-*` items in `harness.md`, 6 real items in
`cleanup.md`, ~16 across eight other status files.

**Step 2b was not triggered**, despite 41 > 10, because the threshold is
crossed by construction (above) and treating that as a signal would convert a
feature slot into a maintenance pass on every eligible run. This is exactly the
decision the `H-FOLLOWUP-THRESHOLD-RETUNE` tracker is being filed to resolve.

## Integration Queue

- **#2140 — MERGED `df69562b`.** All three gates re-verified on the tip
  immediately before merging (CI `build-and-test` + `perf-tier1-smoke` both
  success on `9767e712`, `mergeable_state: clean`), per
  `.claude/rules/pr-merge-gates.md`'s "don't trust stale CI reads" rule.
- **#2139** — not merge-eligible: behavioral NEEDS_REWORK, rework in flight.
- **#2143** — not merge-eligible: QC in flight; CI had not yet started a
  check-suite when this was written.

Branch-staleness note: both open PRs were `mergeable_state: behind`. I updated
#2140's branch onto main (tip `0f0f0763`) **and proved the update was
non-interfering** — main's drift since that PR's base touches nine files, none of
which is among the six the PR changes, and `git diff` across the two SHAs for the
reviewed paths is empty. So the structural APPROVAL transferred to the new tip on
evidence rather than assumption. #2139 is still behind and will need the same
treatment before merge.

## The #2139 behavioral finding — claimed coverage vs actual

Worth recording alongside the #2140 finding, because it is the same shape and the
two together are the story of this run.

The PR's Test plan says "13 tests: … each invariant has a red-path test". The
reviewer checked that quantifier instead of accepting it, by **deleting each
invariant's error branch and re-running**. Four mutations came back **green**:

| mutation | result | what it means |
|---|---|---|
| `positive_prices` neutered (`let bad = false`) | exit **0** | a named invariant has no red path |
| `_check_macro` short-circuited | exit **0** | ditto |
| `Float.abs` removed from the risk identity | exit **0** | the side-aware `abs` is unpinned — every sized candidate in the suite is a long |
| class-boundary `<=` flipped to `<` | exit **0** | the "boundaries fall to the lower class" convention is neither stated in the `.mli` nor tested |

The domain logic itself was **correct on every rule the reviewer could verify** —
both stop sides (long stop below fill, short stop above, matching
`weinstein-book-reference.md` §6.3), the overshoot sign, and the class partition
all agree with the producer. Structural had given it a 5. The gap is entirely
between what the PR *claims* is pinned and what actually is.

The `abs` finding is the sharpest: it is latent today because the generator only
sizes longs, but the module's headline contract is "side-aware", and on a short
the un-absoluted formula yields a **negative** expected risk — a spurious
`risk_consistency` Error on a valid artifact. One sized-short test closes it.

Given that this PR arrived claiming a green suite that was red, "make the coverage
claim true" is exactly the right rework. It is test-only; no production code
changes.

## Escalations

- **[high] `gh` is not installed in this container, and the orchestrator spec
  depends on it in at least six places.** Verified: `which gh` → not found
  (`jst`, `jj`, `git`, `curl`, `python3` all present). Affected: Step 2d's PR
  footer block, Step 4's commit-discipline footer + ready-for-review step, Step
  4.5's PR-creation fallback, Step 5 Stage 4's preferred `record_qc_audit.sh
  --pr-number` path, and the whole of `.claude/rules/pr-merge-gates.md`
  §"Verifying CI before merge". Most call sites end in `|| true`, so they do not
  error — they silently do nothing, which is the worst available behaviour.
  **Observed cost this run:** qc-structural finished a complete, high-quality
  review and could not post it; without my manual intervention that review would
  simply have evaporated. **Fix, in preference order:** (a) install `gh` in the
  orchestrator image; or (b) convert the call sites to the REST recipe that
  demonstrably works here — I supplied it to qc-behavioral in its brief and it
  posted its own review first try, so the recipe is proven, not theoretical.

- **[high] The orchestrator's own GHA dispatch guidance corrupts the shared
  working tree when agents run in parallel.** Step 4's GHA path says each
  subagent should run `git fetch origin && git checkout -b <branch> origin/main`
  — a **shared-tree** operation in a single checkout. One agent did exactly that
  this run and `/__w/trading/trading` sat on `feat/picks-validator-v1` for a
  window, which voided my concurrently-running build gate and briefly made my own
  status-file reads resolve against a feature branch. The same section also says
  "Do NOT set `isolation: worktree`… GHA doesn't need isolation at all" — that
  premise is false for any run with more than one agent. Every agent that *did*
  use its own worktree was fine (four of five). **Fix:** replace the GHA
  checkout instruction with `git worktree add`, and make the orchestrator verify
  `git -C /__w/trading/trading rev-parse --abbrev-ref HEAD` is still `main`
  before trusting any local read or build. This edit lands in
  `.claude/agents/lead-orchestrator.md`, which is behind the write block below.

- **[high] `H-RUNENV-WORKTREE-BLIND` has now produced a false green that reached
  a PR body.** Carried from run 6 and **escalated with new evidence**: #2139
  states "Full `dune build && dune runtest` green" while CI was red on that exact
  SHA with 8 nesting violations in the file the PR adds. `run-in-env.sh` line 59
  pins `PROJECT_ROOT="${GITHUB_WORKSPACE}/trading"` regardless of cwd; the
  devcontainer fallback immediately below it and the local docker path both
  derive correctly from the invoking location, so only the GHA branch is wrong.
  A fix is in flight on `harness/run-in-env-worktree-root`. Note run 6 reported
  this as "filed as `H-RUNENV-WORKTREE-BLIND`" but it **was never added to
  `dev/status/harness.md`** — it existed only in that run's summary prose. Filing
  it is part of the dispatched work.

- **[medium] Container memory, not budget, is the real dispatch ceiling — and an
  OOM kill is indistinguishable from a build failure.** 4 CPUs / 16 GB; dune
  defaults to `-j 4` per tree; three concurrent agents plus one orchestrator
  build ⇒ SIGKILL. **Exit 137 looks exactly like a failed build to an agent that
  greps output instead of reading exit codes**, so this can manufacture false
  `[critical]` reports about main. Step 3.75's sizing logic reasons purely in
  dollars and has no notion of this limit, so it will happily recommend "dispatch
  all eligible tracks" into an OOM. **Fix:** cap concurrent OCaml-building agents
  at ~2, set `DUNE_JOBS`/`-j 2` in agent briefs, and teach Step 3.75 that
  memory — not `max_daily_cost_usd` — is the binding constraint here.

- **[medium] Run 6's `Pending work` table and its `_index.md` disagreed about
  what was in flight.** #2139 opened at 20:55Z, inside run 6's window; the index
  row recorded it, the Pending-work table did not, and its red CI went unnoticed
  for two hours. The two artefacts are generated in the same run from the same
  state, so they should not be able to disagree. **Fix:** derive the Pending-work
  table from the live open-PR list rather than from tracked-track state, so an
  unexpected PR cannot be silently absent.

- **[medium] `git merge-base --is-ancestor` is the wrong staleness test in this
  repo, and it reports every branch as unmerged.** This repo squash-merges, so a
  merged branch's tip is never an ancestor of `main`. I ran the check while
  looking for stale bookmarks and got **47 "unmerged" branches, and 0 merged** —
  a number that would look alarming and is entirely an artefact of the test.
  Anything auditing branch staleness (Step 2c's stale-bookmark rule included)
  must resolve each branch to its PR and check `merged`, not walk ancestry.

- **[high] `H-CI-DISPATCH` reproduced and substantially narrowed — and it is
  blocking the fix for the false-green bug.** PR **#2143** has **zero** Actions
  workflow runs, ~40 minutes after opening. I ruled out three explanations
  rather than guessing:
  1. **Not configuration.** I read `.github/workflows/ci.yml` directly rather
     than trusting the prior run's claim: `on: pull_request` / `push: [main]`,
     **no `paths`, `paths-ignore`, or `branches` filter**. (Worth doing — a
     prior "advisory linter" claim in the orchestrator spec turned out to be
     wrong, so inherited claims get re-verified here.)
  2. **Not the event type.** I tried `opened` (PR creation), then
     **close + reopen** (`reopened`), then an **empty commit** (`synchronize`).
     All three produced zero runs. That kills the "only `opened` is suppressed"
     hypothesis.
  3. **Not a measurement artifact.** I validated the query against a control
     branch — `?branch=harness/followup-count-fix` returns **`total_count: 6`**
     through the identical call. So the zero is real, and slash-containing
     branch names are not breaking the filter.

  What remains: something about this specific branch/PR. Sibling branches in
  the same window (`feat/picks-validator-v1`, `harness/followup-count-fix`)
  triggered normally under the same credentials, which is what makes it
  maddening — and matches the original report on `fix/lapacke-gp-cholesky`
  exactly. **Consequence: a QC-APPROVED (quality 5) fix for the bug that has
  been invalidating verification repo-wide cannot clear gate 1.** The backlog
  item's proposed watchdog ("if a PR has been open > N minutes with zero
  check-suites, say so loudly") would have surfaced this in minutes. Given it
  has now hit twice, I'd raise its priority.

- **[medium] #2143 now conflicts with #2140, which I merged earlier this run.**
  `mergeable_state: dirty`. Both PRs touch `trading/devtools/checks/dune` (each
  adds a `runtest` rule) and `dev/status/harness.md` (each adds backlog items).
  This is ordinary concurrent-edit collision, not a defect — but it is the
  predictable cost of dispatching two harness items into the same two files in
  one run, and Step 2c's "harness items touch disjoint files" assumption is what
  licensed it. Those two files are the harness track's shared spine; **any two
  harness dispatches will contend for them.** Either serialize harness work, or
  have the second agent rebase before opening its PR.

- **[info] `run-in-env.sh` is a `dev/lib/` file, and agents can edit it freely.**
  Worth stating explicitly against the `.claude/agents/` write-block confusion:
  the sensitive-file guard is scoped to specific agent-definition paths, not to
  tooling. The three-run-old belief that "automated actors cannot fix the
  harness" was over-general — this run an agent rewrote the build wrapper, added
  a 174-line regression check, wired it into `dune runtest`, and filed two
  backlog items, all without hitting a permission wall.

- **[info] The docs-only merge carve-out still does not name `dev/audit/` or
  `dev/budget/`.** Unchanged from runs 4 and 6. Every orchestrator summary PR
  carries `dev/audit/*.json`, strictly outside the
  `.claude/rules/pr-merge-gates.md` allowlist. One line to fix.

- **[info] `record_qc_audit.sh`'s loud refusal when `gh` is missing is working as
  designed — and is now load-bearing.** The `H-AUDIT-GH-FALLBACK` fix from #2135
  makes it exit with a clear message rather than silently falling through to file
  mode. It fired correctly this run. The documented fallback (`write_audit.sh`
  with explicit flags) worked. Worth noting that the file-mode fallback would
  have found nothing regardless: **`dev/reviews/` does not exist in this repo any
  more** — reviews live on PRs. Any remaining spec text that treats
  `dev/reviews/<track>.md` as a real artefact (Step 1, Step 1.5's
  `Reviewed SHA:` parse, Step 5's write instructions) is describing a directory
  that is gone.

## Questions for You

1. **Install `gh` in the orchestrator image, or convert the call sites to REST?**
   I'd take (a) — one image change fixes six spec sites at once, and `jst` is
   already there and probably wants `gh` too. (b) is proven to work but means
   editing several files and leaves the next spec author free to write `gh`
   again. This is the single highest-leverage fix available right now.

2. **Please grant the Edit tool permission on `.claude/agents/lead-orchestrator.md`,
   or tell me the intended channel for changing it.** Three separate defects
   this run need edits to that one file — the GHA shared-checkout instruction,
   the `gh`-dependent agent templates, and the stale
   `## Followup / Known Improvements` counting rule at line 546 that
   `dev/config/README.md` and `check_05` have now both moved away from. It is
   filesystem-writable with no matching deny rule in `.claude/settings.json`, so
   the block is a harness-level rule on that path. I have not routed around it
   with `sed`, and I won't — but the backlog behind it is now three runs deep and
   growing.

3. **Should Step 2b's `followup_threshold` be retuned, or should the metric
   exclude tier/roadmap checkboxes?** The count has a hard ~11-item
   non-actionable floor above the threshold of 10, and 10 of those are items
   Step 2c refuses to dispatch — so "threshold exceeded" is now a constant.
   Excluding roadmap-shaped items is my preference (it makes the number mean
   "accumulated debt", which is what Step 2b is actually asking about), but it is
   a semantics decision with a live consumer, so I am not making it unilaterally.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
