# Status — 2026-05-04

**Run timestamp:** 2026-05-04T07:23:00Z (run start) — 2026-05-04T07:55:00Z (summary write)
**Run ID:** 2026-05-04-run-1
**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 1 FAIL: open PR #829 has tip_sha `a0e14d28` and no `dev/reviews/...` review file present pre-dispatch; Condition 2 FAIL: 23 non-summary commits since prior summary touched many `dev/status/*.md` files).

## Saturated-queue check result

- **Condition 1** FAIL — open PR #829 (`feat/delete-bar-reader-of-panels`) had no review file at run start; tip_sha `a0e14d28688a5a9c21134defd1fd7f11e6e310d9` ≠ Reviewed_SHA (no file).
- **Condition 2** FAIL — 23 non-summary commits on `main` since prior summary commit (#806 at 2026-05-03T13:42Z): #807..#828, plus 1 chore (#818). Multiple `dev/status/*.md` modifications among them (most notably `dev/status/data-foundations.md`, `dev/status/experiments.md`, `dev/status/tuning.md`, `dev/status/cleanup.md`, plus the run-2 summary commit itself).
- **Condition 3** N/A — Step 1b ran; no `[drift]` warnings emitted (run-2's pending-work table had no `dispatched`/`awaiting-merge` rows to verify).
- **Condition 4** PASS — `harness.md` and `cleanup.md` unchanged on main since prior summary (track-pacer agent #823 added new `.claude/agents/track-pacer.md` + `.github/workflows/track-pacer-weekly.yml` but did not edit `dev/status/harness.md`).

Decision: full pass. One QC pipeline dispatched (1 open PR; no fresh feat-* dispatchable today — see "Pending work" rationale below).

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| data-foundations | awaiting-merge | feat/delete-bar-reader-of-panels | #829 | F.3.a-4 — QC APPROVED q=5/5 this run; awaiting human merge (auto_merge=false). Post-merge: F.3.b (`Weekly_ma_cache` port off `Bar_panels.t`) is the next plan-first dispatch on this track. |
| experiments | not dispatched (no eligible item) | — | — | M5.4 E3 (#815) + E4 (#816) sweep harnesses MERGED today; the actual sweeps are local-only (~5×2h tier-3 budget). M5.2a–d still PLANNED but the in-status-file claim "blocked on M5.1" is stale (split_day_stop_exit P0 was resolved by PR #752 weeks ago). Status file body refresh deferred. |
| tuning | not dispatched (no eligible item) | — | — | T-A grid_search (#805 MERGED) + T-B Bayesian opt (#817 MERGED) both shipped. Status file body still claims `## Status READY_FOR_REVIEW` (stale; #817 merged). T-A CLI binary + T-B CLI binary deferred (need real backtest runner integration; bigger PR). T-C blocked on Norgate ingest. |
| backtest-perf | not dispatched | — | — | Tier-4 release-gate scaffolding (#810) + Tier-4 SCALE (#811) MERGED today. Tier-4 manual `workflow_dispatch` is local-only. N≥5000 release-gate path now structurally complete via Phase F.2 + Tier-4 scaffolding; pending V1/V2/V3 verifications + F.3 deletion. |
| simulation | not dispatched | — | — | M5 walk-forward + tuner; sp500-2019-2023 baseline rerun (local-only). Status file P0 claim is stale (resolved by #752). |
| short-side-strategy | merged | — | — | sp500-2019-2023 baseline rerun (local-only). G6/G14/G15/G4 all RESOLVED. |
| weekly-snapshot | merged | — | — | M6.1–M6.5 all merged; M6.6 live cycle DEFERRED. Track is owned by feat-weinstein per #778 scope expansion. Two advisory standalone-script integrity FAILs on the file remain (free-form `## Status PLANNED` + free-form trailing clause on `## Interface stable`). |
| harness | not dispatched | — | — | Backlog drained. **#823 added track-pacer agent (weekly Sunday 06:00 UTC cron — decoupled from daily orchestrator)**; harness.md not edited by that PR so the integrity check still doesn't see it as new content. |
| orchestrator-automation | blocked (Phase 2 pending) | — | — | Phase 2 background-execution items pending empirical tests per status file. |
| cleanup | blocked (no findings) | — | — | Backlog empty. No new `[medium]`/`[high]` findings from Step 6 deterministic checks. |
| cost-tracking | blocked | — | — | Today's measured cost will land in `dev/budget/2026-05-04-<run_id>.json` post-GHA-capture. |
| ops-data | not dispatched | — | — | All major data gaps RESOLVED yesterday/today: sp500 12-missing (#807); broad-universe coverage 100% (#814); sp500 historical delisted bars 217/218 (#812, ACE the only unfetchable). data-gaps.md last-updated reflects resolution. |
| (all other tracks merged earlier) | — | — | — | optimal-strategy / screener / trade-audit / backtest-infra / backtest-scale / data-panels / data-layer / portfolio-stops / strategy-wiring / sector-data / support-floor-stops / hybrid-tier — no change this run. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| data-foundations | qc-structural (iter 1) | NEEDS_REWORK (false-positive H3) | Agent claimed `dune runtest` fails on pre-existing linter violations (`runner.ml:426`, `entry_audit_capture.ml:163`, `weinstein_strategy.ml:465`, etc.). Orchestrator verified `dune runtest --force` exit 0 on tip SHA `a0e14d28` (verbatim shell evidence) and challenged via SendMessage. Recurring "qc-structural H3 false-positive on advisory linter text" pattern (carried 10+ runs as `[info]` escalation). |
| data-foundations | qc-structural (iter 2 — re-evaluation) | APPROVED quality_score 5 | After challenge, agent re-issued APPROVED with note: "build/runtest exit 0 on tip SHA `a0e14d28`; advisory linter findings on `runner.ml`, `entry_audit_capture.ml`, `weinstein_strategy.ml` do not gate the runtest target." All H1–H3, P1–P6, A1–A3 PASS. |
| data-foundations | qc-behavioral | APPROVED quality_score 5 | Pure-infra refactor PR — domain S*/L*/C*/T* checklist NA per `.claude/rules/qc-behavioral-authority.md` "skip entirely" rule. CP1–CP4 all PASS: `Bar_reader.of_in_memory_bars` semantics anchored by `test_weinstein_backtest.ml` integration tests (3 multi-year scenarios pinning exact n_buys / n_sells / symbol sets / round-trip W/L / final-value bands), `test_weinstein_breakout_trade` divergence (180→184 shares, 166.38→162.44 entry) is documented in-line as weekly-aggregation cache-warmup edge effect. |
| data-foundations | (orchestrator) | flipped draft→ready (no-op — PR was never draft); audit written | `dev/audit/2026-05-04-data-foundations-pr829.json` (overall_qc APPROVED, quality_score 5, consecutive_rework_count=0). PR #829 stays open for human merge (auto_merge_enabled=false in `dev/config/merge-policy.json`). |
| ops-data | — | not dispatched | All major data gaps RESOLVED in 2026-05-03 maintainer cycle (#807/#812/#814); data-gaps.md substantively unchanged today (last_updated 2026-05-03). |
| harness | — | not dispatched | Backlog drained; #823 (track-pacer agent) merged but no orchestrator-dispatchable T1/T3 items remain. |
| cleanup (Step 2e) | — | not dispatched | Backlog empty AND no new `[medium]`/`[high]` findings from Step 6 deterministic checks. |

## Feature Progress

### data-foundations [IN_PROGRESS — F.3.a-4 QC APPROVED today; major maintainer-driven progress overnight]

23 PRs landed on main since 2026-05-03 run-2 summary, including the entire **Wiki+EODHD PR-B/C/D** chain (#808/#809/#813), **F.3.a sub-sequence** (#825/#827/#828), **#826 (`## Status` integrity restore + ocamlformat 0.29 promotion across 14 stale files** — single biggest hygiene PR in months, dropped advisory integrity FAILs from 6 → 2), and the closing **data-pipeline-automation** track via PRs 1/4–4/4 (#819/#820/#821/#822). Today's orchestrator dispatch (1 open PR `#829`) ran the QC pipeline cleanly — q=5/5 on both axes, audit recorded, sits in §Integration Queue.

PR #829 specifically deletes:
- `Bar_reader.of_panels` constructor + 4 panel-helper functions
- `Weinstein_strategy.make ?bar_panels` optional argument
- `_resolve_bar_reader` helper (now trivial — direct match on the single remaining `?bar_reader`)

Migrates 6 remaining `~bar_panels` test callers to `Bar_reader.of_in_memory_bars`. The `Bar_reader.ma_cache` field stays (always `None` post-PR; future cleanup follow-up).

Post-merge: F.3.b (`Weekly_ma_cache` port off `Bar_panels.t`) is the next eligible feat-data dispatch — likely plan-first candidate per Step 3.5 trigger 2 (cross-cutting change touching > 5 files) once a-4 lands.

### experiments / tuning / backtest-perf / simulation [no work this run]

No new orchestrator-dispatchable items. Maintainer-driven progress overnight: experiments absorbed M5.4 E3 (#815) + E4 (#816); tuning absorbed T-B Bayesian (#817); backtest-perf absorbed Tier-4 scaffolding (#810) + SCALE (#811). Status file bodies on `experiments.md`, `tuning.md`, `simulation.md` are stale on multiple sub-items — refresh deferred until next dispatched-on-this-track session.

### harness [#823 track-pacer agent landed]

PR #823 added a **track-pacer agent** for weekly work-pace + strategic-fit audits (Sunday 06:00 UTC GHA cron, branch `ops/track-pacer-<date>`, distinct from `health-scanner`). The agent reads all track status files + git log over the prior week, surfaces 7 specific patterns (P1 PR cadence, P2 Next Steps staleness, P3 [info] carryover age, P4 unowned new tracks, P5 recurring discussion topics, P6 diminishing returns, P7 capability gaps vs milestone plan), and writes `dev/reviews/track-pacer-YYYY-MM-DD.md`. Output is advisory — no auto-merge, human reviews and merges. Decoupled from daily orchestrator; first run will fire on the next Sunday.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| data-foundations (F.3.a-4) | #829 (open, not draft, mergeable_state=clean) | APPROVED (re-evaluation after H3 false-positive challenge at SHA `a0e14d28`) | APPROVED | 5 | `dev/audit/2026-05-04-data-foundations-pr829.json` (consecutive_rework_count=0) |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **3** (1 qc-structural initial + 1 qc-structural re-evaluation via SendMessage + 1 qc-behavioral)
- Per-subagent breakdown:
  | Agent | Model | Status | Est. cost |
  |-------|-------|--------|-----------|
  | qc-structural (initial review PR #829) | opus | completed (~6 min, 44 tool uses, 97.7K tokens) | ~$0.50 |
  | qc-structural (re-evaluation via SendMessage) | opus | completed (~2.5 min, 11 tool uses, 37.8K tokens) | ~$0.20 |
  | qc-behavioral (PR #829) | opus | completed (~3 min, 30 tool uses, 111K tokens) | ~$0.55 |
- Any subagent killed mid-flight: NO.

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-05-04-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$2 of $50 cap (~4%) — orchestrator overhead (Read/Edit/Bash/curl + 2× `dune runtest --force` on main and PR branch) + 3 subagent dispatches.
- Utilization assessment: **UNDER_TARGET** (<60%) — ~4% of cap. Single-PR review window; no fresh feat-* work was eligible: Wiki+EODHD chain merged in maintainer cycle; F.3.a-4 (#829) is the only open PR and serializes against F.3.b (which would touch the same surface). M5.4 sweeps + Tier-4 release-gate are local-only. Eligible work was dispatched; nothing was skipped for budget reasons. Distinct from observation-mode runs — this run dispatched all eligible items within capacity.
- Scope reduced due to budget: NO.

## Data Operations

(ops-data did not run.)

- All three major data gaps RESOLVED 2026-05-03: sp500 12-missing symbols (#807, 491→503); broad-universe coverage 100% (#814, 10,472/10,472 — fixed coverage script bug + bulk-fetched 2,899 symbols); sp500 historical delisted bars (#812, 217/218; ACE the only unfetchable).
- `data/sectors.csv` is 18 days old as of today (refreshed 2026-04-16); refresh via `fetch_finviz_sectors.exe` recommended at >30 days. Not yet stale; not dispatched.

## Harness Work

(harness-maintainer did not run — backlog drained.)

PR #823 added track-pacer agent (weekly cron, decoupled from daily orchestrator).

## Health Scan

From `dev/health/2026-05-04-fast.md` (deterministic per Step 6):

- `dune build` exit **0** (PASSING) on HEAD `9a184d4`.
- `dune runtest --force` exit **0** (PASSING) on HEAD `9a184d4` — verified live this session pre-dispatch (also verified on PR #829 tip `a0e14d28` during H3 challenge).
- `status_file_integrity.sh` (standalone): 2 advisory FAILs (down from 6 at run-2 yesterday) — `weekly-snapshot.md` is the only remaining file with the free-form trailing-clause pattern. PR #826 reconciled 4 of the prior 6 FAILs (`data-foundations.md` + `experiments.md` cleaned up). Script exits 0; not a gate contributor.
- Result: **CLEAN** (build/runtest green; only advisory integrity warnings carried).

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~5 (carried).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): local-only.
- **covid-recovery 20.81% WR investigation** (carried; deferred until short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**: **EXERCISED + SELF-CORRECTED THIS RUN** — agent's first pass produced an H3 FAIL verdict citing `runner.ml:426`, `entry_audit_capture.ml:163`, `weinstein_strategy.ml:465` as gating violations. Orchestrator verified `dune runtest --force` exit 0 on the same SHA and challenged via SendMessage with verbatim evidence per the orchestrator's "live-evidence rule" (Step 6.1 + Step 7 §Escalations). Agent re-issued APPROVED. Recurring 10+ consecutive runs; recommendation re-iterated: tighten qc-structural agent definition to enforce exit-code-only gate verification (forbid citing finding text without paired exit-code).
- **qc-structural review-file persistence gap**: **EXERCISED THIS RUN** — agent wrote review file to typo path `/w/trading/trading/dev/reviews/...` (single underscore) instead of `/__w/trading/trading/dev/reviews/...`. Orchestrator detected via `find / -name "*pr829*"` and copied to correct path. Recurring 10+ consecutive runs; recommendation: tighten qc-structural agent definition to use absolute paths verified against `${REPO_ROOT}` (or current working directory) before write.
- **Schema drift from maintainer-driven new tracks** (carried 5 runs, materially improved). Run-2 fixed `tuning.md`; PR #826 today fixed `data-foundations.md` + `experiments.md`. Only `weekly-snapshot.md` remains (2 advisory FAILs — `## Status PLANNED` + `## Interface stable NO — track is brand-new.` trailing clause). Pre-decision: grow linter enum vs. edit the file. Harness backlog candidate.
- **magic_numbers linter false-positives on docstring text** (carried 4 runs): year / PR-number citations like `2019`, `2026`, `744`, `103.58` appearing inside `(* ... *)` comments are flagged as bare numerics. Linter-side fix candidate; harness backlog.
- **Stale per-track status files for the new tracks** (carried 5 runs, partially resolved). PR #826 today addressed 14 stale files but `tuning.md` (`## Status READY_FOR_REVIEW` after T-B already merged via #817) and `experiments.md` (claims #769 awaiting review — merged) and `simulation.md` (claims P0 RED on `split_day_stop_exit:1:post_split_exit_no_orphan_equity` — resolved by #752 long ago) all still drift. Refresh deferred to next dispatched-on-this-track run.

## Integration Queue

(Features with overall_qc APPROVED — ready to merge to main pending human decision)

- **#829 (data-foundations / F.3.a-4)** — `feat/delete-bar-reader-of-panels` @ tip SHA `a0e14d28`. mergeable_state=clean; not draft; CI green (`build-and-test` + `perf-tier1-smoke` both completed success). qc-structural APPROVED q=5; qc-behavioral APPROVED q=5; audit `dev/audit/2026-05-04-data-foundations-pr829.json` written. **Recommended merge action**: human reviewer can merge cleanly; this completes the F.3.a sub-sequence (Bar_reader.of_panels retirement) and unblocks F.3.b (Weekly_ma_cache port).

## Current Milestone Target

After this run's QC cycle on PR #829:

- **M5.1 foundation hardening** — DONE.
- **M5.2 experiment infrastructure** — DONE (M5.2a/b/c/d/e all MERGED).
- **M5.3 daily-snapshot streaming** — Phases A through F.2 MERGED; **F.3.a-1/-2/-3 MERGED, a-4 (#829) APPROVED today, awaiting human merge**. Snapshot mode is the canonical runtime path. After #829 merges: F.3.b (Weekly_ma_cache) is next; F.3.c/d (Panel_callbacks + Macro_inputs) follow; final `bar_panels.{ml,mli}` deletion gated on snapshot-mode-default soak across all baseline + tier-3 + tier-4 scenarios.
- **M5.4 mechanical experiments** — partial. E1 / E2 / E3-harness (#815) / E4-harness (#816) MERGED; E3 + E4 sweep runs themselves are local-only.
- **M5.5 parameter tuning** — T-A grid_search (#805) + T-B Bayesian (#817) MERGED. T-A/T-B CLI binaries deferred (need real backtest runner integration); T-C ML/HMM blocked on `data-foundations` Norgate ingest.
- **M6.1–M6.5 weekly-snapshot verification** — DONE on content.
- **M6.6 live cycle** — DEFERRED.
- **M7.0 Track 1 Norgate** — pending (vendor signup blocker).
- **M7.0 Track 2 multi-market** — DONE (#772).
- **M7.0 Track 3 Synth-v1 + v2** — DONE; **Synth-v3 pending** (~1000 LOC).
- **Wiki+EODHD interim historical universe** — **PR-A/B/C/D ALL MERGED** (#803/#808/#809/#813). 2010-2026 dynamic universe surface now complete.
- **Tier-4 release-gate** — scaffolding (#810) + SCALE (#811) MERGED; manual `workflow_dispatch` is local-only.
- **data-pipeline-automation track** — CLOSED via #819–#822.

## Dependency Unlocks

- **F.3.a-4 #829 awaiting merge unblocks F.3.b dispatch** (Weekly_ma_cache port off `Bar_panels.t`). Once #829 lands, the `Bar_reader.of_panels` surface is gone — F.3.b proceeds without serialization risk.
- **Wiki+EODHD chain complete (PR-A/B/C/D)** — interim historical universe surface ready; downstream `build_universe.exe --fetch-prices` auto-fetches on cache miss (PR-C), closing the recurring "missing symbols" gap pattern at construction time.
- **broad-universe coverage 100%** unblocks Tier-4 release-gate's data-completeness gate.
- **Status integrity drift much reduced** — PR #826's 14-file pass dropped advisory FAILs from 6 → 2; only `weekly-snapshot.md` remains.

## Escalations

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried 10+ consecutive runs; **EXERCISED + SELF-CORRECTED THIS RUN** via SendMessage challenge). Agent's initial verdict cited specific function-length / nesting / magic-number findings on `runner.ml`, `entry_audit_capture.ml`, `weinstein_strategy.ml` as H3 FAIL despite `dune runtest --force` exit 0 on the same tip SHA. Orchestrator's "live-evidence rule" (verbatim shell command + exit code) restored APPROVED. Recommendation: amend qc-structural agent definition to require pairing every gating finding with a verbatim exit-code citation; never derive H3 FAIL from finding-name text alone.

[info] **qc-structural review-file persistence gap** (carried 10+ consecutive runs; **EXERCISED THIS RUN**). Agent wrote review file to `/w/trading/trading/dev/reviews/...` (single underscore) instead of `/__w/trading/trading/dev/reviews/...`. Symbolic link or absolute-path normalisation issue — not workspace isolation. Orchestrator detected via filesystem search and relocated. Recommendation: amend qc-structural agent definition to verify the review file is reachable at `${REPO_ROOT}/dev/reviews/...` before returning, or use `git rev-parse --show-toplevel` to anchor the path.

[info] **Stale per-track status files** (carried 5 runs, materially improved). PR #826 fixed 14 files in one pass (drops advisory integrity FAILs from 6 → 2). Remaining drift: `weekly-snapshot.md` (2 advisory FAILs); `tuning.md` claims READY_FOR_REVIEW after T-B (#817) merged; `experiments.md` claims #769 awaiting review (merged); `simulation.md` claims P0 RED on `split_day_stop_exit:1:post_split_exit_no_orphan_equity` (resolved by #752).

[info] **magic_numbers linter false-positives on docstring text** (carried 4 runs). Year / PR-number citations like `2019`, `2026`, `744`, `103.58` appearing inside `(* ... *)` comments are flagged as bare numerics. Linter-side fix candidate; harness backlog.

[info] **F.3.b/c/d plan-first opportunity** (NEW). Once #829 lands, the next 3 sub-PRs of F.3.a-class deletion (Weekly_ma_cache + Panel_callbacks + Macro_inputs ports) are eligible for plan-first dispatch per Step 3.5 trigger 2 (cross-cutting change touching > 5 files). Estimated 800–1200 LOC across 3 PRs. Suggest writing `dev/plans/snapshot-engine-phase-f3-bcd-2026-05-XX.md` plan file as the next dispatched-on-data-foundations run's first commit. Does not require human input; flagged here so the next run picks it up without re-discovery.

## Step 1b drift cross-reference

Most recent prior summary on local FS / on main: `dev/daily/2026-05-03-run2.md` (run-2; committed via PR #806 at 2026-05-03T13:42Z; merged to main).

Run-2's `## Pending work` table parsed:
- All rows were classified as `dispatched + merged this run`, `dispatched (no PR opened — duplication)`, `merged-on-content`, `not dispatched`, or `blocked` — no `dispatched`/`awaiting-merge` rows to verify against today's state.

Status file commits on main since prior summary commit (PREV_ISO 2026-05-03T13:42Z, excluding `ops:` summary commits): many. Aggregated as a single reconcile paragraph in `_index.md` rather than itemized here.

No `[drift]` warnings emitted (no track had a "dispatched" row in run-2 that would need verification today).

## Step 1c carried-forward critical verification

Run-2 carried zero `[critical]` items into today (run-2 explicitly noted "Carried-forward verification: no still-real `[critical]` items; main green on `bf0e9c3`"). Re-verified live this session on the new HEAD:

```
$ dev/lib/run-in-env.sh dune build
exit=0
$ dev/lib/run-in-env.sh dune runtest --force
exit=0
```

Both exit 0 on pre-dispatch HEAD `9a184d4`. **No `[critical]` items to carry forward.**

[info] Carried-forward verification: no still-real `[critical]` items; main green on `9a184d4`.

All carried-forward `[info]` items from prior runs (qc-structural H3, qc-structural review-file persistence, schema drift, magic_numbers) re-carried into today's §Escalations as observed (with `EXERCISED THIS RUN` annotations on the two qc-structural items that fired today).

## Per-run links

- [run 1] `dev/daily/2026-05-04.md` (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
