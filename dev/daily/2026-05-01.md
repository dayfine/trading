# Status — 2026-05-01
Run timestamp: 2026-05-01T08:14:41Z
Run ID: 2026-05-01-run-1

## Saturated-queue check result

Full pass — Step 0.5 fast-exit declined (Condition 2 failed):
- **Condition 1** PASS — 0 open PRs across all branches; trivially holds.
- **Condition 2** FAIL — `dev/status/screener.md` and
  `dev/status/short-side-strategy.md` both modified since prior summary
  timestamp by non-orchestrator-summary commits (#711, #714, #717, #718).
- **Condition 3** PASS — no `[drift]` warnings from Step 1b (prior
  summary's Pending work table classified all tracks as `blocked`; no
  `dispatched`/`awaiting-merge` rows to verify).
- **Condition 4** PASS — `dev/status/harness.md` and `dev/status/cleanup.md`
  unchanged since prior summary.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| short-side-strategy | blocked | — | — | G14 (split-adjustment on `Position.t` Holding state) — first-pass fix attempt reverted 2026-05-01 ~07:00 UTC; Option-1 recommendation in `dev/notes/g14-deep-dive-2026-05-01.md` (pin everything to raw close-price space + lookback truncation at most-recent-split boundary). Owner: `feat-weinstein` — current scope closed; needs scope-extension decision. |
| short-side-strategy | blocked | — | — | G15 (short-side risk control) — phantom `Portfolio_floor` was unintended risk control; with G12+G13 eliminating spurious cascade, sp500 portfolio goes negative (-66.7% return, 117.5% MaxDD). Real candidates: max-short-notional cap + tighter short stops + honest portfolio floor. Owner: `feat-weinstein` — same scope-extension prereq. |
| short-side-strategy | blocked | — | — | sp500-2019-2023 baseline rerun is local-only (491-symbol universe). Maintainer-local action item. |
| short-side-strategy | blocked | — | — | G4 long-side extension + portfolio-peak threshold (per `dev/notes/session-followups-2026-04-29-evening.md` §2). Touches core `Trading_portfolio`; same A1 question as G14/G15. |
| backtest-perf | blocked | — | — | G6 fix surface is in `trading/orders/lib/create_order.ml:_generate_order_id` — A1 core-module per `qc-structural-authority.md`. Forward-guard test landed via PR #703 (merged 2026-04-30). Other follow-ups: §Decision items (4 human/QC sign-off questions); tier-4 first manual `workflow_dispatch` for canonical baseline (local-only); N≥5000 release-gate stays P1 awaiting daily-snapshot streaming. |
| simulation | blocked | — | — | Split-day work fully wrapped. M5 walk-forward + tuner items remain — could be `feat-backtest` scope per agent definition, but baseline still under active investigation (G14/G15 in flight from maintainer); deferring new architectural scope until baseline stabilises. Maintainer-local sp500 baseline rerun documented. |
| hybrid-tier | blocked | — | — | Option 1 engine-pool DONE 2026-04-28. Option 2 daily-snapshot streaming P1 future work (~3,000 LOC across 5–8 PRs per `dev/plans/daily-snapshot-streaming-2026-04-27.md`); plan-first; not fill-window. |
| harness | blocked | — | — | Backlog drained: T1 done, T2 milestone-gated, T3-C superseded, T3-H low-priority, T4-* future end-state. No T1/T3 dispatchable items. |
| orchestrator-automation | blocked | — | — | Phase 2 pending. Carried [info]s: H3 false-positive on advisory linter text; review-file persistence gap (neither exercised this run since no QC ran). |
| cleanup | blocked | — | — | §Backlog empty; no new `[medium]`/`[high]` findings from deterministic Step 6 health checks this run. |
| cost-tracking | blocked | — | — | Run-1 measured cost will land in `dev/budget/2026-05-01-<run_id>.json` post-GHA-capture. |
| ops-data | skipped | — | — | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel rule); EODHD_API_KEY also unset in GHA env. |
| screener | — | — | — | MERGED. Cascade post-stop-out cooldown gate landed via PR #718. Cascade re-base detection queued in §Followup, not in flight. |
| trade-audit | — | — | — | MERGED. No work this run. |
| optimal-strategy | — | — | — | MERGED. All planned phases landed 2026-04-28..29. |
| backtest-infra | — | — | — | MERGED. Step 1 + Step 2 landed; perf-sweep harness extension (#547) landed 2026-04-25; continuous monitoring + release-gate scope moved to `backtest-perf`. |
| backtest-scale | — | — | — | MERGED. Tiered path entirely deleted in `data-panels` Stage 3 PR 3.3 (#573, 2026-04-26). |
| data-panels | — | — | — | MERGED. Stage 4.5 PR-B (#604) landed 2026-04-27. |
| data-layer | — | — | — | MERGED. |
| portfolio-stops | — | — | — | MERGED. |
| strategy-wiring | — | — | — | MERGED. |
| sector-data | — | — | — | MERGED. |
| support-floor-stops | — | — | — | MERGED. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| — | — | none | No eligible feat-*/harness/ops-data/code-health work this run. The four blocked tracks above (G14, G15, G6, G4 long-side extension) all touch core modules outside current `feat-weinstein` and `feat-backtest` scopes. Per the 2026-04-16 precedent in `dev/decisions.md`, scope extension is a human/architectural decision — escalated this run, not dispatched. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel skip per Step 2d). EODHD_API_KEY unset. |
| harness | — | skipped | No dispatchable T1/T3 items (backlog drained). |
| cleanup (Step 2e) | — | skipped | §Backlog empty AND no new `[medium]`/`[high]` findings from deterministic Step 6 health checks. |

Two small status-file repairs landed in the daily summary PR itself (no
agent dispatch, no behavior change):

1. `dev/status/screener.md` — `## Status` flipped from
   `MERGED (with one optional knob in flight: cascade post-stop-out
   cooldown — PR #718)` to `MERGED` (PR #718 merged 2026-04-30 evening
   via 70f9b2c); `## Last updated` advanced to 2026-05-01; `## In Progress`
   block updated to mark #718 merged. Resolves
   `status_file_integrity.sh` advisory FAIL.
2. `dev/status/short-side-strategy.md` — `## Last updated` schema fix
   (`2026-04-30 (evening)` → `2026-05-01`). Two new §Follow-ups entries
   appended for G14 (item 6) and G15 (item 7) so the status file mirrors
   `dev/notes/g14-deep-dive-2026-05-01.md` + `dev/notes/force-liq-cascade-findings-2026-05-01.md`.
   Resolves `status_file_integrity.sh` advisory FAIL.

## Feature Progress

### short-side-strategy [MERGED — no orchestrator dispatch this run; heavy maintainer cycle since prior summary]

- **Maintainer-driven activity since 2026-04-30 13:05Z** (21 PRs):
  G7+G9 force-liq fixes (`#710 G9 mirror`), sp500 with shorts re-enabled
  (`#711` — 32 trade pin), reconciler producer CSVs (`#712`), summary
  win/loss alignment (`#713`), G11 stop_update_cadence flag (`#714`),
  long-side cycle-gate test pin (`#716`), End_of_period exit trigger
  (`#717`), screener cascade post-stop-out cooldown (`#718`),
  warmup-window cleanup (`#720` stop_log + `#722` trade_audit + `#724`
  force_liquidations), G12 (`#725` consistent positions/cash snapshot),
  summary metrics in-window overlay (`#723`), G13 (`#726` non-trading-day
  short-circuit), G14 deep-dive + first-pass-attempt notes (`#727`,
  `#729`), goldens-sp500 long-only counterpart (`#728`).
- **Cross-cycle headlines:** G12+G13 structurally fixed two phantom-peak
  pathways (820 spurious force-liqs → 5 residual); G14 first-pass attempt
  reverted (architectural mismatch — splitting screener prices alone
  breaks symbols with future splits e.g. CTAS 2024 4:1); G14 Option-1
  recommendation filed (raw price space + lookback truncation at
  most-recent-split boundary); G15 risk-control gap newly surfaced
  (sp500 -66.7% return / 117.5% MaxDD post-G13 because phantom floor was
  acting as unintended risk control).
- **Outstanding non-rerun follow-ups** unchanged surface: G14, G15, G4
  long-side extension. All touch core (`weinstein_strategy.ml`,
  `position.ml`, `Trading_portfolio`); same scope-extension question as
  prior runs.

### backtest-perf [IN_PROGRESS — no work this run]

- **PR #703** (G6 forward-guard test) merged 2026-04-30 12:29Z via
  prior run's dispatch.
- G6 fix surface (`trading/orders/lib/create_order.ml:_generate_order_id`)
  is unchanged: still in core orders module, still blocked on the same
  scope-extension question that gates G14/G15/G4. Carried [info] for the
  third consecutive run.

### simulation [IN_PROGRESS]

- All split-day broker-model work merged on main. M5 walk-forward + tuner
  items remain.
- Could plausibly be dispatched as a `feat-backtest` plan-first item
  (parameter tuner is in agent scope), but actively-debugged baselines
  (G14/G15 in flight from maintainer) make this the wrong run to add new
  architectural scope. Defer until baseline stabilises post-G14 fix.

### harness [IN_PROGRESS]

- No backlog dispatch this run — T1 done, T2 milestone-gated, T3-C
  superseded, T3-H low-priority. No maintainer-driven harness PRs since
  prior summary either; the harness work has been quiet for one
  full cycle.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| (none this run — no PRs awaiting QC; no dispatches) | | | | | |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **0**
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | (none — no subagent dispatches) | — | — | — | — |
- Any subagent killed mid-flight: NA.

**Measured cost (from GHA execution file):** Will land in
`dev/budget/2026-05-01-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$1 of $50 cap (~2%) — orchestrator overhead only
  (Read/Edit/Bash/curl tool calls).
- Utilization assessment: **UNDER_TARGET** (<60%) — actionable-work-floor
  with the same shape as 2026-04-30 run-1 + run-2: all dispatchable surface
  requires architectural scope-extension decisions or is local-only or is
  too large for fill-window dispatch (Option 2 streaming ~3,000 LOC).
  Status: "all-work-done" within current scope — no eligible track was
  skipped for cost or queue-depth reasons.
- Scope reduced due to budget: No — under-target due to actionable-work-floor.

## Data Operations

(ops-data did not run — `dev/notes/data-gaps.md` substantively unchanged
since 2026-04-14, EODHD_API_KEY unset.)

## Harness Work

(harness-maintainer did not run this run — backlog drained; no
dispatchable T1/T3 items. No maintainer-driven harness PRs since prior
summary.)

## Health Scan

From `dev/health/2026-05-01-fast.md` (deterministic per Step 6):
- `dune build` exit **0** (PASSING).
- `dune runtest` exit **0** (PASSING).
- `status_file_integrity.sh`: PASSING (exit 0) **after orchestrator
  fixes this run**. Initial run reported 2 advisory FAILs:
  `screener.md` `## Status` prose suffix + `short-side-strategy.md`
  `## Last updated` prose suffix. Both repaired in the same daily
  summary PR; rerun confirms `OK: all dev/status/*.md files have
  required fields.`
- Result: **CLEAN** (build gate); 2 schema drifts repaired in-flight.

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~9 (up 2
since prior summary; G14 + G15 added).

- **G14 — split-adjustment on `Position.t` Holding state** (NEW
  2026-05-01): Option-1 fix recommended (pin everything to raw
  close-price space + lookback truncation at most-recent-split
  boundary); first-pass attempt reverted (architectural mismatch).
  Owner: `feat-weinstein`. See `dev/notes/g14-deep-dive-2026-05-01.md`.
- **G15 — short-side risk control** (NEW 2026-05-01): real risk surface
  inadequate now that phantom `Portfolio_floor` no longer acts as
  unintended risk control. Candidates: max-short-notional cap, tighter
  short stops, honest portfolio floor. Owner: `feat-weinstein`.
- **G6 fix dispatch** (carried 3rd consecutive run):
  `trading/orders/lib/create_order.ml:_generate_order_id` mints order
  IDs with ns-precision timestamp prefix → Hashtbl bucket variance →
  fill order divergence. Out of scope for `feat-backtest`; needs
  core-owner agent.
- **G4 long-side extension + portfolio-peak threshold** (carried 3rd
  consecutive run): touches core `Trading_portfolio`; same A1
  question.
- **sp500 short-side validation rerun** (post-G7+G9 baseline): local-only
  (491-symbol universe).
- **covid-recovery 20.81% WR investigation** (deferred until short-side
  baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**:
  carried, not exercised this run (no QC).
- **qc-structural review-file persistence gap**: carried, not exercised
  this run.
- **Schema drift from maintainer-driven PRs** (NEW [info]): twice in
  the last two summaries the orchestrator has had to patch
  `status_file_integrity.sh` advisory failures rooted in maintainer-
  written status-file edits that include parenthetical context
  (`MERGED (with ...)`, `2026-04-30 (evening)`). The integrity linter
  could become hard-gating, OR the maintainer prose could move to a
  free-form `## Notes` section that the linter ignores. Worth surfacing
  in `dev/status/harness.md` §Follow-up next time the harness backlog
  has slack.

## Integration Queue

- (none — no PRs awaiting human merge this run; the daily summary PR
  produced by Step 8 is the only thing this run pushes.)

## Current Milestone Target

M5 (backtest infrastructure complete + tunable). No incremental progress
this run from orchestrator dispatch. Maintainer-driven progress in the
last 24 hours: G12+G13 structural force-liq cascade fixes; G14
diagnosis + Option-1 recommendation; G15 risk-control gap surfaced;
goldens-sp500 long-only counterpart added (`#728`); screener cascade
post-stop-out cooldown gate (`#718`).

The next sequential milestone moves require:
- ⏭ G14 fix (Option 1 — strategy/position/screener — needs scope decision).
- ⏭ G15 fix (strategy-level risk control — needs scope decision).
- ⏭ G6 fix dispatch (core orders module — same).
- ⏭ G4 long-side extension (core `Trading_portfolio` — same).
- ⏭ sp500 short-side baseline rerun (local-only, post-G14 fix).
- ⏭ Daily-snapshot streaming (Option 2 of hybrid-tier; ~3,000 LOC plan-first).

## Dependency Unlocks

- (none new this run — the maintainer-driven cycle landed; no `Interface
  stable: YES` flips that unblock other tracks.)

## Escalations

[info] **G14 — split-adjustment on `Position.t` Holding state**
(NEW 2026-05-01). All 5 residual force-liqs in the post-G13 sp500
baseline are `Per_position` triggers on positions with stale
pre-split `entry_price` (PANW 3:1 Sep 2022, GOOG 20:1 Jul 2022, ALGN +
DASH + TECH within-window highs). First-pass attempt
(`feat/g14-screener-adjusted-prices`) reverted because Bug A
(split-adjusting screener high/low scans) cannot be fixed alone —
symbols with FUTURE splits (e.g., CTAS 2024 4:1) get
`suggested_entry` in adjusted price space while
`Position.t.entry_price` is set in raw space, producing 250%+
phantom losses on the post-fix path. Recommended fix:
**Option 1 — pin everything to raw close-price space**, with lookback
truncation at most-recent-split boundary. Touches
`entry_audit_capture.ml:80`, `position.ml`, `simulator.ml`, screener
high/low scans. Owner: `feat-weinstein` — current scope is closed;
needs scope-extension decision per the 2026-04-16 precedent before
dispatch.

[info] **G15 — short-side risk control** (NEW 2026-05-01). With G12
(#725) + G13 (#726) eliminating the spurious `Portfolio_floor`
cascade, `goldens-sp500/sp500-2019-2023` portfolio goes negative
(-$175K minimum 2021-11-04, -66.7% return, **117.5% MaxDD**). The
phantom floor was acting as an unintended risk control. Real
candidates: (a) max total short notional as fraction of portfolio;
(b) tighter per-position short stop-loss threshold (Weinstein
recommends tighter stops on shorts than longs); (c) honest portfolio-
floor based on actual peak observations (now correct post-G13).
Combination of (a) + (b) likely needed. Owner: `feat-weinstein` —
same scope-extension prereq as G14.

[info] **G6 fix surface is in `trading/orders/lib/create_order.ml`
(core module)** (carried 3rd consecutive run). Process question:
which feat-agent should own a core-module fix when neither
`feat-backtest` nor `feat-weinstein` has a current scope covering
`trading/orders/`? Two design choices: (a) replace the timestamp prefix
with a deterministic per-process counter (cheapest, but loses the
"human-readable timestamp in ID" property); (b) replace `Hashtbl`
with a sortable structure (Map.t) and have `list_orders` return in
deterministic order regardless of ID. Forward-guard test (PR #703)
is on main pinning the post-fix invariant.

[info] **G4 long-side extension + portfolio-peak threshold** (carried
3rd consecutive run). Same core-module question — touches
`Trading_portfolio`. Sequencing prereq satisfied; remaining blocker
is the scope-extension decision.

[info] **qc-structural recurring H3 false-positive on advisory linter
text** (carried 4+ consecutive runs). Permanent fix still pending:
tighten the agent's prompt to explicitly cite Step 6.1 of
`lead-orchestrator.md` ("trust only the exit code, not stdout `FAIL:`
strings") and require running `dune runtest 2>&1 | tail -3 ;
echo "exit=$?"`. Did not trigger this run (no QC dispatched).

[info] **qc-structural review-file persistence gap** (carried 4+
consecutive runs). Did not recur on prior run's last-known PR review;
not exercised this run.

[info] **Schema drift from maintainer-driven PRs** (NEW [info]).
Twice in the last two daily-summary cycles the orchestrator has had
to repair `status_file_integrity.sh` advisory failures rooted in
maintainer-written status-file edits that include parenthetical
context. Two options: (a) make the integrity linter hard-gating
(currently advisory — exit 0 with FAIL prints); (b) move free-form
maintainer prose to a `## Notes` section the linter ignores.
Pre-decision; surfacing for the next harness slack window.

[info] **Cumulative day spend low (~2% of $50 cap)**. Same shape as
the prior two runs: all eligible GHA-runnable work dispatched in
prior maintainer cycles; orchestrator's actionable-work-floor is set
by the four pending scope-extension decisions (G14, G15, G6, G4) +
local-only items (sp500 rerun) + plan-first ~3,000 LOC items (Option
2 streaming). Surfacing as `[info]` (not `[critical]` or `[high]`)
because it reflects an actionable-work-floor, not a budget-skip
decision.

## Step 1b drift cross-reference

Most recent prior summary: `dev/daily/2026-04-30-run2.md` (committed
via `ops/daily-2026-04-30-run2` branch — verified present on local FS
mtime 2026-05-01T08:03:32Z, content reads as run-2 of 2026-04-30).
Pending work table classifies all tracks as `blocked` — no
`dispatched` or `awaiting-merge` rows to verify.

No `[drift]` warnings emitted.

## Step 1c carried-forward critical verification

Prior summary (2026-04-30 run-2) carried zero `[critical]` items into
this run (all `[info]`). Build + integrity gates re-verified clean
on this run (build exit 0; integrity exit 0 after orchestrator's
in-flight schema repairs). This run carries forward zero `[critical]`
items.

[info] Carried-forward verification: no still-real `[critical]` items;
main green (exit 0 on `7a8e778`).

## Per-run links

- [run 1] dev/daily/2026-05-01.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
