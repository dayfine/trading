# Status — 2026-09-25

**Run ID:** 2026-09-25-run1 (GHA orchestrator run 36137000433)
**Run timestamp:** 2026-09-25T12:52Z
**Mode:** FULL
**Main at run start:** `2b0b37af`. **Main after this run's merges:** `8dc31171`.

## Headline

Two harness PRs were dispatched and both merged inside this run. The queue is empty again.

- **#2955** (issue #2876): the file-length linter now scans every non-test `.ml` repo-wide, not just `*/lib/*.ml`. It took one rework and merged as `ed6e89a6`.
  - 21 files became visible for the first time: 9 over the 500-line hard limit and 12 in the 300–500 band.
  - Each got a deliberate disposition: 2 new `@large-module` markers, 1 marker that already existed, and 18 dated exceptions (`review_at` 2026-11-09 or 11-30), each with a matching `cleanup.md` §Backlog row. No limit was changed.
  - It also fixed a latent bug: `linter_exceptions.conf` was not declared as a dune dependency, so exceptions did not apply under the `dune runtest` sandbox.
- **#2956**: the intent-to-add pollution behind yesterday's `-run2`→`-run3` counter flip is root-caused and fixed. It took two reworks and merged as `8dc31171`.
  - **Cause:** `trading/devtools/checks/jj_workspace_smoke.sh` runs `jj workspace add/list/forget` against the jj-colocated repo root. That exports the caller's untracked files into the git index as ` A` (intent-to-add) entries.
  - **Fix 1:** a snapshot/restore guard around the smoke check. It restores the exact staged blob via `update-index --cacheinfo`.
  - **Fix 2:** `current_summary_path` now counts only files committed in `HEAD` (`git ls-tree`).
  - **Reproduced live this run:** my own Step 6 `dune runtest` on main turned this run's untracked summary and audit files into ` A` entries. I undid it with `git reset -- dev/audit dev/daily` before publishing.

**Neither run-loss failure mode recurred.** I never ended the turn while an agent was outstanding: every wait was a bounded foreground poll against the GitHub API. Three agents that went quiet were nudged or confirmed by API before moving on.

## Step 0.5 — fast-exit skipped

`QUEUE_NON_EMPTY=0`: zero open PRs, so the queue was empty, not saturated. Per the #2579 precondition I did not evaluate the four conditions.

## Step 1b — drift cross-reference

The prior summary is `dev/daily/2026-09-24-run2.md`. Every row in it resolved to a merged PR (#2949, #2950, #2951). Since then the maintainer merged #2952 (the summary), #2953 (`dev/budget/*.json` treated as docs-only) and #2954 (spend records). **No `[drift]` warnings.**

## Step 1c — carried-forward verification

Run 2 on 09-24 carried no `[critical]` items. Main CI at `2b0b37af` was green: build-and-test, perf-tier1-smoke, golden-sp500-5y, golden-custom-universe and perf-tier2-nightly all succeeded.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | auto-merged | harness/file-length-scope-2876 | #2955 | auto-merged (all 3 gates green) — Step 6.5 |
| harness | auto-merged | harness/intent-to-add-counter | #2956 | auto-merged (all 3 gates green) — Step 6.5 |
| cleanup | blocked | — | — | 18 new `file_length` backlog rows; next run's Step 2e should dispatch the largest first |

## Dispatched this run

Dispatch conditions:
- **Disk:** `dispatch_disk_guard.sh 2` → OK (61 GB free vs a 52 GB floor).
- **Isolation:** every agent got its own orchestrator-created `git worktree` (#2470), removed as soon as that agent finished.
- **Dune:** every dune call ran behind `flock /tmp/dune.lock`, one at a time.

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | harness-maintainer | completed — PR #2955 | #2876; stalled once waiting on a Monitor for dune; nudged with a foreground recipe and finished |
| harness | harness-maintainer | completed — PR #2956 | intent-to-add root cause + counter hardening |
| harness | qc-structural | APPROVED (5) | #2955 @ `b55d30bc` |
| harness | qc-behavioral | NEEDS_REWORK (2) | #2955: M5 (the issue's lib/bin/scripts allowlist), M7 and M8 survived; fixtures H/I/J requested |
| harness | harness-maintainer | completed — rework iter 1 on #2955 | fixtures H/I/J; M5/M7/M8 now RED |
| harness | qc-structural | APPROVED (5) | #2955 @ `517940fa` |
| harness | qc-behavioral | APPROVED (5) | #2955 @ `517940fa`; M1–M8 all killed |
| harness | — | merged | #2955 → `ed6e89a6` (Step 6.5) |
| harness | qc-structural | APPROVED (5) | #2956 @ `da4db49c` |
| harness | qc-behavioral | NEEDS_REWORK (2) | #2956: "restore only undoes what the check added" unpinned (M4 survived) |
| harness | harness-maintainer | completed — rework iter 1 on #2956 | Part 4 fixture. It also found `jj workspace forget` reshapes a really-staged file into ITA; guard fixed |
| harness | qc-structural | APPROVED (5) | #2956 @ `4b54f8da` |
| harness | qc-behavioral | NEEDS_REWORK (2) | #2956: a staged-then-edited (`AM`) file was unstaged by the guard, contradicting its docstring |
| harness | harness-maintainer | completed — rework iter 2 of 2 on #2956 | exact-blob restore via `update-index --cacheinfo`; `AM` fixture; PR body PATCHed |
| harness | (orchestrator) | merge of origin/main into #2956 | the branch went `dirty` after #2955 merged; additive conflict in `dev/status/harness.md` only; `devtools/checks` runtest rc=0 before push |
| harness | qc-structural | APPROVED (5) | #2956 @ `a970d980` (nudged once; no dune running and no review posted) |
| harness | qc-behavioral | APPROVED (5) | #2956 @ `a970d980`; 11/11 mutations killed |
| harness | — | merged | #2956 → `8dc31171` (Step 6.5) |
| cleanup | — | skipped | §Backlog had no dispatchable item at run start (see the 09-09 audit note); #2955 has since added 18 `file_length` rows for next run |
| ops-data | — | skipped | `data-gaps.md` unchanged; `EODHD_API_KEY` still needed for #2915 part 1 |
| feature tracks | — | none eligible | every IN_PROGRESS feature row is LOCAL-fenced, data-gated or human-gated (see the `_index.md` standing constraints) |

## QC Status

| Track | PR | reviewed tip | CI | structural | behavioral | outcome |
|---|---|---|---|---|---|---|
| harness | #2955 | `b55d30bc` | pass | APPROVED (5) | NEEDS_REWORK (2) | reworked |
| harness | #2955 | `517940fa` | pass | APPROVED (5) | APPROVED (5) | auto-merged |
| harness | #2956 | `da4db49c` | pass | APPROVED (5) | NEEDS_REWORK (2) | reworked |
| harness | #2956 | `4b54f8da` | pass | APPROVED (5) | NEEDS_REWORK (2) | reworked (iter 2) |
| harness | #2956 | `a970d980` | pass | APPROVED (5) | APPROVED (5) | auto-merged |

All three NEEDS_REWORKs followed the pattern of the last several runs: a claim in the docstring or PR body that no test defended. Behavioral reviewers found each one by mutating the code. The #2956 iteration-1 rework also exposed a **real** defect in the first guard, not just a missing test: `jj workspace forget` reshapes a staged file into ITA, and the original restore then unstaged it.

## Integration Queue

**Empty.** Both PRs were auto-merged by Step 6.5.
- **Hold check** (Rule 0): the `do-not-merge` label and `convert_to_draft` timeline, checked before and again immediately before each merge. CLEAR both times.
- **Merge call:** `PUT /merge` with `sha` pinned to the reviewed tip.

## Audit records

Written via `write_audit.sh --sha` on every call; the `-prev<ts>-` records preserve the NEEDS_REWORK iterations.

- `2026-09-25-harness-file-length-scope-2876-prev…-harness.json`: NEEDS_REWORK @b55d30bc
- `2026-09-25-harness-file-length-scope-2876-harness.json`: APPROVED @517940fa (count reset to 0)
- `2026-09-25-harness-intent-to-add-counter-prev…-harness.json` (×2): NEEDS_REWORK @da4db49c, @4b54f8da
- `2026-09-25-harness-intent-to-add-counter-harness.json`: APPROVED @a970d980 (count reset to 0)

## Budget

- Cap: **$200** (`max_daily_cost_usd`), a runaway backstop on a flat-rate subscription.
- Subagent sessions: **15**. That is 2 implementations, 3 reworks, 5 qc-structural and 5 qc-behavioral. Two stalled agents were resumed with a nudge and are not counted separately.
- Subagent tokens (measured, as reported per agent): **≈ 2.3 M**. The largest were the #2876 implementation (274 k), the #2956 implementation (241 k) and #2956 rework 2 (185 k).
- Measured cost: written after the session by the workflow's cost step to `dev/budget/2026-09-25-36137000433.json`. It is not stated here, to avoid an unlabelled estimate.
- Any subagent killed mid-flight: **No.**
- Utilization: resource-bound, not budget-bound: one dune at a time, and the rework loop is serial. **All eligible, dispatchable work was done**: two harness items, no cleanup backlog at run start, and feature tracks fenced.

## Health Scan

Full report: `dev/health/2026-09-25-fast.md`. **Result: CLEAN. Critical items: none.**

```
BUILD_EXIT=0      (dune build, main @ 2b0b37af)
RUNTEST_EXIT=0    (dune runtest; 0 ^FAIL: lines)
status_file_integrity exit=0
index_size_linter OK (16230/20480)
```

## Scheduled workflows

FINDINGS (exit 1): RED/STALE workflows and newest_completed_run_id below.

    scheduled_workflow_health: repo=dayfine/trading stale_hours=216 runs_per_workflow=10
    NO-SCHEDULE-CROWDED	CI Main Watchdog	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    NO-SCHEDULE-CROWDED	CI	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    OK	Dependency freshness check	run_id=35615199970 status=completed conclusion=success created_at=2026-09-21T14:53:29Z age_hours=94 streak=0 in_progress=0 newest_completed_run_id=35615199970
    NO-SCHEDULE-CROWDED	golden-runs-custom-universe	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    OK	golden-runs-sp500-15y	run_id=36009564166 status=completed conclusion=success created_at=2026-09-24T13:59:51Z age_hours=22 streak=0 in_progress=0 newest_completed_run_id=36009564166
    NO-SCHEDULE-CROWDED	golden-runs-sp500-5y	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    NO-SCHEDULE-CROWDED	goldens-affected	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    OK	Weekly deep health scan	run_id=35648266280 status=completed conclusion=success created_at=2026-09-21T19:59:38Z age_hours=88 streak=0 in_progress=0 newest_completed_run_id=35648266280
    OK	Build CI image	run_id=35600171824 status=completed conclusion=success created_at=2026-09-21T12:32:56Z age_hours=96 streak=0 in_progress=0 newest_completed_run_id=35600171824
    NO-SCHEDULE	IWV Scrape (one-shot)	(no scheduled runs observed)
    OK	Daily orchestrator	run_id=36137000433 status=in_progress conclusion=null created_at=2026-09-25T12:47:41Z age_hours=19 streak=0 in_progress=1 newest_completed_run_id=36032653844
    OK	perf-nightly	run_id=36121176058 status=completed conclusion=success created_at=2026-09-25T09:55:52Z age_hours=2 streak=0 in_progress=0 newest_completed_run_id=36121176058
    NO-SCHEDULE-CROWDED	perf-tier1	(page of 50 runs held no scheduled run -- crowded; raise SCHEDULED_WF_HEALTH_RUNS_PAGE_FACTOR)
    OK	perf-weekly	run_id=35605439371 status=completed conclusion=success created_at=2026-09-21T13:24:45Z age_hours=95 streak=0 in_progress=0 newest_completed_run_id=35605439371
    RED	Prune candidates weekly	run_id=35650912772 status=completed conclusion=failure created_at=2026-09-21T20:24:56Z age_hours=n/a streak=5 in_progress=0 newest_completed_run_id=35650912772
    OK	Weekly track pacer	run_id=35506154608 status=completed conclusion=success created_at=2026-09-20T10:49:59Z age_hours=122 streak=0 in_progress=0 newest_completed_run_id=35506154608
    OK	Weekly start sweep (BAH SPY)	run_id=35641922377 status=completed conclusion=success created_at=2026-09-21T18:59:10Z age_hours=89 streak=0 in_progress=0 newest_completed_run_id=35641922377
    SUMMARY: active=17 (1 page(s) fetched, full pagination -- a real total, not a floor) ok=9 red=1 stale=0 no-schedule=1 no-schedule-crowded=6 unobservable=0 -- red/stale/ok/unobservable are each computed from up to 10 most-recent scheduled runs per workflow (not just the newest); red includes a per-workflow failure-streak count (see each RED line's streak=N, or streak=>=N when the fetched page was exhausted before the streak closed); no-schedule-crowded counts workflows whose unfiltered runs page came back full while holding zero scheduled runs -- UNOBSERVABLE-like (not measured-OK, never forces a non-zero exit), kept out of the no-schedule bucket so it is never read as a clean 'no cron here'
    SUMMARY: RED workflows: Prune candidates weekly(streak=5)

**Reading of this table:**
- **`Prune candidates weekly` RED streak=5** is still #2847 (`gh pr create` in an image without `gh`), blocked on `workflow` scope.
- **`Daily orchestrator` is OK again.** The 09-24 run 2 completed successfully, which closed the prior streak=2.
- **Six workflows now read `NO-SCHEDULE-CROWDED`, where the 09-24 run showed plain `NO-SCHEDULE`.** That is #2950's new class, working as designed. PR and push traffic fills their 50-run page, so "no cron" cannot be confirmed from one page. None of the six has a cron, so this is not a finding.

## Follow-up Queue

- **cleanup:** 18 new `file_length` backlog rows from #2955 (`release_report.ml` 1005 lines, `trade_audit_ratings.ml` 1001 lines, and so on). Their dated exceptions expire 2026-11-09 and 11-30, so Step 2e should work them down one per run from the next run on.
- **harness:** two non-blocking #2956 follow-ups were added to `harness.md`: H-ITA-GUARD-QUOTED-PATHS (use `-z` porcelain) and H-ITA-GUARD-SILENT-RESTORE-FAIL (print a `WARN:` line).

## Escalations

- **`[info]` Carried-forward verification:** no still-real `[critical]` items; main is green (exit 0 on `2b0b37af`).
- **`[info]` Issue #2632 is fixed on main but still open.** The fix is #2654 (`bf552095`, the `Window_filter` guard, cited at `runner.ml:372`). The orchestrator token cannot close issues (`PATCH /issues` returns 403), so please close it by hand, along with #2921 (from the 09-24 summary).
- **`[info]` #2876 was closed automatically** by #2955's `Closes` line.
- **`[info]` Agents still sometimes wait on Monitor or background events** that never reach a subagent. It happened twice this run: #2876 impl, and #2956's final structural re-review. Each was recovered with one nudge. The brief already says "never wait on a background task", so the durable fix is the #2738 event-wait helper, or putting the rule into `.claude/agents/*.md`, which this runtime cannot write.
- **`[info]` A merge into a stale branch was done from the dispatcher side.** #2956 went `dirty` once #2955 merged, and GitHub runs no `pull_request` CI on a conflicted PR. The conflict was additive (both PRs appended a `harness.md` section). I resolved it with a merge commit, and both QC gates then re-reviewed at the new tip.

## Questions for You

1. #2955 turned an unobserved debt into 18 dated exceptions expiring in November. Should the orchestrator work the cleanup backlog at more than 1 per run until then? At the Step 2e cap of 1 per run and 2 runs a day, 18 items take about 9 days, which is fine unless runs get skipped.

---
## Your Response
(Edit this section.)
