# Status — 2026-08-28 (run 2)

**Run ID:** 2026-08-28-run-2
**Run timestamp:** 2026-08-28T16:29Z–~18:00Z (GHA orchestrator run 33189885747)
**Mode:** FULL
**Main:** `5c20fe07` → **`409b85f4`** (#2589)
**Baseline gate, post-merge:** `dune build` **0**, `dune runtest` **0**, **0** `^FAIL:` lines

## Headline

**A fix merged at 05:10 was silently reverted by a PR cut at 07:48, and every
gate was about to pass.**

Unlike run 1 (empty queue), the maintainer opened **#2587** — the
`entry_order_max_rest_weeks` `0 → 52` promotion — at 14:49Z with **red CI and
zero reviews**, so the Step 0.5 fast-exit correctly did not fire. Fixing its CI
turned up the run's real finding.

#2587 branched from `cbac374f`, which is the **direct parent** of `b1f10412`
(#2584, merged 05:10 PT the same morning). So the branch never contained #2584's
odoc fixes, and its docstring rewrite carried the pre-fix text forward,
**un-escaping 2 of that PR's 18 `\{...\}` sequences, 2h38m after they landed.**

Neither PR gate caught it, for two independent reasons:

- `ci.yml:104` is `dune runtest`; **line 109** is a *separate* `dune fmt (check
  only)` step. `dune runtest` failed first on an unrelated unpinned default, so
  **step 109 never executed** — the fmt regression was invisible inside a red-CI
  log that appeared to have one, unrelated cause.
- After the author's own `dune fmt` promotion, `@fmt` went **green without
  restoring the escapes**. There is no odoc linter in `trading/devtools/checks/`.

So the revert was about to merge fully green. Restored in `5044eb5f`; the general
class — *a fix merged between a PR's branch point and its CI run, silently
reverted, with no gate noticing* — is filed as **#2588**.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | auto-merged | `harness/2585-residuals-r1a-r5` | #2589 | merged `409b85f4`; next is **O2**, the one green-while-broken evasion QC found |
| simulation | **blocked (NEEDS_REWORK)** | `config/clock-52-2405` | #2587 | **human**: run the 12 paired goldens, paste the table, apply `paired-run-done`; decide G2 |
| all others | not dispatched | — | — | see §Dispatch decisions |

**One open PR at run end (#2587), deliberately not merged.**

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| simulation | feat-weinstein | completed | fixed #2587's red `build-and-test`; **detected the human's mid-run force-push and refused to force over it** |
| harness | harness-maintainer | completed | R-1a + R-5; 5-step mutation transcript against the real production script |
| harness | qc-structural | APPROVED (4) | #2589 @ `feeb193f`; reproduced 3 mutations independently |
| simulation | qc-structural | APPROVED (5) | #2587 @ `5044eb5f`; **over-credited B1/B2 — corrected, see below** |
| harness | qc-behavioral | APPROVED (4) | #2589; **13 independent attacks**, 12 caught or fail-closed |
| simulation | qc-behavioral | **NEEDS_REWORK (2)** | #2587; B1 + B2 + G2 FAIL |
| cleanup | — | skipped | capacity: 4-core runner, load hit 10.8 with 2 agents; PR-gate work outranks (`container-capacity-scheduling.md`) |
| ops-data | — | skipped | `data-gaps.md` unchanged **117 days**; `EODHD_API_KEY` absent |

**6 agent dispatches, 0 stalls, 0 lost work.** Never more than 2 concurrent.
Every agent ran in its own `git worktree` created before dispatch (#2470); the
shared checkout was never used for agent work.

## The QC findings, in detail

### #2587 — three FAILs, and a reviewer who overruled the reviewer above it

qc-behavioral **adjudicated against qc-structural** on B1/B2 and agreed with my
correction. qc-structural had credited `config-default-blast-radius.md` B1/B2 as
satisfied by the presence of `run-goldens-52.sh`, calling the pairing "implicit
in the test names". Behavioral's finding is sharper than mine was:

> The script **cannot have run** on any shared runner: it hardcodes
> `REPO=/Users/difan/Projects/trading-1` and `docker exec trading-1-dev`, and
> pins `PIN=5dc61da07`, which is not this tip.

Zero golden results are committed. B2 is not merely single-arm — **neither** arm
has numbers, and `sp500-2019-2023-armed-stoplimit` (the exact golden that
measured **−40.91pp** on 2026-08-19) is first in the script's queue with no
result. It also credited what the script gets right: its "the current pins ARE
the old arm, so one run per cell + compare-to-pin = the paired table" argument is
sound and halves the runtime.

**G2 is the substantive one.** The ledger's own notes say, verbatim, *"a
composition-independent cell (different PIT vintage/breadth) **remains open for
the flip PR**."* This is the flip PR; it does not supply that cell. Supporting:
the grid's universe axis is **confounded with its period axis** (cell B is the
only different vintage *and* the only different period); an explicitly
*path-dominated* mechanism was measured at **salt 0 only**, with **no null
measured for cells B or C at all**, while cell B's margin is +7.3pp on a
187-trade book; and the **pre-registered confirmatory test did not fire as
written** (the removed-fills cohort came out net-**positive** +$1.09M at w26).

Crucially, it was precise about what it was *not* saying: `promotion-confirmation.md`'s
decision rule **is** satisfied — 52 is top on return, sharpe *and* maxDD in all
three cells, which the reviewer **re-derived digit-for-digit from the committed
`results/*-actual.sexp` rather than reading off the ledger prose**. ACCEPT(mechanism)
and "robust value = 52" are well-earned. The FAIL is that the flip *executes*
while a precondition the ledger wrote for this specific PR is still open.

### #2589 — 13 attacks on "is this guard real?"

The PR's whole value is the claim that its new assertions are non-vacuous — the
claim the last four reviews here found false when tested. The reviewer mutated
the **real** production script rather than reproducing the author's transcript:

| direction | result |
|---|---|
| gut `add_warning()`; drop its argument; rename the `W:` prefix | all **CAUGHT** |
| retarget `add_warning` → `add_info`, identical text, **with a decoy `W:` line present** | **CAUGHT** — the `^W:` anchor + field-name content defeats "a W: line exists" vacuity |
| three benign rewordings of the call site (**the #2580 vacuous-green shape**) | all **RED, fail-closed** |
| three fixture tamperings | all **CAUGHT** with correct diagnosis |
| **E2: per-label special-case inside the shared function** | **NOT caught — exit 0** |

It also verified the guard is red **through dune**, not just by hand, and noted
the rule carries `(universe)` so dune's cache cannot hide it — the exact failure
mode `code-health-discipline.md` records. Both of the author's self-declared
limits were checked and found **accurate**; the real uncovered set is two
branches wider. Filed as **O1–O5** in `harness.md`.

## Corrections and checks on my own work

- **I fabricated a SHA into a durable record.** qc-structural returned only the
  short `feeb193f`; I expanded it into `feeb193fbc1de…` — a plausible-looking
  40-hex string that **does not exist** — and posted it to #2589 and wrote it to
  `dev/reviews/`. This is exactly the defect 08-27 logged as `[low]`, and worse
  than garbled: a valid-shaped non-matching sentinel makes `pr_gate_status.sh`
  read the verdict as **stale forever**. Caught by checking the *behavioral*
  agent's SHA against the tree — it was correct and mine was not. Corrected in
  the file and in a posted correction on the PR.
- **I got the fmt finding backwards, twice, before getting it right.** I first
  "confirmed with a control" that `fmt_check.sh` false-greens; then decided my
  `/tmp` scratch-dir probe was the artifact and announced the opposite; the truth
  is that `dune build @fmt` **does** exit 1 on the PR tip while `fmt_check.sh`
  exits 0, and I could **not** pin the mechanism. #2588 records the gate
  disagreement as a measured fact and the cause as **unresolved**, rather than
  guessing. Stated here because two of my intermediate claims were wrong.
- **I over-trusted a linter through a pipe.** After reconciling `_index.md` I read
  `index_size_linter` as exit 0 — that was `tail`'s status. Unpiped it was **1**
  (a 265-char row against a 250 cap). The same `| tail` trap I brief every agent
  against, and the same one run 1 hit. Fixed, then re-verified unpiped.
- **qc-structural's B1/B2 credit** — I recorded my disagreement in the posted
  review as a clearly-labelled orchestrator correction rather than letting the
  reviewer's text stand, then asked behavioral to adjudicate **independently**
  and told it to say so if I was wrong. It agreed with me; had I not asked
  neutrally, that agreement would be worth much less.
- **I did not merge #2587**, despite having the permission and a structural
  APPROVED. Behavioral said NEEDS_REWORK and `goldens-affected` is red by design.

## A better permission probe (supersedes 08-27's method)

08-27 measured the `.github/workflows/` block by creating and deleting a
throwaway branch. A `PUT /contents` with a **deliberately wrong blob sha** is
decisive and **mutates nothing** — permission is checked before the sha:

| path | http |
|---|---|
| `.github/workflows/ci.yml` | **403** "Resource not accessible by personal access token" |
| `dev/notes/data-gaps.md` (control, same branch, same call) | **409** "does not match 0000…" |

`main` HEAD unchanged throughout. Same conclusion, no branch churn, no cleanup.

## Health Scan

`dev/health/2026-08-28-fast-run2.md` — **CLEAN**. On `5c20fe07`: build **0**,
runtest **0** (0 `^FAIL:`), `status_file_integrity` **0**, `index_size_linter`
**0**; each also run **unpiped** so the captured status is the script's. Re-run
after the #2589 merge on `409b85f4`: build **0**, runtest **0**, 0 `^FAIL:`.
Independently, CI on `5c20fe07` was green on all six checks including both
golden workflows.

**Step 6.2 path defect — 4th consecutive run.** Re-measured:
`sh trading/devtools/checks/status_file_integrity.sh` → exit **2**
(`sh: 0: cannot open`), `sh devtools/checks/…` → exit **0**. `run-in-env.sh`
already cds into the inner `trading/`, so the spec's prefix is doubled; an exit
2 reads exactly like a check failure. The one-line fix is drafted and verified
but still unapplied — writing `.claude/agents/**` is gated by a harness
permission prompt that was not granted.

## Budget

- Cap: $200 (`max_daily_cost_usd` — a runaway backstop; auth is flat-rate Max-20x)
- Target utilization: 60–95%
- Subagents dispatched: **6**, all completed, **none killed mid-flight**
- Measured subagent tokens: **841,795** (sum of reported per-agent totals)
- Estimated cost: **~$4–7** including orchestrator overhead
- **Utilization assessment: UNDER_TARGET (~3%)** — and, as on 08-26/27 and run 1,
  **not a dispatch failure to correct.** The binding constraint is the **4-core
  runner**: load hit **10.8** with two agents building concurrently, and the gate
  loop (feat → structural → behavioral → merge) is inherently **serial**. Raising
  utilization needs a bigger runner or more independent tracks, not more agents.
- Scope reduced due to budget: **No.** Bounded by capacity and dispatchability.

## Dispatch decisions — why the other tracks did not run

- **cleanup / code-health** — the one honestly-actionable item was available
  (`doc_precision`, a self-correction in `cleanup.md`), and I **skipped it for
  capacity**, not because the backlog was empty. Recording that plainly: the top
  item is still the test-file-length **human policy decision** (**15th** run); the
  `test_gap` item is explicitly "not a defect, recorded so a future edit does not
  delete it"; the `linter_candidate` item is a new linter, harness-shaped and
  larger than a cleanup slot.
- **#2539** (SC2012/SC2086/SC2010) — undispatchable: the violations live in
  `.github/workflows/*.yml`; writes there are **403** (re-measured above). Same
  blocker as **H-BLAS**.
- **#2394** (long cascade RS phase) — precondition still false; `min_rs_normalized`
  still defaults to `0.0` and zero specs arm it.
- **#2403** (goldens should track the live config) — still the strongest
  remaining candidate, still wants a human scoping call. **#2587 is a live
  instance of exactly its thesis**: 15 goldens inherit a default none of them pins.
- **#2382 / #2405 / #2489 / #2408 / #2406 / #1729 / #2547** — container-exclusive,
  human-gated, or needing flake-rate accumulation. `docker` does not exist here.

## Data Operations

ops-data **not dispatched**. `dev/notes/data-gaps.md` unchanged since 2026-05-03
(**117 days**); `EODHD_API_KEY` absent in GHA. Both remaining gaps need a human
first: EODData free-tier registration to check whether `INDEX:ADVN`/`INDEX:DECL`
exist as absolute counts, and a cost/license call on Pinnacle Data. **12th
consecutive run reporting this.**

## Escalations

- **[info] Carried-forward verification: no still-real `[critical]` items.** Run 1
  carried none. Main was green at run start (`5c20fe07`, CI 6/6) and green after
  the merge (`409b85f4`, measured locally).

- **NEW [high] #2588 — a fix can be silently reverted by a PR cut before it
  landed, and nothing notices.** Measured on #2587 (details in §Headline).
  Cheapest remedy: **reorder `ci.yml` so `dune build @fmt` runs before
  `dune runtest`, or make it `if: always()`** — a formatting gate that only runs
  when every test already passes cannot report on the PRs most likely to need it.
  **I cannot make this change** (`.github/workflows/` → 403).

- **NEW [high] #2587 is NEEDS_REWORK and must not be merged on CI green.** Three
  blast-radius conditions FAIL (B1, B2, B3) plus G2. This is the knob that caused
  **−40.91pp** nine days ago; the paired-golden protocol exists because of it.
  The work needed is a **measurement, not code** — 12 golden cells requiring
  `docker`, which does not exist in this runtime — so I did **not** re-dispatch a
  feat-agent under Step 5a. It is a human action item.

- **NEW [medium] The blast-radius figure is wrong in the PR body *and* in the
  script.** **12** strategy cells, not 13: 15 goldens inherit, exactly 3 carry a
  `Bah_benchmark` stanza. The script's own loops enumerate 12 while its header
  says 13 — it contradicts itself. Also unaccounted: **`dev/weekly-picks/live-config-overrides.sexp`
  does not pin this knob**, so the live weekly-picks pipeline inherits 52 too.

- **NEW [medium] I fabricated a SHA into a posted review and a review file.**
  See §Corrections. Corrected. A cheap guard for the class: after any QC dispatch,
  resolve the agent's short SHA with `git rev-parse` instead of expanding it by
  hand. Note 08-27's proposed "validate 40 hex chars" remedy would **not** have
  caught this — the fabricated string *was* 40 valid hex characters.

- **[medium] Issue writes remain create-only — re-verified live, not copied.**
  `PATCH /issues/2556 {"state":"closed"}` → **403**. #2556 still open and still
  appears closeable. **Third consecutive run unable to say so on the issue.**

- **[medium] The QC audit trail — one correction to the standing claim.**
  `record_qc_audit.sh --pr-number 2589` **exits 1** without `gh`, as recorded. But
  the fallback is **not** as blind as run 1 stated: `write_audit.sh` recorded
  `consecutive_rework_count=1` for #2587's NEEDS_REWORK. So the `>= 3 consecutive
  rework` escalation **can** fire from file mode; what is lost is intra-run rework
  *history*, not the counter itself.

- **[medium] `.claude/agents/**` writes are a harness tool-permission gate**, not
  a token or filesystem restriction. The Step 6.2 one-liner is drafted and
  verified both ways and has now waited **four** runs.

- **[low] qc-structural over-credited B1/B2 on #2587.** A reviewer treating a
  *script that would produce* evidence as the evidence is the precise softening
  the blast-radius rule exists to prevent. Corrected in the posted review and
  independently adjudicated by behavioral.

- **[info] The human force-pushed #2587 mid-run** (`head_ref_force_pushed` by
  `dayfine`, 16:58:22Z), amending the flip and adding `run-goldens-52.sh`. The
  dispatched agent detected the rewritten base, **reset onto it and dropped its
  now-redundant work rather than force-pushing** — which would have destroyed
  100 lines of the human's paired-golden harness. Worth recording as the
  behaviour we want: "my branch is behind" is never a reason to force.

- **[info] `H-JJ-JST-BROKEN-GHA` confirmed again.** #2589 was opened by the Step
  4.5 curl REST fallback; no dispatched agent can open its own PR. All 6 agents
  were briefed around the absence of `gh`, `docker`, `jj` and `jst`, and around
  the fact that their authority files' `docker exec` instruction is unrunnable
  here and is **not** an ENVFAIL.

- **[info] Branch hygiene.** Deleted merged `ops/daily-2026-08-28` and
  `harness/2585-residuals-r1a-r5`. Left alone: two `wip/*` salvage branches
  (7 days old, under the 14-day rule, never had a PR).

## Follow-up Queue

- **O2** (`H-EXPIRY-ROLLUP-SHARED-FN-ASSUMED`) — the one green-while-broken
  evasion in 13 attacks; cheap to close. **Top of the harness queue.**
- **O3** (`H-EXPIRY-ADDWARNING-SITES-UNPINNED`) — the date-expired branch is the
  only one of **five** `add_warning` sites in that function that is pinned.
- **O1** (`H-EXPIRY-MUTATION-DIAGNOSTIC-MISLEADS`), **O4**
  (`H-EXPIRY-CONSUMER-HALF-UNPINNED`), **O5** (two off-by-one counts) — filed.
- **R-4** (`H-EXPIRY-GLOB-CLOSES-CLASS`) — still open; wants the data-driven
  `(path, label, guard)` table.
- **BOOK-CHECK-NEEDED (for a local session, book unreachable here):** *the book
  grants cancel authority for a GTC buy-stop but names no criterion. Is a blind
  **time-based** cancel a faithful stand-in for the discretionary cancel
  Weinstein describes, given the **condition-based** half (the weekly re-screen)
  was measured at −137pp and REJECTED?* Tier 1 is silent; §7's "weekend homework
  → enter GTC orders" hints tickets lapse weekly, which would make an unbounded
  rest a simulation artifact — but the reference does not say so.
- **From #2587:** a one-line default-config test would close CP4's compositional
  gap (no test runs a default-config tick and observes a cancel).
- **From #2581 (carried):** a scoped mutation harness for `simulation/lib/`.
  **Five** consecutive reviews have now derived their value from a hand-rolled
  mutant sweep. That is a tool wanting to exist.
- `dev/scripts/launch_sweep.sh:195` still carries the `echo | grep -q` + `pipefail`
  shape (non-gate script).
- `dev/status/cleanup.md` — top item is still the test-file-length **policy
  decision**, human-gated across **fifteen** runs.
- **Stale `0`-default claims outside #2587's file set** (reported, not fixed, to
  keep the PR scoped): `test_entry_ticket_ttl.ml:14` says "emits nothing at the
  defaults (`[false]`/`[0]`)" — now false, and it will **not** fail `runtest`
  (the test builds configs explicitly), so it is silent doc rot;
  `dev/status/simulation.md:8,39,75,811` narrates the `0`/`26` era as current.

## Integration Queue

- **#2589** → **MERGED `409b85f4`.** Three gates green, both verdicts pinned to
  `feeb193f9a0da9c6f74a80c0ac59a1ca7e5cde13`, `mergeable_state: clean`, hold
  re-checked immediately before the merge call (`pr-merge-gates.md` Rule 0): no
  `do-not-merge` label, not draft, no `convert_to_draft` post-dating a review.
- **#2587** → **HELD OPEN, deliberately.** `build-and-test` and `perf-tier1-smoke`
  green at `5044eb5f`, qc-structural APPROVED — but qc-behavioral **NEEDS_REWORK**
  and `goldens-affected` red by design. Handed back to the human.

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable. For the
**fourth** consecutive run the substantive findings were **measurement-integrity**
defects rather than strategy defects — this time a fix silently reverted by a
stale branch point with no gate noticing, and a default promotion whose evidence
is real but whose own ledger deferred a precondition to the very PR that flips it.

## Dependency Unlocks

None new. #2585's residuals closed by #2589. #2556 still appears closeable and I
still lack the permission to close it.

## Questions for You

1. **#2587 — how do you want G2 resolved?** The evidence for 52 is genuinely
   good (verified digit-for-digit: top on return, sharpe *and* maxDD in all three
   broad cells). But the ledger deferred a composition-independent cell **to this
   PR**, and cells B and C have no measured null for a mechanism the ledger itself
   calls path-dominated. Either add the cell and re-run B at ≥3 salts, **or**
   promote as an explicit user-gated exception naming the three deviations — and
   amend the ledger, which still reads "DEFAULT FLIP NOT EXECUTED … USER DECISION
   REQUIRED" and is stale either way. I did not self-authorize either path.
2. **Reorder `ci.yml` so `dune build @fmt` runs before `dune runtest`?** This is
   the cheapest fix for #2588 and it would have caught today's revert. Needs
   `.github/workflows/` write access, which is **403** for this token.
3. **Grant the token write access to `.github/workflows/`?** Unchanged and still
   the highest-leverage permission available — it unblocks #2539, H-BLAS, the
   #2579 backstop, and now #2588.
4. **Grant this session write access to `.claude/agents/**`?** Separate from Q3 —
   a harness tool gate, not the token. The Step 6.2 one-liner has waited **four**
   runs.
5. **#2556 can be closed** — implemented by #2563, verified on main. I cannot
   close or comment on issues (403, re-probed live this run).

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
