# Status — 2026-04-17
Run timestamp: 2026-04-17T21:15:00Z
Run ID: 2026-04-17-run-2

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-infra | blocked | feat/backtest-scenario-small-universe | #399 | Rework — fix `goldens-small/six-year-2018-2023.sexp` `total_trades` range (behavioral re-review NEEDS_REWORK @ 8ccc8c8; see BC4-F1) |
| backtest-scale | blocked | — | — | Still blocked on Step 2 (per-phase tracing) under backtest-infra; #399 now blocked too |
| short-side-strategy | blocked | — | — | Unblocked by support-floor-stops merge; deferred again to keep main stable while #399 rework lands |
| sector-data | blocked | — | — | Item 2 still blocked on out-of-band 2.2h Finviz fetch (human) |
| harness | blocked | — | — | Top dispatch target: T3-F architecture graph analyzer in health-scanner deep scan; `run-in-env.sh` GHA fix LANDED via #402 (resolved — see §Escalations) |
| orchestrator-automation | blocked | — | — | Human-only blockers (BOT_GITHUB_TOKEN + CLAUDE_CODE_OAUTH_TOKEN; gh/jj in container) |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-infra | qc-structural (re-review @ 8ccc8c8) | APPROVED | Fixture-only commit; no OCaml source changes; all structural checks PASS or NA. Noted a GHA container infra defect (`agent_compliance_check.sh`) — present on main too, not introduced by this PR. |
| backtest-infra | qc-behavioral (re-review @ 8ccc8c8) | **NEEDS_REWORK** | BC4-F1: re-pinned `total_trades` range `[200, 280]` in `goldens-small/six-year-2018-2023.sexp` disagrees with committed evidence (`actual.sexp` emits `10`) by ~24x. Scenario will FAIL `_check_one "total_trades"` on every clean run. Quality Score: 2. Two other goldens-small scenarios likely share the same conflation (header comments cite 251 / 253 trades) but have no committed evidence to cross-check. |
| — | health-scanner (fast, run 2) | **FINDINGS (critical)** | **Main build FAILING on main@origin (0799b72): `fn_length_linter` reports 2 violations — `run` at 102 lines in `fetch_finviz_sectors_lib.ml:167`, `test_keep_if_sector_rescues_reits` at 69 lines in `test_universe_filter.ml:458`.** Also clarified that run-1's status-file-schema warning was **incorrect** — PENDING + N/A is a valid combination per `status_file_integrity.sh`; the warning is withdrawn. |
| backtest-infra | feat-backtest | skipped | Awaiting behavioral re-review outcome before dispatching rework; scope is narrow (1–3 fixture ranges + optional evidence for 2 sibling scenarios) — will dispatch next run. |
| short-side-strategy | feat-weinstein | skipped | Re-deferred — main is currently red (fn_length) and #399 is back in rework; don't pile new feature work on top. |
| harness | harness-maintainer | skipped | `run-in-env.sh` item is already merged (PR #402 landed on main). Next priorities are T3-F graph analyzer and the forbidden-`## Recent Commits`-heading deep-scan gap. Deferred one more run while #399 rework is the critical path. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since prior run; no actionable gap. |

## Step 1b drift cross-reference

Last prior summary: `dev/daily/2026-04-17.md` (run 1). Its "pending work" state:

- **backtest-infra #399 — awaiting-merge**: run-1 recorded overall_qc APPROVED. Between run 1 and run 2 the author pushed commit `8ccc8c8` (BC4 follow-up, fixture-only) on top of the previously-approved tip. Per Step 1.5 policy the new SHA != last Reviewed SHA (`6e28734`), so re-QC fires — this is **not drift**, it is the designed incremental-review path. qc-structural and qc-behavioral re-dispatched; behavioral flipped back to NEEDS_REWORK.
- **harness / sector-data / short-side-strategy / orchestrator-automation — all blocked in run 1**: unchanged. Blockers carry forward.

Drift warnings raised this run: **none**.

## Feature Progress

### backtest-infra  [READY_FOR_REVIEW — overall_qc NEEDS_REWORK]
- Done since run 1: Commit `8ccc8c8` — re-pin `goldens-small/*.sexp` from real 302-symbol runs; widen `goldens-broad/*.sexp` to always-pass bounds with a SKIPPED banner; commit evidence at `dev/backtest/scenarios-2026-04-17-184456/`; add follow-up item 5 (broad re-pin via GHA workflow).
- QC: structural APPROVED (re-review @ 8ccc8c8); **behavioral NEEDS_REWORK @ 8ccc8c8** — F1 on `total_trades` range. overall_qc: **NEEDS_REWORK**.
- Blocked: Yes — rework required before merge. Scope is surgical (see "Required fix" options in `dev/reviews/backtest-infra.md` §Behavioral Re-review @ 8ccc8c8).
- Next: feat-backtest dispatch to either (a) re-measure + re-pin `total_trades` to bracket the runner-observed value (the evidence says `10`; plausible range `[5, 20]`), or (b) switch `_actual_of_result` to emit `WinCount + LossCount` if the intent was the in-sim full-span round-trip count; apply the same fix to `bull-crash-2015-2020.sexp` and `covid-recovery-2020-2024.sexp` and commit matching evidence.

### support-floor-stops  [MERGED]
- Unchanged since run 1 — both PRs merged earlier today.

### harness  [IN_PROGRESS]
- `dev/lib/run-in-env.sh` GHA-mode fix **LANDED on main** via PR #402 between run 1 and run 2 — detects the real GHA checkout via `$GITHUB_WORKSPACE`/`PROJECT_ROOT` rather than the baked-in devcontainer path. Confirmed by reading the current script on main. §Escalations updated accordingly.
- Next backlog items queued: T3-F architecture graph analyzer in health-scanner deep scan; deep-scan heuristic gap sub-item 2 (grep for forbidden `## Recent Commits` heading).

### short-side-strategy / sector-data / orchestrator-automation  [UNCHANGED]
- No dispatches; blockers unchanged.

## QC Status

| Track | overall_qc | structural | behavioral | Notes |
|-------|------------|------------|------------|-------|
| backtest-infra | **NEEDS_REWORK** (@ 8ccc8c8) | APPROVED (@ 8ccc8c8) | **NEEDS_REWORK** (@ 8ccc8c8, Q-score 2) | BC4-F1 only; fixture bug. See `dev/reviews/backtest-infra.md`. |
| support-floor-stops | APPROVED (prior run) | APPROVED | APPROVED | Both PRs merged. |
| All other tracks | — | — | — | No READY_FOR_REVIEW state. |

## Budget

- Budget cap: **$50** (from `dev/config/merge-policy.json` `max_daily_cost_usd`).
- Subagents spawned this run: **3**.

| Agent | Model | Status | Est. tokens | Est. cost |
|-------|-------|--------|-------------|-----------|
| qc-structural (re-review @ 8ccc8c8) | sonnet | completed (APPROVED) | ~30k | ~$0.35 |
| qc-behavioral (re-review @ 8ccc8c8) | opus | completed (NEEDS_REWORK) | ~50k | ~$2.50 |
| health-scanner (fast run 2) | sonnet | completed (FINDINGS) | ~30k | ~$0.20 |

- Killed mid-flight: **none**.
- Orchestrator overhead (rough): ~$1.
- Budget utilization: ~**$4** / $50 (~8%) for run 2.
- Combined with run 1 (~$6.25): ~$10.25 spent on 2026-04-17 across two runs.
- Scope reduced due to budget: No — scope was cut by **critical path** (don't spawn new feature work while #399 is in rework and main is red on fn_length) rather than tokens.

## Data Operations

Not run this session. `dev/notes/data-gaps.md` unchanged since prior run, so per Step 1.5 ops-data was skipped.

Still-binding decisions from `dev/decisions.md`:
- ADL live source — **DROPPED** (synthetic-only final).
- Sector-data Item 2 — human action item (2.2h Finviz fetch outside subagent).

## Harness Work

Not dispatched this session. Top-priority open items for next run:

1. **T3-F architecture graph analyzer in health-scanner deep scan** — now #1 priority since `run-in-env.sh` fix landed.
2. Deep-scan heuristic gap sub-item 2 — grep for forbidden `## Recent Commits` heading in status files.
3. `harness/t3g-trend-analysis@origin` — open a PR for the already-pushed branch (orphaned from an earlier run; Step 4.5 fallback didn't fire).
4. **(New candidate)** `fn_length_linter` violations on `fetch_finviz_sectors_lib.ml` and `test_universe_filter.ml` — see §Escalations item 1. Small refactor.

## Health Scan

- Result: **FINDINGS** (1 critical, 1 warning, 1 info).
- **Critical:** Main build FAILING on main@origin (commit `0799b72`). `dune runtest` exits 1. `fn_length_linter` reports:
  - `analysis/scripts/fetch_finviz_sectors/lib/fetch_finviz_sectors_lib.ml:167` — `run` is 102 lines (limit 50)
  - `analysis/scripts/universe_filter/test/test_universe_filter.ml:458` — `test_keep_if_sector_rescues_reits` is 69 lines (limit 50)
  - Fix options: refactor under 50 lines, add `(* @large-function: <reason> *)` annotation, or add a linter exception with justification. See §Escalations for dispatch plan.
- Warning: 45 nesting-linter warnings across 8 files in `analysis/scripts/` and `trading/weinstein/strategy/` — pre-existing, non-gating (exit 0 from nesting linter).
- Info: Run-1's schema-violation warning for `backtest-scale.md` + `short-side-strategy.md` was **incorrect** — PENDING + "N/A — not started" is compliant per `status_file_integrity.sh`. Withdrawn.
- Full report: `dev/health/2026-04-17-fast.md`.

## Follow-up Queue

Unchanged counts from run 1 (no feat-agent dispatched this run that would add items):

- `backtest-infra`: 5 (item 0 "BC4 re-pin from real runs" remains tagged resolved-in-intent but is re-opened de facto by F1 finding; item 5 broad re-pin tracked)
- `data-layer`: 5
- `harness`: 5
- `portfolio-stops`: 1
- `simulation`: 12
- `sector-data`: 3
- `support-floor-stops`: 1

**Total: ~32** (threshold: 10; maintenance cycle still eligible — not triggered this run because the critical path is #399 rework, not maintenance).

## Integration Queue

Pending human merge:

- **#399** — `feat/backtest-scenario-small-universe` — **NOT ready to merge this run.** overall_qc flipped from APPROVED (run 1, @6e28734) → NEEDS_REWORK (run 2, @8ccc8c8) because BC4-F1 (`total_trades` range vs. committed evidence) will fail the runtime gate on every clean run. Fix is surgical; next run's feat-backtest dispatch should land it.

No other READY_FOR_REVIEW PRs.

## Current Milestone Target

M5 — Walk-forward backtest + parameter tuner. Near-term sequence unchanged:

- **#399 rework + merge** → Step 2 (per-phase tracing) dispatches → feeds Step 3 tier-aware bar loader (backtest-scale).
- Support-floor experiment (fixed-buffer vs support-floor on goldens) — unblocked; queued as config-override variant after #399 lands.
- Experiment framework formalization — deferred.
- Drawdown circuit breaker (Weinstein Ch. 7) — medium-term.

## Dependency Unlocks

- **`run-in-env.sh` GHA-mode fix (PR #402) merged** → orchestrator-runtime build/test signal now **trustworthy in GHA**. Confirmed the live script on main checks `$GITHUB_WORKSPACE`/`PROJECT_ROOT` and verifies `dune-workspace` at the chosen root. Downstream: health-scanner's "main build" signal is now reliable — and immediately flagged a real gate failure this run (fn_length).
- No other new interface-stable flags.

## Escalations

1. **CRITICAL — Main build is RED on main@origin.** `dune runtest` exits 1 on commit `0799b72`. Two `fn_length_linter` violations:
   - `analysis/scripts/fetch_finviz_sectors/lib/fetch_finviz_sectors_lib.ml:167` — `run` is 102 lines (limit 50)
   - `analysis/scripts/universe_filter/test/test_universe_filter.ml:458` — `test_keep_if_sector_rescues_reits` is 69 lines (limit 50)
   - **Origin:** These violations were introduced by recent sector-data / universe-filter work landed after 2026-04-08 (the previous successful build). The health-scanner's run-1 scan did not see them because run-in-env.sh was routing to a stale devcontainer snapshot — the PR #402 fix exposed the real state.
   - **Impact:** Any new PR whose CI runs `dune runtest` will fail the gate until the baseline is green. qc-structural's run-2 re-review of #399 observed this and correctly attributed it to main (not to this PR).
   - **Dispatch plan:** Next run should spawn a `harness-maintainer` (or `feat-backtest` / `ops-data`, whichever owns the file) to refactor `run` in `fetch_finviz_sectors_lib.ml` under 50 lines (extract natural sub-helpers like `_fetch_page`, `_parse_response`, `_write_shard`) and split `test_keep_if_sector_rescues_reits` into 2–3 scenario-specific tests. Scope is ~1 small PR.

2. **CRITICAL — #399 BC4 regression-gate bug (BC4-F1).** `goldens-small/six-year-2018-2023.sexp` pins `total_trades: [200, 280]` but the committed evidence emits `10`. Cause is almost certainly a conflation between `_actual_of_result` (`List.length r.round_trips` on post-start-date steps) and the metric-suite's `WinCount + LossCount` over warmup + measurement. The author's fix is surgical — either re-measure and re-pin to `[5, 20]` or switch `_actual_of_result` to emit `WinCount + LossCount` — and the same conflation almost certainly affects `bull-crash-2015-2020.sexp` and `covid-recovery-2020-2024.sexp`. Dispatch feat-backtest next run. Full finding at `dev/reviews/backtest-infra.md` §Behavioral Re-review @ 8ccc8c8 → F1.

3. **RESOLVED — `dev/lib/run-in-env.sh` GHA-mode bug (was top escalation in run 1).** Fixed on main via PR #402. The script now resolves the real checkout via `$GITHUB_WORKSPACE` (or by walking for `dune-workspace` / `dune-project`) rather than the baked-in `/workspaces/trading-1/trading` devcontainer path. Dropping from active-escalation list.

4. **Infrastructure defect — `agent_compliance_check.sh: FAIL: could not locate repo root`.** qc-structural's run-2 re-review observed this linter failure reproducibly on both #399 and main@origin. It appears to be a walk-up resolution mismatch between the `dune runtest` sandbox cwd and the `run-in-env.sh` wrapper. Symptom is a persistent test failure on main that is NOT a real code issue. Recommend the harness-maintainer pair this with the fn_length refactor in (1) — both are baseline-red issues.

5. **Carried forward — `harness/t3g-trend-analysis@origin` still has commits but no open PR.** Step 4.5 PR-fallback clause did not fire when this branch was pushed. Surface to human: create the PR manually or dispatch harness-maintainer to run `jst submit harness/t3g-trend-analysis`.

6. **Carried forward — schema-drift warning (now downgraded).** Run-1 flagged `backtest-scale.md` + `short-side-strategy.md` as schema violations. Run-2 clarifies: **not violations** — PENDING + "N/A — not started" is a valid combination per `status_file_integrity.sh`. No fix needed.

7. **Carried forward — orchestrator Step 4.5 gh-dependency.** Step 4.5 as originally written used `gh api`; the current orchestrator definition (and this run) correctly uses `curl` against the GitHub REST API. Consider this item closed for the lead-orchestrator; it remains partially open for any subagent definitions that still call `gh` (qc-structural dispatch prompt already uses curl).

## Questions for You

1. **Dispatch feat-backtest next run to fix BC4-F1?** Scope: re-measure `total_trades` on goldens-small (likely `[5, 20]` for six-year-2018-2023; similar rework + evidence for bull-crash-2015-2020 and covid-recovery-2020-2024), OR switch `_actual_of_result` to emit `WinCount + LossCount` and re-emit evidence. My recommendation: option 1 (re-measure) — it preserves the semantics the author seemed to intend in BC4 and keeps the fix localized to fixtures.

2. **Dispatch harness-maintainer on the fn_length violations next run?** Refactor `run` in `fetch_finviz_sectors_lib.ml` + split the oversized test in `test_universe_filter.ml`. Needed to unred the main build baseline. Small PR. I suggest running this in parallel with the #399 rework — they touch disjoint files.

3. **Short-side-strategy — defer one more run?** Same reasoning as run 1: don't pile new feature work on top of a rework + a red baseline. If you prefer to dispatch anyway, `feat-weinstein` is the candidate owner and scope is well-defined in `dev/status/short-side-strategy.md`.

4. **Auto-merge this ops summary?** `auto_merge_enabled: false` in `dev/config/merge-policy.json`, so per policy I will push the branch + open the PR but NOT auto-merge. If you'd like run-to-run summary visibility (Step 1b drift detection reads from `main` filesystem) without manual merges, flip the flag to `true`.

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session locally, or trigger the orchestrator workflow manually via `workflow_dispatch`.)
