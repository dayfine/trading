# Consolidated Summary — 2026-04-20
Runs included: run-1, run-2, run-3, run-4, run-5, run-6 (6 total)
Generated: 2026-04-20T21:42:03Z

## Pending work (from last run)

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-scale | awaiting-merge | feat/backtest-scale-3f-part3 | #474 | 3f-part3 tiered runner Friday cycle + per-transition promote/demote — APPROVED (structural + behavioral, Quality Score 4) in run-5, unchanged this run. Awaiting human merge. |
| harness | awaiting-merge | harness/t3f-rule-promotion | #473 | T3-F rule-promotion path (auto-generate dune checks from enforced rule registry) — APPROVED (structural) in run-5 via audit record, unchanged this run. Awaiting human merge. |
| backtest-scale | blocked | — | — | 3g (parity acceptance test) is the merge gate closing the tiered loader track; per plan Resolutions §4 it needs 3f-part3 landed on main (not just branch-existing), so cannot dispatch until #474 merges. |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution for scrapers, golden re-runs, cross-feature QC) — human-only per status file. |

## Dispatched across all runs (deduped)
| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| cleanup | (orchestrator) | merged | PR #457 — orchestrator-authored cleanup commit clearing 2 of 3 baseline-red linter failures (magic_numbers + fn_length); committed before any feat-* dispatch so dispatched agents don't inherit a worse baseline |
| backtest-scale | feat-backtest | completed | PR #459 — increment 3e (loader_strategy plumbing, ~218 LOC, 5 commits). Background dispatch via `run_in_background:true` (Phase 2 test); ran ~21min. Agent could not open PR (no `gh` in container); orchestrator opened via REST fallback. |
| harness | harness-maintainer | completed | PR #458 — deep-scan drift coverage extension to `trading/trading/backtest/`; closes harness §Follow-up sub-item 1. Foreground dispatch in parallel with the BG feat-backtest. |
| backtest-scale | qc-structural | APPROVED | Branch `feat/backtest-scale-3e` SHA c51d42b. All 13 checklist items PASS or NA. Module boundary correct (separate `loader_strategy/` lib avoids dep cycle), Tiered raises loudly, sexp.option preserves backward compat. (run-1) |
| backtest-scale | qc-behavioral | APPROVED (quality 5) | Legacy path byte-identical to pre-PR (verified by diffing extracted `_run_legacy` against `origin/main:runner.ml`); Tiered raises uncaught; CLI/scenario defaults flow to Legacy via OCaml optional-arg punning. (run-1) |
| harness | qc-structural | APPROVED | Branch `harness/deep-scan-drift-coverage` SHA f0c402a. All structural checks pass; H3 dune runtest fails on pre-existing nesting_linter only (untouched paths). One minor observation noted (smoke test short-circuit OR makes its 2nd arm always true; intent satisfied in practice). |
| harness | qc-behavioral | not run | harness work — no domain logic to behaviorally review (run-1) |
| (none) | code-health | not dispatched | §Backlog had only the @large-module weinstein finding which #453 cleared; the 320-line `weinstein_strategy.ml` is now annotated. New nesting backlog items are not yet refreshed into §Backlog (deferred to code-health backlog refresh — see §Escalations) |
| (none) | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since last run |
| backtest-scale | qc-structural | NEEDS_REWORK → APPROVED | First pass found P6 violation in `test_shadow_screener.ml` (three separate `assert_that` calls on one value). Scoped rework dispatch composed them into one `assert_that` with `all_of` + `field`. Re-QC APPROVED at SHA `ffb17c4720`. (run-2) |
| backtest-scale | qc-behavioral | APPROVED (quality 4) | Plan-faithful adapter preserves Legacy byte-identical; Summary fabrication into minimal `Daily_price.t list` is domain-correct; stub paths raise loudly; 1 minor observation on fabricated-bar count (≥20 weeks of synthetic data for screener warm-up). (run-2) |
| harness | qc-behavioral | SKIPPED | harness work — no domain logic to behaviorally review (run-2) |
| backtest-scale | qc-behavioral | APPROVED | PR #466; quality 4/5 — skeleton is behaviorally correct (raises loudly with pointer to 3f-part3), tests assert the intentional split, no silent partial behavior. (run-3) |
| — | qc-behavioral | — | Skipped for PR #467 per agent contract (harness/non-strategy). |
| backtest-scale | — | draft-flip | PR #466: draft → ready-for-review via GraphQL `markPullRequestReadyForReview`. (run-3) |
| harness | — | draft-flip | PR #467: draft → ready-for-review via GraphQL `markPullRequestReadyForReview`. (run-3) |
| backtest-scale | re-QC | skipped | PR #463 tip SHA == Reviewed SHA from run-2; no new commits since APPROVED. |
| harness | re-QC | skipped | PR #464 tip SHA == Reviewed SHA from run-2; no new commits since APPROVED. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since run-2. |
| cleanup | code-health | skipped | `dev/status/cleanup.md` §Backlog empty; run-2 deep scan has no new medium/high findings. |
| backtest-scale | — | skipped | plan-first stack cap reached (#463 root + #466 stacked = 2/2); no new dispatch until one lands (run-4) |
| harness | — | skipped | 2 open PRs in flight (#464, #467), non-plan-first — Step 1.5 skips further dispatch (run-4) |
| ops-data | — | skipped | data-gaps.md unchanged since last run |
| cleanup | — | skipped | backlog empty after fast-run5 |
| — | health-scanner | skipped | fast scan deterministic (Step 6); deep scan is weekly GHA cron, not dispatched here |

## QC across all runs
- backtest-scale (3e): APPROVED (structural + behavioral, quality 5) — see `dev/reviews/backtest-scale.md`
- harness (drift coverage): APPROVED (structural; behavioral N/A) — see `dev/reviews/harness.md`
- backtest-scale (3f-part1): APPROVED (structural + behavioral, quality 4/5 after one P6 rework) — see `dev/reviews/backtest-scale.md`
- harness (T3-G): APPROVED (structural; behavioral N/A) — see `dev/reviews/harness.md`
- backtest-scale: APPROVED (PR #463 run-2; PR #466 run-3 quality 4/5) — unchanged this run (see dev/reviews/backtest-scale.md)
- harness: APPROVED structural (PR #464 run-2; PR #467 run-3) — unchanged this run (see dev/reviews/harness.md). Behavioral review not required for harness tracks.
- short-side-strategy, strategy-wiring, sector-data, support-floor-stops, simulation, portfolio-stops, screener, data-layer: MERGED — no fresh QC this run.
- backtest-scale (3f-part3 / #474): APPROVED (structural + behavioral, Quality Score 4, run-5) — dev/reviews/backtest-scale.md §3f-part3; dev/audit/2026-04-20-backtest-scale.json.
- harness (T3-F / #473): APPROVED (structural, run-5) — dev/audit/2026-04-20-harness.json. Review body in dev/reviews/harness.md lacks the T3-F section (see [info] below).
- sector-data: — (merged)
- backtest-infra: — (merged)
- short-side-strategy: — (merged)
- strategy-wiring: — (merged)
- All other tracks: MERGED or BLOCKED — no fresh QC this run.

## Budget (summed across runs)
- Total subagents spawned: 20
- run-1: - Budget utilization: ~$15.20 + ~$1.50 orchestrator overhead ≈ $16.70 / $50 (33%)
- run-2: - Budget utilization: ~$16.80 + ~$1.70 orchestrator overhead ≈ $18.50 / $50 (37%)
- run-3: - Budget utilization: $18.75 / $50.00 (~37.5%)
- run-4: - Budget utilization: ~$1.00 / $50.00 (~2%)
- run-5: - Budget utilization: ~$14.00 / $50.00 (28%) including ~$1 orchestrator overhead.
- run-6: - Budget utilization: ~$1.00 / $50.00 (~2%)
- Killed mid-flight: None reported across all runs.

## Escalations (merged)
- [info] **Phase 2 test: dispatch returned immediately.** As requested in the run prompt, the first feat-* dispatch (`feat-backtest` for backtest-scale 3e) was issued with `run_in_background: true`. The Agent tool returned an `agentId` immediately ("Async agent launched successfully") without blocking; the orchestrator then dispatched `harness-maintainer` foreground in the same message, which ran in parallel and completed in ~13min while the BG agent kept running. The BG agent finished ~21min later with a completion notification. Conclusion: **background dispatch returns control immediately** — Phase 2 of orchestrator-automation is unblocked from the API side. Caveat observed: in the GHA container the working tree is shared across subagents, so when the BG agent does `git checkout <its branch>`, the orchestrator cannot safely run other commands that touch the working tree until the BG agent finishes. Foreground subagents that share the same working tree state (here: harness-maintainer) ran fine because they used branches with disjoint files; serialization via shared working tree is the new constraint to think about for Phase 2 design (separate worktrees, or strict file-disjoint partitioning, or just restrict BG to long-running agents whose branches the orchestrator doesn't need to touch until they finish).
- [critical] **Main baseline RED on `dune runtest`** — exit 1 from `nesting_linter` on pre-existing files in `analysis/scripts/universe_filter/`, `analysis/scripts/fetch_finviz_sectors/`, `trading/weinstein/strategy/lib/ad_bars.ml`, `analysis/technical/indicators/atr/lib/atr.ml`. **All in files NOT touched by today's PRs.** This was previously documented as "advisory" in the orchestrator's Step 1c/6.1 — but the linter source `trading/devtools/nesting_linter/nesting_linter.ml:405` explicitly `exit 1` on FAIL, so the gate fires. Action items: (1) update `lead-orchestrator.md` Step 1c + 6.1 to remove the false "advisory" claim for `nesting_linter` (and re-verify `linter_magic_numbers` — empirically gating in this run too); (2) refactor the offending functions OR add `(* @nesting-ok: <reason> *)` exemptions OR add `nesting <path> <reason>` entries to `linter_exceptions.conf`. Tracked in `dev/status/harness.md` §Follow-up "Pre-existing nesting linter failures" (item present but stale — predicted M3 cleanup never landed). I did NOT clear these today because they're outside the eng-design plan's active scope and would have crowded out the scheduled work.
- [info] **PR #457 ↔ PR #459 conflict on `runner.ml`.** Both PRs touch `run_backtest` in `trading/trading/backtest/lib/runner.ml`. #457 (cleanup, MERGED this run) extracts `_make_summary`. #459 (3e, awaiting merge) extracts `_run_legacy`. The two extractions don't overlap conceptually but produce a textual conflict because both edit the same function body. Resolution: rebase #459 onto current main; the rebased version should keep both helpers (`_make_summary` from #457, `_run_legacy` from #459) and `run_backtest` becomes thin (likely under 50 lines). I did not auto-rebase from the orchestrator side because the rebase needs domain-aware judgment about whether to keep the eprintf banner that 3e added or move it elsewhere.
- [info] **`run_backtest` will fail fn_length_linter after #459 lands** — currently 59 lines on `feat/backtest-scale-3e` (was 56 before). After #457's `_make_summary` extraction is folded in, the rebased `run_backtest` will likely drop under 50; if not, it'll need an `@large-function` annotation or further splitting. QC missed this because it scoped `dune runtest` to `trading/backtest` (the scope where the feature-track tests live), which doesn't include `devtools/checks/dune` where fn_length_linter runs. Surfacing for the human's attention; orchestrator will catch it on the next deterministic Step 6 run if it persists.
- [info] **Lost-and-recovered QC review**: the qc-structural review for #458 (harness) ran AFTER the 3e qc-behavioral review and did `git checkout harness/deep-scan-drift-coverage` in the shared working tree, which clobbered today's `dev/reviews/backtest-scale.md` (overwritten with the harness branch's stale 3a content). Recovered from `git stash` (which jj/git auto-created when the qc agent ran `git checkout`) before writing this summary. Worth a small lead-orchestrator hardening: stash the orchestrator-authored review files BEFORE dispatching another QC agent that will checkout a different branch, or have QC agents write reviews on a dedicated `ops/reviews-<date>` branch separate from feature branches.
- [info] **Code-health backlog refresh deferred** — Step 2e.1 didn't refresh §Backlog from today's `dev/health/2026-04-20-fast.md` because the cleanup branch was merged in-flight (#457) and the new nesting backlog items would have crowded out the deterministic check finding. The 27-violation pre-existing nesting set from `analysis/scripts/...` should be added as a single batched §Backlog item next run with severity `[medium]` (one entry per offending file rather than per function).
- [info] **Run-1 [critical] resolved.** Run-1 flagged main as RED on `dune runtest`
- [info] **Git HEAD contention between concurrent feat-backtest and
- [info] **QC P6 rework cycle on #463.** First qc-structural pass FAILed on
- [info] **Draft→ready flip exercised.** PR #463 was opened as draft and
- [info] **Plan-first adapter route confirmed.** 3f's plan file
- [info] **Merge ordering for stacked PRs.** #466 is stacked on #463 (`feat/backtest-scale-3f-part2` branches off `feat/backtest-scale-3f`, not main). GitHub will flag a conflict if #466 is merged first. Please merge **#463 first**, then #466. Same-track non-blocker between #464 and #467 — both target main independently.
- [info] **record_qc_audit.sh still not on main.** T3-G audit recording (Stage 4 of QC pipeline) remains gated on #464 merging. Skipped recording an audit row for this run's 3 QCs. Once #464 merges, next run will have audit capture active (seen in: run-3,run-4)
- [info] **UNDER_TARGET budget utilization (~37.5%).** Not a skip-decision issue — all eligible tracks were dispatched. Throttle is the 4 open PR review queue, not orchestrator capacity. If the 4 PRs merge together, the next run will have 3f-part3 + fresh follow-ups and should land closer to 60%.
- [info] **3f-part2 shipped as skeleton split.** The agent followed plan §3f's guidance that this is "architecturally largest" and split off 3f-part3 (Friday cycle wiring + per-transition promote/demote) into a new follow-up. Structural + behavioral QC APPROVED the split. No human decision needed — documented in Follow-up Queue.
- [info] **Review queue is the throttle, not the orchestrator.** 4 open PRs (#463, #464, #466, #467) all APPROVED and awaiting human merge — 2 of them (#463, #464) have been APPROVED since run-2 on 2026-04-20 morning. Today's 4 runs have dispatched all readily-available work; capacity will stay under-utilized until merges resume. Merging the oldest two (#463 then #466 in stack order; #464 independently) unblocks 3f-part3 + T3-F for the next run.
- [info] **Merge ordering for stacked PRs (carried from run-3).** #466 is stacked on #463 (`feat/backtest-scale-3f-part2` branches off `feat/backtest-scale-3f`, not main). Merge **#463 first**, then #466. Same-track non-blocker between #464 and #467 — both target main independently.
- [info] **Same-day summary consolidation (Step 8b) not yet live.** #467 ships `dev/lib/consolidate_day.sh` + Step 8b wiring but hasn't merged. Today is run-4 on the same date — once #467 lands, future N ≥ 3 same-day runs will produce a `${DATE}-summary.md` roll-up.
- [info] Latent `linter_file_length` dep-graph gap: the dune rule for file_length only declares the linter script + `_check_lib.sh` as deps. Growth in `lib/*.ml` files does NOT invalidate the cache, so a file growing past 300 lines can be merged without the gate tripping (runner.ml=344 on main@origin today is the proof-of-miss). Standalone invocation of the script correctly FAILs. #474 masks the current violation as a side-effect of tiered_runner extraction; the bug itself remains until the harness/T3-F rule-promotion work (or a one-shot fix adding `(deps (glob_files ../../{trading,analysis,base}/**/lib/*.ml))` to the rule) lands.
- [info] Merge-order guidance for human: either merge #474 first (restores runner.ml<300, clean state for any follow-on checks) or merge both in parallel. #473 is independent of #474 and safe either order.
- [info] Drift cross-reference (Step 1b/1c): run-4's [critical] "review queue depth at 4 PRs" resolved — all 4 PRs merged between 16:37-17:03 UTC. No new [critical] escalations carried forward.
- [info] Worktree contamination latent risk: parallel subagent dispatches in the GHA container share a filesystem checkout. Pushed commits were clean in both cases (harness c251b3c touches 5 files; feat-backtest d493f2a only the backtest-scale diff) but the pattern is flagged in `.claude/rules/worktree-isolation.md` without a GHA-specific remediation. Consider T3-K (GHA worktree isolation) as a follow-up.
- [info] **Review queue remains the throttle.** 2 open PRs (#473, #474), both APPROVED as of run-5 (~4 hours before this run), awaiting human merge. This run (run-6) is a near-no-op: no eligible dispatches beyond already-APPROVED work. Future runs will continue to be near no-ops until merges resume. Merging #474 unblocks 3g (parity acceptance test — the last tiered-loader gate); merging #473 unblocks the dep-graph fix for `linter_file_length` via the rule-promotion path.
- [info] **Carry-forward from run-5: latent `linter_file_length` dune dep-graph gap.** Still real — the dune rule for file_length declares only `_check_lib.sh` + the linter script as deps, so growth in `lib/*.ml` doesn't invalidate the cached rule run. #474 masks the current miss by extracting tiered_runner and returning runner.ml to 287 lines; the cache-invalidation bug itself remains. Fix lands naturally once #473 merges and a follow-up uses the rule-promotion path. Re-verified (Step 1c): still a gap; carried as [info] not [critical].
- [info] **dev/reviews/harness.md lacks T3-F review body.** run-5's daily summary says "Review appended to dev/reviews/harness.md §T3-F" but the file content shows only prior reviews (deep-scan-drift-coverage and consolidate_day). The audit record at dev/audit/2026-04-20-harness.json stands (structural + behavioral APPROVED), so the QC verdict is durable; only the human-readable narrative is missing. Traceability gap, not a correctness gap. Not backfilling this run (would require re-dispatching qc-structural on a non-changed PR — false signal). Flag for future: qc-structural's "append to review file" contract isn't enforced by any check; consider a harness linter that verifies the active open-PR SHA appears in the corresponding review file.
- [info] **Stale `Reviewed SHA:` header fixed (backtest-scale.md).** Housekeeping: updated dev/reviews/backtest-scale.md top-line from ffb17c47 (3f-part1, merged) to d493f2a9 (3f-part3, current open PR #474). This matters for Step 1.5 idempotency — a stale header would trigger re-QC on an already-reviewed PR. No content below the header changed.
- [info] **Daily session count quirk.** `ls dev/daily/${DATE}*.md | grep -v '\-plan\.md'` incorrectly counts `${DATE}-summary.md` (the consolidated summary produced by Step 8b at N≥3) as if it were a per-run file. Strict spec would compute N=7 here; correct answer is N=6 (runs 1–5 + this one). Filed the filename as `-run6.md` manually. Harness follow-up: extend the grep filter in Step 7 (and Step 8) to also exclude `\-summary\.md`.

## Integration Queue (last run's view)
- PR #474 — backtest-scale 3f-part3 (APPROVED structural + behavioral Quality Score 4). Merge first (or in parallel with #473) — runner.ml shrinks back under 300 lines as a side-effect.
- PR #473 — harness T3-F rule-promotion (APPROVED structural). Independent of #474; safe either order.

## Per-run links
- run-1 -> `2026-04-20.md`
- run-2 -> `2026-04-20-run2.md`
- run-3 -> `2026-04-20-run3.md`
- run-4 -> `2026-04-20-run4.md`
- run-5 -> `2026-04-20-run5.md`
- run-6 -> `2026-04-20-run6.md`
