# Status — 2026-05-05

**Run timestamp:** 2026-05-05T08:05:00Z (run start) — 2026-05-05T09:50:00Z (summary write)
**Run ID:** 2026-05-05-run-1
**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 2 + 4 FAIL: 23 PRs landed since prior summary; `dev/status/harness.md` modified by 3 maintainer PRs (#838/#839/#840); `dev/status/data-foundations.md`, `backtest-infra.md`, `backtest-perf.md` also modified).

## Saturated-queue check result

- **Condition 1** PASS (trivially) — 0 open PRs at run start.
- **Condition 2** FAIL — non-summary commits on main since prior summary (#834 at 2026-05-04T13:28:40Z) include 7 status-file-touching commits: #838/#839/#840 (harness.md×3), #842/#847 (data-foundations.md×2), #845 (backtest-perf.md), #853 (backtest-infra.md).
- **Condition 3** N/A — Step 1b ran; no `[drift]` warnings (run-2 had only `awaiting-merge` rows that resolved cleanly via human merges).
- **Condition 4** FAIL — `dev/status/harness.md` modified 3 times by #838/#839/#840 (jj-workspace contamination doc + boilerplate fix).

Decision: full pass. **2 parallel agents dispatched** + QC pipelines on both resulting PRs.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| data-foundations | awaiting-merge | feat/snapshot-bar-views-848-forward-fix | #861 | Forward fix for #848 (path-dependent regression in `Snapshot_bar_views`); QC APPROVED q=5/5 both axes; CI green. **DUPLICATES maintainer's own #860** (`feat/snapshot/848-parity-fix`) opened ~1h before #861 with the same scope and 0-diff diag verified locally — both PRs left open for human resolution. |
| harness | awaiting-merge | harness/prework-setup-enforcement | #858 | 2 follow-ups from PR #840 closed; QC APPROVED q=5/5 both axes; CI green. After merge, harness backlog drained again. |
| experiments | not dispatched (no eligible item) | — | — | M5.4 sweeps local-only; CLI binaries deferred. Status body refreshed via #832 yesterday. |
| tuning | not dispatched (no eligible item) | — | — | T-A grid_search lib (#805) + T-B Bayesian (#817) MERGED. CLI binaries deferred. T-C blocked on Norgate. |
| backtest-perf | not dispatched | — | — | Tier-4 manual `workflow_dispatch` is local-only. V1/V2/V3 verifications local-only. Daily_panels O(log N) reads + 1 GiB cache MERGED via #845 yesterday. |
| simulation | not dispatched | — | — | M5 walk-forward + tuner; sp500-2019-2023 baseline rerun (local-only). |
| short-side-strategy | merged | — | — | sp500-2019-2023 baseline rerun (local-only). |
| weekly-snapshot | merged | — | — | M6.6 live cycle DEFERRED. |
| orchestrator-automation | blocked (Phase 2 pending) | — | — | Phase 2 background-execution items pending empirical tests. |
| cleanup | blocked (no findings) | — | — | Backlog empty. No new `[medium]`/`[high]` findings from Step 6 deterministic checks. |
| cost-tracking | blocked | — | — | Today's measured cost will land in `dev/budget/2026-05-05-<run_id>.json` post-GHA-capture. |
| ops-data | not dispatched | — | — | data-gaps.md substantively unchanged (last_updated 2026-05-03); all 3 major data gaps RESOLVED in 2026-05-03 maintainer cycle. |
| (all other tracks merged earlier) | — | — | — | optimal-strategy / screener / trade-audit / backtest-infra / backtest-scale / data-panels / data-layer / portfolio-stops / strategy-wiring / sector-data / support-floor-stops / hybrid-tier — no change this run. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| data-foundations | feat-data | completed | Branch `feat/snapshot-bar-views-848-forward-fix` (tip `f2938ba`); 2 commits; PR #861 opened. Implements Option C from investigation note: adds `~calendar` parameter to `Snapshot_bar_views.daily_view_for` + `low_window`; reads `Snapshot_schema.Open` field (was returning `Float.nan`). 14/14 unit tests pass (9 existing + 5 new). Could NOT run `diag_panel_vs_snapshot_extended.exe` or `check_sp500_baseline.sh` — both require sp500 CSV corpus not available in GHA runner. **DUPLICATES maintainer's own #860** opened ~1h before #861 with the same scope. |
| harness | harness-maintainer | completed | Branch `harness/prework-setup-enforcement` (tip `5542c9e`); 2 commits; PR #858 opened. Extends `agent_compliance_check.sh` Rule 2 (require `## Pre-Work Setup` on jj-writing agents) + adds `jj_workspace_smoke.sh` (runtime correctness, skips on GHA where jj not on PATH). |
| harness-pr858 | qc-structural | APPROVED | Pure harness PR; H1/H2 PASS; H3 row claimed FAIL on pre-existing linter findings (recurring false-positive — orchestrator verified `dune runtest --force` exit 0 on PR tip `5542c9e6`); verdict APPROVED so no re-dispatch challenge. P1-P5 NA (shell-only). A1-A3 PASS/NA. |
| harness-pr858 | qc-behavioral | APPROVED quality_score 5 | CP1/CP2/CP4 PASS; CP3 NA. Both #840 follow-ups closed cleanly. |
| harness-pr858 | (orchestrator) | flipped draft→ready (no-op — never draft); audit written | `dev/audit/2026-05-05-harness-pr858.json` (overall_qc APPROVED q=5; consecutive_rework_count=0). Review file written to `/w/trading/...` (single underscore — recurring pattern); orchestrator relocated to canonical path `/__w/trading/trading/dev/reviews/harness-pr858.md`. |
| data-foundations-pr861 | qc-structural | APPROVED | All H1-H3 + P1-P6 + A1-A3 PASS. CI on PR tip `f2938ba6` green (build-and-test + perf-tier1-smoke both success). Review file written correctly to canonical path `/__w/trading/trading/dev/reviews/data-foundations-pr861.md`. |
| data-foundations-pr861 | qc-behavioral | APPROVED quality_score 5 | All four claims (calendar-column parity for `daily_view_for`, NaN-open fix in `_assemble_daily_bars`, same fix shape for `low_window`, required `~calendar` arg on `Panel_callbacks.support_floor_callbacks_of_snapshot_views`) pinned by committed unit tests using bit-equal per-cell parity assertions + the holiday-gap regression scenario from the investigation note. No harness gaps identified. |
| data-foundations-pr861 | (orchestrator) | flipped draft→ready (no-op — never draft); audit written | `dev/audit/2026-05-05-data-foundations-pr861.json` (overall_qc APPROVED q=5; consecutive_rework_count=0). |
| ops-data | — | not dispatched | data-gaps.md substantively unchanged since 2026-05-03 maintainer cycle. |
| cleanup (Step 2e) | — | not dispatched | Backlog empty AND no new `[medium]`/`[high]` findings from Step 6 deterministic checks. |

## Feature Progress

### data-foundations [IN_PROGRESS — #848 forward fix dispatched]

23 PRs landed on main since 2026-05-04 run-2 summary, including the parity-blocking partial revert (#847 closes #843 — strategy bar reads back on `Bar_reader.of_panels`), the perf O(log N) Daily_panels reads + 1 GiB cache (#845), F.3.c-1 Panel_callbacks port (#837), F.3.d-1 Macro_inputs port (#842), 15-year diagnostic + position-sizing override (#853-#855), share-class dedup (#851), and end-of-session priorities note (#857 + `dev/notes/next-session-priorities-2026-05-05.md`).

**Today's orchestrator dispatch on Priority 1 from the priorities note**: feat-data on the #848 forward fix. PR #861 (`feat/snapshot-bar-views-848-forward-fix`):
- `Snapshot_bar_views.daily_view_for` + `low_window` now accept `~calendar`; walk calendar weekday columns mirroring `Bar_panels` semantics with NaN-passthrough.
- `_assemble_daily_bars` reads `Snapshot_schema.Open` (was returning `Float.nan`).
- `Panel_callbacks.support_floor_callbacks_of_snapshot_views` requires `~calendar` (sole external consumer; test caller updated).
- 14/14 unit tests pass (9 existing + 5 new) including the holiday-gap regression scenario (n_days=9 vs lookback=10 with one synthetic holiday).
- Diff: 9 source files + 1 status file. Final `snapshot_bar_views.ml` 296 lines.

**DUPLICATION** (filed in §Escalations): the maintainer's own **#860** (`feat/snapshot/848-parity-fix`) opened at 08:47:59 UTC — about 1 hour before #861 — with the same scope. #860 reports 0 diffs across all 5 diag primitives (verified locally with the diag exec which #861's agent could not run in GHA). Both PRs left open for human resolution. The maintainer's PR is likely the one to keep (verified locally with the canonical acceptance test); #861 may be closed as duplicate or merged alongside if the maintainer wants the agent's specific test additions.

### harness [#858 closed both #840 follow-ups]

Two T3-class follow-ups filed by PR #840 (jj-workspace boilerplate compliance + smoke test) closed by today's PR #858:
- Rule 2 in `agent_compliance_check.sh` now enforces `## Pre-Work Setup` on every jj-writing agent (5 jj-writing files: feat-backtest/feat-data/feat-weinstein/harness-maintainer/ops-data; 6 read-only QC agents skipped).
- New `agent_compliance_test.sh` smoke test (2 assertions) verifies the rule fires.
- New `jj_workspace_smoke.sh` execs the canonical Pre-Work Setup boilerplate end-to-end and asserts `jj workspace list` shows the new entry; skips on GHA (no jj).
- Both new tests wired into `dune runtest devtools/checks/`.
- Status file flipped both items from open to DONE.

After #858 lands, harness backlog is again drained.

### experiments / tuning / backtest-perf / simulation [no work this run]

No new orchestrator-dispatchable items. Maintainer-driven progress overnight: data-foundations absorbed #842 (F.3.d-1 Macro_inputs port); backtest-perf absorbed #845 (Daily_panels O(log N)); strategy regression closed via #847 (partial revert of #828); 15-year backtest investigation via #853-#855; fixture tight-pinning via #857.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| harness (Pre-Work Setup enforcement) | #858 (open, not draft, mergeable_state=unstable, CI green) | APPROVED | APPROVED | 5 | `dev/audit/2026-05-05-harness-pr858.json` (consecutive_rework_count=0) |
| data-foundations (#848 forward fix) | #861 (open, not draft, mergeable_state=unstable, CI green) | APPROVED | APPROVED | 5 | `dev/audit/2026-05-05-data-foundations-pr861.json` (consecutive_rework_count=0) |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **6** (1 feat-data + 1 harness-maintainer + 1 qc-structural × 2 PRs + 1 qc-behavioral × 2 PRs)
- Per-subagent breakdown:
  | Agent | Model | Status | Tool uses | Tokens |
  |-------|-------|--------|-----------|--------|
  | feat-data (PR #861) | (default) | completed (~68 min, 173 tool uses) | 173 | 292K |
  | harness-maintainer (PR #858) | (default) | completed (~8 min, 57 tool uses) | 57 | 73K |
  | qc-structural (PR #858) | (default) | completed (~3 min, 24 tool uses) | 24 | 80K |
  | qc-behavioral (PR #858) | (default) | completed (~1.6 min, 17 tool uses) | 17 | 79K |
  | qc-structural (PR #861) | (default) | completed (~6.5 min, 48 tool uses) | 48 | 72K |
  | qc-behavioral (PR #861) | (default) | completed (~2.5 min, 26 tool uses) | 26 | 117K |
- Any subagent killed mid-flight: NO.

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-05-05-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$5–7 of $50 cap (~10–14%) — orchestrator overhead (Read/Edit/Bash/curl + 4× `dune runtest --force` verifications) + 6 subagent dispatches.
- Utilization assessment: **UNDER_TARGET** (<60%) — ~10–14% of cap. All-work-done: only 2 dispatchable feat tracks (Priority 1 #848 + harness follow-ups); Priority 2 (#856 grid sweep) blocked on grid_search CLI binary not yet built; Priority 3 (F.3 caller migrations) blocked on Priority 1 landing; Priority 4 (10k universe) deferred per user. Eligible work was dispatched; nothing was skipped for budget reasons.
- Scope reduced due to budget: NO.

## Data Operations

(ops-data did not run.)

`data-gaps.md` substantively unchanged since 2026-05-03 maintainer cycle. `data/sectors.csv` is 19 days old (refreshed 2026-04-16); refresh recommended at >30 days. Not yet stale; not dispatched.

## Harness Work

PR #858 (`harness/prework-setup-enforcement`) closes the 2 follow-ups from PR #840:
- `agent_compliance_check.sh` Rule 2 (require `## Pre-Work Setup` on jj-writing agents)
- `jj_workspace_smoke.sh` (runtime correctness for the canonical boilerplate)

After #858 lands, harness backlog is drained again.

## Health Scan

From `dev/health/2026-05-05-fast.md` (deterministic per Step 6):

- `dune build` exit **0** (PASSING) on HEAD `0af6c63`.
- `dune runtest --force` exit **0** (PASSING) on HEAD `0af6c63` — verified live this session pre-dispatch (also verified on PR #858 tip `5542c9e` and PR #861 tip `f2938ba` via CI green).
- `status_file_integrity.sh` (standalone): 5 advisory FAILs (same as run-2 yesterday) — `hybrid-tier.md`, `orchestrator-automation.md`, `tuning.md`, `weekly-snapshot.md` (×2 — `## Status` + `## Interface stable`). Recurring schema drift; script exits 0; not a gate.
- Result: **CLEAN** (build/runtest green; advisory integrity warnings carried).

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~5 (carried).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): local-only.
- **covid-recovery 20.81% WR investigation** (carried; deferred until short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**: **EXERCISED THIS RUN AGAIN ON PR #858** (12+ consecutive runs now). Agent's H3 row read FAIL citing pre-existing linter failures on `entry_audit_capture.ml`, `weinstein_strategy.ml`, etc. — orchestrator verified `dune runtest --force` exit 0 on PR tip `5542c9e6`. Agent's verdict was correctly APPROVED (so no re-dispatch needed). NOT exercised on PR #861 — that agent's H3 row was clean. **Recommendation re-iterated**: tighten qc-structural agent definition to use exit-code-only H3 verdict; never derive H3 FAIL from finding-name text alone.
- **qc-structural review-file persistence gap**: **EXERCISED THIS RUN AGAIN ON PR #858** (12+ consecutive runs now). Agent wrote review file to `/w/trading/...` (single underscore) instead of `/__w/trading/...` (double underscore). Orchestrator detected via `find / -name "*pr858*"` and relocated. NOT exercised on PR #861 — that agent wrote correctly to the canonical path. **Recommendation**: tighten qc-structural agent definition to anchor write path with `realpath` or `git rev-parse --show-toplevel`.
- **Schema drift from maintainer-driven status updates** (carried 7 runs, advisory only — 5 FAILs unchanged from yesterday). Pre-decision: grow `status_file_integrity.sh` to allow trailing clauses on `## Status` lines vs. enforce strict prefix-only. Harness backlog candidate.
- **magic_numbers linter false-positives on docstring text** (carried 6 runs, not exercised this run). Linter-side fix candidate; harness backlog.
- **F.3 caller migrations + F.3.e plan-first opportunity** (carried, refined). Once #861 (or #860) lands and the diag exec passes 0/0/0/0/0 locally, the next dispatch is F.3.a-3 redo (re-route `Panel_runner._setup_*` from `Bar_reader.of_panels` back to `Bar_reader.of_snapshot_views`); then F.3.b-2/c-2/d-2/e in turn (each ~50-200 LOC). All gated on multi-week soak post-redo.

## Integration Queue

(Features with overall_qc APPROVED — ready to merge to main pending human decision)

- **#858 (harness / Pre-Work Setup enforcement)** — `harness/prework-setup-enforcement` @ tip SHA `5542c9e6a895bef5190f815d7aa23258a217c6a4`. mergeable_state=unstable; not draft; CI green. qc-structural APPROVED; qc-behavioral APPROVED q=5; audit `dev/audit/2026-05-05-harness-pr858.json` written. **Recommended merge action**: human reviewer can merge cleanly. Closes the 2 follow-ups from PR #840.

- **#861 (data-foundations / #848 forward fix)** — `feat/snapshot-bar-views-848-forward-fix` @ tip SHA `f2938ba6cfe32628fca7f9dcc54d0e1c2f891061`. mergeable_state=unstable; not draft; CI green. qc-structural APPROVED; qc-behavioral APPROVED q=5; audit `dev/audit/2026-05-05-data-foundations-pr861.json` written. **DUPLICATION caveat**: maintainer's own **#860** opened ~1h before #861 with the same scope and 0-diff diag verified locally. Recommended action: human reviewer compares #860 and #861, merges whichever is cleaner (likely #860 since the diag exec was actually run), closes the other as duplicate. If both have unique value (e.g. #861 has more comprehensive unit tests), consider rebasing one onto the other.

## Current Milestone Target

After this run's dispatches:

- **M5.3 daily-snapshot streaming** — Phases A through F.2 MERGED; F.3.a-1/-2/-3/-4 + F.3.b-1 + F.3.c-1 + F.3.d-1 MERGED; **F.3.a-3 partial-reverted via #847 closes #843**; **#848 forward fix in flight via #860 (maintainer) + #861 (agent — duplicate)**. After fix lands: F.3.a-3 redo eligible; then F.3.b-2/c-2/d-2/e in turn (gated on multi-week soak).
- **M5.4 mechanical experiments** — partial. E1 / E2 / E3-harness / E4-harness MERGED; sweeps local-only.
- **M5.5 parameter tuning** — T-A grid_search + T-B Bayesian MERGED. CLI binaries deferred. T-C ML/HMM blocked on Norgate.
- **M6 weekly-snapshot verification** — DONE on M6.1-M6.5 content; M6.6 live cycle DEFERRED.
- **M7.0 Track 1 Norgate** — pending (vendor signup blocker).
- **M7.0 Track 2 multi-market** — DONE (#772).
- **M7.0 Track 3 Synth-v1 + v2** — DONE; **Synth-v3 pending** (~1000 LOC).
- **Wiki+EODHD interim historical universe** — PR-A/B/C/D ALL MERGED.
- **Tier-4 release-gate** — scaffolding (#810) + SCALE (#811) + Daily_panels O(log N) (#845) MERGED; manual `workflow_dispatch` is local-only.

## Dependency Unlocks

- **#848 forward fix awaiting merge unblocks F.3.a-3 redo** (re-route `Panel_runner._setup_*` from panel-backed `Bar_reader.of_panels` back to snapshot-backed `Bar_reader.of_snapshot_views`). Then F.3.b-2/c-2/d-2 caller migrations + F.3.e `bar_panels.{ml,mli}` deletion all unblock in sequence (gated on multi-week soak post-redo).
- **#858 (harness Pre-Work Setup enforcement) awaiting merge** — after merge, the contamination class introduced by `isolation: "worktree"` is permanently sealed (compliance check + smoke test both run on every PR via `dune runtest devtools/checks/`).

## Escalations

[info] **DUPLICATION on #848 forward fix — maintainer's own PR #860 collides with agent's #861**. The maintainer opened **#860 (`feat/snapshot/848-parity-fix`)** at 2026-05-05T08:47:59Z — approximately 1 hour before the agent's #861 (`feat/snapshot-bar-views-848-forward-fix`) was opened. Both PRs implement the same Option C from `dev/notes/path-dependent-regression-848-investigation-2026-05-05.md` (calendar-column walking in `daily_view_for` + NaN-open fix in `_assemble_daily_bars` + same fix shape for `low_window`). #860 reports 0 diffs across all 5 diag primitives verified locally; #861 could not run the diag exec in GHA but its committed unit tests pin the parity claims. **Recommended human action**: compare both PRs side-by-side; merge whichever has the cleaner diff and richer test coverage; close the other as duplicate. Lessons: the dispatch prompt should grep for any maintainer PRs targeting the same scope before launching — but this PR pre-existed by ~1h and was not visible at run start (the orchestrator listed PRs at run start and saw only #858 + #861; #860 must have been opened later by the maintainer using `gh pr create` from a local session).

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried 12+ consecutive runs; **EXERCISED ON PR #858 THIS RUN**). Agent's H3 row read FAIL citing pre-existing linter failures (`entry_audit_capture.ml`, `weinstein_strategy.ml`, etc.) despite `dune runtest --force` exit 0 on PR tip. Verdict was correctly APPROVED so no re-dispatch challenge needed. NOT exercised on PR #861 (clean H3 row). **Recommendation re-iterated**: tighten qc-structural agent definition to enforce exit-code-only H3 verdict; never derive H3 FAIL from finding-name text alone. Should be promoted to `[high]` if it recurs in another run.

[info] **qc-structural review-file persistence gap** (carried 12+ consecutive runs; **EXERCISED ON PR #858 THIS RUN**). Agent wrote review file to `/w/trading/...` (single underscore) instead of `/__w/trading/...` (double underscore) — orchestrator detected via filesystem search and relocated to canonical path `/__w/trading/trading/dev/reviews/harness-pr858.md`. NOT exercised on PR #861 (the agent wrote to canonical path correctly — possibly because the orchestrator's dispatch prompt for #861 explicitly mentioned the path-typo gap with "TWO underscores" callout, which appears to have been heeded). **Recommendation**: amend qc-structural agent definition to anchor the write path with `realpath` or `git rev-parse --show-toplevel`.

[info] **Schema drift from maintainer-driven status updates** (carried 7 runs, unchanged from yesterday — 5 advisory FAILs). Pre-decision: grow `status_file_integrity.sh` enum vs. enforce strict prefix-only. Harness backlog candidate.

[info] **magic_numbers linter false-positives on docstring text** (carried 6 runs, not exercised this run). Linter-side fix candidate; harness backlog.

## Step 1b drift cross-reference

Most recent prior summary on local FS / on main: `dev/daily/2026-05-04-run2.md` (run-2; committed via PR #834 at 2026-05-04T13:28:40+02:00; merged to main).

Run-2's `## Pending work` table parsed:
- `data-foundations (F.3.b-1)` — `awaiting-merge` (PR #833). Status today: **MERGED** (PR #833 landed 2026-05-04 evening). Normal lag, not drift.
- `data-foundations (status doc)` — `awaiting-merge (no QC)` (PR #831). Status today: **MERGED** (PR #831 landed 2026-05-04). Normal lag.
- All other rows were `not dispatched`, `merged`, or `blocked` — no `dispatched` rows to verify.

Status file commits on main since prior summary commit (PREV_ISO 2026-05-04T13:28:40+02:00, excluding `ops:` summary commits): #838/#839/#840 (harness.md×3), #842/#847 (data-foundations.md×2), #845 (backtest-perf.md), #853 (backtest-infra.md). All maintainer-driven; no agent-driven status drift.

No `[drift]` warnings emitted (no track had a `dispatched` row that would need verification today).

## Step 1c carried-forward critical verification

Run-2 carried zero `[critical]` items into today (run-2's §Escalations were all `[info]`-tagged). Re-verified live this session on the new HEAD:

```
$ dev/lib/run-in-env.sh dune build
exit=0
$ dev/lib/run-in-env.sh dune runtest --force
exit=0
```

Both exit 0 on pre-dispatch HEAD `0af6c63`. **No `[critical]` items to carry forward.**

[info] Carried-forward verification: no still-real `[critical]` items; main green on `0af6c63`.

All carried-forward `[info]` items from prior runs re-carried into today's §Escalations as observed: qc-structural H3 false-positive (exercised + corrected on PR #858), qc-structural review-file persistence (exercised + corrected on PR #858), schema drift (unchanged), magic_numbers linter false-positives (not exercised). NEW [info] this run: duplication on #848 forward fix between #860 (maintainer) + #861 (agent).

## Per-run links

- [run 1] `dev/daily/2026-05-05.md` (this file — feat-data on #848 + harness on #840 follow-ups; both QC pipelines APPROVED q=5/5)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
