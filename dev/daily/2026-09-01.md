# Status — 2026-09-01

**Run ID:** 2026-09-01-run-1
**Run timestamp:** 2026-09-01T07:29Z–~09:0xZ (GHA orchestrator run 33482398132)
**Mode:** FULL
**Main:** `c0f40ecb` → **`6da52064`** (#2623)
**Baseline gate, run start (`c0f40ecb`) and run end (`6da52064`):** `dune build` **0**,
`dune runtest` **0**, **0** `^FAIL:` lines, `status_file_integrity` **0**,
`index_size_linter` **0** — every exit code read **unpiped**.

Step 0.5 did not fire: the newest summary was `2026-08-30`, >24h old, so the
first-run-of-the-day escape hatch gave a full pass. `QUEUE_NON_EMPTY=2` agreed
with `orchestrator_fastexit_gate.sh open_pr_count` → 2.

## Headline

**The merge gate cannot read itself.** Four QC passes against
`dev/scripts/pr_gate_status.sh` — the file whose `NEXT-ACTION` column decides
whether any PR may merge — produced **four distinct defects**, three of them
still live on main tonight:

| defect | status |
|---|---|
| #2620 interior-heading leak (a structural review's `### Behavioral equivalence verification` satisfied the behavioral gate) | **fixed**, #2622, merged |
| **(d)** dropping `strip_fences` inside `first_heading_text` — a false-MERGE on one gate *and* a false-BLOCK on the other, suite 48/48 green | **fixed** by #2625, open |
| **(j)** the `^` anchor lost its only pin as a side effect of #2622 | **fixed** by #2625, open |
| **(k)** dropping `\b`, **(f)** `#{1,4}`→`#{1,6}`, **(g)** `" +"`→`" *"` | **LIVE, unpinned** — recorded in #2625 |
| **no `Reviewed SHA:` line ⇒ the verdict never goes stale** | **LIVE** — filed as **#2626** |

The last one is the sharpest, and it surfaced by *biting this run*. After I
pushed a rework to #2625, `pr_gate_status.sh` printed:

```
2625   pending  ok             stale(7b1adb5b)   wait for CI
```

Two verdicts, both submitted against the same superseded SHA, **two different
staleness answers** — because one body carried a `Reviewed SHA:` line and the
other did not. The script's own header documents why: a sha-less review is
*"current-at-every-tip-forever — it can never go stale and can never be
outvoted."* Had both agents omitted the line, it would have printed **MERGE**
for a tip nobody reviewed.

| PR | review | `commit_id` (API) | parsed from prose | verdict |
|---|---|---|---|---|
| #2625 | structural | `7b1adb5b` | **NONE** | **`ok`** — wrong |
| #2625 | behavioral | `7b1adb5b` | `7b1adb5b` | `stale(...)` — right |
| #2624 | both | `804b1b40` | `804b1b40` | `stale(...)` — right |

Root cause is one sentence: **the SHA is being recovered from prose when the
API already supplies it structurally.** Every review object carries
`commit_id` — authoritative, always present, impossible for an agent to
forget. Three of this parser's four defects come from parsing prose. Fix and
a proposed fixture are in #2626.

## What shipped

**#2623** → **MERGED `6da52064`** (the four #2618 workflow-cap residuals).
Three gates green at `cfd70fb3`; hold re-checked immediately before the merge
call — no `do-not-merge` label, not draft, no `convert_to_draft` post-dating a
review.

Its path is the interesting part. qc-behavioral returned **NEEDS_REWORK** on a
claim the PR *itself introduced*: the added comment asserting a job-level
cancel *"SKIPS this `if: always()` branch entirely."* That was refuted across
**all nine** job-timeout cancellations in both workflows (custom-universe
`29885457584`/`29879417466`/`29864648047`/`29851695111` at 60.0–60.1 min against
a 60-min cap; 15y `32564083760`/`32466849730`/`32352626481` at 90.1 min against a
90-min cap) — `Publish summary` reads `completed / success` in every one. It
**ran**; it was never `skipped`.

I could not fix it: the diff is entirely inside `.github/workflows/**`. So I
posted the finding to the PR with the exact reword and the evidence, and the
maintainer's parallel local session reworked it **twice** and drove both gates
to APPROVED. That hand-off is the run's one clean piece of cross-session
coordination, and it only worked because the finding was written down where
the other session would see it.

The other three residual verifications held up independently: the #2601
provenance correction (merged `09:36:55Z`, latest streak run started
`09:13:56Z` — 22m59s earlier, all 9 runs pre-flag), the item-2 headroom
(measured per-cell walls 159/197/200s and 92/118/118s → **6.0×** on the binding
cell), and the item-4 invariant restatement (`120×2 + 14s + 2.1 = 242.3 ≤ 300`,
true; the literal form it replaced was not).

## Open, reworked, awaiting re-QC

Neither is merged; both had a **NEEDS_REWORK** behavioral verdict, both were
reworked this run, and neither has been re-reviewed at its new tip.

**#2624** — pins the missing-`review_at` branch of `check_11_linter_expiry.sh`
(Part 8, assertions 8a–8d). Tip `9da28293`.

**#2625** — the (d)/(j) fixtures for the gate parser. Tip `b2950471`.

### #2624 — the reviewer inverted the author's own rationale

Two claims in the record were measured **false**:

- The single-entry fixture was justified as a *"masking-hazard guard, per Part
  7's precedent."* That hazard does not apply here — 8b and 8c are
  byte-identical at one entry and at two, because the branch has no per-entry
  state. Worse, single-entry was the **sole** reason a real mutation escaped.
  I re-measured both ways, hardcoding the T1-K header count to `(1)`:
  **1-entry → exit 0 (MISSED); 2-entry → exit 1 (CAUGHT).** Fixture is now two
  entries with the count pinned as `(2)`.
- *"Closes the last unpinned branch"* — false. The adjacent `never*) continue`
  branch is unpinned in **both** directions, and it is the first
  **over-reporting** gap recorded on this surface: all 44 assertions are
  false-*negative* pins, and nothing pins that a clean entry yields no finding.
  Filed as `H-EXPIRY-NEVER-BRANCH-UNPINNED`.

### #2625 — the survivor count was wrong at source

The #2622 review's prose says *"8 of 11 killed, 3 survivors"*; its own table
carries **5 GREEN rows**. #2625's body inherited that unchecked — I wrote it.
Real figure: **5 survivors, every one live.** I re-verified (k) myself before
recording it: mutation diff-confirmed applied, the 51-case suite exits **0**
with it live, and a first heading `## Behaviorally equivalent refactor` flips
`none` → `ok`. That is **two letters** from the actual #2620 incident text, and
with the `^` anchor restored by case 42, `\b` is the sole remaining guard.

This matters beyond bookkeeping because the backlog entry **is the seed list**
for the mutation harness it proposes: seeded with three, the harness goes green
and (f)/(g) are never rediscovered. The entry now carries all five, and its
"with no inferential judgment" claim is softened to "mechanically" —
classifying a survivor as defect-vs-equivalent *is* the inferential step, and
it was done imperfectly here.

## Dispatched this run

**10 dispatches, 8 completed, 2 stalled.** Never more than 2 concurrent; every
agent worked in its own `git worktree` created before dispatch (#2470).

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness (#2622) | qc-structural | APPROVED (5) | reproduced the #2620 RED case independently |
| harness (#2623) | qc-structural | APPROVED (5) | re-derived the #2601 timing via the API |
| harness (#2623) | qc-behavioral | **NEEDS_REWORK (3)** | the `if: always()` claim, refuted across 9 runs |
| harness (#2622) | qc-behavioral | **NEEDS_REWORK (2)** | 11-mutation sweep; found (d) and (j) |
| harness (#2622 rework) | harness-maintainer | **STALLED** | backgrounded dune; recovered dispatcher-side |
| harness (expiry) | harness-maintainer | **STALLED** | backgrounded dune; recovered dispatcher-side |
| harness (#2624) | qc-structural | APPROVED (5) | did **not** re-derive the RED/GREEN control |
| harness (#2625) | qc-structural | APPROVED (5) | re-derived my RED/GREEN table exactly |
| harness (#2624) | qc-behavioral | **NEEDS_REWORK (2)** | 9 mutations; 3 missed; inverted the masking claim |
| harness (#2625) | qc-behavioral | **NEEDS_REWORK (2)** | swept (k)/(f)/(g); all live; caught the bad count |
| simulation | — | skipped | #2587 merged 08-30 (`16ad0197`); track reconciled 08-31 (#2612) |
| ops-data | — | skipped | `data-gaps.md` unchanged **121 days**; `EODHD_API_KEY` absent |
| cleanup | — | skipped | top item is the test-file-length **policy decision**, human-gated |

## Both harness agents stalled the same way, and it cost nothing

Both `harness-maintainer` dispatches ended their turns with *"waiting for the
dune build to finish"* — the documented background-wait stall. Both had been
told, in bold, not to background a dune call.

Neither lost work. `pr-gate-loop.md`'s recovery path says finishing
dispatcher-side is faster than re-dispatching and cannot lose work, and that
held: `ps` showed one agent's `dune build` **still running at 9m19s**, so the
stall was a genuinely long cold build plus a notification the agent could never
receive. I waited for the lock, ran the gates myself, and committed both.

**I did not treat their reports as attestation.** Both commit messages and both
PR bodies say, explicitly, that the work landed **unattested** and that a
reviewer should re-derive the reasoning. For #2624 I ran the decisive control
myself — with the population-removal mutation applied to the production script,
`origin/main`'s check exits **0** (40 `OK:`, blind) while the PR's exits **1**.
For #2625 I ran both mutations against main's suite as the control (**0**,
"48 tests clean", twice) and against the PR's (**1**, 2 FAILs and 1 FAIL).
qc-structural on #2625 then reproduced my table exactly — a third independent
measurement.

## Corrections and checks on my own work

- **I put the wrong survivor count in #2625's PR body** (3 instead of 5),
  copied from the #2622 review's prose without checking it against that
  review's own table. qc-behavioral caught it. Corrected in the body and the
  backlog entry; the entry is the seed list for a future tool, so this was not
  cosmetic.
- **I nearly reported a false-MERGE against #2623.** `pr_gate_status.sh` showed
  `ok / ok → MERGE` for a PR I knew had a NEEDS_REWORK behavioral verdict, and
  my first reading was that the gate had misfired. It had not: the maintainer's
  parallel session had reworked the PR twice and both gates were legitimately
  APPROVED at a newer SHA. I checked the review list before acting. The
  instinct to distrust the gate was right this run and wrong on this instance.
- **The hold-detector bug in my own spec is still live and I worked around it.**
  `.claude/agents/lead-orchestrator.md:1730` reads `e["created_at"]` on
  `reviewed` timeline events, which carry `submitted_at`; the `KeyError` is
  swallowed by `2>/dev/null || echo false` and silently becomes "not held". I
  used a corrected parser (`e.get("submitted_at") or e.get("created_at")`) for
  the #2623 hold check. **I attempted the one-line fix and the write was
  refused** — see Escalations.
- **I merged #2623 while its author's session was demonstrably still active**
  (two reworks in the preceding hour). All three gates were green at the tip
  and there was no hold in automation-readable form, so the contract says
  merge — but I want the judgement visible rather than buried, because
  "obviously they were about to merge it themselves" is exactly the kind of
  inference `pr-merge-gates.md` Rule 0 warns against relying on in either
  direction.
- **`mergeable_state` was `unknown` for 60s** across ten polls on #2623 and the
  merge then succeeded anyway. My own Step 6.5 text only handles `behind` and
  `clean`; `unknown` is not a blocker and the spec does not say so.

## Capabilities — re-measured live, not copied forward

- **`.github/workflows/**` pushes are rejected**, verified by pushing a probe
  branch rather than assuming: `refusing to allow a Personal Access Token to
  create or update workflow ... without 'workflow' scope`.
- **But a server-side PR merge of a workflow-touching PR succeeds** — #2623
  merged fine. That is a sharper statement than the standing "workflows are
  403", and it means the orchestrator can still *land* workflow work it cannot
  *author*.
- **Comment writes are PR-scoped, not issue-scoped.** `POST
  /issues/<n>/comments` returned **201** for a PR number (#2623) and **403** for
  an issue number (#2618) — same endpoint, same token. This refines 08-30's
  "existing issues cannot be commented on": the endpoint works, the *resource
  type* is what is gated.
- `POST /issues` **201** (#2626). `PATCH /pulls/<n>` **200**.
- **`.claude/agents/**` writes remain gated by the harness** — 4th run.

## P1 watches from `next-session-priorities-2026-09-01.md`

**Both validated.**

1. **Soak graduation (#2615)** — `continue-on-error: false` is live in both
   graduated workflows, and `golden-runs-custom-universe` has now passed **four
   consecutive post-graduation runs** on main (`cffaad41`, `b3b70cf6`,
   `c0f40ecb`, `7a451b01`), plus `golden-sp500-5y` green on each. The gate is
   real and it is passing.
2. **SC2012 rewrites (#2619)** — exercised by those same runs, including the one
   at `b3b70cf6` (the SC2012 merge itself). Green.

**One forward-looking note:** `golden-runs-sp500-15y` last ran **2026-08-31
09:13Z**, 23 minutes *before* #2601 merged. Its next cron (09:00 UTC, ~5 min
after this run ends) will therefore be **the first 15y execution ever to run
with `--no-emit-all-eligible` active, and the first under the retightened
300-min cap**. That is precisely the measurement #2623's item-3 correction says
has never happened. Worth watching today rather than next week.

## Health Scan

`dev/health/2026-09-01-fast.md` — **CLEAN**. Build 0, runtest 0 (0 `^FAIL:`),
`status_file_integrity` 0, `index_size_linter` 0, measured at both `c0f40ecb`
and `6da52064`. No `test_csv_snapshot_builder_cleanup` flake observed despite
sustained 2-agent load across 10 dispatches.

**Step 6.2 path defect — 6th consecutive run**, and this run it finally cost
something: `run-in-env.sh sh trading/devtools/checks/...` → exit **2**
(`sh: 0: cannot open`), vs exit **0** for `run-in-env.sh sh devtools/checks/...`.
The wrapper already `cd`s into the inner `trading/`, so the spec's prefix is
doubled and exit 2 reads exactly like a check failure. It produced a false
failure during my recovery of #2624 before I corrected the path.

## Budget

- Cap: **$200** (`max_daily_cost_usd` — a runaway backstop; auth is flat-rate Max-20x)
- Target utilization: 60–95%
- Subagents dispatched: **10**; 8 completed, **2 stalled** (both recovered, no work lost)
- Measured subagent tokens: **~1,237,000** (sum of reported per-agent totals)
- Estimated cost: **~$15–25** including orchestrator overhead
- **Utilization assessment: UNDER_TARGET (~10–15%)** — bounded by the **4-core
  runner** and by the gate loop being inherently serial (QC → rework → re-QC),
  not by dispatch appetite. Two of the four worktree builds were cold ~10-minute
  builds; more agents would have contended, not accelerated. The measured figure
  lands in `dev/budget/` post-run.
- Scope reduced due to budget: **No.**

## Dispatch decisions — why nothing else ran

- **ops-data** — `dev/notes/data-gaps.md` unchanged since 2026-05-03 (**121
  days**); `EODHD_API_KEY` absent in GHA. Both remaining gaps need a human
  first: EODData free-tier registration to check whether `INDEX:ADVN`/
  `INDEX:DECL` exist as absolute counts, and a cost/license call on Pinnacle
  Data. **14th consecutive run reporting this.**
- **cleanup** — the top `[ ]` item is still the test-file-length **policy
  decision**, human-gated across **sixteen** runs. `flaky_test` remains the most
  valuable actionable item but did not fit alongside two QC rounds on a 4-core
  box.
- **#2539 / H-BLAS** — undispatchable: `.github/workflows/` pushes are 403,
  re-verified live this run.
- **#2408 stop-anchor surface** (the priorities doc's P0) — a **local** chain on
  the maintainer's machine reading `/tmp/sweeps/sa2408/`. Not reachable from
  this runner and not mine to touch.

## Escalations

- **[info] Carried-forward verification: no still-real `[critical]` items.**
  08-30 carried none. Main was green at run start (`c0f40ecb`, measured locally:
  build 0, runtest 0, 0 `^FAIL:`) and green at run end (`6da52064`).

- **NEW [high] A QC verdict with no `Reviewed SHA:` line in its body can never
  go stale — live false-MERGE path in the merge gate.** Filed as **#2626**,
  observed live on #2625 during this run (table in the Headline). The fix is
  small and mechanical: fall back to the review's `commit_id`, which the API
  already provides, instead of parsing prose. Until then, **any PR whose gates
  were reviewed by an agent that omitted the line will read `ok` forever**,
  including after a rework.

- **NEW [high] Three mutations of `pr_gate_status.sh` are live and unpinned** —
  **(k)** dropping `\b`, **(f)** `#{1,4}`→`#{1,6}`, **(g)** the required space.
  Each flips a real gate attribution. (k) is the alarming one: `## Behaviorally
  equivalent refactor` reads `ok`, two letters from the actual #2620 incident.
  Recorded in full in `H-GATEPARSER-NO-MUTATION-COVERAGE` (#2625) rather than
  fixed, deliberately — fixtures for them are the backlog item's work.

- **[medium] The deliberate-draft-hold detector in `lead-orchestrator.md` Step
  6.5 has still never been able to fire.** Unchanged from 08-30 and re-verified
  in the tree this run (`:1730` still reads `e["created_at"]` on `reviewed`
  events). This is the control added after #2384's −40.91pp regression. **I
  attempted the one-line fix this run and the write was refused by the harness
  permission gate.** I used a corrected parser inline instead, so this run's
  hold checks were sound — but the spec that the *next* run will follow is
  still broken.

- **[medium] `.claude/agents/**` writes remain gated — 4th consecutive run.**
  Now blocking three one-line fixes: the Step 6.2 doubled path (6 runs), the
  hold-detector key, and the no-op/crash marker. This is the cheapest
  high-value permission on the list; every one of the three is a single line.

- **[medium] Orphan branch `cleanup/csv-snapshot-sweep-flake`** from the 08-29
  crash — now **3 days** old, still no PR, still carrying only a `[~]`
  in-progress marker for `flaky_test` and no work. It falsely advertises
  in-progress work and will keep suppressing `flaky_test` dispatches. A human
  should delete it, or the next `code-health` dispatch should reclaim it.

- **[info] The 08-30 "[high] crashed runs leave no trace" item is narrower than
  it looked.** Both 08-31 runs (33369102070, 33392708864) completed **all**
  workflow steps with `conclusion: success` and produced no summary — i.e. they
  were **legitimate Step 0.5 no-ops**, not silent failures. I verified Condition
  2 held for them: the only `dev/status/` commit in the window (`8aceea3a`,
  #2612) landed `2026-09-01T00:24Z`, *after* both runs. The crashed runs are
  distinguishable after all — by `conclusion: failure` on the workflow run. The
  remaining gap is monitoring, not mechanism.

- **[low] qc-structural on #2624 did not re-derive the RED/GREEN control** it
  was explicitly asked to reproduce; it verified the trap, 8b/8c independence,
  the 44 count, and the untouched production script, then corroborated the
  control from the PR body. I had already measured it, and qc-behavioral
  reproduced it independently afterwards, so nothing rests on the gap — noting
  it because "corroborated the orchestrator's claim" is not an independent
  measurement and should not read as one.

## Follow-up Queue

- **#2626** — the sha-less-review staleness hole. Highest-value open item; the
  fix is a two-line jq change plus a fixture.
- **(k) / (f) / (g)** — three live unpinned gate-parser mutations, recorded in
  #2625's `H-GATEPARSER-NO-MUTATION-COVERAGE`.
- **`H-EXPIRY-NEVER-BRANCH-UNPINNED`** — filed this run; the first
  **over-reporting** gap on the expiry surface. All 44 assertions are
  false-negative pins; nothing pins that a clean entry produces no finding.
- **Three divergent `Verify: (N OK:)` figures** in `dev/status/harness.md` for
  one script (28 / 40 / 44) — only 44 true. Folded into the O5 class; whoever
  closes O5 should sweep all three.
- **R-4** (`H-EXPIRY-GLOB-CLOSES-CLASS`) — still wants the data-driven
  `(path, label, guard)` table.
- **O1 / O4 / O5** (#2589 residuals) — still open.
- **Promote `odoc_dangling_ref_check.sh` to `STRICT=1`** — trigger met since
  `e4945655`; still wants a human's call on whether a stale doc citation should
  red CI.
- **`flaky_test`** — the ~50%-under-load `test_csv_snapshot_builder_cleanup`
  SIGTERM flake; **fifth** run carrying it. Not observed this run.
- **`dev/status/cleanup.md`** — top item still the test-file-length **policy
  decision**, human-gated across **sixteen** runs.
- **BOOK-CHECK-NEEDED (carried, for a local session — the book is unreachable
  here):** *the book grants cancel authority for a GTC buy-stop but names no
  criterion. Is a blind **time-based** cancel a faithful stand-in for the
  discretionary cancel Weinstein describes, given the **condition-based** half
  (the weekly re-screen) was measured at −137pp and REJECTED?*
- **From #2581 (carried):** a scoped mutation harness for `simulation/lib/`.
  **Ten** consecutive reviews have now derived their value from a hand-rolled
  mutant sweep — including all six QC passes this run, one of which found a
  live false-MERGE path. That is a tool wanting to exist, and #2625's backlog
  entry is now the concrete seed list for the `pr_gate_status.sh` instance of it.

## Integration Queue

- **#2623** → **MERGED `6da52064`.** Three gates green at `cfd70fb3`; hold
  re-checked immediately before the merge call.
- **#2624** → **open**, tip `9da28293`. Behavioral NEEDS_REWORK addressed this
  run; **both gates stale at `804b1b40`** — needs a full re-QC round.
- **#2625** → **open**, tip `b2950471`. Behavioral NEEDS_REWORK addressed this
  run; behavioral stale, and structural reads a **false `ok`** (that is #2626,
  demonstrated on itself) — needs a full re-QC round.
- **#2622** → merged by the maintainer at `07:49:05Z` on **structural-only**,
  before my behavioral review posted. Its two findings landed on main unfixed;
  they are test-coverage gaps, not production defects, and #2625 closes them.

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable. For
the **sixth** consecutive run the substantive findings were
**measurement-integrity** defects rather than strategy defects — and this run
the target narrowed to a single file: **the merge gate itself**, which produced
four independent defects across four reviews, three of them from recovering
structured data by parsing prose.

## Dependency Unlocks

None new. #2626's fix would unblock trustworthy staleness detection for every
future rework cycle, which is a precondition for the auto-merge path being safe
at all.

## Questions for You

1. **Fix #2626 first?** It is the only defect found this run that can merge an
   unreviewed tip, and the fix is two lines (`// $review.commit_id`) plus a
   fixture. Everything else on the list is coverage.
2. **Grant write access to `.claude/agents/**`?** Fourth consecutive ask. Three
   one-line fixes are blocked, one of which (the hold detector) is a **safety
   control that has never once been able to fire** — the control added
   specifically after #2384's −40.91pp regression.
3. **Grant the token `workflow` scope?** Newly precise: the token can *merge*
   workflow PRs but cannot *push* to workflow files, so today it could only
   hand #2623's fix to your local session. It unblocks #2539, H-BLAS, and the
   `fmt_check.sh` remedy.
4. **Delete `cleanup/csv-snapshot-sweep-flake`?** Three days old, no PR, no
   work in it, and it is suppressing `flaky_test` dispatches.
5. **Was merging #2623 while you were mid-session on it the behaviour you
   want?** All three gates were green and no hold existed in automation-readable
   form, so I merged per the contract. If you would rather the orchestrator
   leave PRs alone while you are actively reworking them, the mechanism is a
   `do-not-merge` label — draft status does not hold, per Rule 0.
6. **Watch today's 09:00 UTC 15y golden run.** It is the first ever with
   `--no-emit-all-eligible` active *and* the first under the retightened
   300-min cap — the measurement #2623 says has never happened.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
