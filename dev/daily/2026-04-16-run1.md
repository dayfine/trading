# Status — 2026-04-16 (run1)

This is the first orchestrator run executed inside the GitHub Actions
`trading-devcontainer` container (per `dev/status/orchestrator-automation.md`).
`TRADING_IN_CONTAINER=1` was set; VCS was **git** (jj is available in the image
but the `actions/checkout` working tree is git-only, no `.jj/`). `jst submit`
cannot open PRs from a git-only checkout; the orchestrator fell back to the
GitHub REST API via `curl` with the `GH_TOKEN` env var. Both subagent branches
landed as draft PRs via this fallback — see §Integration Queue and §Escalations.

## Feature Progress

### harness / T3-A  [DONE]
- Done today: extended `trading/devtools/checks/deep_scan.sh` with **Check 7 — harness scaffolding review** (3 heuristics: H1 scripts not referenced, H2 linter binaries not wired, H3 broken agent path references). Output lands in a new `## Harness Scaffolding` section of `dev/health/<DATE>-deep.md`. Updated `.claude/agents/health-scanner.md` Phase 2 Step 8 and `docs/design/harness-engineering-plan.md` T3-A annotation. Plan doc at `dev/plans/t3a-harness-scaffolding-review-2026-04-16.md`.
- Status: `[x]` in `dev/status/harness.md`; `dev/status/_index.md` row updated.
- In progress: none.
- Blocked: No.
- Recent commits (branch `harness/t3a-harness-scaffolding-review`):
  - `f0b911f` — mark T3-A in-progress
  - `1bfec4b` — plan doc
  - `6eec563` — Check 7 in deep_scan.sh
  - `a76b6f1` — completion note + agent def update
- PR: **#373** (draft, opened via REST fallback)
- Verification: current `deep_scan.sh` run produced `dev/health/2026-04-16-deep.md` with `## Harness Scaffolding` section — all 12 components (9 shell scripts + 3 OCaml binaries) PASS.

### sector-data / Item 4.1 (REIT + royalty-trust rescue)  [DONE]
- Done today: added `Keep_if_sector` rule variant to `trading/analysis/scripts/universe_filter/lib/universe_filter_lib.{mli,ml}`; updated `dev/config/universe_filter/default.sexp` to rescue `sector ∈ {Real Estate, Energy, Materials}` before the `Name_pattern` drop fires. 3 new unit tests added; existing tests updated to match the 4-rule default shape.
- Dry-run delta: Real Estate +51, Energy +1, Materials +1 — total kept goes from 4,916 to ~4,969; dropped from 4,125 to ~4,072. All 51 REIT false positives (AAT, ABR, AKR, BRT, …) and the 5 royalty trusts (MTR, NRT, PBT, PVL, MSB) are back in the universe. `AAAA` (fund, ETF in name) still dropped. `SPY` (allow-list) still preserved.
- Status: Item 4.1 marked complete in `dev/status/sector-data.md`; `dev/status/_index.md` Next task for `sector-data` still points to Item 2 (one-shot fetcher run).
- Blocked: No.
- Recent commits (branch `ops/sector-filter-reit-rescue`):
  - `4757a04` — rule variant
  - `861323f` — default.sexp update
  - `0f9bd10` — status + index updates
- PR: **#374** (draft, opened via REST fallback)

### backtest-infra  [IN_PROGRESS — deferred]
- Done today: nothing (deferred — upstream dependency still unresolved).
- Reason: next item (support-floor-based stops) requires a new primitive in `weinstein/stops/`, owned by feat-weinstein. Per run1 escalation on 2026-04-14 ("expand feat-backtest scope or dispatch feat-weinstein for the stops feature"), the human decision has not yet landed in `dev/decisions.md`. See §Escalations.
- Blocked: Yes — cross-track stops-module work pending.

### strategy-wiring  [MERGED]
- PR #355 merged to main on 2026-04-15. overall_qc APPROVED. Nothing to do this run.

## QC Status

- No feature-track PR reached READY_FOR_REVIEW in this run — QC pipeline was a no-op.
- Harness (#373) and ops-data (#374) PRs are draft and do not flow through `qc-structural` / `qc-behavioral` (per orchestrator Step 5 — those stages run only for feat-track READY_FOR_REVIEW).
- Prior-run QC review artefacts: `dev/reviews/strategy-wiring.md` APPROVED.

## Budget

- Budget cap: **$50** (from `dev/config/merge-policy.json` `max_daily_cost_usd`).
- Subagents spawned: **3**.

| Agent | Model | Status | Est. tokens | Est. cost |
|-------|-------|--------|-------------|-----------|
| harness-maintainer (T3-A) | sonnet | completed | ~89k | ~$0.55 |
| ops-data (Item 4.1) | sonnet | completed | ~76k | ~$0.45 |
| health-scanner (fast scan) | sonnet | completed | ~54k | ~$0.30 |

- Killed mid-flight: **none**.
- Orchestrator overhead (this session, rough): ~$2.
- Budget utilization: ~$3 / $50 (~6%).
- Scope reduced due to budget: No — the conservative dispatch (2 subagents + 1 health scanner, sequential) was a deliberate coordination choice for the first GHA orchestrator run, not a budget cap.

## Data Operations

- Gaps resolved this run:
  - sector-data Item 4.1 (REIT / royalty-trust rescue) — filter config now preserves 51 real-estate rows + 5 royalty trusts that the iteration-2 default rule was sweeping up.
- Gaps still blocked:
  - **ADL live source**: human decision needed (EODData registration to check absolute count tickers, or Pinnacle evaluation). Current mitigation: synthetic ADL covers post-2020-02-10; Unicorn covers 1965–2020. Acceptable for macro regime filter (corr ~0.85–0.90 target).
  - **sector-data Item 2** (one-shot Finviz fetch, ~2.2h / ~8k HTTP): unsuitable for a subagent session. `origin/ops/finviz-checkpoint-and-skip` has the regex+checkpoint fix from the human (2026-04-15 commit 6a53b1d), not yet merged. Next step = merge that PR and run the fetcher out-of-band.
- Data fetched this run: none (no EODHD key; fetcher unchanged).

## Harness Work

- Item worked on: **T3-A — health-scanner deep scan harness scaffolding review**
- Status: **DONE**
- Branch: `harness/t3a-harness-scaffolding-review`
- PR: **#373** (draft)

## Health Scan

- Result: **FINDINGS (1 critical)**
- Critical: **7 test suites / 38 total assertion failures on `main`** — `test_sector_map`, `test_screener_e2e`, `regression_test` (screener), `test_macro_e2e`, `test_portfolio_risk_e2e`, `test_weinstein_backtest`, `test_weinstein_strategy_smoke`. Root cause per health-scanner's analysis: `Data_path.default_data_dir()` falls back to `/workspaces/trading-1/data` (Docker workspace path) when `TRADING_DATA_DIR` is unset, but this path does not exist in the GHA runner (`/__w/trading/trading`). Previous fast-scan (2026-04-14) was PASSING — **this is a GHA-environment regression, not a code regression**.
- Recommendation from health-scanner: set `TRADING_DATA_DIR` in the workflow env (e.g. `$GITHUB_WORKSPACE/test_data`), OR add `(env_vars TRADING_DATA_DIR)` to the affected dune test stanzas so they pick it up from the job env.
- See: `dev/health/2026-04-16-fast.md`. Also: `dev/health/2026-04-16-deep.md` (emitted by the harness-maintainer as part of T3-A verification — 12/12 harness components PASS on Check 7).

## Follow-up Queue

Totals (from `## Follow-up` / `## Follow-up items` / `## Follow-ups` sections in each track's status file):

- `backtest-infra`: 5
- `data-layer`: 5
- `harness`: 5
- `portfolio-stops`: 1
- `simulation`: 12
- `sector-data`: open Items 4.1 (DONE this run) → 5, 6, 7 still open
- **Total: ~28** (threshold: 10; maintenance cycle: every 3rd run — this run qualifies).

This run did NOT swap in a formal maintenance pass because:
1. The `simulation` tail (12 items) is dominated by TODOs and milestone-deferred work (M5 walk-forward, bar-granularity engine changes), not extractable refactors.
2. The `harness` tail (5 items) is the known-gap list the orchestrator already drains every run.
3. A single-item tick-off like T3-A is roughly equivalent in value to a maintenance pass, and was delivered today.

Worth revisiting next run if the `backtest-infra` queue (added 2026-04-16 — 4 items) or the simulation queue grows further.

## Integration Queue

(Open PRs that target main, filtered to this orchestrator's work + known in-flight items.)

- **#373** (`harness/t3a-harness-scaffolding-review`) — draft, opened by orchestrator REST fallback. Harness work, no QC pipeline needed. Ready for human review + ready-for-review flip.
- **#374** (`ops/sector-filter-reit-rescue`) — draft, opened by orchestrator REST fallback. Tests pass locally in the dune cache; human review for the filter-semantics decision (option (a) REIT rescue).
- **origin/ops/finviz-checkpoint-and-skip** — human-authored branch from 2026-04-15 (commit 6a53b1d — critical regex fix + checkpoint + always-skip for Finviz fetcher regression). Not owned by this orchestrator run; flagged in §Escalations as "needs human to open the PR and merge before Item 2 can execute."
- **origin/harness/dispatch-flow-revise** — stale (~since 2026-04-14 per prior-run notes). Candidate for cleanup.
- **origin/deps/opam-weekly**, **origin/docs/backtest-data-followups**, **origin/docs/decisions-update**, **origin/dev/reviews/strategy-wiring-structural**, **origin/dev/reviews/strategy-wiring-behavioral** — existing branches, state not verified this run (no `gh` in container).

## Current Milestone Target

M5 — Walk-forward backtest + parameter tuner. Requires:
- Support-floor-based stops primitive in `weinstein/stops/` (blocks backtest-infra; see §Escalations).
- Backtest experiment framework formalization (`dev/experiments/<name>/` shape) — deferred until 1–2 more experiments run.
- Annualized-return metric clarity (backtest-infra follow-up #2, queued 2026-04-16).

## Dependency Unlocks

- Strategy-wiring MERGED → `Ad_bars.load` now composes Synthetic ADL on top of Unicorn; `Macro_inputs.default_global_indices` wired. No remaining macro-input gaps on the strategy side; all ADL degradation is now about source quality, not wiring.
- Item 4.1 landing (draft PR #374) will unlock more accurate REIT/royalty-trust exposure in the backtest universe once merged — expect a small but real delta in the 2018–2023 and 2015–2020 golden scenarios. Track as an experiment rerun per backtest-infra follow-up #3.

## Escalations

Items that need human decision or out-of-band human action. (Pipeline-failing on the GHA workflow's post-step per `.github/workflows/orchestrator.yml`'s `## Escalations` check.)

- **Support-floor-based stops, cross-track dispatch decision** (carried forward from 2026-04-14-run1): feat-backtest cannot land the support-floor stops experiment until the primitive ships in `weinstein/stops/`. Owner decision is outstanding:
  - (a) Reopen feat-weinstein scope for one stops-primitive task, then feat-backtest picks up the experiment next run.
  - (b) Expand feat-backtest's scope to include the stops-module change with explicit review.
  Please pick (a) or (b) in `dev/decisions.md`.

- **GHA container tooling gap — `gh` + `jj git init --colocate`**: `actions/checkout@v4` creates a git-only tree. `jst submit` therefore fails (requires `.jj/`), and the container has no `gh` CLI installed (only `curl`). Today's fallback: open PRs directly via the REST API with `GH_TOKEN`. Two options to make this clean:
  - (1) Add `gh` CLI to the `trading-devcontainer` base image and have the orchestrator / subagents use `gh pr create` directly.
  - (2) Have the workflow run `jj git init --colocate && jj git fetch` as a pre-step, so `jst submit` works inside the job.
  Option (1) is lower-risk; option (2) aligns the container with local-dev tooling.

- **CI / test environment regression on `main`**: 7 test suites / 38 failures inside the GHA container because `TRADING_DATA_DIR` is unset and tests fall back to `/workspaces/trading-1/data` (Docker path). **This is an environment regression, not a code regression** — locally the same commits pass. Recommend setting `TRADING_DATA_DIR: ${{ github.workspace }}/test_data` at the job env level and/or adding `(env_vars TRADING_DATA_DIR)` to the affected dune test stanzas. See `dev/health/2026-04-16-fast.md` §Critical.

- **`origin/ops/finviz-checkpoint-and-skip` has no open PR**: critical fetcher regex fix + checkpoint/resume work from the human (2026-04-15, commit 6a53b1d). Orchestrator did not touch it. Needs human to open the PR so Item 2 (one-shot fetcher run) becomes tractable. Fix sector-data Next task after that lands.

- **ADL live source (unchanged)**: human action still required (EODData registration to check `INDEX:ADVN`/`INDEX:DECL` absolute counts, or Pinnacle evaluation). Mitigation in place via synthetic ADL.

- **Stale bookmarks + stale branches**: `origin/harness/dispatch-flow-revise` (no open PR, ~2 days stale); the local-bookmark list from 2026-04-14-run1 is not re-verified this run. Recommend periodic prune via `git push origin --delete <name>` — but only after confirming no open PR points at each branch.

## Questions for You — RESOLVED 2026-04-16

All five items below have been decided / handled. Recorded in `dev/decisions.md` §Direction Changes 2026-04-16 for future sessions.

1. **Support-floor stops** — **(a) reopen feat-weinstein.** Agent definition rewritten (`.claude/agents/feat-weinstein.md`) to scope `Support_floor.find_recent_low` + wire into `Stops.compute_initial_stop`. New status file at `dev/status/support-floor-stops.md`. PR #379 records the change; `backtest-infra` flipped to BLOCKED until the primitive lands.
2. **PR #373** (harness/T3-A scaffolding review) — merged (was green; flipped out of draft).
3. **PR #374** (ops Item 4.1 REIT rescue) — merged (CI-fmt issue fixed). Stop-buffer goldens rerun follow-up stays queued in `dev/status/backtest-infra.md` §Follow-ups item 3, not blocking.
4. **GHA tooling fix** — **(2) jj git init --colocate pre-step** chosen; see PR #378. Keeps base image as-is; aligns container tooling with local jj-based workflow.
5. **`TRADING_DATA_DIR`** — set at job env level to `${{ github.workspace }}/trading/test_data` (same as `ci.yml`). Landed as PR #377.

## Operational cleanup

- Stale escalations re-verified: `origin/ops/finviz-checkpoint-and-skip` actually merged as #359; `origin/harness/dispatch-flow-revise` merged as #338; `origin/docs/backtest-data-followups` merged as #370; `origin/docs/decisions-update` merged as #335. Confirmed safe to prune. `deps/opam-weekly` + `dev/reviews/strategy-wiring-{structural,behavioral}` had no open PRs — also pruned.
- **ADL live source** — decision: synthetic-only is final. Dropped from future escalations.

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session locally, or trigger the orchestrator workflow manually via `workflow_dispatch`.)
