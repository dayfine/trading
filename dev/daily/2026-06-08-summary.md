# Consolidated Summary — 2026-06-08
Runs included: run-1, run-2, run-3 (3 total)
Generated: 2026-06-08T20:22:27Z

## Pending work (from last run)

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-perf (rolling-start / WF-CV) | blocked (data-gated) | — | — | #1491 (maintainer) shipped `--snapshot-dir` so N≥1000 WF-CV reads pre-built snapshots; the broad-PIT WF-CV *experiment runs* (P0 stale-exit grid, P1 ledger re-check, P2 breadth-robustness curve) need deep top-3000 PIT snapshots + `EODHD_API_KEY` → maintainer-local. rolling-start worst-vol-decile DEFERRED (regime-tag infra, plan §P3); multi-start E2E `run` data-gated. |
| simulation | blocked (data-gated) | — | — | Stale-exit (#1487) shipped default-off; its period×universe confirmation grid (promote-to-default-on) is broad-PIT data-gated → maintainer-local. |
| stage-accuracy / spy-only-reference | blocked (data-gated / protected-core) | — | — | 6 consecutive single-dial DD levers REJECTED/FRAGILE; **breadth is the lever** (priorities-doc + 6 rejections). Breadth test data-gated in GHA; partial-trim needs protected Position-core partial-exit. New single-dial work explicitly deferred (priorities-doc). |
| experiment-platform | blocked (data-gated) | — | — | P1 re-check of the TWO breadth-sensitive ledger verdicts (laggard-rotation, continuation-buy) on top-3000 PIT — candidate-supply-sensitive, data-gated. |
| data-foundations | blocked (maintainer-decision) | — | — | READY_FOR_REVIEW — bars-retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close. |
| tuning | blocked (maintainer-enablement) | — | — | M2 (qNEHVI) not yet enabled for GHA dispatch (precedent #1327). |

## Dispatched across all runs (deduped)
| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-perf (rolling-start P3) | feat-backtest | **completed → merged** | Built `Convexity_stats` (`time_underwater_pct`/`tail_ratio`/`return_skew`) + 17 tests (51 in dir). worst-vol-decile deferred (regime-tag infra). Draft PR #1479. |
| backtest-perf (rolling-start P3) | qc-structural | **APPROVED** (q=5) | Reviewed SHA `34cd3d43`. H1–H3, P1–P6, A1–A3 all PASS — but ran *targeted* runtest, missing the codebase-wide nesting linter (see rework). |
| backtest-perf (rolling-start P3) | qc-behavioral | **APPROVED** (q=5) | Reviewed SHA `34cd3d43`. CP1–CP4 PASS (mutation-style numeric checks confirmed); Weinstein domain block NA — analysis-only tooling. |
| backtest-perf (rolling-start P3) | — (gate suite, CI) | **NEEDS_REWORK** | CI `build-and-test` full `dune runtest` caught `FAIL: nesting linter` on `time_underwater_pct` + `_variance` (avg>3.0/max>5) — gate missed by all targeted runtests; CI is the backstop. |
| backtest-perf (rolling-start P3) | orchestrator (deterministic) | **rework iter 1** | Authored mechanical behaviour-preserving fix `a4241829` (extracted fold-step helpers `_underwater_step`/`_add_sq_dev`; no `@nesting-ok`). Full runtest then green (nesting OK, 3254 fns). |
| backtest-perf (rolling-start P3) | qc-structural (re-review) | **APPROVED** (q=5) | Reviewed SHA `a4241829` (review 4446374862); FULL `dune runtest` verified green this time. |
| backtest-perf (rolling-start P3) | — (Step 6.5) | **auto-merged** | All 3 gates green @ `a4241829` (CI build-and-test + perf-tier1-smoke SUCCESS; both QC APPROVED). `mergeable_state: clean`; squash-merged via REST → `cf006d71`; branch deleted. |
| stage-accuracy / experiment-platform / spy-only-reference / data-foundations / tuning / sweep-perf | — | skipped | Data-gated (deep PIT OHLCV absent + no `EODHD_API_KEY`), protected Position-core, core-perf, or awaiting maintainer GHA-enablement (see §Pending work). Unchanged from run-3. |
| harness | — | skipped | No eligible T1/T3 item — Tier 1 drained (all `[x]`); only T2 (milestone-gated), T3-C (superseded), T3-H (low-pri, conditional on T1-N stability), T4 (human-request-only) remain. `harness.md` unchanged since 2026-05-22. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged since 2026-05-03 (#814). ADL + sector-metadata gaps need human input / live HTTP. |
| code-health | — | skipped | `dev/status/cleanup.md` §Backlog has no real `[ ]` finding (all `[x]`); deterministic Step 6 surfaced no new `[medium]`/`[high]`. |
| backtest-perf / simulation / stage-accuracy / spy-only-reference / experiment-platform / data-foundations / tuning | — | skipped | All forward increments broad-PIT data-gated (deep top-3000 PIT OHLCV + `EODHD_API_KEY` absent in GHA), protected-core, or awaiting maintainer GHA-enablement. The #1481 OOM fix unblocks the *local* broad-universe path, not GHA. |

## QC across all runs
(no QC entries found)

## Budget (summed across runs)
- Total subagents spawned: 4
- Killed mid-flight: None reported across all runs.

## Escalations (merged)
- [info] **Main CI red on `fc23fe24` (#1478) is a disk-exhaustion infra flake, not a regression** — `/usr/bin/ld: final link failed: No space left on device` (run 27104161942); no `^FAIL:`; parent `20452974` green; local + post-merge builds green. **Orchestrator token lacks `actions:write` (could not re-run jobs) and `issues:write` (could not comment on #1478) — both REST attempts returned 403.** Action for human: re-run failed jobs on run 27104161942 (or simply confirm `cf006d71`'s green CI supersedes it) and close #1478. If runner disk-exhaustion recurs across runs it becomes a trend worth a CI-image/cleanup fix — one occurrence is a flake.
- [info] **Orchestrator GH token scope gap** (new this run) — the bot token can create/merge PRs + post PR reviews (`pull_requests:write`, `contents:write`) but lacks `actions:write` + `issues:write`. This blocks (a) re-running failed CI jobs to clear transient red, and (b) commenting on / closing watchdog issues. Granting these two scopes would let the orchestrator self-heal infra-flake red main + manage `ci-red` watchdog issues autonomously.
- [info] **Nesting linter (and other `devtools/checks` linters) are NOT run by a dir-scoped `dune runtest <dir>`** — the orchestrator's targeted deterministic gate and both QC agents' targeted runtest all missed the P3 nesting violation; only CI's full `dune runtest` (the backstop per `pr-merge-gates.md`) caught it. **Process tightening:** the orchestrator's pre-QC deterministic gate should run the FULL `dune runtest` (or at minimum `dune runtest devtools/checks/`) on the feature branch, and QC dispatch prompts should instruct a FULL runtest. Applied ad hoc this run (re-QC ran full runtest); worth codifying.
- [info] **Breadth / broad-universe test is the single highest-value unblock — DATA-GATED >30 days** (carried; track-pacer #1469 + 6th-rejected-dial conclusion). Adding `EODHD_API_KEY` as a GHA secret + plumbing it through `.github/workflows/orchestrator.yml` is the top unblock — it would let the orchestrator run the broad-universe fetch + test autonomously AND drive the rolling-start `run` E2E. Maintainer working the local side (#1474: N=3000 OOM at COVID). (carried, ~16th run.)
- [info] **Pause single-dial strategy-mechanism exploration** (track-pacer #1469 §Rec 2). Six consecutive levers landed default-off and REJECTED/FRAGILE. Redirect stage-accuracy / experiment-platform effort behind the breadth result rather than building a 7th dial. Default with no answer: hold new-dial work.
- [info] **data-foundations READY_FOR_REVIEW ~3 weeks** (track-pacer #1469 §Rec 3) — maintainer to decide bars-retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close the track.
- [info] **`gh` / `jst` not available in the GHA env** (carried) — orchestrator creates/merges PRs + posts QC reviews via `curl` REST; worked cleanly this run (#1479 auto-merged via REST `PUT /merge`; QC posted COMMENT reviews via REST; the structural re-review's own REST POST didn't land from the agent, so the orchestrator posted the transcribed verdict).
- [info] **Stale ci-watchdog issues #1280 + #1264 + #1320** (carried) — plus the new #1478 (infra flake, above). Human triage to confirm/close (seen in: run-1,run-2,run-3)
- [info] **15y split-day adjustment regression on `weinstein_strategy.ml`** (carried) — Investigation #998 root-caused; fix pending; touches core, surfaced as a feat-weinstein scope-decision item (seen in: run-1,run-2,run-3)
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (carried, ~20th run) — `tuning.md`: M2 not yet enabled for GHA dispatch (precedent #1327) (seen in: run-1,run-2,run-3)
- [info] **sweep-perf Win-#4 production wiring not declared orchestrator-eligible** (carried) — add an `enable sweep-perf Win-#4 for GHA dispatch` signal to dispatch it (seen in: run-1,run-2,run-3)
- [info] **stage-accuracy partial-trim variant needs Position-core partial-exit support** (carried) — modifies the protected `Position` core module → design/scope-decision for the maintainer. Lower priority post-REJECT.
- [info] **Winding-down tracks** (track-pacer #1469 §Rec 5) — orchestrator-automation (Phase 1 stable / Phase 2 deferred) + harness (Tier 1 done, T3 drained) are candidates to flip to a steady-state/closed marker.
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~24th run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled (track-pacer #1469 §Rec 6). Not a bare `[critical]` (does not trip the gate-fail).
- [info] **Carried-forward verification: no still-real bare `[critical]` items; main GREEN** (build+runtest exit 0 on `7da9d8b9`; #1478 CLOSED; 0 open `ci-red` issues) (seen in: run-2,run-3)
- [info] **Breadth / broad-universe test — the single highest-value unblock — is now LOCAL-unblocked (#1481 OOM fix) but still GHA DATA-GATED.** Adding `EODHD_API_KEY` as a GHA secret + plumbing it through `.github/workflows/orchestrator.yml` (and staging the deep top-3000 PIT snapshots) is the top unblock — it would let the orchestrator run the stale-exit promotion grid (P0), the ledger re-check (P1), and the breadth-robustness curve (P2) autonomously. (carried, ~17th run; the local blocker just cleared, so the GHA-data gate is now the *sole* remaining barrier.) (seen in: run-2,run-3)
- [info] **Decide the `stale_exit_after_days` default + lock the honest broad-PIT baseline** (priorities-doc P0). #1487 shipped default-off; it changes results (frees + redeploys cash), so per flag-discipline it needs a period×universe confirmation grid before default-on. Honest top-3000 re-baseline showed it is *not* free (can raise MaxDD). Maintainer to run the grid (data-gated) and either promote default-on or always set `=5` in broad-PIT specs (seen in: run-2,run-3)
- [info] **Pause single-dial strategy-mechanism exploration** (carried, track-pacer #1469 §Rec 2). 6 consecutive levers REJECTED/FRAGILE; redirect behind the breadth result. Priorities-doc explicitly defers single-dial mechanics ("no broad-grounded motivating signal beyond breadth itself"). Default with no answer: hold new-dial work (seen in: run-2,run-3)
- [info] **Orchestrator GH token scope gap** (carried from run-1) — bot token can create/merge PRs + post PR reviews but lacks `actions:write` + `issues:write`. Granting these would let the orchestrator re-run transient-failed CI to clear infra-flake red main and manage `ci-red` watchdog issues autonomously. (#1478 was cleared by a maintainer this run, validating the need.)
- [info] **data-foundations READY_FOR_REVIEW** — maintainer to decide bars-retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close the track (seen in: run-2,run-3)
- [info] **`gh`/`jst` not available in the GHA env** (carried) — orchestrator creates/merges PRs + posts QC reviews via `curl` REST. No dispatch this run, so only the daily-summary PR exercises it (seen in: run-2,run-3)
- [info] **~90 stale `.claude/worktrees/agent-*` accumulated** (priorities-doc note) — disk 13%, below the 85% sweep threshold; the SessionStart hook will sweep when disk pressure rises. No action needed now (seen in: run-2,run-3)
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~25th run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled. Not a bare `[critical]` (does not trip the gate-fail).
- [info] **`Weekly start sweep (BAH SPY)` workflow `failure` on `94dff0ee`** (new) — a scheduled weekly sweep workflow, NOT a main-CI gate (main `CI`/`perf-tier1`/goldens all SUCCESS). May be the recurring sweep-disk / data-availability flake (`.claude/rules/sweep-hygiene.md`). Human triage if the weekly sweep matters; does not block the orchestrator.
- [info] **Orchestrator GH token scope gap** (carried) — bot token can create/merge PRs + post PR reviews but lacks `actions:write` + `issues:write`. Granting these would let the orchestrator re-run transient-failed CI to clear infra-flake red main and manage `ci-red` watchdog issues autonomously.
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~26th run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled. Not a bare `[critical]` (does not trip the gate-fail).

## Integration Queue (last run's view)

- **#1469** `ops: weekly track pacer 2026-06-07` (`ops/track-pacer-2026-06-07`, docs-only) —
  generated by the weekly track-pacer workflow, not orchestrator-dispatched; QC-exempt (docs-only).
  Out of the orchestrator's Step 6.5 lane — left for the human / its own automation. (Still open.)
- **No orchestrator feature PR is awaiting merge** — nothing dispatched this run.

## Per-run links
- run-1 -> `2026-06-08.md`
- run-2 -> `2026-06-08-run2.md`
- run-3 -> `2026-06-08-run3.md`
