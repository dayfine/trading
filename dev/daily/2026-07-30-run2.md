# Status — 2026-07-30 [run 2]

Run timestamp: 2026-07-30T13:00Z– (GHA orchestrator run 30544387701)
Run ID: 2026-07-30-run-2

**Mode:** IN PROGRESS — provisional summary, written up-front on purpose.

## Why this file exists before the run finished

Run 1 today (`30524920933`) established that **a truncated run is
indistinguishable from a no-op** and loses its summary, its cost record, and any
PR it stranded (`A-SUMMARY-STALE-FALLBACK`). Run 1 itself then truncated — it
dispatched two agents and died before either produced a durable artefact, so
its own file still reads `IN PROGRESS` on `main` (merged as `bf159f7f` via
#2174).

This run applies the same self-discipline run 1 recommended: the summary file
and its PR exist from the first minute and get rewritten in place at Step 7.

**If this file still says `IN PROGRESS`, the run was truncated** — treat the
sections below as the state at truncation.

## Established before dispatch (verified this run)

- **Main is GREEN** at `bf159f7f`: `build-and-test` SUCCESS, `perf-tier1-smoke`
  SUCCESS, `golden-sp500-5y` SUCCESS, `golden-custom-universe` SUCCESS. Read
  from the check-runs API. (`golden-sp500-15y` shows `cancelled` — a
  concurrency-cancelled long job, not a failure.)
- **Step 1c — no still-real `[critical]` items.** Run 1 carried no `[critical]`
  escalations (its four were `[high]`/`[info]`), and main is green. Nothing to
  carry forward at `[critical]`.
- **Step 0.5 did NOT fast-exit.** Condition 1 genuinely FAILS: `harness` has
  open PR #2169 at tip `993a1437` against `dev/reviews/harness.md`'s
  `Reviewed SHA: f37379e0`. Real unreviewed work was queued, so the
  `A-FASTEXIT-VACUOUS` override question did not need exercising this run either.
- **Run 1's two dispatches produced nothing durable.** Verified, not assumed:
  `dev/reviews/harness.md` still carries `Reviewed SHA: f37379e0` (PR #2155's),
  #2169 still has **zero** reviews, and #2166 is still a **claim commit only**
  (`dev/status/cleanup.md` +1 −1, tip `fa8c1595` unchanged since 2026-07-29
  16:55Z). Both work items were re-dispatched fresh this run.
- **Toolchain re-verified, correcting the standing note further.** `gh` ABSENT,
  `docker` ABSENT, but **`dune` 3.24.1 works** via
  `bash dev/lib/run-in-env.sh dune …`. Confirms run 1's correction: the "dune
  unavailable" conclusion in earlier runs was an `sh`-vs-`bash` invocation bug
  (`set: Illegal option -o pipefail`), not an absent toolchain. Pre-supplied in
  both briefs this run so neither agent burns cycles rediscovering it.
- **`jj` IS present** at `/usr/local/bin/jj` in this image, contrary to the
  spec's "jj is not available in the GHA container" (Step 4). Immaterial — the
  GHA path uses plain git regardless — but the spec's claim is stale.

## Scope this run — the maintainer's handoff assigns it explicitly

`dev/notes/next-session-priorities-2026-07-30.md` §P1 (read off PR #2173's
branch, since the doc is not yet on `main`):

> Two orchestrator PRs open at handoff: #2166 (adjusted-basis rescale sync
> pin), #2169 (atomic write_audit.sh) — its 2-slot loop owns them
> [non-blocking].

So this run's whole thrust is finishing those two, plus unblocking the handoff
doc itself.

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | qc-structural | in flight | first review of stranded #2169 (~14h unreviewed, zero reviews) |
| cleanup | code-health | in flight | resume #2166 — implement the sync pin for real |

## Escalations

- **[high] `A-SUMMARY-STALE-FALLBACK` — carried from run 1, still unfixed.**
  Root cause is the `ls -t dev/daily/*.md` cross-day fallback in
  `.github/workflows/orchestrator.yml` (*Locate daily summary + detect no-op*):
  a run that writes no summary for today silently inherits a prior day's, so it
  reports success, escapes the *Fail on escalations* gate, and orphans its cost
  record. Fix is a hard `::error::` on a missing today-summary. **Blocked on the
  `workflow`-scoped token.** Run 1 is now itself an instance: it truncated
  without a Step 7 rewrite.

- **[high] `A-FINISH-PROTOCOL-BACKGROUND` — third consecutive instance.**
  Run 1 dispatched two agents and ended without either landing an artefact,
  matching `30472186063` and `30496638558` yesterday. This run mitigates the
  same way (up-front summary) and additionally pre-supplied the verified
  toolchain facts + an explicit foreground-`dune runtest` instruction in both
  briefs, since a backgrounded verification step is the suspected mechanism.

- **[info] `gh`/`docker` absent, `dune` reachable via `bash` wrapper.** Not new;
  logged so the correction keeps propagating.

## Questions for You

Carried and still unanswered — full text in `dev/daily/2026-07-29.md`
§Questions for You. Priority order unchanged from run 1:

1. **The `workflow`-scoped token.** Seven filed defects share it; the unified
   `A-SUMMARY-STALE-FALLBACK` root cause is the highest-value one.
2. **Sanction or correct `A-FASTEXIT-VACUOUS`.** Not exercised in either of
   today's runs (real work was queued both times), so still undecided.
3. Edit permission on `.claude/agents/lead-orchestrator.md` (ten runs deep).
4. `gh` in the image vs converting call sites to REST.
