# Status — 2026-06-17 [run 3]

Run timestamp: 2026-06-17T12:00Z (GHA orchestrator run 27687153814)
Run ID: 2026-06-17-run-3

**Mode:** FULL PASS / **1 DISPATCH** (first non-no-op run today). Main GREEN on
`3fab6c91`. A new ci-watchdog issue (#1634) appeared since run 2 — verified as a
**runner disk-exhaustion infra flake** (ENOSPC during `ld` linking) on a docs-only
commit, already superseded by the green HEAD. Its **root cause was found** and a fix
designed, but pushing it is **blocked on `workflow` token scope** (see Escalations).
Dispatched `harness-maintainer` on this — the one genuinely GHA-eligible, non-gated
piece of work. Every feature track remains data-gated, human-gated, or LOCAL
(snapshot-format-v2 S4 is the maintainer's active LOCAL P0).

**HEAD:** `3fab6c91` (GREEN) — unchanged from run 2 (no code merged since; the only
on-main activity was run 2's own summary PR #1632).

## Step 0 rampup check (deterministic)

- HEAD `3fab6c91` CI: `build-and-test` SUCCESS, `perf-tier1-smoke` SUCCESS,
  `golden-sp500-5y` SUCCESS, `golden-custom-universe` SUCCESS, `perf-tier2-nightly`
  SUCCESS; `golden-sp500-15y` + `orchestrator` in_progress (= this run). **Main GREEN.**
- **1 open `ci-red` issue: #1634** — NOT a hard stop. Verified stale: it fired on the
  PRIOR commit `74acd21` (docs-only, one notes file) whose `build-and-test` failed with
  `/usr/bin/ld: final link failed: No space left on device` (disk exhaustion during
  linking of `test_grid_search.exe`), and the immediately-following docs-only commit
  `3fab6c91` passed `build-and-test` cleanly on the same code tree. No `^FAIL:` linter
  line; it is the documented infra-flake class (ENOSPC), so the merge-gate "any FAIL
  means real failure" rule does not apply. Could not auto-close #1634 (token lacks
  `issues:write` — 403); flagged for the human in §Escalations.

## Step 0.5 saturated-queue check

**Evaluated — NOT a no-op exit.** Escape hatch N/A (run 1 + run 2 today exist).
Conditions 1 (0 open PRs at run start → vacuous PASS), 2 (no `dev/status` change since
run 2), and 4 (harness/cleanup backlogs unchanged) would individually pass — but the
no-op exit is **correctly NOT taken** because a **new state appeared since run 2**: the
ci-watchdog issue #1634 (a fresh red-main signal requiring verification + a stale-close)
AND a genuinely GHA-eligible, non-gated harness fix (the ENOSPC root cause). The
saturated-queue heuristic exists to skip a pass when there is nothing to do; there was
real new work, so a full pass + dispatch is the conservative, correct outcome.

## Step 1b drift cross-reference

Run 2's `## Pending work` rows were all `blocked` (data/human/LOCAL-gated). No
`dispatched`/`awaiting-merge` row left hanging → **no `[drift]` warnings.** No new
feature-track status drift since run 2 (HEAD unchanged; nothing merged).

## Step 1c carried-`[critical]` verification

Run 2 carried **no bare `[critical]`** (only `[info]` + one `[critical-decision-needed]`
M6.6 live-cycle, which is explicitly NOT a gate-tripping `[critical]`). Re-verified
main GREEN on `3fab6c91`; status-integrity + index-size linters exit 0 live. The new
ci-red issue #1634 is the only "red" signal — verified as a superseded infra flake
(above), so it is surfaced as `[info]` (stale, human-close needed), **not** `[critical]`.

- **[info] Carried-forward verification: no still-real bare `[critical]` items; main GREEN
  (build-and-test SUCCESS on `3fab6c91`). #1634 = stale infra flake, not a real red.**

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness (CI disk-headroom) | blocked (human: `workflow` token scope) | harness/ci-disk-headroom | #1636 | Apply the exact ci.yml YAML in PR #1636's body (free ~11.4 GB baked `_build` + `df -h` diagnostic), push with a `workflow`-scoped token, merge. Self-validates on:pull_request. |
| snapshot-format-v2 (S4 warehouse regen) | blocked (in-flight LOCAL) | — | — | Maintainer's active LOCAL P0; S0-S3 + #1631 Rosetta fix done; #1633 records "S4 PROVEN". S4 regen + goldens-bit-identical gate needs local warehouses → not GHA-dispatchable. |
| backtest-infra / backtest-perf / rolling-start-lens / stage-accuracy / experiment-platform | blocked (data-gated, LOCAL) | — | — | P2 matrix + factor-lens causal + WF-CV re-runs gate on the deep PIT / top-3000 warehouse, itself gated on snapshot S4 (or a Docker RAM bump). |
| short-side-strategy | blocked (human/data-gated) | — | — | Phase-5 entry-path wiring (human-gated) + WF-CV `short_min_price` (data-gated). |
| weekly-snapshot (M6.6 live cycle) | blocked (human-gated) | — | — | Generator (#1588) + first baseline (#1598) + C2 test (#1596) MERGED; live cycle unowned + human-gated. |
| cash-floor-correctness | blocked (human/data-gated) | — | — | NS1 done (#1567/#1582); NS2 impl human-gated; NS4 DD-validation data-gated. |
| margin / long-short (Initiative B, Phase 5) | blocked (human-gated, LOCAL) | — | — | Touches core Portfolio (A1 flag); default-off; human-supervised session. The profit lever. |
| tuning (M2 qNEHVI) | blocked (maintainer-enablement) | — | — | Awaiting the #1327 enable-commit. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness (ci-disk-headroom) | harness-maintainer | completed (fix designed; push BLOCKED) | Found root cause (CI image bakes unused ~11 GB `_build` under /workspaces/trading-1/ → runner disk exhaustion during ocamlopt link). Fix committed locally (`645ef2e5`: free ~11.4 GB + `df -h` diagnostic). Could NOT push `.github/workflows/ci.yml` — token lacks `workflow` scope. Opened PR #1636 (harness.md note + exact YAML in body). |
| snapshot-format-v2 (S4) | — | skipped | Active LOCAL P0; collision-avoidance per `gha-local-coordination.md`; S4 gate needs local warehouses absent in GHA. |
| all other feature tracks | — | skipped | Data-gated, human-gated, or LOCAL-routed (see Pending work). No GHA-eligible non-gated surface. |
| code-health | — | skipped | `cleanup.md` §Backlog all `[x]`; no new `[medium]`/`[high]` findings. |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged since 2026-05-03. |

## Feature Progress

No feature code shipped or merged this run. The only dispatch was the harness CI
disk-headroom fix (above), which is blocked on token scope. Off-orchestrator state is
unchanged from run 2 (HEAD `3fab6c91`; snapshot-format-v2 NEXT=S4, fenced to LOCAL).

## QC Status
- PR #1636 (harness): NOT reviewed — its only pushed diff is `dev/status/harness.md`
  (docs-only → both QC gates skipped per `pr-merge-gates.md`). The substantive ci.yml
  change is unpushable (workflow scope) so there is nothing for QC to review yet.
- No other PRs open or reviewed this run.

## Budget
- Budget cap: **$200.00** (`merge-policy.json`; runaway-dispatch backstop on a flat-rate
  Max-20x subscription — the $ figure does not bill). `auto_merge_enabled: true`;
  `target_utilization` band 60–95%.
- Subagents spawned: **1** (harness-maintainer).
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | harness-maintainer | (inherited) | completed (push blocked) | ~153K subagent tokens | ~$1.0 (estimated) |
- Estimated cost this run (estimated, not measured): **~$1.5** — 1 subagent (~153K tokens)
  + orchestrator state reads, CI/PR REST polling, two in-container linter runs, index
  reconcile, summary write. No `dune build`, no merges.
- Utilization assessment: **UNDER_TARGET — "all-work-done."** The one eligible non-gated
  item was dispatched; it surfaced a token-scope blocker. Everything else is gated/LOCAL.
  Nothing skipped for budget. Low $ is by design (flat-rate; cap is a backstop).
- Killed mid-flight: No.
- Scope reduced due to budget: No.

## Data Operations
- ops-data did not run. `EODHD_API_KEY` absent in GHA; `dev/notes/data-gaps.md` unchanged
  since 2026-05-03. No actionable gap.

## Harness Work
- **Item:** CI disk-headroom fix — recurring ENOSPC during `dune build` linking (issue #1634).
- **Status:** Fix DESIGNED + committed locally; **BLOCKED** on `workflow` token scope.
- **Branch:** `harness/ci-disk-headroom`; **PR:** #1636 (open).
- **Root cause:** the CI image bakes an unused ~11 GB copy of `trading/_build` under
  `/workspaces/trading-1/` (the job builds the `/__w/` checkout, not the baked snapshot,
  per the ci.yml header). On smaller ubuntu-latest runners (~12 GB free after setup),
  the `_build` cache restore + linking many static OCaml test `.exe` files crosses the
  line → `/usr/bin/ld: final link failed: No space left on device`.
- **Fix (in PR #1636 body, exact YAML):** add a `Free disk headroom` step after
  `Cache _build` (`rm -rf /workspaces/trading-1/trading/_build` + inactive opam `5.3`
  switch + opam download-cache/log; ~11.4 GB freed) plus a `df -h` diagnostic step
  immediately before `dune build`. Self-validates because `ci.yml` runs `on: pull_request`.
- **Why blocked:** the GHA orchestrator's tokens (`BOT_GITHUB_TOKEN` PAT + GitHub App
  token) lack `workflow` scope; GitHub rejects any push/ref-update/contents-API write to
  `.github/workflows/*.yml` without it. A human with a `workflow`-scoped PAT must apply
  the YAML (the referenced commit hash in the PR body is NOT reachable — apply the YAML
  by hand; a clarifying PR comment was posted).

## Health Scan
(From `dev/health/2026-06-17-fast-run3.md` + deterministic Step 6 checks)
- **Result: CLEAN** for main health.
- Build gate (6.1): CI on `3fab6c91` GREEN (authoritative). Did not re-run local
  `dune build && dune runtest` (CI green; and this would risk the same runner ENOSPC class).
- Status integrity (6.2): `status_file_integrity.sh` exit 0; `index_size_linter.sh` exit 0
  (7303/20480 bytes, all rows ≤ 250).
- **New finding [medium]:** the recurring CI-infra ENOSPC-during-link — root cause now
  identified, fix designed, blocked on token scope (above). Intermittent; main green.

## Follow-up Queue
Total open followup items across status files: **~43** (carried; > 10 threshold). **Not
actionable for a maintenance dispatch** — they live in data-gated / human-gated / LOCAL
in-flight tracks. No maintenance cycle swap warranted this run.

## Integration Queue
- **PR #1636 (harness, ci-disk-headroom):** awaiting **human action** (apply the ci.yml
  YAML with a `workflow`-scoped token, then merge). Held from auto-merge: the pushed diff
  is only a status note; the substantive fix is unpushable here. Blast-radius-wide CI
  change → human review intended regardless.
- Otherwise empty.

## Current Milestone Target
- **GHA-eligible non-gated surface — essentially drained**, save infra fixes that hit the
  `workflow`-scope wall. The durable headline work (snapshot-format-v2 S4→S5, "S4 PROVEN"
  per #1633) is the maintainer's LOCAL P0; the broad-universe long-only / short-side
  cluster + M6.6 live cycle remain data- or human-gated.
- **Highest-leverage unblock (carried):** the 26y/top-3000 matrix perf path — S0-S3 +
  Rosetta fix landed; **S4 (warehouse regen, LOCAL)** is the single remaining step.

## Dependency Unlocks
- None cross-track this run.

## Escalations
(Main GREEN on `3fab6c91`; linters exit 0 live. **No bare `[critical]`.**)

- [info] **`workflow` token scope needed — NEW, now load-bearing.** The orchestrator
  cannot push `.github/workflows/*.yml`, so the CI disk-headroom fix it designed cannot
  land autonomously (PR #1636 carries only a status note + the exact YAML in its body).
  **Action:** grant the orchestrator token `workflow` scope, OR a human applies the YAML
  in PR #1636 with a `workflow`-scoped PAT. This generalizes the carried
  `issues:write`/`actions:write` request — the orchestrator's token is under-scoped for
  CI/infra self-repair.
- [info] **Stale ci-watchdog issue #1634 — please close.** Fired on docs-only `74acd21`'s
  ENOSPC link flake; superseded by green HEAD `3fab6c91`. The orchestrator could not close
  it (token lacks `issues:write` — 403). A clarifying assessment is posted on PR #1636.
- [info] **Recurring CI-infra ENOSPC-during-link — ROOT CAUSE FOUND** (was carried as a
  vague "runner disk exhaustion"). It is the unused ~11 GB baked `_build` snapshot in the
  CI image. Fix is one PR away once `workflow` scope is available (see PR #1636).
- [info] **CI-runner sizing (larger/paid runner) — maintainer billing decision.**
  Deliberately excluded from the harness fix scope. The free-disk fix should resolve the
  ENOSPC without a runner upgrade; raise only if ENOSPC recurs after the fix lands.
- [info] **Broad-universe / deep PIT warehouse — single highest-leverage GHA unblock,
  still DATA-GATED** (carried; ≥5 tracks gate on it). Add `EODHD_API_KEY` as a GHA secret
  + stage the deep top-3000 PIT warehouse to let the orchestrator run these autonomously.
- [info] **26y/top-3000 matrix perf decision — narrowing** (carried) — RAM bump (quick)
  vs snapshot-format-v2 (durable; #1633 records "S4 PROVEN"; only S4 regen remains LOCAL).
- [info] **Design-doc drift accumulating (advisory)** (carried) — 2026-06-15 deep scan
  flags ~22 dirs not mentioned in their design/plan docs; "Action required: NO."
- [info] **Followup accumulation = ~43 (> 10 threshold)** but all on gated/LOCAL tracks
  → no actionable maintenance dispatch.
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (#1327, carried).
- [info] **`enable_short_side` default still `true`** — flip needs a ledger ACCEPT +
  confirmation grid (carried).
- [critical-decision-needed] **M6.6 live cycle** (carried) — live `DATA_SOURCE` + cron +
  alerts + trading-state durability; the next capability milestone, unowned + human-gated.
  Generator (#1588) + first baseline (#1598) + C2 test (#1596) all MERGED. Not a bare
  `[critical]` (does not trip the gate-fail).

## Questions for You
1. **Grant the orchestrator token `workflow` scope (and `issues:write` + `actions:write`)?**
   This run hit all three walls: it could not push the CI fix (`workflow`), close the stale
   #1634 (`issues:write`), or rerun a flaky job (`actions:write`). Granting these turns
   CI/infra self-repair from "design + escalate" into "design + land".
2. **Apply the ci.yml disk-headroom fix in PR #1636** (free the unused 11 GB baked `_build`)
   to stop the recurring ENOSPC red-main noise? One-line manual apply from the PR body.
3. **Snapshot-format-v2 (S4→S5) vs Docker RAM bump:** S0-S3 + #1631 done, #1633 = "S4
   PROVEN"; S4 (warehouse regen, goldens-bit-identical) is the single remaining LOCAL step
   and the durable unblock for the top-3000 matrix. Continue to S4, or bump Docker RAM now?
4. **EODHD_API_KEY in GHA + stage the deep top-3000 PIT warehouse?** (carried — gates the
   28y WF-CV, the data-refresh runbook, and ≥4 other tracks.)
5. **M6.6 live cycle green-light?** Generator + first baseline + C2 test all MERGED.
6. **Resolve tuning M2 qNEHVI enable-commit (#1327)?**
7. **Green-light Initiative B (margin / long-short) Phase-5 in a human-supervised session?**
   The profit lever; touches core Portfolio (A1 flag), default-off keeps goldens bit-equal.

## Per-run links
- [run 1] `dev/daily/2026-06-17.md` — FULL PASS / NO-DISPATCH reconcile. Main GREEN
  `fc81ca8a`. Health CLEAN. 0 merges, 0 subagents (~$0.50).
- [run 2] `dev/daily/2026-06-17-run2.md` — FULL PASS / NO-DISPATCH reconcile. Main GREEN
  `e22b1613`. Health CLEAN. 0 merges, 0 subagents (~$0.50).
- [run 3] `dev/daily/2026-06-17-run3.md` (this file) — FULL PASS / **1 DISPATCH**. Main
  GREEN `3fab6c91`. Found root cause of the recurring CI ENOSPC; fix designed (PR #1636)
  but BLOCKED on `workflow` token scope. Stale ci-red #1634 verified (could not close —
  `issues:write`). Health CLEAN. 0 merges, 1 subagent (~$1.5).

---
## Your Response
(Edit this section. Run `dev/run.sh` after editing to start the next session.)
