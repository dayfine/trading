# Status — 2026-05-01 [run 2]

**Run ID:** 2026-05-01-run-2
**Generated:** 2026-05-01T12:50:24Z
**Mode:** Full pass — Step 0.5 fast-exit declined (would clobber run-1's
unmerged daily-summary PR #730). Same shape as 2026-04-30 run-2.

## Saturated-queue check result

Formal evaluation:

- **Condition 1** PASS — only PR #730 (`ops/daily-2026-05-01`, run-1's
  own daily summary) is open across all branches; no feature-track PRs
  awaiting review.
- **Condition 2** PASS (literal) — local file mtime sets PREV_ISO ≈
  12:41 UTC today (checkout time of `dev/daily/2026-04-30-run2.md`), and
  no commits to `dev/status/*.md` between 12:41 UTC and now. Real-world
  reading: `dev/status/screener.md` (#718) and
  `dev/status/short-side-strategy.md` (#711) DID change since the
  *actual* prior summary commit on main (2026-04-30 run-2 commit
  f9cd682 at 13:29Z), but run-1 already accounted for those — no new
  drift between run-1 and run-2.
- **Condition 3** PASS — no `[drift]` warnings from Step 1b.
- **Condition 4** PASS (literal) — same PREV_ISO short-circuit as C2;
  `dev/status/harness.md` and `dev/status/cleanup.md` unchanged on main
  since prior summary commit either way.

Decision: **decline fast-exit**. Reasoning: PR #730 is open with run-1's
content on the `ops/daily-2026-05-01` branch; writing a no-op summary to
`dev/daily/2026-05-01.md` would clobber run-1's data. Proceeding with a
normal pass to a separate `ops/daily-2026-05-01-run2` branch preserves
both records cleanly. Same precedent as 2026-04-30 run-2.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| short-side-strategy | blocked | — | — | G14 (split-adjustment on `Position.t` Holding state) — first-pass attempt reverted; Option-1 recommendation in `dev/notes/g14-deep-dive-2026-05-01.md`. Owner: `feat-weinstein` — current scope closed; needs scope-extension decision. |
| short-side-strategy | blocked | — | — | G15 (short-side risk control) — phantom `Portfolio_floor` was unintended risk control; with G12+G13 eliminating spurious cascade, sp500 portfolio goes negative. Real candidates: max-short-notional cap + tighter short stops + honest portfolio floor. Owner: `feat-weinstein` — same scope-extension prereq. |
| short-side-strategy | blocked | — | — | sp500-2019-2023 baseline rerun is local-only (491-symbol universe). Maintainer-local action item. |
| short-side-strategy | blocked | — | — | G4 long-side extension + portfolio-peak threshold (per `dev/notes/session-followups-2026-04-29-evening.md` §2). Touches core `Trading_portfolio`; same A1 question as G14/G15. |
| backtest-perf | blocked | — | — | G6 fix surface is in `trading/orders/lib/create_order.ml:_generate_order_id` — A1 core-module per `qc-structural-authority.md`. Forward-guard test landed via PR #703 (merged 2026-04-30). Other follow-ups: §Decision items (4 human/QC sign-off questions); tier-4 first manual `workflow_dispatch` for canonical baseline (local-only); N≥5000 release-gate stays P1. |
| simulation | blocked | — | — | Split-day work fully wrapped. M5 walk-forward + tuner items remain — could be `feat-backtest` scope per agent definition, but baseline still under active investigation (G14/G15 in flight); deferring new architectural scope until baseline stabilises. |
| hybrid-tier | blocked | — | — | Option 1 engine-pool DONE 2026-04-28. Option 2 daily-snapshot streaming P1 future work (~3,000 LOC across 5–8 PRs per `dev/plans/daily-snapshot-streaming-2026-04-27.md`); plan-first; not fill-window. |
| harness | blocked | — | — | Backlog drained: T1 done, T2 milestone-gated, T3-C superseded, T3-H low-priority, T4-* future end-state. No T1/T3 dispatchable items. |
| orchestrator-automation | blocked | — | — | Phase 2 pending. Carried [info]s: H3 false-positive on advisory linter text; review-file persistence gap. |
| cleanup | blocked | — | — | §Backlog empty; no new `[medium]`/`[high]` findings from deterministic Step 6 health checks. |
| cost-tracking | blocked | — | — | Run-1 + run-2 measured costs will land in sibling `dev/budget/2026-05-01-<run_id>.json` post-GHA-capture. |
| ops-data | skipped | — | — | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel rule); EODHD_API_KEY also unset in GHA env. |
| screener | — | — | — | MERGED. Cascade post-stop-out cooldown gate landed via PR #718. |
| trade-audit | — | — | — | MERGED. No work this run. |
| optimal-strategy | — | — | — | MERGED. All planned phases landed 2026-04-28..29. |
| backtest-infra | — | — | — | MERGED. |
| backtest-scale | — | — | — | MERGED. |
| data-panels | — | — | — | MERGED. |
| data-layer | — | — | — | MERGED. |
| portfolio-stops | — | — | — | MERGED. |
| strategy-wiring | — | — | — | MERGED. |
| sector-data | — | — | — | MERGED. |
| support-floor-stops | — | — | — | MERGED. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| — | — | none | No eligible feat-*/harness/ops-data/code-health work this run. The four blocked tracks (G14, G15, G6, G4 long-side extension) all touch core modules outside current `feat-weinstein` and `feat-backtest` scopes; same architectural-exception decision pending as the last several runs. Main has not moved since run-1 generated its summary at 08:14Z (HEAD still `7a8e778`); no new dispatchable surface emerged in the ~4.5h between run-1 and run-2. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel skip per Step 2d). EODHD_API_KEY unset. |
| harness | — | skipped | No dispatchable T1/T3 items (backlog drained). |
| cleanup (Step 2e) | — | skipped | §Backlog empty AND no new `[medium]`/`[high]` findings from deterministic Step 6 health checks. |

Run-2 deliberately does NOT bundle the two `dev/status/*.md` schema repairs
already in run-1's PR #730 — doing so would create a merge conflict
between the two ops PRs. The fixes will land when #730 merges.

## Feature Progress

### short-side-strategy [MERGED — no orchestrator dispatch this run; no maintainer activity since run-1]

- Main HEAD unchanged since run-1 (`7a8e778`, PR #729 merged 07:14Z). The
  21-PR maintainer cycle described in run-1 (G7+G9, sp500 with-shorts,
  reconciler, summary, G11, cycle-gate test, End_of_period exit, screener
  cooldown, warmup-window cleanup, G12, summary metrics overlay, G13,
  G14 deep-dive + first-pass-attempt notes, goldens-sp500 long-only)
  is all already on main and was fully accounted for in run-1's analysis.
- No new code or status changes between 08:14Z (run-1) and now. The
  G14/G15/G6/G4 surface is the same as run-1.
- **Outstanding non-rerun follow-ups** unchanged: G14 (Option 1
  recommendation pending scope decision), G15 (risk-control gap), G4
  long-side extension. All touch core modules; same scope-extension
  question.

### backtest-perf [IN_PROGRESS — no work this run]

- PR #703 (G6 forward-guard test) merged 2026-04-30 12:29Z via 2026-04-30
  run-1's dispatch.
- G6 fix surface (`trading/orders/lib/create_order.ml:_generate_order_id`)
  unchanged: still in core orders module, still blocked on the same
  scope-extension question that gates G14/G15/G4. Carried [info] for the
  fourth consecutive run (2026-04-30 run-1 → run-2 → 2026-05-01 run-1
  → run-2).

### simulation [IN_PROGRESS]

- All split-day broker-model work merged on main. M5 walk-forward + tuner
  items remain.
- Same deferral reasoning as run-1: actively-debugged baselines (G14/G15
  in flight from maintainer) make this the wrong run to add new
  architectural scope. Defer until baseline stabilises post-G14 fix.

### harness [IN_PROGRESS]

- No backlog dispatch this run — T1 done, T2 milestone-gated, T3-C
  superseded, T3-H low-priority. No maintainer-driven harness PRs since
  prior summary (2026-04-30 run-2) either; the harness work has now
  been quiet for two full cycles.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| (none this run — no PRs awaiting QC; no dispatches) | | | | | |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Run-1 estimated cost (per run-1 summary): **~$1** (orchestrator
  overhead only — no subagent dispatches).
- Run-2 spent so far: orchestrator overhead only (no subagents).
  Estimated: **~$1**.
- Subagents dispatched this run: **0**
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | (none — no subagent dispatches) | — | — | — | — |
- Any subagent killed mid-flight: NA.

**Measured cost (from GHA execution file):** Will land in
`dev/budget/2026-05-01-<run_id>.json` post-GHA-capture step (sibling to
run-1's JSON, different `run_id`).

- Total (estimated): ~$1 of $50 cap (~2%) — orchestrator overhead only
  (Read/Edit/Bash/curl tool calls).
- Cumulative day spend (run-1 + run-2 estimate): ~$2 of $50 cap (~4%).
- Utilization assessment: **UNDER_TARGET** (<60%) — actionable-work-floor
  with the same shape as run-1: all dispatchable surface requires
  architectural scope-extension decisions or is local-only or is too
  large for fill-window dispatch. Status: "all-work-done" within current
  scope — no eligible track was skipped for cost or queue-depth reasons.
- Scope reduced due to budget: No — under-target due to actionable-work-floor.

## Data Operations

(ops-data did not run — `dev/notes/data-gaps.md` substantively unchanged
since 2026-04-14, EODHD_API_KEY unset.)

## Harness Work

(harness-maintainer did not run this run — backlog drained; no
dispatchable T1/T3 items. No maintainer-driven harness PRs since prior
summary.)

## Health Scan

From `dev/health/2026-05-01-fast-run2.md` (deterministic per Step 6):

- `dune build` exit **0** (PASSING).
- `dune runtest` exit **0** (PASSING; full test suite green).
- `status_file_integrity.sh` (standalone): exit **1**, two advisory
  failures on main:
  - `screener.md`: `## Status` value still has prose suffix `(with one
    optional knob in flight: cascade post-stop-out cooldown — PR #718)`.
  - `short-side-strategy.md`: `## Last updated` value
    `2026-04-30 (evening)` not YYYY-MM-DD.
  Both already repaired on `ops/daily-2026-05-01` (run-1's PR #730);
  fixes land when #730 merges. Run-2 deliberately does NOT bundle the
  same fixes (would create merge conflict). The integrity check is
  advisory — does not gate `dune runtest`.
- Result: **CLEAN** (build gate); 2 schema drifts already fixed in-flight
  on run-1's branch.

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~9 (unchanged
from run-1).

- **G14 — split-adjustment on `Position.t` Holding state** (NEW
  2026-05-01, carried from run-1): Option-1 fix recommended (pin
  everything to raw close-price space + lookback truncation at
  most-recent-split boundary); first-pass attempt reverted
  (architectural mismatch). Owner: `feat-weinstein`. See
  `dev/notes/g14-deep-dive-2026-05-01.md`.
- **G15 — short-side risk control** (NEW 2026-05-01, carried from
  run-1): real risk surface inadequate now that phantom
  `Portfolio_floor` no longer acts as unintended risk control.
  Candidates: max-short-notional cap, tighter short stops, honest
  portfolio floor. Owner: `feat-weinstein`.
- **G6 fix dispatch** (carried 4th consecutive run):
  `trading/orders/lib/create_order.ml:_generate_order_id` mints order
  IDs with ns-precision timestamp prefix → Hashtbl bucket variance →
  fill order divergence. Out of scope for `feat-backtest`; needs
  core-owner agent.
- **G4 long-side extension + portfolio-peak threshold** (carried 4th
  consecutive run): touches core `Trading_portfolio`; same A1 question.
- **sp500 short-side validation rerun** (post-G7+G9 baseline):
  local-only (491-symbol universe).
- **covid-recovery 20.81% WR investigation** (deferred until short-side
  baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**:
  carried, not exercised this run (no QC).
- **qc-structural review-file persistence gap**: carried, not exercised
  this run.
- **Schema drift from maintainer-driven PRs** (carried [info] from
  run-1): twice in the last two summaries the orchestrator has had to
  patch `status_file_integrity.sh` advisory failures rooted in
  maintainer-written status-file edits that include parenthetical
  context. The integrity linter could become hard-gating, OR the
  maintainer prose could move to a free-form `## Notes` section that
  the linter ignores. Worth surfacing in `dev/status/harness.md`
  §Follow-up next time the harness backlog has slack.

## Integration Queue

- **#730** `ops: daily orchestrator summary 2026-05-01` (run-1) — open,
  awaiting auto-merge via the workflow's "Bundle budget into daily
  summary and auto-merge" step. Includes the two `dev/status/*.md`
  schema repairs (screener.md + short-side-strategy.md) and the
  `_index.md` reconcile of the 21-PR maintainer cycle since prior
  summary.
- (This run — run-2 — produces a sibling `ops/daily-2026-05-01-run2`
  PR pointing at this file.)

## Current Milestone Target

M5 (backtest infrastructure complete + tunable). No incremental
progress this run from orchestrator dispatch. No maintainer-driven
progress in the last ~4.5h either (main HEAD unchanged at `7a8e778`).
The next sequential milestone moves require the same prerequisite list
as run-1:

- ⏭ G14 fix (Option 1 — strategy/position/screener — needs scope decision).
- ⏭ G15 fix (strategy-level risk control — needs scope decision).
- ⏭ G6 fix dispatch (core orders module — same).
- ⏭ G4 long-side extension (core `Trading_portfolio` — same).
- ⏭ sp500 short-side baseline rerun (local-only, post-G14 fix).
- ⏭ Daily-snapshot streaming (Option 2 of hybrid-tier; ~3,000 LOC plan-first).

## Dependency Unlocks

- (none new this run — no new code landed since run-1; no `Interface
  stable: YES` flips that unblock other tracks.)

## Escalations

[info] **G14 — split-adjustment on `Position.t` Holding state** (carried
from run-1). All 5 residual force-liqs in the post-G13 sp500 baseline
are `Per_position` triggers on positions with stale pre-split
`entry_price`. First-pass attempt (`feat/g14-screener-adjusted-prices`)
reverted because Bug A (split-adjusting screener high/low scans) cannot
be fixed alone — symbols with FUTURE splits (e.g., CTAS 2024 4:1) get
`suggested_entry` in adjusted price space while
`Position.t.entry_price` is set in raw space, producing 250%+ phantom
losses on the post-fix path. Recommended fix: **Option 1 — pin
everything to raw close-price space**, with lookback truncation at
most-recent-split boundary. Touches `entry_audit_capture.ml:80`,
`position.ml`, `simulator.ml`, screener high/low scans. Owner:
`feat-weinstein` — current scope is closed; needs scope-extension
decision per the 2026-04-16 precedent before dispatch.

[info] **G15 — short-side risk control** (carried from run-1). With
G12 (#725) + G13 (#726) eliminating the spurious `Portfolio_floor`
cascade, `goldens-sp500/sp500-2019-2023` portfolio goes negative
(-$175K minimum 2021-11-04, -66.7% return, **117.5% MaxDD**). The
phantom floor was acting as an unintended risk control. Real
candidates: (a) max total short notional as fraction of portfolio;
(b) tighter per-position short stop-loss threshold (Weinstein
recommends tighter stops on shorts than longs); (c) honest portfolio-
floor based on actual peak observations (now correct post-G13).
Combination of (a) + (b) likely needed. Owner: `feat-weinstein` —
same scope-extension prereq as G14.

[info] **G6 fix surface is in `trading/orders/lib/create_order.ml`
(core module)** (carried 4th consecutive run). Process question: which
feat-agent should own a core-module fix when neither `feat-backtest`
nor `feat-weinstein` has a current scope covering `trading/orders/`?
Two design choices: (a) replace the timestamp prefix with a
deterministic per-process counter (cheapest, but loses the
"human-readable timestamp in ID" property); (b) replace `Hashtbl`
with a sortable structure (Map.t) and have `list_orders` return in
deterministic order regardless of ID. Forward-guard test (PR #703)
is on main pinning the post-fix invariant.

[info] **G4 long-side extension + portfolio-peak threshold** (carried
4th consecutive run). Same core-module question — touches
`Trading_portfolio`. Sequencing prereq satisfied; remaining blocker
is the scope-extension decision.

[info] **qc-structural recurring H3 false-positive on advisory linter
text** (carried 5+ consecutive runs). Permanent fix still pending:
tighten the agent's prompt to explicitly cite Step 6.1 of
`lead-orchestrator.md` ("trust only the exit code, not stdout `FAIL:`
strings") and require running `dune runtest 2>&1 | tail -3 ;
echo "exit=$?"`. Did not trigger this run (no QC dispatched).

[info] **qc-structural review-file persistence gap** (carried 5+
consecutive runs). Did not recur on prior run's last-known PR review;
not exercised this run.

[info] **Schema drift from maintainer-driven PRs** (carried [info]
from run-1). The same two advisory failures still on main (`screener.md`
prose suffix; `short-side-strategy.md` non-YYYY-MM-DD timestamp);
fixes pending merge of run-1's PR #730. Going forward, two options:
(a) make the integrity linter hard-gating (currently advisory — exit 1
on stdout FAIL but not wired into dune runtest); (b) move free-form
maintainer prose to a `## Notes` section the linter ignores.
Pre-decision; surfacing for the next harness slack window.

[info] **Cumulative day spend low (~4% of $50 cap)**. Same shape as
the prior several runs: all eligible GHA-runnable work dispatched in
prior maintainer cycles; orchestrator's actionable-work-floor is set
by the four pending scope-extension decisions (G14, G15, G6, G4) +
local-only items (sp500 rerun) + plan-first ~3,000 LOC items (Option
2 streaming). Surfacing as `[info]` (not `[critical]` or `[high]`)
because it reflects an actionable-work-floor, not a budget-skip
decision.

[info] **Daily summary PR ordering with run-1's PR #730**. Run-2's PR
will target main on a separate branch (`ops/daily-2026-05-01-run2`).
If run-1's PR merges first, run-2's `dev/health/2026-05-01-fast-run2.md`
+ `dev/daily/2026-05-01-run2.md` (the only files run-2 touches) won't
conflict with run-1's repaired status files. If run-2 merges first,
run-1's PR will need rebase. Either order resolves cleanly because the
two summaries don't touch the same files.

## Step 1b drift cross-reference

Most recent prior summary on local FS: `dev/daily/2026-04-30-run2.md`
(committed via PR #706 at 2026-04-30 13:29Z). Run-1 of today produced
`dev/daily/2026-05-01.md` on `ops/daily-2026-05-01` (PR #730, open,
unmerged) — visible on remote, not on local FS, so falls outside the
literal Step 1b filesystem walk. Treating run-1's summary as the
effective prior context for drift purposes (fetched via REST from the
unmerged branch).

Run-1's `## Pending work` table parsed:
- All 23 rows classified as `blocked`, `skipped`, or `— (MERGED)` — no
  `dispatched`/`awaiting-merge` rows to verify.

Status file commits on main since prior summary commit (PREV_ISO =
2026-04-30T13:29:30Z, excluding `ops:` summary commits):
- `dev/status/screener.md` modified by #718 (post-stop-out cooldown,
  merged 2026-04-30 evening).
- `dev/status/short-side-strategy.md` modified by #711 (sp500 with
  shorts re-enabled, merged 2026-04-30 evening).

Both pre-date run-1 and were already analyzed there. No new drift
between run-1 and run-2.

No `[drift]` warnings emitted.

## Step 1c carried-forward critical verification

Run-1 of today carried zero `[critical]` items into run-2 (all `[info]`).
Build + integrity gates re-verified clean (build exit 0; integrity
standalone exit 1 with two advisory FAILs already fixed on run-1's
branch but not yet merged to main). Run-2 carries forward zero
`[critical]` items.

[info] Carried-forward verification: no still-real `[critical]` items;
main green (build exit 0 on `7a8e778`).

## Per-run links

- [run 1] dev/daily/2026-05-01.md (open PR #730 on `ops/daily-2026-05-01`)
- [run 2] dev/daily/2026-05-01-run2.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
