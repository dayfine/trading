# Status — 2026-07-28 [run 2]

Run timestamp: 2026-07-28T16:50Z– (GHA orchestrator run 30380136239)
Run ID: 2026-07-28-run-2

**Mode:** FULL PASS — **#2145 merged after a clean re-QC, and the run's larger
finding is that this orchestrator's own cost-accounting has been silently broken
for two weeks.**

## Headline

- **#2145 is merged (`790d23a0`).** Both gates APPROVED at quality 4 on the exact
  tip, CI re-verified immediately before the merge. `consecutive_rework_count`
  went **2 → 0**; one more NEEDS_REWORK would have tripped the 3-consecutive
  escalation threshold.
- **The behavioral reviewer retracted two of its own prior-round prescriptions,
  under its own re-measurement.** Its previous review said a particular test
  "kills M4"; re-running M4 showed **14 of 15 tests passed**, *including the exact
  test shape it had prescribed*. The implementer had pushed back on that
  prescription last run and was right. The reviewer wrote that up as a review
  defect rather than quietly adopting the author's fix. That is the single most
  valuable thing in this run.
- **31 orphaned budget records, `$283.31` of measured spend, were sitting off
  `main` — some since 2026-07-14.** Recovered into this run's PR. The
  consequence is not bookkeeping: **run 1's `[high]` budget escalation was
  computed from the incomplete set and understated it.** 2026-07-27 was
  **`$463.74` (232% of cap)**, not `$405.30` (203%); 2026-07-26 was **`$191.91`
  (96%)**, not `$107.77` (54%). A metric whose entire job is to detect overrun was
  itself reporting low.
- **Two orchestrator runs today exited `success` having written no summary at
  all** (`30353290609`, `30366079322` — `$21.03` between them). One of them
  mutated PR #2145 (a branch update at 14:15:17Z) with no artefact recording it.
  Both exited **voluntarily** at 33 and 51 turns of a 200 cap — not a timeout,
  which was my first hypothesis and was wrong. Three workflow defects filed:
  the **"Fail on escalations" gate graded those runs against a different run's
  file**, budget records are orphaned, and **`git` is unusable at the start of
  every run**.
- I did **not** re-verify #2145's production code from scratch. I established
  byte-level that the rework's production delta is comment-only and told the
  structural reviewer it could rely on that — stated in its review as a scoped
  delta re-review rather than passed off as a full one.

## Step 1b drift cross-reference

Newest merged summary on disk: `dev/daily/2026-07-28.md` (run 1, PR #2147,
`16d28ed4`). Its `Pending work` rows, re-verified against live GitHub state:

| run-1 row | run-1 state | verified now | classification |
|---|---|---|---|
| harness #2143 | auto-merged | merged `916825fb` | confirmed |
| cleanup #2146 | auto-merged | merged `d37f94a8` | confirmed |
| weekly-snapshot #2145 | reworked, awaiting re-QC | **re-QC'd + merged `790d23a0` this run** | resolved this run |
| ops-data | not dispatched | `EODHD_API_KEY` re-verified **absent**; `data-gaps.md` unchanged since 2026-05-03 (`115a32e1`) | unchanged |
| harness (2nd item) | not dispatched | still one-at-a-time; see §Escalations | unchanged |

**No `[drift]` warnings.** Tenth clean cross-reference in a row.

**But the cross-reference is incomplete through no fault of the tracks, and that
is itself the finding.** Two orchestrator runs executed between run 1 and this
one (`30353290609` at 11:04Z, `30366079322` at 13:58Z) and left **no summary rows
to cross-reference at all**. #2145 sat merge-blocked for ~7 hours across both.
See §Escalations `A-SUMMARY-STALE-FALLBACK`.

**Run-number derivation.** Three sources agree on **run 2**: exactly one
`dev/daily/2026-07-28*.md` exists (run 1's, no `-runN` suffix), no open
`ops/daily-2026-07-28*` PR existed at start, and the two intervening runs filed
no summary. Per Step 7 the next unused integer is 2 — `-run1` is never a valid
suffix.

## Step 1c carried-`[critical]` verification

Run 1 carried **no `[critical]`**. Re-verified its standing items:

1. **Main baseline** → **GREEN** on the exact starting HEAD `16d28ed4`:
   `build-and-test` SUCCESS, `perf-tier1-smoke` SUCCESS, `golden-sp500-5y`
   SUCCESS, `golden-custom-universe` SUCCESS.
2. **`[high]` `gh` not installed** → **carried, re-verified directly.**
   `command -v gh` → not found. `jj`, `jst`, `git`, `curl`, `python3`, `jq` all
   present. Mitigated the same way: every dispatch brief carried a curl+jq REST
   recipe. Both QC reviews self-posted first try (4799919475, 4799994353) —
   the recipe is now 6-for-6 across two runs.
3. **`[high]` GHA shared-checkout corruption** → **carried, mitigated by dispatch
   discipline again.** Step 4's GHA path still says `git checkout -b`; I
   overrode it with `git worktree add` in all four briefs.
   `/__w/trading/trading` was verified clean and on `main` at every checkpoint
   through the run, including after all four agents had finished.
4. **`[high]` `H-RUNENV-WORKTREE-BLIND`** → resolved in run 1 (`916825fb`); the
   fixed `run-in-env.sh` is what let this run's agents verify inside worktrees
   honestly. Confirmed present on main.
5. **`[high]` budget model wrong** → **carried and substantially strengthened.**
   It is worse than run 1 reported, and the reason is new — see §Budget.
6. **`[medium]` container memory is the real ceiling** → carried and respected.
   Concurrency capped at 3; every builder told `-j 2`; the non-building
   structural agent was deliberately given a no-local-build mandate so only two
   OCaml builds ever ran at once. No OOM.

**No still-real `[critical]` items.** Main is green; no `[critical]` is warranted
and none is emitted.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| weekly-snapshot | **auto-merged** | feat/sketch-adjusted-basis | #2145 | **MERGED `790d23a0`** — both gates APPROVED q4; next is the #2133 follow-up warehouse rebuild + golden re-pin |
| harness | dispatched | harness/check-universe-deps | #2148 | H-CHECK-CACHE-BLIND — see §Dispatched for outcome |
| cleanup | dispatched | cleanup/cholesky-spd-justification | — | test_pin on `test_bayesian_opt_cholesky.ml` — see §Dispatched for outcome |
| orchestrator-automation | **new work filed** | ops/daily-2026-07-28-run2 | #2149 | A-BUDGET-ORPHAN, A-SUMMARY-STALE-FALLBACK, A-GIT-SAFE-DIRECTORY filed with root cause + fix options |
| ops-data | skipped (no-change) | — | — | `EODHD_API_KEY` absent; sentinel `data-gaps.md` unchanged since 2026-05-03 |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| weekly-snapshot | qc-structural | **APPROVED** q4 | #2145 @ `77e5b57b`. Scoped delta re-review. Review 4799919475, self-posted |
| weekly-snapshot | qc-behavioral | **APPROVED** q4 | #2145 @ `77e5b57b`. 7 mutations, 6 killed. Review 4799994353, self-posted |
| weekly-snapshot | — | **merged** | #2145 → `790d23a0`; three gates re-verified on the tip immediately before merge |
| harness | harness-maintainer | see below | #2148 — H-CHECK-CACHE-BLIND |
| cleanup | code-health | see below | `cleanup/cholesky-spd-justification` |
| ops-data | — | skipped (no-change) | `EODHD_API_KEY` absent; sentinel unchanged since 2026-05-03 |
| harness (2nd) | — | skipped | Serialized deliberately — `harness.md` + `devtools/checks/dune` contention, unchanged from run 1 |

## QC Status

- **weekly-snapshot (#2145)**: structural **APPROVED** (q4) → behavioral
  **APPROVED** (q4) → **MERGED `790d23a0`**. Audit:
  `dev/audit/2026-07-28-feat-sketch-adjusted-basis-weekly-snapshot.json`
  (`consecutive_rework_count` **2 → 0**). Written via the `write_audit.sh`
  direct-flag fallback, because the preferred `record_qc_audit.sh --pr-number`
  path needs `gh`.

### How the structural gate was scoped, and why that is honest

The rework (`eaba3891`) landed on top of a structural APPROVED at `ae52b3f1`. I
did not assume the approval transferred. I computed, per file, whether anything
executable had changed between the reviewed SHA and the tip:

| file | delta |
|---|---|
| `adjusted_basis.ml` | comment only |
| `resistance_sketch.ml` | comment only |
| `adjusted_basis.mli`, `resistance_sketch.mli`, `live_resistance_sketch.mli` | docstring text only |
| `test_adjusted_basis.ml` (new), `test_panel_callbacks.ml`, `test/dune` | **+296 lines, never structurally reviewed** |

Everything else differing between the two SHAs (`dev/lib/run-in-env.sh`,
`trading/devtools/checks/*`, `docs/design/*`) arrived via the merge of main —
confirmed absent from the PR's own file list via `GET /pulls/2145/files`.

So the prior approval genuinely covered the production code, and the **new test
code was the uncovered surface**. That is what I scoped the re-review to, and the
reviewer said so in its own body rather than presenting a delta review as a full
one. It found no P6 violations and separately confirmed the new
`test_adjusted_basis` is actually attached to the `runtest` alias — the precise
failure mode that was run 1's best finding on #2143.

Hard gates H1–H3 were marked PASS **on completed CI evidence for the exact tip**
(`build-and-test` + `perf-tier1-smoke` SUCCESS, run 30367458897), not on a local
re-run, and the review says *verified*, not *predicted*. Run 1 raised a
`[medium]` about a structural APPROVED that could not finish its build gates and
said it "strongly predicts" a pass; instructing the reviewer to cite completed CI
instead is the cheap half of that fix. The spec-level half — making an incomplete
hard gate produce a distinct verdict — is still open.

### The #2145 finding worth reading — a reviewer marking its own prior review wrong

Last run's behavioral review raised two blocking mutations and prescribed fixes.
The implementer disputed one prescription. This run's re-review re-ran everything
and concluded **the implementer was right and the reviewer had been wrong**:

> My previous required fix said: *"add one test in `test_panel_callbacks.ml` that
> calls `stock_analysis_callbacks_of_weekly_views` with … `~sidetable_basis:Adjusted`
> … **That kills M4.**"* That last sentence is **false**.

M4 deletes the argument at the **screening** call site
(`weinstein_strategy_screening.ml:144`). A test that hands
`stock_analysis_callbacks_of_weekly_views` a basis itself never executes that
line — it asserts on its own input. The empirical proof is exact: under M4,
**14 of 15 tests passed, including `test_weekly_sidetable_threading`, which is
precisely the shape the reviewer had prescribed.** Only the author's
`Internal_for_test.on_market_close`-driven recorder failed.

The reviewer also retracted its third finding's scope: it had claimed the false
"no-op for split-free data" wording appeared in `weekly_sidetable_builder.ml`'s
header. It never did — that file is byte-unchanged since `ae52b3f1`. There were
two real sites; the author corrected four.

Its own summary of the pattern: *"Two of my three findings last round carried an
inaccuracy in the prescription even though the diagnosis was sound in both
cases."* A diagnosis that is right for a reason that is wrong is exactly the
defect class this PR chain has been chasing since #2139 — and the reviewer
applied it to itself.

The mutation ledger backing the approval, all re-run rather than taken on trust:

| ID | Mutation | Exit | Result |
|---|---|---|---|
| M3 | delete corrupt-close guard entirely | 1 | killed (3 tests) |
| N1 | `<= 0.0` → `< 0.0` (boundary) | 1 | killed — boundary independently pinned |
| N2 | drop only the `is_nan` disjunct | 1 | killed — each disjunct independently pinned |
| N3 | 1e-15 relative drift (sub-epsilon) | 1 | killed — proves bit-identity is *bitwise* |
| M4 | delete `~sidetable_basis` at screening site | 1 | killed |
| N4 | `Panel_callbacks` `Raw`→`Adjusted` default | 1 | killed |
| N5 | `Bar_reader` `Raw`→`Adjusted` default | **0** | **survives** — FLAG, non-blocking |

N3 is the one that matters for the repo: a 1e-15 relative drift is ~1e-13
absolute at these magnitudes and would pass `float_equal ~epsilon:1e-9`. It fails
`Int64.bits_of_float`. The R1 bit-identity claim — load-bearing for every
prebuilt golden — is pinned as identity, not approximation.

N5 was investigated before being downgraded: both production consumers
(`snapshot_warehouse_reader.ml:76-80`, `panel_runner.ml:214-222`) resolve the
basis from the manifest hash and pass it explicitly, and every caller that omits
it supplies no side-table loader, so the default is never consulted. FLAG, not a
gap.

## Budget

- Budget cap: **$200** (`dev/config/merge-policy.json`; a runaway-dispatch
  backstop under a flat-rate Max-20x subscription, not a real bill)
- Target utilization: 60–95%
- Subagents dispatched: **4** (2 QC, 1 harness-maintainer, 1 code-health)

### The measured record on `main` was incomplete, and the previous escalation was therefore too low

`.github/workflows/orchestrator.yml:423-453` bundles a run's budget JSON onto the
`ops/daily-*` PR **only if such a PR is currently open**. If there is none —
because the orchestrator died before its Step 8, or because a prior run's summary
PR already merged — it pushes to a standalone `ops/budget-<date>-<run_id>` branch
and, by explicit design (workflow comment line 384), **opens no PR for it**.
Nothing has ever merged those branches.

**31 had accumulated since 2026-07-14, holding `$283.31`.** All 31 are recovered
into this run's PR. Corrected totals:

| date | on main (what run 1 reported) | orphaned | **true** | **% of $200** |
|---|---|---|---|---|
| 2026-07-24 | $59.13 | $17.40 | **$76.53** | 38% |
| 2026-07-25 | $50.54 | $14.84 | **$65.38** | 33% |
| 2026-07-26 | $107.77 (54%) | $84.14 | **$191.91** | **96%** |
| 2026-07-27 | $405.30 (203%) | $58.44 | **$463.74** | **232%** |
| 2026-07-28 (5 runs, pre-this-run) | $46.72 | $45.21 | **$91.93** | 46% |

Run 1's `[high]` escalation was right about the trend and **wrong about the
level, in the direction that matters**: it under-reported 2026-07-26 by 42
percentage points and 2026-07-27 by 29. It also could not have known — the
records were not on `main` to be read.

### How I sized this run

Today stood at **$91.93 (46%)** across 5 prior runs when I started. Calibrating
from run 1's measured `$46.72` for 7 agents (~$6.7/agent including orchestrator
overhead), 4 agents puts this run near **$25–30**, landing the day around
**$115–125, i.e. ~60%** — the bottom of the target band, deliberately, on a week
that has twice blown through the cap.

I used measured records rather than Step 3.75b's `model_prices` estimate. That is
a deviation from the spec and I am flagging it rather than burying it: the
estimate is the thing that has been reporting 5–15× low, and Q4 from run 1 asking
whether Step 3.75 may read measured cost is still unanswered.

Per-run today: `30321755696` $13.65 · `30331408218` $10.53 · `30340958135`
$46.72 · `30353290609` $8.11 · `30366079322` $12.92. (The first two are filed as
`2026-07-27-run6/run7` — budget records are keyed by UTC calendar date, summaries
by run label, so the two indices do not line up. Minor, but it costs a minute
every time someone reconciles them.)

Utilization assessment: **UNDER_TARGET by estimate (~60%), intentionally.** The
measured figure for this run lands in
`dev/budget/2026-07-28-30380136239.json` after I exit.

## Health Scan

From `dev/health/2026-07-28-fast-run2.md`.

- Result: **CLEAN**
- Main build: **PASSING** — CI on the exact starting HEAD `16d28ed4`, all
  workflows success
- Status file integrity: **PASSING** (exit 0, run directly)
- Index size linter: **PASSING** (exit 0) — it caught a 271-char row I wrote
  during the Step 5.5 reconcile and I shortened it. Second run running that
  linter has caught a row the orchestrator wrote; it is earning its place.
- Critical items: **none**
- Step 2e.1 backlog refresh: **no-op** — no new `[medium]`/`[high]` findings

The build gate is CI evidence rather than a local re-run, for the two reasons run
1 and run 7 gave (strictly stronger — full linter suite in a clean container; and
a fourth concurrent OCaml build on a 4-CPU/15-GB box is how run 7 manufactured a
false `exit 137`). The two cheap deterministic checks were run directly here.

## Follow-up Queue

**46** open `- [ ]` items across `dev/status/*.md` — unchanged in total from run
1, though the composition moved (2 new orchestrator-automation items filed this
run; cleanup items closed/opened by the dispatches below). Distribution: harness
20, cleanup 10, tuning-methods 4, all-eligible 3, and 9 across six other files.

**Step 2b was not triggered**, despite 46 > 10, for the third run running: the
~11-item non-actionable floor (T2/T3/T4 roadmap items Step 2c explicitly refuses
to dispatch, plus a template placeholder) sits above the threshold of 10, so
"threshold exceeded" is a constant rather than a signal. `code-health` was
dispatched anyway via Step 2e. **Q3 below is unanswered from runs 7 and 1.**

## Integration Queue

- **#2145 — MERGED `790d23a0`.** All three gates re-verified on the tip
  immediately before merging, per `pr-merge-gates.md`'s "don't trust stale CI
  reads". No `update-branch` was needed: the 13:58Z run had already updated the
  branch at 14:15Z, so it was `clean` and current when I arrived — the one useful
  thing that run accomplished, and it left no record of having done it.
- **#2148, cleanup** — see §Dispatched.
- **#2149** — this summary + the 31-record budget backfill + the two filed
  workflow defects.

## Escalations

- **[high] The orchestrator's cost record has been silently incomplete for two
  weeks, and the escalation that was supposed to catch overrun under-reported it
  by up to 42 points.** 31 `ops/budget-*` branches, `$283.31`, never merged
  because the bundling step opens no PR for its own fallback. Recovered this run;
  the *mechanism* is filed as **`A-BUDGET-ORPHAN`** in
  `dev/status/orchestrator-automation.md` with three fix options, the cheapest
  being one `curl -X POST /pulls` next to the existing push. Corrected totals in
  §Budget. This supersedes run 1's version of the escalation, which stands as
  directionally correct and numerically low.

- **[high] Two runs today exited `success` having produced no summary, and the
  escalation gate graded them on a different run's file.**
  `orchestrator.yml:332` resolves the summary as
  `ls -t dev/daily/${DATE}.md dev/daily/${DATE}-run*.md | head -n 1`. When a run
  writes nothing, this does not fail — it silently picks up the newest summary
  already on disk, typically an already-merged one from another run. Both
  `30353290609` and `30366079322` logged `Using daily summary:
  dev/daily/2026-07-28.md` — run 1's file. The **`Fail on escalations`** step
  (line 543) then greps *that* file. So a run that died before writing a
  `[critical]` is graded clean and reports success: the gate's guarantee is void
  in precisely the circumstance it exists for. Filed as
  **`A-SUMMARY-STALE-FALLBACK`**.

  **My first hypothesis was wrong and I checked it before filing.** I assumed
  wall-clock exhaustion inside Step 6.5's 15-minute CI poll. The logs say
  otherwise: both runs reported `"is_error": false` with `"num_turns"` **33** and
  **51** against a `--max-turns 200` cap, in 8 and 13 minutes. They ended
  *voluntarily*. Nor did either take the Step 0.5 no-op path — that writes a
  `**Mode:** NO-OP` file which `publish-summary` would then have picked as the
  newest; both picked run 1's file instead, so no summary file was written at
  all. See the next item for what they did hit.

- **[high] `git` is dead on arrival in this container, and each run has to
  discover that for itself.** The job runs `--user 0` with `HOME: /home/opam`, so
  the checkout fails git's ownership check and **every** git command exits 128
  with `detected dubious ownership`. My very first `git log` this run failed that
  way. `actions/checkout` does run `git config --global --add safe.directory`,
  but it does not reach the agent: `git config --global --get-all safe.directory`
  returned **exactly one** entry — the one I added mid-run — and
  `/root/.gitconfig` does not exist. Until an agent fixes it by hand, Step 0.5's
  git-based conditions, Step 1b, Step 5.5 and Step 8's push are all inoperable,
  and every dispatched subagent hits the same wall independently. **Both silent
  runs today logged `ShellError: Failed with exit code 128` about 10 seconds
  after start.** I am not claiming that proves causation — the agent's turn-level
  reasoning is not in the workflow log — but it is the one defect both
  demonstrably hit, and it is a one-line workflow fix worth making regardless.
  Filed as **`A-GIT-SAFE-DIRECTORY`**.

- **[high] `gh` is not installed in this container, and the spec depends on it in
  at least six places.** Unchanged from runs 7 and 1, re-verified directly. The
  REST workaround is now 6-for-6 across two runs, so the acute symptom is
  handled — but the call sites still end in `|| true` and fail silently for
  anyone who doesn't get the recipe. See Q1.

- **[high] Step 4's GHA path still tells agents to `git checkout -b` in the
  shared tree.** I overrode it with `git worktree add` in all four briefs;
  `/__w/trading/trading` stayed clean on `main` all run, verified repeatedly.
  That is four more copies of a workaround for one wrong paragraph, and it holds
  only while the orchestrator remembers to write it. The file is behind the
  `.claude/agents/` write block. See Q2.

- **[medium] An incomplete hard gate should produce a distinct verdict, not an
  APPROVED with a caveat.** Carried from run 1. Mitigated this run by dispatch
  discipline — I told the structural reviewer not to build locally and to cite
  completed CI for the exact tip, so its H1–H3 PASS is backed by stronger
  evidence than a local run and is stated as verified. But that is my prompt
  doing the work, not the spec. The spec fix (an `ENVFAIL`/`PARTIAL` verdict
  distinct from APPROVED) is still open.

- **[medium] The harness track's two shared files still cap it at one dispatch
  per run while Step 2c claims two.** Any two harness dispatches contend on
  `dev/status/harness.md` and `trading/devtools/checks/dune`. Unchanged from run
  1; I again dispatched one and again ate the throughput. Concretely this run: I
  filed the two new workflow defects in `dev/status/orchestrator-automation.md`
  instead of `harness.md` specifically to avoid colliding with #2148 — which was
  the right home for them anyway, but the choice was forced.

- **[medium] The `Reviewed SHA:` mechanism is half-migrated, and I made it worse
  before repairing it.** Step 1.5 and Step 0.5 Condition 1 both parse
  `Reviewed SHA:` out of `dev/reviews/<track>.md` to decide whether a PR has
  unreviewed commits. PR-D'c (open, in `orchestrator-automation.md`) proposes
  dropping those file writes entirely in favour of PR review comments, and I
  pre-empted it by telling both QC agents not to write `dev/reviews/*`. That left
  `weekly-snapshot.md` claiming `Reviewed SHA: a007d5503` while the actually
  reviewed tip was `77e5b57b` — a stale value the next run would have trusted. I
  wrote the round-2 entry myself (Step 5 makes the orchestrator the owner of the
  combined result), so the file is correct now. But the general shape is a trap:
  **a consumer that is still live and a producer that is being retired**. Either
  land PR-D'c *and* move the SHA lookup to the PR API in the same change, or keep
  the file writes until it does. Half-migrating it silently corrupts a dispatch
  guard. 27 `dev/reviews/*` files were touched in the last 30 days, so the
  producer is very much still in use.

- **[info] 169 remote branches, and `delete_branch_on_merge` is `false`.** 92 are
  orchestrator-generated (`ops/daily-*` 61, `ops/budget-*` 31). Because PRs are
  squash-merged, merged branch tips never become ancestors of `main`, so nothing
  ever cleans them up and "is this branch merged?" cannot be answered by
  ancestry. One repo-settings toggle fixes it going forward. Low priority, but it
  is the reason the 31 orphaned budget branches were invisible for two weeks —
  they looked like all the other clutter.

- **[info] The docs-only merge carve-out still does not name `dev/audit/` or
  `dev/budget/`.** Unchanged from runs 4, 6, 7 and 1 — **fifth consecutive run
  reporting it**, and materially relevant this time: this run's PR carries 31
  `dev/budget/*.json` files plus a `dev/audit/*.json`, all strictly outside the
  `pr-merge-gates.md` allowlist. One line to fix.

- **[info] Budget records are keyed by UTC calendar date, summaries by run
  label.** `30321755696` and `30331408218` are `2026-07-28-*.json` but their
  summaries are `2026-07-27-run6/run7`. Harmless until someone reconciles the two
  indices, which §Budget had to do by hand this run.

## Questions for You

1. **Install `gh` in the orchestrator image, or convert the call sites to REST?**
   Carried from runs 7 and 1. New evidence for (b): the REST recipe is now
   6-for-6 across two runs and four independent agents. I still prefer **(a)** —
   one image change fixes six spec sites and stops the next spec author writing
   `gh` again.

2. **Please grant Edit permission on `.claude/agents/lead-orchestrator.md`, or
   name the intended channel.** Now **five runs deep**. The queue behind it: the
   GHA shared-checkout instruction (hand-patched into four briefs again today),
   the `gh`-dependent agent templates, the stale Step 2b counting rule, Step
   3.75's estimate-only budget model, Step 2c's false "harness items touch
   disjoint files" claim, and now the Step 6.5-poll-before-Step-7 ordering that
   probably cost two runs their summaries. It is filesystem-writable with no
   matching deny rule in `.claude/settings.json`, so this is a harness-level rule
   on that path. I have not routed around it and won't.

3. **Should Step 2b's `followup_threshold` be retuned, or should the metric
   exclude tier/roadmap checkboxes?** Unanswered from runs 7 and 1. The count has
   a hard ~11-item non-actionable floor above a threshold of 10, so "threshold
   exceeded" is constant. Excluding roadmap-shaped items is my preference — it
   makes the number mean "accumulated debt", which is what Step 2b is asking —
   but it is a semantics change with a live consumer.

4. **Should Step 3.75 read measured cost and act on it?** Carried from run 1, and
   now much stronger: this run found that the measured record it *would* read has
   itself been incomplete since 2026-07-14. Both halves need fixing together —
   reading measured cost is only safe once `A-BUDGET-ORPHAN` is closed, or the
   reader must union `dev/budget/` with the un-merged `ops/budget-*` branches. I
   sized this run down on my own judgement again, which I would rather have
   sanctioned in the spec than repeated ad hoc a third time.

5. **New: should a run that writes no summary be a hard failure?** Today two runs
   spent `$21.03`, mutated a PR, and exited green with no artefact. I think the
   answer is plainly yes and it is a five-line change
   (`A-SUMMARY-STALE-FALLBACK`), but it converts a class of silent no-ops into
   visible red runs, so it is your call whether that noise is wanted.

## Hand-off to the next run — in priority order

1. **#2148 (harness, H-CHECK-CACHE-BLIND) and the cleanup PR** — state at exit
   recorded in §Dispatched. Both were dispatched this run; verify CI, QC per the
   normal gates, merge.
2. **`A-BUDGET-ORPHAN`** — the history is repaired but the mechanism is live;
   every future run that dies before Step 8 strands another record. Cheapest fix
   is one `curl -X POST /pulls` beside the existing fallback push.
3. **`A-GIT-SAFE-DIRECTORY`** — one workflow line, and it unblocks every git
   operation in every run for every agent. Cheapest fix on this list by some
   margin; do it first.
4. **`A-SUMMARY-STALE-FALLBACK`** — the escalation gate is currently unsound for
   exactly the runs that need it most.
5. **`H-CHECK-SETE-DIAGNOSTICS`** — still open in `harness.md`, and it is the
   companion to #2148's finding: `set -e` + `VAR=$(cmd); CODE=$?` produces
   non-zero exit with empty output and no `FAIL:` line, which is precisely the
   signature `pr-merge-gates.md` lists as a candidate infra-flake exception.
6. **`code-health`** — backlog state after this run's dispatch is in
   `dev/status/cleanup.md`; two items from #2145's review
   (`adjusted_basis_guard_asymmetry`, `rescale_duplication`) are now unblocked
   for dispatch since #2145 has merged and no longer contends on those files.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
