# Status — 2026-08-26

**Run ID:** 2026-08-26-run-1
**Run timestamp:** 2026-08-26T07:37Z–09:30Z (GHA orchestrator run 32943299640)
**Mode:** FULL
**Main:** `55605922` → `3a51cb6b` (green: `dune build` 0, `dune runtest` 0, CI success)

## Headline

**Two PRs, one merged, and the more interesting result is the one that did *not* merge.**

**#2557** (RSS digit-fusion, #2553) went dispatch → structural APPROVED (5) →
behavioral APPROVED (4) → **auto-merged `3a51cb6b`** with **zero rework
iterations**. Its behavioral reviewer then found the thing that matters more than
the PR: **the byte-identical unfixed bug lives in five sibling scripts, one of
which — `dev/scripts/perf_tier1_smoke.sh` — backs the *required* `perf-tier1-smoke`
PR gate.** Filed as **#2559**, independently re-verified here by grep. #2553 closed
one call site out of six.

**#2555** (RS trend classifier, #2380) was **driven by a concurrent local pipeline
the entire time I was reviewing it.** Its tip moved three times under review. The
run's single most valuable output came from a reviewer noticing that:

> I was dispatched against `d03c1858`; the branch was already at `a8831839`. I
> reviewed the current tip and pinned my review there — a verdict on `d03c1858`
> would itself have been the stale-green this repo has been bitten by.

Reading the tip it was handed would have produced a clean APPROVED. Reading the
tip that actually existed found **UTF-8 double-encoding in three golden
`(description ...)` data fields** — `\xe2\x80\x94` → `\xc3\xa2\xc2\x80\xc2\x94`,
byte-proven, reaching a data field, **with `dune runtest` at exit 0 and CI green
throughout**. Nothing in the repo catches it, and the next re-pin would have
double-encoded again. Repaired at `88b60dc9`; **independently verified here** (0
mojibake sequences, em dashes intact across all six re-pinned cells).

That catch was luck, not process — see `[high]` on #2432 in Escalations.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| harness | auto-merged | `harness/2553-postsubmit-rss-parse` | #2557 | merged `3a51cb6b`; follow-up is #2559 (5 sibling scripts) |
| screener | blocked (concurrent pipeline) | `feat/rs-trend-live` | #2555 | re-QC at `88b60dc9`; `mergeable_state=dirty` — resolve conflict with main first |
| all others | not dispatched | — | — | see §Dispatch decisions |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| screener | qc-behavioral | NEEDS_REWORK (3) | at `5b6472af`; found the undisclosed `rs_blocks_short` consumer |
| harness | harness-maintainer | completed | #2553 fix + fixture test, RED-then-GREEN verified |
| screener | feat-weinstein (Rework mode) | completed | rework 1/2 → `d03c1858`; both blocking findings addressed |
| harness | qc-structural | APPROVED (5) | #2557 at `4f32b9ab` |
| screener | qc-structural | APPROVED (5) | #2555 re-review at `d03c1858` |
| harness | qc-behavioral | APPROVED (4) | #2557; surfaced #2559 |
| screener | qc-behavioral | NEEDS_REWORK (3) | re-review; self-repinned to `a8831839`, found the mojibake |
| ops-data | — | skipped | `data-gaps.md` unchanged since 2026-05-03 (115 days); `EODHD_API_KEY` absent in GHA |
| cleanup | — | skipped | fill-window work; 3-agent container cap consumed by the QC loop all run |
| backtest-infra / experiments | — | not dispatched | see §Dispatch decisions |

**7 agent dispatches, 0 stalls, 0 lost work.** Never more than 2 concurrent
(4-core runner; load hit 8.4 with two agents building — a third would have
thrashed, per `container-capacity-scheduling.md`).

## Dispatch decisions — why the other tracks did not run

Not queue-depth reasoning; each has a named blocker:

- **#2405** (re-flip `entry_order_max_rest_weeks`→26) — gated on three explicit
  preconditions in its own body, two of which are user decisions (the
  `enable_sim_entry_stoplimit` fill-model default). Not orchestrator-dispatchable.
- **#2394** (long cascade RS phase) — the issue **itself** says "only once some
  config actually arms the gate"; `min_rs_normalized` still defaults to `0.0`, so
  the attribution it would add is unobservable. Dispatching it would be churn
  against a record several reports read.
- **#2440** (record_qc_audit scenario 22) — eligible, and was next in the queue;
  displaced by the #2555 rework loop, which is higher priority per
  `container-capacity-scheduling.md` §"QC outranks backtests".
- **#2489 / #2408 / #2406** — P1/P3 research needing a container. Structurally
  undispatchable from GHA (no docker). Unchanged from 08-24.

## QC Status

- **harness (#2557)**: structural APPROVED (5) + behavioral APPROVED (4) at
  `4f32b9ab` → **merged**.
- **screener (#2555)**: both verdicts **stale** at the current tip `88b60dc9`.
  Last: structural APPROVED (5) @ `d03c1858`, behavioral NEEDS_REWORK (3) @
  `a8831839`. `sh dev/scripts/pr_gate_status.sh` says
  `re-run qc-structural at 88b60dc9`.

Every substantive QC pass this run found something the author had not. Two
reviewers independently reproduced their subjects' RED-then-GREEN claims by
mutation rather than accepting them; one went further and established that
`test_clamp_band_compares_over_a_shorter_span` is **the sole detector of the
`max 0` guard anywhere in the tree** (the whole 16-test `rs/` suite passes
without it).

## Corrections I made to my own work

- I filed **#2558** claiming `goldens_affected_check.sh` has "inverted
  sensitivity" — loud for narrow radius, silent for broad. **Then the same check
  went RED on the rework tip**, via its *docstring cross-reference* path, listing
  all 12 affected goldens. The code change was identical between tips; what
  flipped it was the rework's docstring edit naming `resistance_lookback_bars`.
  So the check is not purely name-matching, and my framing was too strong. The
  real gap is narrower: **the cross-reference is only as good as the docstring.**
  Correction recorded in #2555's body (see Escalations for why not in #2558).
- My dispatch brief told the #2555 rework agent the paired postsubmit table was
  the dispatcher's job. It was landed by the concurrent local pipeline instead,
  and `paired-run-done` is now applied — so B1/B2/B3 PASS. The brief's
  instruction was right; my assumption about who would execute it was wrong.

## Health Scan

`dev/health/2026-08-26-fast.md` — **CLEAN**. `dune build` 0, `dune runtest` 0,
`status_file_integrity` 0, `index_size_linter` 0. Critical items: none.

## Budget

- Cap: $200 (`max_daily_cost_usd`, a runaway backstop — auth is flat-rate Max-20x)
- Target utilization: 60–95%
- Subagents dispatched: **7**, all completed, **none killed mid-flight**
- Measured subagent tokens: ~990K across the 7 agents
- Estimated cost: **~$12–14** including ~$1 orchestrator overhead
- **Utilization assessment: UNDER_TARGET (~7%)**, and this one is *not* a
  dispatch failure to correct. The binding constraint was the **4-core runner**,
  not quota: two concurrent agents already put load at 8.4. The QC loop on #2555
  (behavioral → rework → structural → behavioral) is inherently serial — each
  stage needs the previous stage's output. Raising utilization here means a
  bigger runner, not more agents.
- Scope reduced due to budget: **No.**

## Data Operations

ops-data **not dispatched**. `dev/notes/data-gaps.md` is unchanged since
2026-05-03 (**115 days**), and `EODHD_API_KEY` is absent in GHA. The remaining
gaps both need a human first: EODData free-tier registration to check whether
`INDEX:ADVN`/`INDEX:DECL` exist as absolute counts, and a cost/license call on
Pinnacle Data. This is the 9th consecutive run reporting the same thing.

## Escalations

- **[info] Carried-forward verification: no still-real `[critical]` items.** Main
  green at `3a51cb6b` (`dune runtest` exit 0). Every `[high]`/`[medium]` item from
  2026-08-24 was **re-verified by API, not copied** — #2503, #2505, #2506, #2507,
  #2508, #2509, #2432, #2386, #2537 are **all closed**. The 08-25 overnight
  session cleared them.

- **NEW [high] #2559 — the required `perf-tier1-smoke` gate reports corrupted RSS
  on failing cells.** #2553/#2557 fixed one call site; the byte-identical line is
  in five more (`perf_tier1_smoke.sh:145`, `perf_tier2_nightly.sh:152`,
  `perf_tier3_weekly.sh:158`, `perf_tier4_release_gate.sh:157`,
  `run_tier4_release_gate.sh:204`). Recommend option 2 in the issue: extract the
  tested helper to `dev/lib/` so there is one definition, not six.

- **[high] The concurrent local/GHA pipeline is still undetectable before dispatch
  (#2432 — closed, phenomenon not).** #2555's tip moved three times under review.
  It cost nothing this run and in fact produced the best finding — but only
  because a reviewer checked its own SHA assumption unprompted. A dispatched
  agent that trusts the SHA in its brief is the default, and that default is
  wrong here. A claim marker on the branch would make this deterministic.

- **NEW [medium] This token can CREATE issues but can neither COMMENT on nor
  UPDATE them** — re-probed twice, 403 both (`POST /issues/N/comments`,
  `PATCH /issues/N`). It *can* PATCH pull requests. Consequence this run: my
  correction to **#2558** could not land on the issue, so it lives in #2555's body
  instead — where a reader of #2558 will not find it. This is the 08-24
  escalation, now with a sharper boundary (PRs writable, issues create-only).

- **NEW [medium] The token lacks the `workflow` scope**, so `workflow_dispatch`
  returns 403 on all three `golden-runs-*.yml`. **This is worth granting.** All
  three already declare `workflow_dispatch`, and main's own green runs are the
  base arm — so with that one scope, a future run could generate the paired
  postsubmit table `config-default-blast-radius.md` demands **automatically**,
  instead of declaring it out of environment. That is the single highest-leverage
  permission change available to this orchestrator.

- **[medium] `H-JJ-JST-BROKEN-GHA` confirmed again.** #2557 was opened by the
  curl REST fallback (Step 4.5); no dispatched agent can open its own PR. `gh`
  and `docker` are both absent; I briefed all 7 agents around it explicitly.

- **[info] Two `wip/*` salvage branches** (`wip/candidate-emission-salvage-0823`,
  `wip/g2-preport-salvage`) are 3 days old — not yet stale by the 14-day rule, so
  left alone. Two merged leftovers (`config/2404-value-unify`,
  `goldens/wall-min-floor-2547`) **were deleted** this run.

- **[info] `golden-runs-sp500-15y` was still in progress** on `3a51cb6b` at run
  end. Non-blocking (`continue-on-error: true`), but it is the workflow that
  covers the two long-short cells #2555 moves — worth a look.

## Follow-up Queue

- **From #2557 (ADV-2):** `.github/workflows/golden-runs-sp500-15y.yml:62-65`
  still calls the 12.4 GB figures "unexplained-but-moot" and blames the
  per-process RSS surface. #2553 supersedes that — the digits were *fused*, not
  measured. **Two durable records now disagree about the cause**, which is the
  same record-corruption failure mode #2553 was filed for. Fold into #2559.
- **From #2555 (N1) — free decomposition sitting in already-collected data:**
  `sp500-2019-2023-long-only` differs from `sp500-2019-2023` *only* by
  `enable_short_side=false`. The paired runs give shorts-off **+5.3pp** vs
  shorts-on **+39.3pp**: the short-side re-enablement dominates and the
  RS+widening effect is modest. No new run needed.
- **From #2555 (N2):** `sp500-2010-2026` maxDD dropped **78.39 → 21.26** at flat
  trade count. That is not obviously reshuffling; worth a `trade-dissection`.
- **From #2555:** `rs_blocks_long`'s docstring (`screener_admission.mli:153`)
  cites #2380 and is made **stale** by this PR — post-fix the `n < 2` fallback is
  a warmup-only edge, not the universal state. One-sentence fix.
- **From #2555 (BK1):** two `BOOK-CHECK-NEEDED` items queued for the next **local**
  session (the book path is unreachable from GHA, #2457).
- **#2555 body H1 still says "NOT a return lever" unqualified**, with the walk-back
  four sections down. **The squash-merge message must not carry that claim** —
  re-enabling the short side has a return impact by construction.
- `dev/status/cleanup.md` — top item is still the test-file-length policy
  decision, human-gated across **twelve** runs.

## Integration Queue

- **#2557** → **MERGED `3a51cb6b`.** Hold re-checked immediately before the merge
  call (label + `convert_to_draft`-after-first-review, per `pr-merge-gates.md`
  Rule 0): no hold, no label touched. CI re-verified at tip in the same step
  rather than trusting the earlier read.
- **#2555** — **open, not merged, and correctly so.** At `88b60dc9`:
  `mergeable_state=dirty` (conflicts with main, partly because it carries a
  cherry-pick of #2554 that has since landed), and **both QC verdicts are stale**.
  `paired-run-done` is applied and the paired table has landed, so the
  blast-radius gate is satisfied — what remains is a conflict resolution and a
  re-QC at the tip. **I did not merge it and did not touch its labels.**

## Current Milestone Target

Unchanged: is the stop machine / selection path faithful and measurable. Both
this run's substantive findings were **measurement-integrity** defects rather
than strategy defects — a corrupted RSS number on the gate that guards every PR,
and corrupted text in golden data files. Neither would have been caught by any
existing check.

## Dependency Unlocks

None new. #2380 stays open until #2555 merges (its body closes it).

## Questions for You

1. **Grant the token the `workflow` scope?** It converts "the paired postsubmit
   table is out of environment" into "the orchestrator produces it." Highest
   leverage item on this list.
2. **#2559 — extract the RSS helper to `dev/lib/` (one definition) or copy it
   into five scripts?** I recommend extraction; six copies were wrong in unison
   once already.
3. **#2555 is mid-flight between two pipelines.** Do you want me to take it over
   next run (re-QC at tip + conflict resolution), or is the local session driving
   it to completion? Right now neither of us is clearly the owner, which is how
   the tip moved three times under review.
4. **#2489** still carries the deferred §2.2/arc reclassification decision; its
   1–3 preconditions are done and it is open solely as that decision's carrier.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
