# Consolidated Summary — 2026-04-21
Runs included: run-1, run-2, run-3, run-4 (4 total)
Generated: 2026-04-21T20:57:50Z

## Pending work (from last run)

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-scale | awaiting-merge | feat/backtest-scale-3g | #484 | Re-APPROVED run-4 after review-follow-up commit `db925c5a` (F2 partial fix — threads `benchmark_symbol=GSPC.INDX` into Tiered `Bar_loader`). Structural + behavioral both green (Quality Score 3). F2 narrowed: benchmark-symbol divergence closed, but `Summary_compute` still emits `None` for every universe symbol on every Friday, so Tiered remains observationally inert on the parity scenario — G5 FLAG downgraded, not resolved; must be cleared before Tiered is flipped to default. Awaiting human merge. |
| harness | awaiting-merge | harness/gha-cost-tracking | #483 | Re-APPROVED run-4 after force-push rebase onto post-#487 main (new tip `a3075010d3a0`; the old `792b5b09` Reviewed-SHA anchor was invalidated). POSIX-sh fixes intact; `dash -n` + `bash -n` clean on both scripts; all hard gates pass. Structural APPROVED (Quality Score 4); behavioral N/A (utility scripts, no domain logic). Awaiting human merge. |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution for scrapers, golden re-runs, cross-feature QC) — human-only per status file. |

## Dispatched across all runs (deduped)
| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| backtest-scale | feat-backtest | completed | 3g parity acceptance test — opened PR #484 at tip 6d69081; 203-line test file + 14 synthetic OHLCV fixtures + 2 scenario sexp files; escalated two Tiered-path divergences to §Follow-up (non-blocking: Bar_loader SPY vs Runner GSPC.INDX benchmark; _promote_universe_metadata hard-fails on missing CSVs vs Legacy skip). |
| backtest-scale | qc-structural | APPROVED | All hard gates pass. 13-item checklist all PASS/NA. No production code modified; test harness only. Reviewed SHA 6d690819. |
| backtest-scale | qc-behavioral | APPROVED | Quality Score 3/5. Test faithfully implements plan §3g spec. F1/F2 observability FLAGs (tolerance too tight vs plan Resolutions #1; Tiered wrapper observationally inert on scenario due to benchmark_symbol divergence) are non-blocking but must be cleared post-merge before Tiered becomes default. flipped draft→ready. Audit: quality_score=3, consecutive_rework_count=0. |
| harness | qc-structural | NEEDS_REWORK | H3 FAIL — `dune runtest` exit non-zero on `budget_rollup_check.sh` due to bash-only syntax (`#!/usr/bin/env bash`, `set -euo pipefail`, `${BASH_SOURCE[0]}`, `<<<`, `[[ ]]`, bash arrays). Dune invokes with `/bin/sh`. Behavioral QC SKIPPED per Step 5 gate policy. Reviewed SHA d1ba14a3. Audit: harness_gap="POSIX shell portability lint check absent"; consecutive_rework_count=1. (run-1) |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` unchanged since 2026-04-14; no new gaps surfaced. |
| cleanup | code-health | skipped | `dev/status/cleanup.md` §Backlog empty; latest fast scan (today) is CLEAN; no candidate to dispatch. |
| harness (backlog) | harness-maintainer | skipped | Open PR #483 already in flight on the only harness track with work-in-progress; Step 1.5 defers additional harness dispatch until #483 resolves. T1-N/T3-H/T4 items remain gated. |
| — | health-scanner | skipped | Fast scan is deterministic (Step 6.1/6.2). Deep scan is weekly GHA cron; not dispatched here. |
| harness | harness-maintainer | completed | Scoped POSIX-sh rework of PR #483 per run-1 recommendation (a). New commit `792b5b09` on top of `d1ba14a3`. Diff: +22 −8 `dev/lib/budget_rollup.sh`, +10 −5 `trading/devtools/checks/budget_rollup_check.sh` (32 LOC total, within 40-LOC budget). Semantic preservation verified by agent (tmpfile+xargs idiom is POSIX equivalent of bash array expansion). All 4 acceptance checks passed. |
| harness | qc-structural | APPROVED | Re-review at tip `792b5b09`. All 13 items PASS/NA (H3 now PASS — was FAIL in run-1). POSIX-conformance verification added to the checklist (`bash -n`, `dash -n`, direct `sh` invocation, each script, each rewrite pattern). Scope verified: exactly 2 files changed. Quality Score 4/5 (docked for the avoidable oversight of shipping bash-isms against the codebase's established `#!/bin/sh` pattern). Behavioral N/A (utility script). Audit: `dev/audit/2026-04-21-harness.json` updated from NEEDS_REWORK → APPROVED; `consecutive_rework_count` reset 1 → 0. (run-2) |
| backtest-scale | — | skipped | Open PR #484 in flight with tip_SHA == Reviewed_SHA (`6d69081`) — no new commits since run-1 APPROVAL. Step 1.5 correctly skips re-QC. |
| harness | — | skipped | Open PR #483 in flight; tip `792b5b09` == Reviewed_SHA. Non-plan-first track with N=1; Step 1.5 skips further dispatch. |
| — | qc draft→ready flip | no-op | Both PRs already `draft=false` at run start (flipped in prior runs). GraphQL mutation not invoked. |
| backtest-scale (next increment) | — | skipped | 3h (nightly A/B) is a post-merge follow-on per plan `dev/plans/backtest-tiered-loader-2026-04-19.md` §Increments — requires 3g to LAND on main, not just branch-exist. Fresh-stack ineligible. |
| harness (backlog) | — | skipped | Non-plan-first track, N=1 open PR (#483). Step 1.5 strict-skip applies (carried from run-1..run-3). T1-N data-gated / T3-H / T4 + POSIX-sh-linter follow-up remain gated on #483 merge. |

## QC across all runs
- backtest-scale (3g / #484): APPROVED (structural + behavioral, Quality Score 3) — unchanged since run-1. See `dev/reviews/backtest-scale.md` (top Reviewed SHA: `6d69081`); `dev/audit/2026-04-21-backtest-scale.json`.
- harness (gha-cost-tracking / #483): APPROVED (structural, behavioral N/A, Quality Score 4) — unchanged since run-2 rework + re-review. See `dev/reviews/harness.md` (top Reviewed SHA: `792b5b09`); `dev/audit/2026-04-21-harness.json`.
- All other tracks: MERGED or BLOCKED — no fresh QC this run.
- **backtest-scale (3g / #484):** APPROVED (structural + behavioral re-review at `db925c5a4c56`, Quality Score 3). See `dev/reviews/backtest-scale.md` §"Structural Checklist — backtest-scale 3g re-review" + §"Behavioral Checklist — backtest-scale 3g re-review". Audit: `dev/audit/2026-04-21-backtest-scale.json`.
- **harness (gha-cost-tracking / #483):** APPROVED (structural re-review at `a3075010d3a0`, Quality Score 4; behavioral N/A). See `dev/reviews/harness.md` §"Structural Checklist — harness gha-cost-tracking re-review (PR #483 at tip a3075010d3a0, post-rebase)". Audit: `dev/audit/2026-04-21-harness.json`.

## Budget (summed across runs)
- Total subagents spawned: 9
- run-1: - Budget utilization: ~$14–18 / $50 (~28–36%)
- run-2: - Budget utilization: ~$6 / $50 (~12%)
- run-3: - Budget utilization: ~$1.00 / $50.00 (~2%)
- run-4: - Budget utilization: ~$5.7 / $50.00 (~11%)
- Killed mid-flight: None reported across all runs.

## Escalations (merged)
- [critical] **Carried-forward verification (Step 1c):** All prior `[critical]` items (run-6 carried "review queue as the throttle" as `[info]`, not `[critical]`; no other critical items in the last 3 summaries). Main baseline verified green this run (`dune runtest` exit 0, evidence in dev/health/2026-04-21-fast.md). No new `[critical]` escalations.
- [high] **harness #483 NEEDS_REWORK — POSIX-sh violations.** Two new shell scripts under `trading/devtools/checks/` and `dev/lib/` use bash-only syntax but are invoked by dune via `/bin/sh`. H3 fails with `set: Illegal option -o pipefail` at `budget_rollup_check.sh:15`. Evidence: qc-structural report in dev/reviews/harness.md §"NEEDS_REWORK Items". Fix is scoped (shebang + 6 syntax replacements in two files). Recording `harness_gap: "POSIX shell portability lint check absent"` in dev/audit/2026-04-21-harness.json for future linter candidacy.
- [info] **backtest-scale #484 behavioral FLAGs (non-blocking).** F1: hard-coded $0.01 tolerance tighter than plan spec. F2: Tiered path observationally inert on the parity scenario due to `Bar_loader.create` defaulting `benchmark_symbol="SPY"` while `Runner` uses `"GSPC.INDX"` — every Summary promote fails silently, so parity holds trivially through the simulator's pre-loaded bar cache rather than through the tiered path. Both documented as post-merge follow-ups in dev/status/backtest-scale.md §Follow-up. Must be resolved before Tiered is flipped to default. Quality Score is 3/5 primarily because the test's pass signal is currently too permissive on the very divergence it's meant to pin.
- [info] **Overnight merge wave cleared the review queue.** Between 06:23 and 10:06 UTC on 2026-04-21, 7 PRs merged (#473, #477, #478, #479, #480, #481, #482) plus daily-summary #476. Run-6's "review queue as the throttle" complaint is resolved — orchestrator is back to productive dispatch. No drift from run-6 — all status files accurately reflect the post-merge state.
- [info] **Stash recovery during review-file reconstruction.** A side effect of dispatching two foreground QC agents with `git checkout` operations on the feat branch: when the orchestrator returned to main for the summary, residual feat-branch state produced a merge conflict on `dev/status/backtest-scale.md`. Recovered cleanly by reading qc-structural's review body back from `git stash@{1}` (feat-agent stashed its work before QC ran) and rebuilding the reviews file from that source. No content lost. Mechanism to tighten for future runs: have QC agents work on detached-HEAD checkouts or in dedicated QC worktrees, so the orchestrator's own working tree is never modified.
- [info] **qc-structural extraction fallback inaccuracy.** `record_qc_audit.sh` auto-extraction for PR #483 pulled the wrong verdicts (structural=NEEDS_REWORK but behavioral=NEEDS_REWORK and overall=APPROVED) because the current review file retains the APPROVED verdict lines from prior run-6 reviews kept below the current section. Worked around by calling `write_audit.sh` directly with explicit flags. Tightening candidate: update `record_qc_audit.sh` to only parse the leading `## Structural Checklist —` / `# Behavioral QC —` sections whose Reviewed SHA matches the branch's tip, OR have qc-agents write a machine-readable `structural_qc:` / `behavioral_qc:` / `overall_qc:` field at the top of the review file (similar to Reviewed SHA) rather than relying on `## Verdict` block scanning.
- [critical] **Carried-forward verification (Step 1c).** Run-1 had no still-real `[critical]` items (the carried-forward check reported "no new [critical]"); main baseline was green. Re-verified this run: `dune runtest` exit 0 on `2fdcc39` (run-1 summary merge). No new `[critical]` escalations.
- [info] **Branch-switching in orchestrator working tree caused review-file loss.** The qc-structural subagent's `git checkout harness/gha-cost-tracking` (per its own instructions) left the orchestrator's working tree on the feature branch. The review file it wrote was on that branch's filesystem snapshot. When I stashed + switched back to main + popped, the review-file changes did not surface through as a conflict (they were silently dropped because the stashed copy matched main's HEAD). I rebuilt the re-review section manually from the agent's return-value summary. No content lost; the verdict and score are identical to what the agent produced. Tightening candidates (same as run-1's analogous escalation): (a) qc agents should work in a detached HEAD or dedicated QC worktree so the orchestrator's working tree is never mutated, OR (b) the orchestrator should capture review-file writes from the agent's return value directly rather than relying on working-tree persistence.
- [info] **Audit auto-extractor over-parsed the re-review's APPROVED verdict as behavioral_qc=APPROVED.** The agent wrote `## Verdict\n\nAPPROVED\n\nBehavioral review: N/A — harness/utility-script PR; no domain logic.` `record_qc_audit.sh` grabs the LAST `## Verdict` block regardless of surrounding context, so it wrote `behavioral_qc: APPROVED` even though behavioral was SKIPPED. Manually corrected in `dev/audit/2026-04-21-harness.json` to `SKIPPED` with a `notes` field documenting the ambiguity. Same tightening candidate as run-1's analogous `[info]`: have qc agents emit a machine-readable top-line `behavioral_qc: SKIPPED|APPROVED|NEEDS_REWORK` field near the `Reviewed SHA:` line, independent of the prose Verdict block.
- [info] **PR #483 mergeable_state is "dirty".** #485 (run-1 summary) merged during this run and touched the same `dev/status/_index.md` + `dev/status/harness.md` files that #483 now updates with the run-2 APPROVED content. The conflict is entirely in the status-file rows and is trivially resolvable by favoring #483's rows (they reflect the post-rework state). Not a QC failure; surfaced here so the human knows what to do at merge time.
- [critical] **Carried-forward verification (Step 1c).** Run-1 and run-2 had no still-real `[critical]` items (run-1 Step 1c reported "no still-real [critical]"; run-2 reiterated same with fresh evidence). Re-verified this run by running `dune build` and `dune runtest` directly: both exit 0 on main@origin tip `12bc755` (post-#486-merge baseline). No new `[critical]` escalations.
- [info] **Review queue remains the throttle (carried, unchanged).** 2 open PRs (#483, #484), both APPROVED, awaiting human merge. Runs-6 / run-1 / run-2 / run-3 have all been near-no-ops for this reason — each spent its dispatch budget correctly deciding not to redispatch already-APPROVED work. Merging either PR unblocks the next tranche; merging both opens up 3h (backtest-scale) + the harness T-item backlog.
- [info] **Saturated-queue fast-exit (Step 0.5) did NOT fire this run.** Condition 2 technically failed because PR #486 (run-2 summary) merged during the window between run-2's timestamp (`2026-04-21T11:13:43Z`) and now, and that merge touched `dev/status/_index.md` + `dev/status/harness.md`. These changes are the run-2 output landing on main, not new drift. Full-pass behavior this run is the same as a hypothetical no-op exit would have been (two skipped dispatches + health gates), so the letter-of-the-spec behavior is acceptable here. Tightening candidate: Step 0.5 Condition 2 could additionally exempt commits whose commit message is `ops: daily orchestrator summary <DATE>*` — those ARE the prior summary's own output landing, not meaningful status drift. Low priority; the full pass is cheap when the eligible surface is already empty (seen in: run-3,run-4)
- [info] **Stale `dev/status/backtest-scale.md` §Next Steps entries.** The §Open PR and §Next Steps sections in `dev/status/backtest-scale.md` still describe `feat/backtest-scale-3f-part3a` and `feat/backtest-scale-3f-part3b` as "Ready for QC" and list them as review targets #5 and #6. Those PRs merged as #477 and #478 respectively. Lines do not misrepresent current state at the track-summary / Status level (that says READY_FOR_REVIEW for 3g), and `dev/status/_index.md` correctly reflects the live state. Informational drift in a status-file sub-section; queued as a harness follow-up rather than dispatched this run (touching status files during a no-op run adds merge friction against #483/#484 for no benefit).
- [info] **Carried-forward verification (Step 1c).** Run-3 had no still-real `[critical]` items. Re-verified this run by running `dune build && dune runtest` directly on main@origin tip `3ecff21` (post-#488 merge): exit 0, exit 0. No new `[critical]` escalations. No carry-forward.
- [info] **G5/F2 is narrower than it was, but still a gate on the Tiered default-flip.** This run's re-QC pinned down the scope: the benchmark-symbol plumbing is fixed; the residual symptom is that `Summary_compute.compute_values` returns `None` for every universe symbol on every Friday on the parity scenario. The parity test still passes because both Legacy and Tiered fall through to the simulator's pre-loaded bar cache — parity holds for the WRONG reason. Do not flip Tiered to default until the Summary-compute root cause is identified and fixed. Track as a stacked follow-up PR after #484 merges.
- [info] **Run-3 summary missed that PRs had new commits.** Reading the run-3 summary file, §Pending work said both PRs "tip == Reviewed_SHA" with no outstanding QC. That was accurate at run-3 time but humans reading it in isolation might have assumed the PRs were ready-to-merge without action. Each run's summary is a snapshot; the human should treat the most recent summary as authoritative. No process change recommended — just a note for readers.

## Integration Queue (last run's view)

- **PR #484** — backtest-scale 3g parity acceptance test (APPROVED structural + behavioral, Quality Score 3; re-APPROVED after `db925c5a` F2 partial fix). Merges the tiered-loader merge gate. Post-merge follow-up PR (Summary_compute root cause) is now better-scoped.
- **PR #483** — harness gha-cost-tracking (APPROVED structural after rebase; behavioral N/A, Quality Score 4). POSIX-sh fixes survived the rebase; all hard gates pass.

## Per-run links
- run-1 -> `2026-04-21.md`
- run-2 -> `2026-04-21-run2.md`
- run-3 -> `2026-04-21-run3.md`
- run-4 -> `2026-04-21-run4.md`
