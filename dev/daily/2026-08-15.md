# Status — 2026-08-15

Run timestamp: 2026-08-15T07:32Z (GHA orchestrator run 31872019203)
Run ID: 2026-08-15-run-1
Mode: FULL

> **Checkpointed mid-run, deliberately** — same reason as 08-12 through 08-14.
> A run that dies before Step 7 loses everything. This file was written and
> pushed before any agent returned, then updated in place.

## Headline

*(in progress — updated at end of run)*

## Established before dispatch (measured this run, not assumed)

- **Main GREEN at `4b7c410e`.** GitHub check-runs on that exact SHA:
  `build-and-test` **success**, `perf-tier1-smoke` **success**,
  `golden-custom-universe` **success**, `golden-sp500-5y` **success**,
  `perf-tier2-nightly` **success**. Zero open `ci-red` watchdog issues.
- **Local gates green, measured by me:** `dune build` **exit 0**.
  Full `dune runtest` — see §Health Scan.
- **Queue: 1 open PR** — #2327, draft, tip `5d59880e`, CI green.
- **Toolchain unchanged — twelfth consecutive run carrying these by hand:**
  `gh` **ABSENT**, `docker` **ABSENT**, `dune` not on PATH, `jj`/`jst` present
  but **BROKEN**. Working: `git`, `curl`, `jq`, `python3`, `bash`,
  `dev/lib/run-in-env.sh`. Plus the `git config --global --add safe.directory`
  dance before the first `git` call.
- **Runner capacity, measured:** 4 cores, 15 GB RAM, 64 GB disk free before my
  own build (50 GB after).

## Step 0.5 — fast-exit declined, deterministically

Queue was non-empty (1 PR), so the vacuous-pass precondition did not fire.
**Condition 2 FAILED**: `dev/status/_index.md` was modified after the prior
summary by `4a65be8d` — `[main-fix] index_size_linter: move orchestrator run
history out of _index.md (#2331)`, which is not an `ops: daily orchestrator
summary` commit and so is not exempt. Full pass.

Worth noting what that main-fix was: **the prior run's own `_index.md` write
tripped the `index_size_linter` and had to be fixed on main.** Step 5.5's
terseness contract (one line, ≤160 chars per Next-task cell) exists precisely
to prevent this, and it was not honoured.

## Dispatched this run

*(in progress)*

## Budget

*(in progress)*

## Escalations

*(in progress)*
