# Status — 2026-04-26

**Run ID:** 2026-04-26-run-1
**Run timestamp:** 2026-04-26T11:35:11Z
**Mode:** FULL (Step 0.5 escape hatch fired — most recent prior summary `2026-04-23-run3` is >24h old)

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| data-panels | awaiting-merge | feat/panels-stage03-pr-d-delete-legacy | #575 | QC APPROVED 5/5 — awaiting human merge; then dispatch Stage 4 (callbacks-through-runner). |
| backtest-perf | awaiting-merge | feat/backtest-perf-tier1-catalog | #574 | QC APPROVED 5/5 — awaiting human merge; held-out workflow YAML in PR body needs `workflow`-scoped token to commit. |
| short-side-strategy | blocked | — | — | Practical block on broad-universe scale; still gated on data-panels Stage 4 memory-win. Reactivation cue's "Tiered loader flip" is moot (Tiered deleted in #573); leave as MERGED follow-ups. |
| cleanup | dispatched (next-run) | — | — | New `[ ]` Backlog entry for `csv_storage.ml:_stream_in_range_prices` nesting linter — first dispatch on next orchestrator run. |
| harness | blocked | — | — | T1 done; T2 milestone-gated; T3-C superseded; T3-H low-priority. No dispatchable items this run. |
| orchestrator-automation | blocked | — | — | Phase 2 background-execution items pending empirical tests. No dispatchable items this run. |
| cost-tracking | blocked | — | — | Awaiting next budget JSON to verify cost-capture wiring; nothing actionable this run. |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| data-panels | feat-backtest | completed | Stage 3 PR 3.4 — delete `_run_legacy` + `Loader_strategy` enum + `--loader-strategy` CLI flag + `Scenario.loader_strategy` field. ~271 LOC net delete across 22 files. PR #575 open, ready-for-review. Worktree contamination from sibling agent recovered inline. |
| data-panels | qc-structural | APPROVED | Reviewed SHA 8c733ac3. Hard gates pass; back-compat sexp parse test verified non-vacuous. No contamination in PR file list. |
| data-panels | qc-behavioral | APPROVED | Quality score: 5. No trading-domain code touched. Three info-only FLAGs (stale `dev/scripts/*` refs to `--loader-strategy`, two stale doc-comments in `panel_runner.mli` / `backtest_runner.ml`). |
| data-panels | — | flipped draft→ready | Already non-draft when QC ran (agent called `jst submit` at session end). |
| data-panels | — | audit written | `dev/audit/2026-04-26-data-panels.json` quality_score=5. |
| backtest-perf | feat-backtest | completed | Steps 1+2 — 15 scenarios cataloged into 4 perf tiers (4×T1, 6×T2, 2×T3, 3×T4); `perf_catalog_check.sh` + `perf_tier1_smoke.sh` (POSIX-sh). PR #574 open, ready-for-review. **Held out**: `.github/workflows/perf-tier1.yml` — drafted in PR body, needs `workflow`-scoped token to commit. |
| backtest-perf | qc-structural | APPROVED | Reviewed SHA 6f689d62. Hard gates pass. No-Python rule respected. POSIX-sh linter count went 43→45. Workflow-file gap surfaced as FLAG, not blocker. |
| backtest-perf | qc-behavioral | APPROVED | Quality score: 5. Tier assignments defensible; one info-only concern (T1 perf-sweep cells will hit full broad universe since `scenario_runner.exe` doesn't honor `universe_cap` override — mitigated by per-cell 120s timeout). Annotate-only default + strict mode toggle + workflow drift cross-check all verified empirically. |
| backtest-perf | — | flipped draft→ready | Already non-draft when QC ran. |
| backtest-perf | — | audit written | `dev/audit/2026-04-26-backtest-perf.json` quality_score=5. |
| harness | — | skipped | No dispatchable items: T1 done; T2 milestone-gated; T3-C superseded; T3-H low-priority. |
| ops-data | — | skipped | data-gaps.md unchanged since 2026-04-14 (sentinel rule). |
| cleanup | — | skipped | Backlog was empty at run start; one new `[ ]` entry added during Step 6 (csv_storage.ml nesting). Dispatch on next run. |

## Feature Progress

### data-panels [IN_PROGRESS — Stage 3 PR 3.4 awaiting merge]
- Done today: Stage 3 PR 3.4 implemented + QC APPROVED both stages.
  - Deleted entire `loader_strategy/` library; deleted `_run_legacy` + helpers from `runner.ml`; removed `--loader-strategy` CLI flag; removed `loader_strategy` field from `Scenario.t` (back-compat preserved via `[@@sexp.allow_extra_fields]` and tested).
  - `run_backtest` now calls `Panel_runner.run` directly. Net diff −271 LOC across 22 files.
  - Pre-flag verifications: PR-F int-then-float fold preserved; PR-H `Bar_reader.accumulate` / `_all_accumulated_symbols` fully removed in PR 3.2 #569; `bars_for_volume_resistance` left for Stage 4 cleanup as planned.
- In progress: PR #575 awaiting human merge.
- Blocked: No.
- **Next dispatch (post-merge):** Stage 4 — callbacks-through-runner wiring. Per `dev/notes/panels-rss-spike-2026-04-25.md`, post-3.2 Panel-mode RSS at N=292 T=6y is 3.47 GB vs the <800 MB projection because every reader site rebuilds `Daily_price.t list` from panels per tick. Stage 4 wires `*_with_callbacks` entry points all the way through `Panel_runner` so production no longer materializes lists. Estimated ~600 LOC, 3-4 working days. Plan: `dev/plans/columnar-data-shape-2026-04-25.md` §"Stage 4".

### backtest-perf [IN_PROGRESS — Steps 1+2 awaiting merge]
- Done today: Steps 1+2 from `dev/status/backtest-perf.md` §"Next steps (post-#550 merge)" implemented + QC APPROVED both stages.
  - 15 scenarios cataloged into 4 perf tiers via `;; perf-tier:` headers (comment-only; no behavior change). Tier breakdown: 4×T1 (smoke + 2 perf-sweep), 6×T2 (goldens-small + 3 smoke), 2×T3 (perf-sweep bull-1y/bull-3y), 3×T4 (goldens-broad SKIPPED placeholders).
  - New `trading/devtools/checks/perf_catalog_check.sh` — POSIX-sh, annotate-only default, `PERF_CATALOG_CHECK_STRICT=1` to gate. Wired into `dune runtest`.
  - New `dev/scripts/perf_tier1_smoke.sh` — POSIX-sh, auto-discovers tier-1 scenarios, runs each via `scenario_runner.exe` with timeout 120, prints summary table.
- In progress: PR #574 awaiting human merge.
- Blocked: No, but held-out `.github/workflows/perf-tier1.yml` needs maintainer with `workflow`-scoped token to commit (draft in PR body).
- **Next dispatch (post-merge):** Steps 3+4 — define tier 2 (`perf-nightly.yml`) and tier 3 (sibling weekly workflow). Tier 4 release-gate scenarios are now structurally unblocked (data-panels Stages 0-3 all merged) but practically gated on Stage 4 (panel-mode memory-win materialization).

### backtest-scale [MERGED — superseded by data-panels]
- Status flipped at the index level; per-track file lags. Tiered path entirely deleted (data-panels Stage 3 PR 3.3 #573). PR #525 (`Bar_history.trim_before` primitive) merged 2026-04-24 but is now moot — `Bar_history` itself deleted in PR #569. All RSS / parity / Legacy→Tiered-flip concerns absorbed by `data-panels` columnar redesign.

## QC Status

| Track | Structural | Behavioral | Quality | Audit |
|-------|------------|------------|---------|-------|
| data-panels | APPROVED (8c733ac3) | APPROVED | 5 / 5 | dev/audit/2026-04-26-data-panels.json |
| backtest-perf | APPROVED (6f689d62) | APPROVED | 5 / 5 | dev/audit/2026-04-26-backtest-perf.json |

Review files: `dev/reviews/data-panels.md`, `dev/reviews/backtest-perf.md`.

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80%
- Subagents dispatched: 6
  - 2× feat-backtest (data-panels PR 3.4, backtest-perf catalog)
  - 2× qc-structural (PR #575, PR #574)
  - 2× qc-behavioral (PR #575, PR #574)
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | feat-backtest (data-panels 3.4) | opus | completed | ~300K total | ~$2.50 |
  | feat-backtest (backtest-perf 1+2) | opus | completed | ~160K total | ~$1.50 |
  | qc-structural (data-panels) | haiku | completed | ~60K total | ~$0.10 |
  | qc-structural (backtest-perf) | haiku | completed | ~55K total | ~$0.10 |
  | qc-behavioral (data-panels) | sonnet | completed | ~140K total | ~$0.50 |
  | qc-behavioral (backtest-perf) | sonnet | completed | ~90K total | ~$0.30 |
- Any subagent killed mid-flight: No
- Estimated cost this run: ~$5.50 + ~$1.00 orchestrator overhead = **~$6.50 / $50 (13%)**.
- Measured cost: not yet available — `dev/budget/2026-04-26-run1.json` is written by the GHA "Capture run cost" step post-orchestrator-exit; refer to that file once present for `total_cost_usd` from the action's execution_file.
- Utilization assessment: **UNDER_TARGET (<60%)** — but **all-eligible-tracks-dispatched**: the only available work this run was the two feat-backtest dispatches; harness backlog drained, ops-data sentinel skipped, cleanup empty at start, no other tracks have open work that doesn't depend on the merge of #574 / #575. Dispatching Stage 4 (data-panels) now would have collided with PR #575 on the same `runner.ml` / `panel_runner.ml` files. Not actionable by the orchestrator; resume on the next run after merges land.
- Scope reduced due to budget: No.

## Data Operations

(ops-data did not run today — `dev/notes/data-gaps.md` unchanged since 2026-04-14 sentinel skip.)

## Harness Work

(harness-maintainer did not run today — backlog has no dispatchable T1/T3 items.)

## Health Scan

From `dev/health/2026-04-26-fast.md`:
- Result: **FINDINGS** (both checks RED)
- Critical items:
  1. **Build/test gate RED** — nesting linter on `analysis/data/storage/csv/lib/csv_storage.ml:180:_stream_in_range_prices` (introduced by PR #543 H7 stream-parse refactor merged 2026-04-24).
  2. **Status file integrity RED** — `data-panels.md: '## Last updated' is not YYYY-MM-DD: '2026-04-25 (PR 3.3 pushed)'` (PR #573 merged the malformed timestamp; PR #575 fixes it).

## Follow-up Queue

(Top-level items only — see per-track status files for full lists.)

- **data-panels**: Stage 4 (callbacks-through-runner; load-bearing memory-win work) is the next dispatch.
- **backtest-perf**: Steps 3+4 (tier-2 nightly workflow, tier-3 weekly workflow). Maintainer must commit held-out `.github/workflows/perf-tier1.yml` from PR #574's body.
- **cleanup**: csv_storage.ml `_stream_in_range_prices` nesting violation (added to backlog this run, dispatchable next run).
- **short-side-strategy**: 3 follow-ups (bear-window backtest regression, full short cascade, Ch.11 spot-check) still practically gated on broad-universe scale → data-panels Stage 4.
- **cost-tracking**: verify `dev/budget/2026-04-26-run1.json` lands with `total_cost_usd`; compare costs pre/post #482 (qc-structural → Haiku).

## Integration Queue

Two PRs APPROVED both QC stages, awaiting human merge:
- **#575** — data-panels Stage 3 PR 3.4 (delete Legacy + Loader_strategy enum). −271 LOC; back-compat tested; parity gate intact.
- **#574** — backtest-perf catalog + tier-1 smoke gate. +280 LOC of tooling + 15 scenario header tags; held-out `.github/workflows/perf-tier1.yml` needs `workflow`-scoped token (draft in PR body).

Auto-merge remains disabled (`auto_merge_enabled: false` in `dev/config/merge-policy.json`) for feature PRs; orchestrator's own summary PRs auto-merge per Step 8a.

## Current Milestone Target

M5 (backtest infrastructure complete + tunable) — requires data-panels Stage 4 (memory-win materialization) + backtest-perf tiers 1-3 wired in CI.

## Dependency Unlocks

- **data-panels Stages 0-3 ALL MERGED** (PRs #555/#557/#558/#559–#565/#567/#569/#573 all landed by 2026-04-26 04:05Z). This unblocks `backtest-perf` tier-4 release-gate scenarios at the structural level (was the gating dependency per the `backtest-perf.md` §Blocked on). Practically still gated on data-panels Stage 4 for the memory-win to fit the 8 GB ceiling at 5000×decade scale.
- The "Tiered loader flip" reactivation cue in `dev/status/short-side-strategy.md` is now obsolete (Tiered path was DELETED rather than flipped). The track's practical block now points at data-panels Stage 4 instead.

## Escalations

[critical] **Main baseline RED on `dev/lib/run-in-env.sh dune runtest`.** Evidence (verbatim from this session, BUILD_EXIT=0 TEST_EXIT=1):

```
File "devtools/checks/dune", lines 60-66, characters 0-142:
60 | (rule
61 |  (alias runtest)
62 |  (action
63 |   (run
64 |    %{exe:../nesting_linter/nesting_linter.exe}
65 |    %{workspace_root}
66 |    %{dep:linter_exceptions.conf})))
(cd _build/default/devtools/checks && ../nesting_linter/nesting_linter.exe ../.. linter_exceptions.conf)
FAIL: nesting linter — 1 function(s) exceed limits:

  avg    max  else why        location                                            function
  ----------------------------------------------------------------------------------------
  3.61   9    0    avg+max    analysis/data/storage/csv/lib/csv_storage.ml:180    _stream_in_range_prices

Limits: avg > 3.0, max > 5, nested-else > 0.
Codebase avg nesting: 1.44  |  884 functions scanned  |  108 files
```

Introduced by PR #543 (merged 2026-04-24) — `perf(csv_storage): stream-parse get with in-range filtering (H7)`. The new `_stream_in_range_prices` function streams CSV rows and applies an in-range filter inline; the resulting `Stdlib.Stream.t` interaction nested deeper than the linter allows. Two fix shapes: (a) factor inner predicate into a named helper (preserves perf, drops nesting), (b) add `(* @nesting-ok: streaming filter requires inline match on parsed bar *)` annotation. Cleanup backlog entry added this run; dispatch on next orchestrator run.

[critical] **Status file integrity RED on `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh`.** Evidence (verbatim from this session, EXIT=1):

```
FAIL: status file integrity linter — malformed or missing fields in dev/status/:
data-panels.md: '## Last updated' is not YYYY-MM-DD: '2026-04-25 (PR 3.3 pushed)'

Schema: ## Status (IN_PROGRESS|READY_FOR_REVIEW|APPROVED|MERGED|BLOCKED|PENDING),
        ## Last updated: YYYY-MM-DD,
        ## Interface stable (YES|NO|N/A) — required except for: harness.md backtest-infra.md
        (N/A for Interface stable only valid when Status is PENDING)
```

PR #573 merged with the malformed Last-updated value `2026-04-25 (PR 3.3 pushed)`. PR #575 fixes it (sets `## Last updated: 2026-04-26`) but is awaiting human merge — so main stays RED on this check until #575 lands. This is the **third recurrence** of the "human-merged PR introduces status-file-template drift" pattern (prior occurrences: 2026-04-22 run-3 / 2026-04-23 fix). Tracked in `dev/status/harness.md` Cleanup §"Recurring status-file integrity drift from human-merged PRs is a [info] follow-up — linter already exits 1 on violation; requires branch-protection config to enforce." The structural fix is GitHub branch-protection requiring CI to pass before merge — not something the orchestrator can do unilaterally.

[info] **Concurrent feat-backtest dispatches collided on shared working tree (no production impact).** Both feat-backtest dispatches (data-panels PR 3.4 + backtest-perf catalog) ran in parallel and reported worktree contamination — sibling agents kept switching the working tree to each other's branch, contaminating staging. Each agent self-recovered (stash + reset + reapply). Verified post-dispatch via `git diff origin/main...feat/<branch> --stat` that PR file lists matched intended scope (no perf_catalog files in PR #575, no runner.ml changes in PR #574). This is a known limitation of the GHA-path single-working-tree dispatch model — see `dev/status/orchestrator-automation.md` §"BG-owns-branch convention". Not actionable in this run; longer-term fix is per-agent isolated worktrees in the GHA path. Adds friction, no incorrect output.

[info] **Held-out `.github/workflows/perf-tier1.yml` from PR #574.** The dispatched feat-backtest's PAT lacks `workflow` scope, so it cannot push files under `.github/workflows/`. Full draft is in PR #574's body. Maintainer with a workflow-scoped token (or a separate one-line PR via `gh` from a maintainer machine) can commit the YAML. The `perf_catalog_check.sh` gracefully no-ops the cross-check when the workflow file is absent, so nothing breaks in the meantime; it just means the per-PR perf-tier1 smoke gate doesn't fire until the workflow lands.

## Step 1b drift cross-reference

Most recent prior summary `dev/daily/2026-04-23-run3.md` was a NO-OP run (saturated-queue fast-exit) and contained no `## Pending work` table with `dispatched` or `awaiting-merge` rows. No drift warnings to emit.

## Step 1c carried-forward critical verification

Prior summary carried zero `[critical]` items (only one `[info]` about overlapping cron triggers, which is operational rather than a verifiable assertion). Nothing to verify-carry-forward this run. The main-baseline RED finding above is **newly observed in this run** — not inherited from a prior summary.

## Per-run links

- [run 1] dev/daily/2026-04-26.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
