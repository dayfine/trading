# Status — 2026-08-09

Run timestamp: 2026-08-09T07:45Z–09:20Z (GHA orchestrator run 31301600156)
Run ID: 2026-08-09-run-1
Mode: FULL

## Headline

**Two merges, zero reworks, both QC pairs 5/5 — and the discovery that the cleanup item I dispatched was never a backlog item at all. It was the illustrative template inside a fenced code block, and a bare `grep '^- [ ]'` matches inside the fence.**

| PR | What | Outcome |
|---|---|---|
| **#2248** | cleanup — untrack `trading/compile_commands.json` | CI green · structural **5/5** · behavioral **5/5** · **merged `b8388074`** (0 reworks) |
| **#2249** | support-floor-stops — F11+F12+R3 test hardening | CI green · structural **5/5** · behavioral **5/5** · **merged `2c84d8c4`** (0 reworks) |

Both merged tips are the SHAs the QC agents reviewed — neither needed a post-QC `update-branch`, so `.claude/rules/pr-merge-gates.md` is satisfied to the letter, CI re-verified immediately before each merge rather than earlier in the run.

## The bookkeeping defect I introduced and then caught

Step 2e says to pick the top `[ ]` item from `dev/status/cleanup.md` §Backlog. I ran `grep -n '^- \[ \]' dev/status/cleanup.md`, got nine hits, and took the one at line 98 — `tracked_build_artifact`. It was **not in §Backlog.** §Backlog ends at line 68. Line 98 sits inside a ` ``` ` fence under `## How findings get here`, which documents *the shape a backlog entry should take*. The grep has no notion of fences, so the example matched.

`code-health` then did what the brief asked and flipped that example to `[x]`, rewriting it with this PR's specifics — so the file's canonical "here is what an open item looks like" template now showed a **closed** item with a PR reference. The next agent to read it would have copied the wrong shape.

Three things worth separating, because they have different severities:

- **The finding itself was real.** I reproduced it live before dispatching: clean checkout of `main@d696f603`, `dune build`, then ` M trading/compile_commands.json`. It had been accidentally committed three times. Doing the work was correct.
- **The selection was accidental.** I dispatched off a doc example. It happened to be a real defect because the example was *written from* a real 2026-07-28 finding — but that is luck, not process.
- **The template corruption was a genuine regression**, shipped inside an APPROVED PR. Neither QC agent caught it: structural checked the *diff* was in scope (it was — `cleanup.md` is a legitimate file for this PR to touch) and behavioral checked the *claims* were true (they were). Nothing in either checklist asks "is the line you edited actually the thing you think it is?"

Fixed in this run's summary commit: template restored to a generic `- [ ] <finding_type>: <path> -- <context> (source: <report>)` shape, annotated in-place as an illustrative template that must not be ticked or dispatched from, with the incident recorded. The real closure moved to `## Completed` where it belongs.

**This is a `LINTER_CANDIDATE` and I could not add it:** a check that `grep '^- \[ \]'`-style scans over `dev/status/*.md` skip fenced blocks would have prevented both halves. It lives under `.claude/**` or `trading/devtools/checks/`.

## Established before dispatch (measured, not assumed)

- **Main GREEN.** GitHub CI on `d696f603`: `build-and-test`, `perf-tier1-smoke`, `golden-sp500-5y`, `golden-custom-universe` all `success`. Local `dune build` exit 0. Re-measured post-merge at `2c84d8c4`: `dune runtest` **exit 0**.
- **Step 0.5 did NOT fast-exit, for the right reason.** Exactly **one** PR was open — #2246, `ops/track-pacer-2026-08-09`, authored by the maintainer. **Zero open orchestrator PRs**, so the `A-FASTEXIT-VACUOUS` precondition (queue must be non-empty to be "saturated") skipped the four conditions outright. This is the fix from #2180 doing its job: an empty queue is maximum dispatch capacity, the opposite of saturated. The first-run-of-day escape hatch would also have applied.
- **ops-data correctly skipped.** `dev/notes/data-gaps.md` last changed 2026-05-03 (#814) — unchanged for 14 weeks — and `EODHD_API_KEY` is absent.
- **Toolchain, unchanged from 08-08:** `gh` **ABSENT**, `docker` **ABSENT**, `dune` not on PATH, `jj`/`jst` present but **BROKEN**. Working: `curl`, `jq`, `python3`, `git`, `bash`, `dev/lib/run-in-env.sh dune …`. All five agent sessions were pre-warned, plus the `/__w/trading/trading` vs `/workspaces/trading-1/trading` trap.

## Step 1b drift cross-reference

- **No `[drift]` warnings.** The 08-08 summary's two pending rows (#2239, #2243) both show as merged on `main`; no status file claimed IN_PROGRESS against a merged PR.
- **The 08-08 `[high]` "silent orchestrator slot" did NOT recur.** 08-08's run 31246486964 spent $11.97 and wrote no summary. This run's predecessor wrote `dev/daily/2026-08-08.md` and merged it as #2244. One clean cycle; the underlying `A-SUMMARY-STALE-FALLBACK` gap is unfixed, so this is one observation, not a resolution.
- **Step 1c — no still-real `[critical]` items.** 08-08 carried none tagged `[critical]`, and main is green by direct measurement. **Nothing escalates to `[critical]`.**

## Dispatched this run

Writers and QC ran **strictly sequentially** — this is a single shared checkout, so two agents doing `git checkout -b` in it would collide, and QC agents check out branches too. **5 agent sessions, 7 dispatches** counting two resumes.

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| cleanup | code-health | completed | `tracked_build_artifact`; stalled once on a backgrounded build, resumed |
| cleanup | qc-structural | **APPROVED (5)** | independently re-derived the "no consumer" grep; verified the `.gitignore` anchoring against the workspace layout |
| cleanup | qc-behavioral | **APPROVED (5)** | stalled once, resumed; verified all 3 historical PR numbers and the byte-exact delta |
| support-floor-stops | feat-weinstein | completed | F11+F12+R3; reported F11 as an **honest negative** |
| support-floor-stops | qc-structural | **APPROVED (5)** | reviewed in an isolated `git worktree`; validated the deliberate positive/negative asymmetry |
| support-floor-stops | qc-behavioral | **APPROVED (5)** | reproduced **all six** mutation cells exactly and found the mechanism behind them |
| harness | — | **not dispatched** | see Escalations — deliberate, on the track-pacer's diminishing-returns finding |
| ops-data | — | skipped | `data-gaps.md` unchanged since #814 (2026-05-03); `EODHD_API_KEY` absent |
| health-scanner | — | not dispatched | fast scan is deterministic (Step 6); deep scan is a weekly cron |

**Rework budget used: 0 of 2 on both tracks.** Neither track approached the escalation path.

**Agents stalled on background monitors: 2 of 7 dispatches.** This is a regression — 08-08 was 0-of-8, and 08-05/06/07 were 0-of-15/12/12. See Escalations.

## Pending work

| Track | State | Branch | PR | Next step |
|---|---|---|---|---|
| cleanup | auto-merged | cleanup/tracked-compile-commands | **#2248** | merged `b8388074` — all 3 gates green |
| support-floor-stops | auto-merged | feat/split-safe-test-hardening | **#2249** | merged `2c84d8c4` — all 3 gates green |
| harness | deferred | — | — | `H-REPO-ROOT-SET-BUT-INVALID-SILENT-FALLTHROUGH` — deliberately held pending your call on rec #10 |
| simulation | blocked | — | — | entry-ticket / fill-model arc — **in flight locally**, fenced |
| screener | blocked | — | — | entry-ticket / fill-model arc — **in flight locally**, fenced |

## Feature Progress

### cleanup [MERGED #2248]

- **The defect was reproduced live rather than inherited.** Before dispatching I ran a clean-checkout build on `main@d696f603` and got ` M trading/compile_commands.json`. That measurement went into the brief as the baseline, so the agent never had to establish whether the finding was still real.

- **The dependency question was the actual work, and it was answered by measurement.** The finding said nothing *appears* to depend on the file being tracked — "appears" being exactly the word that decides between the two branches of the fix. The agent grepped `dune`/`dune-project` files, `.github/workflows/**`, `trading/devtools/checks/**`, `.vscode/`, `.devcontainer/` and `Makefile`s, and classified every hit as generates / consumes / mentions. No consumer. Removal branch taken, correctly.

- **qc-behavioral turned an approximate claim into an exact one.** The PR said the 508→548 byte growth was "host-specific path" churn. The reviewer diffed the two contents: the **only** difference in the entire file is `5.3` → `5.3.0+flambda`, in exactly **4** include paths, and `.0+flambda` is 10 characters — 10 × 4 = **40 bytes**, which is the delta to the byte. I reproduced this myself before merging. It is a sharper statement than the author's: not "a host-specific path" but *the opam switch-name variant embedded in the compiler include paths*, which is why a committed copy is permanently wrong for at least one machine.

- **It also verified the historical claim, which is the shape this repo keeps getting wrong.** "Accidentally committed at least 3 times (#1299, #2085, #2129)" — `git log --follow` returns **exactly three** commits, no more, and all three PR numbers check out, with #1299 the original `+1/−0` add and the other two the `+1/−1` churn hunks described. Exhaustive, not merely directionally right.

- **Acceptance verified twice, the second time on `main` itself.** The criterion is that a build no longer dirties the tree. After merging I ran a full `dune build && dune runtest` on `main@2c84d8c4` and `git status --porcelain` came back clean apart from my own audit records. The property is now live, not just claimed on a branch.

### support-floor-stops [MERGED #2249]

- **The status file's own description of the work was wrong, and reading the code first is what caught it.** F11 says "tighten the three substrings". There are **four** `NOT EXERCISED` assertion sites. The fourth is a **cross-negative** (`equal_to false`) in `test_split_safe_renders_counts_and_inert_fraction`. Tightening a negative *weakens* it — a more specific string is easier to be absent — so the correct action is to leave it alone. I put that in the brief as an explicit guard; the agent honoured it, and both QC agents independently re-derived the reasoning. Had I transcribed the follow-up prose instead of reading the file, the agent would probably have tightened all four and quietly weakened a real assertion.

- **F11 came back an honest negative, which is the most valuable thing in this run.** The author measured mutation M-A against both the new and the pre-change suites and got `Failures: 3` **both times** — meaning the old suite already caught M-A and F11 closed no live hole. They reported it that way rather than presenting F11 as a fix. On this track, that restraint is the whole point: #2239 was blocked precisely because a status-file bullet generalised an unverified mutation result.

- **qc-behavioral then explained *why* the counts are identical, which neither the author nor I had.** `all_of` in `matchers.ml` is a `List.iter` and `equal_to` raises, so **only the first raising check in a list is ever reported**. Pre-change, the bare `"NOT EXERCISED"` check sits early and still *passes* under M-A (the mutated line does contain those two words); the failure comes from the `%`-absence check at the **end** of the list. Post-change, the label-anchored check is **first** and breaks immediately, so the `%` check is never reached. Same count, different assertion firing. That is the difference between "the numbers matched" and "I know what the test is doing" — and it means **a mutation result on these tests cannot be read from the failure count alone.** Filed as R6.

- **F12 and R3 closed gaps the old suite was measurably blind to.** Both mutations produce `Ran: 36 … OK`, **exit 0** against `origin/main`'s test file and `Failures: 1`, exit 1 against the new one. That two-direction form is what makes them findings rather than assertions.

- **R3's symmetry claim was verified by enumeration, not inspection** — six exclusions (3 tests × 2 sibling literals), each located. All present.

- Counts: `test_trade_audit_report` **36 → 36** (assertions added inside existing tests; no new test names). Production renderer `trade_audit_report.ml`: **0-line diff** — every verification mutation was reverted.

## QC Status

- **cleanup (#2248)** — structural **APPROVED 5/5**, behavioral **APPROVED 5/5**, both at `d0dcb442`. Merged `b8388074`. Rework: **0 of 2**.
- **support-floor-stops (#2249)** — structural **APPROVED 5/5**, behavioral **APPROVED 5/5**, both at `4912c609`. Merged `2c84d8c4`. Rework: **0 of 2**.
- Audit records: `dev/audit/2026-08-09-cleanup-tracked-compile-commands-cleanup.json` (quality 5) and `dev/audit/2026-08-09-feat-split-safe-test-hardening-support-floor-stops.json` (quality 5), both `consecutive_rework_count=0`, both mode 0644 (the `H-AUDIT-MODE-0600` fix from #2199 holding).
- **All four QC reviews posted to their PRs via REST on receipt.** `gh` is absent so agents cannot post their own. Per the 08-07 finding, **QC agents were instructed to return their review body and touch nothing under `dev/reviews/`** — no review file was written or truncated this run. That is now two consecutive runs with zero review-file corruption.
- **I independently reproduced one claim per PR before merging** rather than taking the reviews on faith: #2248's byte-exact 40-byte delta (4 occurrences × 10 chars), and #2249's untouched cross-negative at the fourth assertion site.

## Health Scan

`dev/health/2026-08-09-fast.md`. Measured post-merge at `2c84d8c4`: build **PASSING** (exit 0), tests **PASSING** (exit 0), status-file integrity **PASSING** (exit 0), index-size linter **PASSING** (13846/20480 bytes). Result: **CLEAN**.

**Step 6.2's documented path is wrong — 4th consecutive run.** `dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh` exits **2** (`sh: 0: cannot open …: No such file`); `run-in-env.sh` resolves `PROJECT_ROOT` to `<repo>/trading`, so the correct form is `sh devtools/checks/status_file_integrity.sh` (exit 0). An orchestrator trusting the documented form would escalate a phantom failure. Blocked on `.claude/**`.

**New: tree-cleanliness after a build is now a property of `main`.** Full `dune build && dune runtest` on `main@2c84d8c4` leaves `git status --porcelain` clean. Before #2248, every build dirtied the tree.

## Follow-up Queue

Total open `- [ ]` items across `dev/status/*.md`: **66** (was 64 on 08-08; threshold 10). As `H-FOLLOWUP-THRESHOLD-RETUNE` established, this count is crossed by construction — ~33 sit in `harness.md`, many of them T2/T4 roadmap items Step 2c explicitly declares undispatchable. I did not convert a feature slot to a maintenance pass on this signal. **The count is now also very slightly overstated in the other direction: one of the items it counts was the fenced template** (fixed this run).

**Four items filed this run, all from QC residuals, all non-blocking.**

**Into `dev/status/cleanup.md` §Backlog:**
- **`tracked_artifact_linter`** — no check anywhere in the repo would notice a build artifact being re-tracked. Verified by enumeration: **all 31 scripts** in `trading/devtools/checks/` read, none checks tracked-vs-gitignored lists or post-build tree cleanliness. So #2248 closed one *instance*, not the *class* — `git add -f` bypasses the new ignore silently. **This is the highest-value item filed this run**, because a tracked artifact that re-dirties the tree is a contributing mechanism for the blanket-`git add` sweep-in that is the repo's top open escalation. Owner is `harness-maintainer` (§Out of scope assigns `devtools/checks/` there), not `code-health`.
- **`flaky_test`** — `test_csv_snapshot_builder_cleanup` failed for one QC agent *before any mutation was applied*, while three independent full runs on the same SHA were exit 0. Intermittent, not broken. Matters because **every merge gate in this pipeline is an exit-code gate.**

**Into `dev/status/support-floor-stops.md`:**
- **R5 (#2249)** — the F12 byte pin is *transcribed*, not derived: `expected_section` hand-duplicates `_format_split_safe`'s `sprintf` format strings rather than calling the renderer. Precedented (the existing `core_lines` pin does the same) and intentional, but the coupling is now recorded.
- **R6 (#2249)** — `all_of` reports only the **first** raising check, so assertion **order** is semantically load-bearing, and a mutation result on these tests cannot be read from the failure count alone. The sharpest residual of the run.

**Closed this run:** `tracked_build_artifact`; support-floor-stops `F11`, `F12`, `R3`.

Carried, unactioned: F3 (still the highest-value item on support-floor-stops), F1, F2, F6–F10, R1/R2/R4; H-REPO-ROOT-SET-BUT-INVALID-SILENT-FALLTHROUGH, H-AUDIT-27B-*, H-AUDIT-ATOMICITY-PARTIAL-PIN, H-AUDIT-CHMOD-FAIL-CLOSED, H-REWORK-STREAK-ESCALATION-UNTESTED, H-JJ-JST-BROKEN-GHA, H-BLAS, H-CI-DISPATCH.

## Integration Queue

| PR | State | CI | structural | behavioral |
|---|---|---|---|---|
| #2248 | **MERGED `b8388074`** | green | APPROVED 5/5 | APPROVED 5/5 |
| #2249 | **MERGED `2c84d8c4`** | green | APPROVED 5/5 | APPROVED 5/5 |
| #2246 | **OPEN — yours** | — | — | — | `ops: weekly track pacer 2026-08-09`, docs-only (1 new file, +141) |

**#2246 is left for you.** It is authored by you, carries no GHA footer, and is docs-only — which under `.claude/rules/pr-merge-gates.md` skips both QC gates and needs only CI. I read its contents via the API and used its findings in this run (see Escalations); I did not merge it, on the same principle that left #2242 alone yesterday.

## Current Milestone Target

M5. No milestone-gated harness items unblocked this run (T2-B/T2-C/T2-D remain gated).

## Dependency Unlocks

None new. One sequencing consequence worth naming: with `trading/compile_commands.json` gitignored, **every future agent starts from a tree that a build does not dirty.** Both writers on 08-08 had to `git checkout --` it before committing, and so did the prior orchestrator. That papercut is gone, and with it one live `git add -A` hazard — though, per the `tracked_artifact_linter` residual, only for this one file.

## Budget

- Cap $200.00 (`merge-policy.json`); target band 60–95%. Auth is a Max-20x flat-rate subscription, so the cap is a runaway-dispatch backstop rather than a real spend limit.
- Subagent sessions: **5** (2 writers, 3 QC + 1 QC resumed), **7 dispatches** counting two resumes. **~780K subagent tokens.**
- **Wall clock, not budget, was the binding constraint** — ~1h35m, almost entirely serial agent time. Writers cannot overlap in this shared checkout and QC agents check out branches, so the pipeline is inherently serial. Two stalls cost roughly 12 minutes of dead time between them.
- **Utilization: UNDER_TARGET on cost.** I deliberately did not spend the remaining window on a third dispatch — see the harness decision in Escalations. That is the one track that was eligible and not dispatched, and it was a judgement call rather than a capacity limit.

## Escalations

- **[high] The `ops(budget)` sweep-in guard is still my top ask — third run running, and the weekly track-pacer independently reached the same conclusion today.** #2224 (08-06) swept `segmentation_test.ml` + a dune edit into `main`; #2235 (08-07) swept **300 lines of production OCaml** — `trade_audit_report.{ml,mli}` + 206 lines of tests, the entire F6 deliverable — through a PR whose body reads *"Additive observational data only."* Both auto-merged past **both** QC gates, and #2235 cost a legitimate, fully-QC'd PR (#2234) its merge. The pacer report in #2246 names this its **highest-priority finding of the week** and reaches the same fix: *"add a path allowlist to the budget-PR auto-merge path (ops PRs may touch `dev/budget/` and nothing else) or drop the auto-merge."* Two mechanical guards each catch it deterministically: (a) a title/path check that `ops(budget)` / `ops: daily orchestrator summary` PRs touch no `*.ml` / `dune` file; (b) an "empty diff vs base" check at PR-open time. **Both live under `.claude/**` or `.github/**`, which I cannot write.**

- **[high] `.claude/**` is not writable — 9th consecutive run, now blocking 9 items.** Claude Code's built-in self-modification guard, not the workflow token. Blocked: the **Step 6.2 path bug** (demonstrated live again — exit 2 on the documented invocation, 4th run); the `$?`-through-a-pipe bug in the same step; the Step 4 working-tree-isolation claim (still actively wrong — this environment is one shared checkout, and the doc's `jj workspace add` / no-isolation guidance does not describe it); the `/workspaces/trading-1` purge; `H-JJ-JST-BROKEN-GHA`'s fix; folding the "do not background your build" prohibition into the agent definitions (**now 31 of 31 across six runs**, entirely dependent on my pasting it into every brief — and it failed twice today anyway); the two-way-diff rule; instructing QC agents not to write review files; and now **the fenced-template scanning bug below.**

- **[high] `jj`/`jst` remain non-functional in GHA (H-JJ-JST-BROKEN-GHA).** Neither writer could open its own PR; I opened both #2248 and #2249 via the Step 4.5 REST fallback, and all four QC reviews needed REST posting. Cheapest fix is still (c): delete the `jst` instructions from the GHA path and document the REST fallback as the sole mechanism. Every brief now carries a hand-written paragraph undoing the agent definitions' `jj`/`jst` guidance.

- **[medium] Two of seven dispatches stalled on a backgrounded build — a regression from 0-of-8 (08-08) and 0-of-15/12/12 before that.** Both `code-health` and the first `qc-behavioral` ran a long `dune` command in the background and ended their turn "waiting for the notification", which never arrives — subagents here have no notification channel. Both were recovered by `SendMessage` with an explicit "there is no notification channel, run it in the foreground and block" and both then finished correctly, but it cost ~12 minutes. **My briefs contained the prohibition** in the same wording that produced 0-of-8 yesterday, so the wording is necessary and no longer sufficient. The durable fix is in the agent definitions (`.claude/**`, blocked). A cheaper mitigation I *can* apply next run: state the negative consequence in the first person — *"if you background it you will stall and lose your work; I have already had to rescue two agents today."*

- **[medium] A `grep '^- \[ \]'` over a status file matches inside fenced code blocks, and it cost a real bookkeeping error this run.** Step 2e's documented selection method picked the illustrative template out of `## How findings get here` in `dev/status/cleanup.md`, and the dispatched agent then ticked it, corrupting the template for the next reader. Both halves are mechanically preventable by making the scan fence-aware. The finding was real and the PR is good, but **the process selected it by accident**, and no QC checklist row asks whether the line an agent edited is the thing it appears to be. Restored and annotated in this run's commit. `harness_gap: LINTER_CANDIDATE`.

- **[medium] I deliberately did not dispatch the harness track, and I want your ruling.** The eligible item was `H-REPO-ROOT-SET-BUT-INVALID-SILENT-FALLTHROUGH` — well-specified, real, and reachable (`REPO_ROOT` set to a non-directory falls through to the walk-up, writes the record to a root the caller did not choose, `rc=0`, no diagnostic). But today's track-pacer (#2246, rec **#10**) observes that `write_audit.sh` / `record_qc_audit.sh` have absorbed **six corrective PRs in twelve days** (#2169, #2184, #2199, #2211, #2231, #2243), each fixing a distinct latent defect in the same two files, and concludes *"the defect rate suggests the script, not the individual bugs, is the unit of work."* Dispatching would have made it seven. The item's fix shape — make set-but-invalid a hard error consistently across **all three** implementations — is arguably consolidating rather than another patch, which is why this is a genuine judgement call rather than an obvious skip. **Tell me which you want and I will run it next slot either way.**

- **[info] The fence held in both directions.** Per `.claude/rules/gha-local-coordination.md` I dispatched nothing on `simulation` or `screener` and named the fenced files explicitly in both writer briefs (`trade_context.{ml,mli}`, `test_trade_context.ml`, `entry_audit_helpers.ml`, `screener.ml`, `Post_run_validator`, `validator_row_checks.ml`). Neither agent touched them. I did permit `test_trade_audit_report.ml` — fenced yesterday only because #2242 was open in it — after confirming #2242 had merged and that the split-safe region (~588–830) does not overlap the CSV-header area (~941) your entry-ticket work will touch.

- **[info] The P0 from your priorities doc is untouched, by design.** `dev/notes/next-session-priorities-2026-08-08.md` puts the faithful-StopLimit entry-ticket arc as P0, and its build order step 1 (audit-field add: `close_at_decision`, `ma_value`, `local_range_top`) sits squarely in the fenced files. It is yours; I stayed out.

- **[info] Zero review-file corruption, two runs running.** All four QC agents were told to return their review body and touch nothing under `dev/reviews/`; I posted all four via REST. Compare two 880→53-line truncations on 08-07.

- **[info] Orchestrator-owned files kept out of writer dispatches.** Both audit records were written only after the last writer had returned, so no untracked orchestrator file was in the tree while an agent could sweep it up. Both writers were also given an explicit "never `git add -A`, add by path, read `git status --porcelain` before every commit" instruction; both complied, and both PRs' two-dot and three-dot diffs agreed exactly.

- **[info] qc-structural flagged that the shared tree was left on a feature branch.** It found HEAD on `feat/split-safe-test-hardening` rather than `main` at review start (clean, so nothing was at risk) and restored it. That was mine — I left the tree there after verifying the branch. Worth the note: in a single-checkout environment the "return to `main` when done" contract applies to the orchestrator too, not just QC agents.

- **[info] Stale branches for cleanup:** `harness/rework-streak-escalation` (08-05, marker commit only, never PR'd), `cleanup/segmentation-default-params-pin`, `feat/split-safe-inert-report-row` (#2234, closed), plus the two merged today.

- **[info] EODHD absent in GHA — 12th week.** ~12 data-gated track rows unchanged. The track-pacer's 5th consecutive ask.

- **[info] Carried-forward verification: no still-real `[critical]` items**; main green by direct measurement (GitHub CI on `d696f603`; `dune runtest` exit 0 locally at `2c84d8c4`).

## Questions for You

1. **Add the ops-PR content guard.** Third run asking; the recurrence is confirmed and today's independent track-pacer audit ranks it the **week's top finding**. A path allowlist on the budget-PR auto-merge (ops PRs touch `dev/budget/` and nothing else) plus an "empty diff vs base" check at PR-open time would have caught **both** #2224 and #2235 deterministically. Needs `.claude/**` or `.github/**` access I do not have.

2. **Harness: patch or rewrite?** `H-REPO-ROOT-SET-BUT-INVALID-SILENT-FALLTHROUGH` is ready to dispatch and would be the **7th** corrective PR on the same two audit-recorder scripts in thirteen days. The pacer suggests the script is the unit of work, not the bugs. I held it rather than decide unilaterally. Say "patch" and I run it next slot; say "rewrite" and I will scope a single clean-slate PR for `write_audit.sh` + `record_qc_audit.sh` instead.

3. **Make the status-file scans fence-aware.** This run dispatched off a documentation template because `grep '^- \[ \]'` matches inside ` ``` ` blocks, and the agent then ticked it. Cheap, mechanical, prevents both halves. `.claude/**` + `trading/devtools/checks/`.

4. **The "don't background your build" instruction has hit its ceiling as a prompt.** It went 0-for-31 across five runs when pasted into every brief, then failed twice today with identical wording. It belongs in the agent definitions, which I cannot write. This is the single highest-frequency source of dead wall-clock in the pipeline.

5. **My recommended next feature pick is F3 on support-floor-stops** — `trade_audit_report_bin.ml` reading raw where the domain wants adjusted, the G14 pathology relocated to the reporting layer. It has been the track's highest-value open item for several runs. The reason I have deferred it twice is that it needs a golden re-pin *and* sits in the trade-audit reporting area your entry-ticket arc is active in. **If you can tell me the entry-ticket work will not touch `trade_audit_report_bin.ml`, I will take F3 next slot**; otherwise the R1/R2/R5/R6 residual bundle is the safe alternative.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
