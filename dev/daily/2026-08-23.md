# Status — 2026-08-23

**Run ID:** 2026-08-23-run-1
**Run timestamp:** 2026-08-23T07:28Z–09:2xZ (GHA orchestrator run 32625658534)
**Mode:** FULL

> **The queue was EMPTY — zero open PRs — which is the opposite of saturated and
> means maximum dispatch capacity.** Three tracks dispatched, three PRs opened,
> **all three merged**, one through a full rework loop that closed a real
> defect *before* it shipped. The headline landing is **#2486 confirmed as a
> provable deadlock**: the majority of positions in this system may not be
> trailing at all.

## Headline

**#2486 is real, and stronger than it was filed.** The issue described a
freeze; the agent proved a **deadlock with an invariant**. For a long in
`Trailing`, with `A0` the extreme seeded at the `Initial → Trailing` transition:

```
reachable_stop  ≤  min(correction_extreme, ma) × (1 − trailing_stop_buffer_pct)
                ≤  A0 × (1 − trailing_stop_buffer_pct)      [monotone non-increasing]
```

Once that bound sits at or below the installed stop, `_is_better_stop` is false
on **every** cycle → no cycle raises → no cycle resets the anchor → the bound
never recovers. **A rising MA cannot rescue it** — it enters through a `min`
already dominated by `A0`. The root cause is a **conflation**: one `option`
answered both *"did a cycle complete?"* (a fact about price) and *"may we
install this stop?"* (the never-lower rule).

Precondition, exactly: fallback `S0 = 0.9792 × entry`, so it freezes whenever
the entry bar's low is more than **1.0909%** below entry — near-certain on a
weekly bar, and permanent once reached. `project_fallback_stop_half_book_band`
records that **the fallback IS the common path**.

**Every one of the four QC passes that reviewed substantive work found something
the author had not.** One of them found a **fourth instance of this repo's
recurring vacuous-pass defect** — in the very PR built to prevent drift — and
caught it at the gate rather than in a follow-up PR. That is the pacer's
recommendation #9 pattern, interrupted for the first time.

## Established before dispatch (measured this run, not inherited)

- **Main GREEN at `e64f8655`** — read from `/commits/<sha>/check-runs`, not a
  workflow listing: `build-and-test` **success**, `perf-tier1-smoke`
  **success**, `golden-custom-universe` **success**. `golden-sp500-5y` was
  `in_progress` and is not a required PR gate.
- **Five shell gates run natively, exit codes read directly** (never piped — a
  `| tail` reports `tail`'s status): `status_file_integrity` **0**,
  `index_size_linter` **0**, `no_python_check` **0**, `tracked_artifact_linter`
  **0**, `posix_sh_check` **0**.
- **Zero open `ci-red` issues** (18 open issues scanned).
- **Zero open PRs.** Not saturation — the opposite.
- **Toolchain re-probed, not inherited:** `gh` **ABSENT**, `docker` **ABSENT**,
  `dune` 3.24.2 works via `eval $(opam env)` but is **not on PATH**, `jj`/`jst`
  present but non-functional (`H-JJ-JST-BROKEN-GHA`), `EODHD_API_KEY` **absent**.
  4 cores / 15 GB / 63 GB free.
- **Preflight verified, not assumed:** `_completed_cycle_stop` /
  `last_correction_extreme` located in `weinstein/stops/`; `cancel_handler.{ml,mli}`
  and `test_entry_fill_retry.ml` present; `simulator.ml` at **487** lines (the
  prior run's "493/500" had improved); **zero unchecked Blocking Refactors** in
  any status file.

## Step 0.5 / 1b / 1c

**Fast-exit declined on two independent grounds.** The first-run-of-day escape
hatch applied (last summary was 2026-08-21-run2, >24h old — there was no
orchestrator summary on 08-22, though a local session was highly active). More
decisively, the **A-FASTEXIT-VACUOUS precondition** fired: with **zero** open
PRs there is nothing to be saturated *with*, and Condition 1 would have passed
vacuously.

**Step 1b drift: NONE.** Everything the 08-21 summary listed as pending
resolved: **#2456 and #2433 both merged on 08-22 by human decision on the
merits** — the two `do-not-merge` holds it was waiting on. That is the hold
mechanism working end to end.

**Step 1c: no still-real `[critical]` items.** The prior summary carried none,
and main measured green independently by CI, five shell gates, and a zero-count
of `ci-red` issues. **No `[critical]` is emitted for this** — a green
verification must never fire the "Fail on escalations" gate.

## What the QC passes actually found

### #2494 — the disease inside the cure, caught at the gate this time

The PR adds a check that diffs `trading/trading/backtest/` subdirectories
against a hand-maintained appendix table (stale twice: #2096, then #2461).
qc-behavioral falsified the script's own inline comment, which claimed the
heading-to-EOF `awk` range meant "a match failure downstream … **never a silent
pass**":

```
appendix with `alpha/`, then a LATER `## Appendix B` containing | `smuggled/` | … |
  -> OK … 2 on-disk subdirectories … all have an appendix row     exit 0
```

**A nonexistent subdirectory silently credited, exit 0** — the same defect shape
as #2462, which found the identical silent-swallow bug surviving *inside its own
fix*. Latent today only because the appendix sits at line 158 of 208, i.e. one
future section away from live.

It also found the script's **headline differentiating claim** ("requires an
actual TABLE ROW … a prose mention does NOT satisfy") was behaviourally correct
but **pinned by nothing**.

**Rework iteration 1 closed all six findings**, and the re-review verified them
independently rather than accepting the transcript: it rebuilt the `smuggled/`
fixture from scratch against both scripts pulled from git (old: exit 0; new:
`FAIL … smuggled/`, exit 1), and re-ran its own **M3** mutation (exclusion
exact→substring) — **old 6-scenario suite GREEN, new 9-scenario suite RED**, a
clean before/after on the exact gap it had reported.

### #2492 — the strongest default-off proof this program has produced

qc-behavioral did not take the author's deadlock argument on trust; it
re-derived it and **actively hunted escape paths**, ruling out `last_trend_extreme`
(gates *whether* a cycle fires, never the level), re-entry to `Initial` (no
transition returns there), `Stop_track.ratchet` (raises only — it makes the
precondition *more* binding), and exit/re-entry. It re-derived the 1.0909%
arithmetic independently.

For the default-off safety property it wrote a **throwaway differential harness
against the public API only** (never naming the new field), ran it at HEAD, then
checked out the merge-base stop machine and re-ran: **1000 tapes × 20 bars =
20,000 `update` calls**, both initial-stop flavours, all 4 stages × 3 MA
directions. **`cmp`: two 51,618-line traces byte-identical.** That is a far
stronger claim than "the flag defaults to false".

It also **strengthened the book argument**: §5.2 says stops "trend upward across
correction cycles", but the sharper point is that the book's `correction_low` is
*this cycle's* low measured from the *current* peak — **the reference resets per
cycle by construction**, so the carried-forward `last_correction_extreme` has no
counterpart in the book's state machine. "Artifact, not faithful" is supported,
not merely plausible.

### #2493 — the reconcile corrected the brief and the pacer

The docs agent found **7 stale `PR OPEN` markers, not the 4 the pacer named**
(oldest stale by 83 days), and corrected two upstream errors: **`#1782` is an
issue, not a PR** (the real PR is #1786), and **the pacer misattributes #2302**
to `rename-twin-dedup` — `700bdcda` touched a *different* module
(`Rename_detector`) booked to `cleanup`, making that track's real idle period
**41 days, not 10**.

It also **declined to close `rename-twin-dedup`**, reporting it as ambiguous
with three options rather than taking a call the brief reserved for the
maintainer. That restraint is the right instinct and worth reinforcing.

## Corrections to my own dispatch briefs

Two of my briefs contained errors, and in both cases the agent caught them
because it was told to re-measure rather than trust:

1. **I told the harness agent the appendix had drifted a third time**, naming
   five suspects (`trade_audit_report`, `tuner`, `validation`, `walk_forward`,
   `warmup_gate`). It did not reproduce — **27 subdirs, 27 rows, `comm -23`
   empty**; #2461 had already reconciled it. My count came from reading a
   `head -25`-truncated grep. The PR correctly mechanizes the check on an
   already-clean tree and **invents no appendix rows**. Two independent
   reviewers reproduced 27/27.
2. **I told QC reviewers "do not overwrite the first line" of the review file.**
   Correct for a first pass, **wrong for a re-review at a new SHA** — see
   `A-AUDIT-REWORK-SHA-STALE` below.

## Dispatched this run

| Track | Agent | Outcome | Notes |
|---|---|---|---|
| support-floor-stops (#2486) | feat-weinstein | **#2492 → MERGED `4141beda`** | deadlock CONFIRMED w/ invariant; default-off `reset_anchor_on_stalled_cycle`; 7 tests + R2 axis; extracted `Stop_geometry` (522→474) rather than bump the limit |
| harness (doc-drift) | harness-maintainer | **#2494 → MERGED `62be11e6`** | **refuted my drift claim** (27/27 clean); 3 guards, all mutation-tested |
| docs (6 track files) | general-purpose | **#2493 → MERGED `3782920a`** | 7 stale markers (not 4); corrected the pacer + the brief; declined the one call reserved for a human |
| #2493 | qc-structural | **APPROVED** (5) | spot-checked merge SHAs; no false claims |
| #2493 | qc-behavioral | **APPROVED** (4) | verified 19 merge SHAs, 4 config defaults, ledger verdict; 3 non-blocking (N1–N3) |
| #2494 | qc-structural | **APPROVED** (5) | reproduced 27/27 and the guard-3 vacuous pass |
| #2494 | qc-behavioral | **NEEDS_REWORK** (2) | **found the silent pass inside the drift check** + an unpinned headline claim |
| #2492 | qc-structural | **APPROVED** (5) | verified R1/R2 mechanically; confirmed no limit bumped, no `@large-module` added |
| #2494 rework | harness-maintainer | **`43379335`** | all 6 findings closed; suite 6→9 scenarios |
| #2492 | qc-behavioral | **APPROVED** (5) | 20,000-call differential vs merge-base, byte-identical; 4 mutations |
| #2494 re-QC | qc-structural | **APPROVED** (5) @ `43379335` | re-ran the B1 fixture + guard-3 mutation itself |
| #2494 re-QC | qc-behavioral | **APPROVED** (4) @ `43379335` | rebuilt the fixture from git; clean M3 before/after |
| ops-data | — | **skipped** | sentinel: `data-gaps.md` unchanged since #814 (2026-05-03, **112 days**); `EODHD_API_KEY` absent |
| code-health | — | **not dispatched** | top `cleanup.md` item is the `linter_coverage` **human policy decision** (pacer rec #8); `design_doc_drift` was routed to harness instead |
| health-scanner | — | not dispatched | fast scan is deterministic (Step 6); deep scan is a weekly cron (08-17, on cadence) |

## QC Status

| PR | structural | behavioral | outcome |
|---|---|---|---|
| #2492 | APPROVED @ `39893ec4` (5) | APPROVED @ `39893ec4` (5) | **MERGED** `4141beda` — all 3 gates |
| #2493 | APPROVED @ `6c86d37e` (5) | APPROVED @ `6c86d37e` (4) | **MERGED** `3782920a` — all 3 gates |
| #2494 | APPROVED @ `775aa1bc` (5), re-APPROVED @ `43379335` (5) | **NEEDS_REWORK** @ `775aa1bc` (2) → **APPROVED** @ `43379335` (4) | **MERGED** `62be11e6` — rework 1/2 |

Both merges of synced branches were checked the 08-21 way: when `update-branch`
moved the SHA, I diffed the files main gained against the reviewed surface.
**Intersection empty in every case**, so the verdicts hold in substance.

## Pending work

| Track | State | Branch | PR | Next step |
|---|---|---|---|---|
| support-floor-stops | **auto-merged** | `fix/stop-ratchet-freeze-2486` | **#2492** | `4141beda`. Next: **H1** — `_ratchet_tightened` has the *same* defect shape |
| docs / 6 tracks | **auto-merged** | `docs/status-reconcile-2026-08-23` | **#2493** | `3782920a`. Pacer #6 discharged, #7 partial |
| harness | **auto-merged** | `harness/design-doc-drift-check` | **#2494** | `62be11e6`. Both gates APPROVED @ `43379335` after rework 1/2 |
| #2486 real-data half | **blocked (LOCAL)** | — | — | Instrumented 26y run, shared with #2489/#2490. **Needs a container — undispatchable from GHA** |
| #2466 salvage | not started | — | — | P3; three concrete test ports. GHA-reachable next run |

## The concurrent pipeline, seventh instance — and this time it was benign

**#2491 was created at 07:36Z — eight minutes after this run started — and
merged, by an actor I cannot identify** (`#2428`: every actor authenticates as
`dayfine`). It records that **#2486 + #2489 + #2490 share ONE instrumented 26y
run**.

That is a direct overlap with my highest-value dispatch, and I only learned of
it because the docs agent noticed "zero open PRs" had become false mid-run.
**It turned out benign, and the reason is structural rather than lucky:** the
local session owns the *real-data* half, which needs a container GHA does not
have; I dispatched only the *code-level* half. A default-off flag leaves main's
measured behaviour bit-identical, so **the instrumented run is unaffected — and
now has a second arm plus an exact precondition (`entry-bar low > 1.09% below
entry`) to predict which positions should show the freeze.**

I fenced it explicitly in #2492's body per `gha-local-coordination.md`
§"Collision avoidance" **before** the agent finished, rather than describing the
overlap afterwards. That is the control #2466's process note asks for.

## Health Scan

`dev/health/2026-08-23-fast.md` — **CLEAN**. Five shell gates exit 0; CI green
on `main@e64f8655`; zero open `ci-red` issues.

**A whole-repo local `dune runtest` was deliberately not launched**, same call
as the 08-21 runs: agents were building concurrently in their own worktrees on a
4-core/15 GB box where a prior run was **OOM-killed (exit 137, silently)**. The
build gate rests on CI plus each agent's own exit-0 run in its own worktree.

**Carried trap, still live:** `dune runtest` output contains **two `FAIL:`
strings that are not failures**, inside `OK:` lines of `sete_diagnostics_check`.
`grep -c 'FAIL:'` → 2 on a green tree; `grep -c '^FAIL:'` → 0. Every brief this
run stated it, along with the `cmd | tail; echo $?` trap.

## Budget

- Cap **$200.00** (`merge-policy.json`), target band **60–95%**. Auth is a
  Max-20x flat-rate subscription, so the cap is a runaway-dispatch backstop, not
  a spend limit.
- **12 dispatches** (3 builders, 1 rework, 8 QC passes), **≈1.79M** subagent
  tokens measured. At the **measured** 08-21 rate of **$5.87/dispatch**
  (`dev/budget/2026-08-21-32482056746.json` = **$58.65** for 10 — note the
  08-21 summary's own "≈$52" estimate was low), this run estimates **≈$70 ≈ 35%**
  — **UNDER_TARGET**. Tagged *estimated*; today's budget JSON supersedes it.
- **Under-target, and this time genuinely because the work ran out.** Every
  eligible GHA-reachable track was dispatched. What remains is either a **human
  decision** (the four-item menu, the `linter_coverage` policy, three one-action
  gates) or **container-gated** (every research issue: #2489, #2490, #2408, and
  #2486's own real-data half). That is the structural ceiling, not a pacing
  choice.
- **Fill-loop not run**: after the initial batch the box was continuously
  occupied by QC, and **QC outranks fill-loop work** when both contend
  (`container-capacity-scheduling.md`).

## Escalations

*(carried items re-verified by execution, not copied; new items marked NEW)*

- **[info] Carried-forward verification: no still-real `[critical]` items.**
  Main green at `e64f8655`; five shell gates exit 0; zero open `ci-red` issues.
  The two `do-not-merge` holds (#2456, #2433) both **resolved by human decision
  on 08-22**, so nothing carries.

- **NEW [high] #2486 is confirmed, and its scope is larger than one flag.** The
  freeze is provable and the fallback is the common path, so **the majority of
  positions may not be trailing at all** — they exit via stage-based exits or at
  a frozen initial stop. The fix ships default-off; **nothing changes until the
  instrumented run measures it and a ledger ACCEPT justifies a flip.** This
  interacts directly with menu item §2.1 (`initial_stop_buffer` 1.02 → 1.0),
  which widens exactly the stop this says is also frozen.

- **NEW [medium] `_ratchet_tightened` has the same defect shape** (follow-up
  H1). Its candidate is monotone non-increasing, so after the single buffer-step
  raise on the first `Tightened` bar it can never raise again — contradicting
  its own docstring. qc-behavioral confirmed the defect **and** confirmed that
  deferring it leaves nothing incoherent (disjoint one-way states, safe
  interaction direction, never-lower holds independently). It also hinges on a
  book question unanswerable on this runner.

- **NEW [medium] `A-AUDIT-REWORK-SHA-STALE` — the audit attributed post-rework
  APPROVALs to the pre-rework SHA.** #2494's record initially read
  `sha: 775aa1bc` — the SHA at which behavioral was **NEEDS_REWORK**. Cause:
  nothing updates the review file's `Reviewed SHA:` first line across a rework
  (my brief said not to overwrite it, which is right for a first pass and wrong
  for a re-review). **Corrected by hand this run**; the record now reads
  `43379335` with the lineage recorded. Worse than a cosmetic bug: it inverts
  the semantic content of the audit trail.

- **NEW [medium] `A-AUDIT-SHA-BACKTICK`** — `record_qc_audit.sh` captured a
  Markdown backtick into the `sha` field, yielding `` "`6c86d37eb2f" ``. Filed
  in `dev/status/orchestrator-automation.md` with the root cause (last-occurrence
  rule + a backticked SHA in an appended section) and fix shape. It corrupts the
  field that distinguishes a genuine rework from a retry, i.e. the input to the
  `>= 3` escalation. Prose workaround applied to briefs; **re-home to
  `harness.md` on next touch** (a rework agent held that file at filing time).

- **NEW [medium] The check/linter suite needed a corrective pass for the fourth
  time in a month** — but for the first time **the gate caught it, not a
  follow-up PR**. #2430 → #2462 → #2464 took two corrective PRs within 24 hours;
  #2494's defect was found pre-merge. Pacer recommendation #9 stands, with one
  data point in the other direction.

- **[high] The concurrent QC/dispatch pipeline remains undetectable before
  dispatch** (**#2432**). Seventh instance (#2491, mid-run). Benign this time by
  structure, not by luck. A claim marker (label or comment convention) would let
  whichever pipeline arrives second stand down.

- **[high] Strategy/research work remains structurally undispatchable from GHA.**
  `docker` absent. **All four P1 research issues** (#2489, #2490, #2408, and
  #2486's real-data half) need a container. **#2492 is the exception that proves
  the shape** — the *code-level* half was dune-only and shipped cleanly.

- **[high] `book-as-authority.md`'s tier 2 is unreachable here** (**#2457** —
  closed by #2484's environment-aware protocol). **The protocol worked as
  designed this run**: one `BOOK-CHECK-NEEDED` was queued, and qc-behavioral
  independently judged it the *right* question and correctly scoped
  (`PLAUSIBLE-pending-book`) rather than guessed. First clean exercise of the
  new protocol.

- **[medium] `H-JJ-JST-BROKEN-GHA` confirmed again.** **All three PRs this run
  were opened by the orchestrator's `curl` REST fallback** — no dispatched agent
  can open its own PR.

- **[medium] Two project rules still contradict each other on how QC agents
  build** (**#2386**). `qc-*-authority.md` mandates `docker exec trading-1-dev`,
  absent here. Overridden by hand in all 8 QC briefs this run.

- **[info] Docs-only allowlist gap.** #2493 was 7 `.md` files plus **one
  comment-only `.sexp` fix**, which falls outside the `pr-merge-gates.md`
  allowlist and therefore took the full gates. **Both reviewers independently
  recommended extending the allowlist** to comment-only
  `dev/experiments/**/*.sexp`. Running the gates was the right call here
  regardless — a PR asserting ~460 lines of fact deserves verification, and
  behavioral found three residuals.

- **[info] EODHD absent in GHA.** `data-gaps.md` untouched since #814
  (2026-05-03) — **112 days**. ops-data correctly sentinel-skipped. Pacer's 7th
  ask on this question.

- **[info] Two stale remote branches** (`docs/handoff-addendum-2`,
  `ops/track-pacer-2026-08-23`) survive their merged PRs. Cosmetic.

## Follow-up Queue

- **From #2492** (recorded in `dev/status/support-floor-stops.md`): **H1** the
  `_ratchet_tightened` twin defect; **H2/H3**; plus a prose nit — the `.mli`
  generalises the no-escape claim to "the life of the position" while the proof
  is about `Trailing`; a `Tightened` transition can raise once, by ≤0.5% of `A0`,
  in the band `S0/A0 ∈ [0.99, 0.995)`. `dev/status/` words it correctly; only the
  `.mli` overreaches.
- **From #2494** (non-blocking, after both APPROVALs): **N1** — the *script*
  header still carries the guard claim that was corrected in the *test* header
  (deleting guard 1 or 2 exits 1; only guard 3 gives a false OK). **N2** — stale
  "6 scenarios" in `trading/devtools/checks/dune:786` and in the PR body. **B6
  behaviour is closed but its test is unpinned** — reverting the regex widening
  leaves the suite green; no hyphenated/dotted fixture exists. **Residual:** a
  nested `### Appendix B` or `##Appendix B` still credits a row (both headers
  scope the claim to `## ` literally, so not a contract violation).
- **From #2493**: `backtest-perf.md` says "those eight paths" then enumerates
  seven — true of the real set (the eighth is `release_report/`), so understated
  rather than wrong; not reworked because fixing one clause would move the SHA
  and invalidate two APPROVED verdicts.
- **`#2466` salvage** (P3) — three concrete, GHA-reachable test ports. Deliberately
  deferred this run to keep the rework loop inside its cap.
- `dev/status/cleanup.md` — top item remains the unanswered **test-file-length
  policy decision**, human-gated across ten runs (pacer rec #8).

## Integration Queue

**Three merged by me, none held, none merged out from under a review.**

- **#2493** → `3782920a`, **#2492** → `4141beda`, **#2494** → `62be11e6`.
  Final `main` state: **`62be11e6`**, re-verified after the merges rather than
  before.
- Every merge used the **pre-merge `state`/`merged` read** plus a **fresh
  hold re-check immediately before the merge call** — label *and* timeline
  `convert_to_draft`-after-first-review, per `pr-merge-gates.md` Rule 0 and the
  #2480 hardening. **No PR carried a hold at any point this run**, and I touched
  no label.
- Both draft→ready flips ran the hold check first. Both drafts were opened *by
  me* as kill-resilience on first push and never converted post-review, so
  neither was a hold — verified, not assumed.

## Current Milestone Target

Axis 1 of the arc is feature-complete and the funding program is closed. The
live question is no longer *"can we fund the ticket"* but **"is the stop
machine trailing at all"** — which #2486 has now turned from a fixture
observation into a proved mechanism with an exact precondition. The measurement
that decides it needs a container.

## Dependency Unlocks

- **#2486's code-level half is done and on main, default-off.** The instrumented
  26y run (#2486 + #2489 + #2490) can now measure **two arms** instead of one,
  and knows exactly which positions to expect the freeze in.
- **Menu item §2.1 (`initial_stop_buffer` 1.02 → 1.0) is now better informed** —
  the flip widens precisely the stop this run proved is also frozen. The two
  should be decided together, as issue #2486 warned.

## Questions for You

1. **Does the ratchet freeze reproduce on real data?** This is the one question
   that matters most and I cannot answer it here. The mechanism is proved and
   the precondition is exact (`entry-bar low > 1.09% below entry`); the flag is
   in place to A/B it. If it reproduces at the rate the invariant predicts, most
   positions in every historical result have not been trailing — which touches
   the arc's faithfulness trade and the `initial_stop_buffer` decision at once.
2. **Should `reset_anchor_on_stalled_cycle` be part of the shared instrumented
   run?** It is default-off and free to add as a second arm.
3. **Extend the docs-only allowlist to comment-only `dev/experiments/**/*.sexp`?**
   Both reviewers said yes independently; I did not change the rule.
4. **The four-item decision menu is unchanged and is now the binding
   constraint.** `initial_stop_buffer`, the arc faithfulness trade, grade-tiered
   reservation, and the rt promotion path. With the queue empty, these gate more
   throughput than anything else.
5. **`linter_coverage` test-file policy** — 10th run unanswered, still blocking
   the top `cleanup.md` item (pacer rec #8).
6. **Three one-action human gates**, all long-stale: `tuning` M2 enable-commit
   (#1327, 88d, M7 critical path), the `workflow`-scoped PAT (#1636), the
   ghcr.io flambda rebuild (71d — without it `-O3` is a silent no-op in CI).

## What the next run should know

1. **An empty queue is maximum capacity, not idleness.** Zero open PRs correctly
   *declined* the fast-exit via its own precondition. The check fired right.
2. **#2486 is proved, not suspected.** The invariant, the exact 1.0909%
   precondition, and the escape paths already ruled out
   (`last_trend_extreme`, `Initial` re-entry, `Stop_track.ratchet`, exit/re-entry)
   are all in `dev/reviews/stop-ratchet-freeze-2486.md`. Don't re-derive them.
3. **Tell agents to re-measure the brief's own claims.** My drift claim was
   wrong and the agent caught it; my SHA instruction was wrong and the audit
   caught it. Both briefs said "verify, don't trust" — that is why the run has
   no silent errors in it.
4. **A differential harness against the merge-base is the gold standard for a
   default-off claim.** 20,000 calls, byte-identical traces, public API only.
   Ask for it whenever a flag claims bit-identity.
5. **Update the review file's `Reviewed SHA:` line on a rework** — see
   `A-AUDIT-REWORK-SHA-STALE`. And write SHAs **bare, never in backticks**.
6. **The rework loop is worth its cost.** One iteration turned a NEEDS_REWORK
   (quality 2) into APPROVED (quality 4) inside the same run and closed a real
   vacuous pass. Waiting a scheduler interval would have shipped it.
7. **Fence local/GHA overlap in the PR body before the agent finishes**, not
   after. #2491 landed mid-run on the same issue.
8. **`#2466` is the best-specified GHA-reachable item left** (three concrete
   test ports).
9. All three PRs were opened by the **curl REST fallback**; all 8 QC briefs had
   to hand-override `docker exec trading-1-dev` (#2386). Budget the boilerplate.

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
