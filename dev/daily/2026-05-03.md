# Status — 2026-05-03
Run timestamp: 2026-05-03T08:10:00Z
Run ID: 2026-05-03-run-1

**Mode:** Full pass — Step 0.5 fast-exit declined (Condition 2 fails: 2 status files modified by 5 non-summary commits since prior summary commit #784 at 2026-05-02T13:08Z).

## Saturated-queue check result

- **Condition 1** PASS — 0 open PRs across the repo (no SHA mismatches to handle).
- **Condition 2** FAIL — `dev/status/data-foundations.md` and `dev/status/_index.md` modified since prior summary commit (PREV_ISO 2026-05-02T13:08Z). 11 non-summary commits on main: #785..#794 (5 of them touched `dev/status/`).
- **Condition 3** N/A — Step 1b ran; no `[drift]` warnings emitted (run-2's pending work table had no `dispatched`/`awaiting-merge` rows to verify).
- **Condition 4** PASS — `harness.md` and `cleanup.md` unchanged on main since prior summary.

Decision: full pass.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| (all tracks) | observation-mode | — | — | **Main is GREEN** on `dune build && dune runtest --force` (HEAD `825bc0e`); yesterday's run-2 [critical] linter regressions are RESOLVED. Maintainer-driven merge stream continues at ~11 PRs/day cycle on the `data-foundations` (M5.3 snapshot pipeline) track; orchestrator stays observational while track-ownership question for the four new tracks (`experiments`, `tuning`, `data-foundations`, `weekly-snapshot`) is unresolved. |
| data-foundations | merged-on-content (5 PRs today) | — | — | M5.3 Phase A.1 (#786), Phase D (#790), Phase E (#791), Phase B perf (#792), Phase F.1 marker (#793) all merged today. Pending: Phase F.2 runner default flip + auto-build (~300-500 LOC); Phase F.3 `Bar_panels.t` retirement (gated on F.2 + several-week soak); Synth-v3 (~1000 LOC); Norgate ingest (vendor signup blocker). Owner unset; same scope-extension question as `experiments` / `tuning`. |
| experiments | blocked-on-ownership | — | — | M5.2d distributional/antifragility (~250 LOC; sister to M5.2c #762 — `_concavity_coef` already partially scaffolded); M5.4 E3 stop-buffer sweep (uses #780 `--fuzz` infra); M5.4 E4 scoring-weight sweep. Status file body still claims M5.2a–d PLANNED + M5.4 blocked on M5.1 (M5.1 hardening landed 2026-05-02; M5.2a/b/c/e + M5.4 E1/E2 also merged — body untrue on main). |
| weekly-snapshot | merged-on-content | — | — | M6.1–M6.5 all merged 2026-05-02; M6.6 live cycle DEFERRED per plan. Future weinstein/snapshot/* work formally lives in feat-weinstein scope per #778. Not orchestrator-dispatchable today. |
| tuning | blocked | — | — | T-A grid_search (~400 LOC) — pre-reqs (M5.2c metrics + M5.2e per-trade context) now merged. Remaining block: Norgate ingest + scope-extension decision. |
| simulation | blocked-by-scope | — | — | M5 walk-forward + tuner; sp500-2019-2023 baseline rerun (local-only). Yesterday's P0 RED CI (#752) RESOLVED; status file body still references the resolved P0. |
| short-side-strategy | merged | — | — | sp500-2019-2023 baseline rerun (local-only). G6/G14/G15/G4 all RESOLVED via #735–#746 (2026-05-02). |
| backtest-perf | blocked | — | — | Tier-4 first manual `workflow_dispatch` (local-only); N≥5000 release-gate stays P1 awaiting daily-snapshot streaming (now actively shipping in `data-foundations` M5.3). |
| hybrid-tier | blocked | — | — | Option 2 daily-snapshot streaming substantively shipped today via `data-foundations` M5.3 Phase A.1 → F.1 (Phases A/A.1/B/B-perf/C/D/E/F.1 all merged); Option 2 effectively in flight on the sister track. Phase F.2 (runner default flip) is the next milestone; Phase F.3 (`Bar_panels.t` retirement) eventually closes hybrid-tier. |
| harness | blocked | — | — | Backlog drained (T1 done, T2 milestone-gated, T3-* superseded/low-priority, T4-* future). No dispatchable items. |
| orchestrator-automation | blocked | — | — | Phase 2 (background execution) pending empirical tests per status file. |
| cleanup | blocked | — | — | Backlog empty. No `[medium]`/`[high]` findings from today's deterministic post-run checks (build green, integrity advisory only). |
| cost-tracking | blocked | — | — | Today's measured cost will land in `dev/budget/2026-05-03-<run_id>.json` post-GHA-capture. |
| ops-data | skipped | — | — | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel rule). EODHD_API_KEY also unset in GHA env. |
| (other tracks merged earlier) | — | — | — | optimal-strategy / screener / trade-audit / backtest-infra / backtest-scale / data-panels / data-layer / portfolio-stops / strategy-wiring / sector-data / support-floor-stops — all MERGED (no change since run-1). |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| — | — | none | Same observational stance as 2026-05-02 run-1 + run-2: the four new tracks (`experiments`, `tuning`, `data-foundations`, `weekly-snapshot`) lack explicit feat-agent ownership in `.claude/agents/`. PR #778 extended only feat-weinstein scope (which is now in scope for weekly-snapshot + future weinstein/* work) — backtest-side new-track scope extension is still pre-decision. The maintainer is driving these tracks directly at ~11 PRs/day; dispatching feat-backtest now risks colliding with active work. Carry [info] for third consecutive run. |
| ops-data | ops-data | skipped | `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14 (sentinel skip per Step 2d). EODHD_API_KEY unset in GHA env. |
| harness | — | skipped | No dispatchable T1/T3 items (backlog drained). |
| cleanup (Step 2e) | — | skipped | §Backlog empty AND no `[medium]`/`[high]` findings from today's deterministic Step 6 checks (main green; only advisory schema FAILs from new tracks). |

## Feature Progress

### data-foundations [IN_PROGRESS — major progress today, no orchestrator dispatch]

Five PRs in the M5.3 snapshot-pipeline plan + one docs PR merged in the run-2 → today window:

- **#786** M5.3 Phase A.1 — OHLCV columns precursor for Phase D (snapshot schema width 7 → 13).
- **#790** M5.3 Phase D — wire snapshot reads into simulator (`--snapshot-mode --snapshot-dir <path>` feature flag; new `Backtest.Snapshot_bar_source` shim; `Market_data_adapter.create_with_callbacks` constructor; bit-identical CSV path when flag absent; 3 new parity tests).
- **#791** M5.3 Phase E — validation + tier-4 spike (PR-5 of 5; entirely under `dev/experiments/m5-3-phase-e-validation/`; pinned bit-for-bit parity between CSV mode and snapshot mode on `parity-7sym` × {2019h2, 2018h2-2019} windows; surfaced F2 = O(N²) per-symbol writer; F3 = tier-4 RSS bounded by LRU cache cap, 50–200 MB regardless of corpus size).
- **#792** Phase B perf O(N²) → O(N) per-symbol (new `indicator_arrays` + `weekly_prefix` modules; AAPL ~11K bars writer 7.47s → 0.21s = 35× speedup; bit-identity preserved).
- **#793** M5.3 Phase F.1 — deprecation marker on `Bar_panels.t` (~25 LOC; documentation-only — no `[@@deprecated]` attribute to avoid breaking the existing call-site stack with `-warn-error`; F.2 = default flip + auto-build deferred; F.3 = actual deletion gated on snapshot-as-default soak).
- **#794** docs persist QC review records #789-#793 + data-foundations status update.

Track is now structurally complete on the snapshot pipeline through Phase E + F.1; remaining: Phase F.2 runner default flip + auto-build (~300-500 LOC, smallest unblock), Phase F.3 `Bar_panels.t` retirement (800-1200 LOC across multiple PRs, gated on F.2 + several-week soak), M7.0 Synth-v3 multi-symbol factor (~1000 LOC), Norgate ingest (vendor signup blocker).

### experiments [IN_PROGRESS — no work this run]

No new merges today. M5.2d distributional/antifragility (~250 LOC) remains the smallest pending; `_concavity_coef` is already partially scaffolded in `trading/simulation/lib/distributional_computer.ml`. M5.4 E3 stop-buffer sweep + E4 scoring-weight sweep also pending. Status file body claims M5.2a–d PLANNED + M5.4 blocked on M5.1; both untrue on main (M5.2a/b/c/e + M5.4 E1/E2 all merged 2026-05-02; M5.1 hardening also landed via #752 yesterday). Refresh deferred to next dispatched-on-this-track run.

### simulation [IN_PROGRESS — no work this run]

Yesterday's P0 RED (`split_day_stop_exit:1:post_split_exit_no_orphan_equity`, fixed by #752) RESOLVED; verified again today via end-to-end `dune runtest --force` exit 0 on HEAD `825bc0e`. M5 walk-forward + tuner remain. Status file body still references the resolved P0 — refresh deferred.

### short-side-strategy [MERGED — no work this run]

PR #778 (yesterday) refreshed the per-track file's status entry with G14/G15 marked RESOLVED via #736/#737/#739/#740 from yesterday. sp500-2019-2023 baseline rerun (local-only) is the only remaining follow-up.

### data-foundations / weekly-snapshot / tuning [no orchestrator dispatch — observation mode]

Track ownership decision still pending (see §Escalations).

### backtest-perf / harness / orchestrator-automation [IN_PROGRESS — no work this run]

No dispatchable items. Recurring [info] items (qc-structural H3 false-positive on advisory linter text; review-file persistence gap) carried but not exercised this run.

## QC Status

| Track | PR | Structural | Behavioral | Quality | Audit |
|-------|----|------------|------------|---------|-------|
| (none this run — no PRs awaiting QC; no dispatches) | | | | | |

## Budget

- Budget cap: $50.00 (from `dev/config/merge-policy.json`)
- Target utilization: 60%–80% ($30 — $40)
- Subagents dispatched this run: **0**
- Per-subagent breakdown:
  | Agent | Model | Status | Est. tokens | Est. cost |
  |-------|-------|--------|-------------|-----------|
  | (none — no subagent dispatches) | — | — | — | — |
- Any subagent killed mid-flight: NA.

**Measured cost (from GHA execution file):** Will land in `dev/budget/2026-05-03-<run_id>.json` post-GHA-capture step.

- Total (estimated): ~$1 of $50 cap (~2%) — orchestrator overhead only (Read/Edit/Bash/curl + targeted `dune build && dune runtest --force` probes).
- Utilization assessment: **UNDER_TARGET** (<60%) — same shape as 2026-05-02 run-1 (actionable backlog drained / scope-extension blocker on new tracks). Distinct from 2026-05-02 run-2 (main red — dispatch unsafe). No eligible track was skipped for cost or queue-depth reasons.
- Scope reduced due to budget: No.

## Data Operations

(ops-data did not run — `dev/notes/data-gaps.md` substantively unchanged since 2026-04-14, EODHD_API_KEY unset in GHA env.)

## Harness Work

(harness-maintainer did not run — backlog drained; no dispatchable T1/T3 items.)

## Health Scan

From `dev/health/2026-05-03-fast.md` (deterministic per Step 6):

- `dune build` exit **0** (PASSING) on HEAD `825bc0e`.
- `dune runtest --force` exit **0** (PASSING) on HEAD `825bc0e`. Yesterday's run-2 RED gate (file_length 7 / fn_length 4 / nesting 73 / magic_numbers many) is **RESOLVED** — the 11 PRs that landed since (#785..#794) included refactors and additions that net out clean against all wired linters.
- `status_file_integrity.sh` (standalone): 8 advisory FAILs on the four new tracks (`data-foundations.md`, `experiments.md`, `tuning.md`, `weekly-snapshot.md` — `## Status` values include free-form trailing clauses like "IN_PROGRESS — M5.3 streaming Phase A...", `## Interface stable` reads `NO — track is brand-new.`). Script exits 0; not a gate contributor. Same 8 FAILs as run-2 yesterday — pre-decision, no schema migration this run.
- Result: **CLEAN** (build/runtest green; only advisory integrity warnings carried).

## Follow-up Queue

Top-level — total open follow-up items across all tracks: ~5 (carried).

- **sp500 short-side validation rerun** (post-G14+G15 baseline): with G14/G15 + portfolio_risk caps all landed and post-#789 sp500-2019-2023 baseline ranges repinned, the post-fix metrics should be re-captured. Local-only (491-symbol universe).
- **covid-recovery 20.81% WR investigation** (carried; deferred until short-side baseline lands).
- **qc-structural recurring H3 false-positive on advisory linter text**: carried, not exercised this run (no QC).
- **qc-structural review-file persistence gap**: carried, not exercised this run.
- **Schema drift from maintainer-driven new tracks** (carried 3+ runs): the four new track files use `## Status\n<enum> — <free-form clause>` (linter currently rejects free-form trailing clauses) and `## Interface stable\nNO — track is brand-new.` (not in `YES|NO|N/A`). Still 8 advisory FAILs; pre-decision; harness backlog candidate.
- **magic_numbers linter false-positives on docstring text** (carried 2 runs): year / PR-number citations like `2019`, `2026`, `744`, `103.58` appearing inside `(* ... *)` comments are flagged as bare numerics. Linter-side fix candidate; harness backlog candidate.

## Integration Queue

(none — no PRs open across the repo at run start; this run's `ops/daily-2026-05-03` branch becomes the only open PR after Step 8.)

## Current Milestone Target

After today's data-foundations cycle:

- **M5.1 foundation hardening** — DONE.
- **M5.2 experiment infrastructure** — partial. M5.2a/b/c/e MERGED; **M5.2d pending** (~250 LOC; sister to M5.2c).
- **M5.3 daily-snapshot streaming** — Phases A / A.1 / B / B-perf / C / D / E / F.1 all MERGED; **F.2 runner default flip** + **F.3 Bar_panels.t retirement** pending.
- **M5.4 mechanical experiments** — partial. E1 / E2 MERGED; **E3 + E4 pending**.
- **M6.1–M6.5 weekly-snapshot verification** — DONE on content.
- **M6.6 live cycle** — DEFERRED.
- **M7.0 Track 1 Norgate** — pending (vendor signup blocker).
- **M7.0 Track 2 multi-market** — DONE (#772).
- **M7.0 Track 3 Synth-v1 + v2** — DONE; **Synth-v3 pending**.
- **M5.5 / M7.1 tuning** — owner-blocked + Norgate-blocked.

The shape of "what's next" still depends on the feat-agent ownership decision for `experiments` / `tuning` / `data-foundations` — the maintainer extended only feat-weinstein in #778 (which addresses weekly-snapshot + future weinstein/* surfaces); backtest-side scope (`experiments` / `tuning` / `data-foundations`) remains pre-decision.

## Dependency Unlocks

- **`hybrid-tier` Option 2 substantively shipped via `data-foundations` M5.3.** Option 2 (daily-snapshot streaming) was hybrid-tier's named P1 future-work item. The M5.3 sequence (Phases A through F.1) on the sister track has now landed the writer, runtime, simulator wire-in, validation, and deprecation marker on `Bar_panels.t`. The remaining items (Phase F.2 runner default flip, F.3 `Bar_panels.t` retirement) close hybrid-tier when they land. No code change in `hybrid-tier` itself.

## Escalations

[info] **Track ownership decision still pending for `experiments`, `tuning`, and `data-foundations`** (3rd consecutive run). PR #778 explicitly extended **feat-weinstein** scope to the entire `trading/trading/weinstein/` subtree — this resolves weekly-snapshot's ownership question and any future weinstein/* dispatch. **feat-backtest** scope vs. the new `experiments`, `tuning`, and `data-foundations` tracks is still pre-decision; per `dev/decisions.md` 2026-04-16 §"Reopen feat-weinstein for support-floor-stops" precedent, scope-extension is a documented decision item. Surfacing as `[info]` because (a) the maintainer is actively driving merges on these tracks at ~11 PRs/day, and (b) dispatching feat-backtest now risks colliding with active work. Recommendation: a one-sentence direction-change in `dev/decisions.md` either extending feat-backtest's documented scope to cover `experiments` + `tuning` + `data-foundations`, OR carving out a new `feat-data-foundations` agent for the snapshot/synthetic/Norgate work, would unblock orchestrator-side dispatch on M5.2d / M5.3 Phase F.2 / Synth-v3 / M5.4 E3+E4 / T-A grid_search. Until that decision lands, the orchestrator's value-add on these tracks is observation-only.

[info] **Per-track status files for the new tracks remain stale against today's merges**. `experiments.md` claims M5.2a/b/c/d still PLANNED + M5.4 blocked on M5.1 (all untrue). `data-foundations.md` `## Status` line claims Phase F.1 "ready for review (this branch)" — F.1 merged as #793 today. `weekly-snapshot.md` claims `## Status PLANNED` (M6.1–M6.5 all merged; only M6.6 deferred). `simulation.md` body still references the resolved P0. Refresh would be ~50 LOC across five files — deferred to maintainer or the next dispatched run on these tracks. `_index.md` reconciliation done in this run's PR (Step 5.5; data-foundations row updated to enumerate all 11 today-merged PRs by phase).

[info] **Schema drift from maintainer-driven new tracks** (carried). 8 advisory FAILs from `status_file_integrity.sh` standalone (exit 0; not a gate contributor). Pre-decision: grow linter enum to include `PLANNED` + accept free-form trailing clause on `Interface stable`, OR edit the four files to use `PENDING` + `N/A`. Same 8 FAILs as run-2 yesterday.

[info] **qc-structural recurring H3 false-positive on advisory linter text** (carried 8+ consecutive runs). Not exercised this run (no QC).

[info] **qc-structural review-file persistence gap** (carried 8+ consecutive runs). Not exercised this run.

[info] **magic_numbers linter false-positives on docstring text** (carried 2 runs). Year / PR-number citations like `2019`, `2026`, `744`, `103.58` appearing inside `(* ... *)` comments on `weinstein_strategy.ml` / `stops_runner.ml` / `portfolio_risk.ml` / `result_writer.ml` are flagged as bare numerics. Real magic-number findings are present too. Linter-side fix candidate: tighten `linter_magic_numbers.sh` comment-stripping. Harness backlog candidate.

[info] **Cumulative day spend low (~1-2% of $50 cap)** in observation mode. Same shape as 2026-05-02 run-1 (actionable-work-floor / scope-extension blocker). Not actionable by the orchestrator without the pending ownership decision.

## Step 1b drift cross-reference

Most recent prior summary on local FS / on main: `dev/daily/2026-05-02-run2.md` (committed via PR #784 at 2026-05-02T13:08Z; merged to main).

Run-2's `## Pending work` table parsed:
- All rows were classified as `blocked-on-main-red`, `blocked`, `merged-on-content`, or `skipped` — no `dispatched`/`awaiting-merge` rows to verify.

Status file commits on main since prior summary commit (PREV_ISO 2026-05-02T13:08Z, excluding `ops:` summary commits): 5 non-summary commits touched `dev/status/`. Status file changes:

- `dev/status/data-foundations.md` — touched by #786, #790, #791, #793, #794.
- `dev/status/_index.md` — touched by #794.

No `[drift]` warnings emitted (no track had a "dispatched" row in run-2 that would need verification).

## Step 1c carried-forward critical verification

Run-2 carried one `[critical]` into today: **Main `dune runtest --force` is RED on HEAD `cabb6b6`** (file_length 7 / fn_length 4 / nesting 73 / magic_numbers many). Re-verified live this session:

```
$ dev/lib/run-in-env.sh dune build
exit=0
$ dev/lib/run-in-env.sh dune runtest --force
exit=0
```

Verification on HEAD `825bc0e`. **Yesterday's [critical] is RESOLVED** — do not carry forward. The 11 PRs that landed since run-2 (#785..#794) collectively addressed the linter regressions through a mix of refactors (e.g., #786 OHLCV columns adds modest schema scope; #790/#791/#792 add new modules but at sizes within limits; #793 is doc-only; #789 is goldens repin; #794 is docs) and the maintainer's normal merge cadence — net delta on the four wired linters is now clean.

[info] Carried-forward verification: yesterday's [critical] resolved between runs (main green; exit 0 on HEAD `825bc0e`).

All carried-forward `[info]` items from prior runs (G6, G14, G15, G4 long-side extension, qc-structural H3, qc-structural review-file persistence, schema drift) remain unchanged and re-carried into today's §Escalations.

## Per-run links

- [run 1] dev/daily/2026-05-03.md (this file)

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
