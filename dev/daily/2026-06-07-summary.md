# Consolidated Summary — 2026-06-07
Runs included: run-1, run-2, run-3 (3 total)
Generated: 2026-06-07T20:28:31Z

## Pending work (from last run)

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| backtest-perf (rolling-start) | **auto-merged** | feat/rolling-start-eval (deleted post-merge) | #1476 | **PR-2 `rolling_start_eval` exe SHIPPED this run.** Next increment: P3 prototypes (`time_underwater_pct` + antifragility convexity — tail-ratio/skew/worst-vol-decile, hold skeptically) **plus** the true multi-start E2E `run` (data-gated — needs `EODHD_API_KEY`/deep PIT OHLCV → maintainer-local). |
| stage-accuracy | blocked (data-gated / maintainer-local) | — | — | 6th consecutive single-dial DD lever REJECTED (macro-trim #1466). Recurring conclusion = **breadth is the lever**. Forward levers (partial-trim = protected Position-core; breadth test) maintainer-local / data-gated. |
| experiment-platform | blocked (data-gated) | — | — | Lever-#1 (broad-universe) DATA-GATED on `EODHD_API_KEY` (absent in GHA) + deep PIT OHLCV; lever-#2 (`neutral_blocks_longs`) FRAGILE per #1412. |
| sweep-perf / backtest-perf (core-perf) | blocked (core-perf / enablement) | — | — | Snapshot-loader Phase-F windowed/mmap decode in `Daily_panels` (per-cycle O(positions) not O(universe)) needs local large-N validation → maintainer-local. Win-#4 production wiring not declared orchestrator-eligible. |
| spy-only-reference | blocked (maintainer-local) | — | — | Empirical K-sweep / Cell-E carrier comparison on PIT S&P-500 — needs local 510-sym backtests. |
| data-foundations | blocked (maintainer-decision) | — | — | READY_FOR_REVIEW ~3 weeks (track-pacer ESCALATE) — maintainer to decide bars-retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close the track. |
| tuning | blocked (maintainer-enablement) | — | — | M2 (qNEHVI) not yet enabled for GHA dispatch (precedent #1327 — maintainer authors an `enable T*.x for GHA dispatch` commit first). |

## Dispatched across all runs (deduped)
| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| (all feature tracks) | — | skipped | 0 open *feature* PRs (no QC/merge surface); forward work is data-gated (deep PIT OHLCV absent in GHA + no `EODHD_API_KEY`), protected Position-core, core-perf, or awaiting maintainer GHA-enablement (see §Pending work). The prior-run P0 (macro-bearish trim) was built (#1464) and its confirmation grid run + **REJECTED** (#1466) by the maintainer this window; the recurring program conclusion is that **breadth, not single-dial tuning, is the lever** — and the breadth test is the data-gated unblock. |
| harness | — | skipped | No eligible T1/T3 item: only two unchecked T3 remain — **T3-C** (cross-feature context injection — "superseded for basic case") and **T3-H** (commit-level QC — "low priority, adds cost; explore when golden scenarios T1-N are stable"). Neither is a clean dispatch; Tier 1 drained, T2 milestone-gated, T4 human-request-only. `harness.md` unchanged since 2026-05-22. (Independently confirmed by today's track-pacer #1469: harness at steady state.) |
| ops-data | — | skipped | `EODHD_API_KEY` absent in GHA (`${#EODHD_API_KEY}`=0); `dev/notes/data-gaps.md` unchanged since 2026-05-03 (ADL still needs human input per the file's §"What's needed"). |
| code-health | — | skipped | `dev/status/cleanup.md` §Backlog contains only the template-placeholder `[ ]` line (no real finding) + 35 `[x]` completed; deterministic Step 6 surfaced no new `[medium]`/`[high]` findings. |
| backtest-perf (rolling-start PR-2) | feat-backtest | **deferred** | Genuinely eligible (N=0 on the track, merged plan, clear un-implemented increment, owned by feat-backtest, buildable+testable without the data gate). **Deferred — not skipped on a disallowed reason**: the maintainer is hand-authoring this exact thread in real time (part-1 #1472 CI created `14:00Z`, minutes before this run; PR body explicitly frames PR-2 as their split-out follow-up). Dispatching now is high-probability collision with the human's live work — a coordination concern distinct from the disallowed "queue depth / wait for unrelated PR" reasons. Surfaced as #1 next-dispatch pick. (run-2) |
| stage-accuracy / experiment-platform / spy-only-reference / data-foundations / tuning | — | skipped | Data-gated (deep PIT OHLCV absent in GHA + no `EODHD_API_KEY`), protected Position-core, core-perf, or awaiting maintainer GHA-enablement (see §Pending work). Unchanged from run-1. |
| backtest-perf (rolling-start PR-2) | feat-backtest | **completed → merged** | Built `Rolling_start_runner` lib (+`.mli`) + `rolling_start_eval` exe + 7 CI-runnable unit tests (34 in dir). PR #1476. Dispatched because the run-2 live-contention deferral had cleared (maintainer's last 2 merges #1474/#1475 on different threads, >1.5h prior; no maintainer rolling-start branch/PR open) — exactly the run-2 handoff (`if NOT taking PR-2 yourself, the next run dispatches it cleanly`). (run-3) |
| backtest-perf (rolling-start PR-2) | qc-structural | **APPROVED** (q=4) | All hard gates pass; 8-file scope exactly as intended (no `_index.md`, no core modules); P6 test-patterns conformant; A1/A2/A3 clean. Reviewed SHA `4a49095f`. |
| backtest-perf (rolling-start PR-2) | qc-behavioral | **APPROVED** (q=4) | CP1–CP4 all PASS — every `.mli`/PR-body contract pinned by a data-free unit test; data-gated E2E `run` honestly disclosed (harness_gap ONGOING_REVIEW). Weinstein domain block S*/L*/C*/T* = NA (analysis-only tooling). |
| backtest-perf (rolling-start PR-2) | — (Step 6.5) | **auto-merged** | All 3 gates green (CI build-and-test + perf-tier1-smoke SUCCESS; structural + behavioral APPROVED). `mergeable_state: clean`; squash-merged via REST → `20452974`. Merged branch deleted. |
| stage-accuracy / experiment-platform / spy-only-reference / data-foundations / tuning / sweep-perf | — | skipped | Data-gated (deep PIT OHLCV absent + no `EODHD_API_KEY`), protected Position-core, core-perf, or awaiting maintainer GHA-enablement (see §Pending work). Unchanged from run-2. |

## QC across all runs
(no QC entries found)

## Budget (summed across runs)
- Total subagents spawned: 3
- Killed mid-flight: None reported across all runs.

## Escalations (merged)
- [info] **Breadth / broad-universe test is the single highest-value unblock — DATA-GATED >30 days** (escalated by today's track-pacer #1469 §P7, and now the 6th-rejected-dial program conclusion). The strategy program's repeated finding is that breadth, not dial-tuning, is the edge — yet the test that would confirm it cannot run autonomously because `EODHD_API_KEY` is absent in GHA and the deep 2000-2026 PIT OHLCV is not in the container (`data/` is metadata-only ~7 MB). **Adding `EODHD_API_KEY` as a GitHub Actions secret + plumbing it through `.github/workflows/orchestrator.yml` is the top unblock** — it would let the orchestrator run the broad-universe fetch + test autonomously. Alternatively, run the EODHD broad-3000 fetch + 2020-2026 test locally. (carried, now ~13th run.) (seen in: run-1,run-2,run-3)
- [info] **Pause single-dial strategy-mechanism exploration** (track-pacer #1469 §Recommendation 2). Six consecutive levers have landed default-off and been REJECTED/FRAGILE (late-Stage2, macro-trim, early-admission, exit-timing, hysteresis, neutral_blocks_longs). Recurring finding: "breadth is the lever." Maintainer decision: redirect stage-accuracy / experiment-platform effort behind the lever-#1 (breadth) result rather than building a 7th dial (seen in: run-1,run-2,run-3)
- [info] **data-foundations READY_FOR_REVIEW ~3 weeks** (track-pacer #1469 §Recommendation 3) — maintainer to decide bars-retention recovery (synthesize vs accept SCTY/MNK/LB loss) or close the track (seen in: run-1,run-2,run-3)
- [info] **`gh` / `jst` not available in the GHA env** (carried) — orchestrator creates/merges PRs and posts reviews via `curl` REST; this run had no feature PR to act on. Working as designed via REST fallbacks (seen in: run-1,run-2,run-3)
- [info] **Stale ci-watchdog issues #1280 + #1264 + #1320** (carried) — this run's `issues` read returned no open `ci-red`-labelled issues, so these may already be closed or unlabelled. Human triage to confirm/close if still open (seen in: run-1,run-2,run-3)
- [info] **15y split-day adjustment regression on `weinstein_strategy.ml`** (carried) — Investigation #998 root-caused; fix pending; touches core, surfaced as a feat-weinstein scope-decision item (seen in: run-1,run-2,run-3)
- [info] **M2 (qNEHVI) GHA-enablement gate awaiting maintainer commit** (carried, ~17th run) — `tuning.md`: M2 not yet enabled for GHA dispatch (precedent #1327 — maintainer authors an `enable T*.x for GHA dispatch` commit before the orchestrator picks up new M-x items) (seen in: run-1,run-2,run-3)
- [info] **sweep-perf Win-#4 production wiring not declared orchestrator-eligible** (carried) — `panel_runner`/`scenario_runner` opt-in to pass `?active_through_for` (~30-60 LOC feat-backtest). If you want it dispatched, add an `enable sweep-perf Win-#4 for GHA dispatch` signal (status-file note or decisions.md entry) (seen in: run-1,run-2,run-3)
- [info] **stage-accuracy partial-trim variant needs Position-core partial-exit support** (carried) — requires modifying the protected `Position` core module → design/scope-decision for the maintainer. Lower priority now that the trim mechanism was REJECTED on the grid (seen in: run-1,run-2,run-3)
- [info] **shares-outstanding fundamentals source decision** (carried) — vendor decision; dormant (seen in: run-1,run-2,run-3)
- [info] **Winding-down tracks** (track-pacer #1469 §Recommendation 5) — orchestrator-automation (Phase 1 stable / Phase 2 deferred / no work surface) and harness (Tier 1 done, T3 drained) are candidates to flip to a steady-state/closed marker.
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~21st run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled (track-pacer #1469 §Recommendation 6). Not a bare `[critical]` (does not trip the gate-fail).
- [info] **Rolling-start PR-2 is the #1 orchestrator-eligible next-dispatch candidate — deferred run-2 for live maintainer contention.** It is a textbook plan-backed fresh-stack increment (`rolling_start_eval` exe; plan §2 P1; feat-backtest-owned; buildable + unit-testable without the data gate by reusing `Runner.run_backtest` + `Bar_source_resolver.resolve` + the `walk_forward_executor` pattern). It was **not** dispatched because the maintainer is hand-authoring this exact thread in real time (part-1 #1472 CI created `14:00Z`, minutes before this run; PR body frames PR-2 as their split-out follow-up). **Action: if you are NOT taking PR-2 yourself, drop a note in `dev/decisions.md` (or just leave PR-2 un-pushed for ~a day) and the next orchestrator run will dispatch it cleanly.** This is the first genuinely-clean autonomous feature increment in many runs — worth not wasting on a collision.
- [info] **Winding-down tracks** (track-pacer #1469 §Rec 5) — orchestrator-automation (Phase 1 stable / Phase 2 deferred) and harness (Tier 1 done, T3 drained) are candidates to flip to a steady-state/closed marker (seen in: run-2,run-3)
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~22nd run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled (track-pacer #1469 §Rec 6). Not a bare `[critical]` (does not trip the gate-fail).
- [info] **Rolling-start PR-2 (#1476) SHIPPED this run** — the run-2 `#1` next-dispatch candidate. Resolves run-2 Question 1: the orchestrator built it because the live-contention that justified deferral had cleared (maintainer on different threads #1474/#1475 for >1.5h, no rolling-start branch/PR open). Both QC gates APPROVED, auto-merged. No action needed.
- [critical-decision-needed] **M6.6 live cycle decisions** (carried, ~23rd run) — live `DATA_SOURCE` + cron + alert dispatch + trading-state durability; the M6.1–M6.5 verification harness is done; live wiring is the next capability milestone and is currently unowned/unscheduled (track-pacer #1469 §Rec 6). Not a bare `[critical]` (does not trip the gate-fail).

## Integration Queue (last run's view)

- **#1469** `ops: weekly track pacer 2026-06-07` (`ops/track-pacer-2026-06-07`, docs-only) — generated by the weekly track-pacer workflow, not orchestrator-dispatched; QC-exempt (docs-only). Out of the orchestrator's Step 6.5 lane — left for the human / its own automation.
- **No orchestrator feature PR is awaiting merge** — #1476 was auto-merged this run (all 3 gates green). The 2 other PRs that landed since the run-2 summary (#1474, #1475) were maintainer-authored and already merged before this run.

## Per-run links
- run-1 -> `2026-06-07.md`
- run-2 -> `2026-06-07-run2.md`
- run-3 -> `2026-06-07-run3.md`
