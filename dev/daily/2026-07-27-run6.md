# Status — 2026-07-27 [run 6]

Run timestamp: 2026-07-27T19:51Z– (GHA orchestrator run 30300054307)
Run ID: 2026-07-27-run-6

**Mode:** FULL PASS — **adopted the work orphaned by a killed run and merged both of its PRs.
Also found, and confirmed twice, that the standard build wrapper silently verifies `main`
instead of the branch under review.**

## Headline

- **I am run 6, and run 5 died without a summary.** Orchestrator run `30287137763` ran
  16:58Z–19:07Z and was **cancelled at 2h09m** (step "Run lead-orchestrator", conclusion
  `cancelled`; the job's own `timeout-minutes` is 360, so this was not the job timeout). It had
  already opened **#2135** and **#2136**, run four QC passes, and driven one rework — then died
  before re-QC, before merging either, and before writing a summary. No `ops/daily-2026-07-27-run5`
  branch exists and no `dev/health/2026-07-27-fast-run5.md` is on disk. **But it had already
  called itself "run 5" in a file that was about to merge** (#2135's `H-FOLLOWUP-COUNT` entry
  cites "2026-07-27 run 5"), so taking `run5` myself would have put two different runs under one
  name in the permanent record. I took **run 6**. Run 4 predicted the in-flight-PR variant of this
  bug; this is a second, worse variant — **a run can consume a number and leave nothing behind but
  a citation.**
- **Both orphaned PRs are merged.** #2136 `1a91ee94`, #2135 `ed816d4f`. Neither needed new
  feature work — #2136 was fully APPROVED and only needed a branch update; #2135's rework was
  complete and only needed the re-QC its parent run never got to run. **The most valuable thing
  this run did was finish someone else's work rather than start its own.**
- **`dev/lib/run-in-env.sh` silently verifies the wrong tree, and I shipped that instruction to
  three agents before catching it.** In `TRADING_IN_CONTAINER` mode it sets
  `PROJECT_ROOT="${GITHUB_WORKSPACE}/trading"` (line 59) **regardless of cwd**, so an agent
  verifying a branch from a git worktree builds and tests **`main`**. qc-structural found it
  independently; I confirmed it by reading the script; code-health then hit it and corroborated a
  third time. I corrected two in-flight agents mid-session. **Every "green" any worktree agent
  reported through that wrapper this run was measuring main.** This is the same failure class as
  the `| tail` trap already escalated: *a verification that reports success without having run.*
- **The seven-run-old "agents cannot edit `.claude/agents/`" escalation is wrong, and I have the
  precise diagnosis.** #2135 edited `qc-structural.md` and `qc-behavioral.md` with the ordinary
  Edit tool. I then tested `lead-orchestrator.md` directly: the file is **filesystem-writable**
  (`-rw-r--r--`, `[ -w ]` true) but the **Edit tool is permission-denied on that one path**. So it
  is a harness permission rule scoped to a single file — not a filesystem block, not a repo-wide
  block. I did **not** route around it with `sed`/`python`: a permission denial is a statement of
  intent, not an obstacle. Exact patch in §Questions.
- **A backlog item I dispatched turned out to be under-specified, and measuring first changed the
  brief.** Before dispatching `H-FOLLOWUP-COUNT` I counted the items myself and found the filed
  fix ("count only `- [ ]`, match `###` headings") is **necessary but not sufficient** — see
  §Escalations. The brief went out with that correction and my measurements attached.

## Step 1b drift cross-reference

Newest **merged** summary on disk: `dev/daily/2026-07-27-run4.md` (#2130). Per run 4's own
finding I did not stop there — I listed open PRs and in-flight workflow runs to check for a newer
summary living only in a PR branch. There was none: **run 5 never produced one.** I reconstructed
its state from its artefacts instead (its two PRs, their four review bodies, and the run's job
conclusion via the Actions API).

Run 4's `Pending work` rows, re-verified against live GitHub state:

| run-4 row | run-4 state | verified now | classification |
|---|---|---|---|
| harness #2123 | auto-merged | merged `fa23c9df` | confirmed |
| cleanup #2126 | auto-merged | merged `ab53f8a6` | confirmed |
| (run 3) #2124 | auto-merged | merged `1ad2c635` | confirmed |
| weekly-snapshot #2125 | auto-merged | merged `e5726315` | confirmed |
| ops-data | not dispatched | `EODHD_API_KEY` re-verified **absent** | unchanged |
| cleanup backlog "9 open" | — | now **8** open (#2132 closed one) | confirmed, normal lag |

**No `[drift]` warnings.** Seventh clean cross-reference in a row. The only unrecorded state was
run 5's, and that is a numbering/summary gap, not track drift.

## Step 1c carried-`[critical]` verification

Run 4 carried **no `[critical]`**. Re-verified the standing items:

1. **Main baseline** → **green.** `dune build` exit **0**; `dune runtest --force` exit **0** across
   **402 suites**, **0** `^FAIL:` and **0** `^Error:` lines; fmt clean; magic numbers clean;
   `status_file_integrity` exit **0**. Gates run on `6a07f208` before any dispatch.
2. **`[high]` `.claude/agents/` unwritable** → **falsified and replaced with a precise finding**
   (§Headline, §Escalations). Run 4's generalisation was too broad.
3. **`[medium]` run-number rule cannot see an in-flight summary PR** → **still real, and it has a
   second variant** — a *killed* run leaves no PR at all yet still consumes the number by citation.
4. **`[medium]` concurrent orchestrator runs undetected** → **RESOLVED between runs.** A
   `concurrency: {group: daily-orchestrator, cancel-in-progress: false}` stanza is now in
   `.github/workflows/orchestrator.yml` (lines 102–104). I verified no sibling orchestrator run was
   in flight when I started — mine was the only one. Run 4's question 2 is answered; dropping it.
5. **`[high]` auto-merge neither sufficient nor safe** → carried, **no victim, sixth run running**.
   Both PRs had `auto_merge = None`; I merged each explicitly after re-verifying all three gates in
   the same step, which handled two branch-staleness cases.
6. **`[info]` `GH_TOKEN` lacks `issues: write`** → not re-tested this run (no issue write needed);
   carried unchanged rather than asserted.

**No still-real `[critical]` items.** Main green before dispatch and after both merges.

## Pending work

| Track | State | Branch | PR | Next step |
|-------|-------|--------|----|-----------|
| weekly-snapshot | **auto-merged** | fix/report-watch-qc-followup | #2136 | **MERGED `1a91ee94`** — F1–F3 from #2129 closed; the CLI now has a test that executes it. Next: 4c.c write-back |
| harness | **auto-merged** | harness/qc-score-polarity-v2 | #2135 | **MERGED `ed816d4f`** — H-QC-SCALE + H-AUDIT-GH-FALLBACK; guard verified in production post-merge |
| cleanup | **auto-merged** | cleanup/live-portfolio-header-schema-pin | #2138 | **MERGED `f3a80c46`** — header now pinned against the record it documents, derived not hand-listed |
| harness (2nd) | **awaiting-merge** | harness/followup-count-fix | #2140 | **QC NOT YET RUN** — opened at 21:09Z, too late in this run to QC + CI + merge. Top item for the next run |
| ops-data | not dispatched | — | — | `EODHD_API_KEY` absent; `data-gaps.md` unchanged since 2026-05-03 — sentinel unchanged |

## Dispatched this run

| Track | Agent | Outcome | Notes |
|-------|-------|---------|-------|
| harness | qc-structural | **APPROVED** q4 | #2135 delta. Ran 2 mutations, both RED→GREEN. **Found the `run-in-env.sh` worktree defect** while working around it |
| harness | qc-behavioral | **APPROVED** q4 | #2135 delta. Re-ran M1/M1b/M2 itself; confirmed scenario 16 pins *multi-digit capture*, not just the range check |
| harness | — | **merged** | #2135 → `ed816d4f`; 6/6 reviewed files byte-identical across branch update; guard re-verified on merged main |
| weekly-snapshot | — | **merged** | #2136 → `1a91ee94`; QC APPROVED by run 5 at `1819417a`, 7/7 reviewed files byte-identical at merge tip |
| cleanup | code-health | completed | #2138. Found the finding **REAL but only partly addressed** by #2125; derived the field list from `sexp_of_t` rather than hand-listing |
| cleanup | qc-structural | **APPROVED** q4 | #2138. Re-ran both mutations; verified `[@sexp.default]` without `[@sexp_drop_default]` does not elide the field, so no vacuous pass |
| cleanup | qc-behavioral | **APPROVED** q3 | #2138. **Instrumented** the derivation rather than reasoning about it; corrected two of the author's narrative claims |
| cleanup | — | **merged** | #2138 → `f3a80c46`; three gates re-verified in the same step; files byte-identical across branch update |
| harness (2nd) | harness-maintainer | completed | #2140. Abandoned heading-scoping rather than patching it, after independently reproducing my measurement; 3 mutations RED |
| ops-data | — | skipped | `EODHD_API_KEY` re-verified **absent**; `data-gaps.md` unchanged since 2026-05-03 |

## Feature Progress

### harness — #2135, a rework that was already done and just needed reading

Run 5 left this PR in the most wasteful possible state: the finding was correct, the rework was
complete and pushed, and **nobody had looked at it.** The cost of the killed run was not lost work,
it was *unverified* work.

qc-behavioral's original NEEDS_REWORK was a CP4 finding — the range guard's own docstrings named
edge values (`"6"`, `"10"`, `"0"`, "non-numeric garbage") that no test exercised, leaving three
mutations alive. The rework added scenarios 15–18 mapping 1:1 onto the four requested fixtures.

Both reviewers re-ran the mutations rather than reading the diff. qc-behavioral confirmed
**M1→15, M1b→17, M2→16 & 18** all now caught, and settled the question I flagged as the one that
mattered: **scenario 16 pins multi-digit capture, not merely the range check** — it reverted the
extractor to a single-char match while leaving the range check intact and the suite still went red,
then reproduced the underlying corruption directly (a posted `10` silently recorded as
`quality_score: 1`, rc 0). That is the H-QC-SCALE failure mode itself, now pinned.

qc-structural found something subtler on the same scenario: its mutation of the end-anchor made 16
go red **not because the bad value slipped through**, but because it fell through to
`write_audit.sh`'s independent still-anchored check, which fails with a *different* message — and
scenario 16 greps for `record_qc_audit.sh`'s own message text. That is exactly the design intent
the scenario's comment claims, **verified rather than asserted**.

**Verified in production after merge**, on merged `main`: a review file posting `## Quality Score`
= `10` now exits **1** with `parsed quality score '10' is not an integer in 1..5`, and **zero**
audit records are written.

### weekly-snapshot — #2136, approved by a run that did not live to merge it

Both gates APPROVED at `1819417a` (structural q4, behavioral q4) with no findings outstanding. The
tip had moved only by a merge of `main`, so I diffed the reviewed content directly: **all 7
reviewed files byte-identical**, and after the branch update onto current `main` (which brought
only `#2134`/`#2137`) still byte-identical, with the PR's diff-vs-main exactly the 7 expected
files. Re-QC would have bought nothing; I merged on the existing verdicts and said so.

Worth recording because it argues *against* reflexive re-QC: #2136 and the newly-merged
`#2134`/`#2137` both touch `html_report_renderer.ml`, so a semantic conflict was plausible even
without a textual one. The thing that actually de-risked it was **CI on the merged result**, not a
second review.

### cleanup — #2138, a finding that was half-fixed by an unrelated PR

I dispatched with an explicit instruction to re-verify before fixing, because the last two cleanup
dispatches both found their target had moved and #2125 had rewritten this very `header` days after
the finding was filed. The verdict came back **REAL but partially addressed**: #2125 added
`test_header_documents_stop_state`, which hardcodes `contains_substring "stop_state"` — that pins
the one field the reviewer had in mind and does nothing for the class.

The fix derives the field names from `Live_portfolio.sexp_of_t` / `sexp_of_position` at runtime
rather than hand-listing them, so the test cannot itself drift.

**Both reviewers re-ran both mutations independently and both attacked the vacuous-pass risk
directly** — the danger being that a runtime-derived assertion set that comes back short or empty
would still go RED on those mutations for the *wrong* reason. qc-behavioral patched a debug print
into the test rather than reasoning about it and observed the actual derived lists:
`t_fields=[cash,as_of,positions]` (3/3) and
`position_fields=[symbol,shares,entry_price,entry_date,stop_price,stop_state]` (6/6). The decisive
detail: **`stop_state` appears even though the sample's value is `None` and it carries
`[@sexp.default None]`** — confirming that `[@sexp.default]` without a paired
`[@sexp_drop_default]` affects only `t_of_sexp` and never elides a field from `sexp_of`.
qc-structural reached the same conclusion from the attribute definition. Not vacuous today; both
flagged the latent case (a future field with `[@sexp_drop_default]` sitting at its default would
silently vanish from the derived list with no signal).

**qc-behavioral also corrected two of the author's own claims**, which is the part worth keeping:
(1) the reported "collateral" failure of the older `test_header_documents_stop_state` under the
rename mutation **did not reproduce** — the header still contains the literal substring, so that
test stayed green; (2) the brief said the author had left a comment disclosing the un-covered
reverse direction, and **no such comment exists anywhere**. Neither changes the verdict — both
cited tests are real and each goes RED on its claimed mutation — but a PR body that overstates
its own evidence is exactly what review is for.

Both reviewers independently landed on the same non-blocking P6 nit: `assert_that undocumented
(size_is 0)` discards *which* field drifted (`Expected size 0 but got 1`); `elements_are []` gives
identical pass/fail with a failure message that names the offender.

### harness (2nd) — #2140, a fix that rejected its own filed scope

`H-FOLLOWUP-COUNT` was filed with the remedy "count only `- [ ]`, match `###` headings too". I
measured before dispatching and found that insufficient, and put the measurements in the brief.
**The agent reproduced them independently before writing code** — 0 `- [ ]` items under any
Follow-up-shaped heading versus 39 unscoped — and then **abandoned heading-scoping entirely**
rather than widening the heading set, on the grounds that widening only defers the same failure to
the next new heading name (`harness.md`'s open items live under a dated `## Added <date>`,
`cleanup.md`'s under `## Backlog`). Old code: 76 in Warnings, 0 in Trends. New code: **39 in
both**, matching the independent count, with a per-file breakdown so the composition is visible
rather than a bare integer.

Three mutations, each RED then reverted: dropping the `- [ ]` filter, reintroducing heading
scoping, and reverting the sidecar to basename keying — the last reproducing the exact
Warnings-vs-Trends contradiction. The sidecar handoff (the headline defect, since it is what let
one report disagree with itself) is now keyed off a shared directory + fixed filename in both
`check_05` and `check_08`. It also honestly flagged the tradeoff it accepted: the unscoped count
now includes Tier 1–4 roadmap checkboxes, which broadens the metric to "total open checkbox work"
and is a `merge-policy.json` threshold retune decision it deliberately did not make.

**#2140 has had no QC.** It opened at 21:09Z, and a QC pair plus a CI cycle plus a merge would not
have fit in the remaining run budget. It is the top item for the next run.

## QC Status

- harness / **#2135: APPROVED** — structural q4 (review `4791273321`) + behavioral q4 (review
  `4791048567`), both @ `dd029ae8`, carried to merge tip `3f7a5f79` on verified byte-identity.
  Audit `dev/audit/2026-07-27-harness-qc-score-polarity-v2-harness.json`.
- weekly-snapshot / **#2136: APPROVED** — structural q4 (`4790394218`) + behavioral q4
  (`4790477557`), both @ `1819417a` from run 5. Audit
  `dev/audit/2026-07-27-fix-report-watch-qc-followup-weekly-snapshot.json`.
- cleanup / **#2138: APPROVED** — structural q4 (review `4791601897`) + behavioral q3 (review
  `4791543331`), both @ `f49b0205`, carried to merge tip `9cc03aaa` on verified byte-identity.
  Audit `dev/audit/2026-07-27-cleanup-live-portfolio-header-schema-pin-cleanup.json`.
- harness / **#2140: NOT REVIEWED** — opened too late in the run; no gate has run.
- All verdicts delivered as **PR review comments** on `/pulls/N/reviews` with `event: COMMENT`.
- **Declared deviations:** (a) structural and behavioral ran **concurrently** rather than gated
  (Step 5 Stage 2) — fourth run running, and it paid again: behavioral produced the decisive
  mutation evidence on #2135 while structural produced the decisive environment finding.
  (b) Reviewers ran in per-agent `git worktree`s.
- **Two Finish Protocol violations, and this is now a pattern, not an incident.** qc-structural
  (#2135) and qc-behavioral (#2138) each ended a turn with the words "waiting for the background
  build" without posting a review. Both briefs contained the rule *and* an explicit instruction not
  to do it. I resumed each with the rule quoted and the verification re-specified in the foreground;
  both then completed correctly. Combined cost ~45 min of wall-clock on the critical path. Run 4
  saw zero violations after adding the failure mode to every brief and called that one run of
  evidence rather than proof — **correctly, because it did not hold.** See §Escalations: this needs
  a mechanical backstop, not more prompt text.

## Budget

- Budget cap: $200.00 (`merge-policy.json`; Max-20x flat rate — a runaway backstop, not a real budget)
- Target utilization: 60%–95%
- Subagent sessions: **6** (2 resumed after Finish Protocol violations)
- Measured subagent tokens: **932,510**

  | Agent | Role | Outcome | Subagent tokens |
  |---|---|---|---|
  | qc-structural | #2135 delta (incl. resume) | APPROVED q4 | 120,574 |
  | qc-behavioral | #2135 delta | APPROVED q4 | 116,163 |
  | code-health | #2138 live_portfolio header | completed | 188,668 |
  | qc-structural | #2138 | APPROVED q4 | 154,337 |
  | qc-behavioral | #2138 (incl. resume) | APPROVED q3 | 146,474 |
  | harness-maintainer | #2140 H-FOLLOWUP-COUNT | completed | 206,294 |

- Any subagent killed mid-flight: **No.** No `pkill` incident; the prohibition was in every brief.
- Total (estimated, Sonnet pricing per `model_prices`): **~$12–16** / $200 (~6–8%)
- Utilization assessment: **UNDER_TARGET on cost, and correctly so.** The binding constraint this
  run was neither budget nor eligible work — it was **wall-clock on the critical path**, from two
  sources: (1) **CI latency** — three sequential branch-update → full-CI cycles at ~11–20 min each,
  every merge needing one because a sibling landed underneath it; (2) **~45 min lost to the two
  Finish Protocol violations**, which is the larger and the only avoidable one. Extra dispatch
  shortens neither.
- **Fill-loop (Step 3.75c) evaluated and acted on**, not just declined: after the initial batch I
  dispatched a second harness item (`H-FOLLOWUP-COUNT`). One candidate was declined for a
  nameable reason: **H-PREV-VERDICT-PIPEFAIL + H-AUDIT-ATOMIC-WRITE both edit `write_audit.sh`,
  the same file #2135 was modifying in flight** — dispatching them concurrently would have
  guaranteed a conflict. They are the top harness item for the next run, and should go **together**
  since the atomic-write fix removes the pipefail's trigger at source.
- Queue depth was **not** a factor in any skip.
- Scope reduced due to budget: **No.**

## Health Scan

Report: `dev/health/2026-07-27-fast-run6.md`. Gates run on `6a07f208` **before** any dispatch.

- `dune build` **0**; `dune runtest --force` **0** across **402 suites**, 0 `^FAIL:`, 0 `^Error:`;
  fmt clean; magic numbers clean.
- `status_file_integrity` **0** — and this is the first run to record it **actually running**. The
  path documented in Step 6.2 (`sh trading/devtools/checks/...`) fails with `sh: cannot open` and
  true exit **2**; the corrected path (`sh devtools/checks/...`, since `run-in-env.sh` already cd's
  to `<repo>/trading/`) returns `OK: all dev/status/*.md files have required fields` and exit **0**.
  I used the corrected form. Eighth run carrying the doc bug — now with a settled cause (§Escalations).
- CPU/host not recorded this run; the LAPACKE/`Bayesian_opt` class has been fixed at source by
  #2113 (the tuner no longer depends on host `dpotrf`), so a green here no longer tests the CPU
  hypothesis either way and I did not claim otherwise.

## Follow-up Queue

**The counter that is supposed to answer this section is broken, so I counted by hand.**
`dev/health/2026-07-27-deep.md` self-contradicts: line 10 says
`Follow-up accumulation: 76 open items across status files (threshold: 10)` while line 303 of the
same report says `No open followup items in either scan.`

My own measurements on current `main`:

- `- [ ]` items **anywhere** in `dev/status/*.md`: **39** — `harness.md` 15, `cleanup.md` 8,
  `tuning-methods.md` 4, `all-eligible.md` 3, then 9 files with 1–2 each.
- `- [ ]` items **under any `##`/`###` heading matching `Follow-?up`**: **0**.

Neither number is 76. **Step 2b decision:** I did **not** substitute a maintenance pass — the
dispatched work *was* defect closure, and the threshold input is currently untrustworthy in both
directions. Recorded rather than acted on, deliberately.

**#2140 fixes this**, and once merged the counter will report **39** consistently in both sections.
Note the consequence the fix surfaces honestly: 39 counts open Tier 1–4 roadmap checkboxes, so it
measures "total open checkbox work", not "follow-ups". Against a `followup_threshold` of 10 that
will trip the Step 2b maintenance cycle **every run**. **The threshold needs retuning at the same
time the fix lands, or the fixed counter will be as misleading as the broken one — just in the
opposite direction.** Flagged by the agent; recorded here so it is not lost between runs.

## Integration Queue

- **#2136 — MERGED `1a91ee94`.** Three gates re-verified in the same step as the merge call; 7/7
  reviewed files byte-identical at the merge tip.
- **#2135 — MERGED `ed816d4f`.** Same; 6/6 reviewed files byte-identical; CI green
  **non-vacuously** — `trading/devtools/checks/dune` declares `(deps record_qc_audit.sh
  write_audit.sh)`, so changing those scripts forces the test to re-run rather than hit cache.
- **#2138 — MERGED `f3a80c46`.** Three gates re-verified in the same step as the merge call; the
  2 reviewed files byte-identical across the branch update.
- **#2140 — OPEN, unreviewed.** `harness/followup-count-fix`. Opened 21:09Z; no gate has run.
  **Top item for the next run**, and it is self-relevant: it fixes the counter that feeds my own
  Step 2b decision.
- **#2139 — OPEN, not mine.** `feat/picks-validator-v1`, opened by **dayfine at 20:55Z**, i.e.
  *during* this run — first slice of #2122 (mechanical pick-record invariants). I did not dispatch,
  review, or touch it, and I did not merge anything that would race it. Recorded here only so the
  open-PR list in this summary is complete and the next run does not mistake it for orphaned
  agent work: **it is human-authored and human-owned.**

**Three for three on what was mergeable; main green after each.** Post-merge CI on `main`
confirmed `build-and-test`, `perf-tier1-smoke`, `golden-sp500-5y` and `golden-custom-universe` all
**success** on `1a91ee94` and `ed816d4f`; `f3a80c46`'s suite was still running at write time, with
`dune build` exit 0 and `dune runtest devtools/checks/` exit 0 locally on the merged tree, and **0
open `ci-red` watchdog issues**.

## Current Milestone Target

Anchored to `dev/notes/next-session-priorities-2026-07-27.md`:

- **weekly-snapshot 4c.c — the write-back — remains the load-bearing next item.** Unchanged by this
  run: #2136 closed review findings on the *report* path, not the persistence path. Until
  `--update-stops` lands, `advance` is read-only and a stop track can only be created by
  hand-editing `portfolio.sexp`.
- `docs/design/weinstein-trading-system-v2.md` still has **no `Current milestone:` line**, so
  `check_11_linter_expiry.sh` cannot evaluate any milestone-pinned exception. **Ninth run carrying
  this**; it needs a human-chosen value.

## Dependency Unlocks

- **#2135 unblocks trustworthy quality-score data.** Both agent definitions now state the polarity
  (1 = worst, 5 = best) at every point a score is written, and both consumers reject out-of-range
  values loudly instead of silently corrupting them. The `record_qc_audit.sh --pr-number` path also
  now fails loudly when `gh` is missing — **which is this runner's normal state**, so the false-
  APPROVED failure mode was live here, not hypothetical.
- **Nothing new is blocked.** The `workflow`-scoped PAT still gates H-BLAS, H-CI-DISPATCH and
  #1636 — now three items, not four: the concurrency guard landed between runs.

## Escalations

- **[high] `dev/lib/run-in-env.sh` silently verifies `main` instead of the branch, for any agent
  working in a git worktree.** In `TRADING_IN_CONTAINER` mode line 59 sets
  `PROJECT_ROOT="${GITHUB_WORKSPACE}/trading"` — the main checkout — **regardless of cwd**. Contrast
  line 65 (non-GHA branch), which derives the root from the script's own location and therefore
  *does* follow a worktree. Consequence: an agent reviewing or implementing a branch runs
  `dev/lib/run-in-env.sh dune runtest`, gets exit 0, and reports a verified green that **measured
  code it never touched**. Found independently by qc-structural, confirmed by me from the source,
  and hit again by code-health — three times in one run. I corrected two agents mid-flight; **any
  worktree-based green reported before that correction is void.** Same class as the `| tail` trap:
  a check that reports PASS without running. **Fix:** derive `PROJECT_ROOT` from the invoking
  worktree (`git rev-parse --show-toplevel`) with `$GITHUB_WORKSPACE` as fallback, or fail loudly
  when cwd is inside a different worktree. Filed as `H-RUNENV-WORKTREE-BLIND`.

- **[high] The `.claude/agents/` write block is real but was mis-scoped for seven runs, and it is a
  tool-permission rule, not a filesystem one.** Run 4 concluded "no automated actor can edit
  `.claude/agents/`". That is **false**: #2135 edited `qc-structural.md` and `qc-behavioral.md`
  with the ordinary Edit tool and merged. I then tested the specific file: `lead-orchestrator.md`
  is **filesystem-writable** (`-rw-r--r--`, `[ -w ]` true) but the **Edit tool is permission-denied
  on that path**. So the blocker is a harness permission rule scoped to one file. **I did not route
  around it via `sed`/`python`** — a permission denial is a statement of intent. The Step 6.2 path
  bug therefore survives an eighth run. Patch in §Questions; granting Edit on that one path also
  fixes the class.

- **[medium] A killed run consumes a run number and leaves nothing behind but a citation.** Run 4
  raised the in-flight-PR variant (summary exists only in an unmerged PR). This is worse: run
  `30287137763` was cancelled before writing *any* summary, so no file and no branch exists — yet
  it had already written "2026-07-27 run 5" into `dev/status/harness.md`, which then merged. Step
  7's on-disk count would have said "run 5" and produced two different runs with one name in the
  permanent record. I took run 6. **Fix:** derive the run number from
  `max(on-disk count, highest run-N among open ops/daily-* PR branches, highest run-N cited by a
  completed Actions run of this workflow today) + 1`.

- **[medium] Nothing reconciles the artefacts of a killed run.** Run 5's death left two open PRs
  carrying four completed QC reviews, one finished rework, and zero summary — invisible to anything
  except a human noticing two stale PRs. I only found them by listing open PRs and reading their
  review bodies. Recovering them was the single highest-value thing this run did, and it happened
  **by accident of Step 1.5**, not by design. **Fix:** make Step 1b explicitly check for a prior
  workflow run whose conclusion is `failure`/`cancelled` with no corresponding summary, and adopt
  its artefacts as first-class inherited work.

- **[medium] `H-FOLLOWUP-COUNT` as filed is necessary but not sufficient, and I measured this before
  dispatching.** The filed fix is "count only `- [ ]`, match `###` headings too". But **open work in
  this repo does not live under Follow-up headings at all**: `harness.md`'s open `H-*` items sit
  under `## Added 2026-07-27 (orchestrator run 2, …)` (line 424) — `## Completed` (line 257) closed
  the `## Follow-up / Known Improvements` section (line 154) long before them — and `cleanup.md`'s
  open items sit under `## Backlog` (line 16). So a heading-scoped counter **with the filed fix
  applied would still report 0** while 23 genuinely open items sit in those two files. The
  dispatched brief carries this correction and my per-file counts.

- **[medium] Finish Protocol violations recurred twice despite explicit briefs — prompt text is not
  working.** qc-structural (#2135) and qc-behavioral (#2138) each backgrounded verification and
  ended a turn "waiting for the background build", costing ~45 min combined on the critical path.
  Both briefs quoted the rule *and* named the failure mode. Run 4 saw zero violations after adding
  the same content and treated it as promising while explicitly cautioning it was one run of
  evidence; **it did not hold at 2-of-5 sessions.** The recovery is cheap and reliable (resume with
  the rule quoted and the verification re-specified in the foreground — worked both times), so the
  cost is pure latency. **Fix directions worth considering, in preference order:** (a) have the
  dispatcher detect the signature — an agent returning with no PR review posted and text matching
  "waiting for"/"background" — and auto-resume, turning 25 min into ~1 min; (b) instruct agents to
  post the review *before* the final full-suite run and amend if it fails, since the review body is
  the durable artefact; (c) accept it as a known tax and budget for it. This is the third distinct
  instance this run of the same meta-problem: **a verification that is believed to have happened but
  did not** (see also the two `[high]` items above).

- **[info] The docs-only merge carve-out still does not name `dev/audit/` or `dev/budget/`.**
  Unchanged from run 4. Every orchestrator summary PR carries `dev/audit/*.json`, which strictly
  falls outside the `.claude/rules/pr-merge-gates.md` allowlist. One line to fix.

- **[info] `run-in-env.sh` also emits a spurious `No dune-project file has been found in directory
  "."` warning** on every invocation. Harmless, but it briefly read as a path-resolution failure
  while I was diagnosing the far more serious defect above. A `dune-project` at the workspace root,
  or a note in the script header, would remove the noise.

## Questions for You

1. **Please either apply this patch to `.claude/agents/lead-orchestrator.md` Step 6.2, or grant the
   Edit tool write access to that one path.** I confirmed this run that the file is
   filesystem-writable and only the tool permission blocks it, and I declined to bypass that with a
   shell write. Measured both forms directly this run:

   ```diff
   -dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
   +# run-in-env.sh already cd's to the dune workspace root (<repo>/trading/), so the
   +# path is relative to THAT: devtools/checks/... not trading/devtools/checks/...
   +dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
    INTEGRITY_EXIT=$?
   ```
   Documented form → `sh: cannot open …`, true exit **2**. Corrected form → `OK`, exit **0**.
   Please also add the two traps that have now each cost real time: *never pipe a gate command into
   `tail`/`head` before reading `$?`*, and *never use `run-in-env.sh` to verify a git worktree — it
   tests `main`* (§Escalations). Lines `:1361`/`:1415` also still document the pre-#2123 audit
   filename.

2. **May I stop carrying the auto-merge `[high]`?** Sixth consecutive run with no victim. Both PRs
   had `auto_merge = None` and I merged each explicitly after re-verifying all three gates in the
   same step, which again handled branch staleness cleanly. Run 4 asked the same question. Absent
   an objection I will drop it next run and treat explicit-merge-after-same-step-verification as
   settled policy.

3. **The `workflow`-scoped PAT now gates three items** — H-BLAS, H-CI-DISPATCH, #1636. One went
   away on its own: the orchestrator concurrency guard run 4 asked for is now in the workflow.
   H-BLAS remains the sharp one: a distro OpenBLAS that returns **silently wrong** linear algebra
   on a subset of runners, against a two-line env fix plus a numerical smoke check.

4. **`followup_threshold` needs a new value in the same change that merges #2140.** The fixed
   counter reports 39 where the broken one reported 76-or-0; against the current threshold of 10 it
   will trip the Step 2b maintenance cycle on **every** run, because the honest count now includes
   Tier 1–4 roadmap checkboxes. I did not pick a number — the right value depends on whether you
   want Step 2b to track "follow-ups" or "all open checkbox work", which is your call, not mine.
   Flagging it now so #2140 does not land and immediately start mis-steering dispatch.

5. **Would you like me to auto-resume agents that park on a background build?** Two of five
   sessions did it this run despite explicit briefs, costing ~45 min. The signature is
   machine-detectable (agent returns, no review posted, return text matches "waiting for"/
   "background") and the recovery is one message that worked both times. I did not build it
   unilaterally because it changes dispatch behaviour rather than just my own bookkeeping.

## Per-run links

- [run 1] dev/daily/2026-07-27.md
- [run 2] dev/daily/2026-07-27-run2.md
- [run 3] dev/daily/2026-07-27-run3.md
- [run 4] dev/daily/2026-07-27-run4.md
- [run 5] *(killed at 2h09m — no summary written; artefacts #2135/#2136 adopted by run 6)*
- [run 6] dev/daily/2026-07-27-run6.md

---
## Your Response
(Edit this section. Run dev/run.sh after editing to start the next session.)
