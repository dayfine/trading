Reviewed SHA: a5c661285befd3344cdf6411266cdeb585876132

# QC Review — PR #2118 `chore: weekly opam dependency update`

Branch: `deps/opam-weekly` · reviewed at `a5c66128` · merged as `950462e0`
Orchestrator run 30262098532 (2026-07-27 run 3).

Third instance of this recurring chore PR; predecessors are
`dev/reviews/deps-opam-weekly-1313.md` and `dev/reviews/deps-opam-weekly-1866.md`.

## Diff

Single file, `trading/deps-snapshot.txt`, +4/-4:

- `dune` / `dune-build-info` / `dune-configurator` 3.24.0 → 3.24.1
- `ppx_deriving` 6.1.2 → 6.1.3

Generated by `.github/workflows/deps-update.yml`
(`opam list ... | sort > trading/deps-snapshot.txt`).

## Structural QC — APPROVED (quality 5)

File list derived from the REST API (`/pulls/2118/files`) per the A3 provenance requirement,
**not** from a git-log ancestry walk: exactly `trading/deps-snapshot.txt`.

- H1 `dune build @fmt` PASS (exit 0) · H2/H3 PASS (CI: `build-and-test` +
  `perf-tier1-smoke` both COMPLETED SUCCESS)
- P1–P6 NA — no source, tests, or docstrings in the diff
- A1–A3 PASS — no core-module modification, no `analysis/`→`trading/trading/` import,
  no unrelated file

## Behavioral QC — APPROVED (quality 5)

Domain checklist (S*/L*/C*/T*) NA in full per `.claude/rules/qc-behavioral-authority.md`
§"When to skip this file entirely" — infrastructure PR, no domain logic. CP1–CP4 vacuous
(no new `.mli`, no new claims). The substantive review was the behavioural-risk question,
and it produced three findings worth keeping:

**1. `deps-snapshot.txt` has no consumer — it is a drift tripwire, not a lockfile.**
Three references repo-wide, all inside `deps-update.yml`, all self-referential (writes it,
diffs it against itself, mentions it in prose). Zero readers in any dune file, script, OCaml
source, or other workflow; installs are driven by `.devcontainer/Dockerfile`. **Merging it
therefore cannot change any build.** Both prior reviews called this file a "lock-file", which
overstates its authority — and this distinction is exactly what collapses the behavioural risk
to zero. This corrects the record for the two earlier reviews.

**2. `ppx_deriving` 6.1.2 → 6.1.3 is a code generator, and the coverage is asymmetric.**
- `eq`: a semantic break **would** be caught by design — 72 direct `equal_*` call sites, several
  pinning both polarities. But note the coverage does *not* come from the matcher layer:
  `Matchers.equal_to` defaults to `?(cmp = Poly.equal)` (`base/matchers/lib/matchers.ml:38`)
  and only **2** sites repo-wide pass `~cmp:equal_*`, so most of the suite bypasses derived `eq`.
- `show`: would be caught, but **by a single incidental tripwire** —
  `base/status/test/status_test.ml:29-30` exact-string-asserts `"Status.Ok"`, which happens to
  pin ppx_deriving's most format-sensitive property (module-qualified constructor rendering).
  Nearly all other `show` usage in tests is non-pinning (`String.length > 0` smoke checks, bare
  liveness calls, `~printer:` args that only evaluate on failure).
- Blast radius had it changed: **nil**. Every non-test `show_*` consumer is human-readable text;
  no golden embeds `show` output (goldens are sexp via the *unchanged* `ppx_sexp_conv`), and
  `snapshot_display.ml` deliberately hand-writes its labels — pre-existing insulation.

**3. Snapshot consistency verified empirically.** `opam list --installed` reproduces every
snapshot entry at the exact recorded version, zero mismatches (the diff is one-directional:
only dev tooling absent from the leaner CI image). No declared constraint is violated
(`ppx_deriving` unbound; `dune {>= "3.17"}` vs 3.24.1; max `(lang dune 3.17)`). Both packages
are installed unpinned, so the drift is the workflow working as designed.

## Findings

None blocking.

One non-blocking observation, `harness_gap: ONGOING_REVIEW`: the `show`-format tripwire is
**luck, not design**. If derived `show` output ever reaches a persisted artifact (a golden, a
report, a serialized record), that single incidental assertion becomes load-bearing and should
be made deliberate.

## Reviewer-declared limitation

The behavioral reviewer's local full rebuild did not run to completion (cold build, zero errors
emitted when stopped) and it said so in the review body rather than implying a clean local pass.
The verdict does not rest on it: CI at this SHA compiled and ran the whole suite **inside the
image that actually contains** dune 3.24.1 / ppx_deriving 6.1.3, with both required workflows
COMPLETED SUCCESS — which is stronger evidence for this particular PR than any local run.

## Verdict

**APPROVED** on both gates. Merged as `950462e0` after a branch update (base had advanced past
#2113) and a full three-gate re-verification on the updated tip `da44e0ad`.
