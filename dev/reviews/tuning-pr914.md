Reviewed SHA: 826e90f8abee4d2fbb5020baa2cafdc23afcc857

## Structural Checklist

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | dune build @fmt (format check) | PASS | No format violations in new code or changes |
| H2 | dune build | PASS | Clean build with new tuner/bin Bayesian opt modules and binary |
| H3 | dune runtest | PASS | 11/11 new tests pass in tuner/bin/test/; full suite exits 0 (pre-existing linter failures in unrelated modules on main are unchanged) |
| P1 | Functions ≤ 50 lines — covered by language-specific linter | PASS | All functions in new modules are ≤ 50 lines: bayesian_runner.ml (_parse_args 19, _load_scenarios 5, _main 26 lines); bayesian_runner_spec.ml (load 4, to_grid_objective 5, to_acquisition 2, to_bo_config 8 lines); bayesian_runner_evaluator.ml (_sector_map_of_scenario 2, _run_one 9, _lookup_scenario 7, _mean 2, build 8 lines); bayesian_runner_runner.ml (_suggest 3, _run_loop 10, _format_metric_value 1, _quote_csv_field 5, _csv_line 1, _csv_header 5, _csv_row 6, _write_bo_log 11, _write_best_sexp 5, _convergence_title 6, _write_convergence_rows 7, _write_convergence_md 3, _result_of 8, run_and_write 12 lines). Test helpers also all ≤ 50: _with_temp_dir, _write_spec_file, all 11 test functions |
| P2 | No magic numbers — covered by language-specific linter | PASS | No bare numeric literals in new lib/ code; tests use assertion-driven values (bounds like 0.0–10.0, totals like 20–30 iterations, seeds like 7 and 23) which are domain-meaningful |
| P3 | All configurable thresholds/periods/weights in config record | PASS | No tunable parameters hardcoded in binary; all BO config comes from spec file (sexp-driven): bounds, acquisition (Expected_improvement or Upper_confidence_bound), initial_random, total_budget, seed, n_acquisition_candidates, objective |
| P4 | Public-symbol export hygiene — covered by language-specific linter | PASS | All three .mli files present with complete documentation: bayesian_runner_spec.mli (types objective_spec/acquisition_spec/t, vals load/to_grid_objective/to_acquisition/to_bo_config with docstrings); bayesian_runner_evaluator.mli (type t and scenario, val build with full sexp example and guard documentation); bayesian_runner_runner.mli (type result/evaluator, val run_and_write with behavior pins) |
| P5 | Internal helpers prefixed per project convention | PASS | All internal helpers properly prefixed with underscore: _usage_msg, _parse_args, _load_scenarios, _main, _sector_map_of_scenario, _run_one, _lookup_scenario, _mean, _suggest, _run_loop, _all_metric_types, _format_metric_value, _quote_csv_field, _csv_line, _csv_header, _csv_row, _write_bo_log, _write_best_sexp, _convergence_title, _write_convergence_rows, _write_convergence_md, _result_of. Test helpers: _with_temp_dir, _spec_text, _write_spec_file, _ucb_spec_text, _parabola_evaluator, _parabola_spec |
| P6 | Tests conform to `.claude/rules/test-patterns.md` (one assert_that per value, matcher composition) | PASS | test_bayesian_runner_bin.ml: 11 tests all use `open Matchers` and `assert_that` with proper matcher composition (size_is, equal_to, elements_are, float_equal, matching, is_some_and, is_none, lt). Three greppable sub-rule violations checked: (1) no `List.exists.*equal_to (true\|false)` patterns found; (2) no `let _ = .*on_market_close\b` or `let _ = .*\.run\b` patterns (all results asserted, e.g., `assert_that result.observations (size_is 20)`); (3) no bare `match .* with` followed by `\| Error .* -> assert_failure` — match expressions in test use proper matchers (e.g., lines 127–132 use matching combinator, lines 275–281 use try/catch with bool assertion). Test data builders inline; stub evaluator defined as _parabola_evaluator. File conforms to test-patterns.md |
| A1 | Core module modifications (Portfolio/Orders/Position/Strategy/Engine) — FLAG if any found | PASS | Zero modifications to trading/trading/portfolio/, trading/trading/orders/, trading/trading/position/, trading/trading/strategy/, trading/trading/engine/. Also zero modifications to tuner/lib/ (bayesian_opt.{ml,mli} remain stable per PR design split). All changes confined to tuner/bin/ (new modules) and tuner/bin/test/ (new tests) |
| A2 | No new `analysis/` imports into `trading/trading/` outside established backtest exception surface | PASS | tuner/bin/dune declares dependencies: core, core_unix.filename_unix, tuner (trading/backtest/tuner/lib), backtest, scenario_lib, tuner_bin. All libraries are from trading/trading/* or core; no imports from analysis/. Reverse direction (trading/trading/ → analysis/weinstein/) is not present and is always fine |
| A3 | No unnecessary modifications to existing (non-feature) modules using PR file list | PASS | PR files from `gh pr view 914 --json files`: dev/status/tuning.md (status update), trading/trading/backtest/tuner/bin/{bayesian_runner.ml, bayesian_runner_spec.{ml,mli}, bayesian_runner_evaluator.{ml,mli}, bayesian_runner_runner.{ml,mli}, dune}, trading/trading/backtest/tuner/bin/test/{dune, test_bayesian_runner_bin.ml}. Matches `git diff main..HEAD --name-only` output exactly. Only new files in tuner/bin/{,test/} and one status update. No cross-feature drift or unrelated module modifications |

## Verdict

APPROVED

---

## Summary

PR #914 wires the stable `Tuner.Bayesian_opt` lib to a CLI binary (`bayesian_runner.exe`) that mirrors the T-A grid_search CLI (PR #893) structure. All 11 new tests pass. Build gates (H1–H3) all PASS. Architecture checks (P1–P5, A1–A3) all PASS. The PR strictly mirrors the T-A template: spec module for sexp parsing, evaluator module for backtest orchestration, runner module for BO loop + output writing, and thin binary entry point. Library tuner/lib/ remains untouched (as intended). The module structure and test coverage follow the established test-patterns.md conventions. No new cross-module dependencies or core-module modifications.

---

# Behavioral QC — tuning (T-B Bayesian-opt CLI binary)
Date: 2026-05-07
Reviewer: qc-behavioral

## Contract Pinning Checklist

| # | Check | Status | Notes |
|---|-------|--------|-------|
| CP1 | Each non-trivial claim in new .mli docstrings has an identified test that pins it | PASS | (a) `Bayesian_runner_spec.load` parses simple Expected_improvement spec → `test_load_simple_spec_parses` (lines 75–89: bounds elements_are, scenarios size_is, initial_random/total_budget/seed/n_acquisition_candidates equal_to). (b) `Spec.load` parses UCB + Composite + n_acquisition_candidates=Some + seed=None → `test_load_ucb_acquisition_and_composite_objective_parses` (lines 106–122). (c) `Spec.load` raises `Failure` on malformed input (line 77 of .mli) → `test_load_malformed_raises` (asserts `String.is_substring msg ~substring:"failed to parse"`). (d) `Spec.to_grid_objective` for all 4 simple variants (lines 79–82 of .mli) → `test_to_grid_objective_simple_variants`; Composite variant pinned by `test_load_ucb_acquisition_and_composite_objective_parses`. (e) `Spec.to_acquisition` round-trip (lines 84–86 of .mli) → `test_to_acquisition_round_trips` (both variants). (f) `Spec.to_bo_config` propagates fields (lines 88–93 of .mli) → `test_to_bo_config_propagates_fields` (bounds + acquisition + initial_random + total_budget all asserted). (g) `Bayesian_runner_evaluator.build` raises `Failure` on cache miss (lines 41–42 of .mli) → `test_evaluator_unknown_scenario_raises`. (h) `Bayesian_runner_runner.run_and_write` emits three artefacts + observations.length = total_budget (lines 18–20 + 39–55 of .mli) → `test_run_and_write_emits_three_artefacts`. (i) `mkdir -p` semantics (lines 55–56 of .mli) → `test_run_and_write_creates_missing_out_dir` (deep/nested/out path). (j) BO loop drives convergence on parabola → `test_run_and_write_converges_on_parabola`. The full backtest path of `Evaluator.build` (real `Backtest.Runner.run_backtest` invocation) is intentionally not unit-tested per the test file's docstring lines 4–7 ("the lib's tests already pin algorithmically") — same scope decision as PR #893. |
| CP2 | Each claim in PR body "Test plan"/"Test coverage" sections has a corresponding test in the committed test file | PASS | PR body advertises 11 tests; 11 tests are present and named consistently with the PR body bullets: (1) "simple Expected_improvement spec parses" → `test_load_simple_spec_parses`. (2) "UCB acquisition + Composite objective + n_acquisition_candidates=Some 500 + seed=None" → `test_load_ucb_acquisition_and_composite_objective_parses` (all four sub-claims asserted: line 111 `Upper_confidence_bound 2.5`, line 113 Composite variant, line 121 `is_some_and (equal_to 500)`, line 122 `is_none`). (3) "malformed sexp raises Failure" → `test_load_malformed_raises`. (4) "to_grid_objective: all 4 simple variants round-trip" → `test_to_grid_objective_simple_variants`. (5) "to_acquisition: Expected_improvement and UCB round-trip" → `test_to_acquisition_round_trips`. (6) "to_bo_config: bounds/acquisition/initial_random/total_budget propagate" → `test_to_bo_config_propagates_fields` (all four fields asserted). (7) "emits bo_log.csv + best.sexp + convergence.md with correct row count" → `test_run_and_write_emits_three_artefacts` (line 219 asserts size_is 21 = 1 header + 20 × 1 scenario). (8) "BO loop converges on parabola within 0.5 of x=3.0 with budget=20" → `test_run_and_write_converges_on_parabola` (assertion at line 233 uses `lt (module Float_ord) 0.5`). (9) "mkdir -p creates nested missing out-dir" → `test_run_and_write_creates_missing_out_dir`. (10) "same seed → byte-identical bo_log.csv" → `test_determinism_same_seed_byte_identical_log` (asserts log1 `equal_to` log2 on full file bytes). (11) "unknown scenario path raises Failure" → `test_evaluator_unknown_scenario_raises`. Eleven claimed, eleven present, all pinned. |
| CP3 | Pass-through / identity / invariant tests pin identity (elements_are [equal_to ...] or equal_to on entire value), not just size_is | PASS | `to_bo_config` propagates spec fields → `test_to_bo_config_propagates_fields` uses `elements_are [equal_to ("x", (0.0, 6.0))]` on bounds (whole-element equality), `equal_to` on acquisition + initial_random + total_budget (whole-value equality). `to_grid_objective` round-trip → `test_to_grid_objective_simple_variants` uses `equal_to` on each variant; Composite weights pinned via `elements_are [equal_to (SharpeRatio, 1.0); equal_to (CalmarRatio, 0.5)]`. `to_acquisition` round-trip → `test_to_acquisition_round_trips` uses `equal_to` on both polymorphic-variant outputs. Spec.load bounds field → `elements_are [equal_to (...); equal_to (...)]`. The determinism test compares full bo_log.csv contents via `equal_to log2` (byte-identical), not just file size. The CSV row count test uses `size_is 21`, but that is a count-of-rows assertion, not a pass-through identity claim. |
| CP4 | Each guard called out explicitly in code docstrings has a test that exercises the guarded-against scenario | PASS | (a) Spec `.mli` line 77: "Raises [Failure] on malformed input" → `test_load_malformed_raises` (input `"((bounds not-a-list))"`, asserts substring "failed to parse"). (b) Evaluator `.mli` lines 41–42: "looks up each scenario path in [scenarios_by_path] (raises [Failure] on miss)" → `test_evaluator_unknown_scenario_raises` (empty Hashtbl + known-unknown key, asserts substring "unknown scenario path"). (c) Runner `.mli` lines 55–56: "Creates [out_dir] via [mkdir -p] if it does not exist" → `test_run_and_write_creates_missing_out_dir` (nested `deep/nested/out` path verified via `Sys_unix.is_directory_exn`). (d) D7 determinism (`bayesian-opt-2026-05-03.md` §D7 + spec `.mli` line 47–51 documenting seeded RNG) → `test_determinism_same_seed_byte_identical_log` exercises the runner's complete output path (BO suggest_next → CSV row generation → file bytes), confirming non-determinism cannot leak in via the runner's own code (e.g., Hashtbl iteration ordering, mtime). All explicit guards pinned. |

## Behavioral Checklist

Pure infra / CLI wiring PR; no Weinstein domain logic introduced or modified per `.claude/rules/qc-behavioral-authority.md` §"When to skip this file entirely". The PR wires the shipped `Tuner.Bayesian_opt` lib (PR #817) to a CLI binary, mirroring PR #893's T-A grid_search CLI shape. D1–D8 from `dev/plans/bayesian-opt-2026-05-03.md` are pinned at the lib level (PR #817's 27 BO-lib tests cover GP fit + acquisition functions + 1D parabola + 2D Branin + determinism + bounds enforcement). The CLI's contracts (spec parsing, evaluator wiring, runner plumbing + three-artefact emission) are pinned by the 11 new unit tests above.

| # | Check | Status | Notes |
|---|-------|--------|-------|
| A1 | Core module modification is strategy-agnostic | NA | qc-structural did not flag A1 (no core module modifications; tuner/lib/ also unchanged). |
| S1–S6 | Stage definitions / buy criteria | NA | Pure infra / CLI wiring PR; no Weinstein domain logic introduced or modified. |
| L1–L4 | Stop-loss rules / state machine | NA | Pure infra / CLI wiring PR; no Weinstein domain logic introduced or modified. |
| C1–C3 | Screener cascade / macro gate / sector RS | NA | Pure infra / CLI wiring PR; no Weinstein domain logic introduced or modified. |
| T1–T4 | Tests covering domain outcomes | NA | Pure infra / CLI wiring PR; the 11 new tests pin CLI plumbing (spec parsing, runner BO ask/tell loop with stub evaluator, three-artefact emission, mkdir-p, determinism, cache-miss guard), not domain outcomes. Domain tests live in lib (PR #817) and are unaffected. |

## Notes (informational, not blocking)

- **Termination condition correctness**: `_run_loop` (`bayesian_runner_runner.ml:25–37`) decrements `iters_left` from `spec.total_budget` and stops at `iters_left <= 0`. `test_run_and_write_emits_three_artefacts` pins `result.observations` to `size_is 20` for `total_budget=20`; the CSV is `size_is 21` (1 header + 20 data rows), confirming both the loop count and the CSV row count match the documented contract.
- **`>= initial_random` random suggestions before GP transition**: convergence test uses `initial_random=5, total_budget=20`, so `_run_loop` calls `BO.suggest_next` 20 times; the lib returns random points for the first 5 calls and GP-driven argmax thereafter (per D2 in `bayesian-opt-2026-05-03.md`). The 15 GP-driven iterations are sufficient for the parabola convergence assertion (`abs(best_x - 3.0) < 0.5`). The runner's loop structure does not bypass or pre-empt `initial_random` — it simply forwards every `_suggest` result to `BO.observe`.
- **PR mirrors PR #893's three-module split**: confirmed structurally — `bayesian_runner_spec.{ml,mli}` mirrors `grid_search_spec.{ml,mli}`, `bayesian_runner_evaluator.{ml,mli}` mirrors `grid_search_evaluator.{ml,mli}`, `bayesian_runner_runner.{ml,mli}` mirrors `grid_search_runner.{ml,mli}`, and `bayesian_runner.ml` mirrors `grid_search.ml`. The runner's `run_and_write ~spec ~out_dir ~evaluator` signature is identical to grid_search_runner's. Argument-parsing in the binary is identical (`--spec`, `--out-dir`, `--fixtures-root`).
- **Smoke-scenario sanity-check explicitly deferred**: PR body §"Deferred (explicit)" calls it out and `dev/status/tuning.md` line 58 documents it ("Smoke-scenario integration deferred (mirrors PR #893's deferral; full-universe single eval ~5–10 min)"). Reasonable trade-off per the precedent.
- **Determinism test exercises the runner's full output**: `test_determinism_same_seed_byte_identical_log` reads the entire `bo_log.csv` via `In_channel.read_all` and asserts `equal_to log2` on bytes. The CSV content includes BO-suggested parameter values formatted via `%.17g` (full precision), the iter counter, scenario name, metric values, and objective value; any non-determinism in the runner's own code paths (e.g., Hashtbl iteration ordering of `scenarios_by_path`, file mtime, parameter ordering) would surface as a byte diff. The lib pins determinism at the `BO.suggest_next` level (PR #817); this test pins it at the runner's complete I/O path.
- **Three-artefact shape coverage depth**: `bo_log.csv` row count is pinned (line 219, `size_is 21`); the determinism test pins the entire byte content. `best.sexp` and `convergence.md` shape pinning is limited to file-existence assertions (`Sys_unix.file_exists_exn`), not content shape. This mirrors PR #893's coverage depth — the contract is "files are created at the documented paths" — and matches the test docstring's stated scope. Not blocking; flag for follow-up if a downstream tool starts depending on `convergence.md` table format or `best.sexp` sexp ordering.
- **D1–D8 plan decisions**: D1 (pure functional state) honored — runner threads `bo` state through tail-recursive `loop`. D2 (two-phase suggestion) inherited from lib via `BO.suggest_next` / `BO.suggest_next_with_candidates`. D5 (acquisition functions) — both `Expected_improvement` and `Upper_confidence_bound β` exposed via the spec sexp and pinned by `test_load_ucb_acquisition_and_composite_objective_parses` + `test_to_acquisition_round_trips`. D6 (bounds enforcement) inherited from lib. D7 (determinism) pinned at the runner level by `test_determinism_same_seed_byte_identical_log`. D8 (best returns highest-metric obs) inherited from lib via `BO.best`. D3–D4 are lib-internal and not exposed to the CLI surface.

## Quality Score

4 — Clean CLI wire-up that faithfully mirrors PR #893's three-module split; every non-trivial `.mli` claim is pinned by a named test (CP1 PASS), all four documented guards have negative-path tests (CP4 PASS), and the determinism test exercises the runner's full byte-output path rather than just the lib's suggest_next sequence. Limited shape pinning of `best.sexp` and `convergence.md` (existence-only, matching PR #893's depth) keeps this from a 5.

## Verdict

APPROVED
