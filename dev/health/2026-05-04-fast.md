# Health Report — 2026-05-04 — fast (run-2)

## Summary

- Main build: PASSING (`dune build` exit 0 on HEAD `0f53b9c`)
- Main runtest: PASSING (`dune runtest --force` exit 0 on HEAD `0f53b9c`)
- PR #833 build/runtest: PASSING (exit 0 on tip SHA `57571d63`; verified live during qc-structural H3 challenge)
- Status file integrity (`dune runtest devtools/checks/`): PASSING (gate is dune exit code; 0)
- Status file integrity (standalone `status_file_integrity.sh`): **5 advisory FAILs** (script exits 0; not a gate contributor) — REGRESSED from 2 at run-1
- Action required: NO

## Run-1 → Run-2 history

Run-1 wrote this file at: 2 advisory FAILs on `weekly-snapshot.md` only (down from 6 yesterday after #826 reconcile).
Run-2 (this file): 5 advisory FAILs — schema drift regressed via #832 chore status refresh.

## Metrics

- Checks run: 3 (deterministic per orchestrator Step 6; no agentic fast scan)
- Advisory standalone-script findings (info-only):
  - `hybrid-tier.md`: free-form trailing clause on `## Status MERGED — both options shipped...` (introduced by #832)
  - `orchestrator-automation.md`: free-form trailing clause on `## Status IN_PROGRESS — Phase 1 stable...` (introduced by #832)
  - `tuning.md`: free-form trailing clause on `## Status IN_PROGRESS — T-A + T-B libs MERGED...` (introduced by #832)
  - `weekly-snapshot.md`: `## Status PLANNED — owner moved...` (carried)
  - `weekly-snapshot.md`: `## Interface stable NO — track is brand-new.` (carried)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)

## Notes

All 5 advisory FAILs are non-gating (linter exits 0). The recurring schema-drift pattern (free-form trailing clause on `## Status` lines) has been carried as an `[info]` escalation across 6+ runs; the harness backlog candidate is to grow `status_file_integrity.sh` to allow trailing clauses or alternatively to enforce strict prefix-only. Pre-decision.
