# Health Report — 2026-05-04 — fast

## Summary

- Main build: PASSING (`dune build` exit 0 on HEAD `9a184d4`)
- Main runtest: PASSING (`dune runtest --force` exit 0 on HEAD `9a184d4`; 100.23s smoke)
- Status file integrity (`dune runtest devtools/checks/`): PASSING (gate is dune exit code; 0)
- Status file integrity (standalone `status_file_integrity.sh`): 2 advisory FAILs on `dev/status/weekly-snapshot.md` (script exits 0; not a gate contributor)
- Action required: NO

## Metrics

- Checks run: 2 (deterministic per orchestrator Step 6; no agentic fast scan)
- Advisory standalone-script findings (info-only): 2 on `weekly-snapshot.md` — `## Status PLANNED` not in enum + `## Interface stable NO — track is brand-new.` free-form trailing clause. Down from 6 advisory FAILs at run-2 yesterday after PR #826 reconciled `data-foundations.md` and `experiments.md`.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)
