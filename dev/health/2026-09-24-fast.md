# Health Report -- 2026-09-24 -- fast

Orchestrator run 36032653844 (2026-09-24 run 2). Checked at `7d7fd6b7` (main after #2949 and #2950;
#2951 merged later as `1dd14b25`, CI green on its pre-merge tip `db7c4adf`).

## Summary
- Main build: PASSING (`dune build` exit 0)
- Main tests: PASSING (`dune runtest` exit 0; zero `^FAIL:` lines in build or test logs)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0, 16198/20480 bytes)
- Action required: NO

## Metrics
- Checks run: 2 deterministic ones (build gate and status integrity), plus the index linter. There is no agentic fast scan.
- Advisory linter output: not checked here; the `dune runtest` exit code covers it.
- Deep scan: see dev/health/2026-09-21-deep.md (weekly GHA cron).
- Exit codes were read unpiped, and dune ran under `flock /tmp/dune.lock` with no agent building at the same time.
