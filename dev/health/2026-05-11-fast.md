# Health Report — 2026-05-11 — fast

## Summary
- Main build: PASSING (exit 0 on HEAD `eeb91b5`)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build: `dune build` exit 0; `dune runtest` exit 0 — all dune-wired linters green
  (fmt_check, fn_length, magic_numbers, mli_coverage, nesting, posix_sh_check,
  status_file_integrity, agent_compliance_check, no_python_check). The full
  test suite (including `test_weinstein_backtest.exe`, the long-running 3-test
  ~108-second cell) completes inside the runtest sweep without timeout.
- Advisory linter output: not checked here — covered by dune runtest exit code
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)
