# Health Report -- 2026-05-11 -- fast (run 2)

## Summary
- Main build: PASSING (exit 0 on `eeb91b5`)
- Status file integrity: PASSING (exit 0 on the local checkout)
- Action required: NO (on main)

Cross-reference: a separate `build-and-test` job FAILED on PR #1030's branch (`ops/daily-2026-05-11`, tip `65ab74f7`) — see today's daily summary §Escalations. The failure is the same `status_file_integrity.sh` linter running against `dev/status/tuning.md` on that branch (run-1 wrote a non-conformant `## Last updated:` line with a parenthetical). On `main` the line is still the older clean value, so the linter passes.

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
  - 6.1 `dune build && dune runtest` — exit 0
  - 6.2 `sh devtools/checks/status_file_integrity.sh` — exit 0
- Advisory linter output: not checked here -- covered by dune runtest exit code
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)
