# Health Report -- 2026-07-12 -- fast

## Summary
- Main build: PASSING
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0 — 8994/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Detail
- Main build health this run was verified via the QC pipeline, not a redundant
  standalone gate: qc-structural ran `dune build @fmt` / `dune build` /
  `dune runtest` on `feat/extension-stop` (= main + the PR diff) — all three
  exit 0, 42 tests pass, all dune-wired linters clean (`fmt_check`,
  `linter_magic_numbers`, `fn_length`, `nesting`, `mli_coverage`,
  `status_file_integrity`). That branch merged squash to main this run
  (`9aa0f98a` → `f9d4bc56`), so main now carries exactly the code QC tested green.
- Main HEAD CI pre-merge (`9aa0f98a`, #1933): `build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all `success`.
- `dune` 3.24.0 is available in the GHA runner via `eval $(opam env)` (confirmed
  this run and by run-4). The docker `trading-1-dev` container is NOT present in
  GHA and is not needed — the wrapper runs natively.

## Metrics
- Checks run: 2 deterministic (status_file_integrity, index_size_linter) +
  full `dune build`/`dune runtest` via the qc-structural gate on the merged code.
- Deep scan: last `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13.
