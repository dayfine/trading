# Health Report -- 2026-06-06 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0; re-validated after `_index.md` reconcile)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): `dev/lib/run-in-env.sh dune build` exit 0 + `dune runtest` exit 0 on HEAD `18b7ea1c`; no `^FAIL:` lines.
- Status integrity (6.2): exit 0 ("all dev/status/*.md files have required fields").
- Advisory linter output: not checked here -- covered by dune runtest exit code (magic-numbers / nesting / fn-length / mli-coverage / status-integrity linters are dune-wired and gate on exit code; runtest exit 0 = all green).
- Deep scan: see dev/health/*-deep.md (weekly GHA cron).
