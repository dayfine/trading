# Health Report — 2026-04-21 — fast

## Summary
- Main build: PASSING (dune runtest exit 0 on main@origin tip 9b6bc95)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here — covered by dune runtest exit code
- Deep scan: see dev/health/*-deep.md (weekly GHA cron)

## Notes
- Post-merge baseline on main is clean after the overnight 6-PR merge wave (#473, #477, #478, #479, #480, #481, #482, #476).
- Test output includes benign `Tiered_strategy_wrapper: [promote universe to Summary] Bar_loader.promote error (continuing): ...` stderr lines during `test_runner_tiered_cycle`. These are expected fixture-missing log lines emitted by the tiered wrapper's "continuing:" recovery path and do not change the test outcome (`OK Ran: 10 tests`). Same phenomenon surfaces in PR #484's behavioral QC as F2 (benchmark_symbol SPY/GSPC.INDX divergence) — not a new health issue; documented in backtest-scale §Follow-up.
