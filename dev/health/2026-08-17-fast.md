# Health Report -- 2026-08-17 -- fast

## Summary

- Main build: **PASSING** — measured locally this run, not substituted.
- Status file integrity: **PASSING** (exit 0)
- Action required: **NO**

## Measured

Run from `trading/` after `eval $(opam env)`, foreground, sentinel-terminated:

```
BUILD_EXIT=0
RUNTEST_EXIT=0
FMT_EXIT=0
```

`grep -c '^FAIL:'` over the full log: **0**.

Shell-only gates, run directly:

| check | exit |
|---|---|
| `status_file_integrity.sh` | 0 |
| `index_size_linter.sh` | 0 (10490/20480 bytes, all rows <= 250 chars) |
| `no_python_check.sh` | 0 |

GitHub CI on main's tip `e1d62260` at run start: `build-and-test` **success**,
`perf-tier1-smoke` **success**, `golden-custom-universe` **success**,
`golden-sp500-5y` **success**, `perf-tier2-nightly` **success**. Zero open
`ci-red` watchdog issues.

## Toolchain correction (carried wrong for 14 runs)

Prior fast reports and daily summaries recorded **"`dune` not on PATH"** and
treated a local build as unavailable in GHA. That is imprecise. `dune` **is**
installed and works — it simply requires the opam environment first:

```
eval $(opam env)   # -> /home/opam/.opam/5.3.0+flambda/bin/dune, 3.24.2
```

This run ran the full local `dune build` + `dune runtest` + `dune build @fmt`
gate to completion (all exit 0) on the GHA runner. Step 6.1 is therefore
**directly executable here** and no longer needs the CI-check-run substitution
that the last several runs used for it.

The known cosmetic warning `No dune-project file has been found in directory "."`
(tracked as `H-RUNENV-DUNE-PROJECT-WARNING`) appears on every invocation and is
not a failure.

## Metrics

- Checks run: 5 (2 deterministic gates + 3 shell linters), all exit 0.
- Local build wall-time: ~7 min cold, concurrent with 5 shell-only QC agents.
- Runner load average during the 5-agent QC wave, before the build started:
  **0.12** on 4 cores — see the daily summary's capacity section.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron); not run here.
