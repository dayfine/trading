# Health Report — 2026-07-17 — fast (run-3)

## Summary
- Main build: PASSING — `dune build` exit 0
- Main tests + linters: PASSING — full `dune runtest` exit 0 (no `FAIL:` / `LAPACKE` / `signal ILL` lines; fmt_check OK, magic_numbers OK)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 9816/20480 bytes, all rows ≤ 250 chars)
- Action required: NO (CI-red-on-HEAD is a confirmed flake — see below)

## CI-red-on-HEAD investigation (build-and-test on #1995)
- HEAD `3a633f6d` (#1995, docs-only orchestrator-summary merge) shows GHA
  `build-and-test=failure`; the failing step is `dune runtest` (step 7).
- Root cause: intermittent `Failure("LAPACKE: 9")` — LAPACK `potrf` (Cholesky)
  non-positive-definite on the GP kernel in `test_bayesian_opt`
  (`BO converges on 2D Branin` + `suggest_next GP phase respects bounds`;
  `FAILED: Cases: 43 Tried: 43 Errors: 2`).
- CONFIRMED FLAKE, not a regression:
  - #1995 is docs-only (identical test code to green parent `ad0b29ab`).
  - Base rate: 4/4 prior main commits (`ad0b29ab`/`9cdc8055`/`36863498`/`093c75b9`)
    `build-and-test=success`.
  - Local reproduction: `test_bayesian_opt.exe` passed 6/6 standalone; full
    `dune runtest` on this HEAD exit 0.
  - DISTINCT from the #1961 SIGILL class (`Command got signal ILL`): `OWL_CFLAGS`
    portable pin (`-march=x86-64-v2 -mtune=generic`) is confirmed active this run.
- Logged as a new harness backlog `[ ]` item (raise GP jitter/nugget floor in the
  Bayesian_opt fit/test; no workflow PAT needed). GHA run 29599087810 job 87946588479.

## Metrics
- Checks run: full `dune build` + `dune runtest` (deterministic) + status-integrity
  + index-size linters; plus targeted 6× stress-run of `test_bayesian_opt.exe`.
- Deep scan: see dev/health/*-deep.md (weekly GHA cron).
