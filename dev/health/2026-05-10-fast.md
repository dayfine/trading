# Health Report -- 2026-05-10 -- fast

## Summary
- Main build: PASSING (exit 0 on tip post-dispatch + post-QC)
- Status file integrity: PASSING (exit 0; "OK: all dev/status/*.md files have required fields")
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan per Step 6 retired-fast-scan policy)
- Build invocation: `dev/lib/run-in-env.sh dune build` (exit 0)
- Test invocation: `dev/lib/run-in-env.sh dune runtest` (exit 0; all dune-wired linters PASS — `fmt_check.sh`, `linter_magic_numbers.sh`, `nesting_linter`, `linter_mli_coverage.sh`, `fn_length_linter`, `no_python_check.sh`, `posix_sh_check.sh`)
- Status integrity invocation: `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` (exit 0)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron — separate)

## Notes
- The orchestrator's run-in-env wrapper cd's into `trading/`, so `status_file_integrity.sh` is invoked as `devtools/checks/...` (not `trading/devtools/...`). The runtime initially produced a "No such file" warning when the wrong path was passed; corrected on retry.
- All hard gates clean on `feat/all-eligible-release-report` tip `f56d1c1c` (qc-structural verified pre-review) and on the orchestrator's main-tracking working tree (deterministic post-dispatch run).
