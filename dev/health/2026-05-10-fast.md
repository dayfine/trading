# Health Report -- 2026-05-10 -- fast (consolidated, run-1 + run-2)

## Summary
- Main build: PASSING (exit 0 on tip `9b3d17f` after run-1 summary auto-merge)
- Status file integrity: PASSING (exit 0; "OK: all dev/status/*.md files have required fields")
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan per Step 6 retired-fast-scan policy)
- Build invocation: `dev/lib/run-in-env.sh dune build` (exit 0)
- Test invocation: `dev/lib/run-in-env.sh dune runtest` (exit 0; all dune-wired linters PASS — `fmt_check.sh`, `linter_magic_numbers.sh`, `nesting_linter`, `linter_mli_coverage.sh`, `fn_length_linter`, `no_python_check.sh`, `posix_sh_check.sh`)
- Status integrity invocation: `sh trading/devtools/checks/status_file_integrity.sh` (exit 0)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron — separate)

## Notes
- run-1 (~08:30Z) verified clean on `feat/all-eligible-release-report` tip `f56d1c1c` (qc-structural pre-review) and on `main` HEAD `b628ef9`.
- run-2 (~12:39Z) re-verified clean on `main` HEAD `9b3d17f` (post run-1 summary auto-merge), confirming run-1's #1018 landed without regressions. dune runtest also passes through the long-running `test_weinstein_backtest.exe` (108.78s, 3 tests OK) without timeout.
- The orchestrator's run-in-env wrapper cd's into `trading/`, so `status_file_integrity.sh` should be invoked with the path relative to that workspace root.
