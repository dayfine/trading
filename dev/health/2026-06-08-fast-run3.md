# Health Report — 2026-06-08 — fast (run 3)

## Summary
- Main build: PASSING (dune build exit 0)
- Main runtest: PASSING (dune runtest exit 0; no `^FAIL:` lines)
- Status file integrity: PASSING (exit 0 — "OK: all dev/status/*.md files have required fields")
- Action required: NO

## Evidence (live, this run, on HEAD af28fc5e)
- `dune build` exit 0; `dune runtest` exit 0 from workspace root `/__w/trading/trading/trading`.
- No `^FAIL:` lines anywhere in runtest output.
- Linters all OK: fmt (`OK: all .ml/.mli files are correctly formatted`), magic numbers
  (`OK: no magic numbers found in lib/ files`), arch-layer R1/R4/R6, status-file-integrity,
  agent-compliance, deep-scan structural guards.
- Benign non-failures observed (runtest still exit 0): `[grid_search] cell N crashed:
  (Failure stub_intentional_crash)` are intentional test stubs; `find: ... No such file`
  lines are the known dune sandbox-race artifact.

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan — retired per Step 6).
- Deep scan: see latest `dev/health/*-deep.md` (weekly GHA cron; unaffected by this run).
