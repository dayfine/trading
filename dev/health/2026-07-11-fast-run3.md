# Health Report — 2026-07-11 — fast (run-3)

## Summary
- Main build: PASSING (CI signal — see note; `dune` absent in GHA runner)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 8875/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Detail
- **Main CI on HEAD `9aec60a6` (#1928):** `build-and-test`, `perf-tier1-smoke`,
  `golden-custom-universe`, `golden-sp500-5y` all `success`; `watchdog` skipped;
  `orchestrator` in_progress (this run). Main **unambiguously green**.
- `dune build` / `dune runtest`: **not runnable in the GHA runner** (`dune`
  absent from PATH; no `docker`; `opam` present but no trading switch). The
  authoritative main-green signal is the CI check-run set on `9aec60a6`. Per the
  live-evidence rule this runner-env gap is explicitly NOT a `[critical]`.
- `status_file_integrity.sh`: exit 0 (post-edit).
- `index_size_linter.sh`: exit 0 (post-edit).

## Metrics
- Checks run: 3 (deterministic shell: CI-signal read + status-integrity + index-size).
- Agentic fast scan: retired (deterministic checks only; see lead-orchestrator Step 6).
- Deep scan: last `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13 (GHA cron).
