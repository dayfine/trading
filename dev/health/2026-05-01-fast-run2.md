# Health Report -- 2026-05-01 -- fast (run 2)

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: FAILING (exit 1) on main; **already fixed in run-1's open PR #730** (`ops/daily-2026-05-01`); waiting on merge.
- Action required: NO — main is green per build/runtest gate; the two integrity failures below are already repaired on run-1's branch.

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here -- covered by dune runtest exit code
- Deep scan: see dev/health/*-deep.md (weekly GHA cron)

## Status integrity violations on main (advisory)

```
FAIL: status file integrity linter — malformed or missing fields in dev/status/:
screener.md: '## Status' value 'MERGED (with one optional knob in flight: cascade post-stop-out cooldown — PR #718)' is not one of IN_PROGRESS|READY_FOR_REVIEW|APPROVED|MERGED|BLOCKED|PENDING
short-side-strategy.md: '## Last updated' is not YYYY-MM-DD: '2026-04-30 (evening)'
```

Both repaired on `ops/daily-2026-05-01` (run-1's PR #730, open). Run-2 deliberately
does NOT touch the same status files to avoid creating a merge conflict between
the two ops PRs. The advisory linter is exit-1 on standalone invocation but does
NOT gate `dune build && dune runtest` (both passed exit 0 this run); the
integrity check is wired in advisory mode under `trading/devtools/checks/`.

Once #730 merges, the next run's Step 6 deterministic check will report CLEAN.
