# Health Report — 2026-06-17 — fast (run 3)

## Summary
- Main build: PASSING (CI authoritative — `build-and-test` SUCCESS on HEAD `3fab6c91`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (7198/20480 bytes, all rows ≤ 250 chars)
- Action required: NO (for main health) — but see the CI-infra ENOSPC finding below

## Metrics
- Checks run: 2 deterministic (status integrity + index size); build gate via CI-green (no local `dune build`, per run-1/2 practice — avoids the same runner disk-exhaustion class this report flags)
- Build gate (6.1): HEAD `3fab6c91` CI GREEN — `build-and-test` + `perf-tier1-smoke` + `golden-sp500-5y` + `golden-custom-universe` + `perf-tier2-nightly` all COMPLETED SUCCESS. (`golden-sp500-15y` + `orchestrator` in_progress = this run.)
- Status integrity (6.2): `status_file_integrity.sh` exit 0.
- Index size (6.2): `index_size_linter.sh` exit 0.
- Deep scan: see weekly GHA cron output under `dev/health/*-deep.md`.

## Finding — CI-infra ENOSPC during link (recurring, ROOT CAUSE FOUND this run)
- **Severity: [medium]** (infra; intermittent; does not block — main is green).
- The `build-and-test` job ENOSPC'd during `ocamlopt` linking on docs-only commit `74acd21` (`/usr/bin/ld: final link failed: No space left on device`), firing ci-watchdog issue #1634; the next docs-only commit `3fab6c91` passed cleanly → disk-pressure flake, not content.
- **Root cause (harness-maintainer, this run):** the CI image bakes an unused ~11 GB copy of `trading/_build` under `/workspaces/trading-1/`; restoring the `_build` cache + linking many static test `.exe` files crosses the ~12 GB free-space line on smaller ubuntu-latest runners.
- **Fix designed + committed locally** (free ~11.4 GB + `df -h` diagnostic) but BLOCKED: orchestrator GH tokens lack `workflow` scope → cannot push `.github/workflows/*.yml`. See PR #1636 (body has the exact YAML) and today's daily summary §Escalations.
