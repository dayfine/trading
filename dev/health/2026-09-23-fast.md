# Health Report -- 2026-09-23 -- fast

Orchestrator run 35862614348. Main at `ed126299`.

## Summary
- Main build: **PASSING** (`BUILD_EXIT=0`)
- Test suite: **PASSING** (`RUNTEST_EXIT=0`)
- Status file integrity: **PASSING** (`INTEGRITY_EXIT=0`)
- Action required: **NO**

## Evidence

Both gates run behind `flock /tmp/dune.lock` so they could not race the two
dispatched agents -- this runner is memory-bound at 15 GB with a *silent* OOM
(no exception, no stack, just a truncated log), so two concurrent OCaml links
is enough to trip it.

Exit codes read **unpiped**. This matters and is not pedantry: `cmd | tail`
reports `tail`'s status, not `cmd`'s, and a check that reads its own exit
status through a pipeline reports green unconditionally.

```
BUILD_EXIT=0
RUNTEST_EXIT=0
INTEGRITY_EXIT=0
```

`^FAIL:` lines in the runtest output: **0**.

The gate is the exit code, not `FAIL:` text -- but both agree here, so there is
nothing to interpret.

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron; newest run
  35648266280, 2026-09-21, conclusion success)

## Note on what a green fast scan does and does not cover

The last `dune runtest` line was the `pr_gate_status` mutation harness:
`26 mutation(s) match pin (21 killed / 4 live survivor / 1 equivalent mutant)`.
That is the merge gate's own reader being checked against deliberate
corruption, and it is the strongest single signal in this report -- a green
build says the code compiles, but that line says a specific guard has been
observed to fail when it should.
