# Health Report -- 2026-07-03 -- fast (run 5)

## Summary
- Main build: PASSING (CI authoritative — HEAD `af2d47b4`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0, 8564/20480 bytes, all rows ≤ 250)
- No-python: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (status-integrity, index-size, no-python) + CI-authoritative build gate.
- Build gate (6.1): main CI on HEAD `af2d47b4` (#1840) — `build-and-test` +
  `perf-tier1-smoke` + `golden-custom-universe` COMPLETED SUCCESS; `golden-sp500-5y` +
  `orchestrator` in_progress (long goldens / this run). No `^FAIL:`; exit-code-green. This
  run's writes are docs-only (Step 5.5 reconcile + summary), so no new build surface.
- Advisory linter output: not checked here — covered by dune runtest exit code in CI.
- Deep scan: 2026-06-29 (#1789) — 0 critical/high/medium; weekly GHA cron owns deep mode.
