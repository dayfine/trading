# Health Report -- 2026-07-11 -- fast

## Summary
- Main build: PASSING (GitHub CI on HEAD `9d099c49` — `build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all `success`;
  `watchdog` skipped; `orchestrator` in_progress = this run)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (8766/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (CI-status read + status_file_integrity.sh + index_size_linter.sh)
- `dune build`/`dune runtest`: NOT runnable in the GHA runner (`dune` absent from
  PATH). Authoritative main-green signal is GitHub CI on HEAD `9d099c49`. Per the
  live-evidence rule this runner env gap is explicitly NOT a `[critical]`.
- Advisory linter output: covered by CI `build-and-test` (fn_length, nesting,
  magic_numbers, file_length, status_file_integrity) — green on `9d099c49`.
- Deep scan: last `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13.
