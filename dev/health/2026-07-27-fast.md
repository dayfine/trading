# Health Report -- 2026-07-27 -- fast

Gates run on `main` @ `060ea11b`, strictly before any agent dispatch, in the
orchestrator's own container (`trading-devcontainer`), GHA run 30231043807.

## Summary

- Main build: **PASSING** (`dune build` exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0, 10094/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- Test binaries executed: **359**
- `Error:` lines in runtest output: **0**
- `^FAIL:` lines in runtest output: **0**
- Build verified non-vacuous: yes -- 359 distinct test binaries ran and reported
  `Ran: N tests`, so this is not a cached no-op.

## The LAPACKE / `Tuner.Bayesian_opt` question -- fourth green datapoint

Run 3 (2026-07-26) escalated `[critical]`: "`dune runtest` fails deterministically
on main, and #2009 does not fix it", reproduced 3/3 on that host. Run 4 could not
reproduce it. **This run cannot reproduce it either**, and the evidence is direct
rather than inferential:

```
(cd _build/default/trading/backtest/tuner/test && ./test_bayesian_opt.exe)
............................................
Ran: 44 tests in: 1.08 seconds.
OK
```

44 tests, 44 dots, zero `E`, `OK`. These are the exact tests (`Tuner.Bayesian_opt:11`
and `:17`) that run 3 saw error with `Failure("LAPACKE: 9")`.

Tally across runs on the same image: **runs 1, 2, 4 and 5 (this run) green; run 3 red.**
Four-to-one is consistent with the host/BLAS-kernel-dependence reading run 4 proposed
and inconsistent with a code regression on main. Carried as `[medium]`, not `[critical]`
-- per Step 1c a clean verification must never be tagged `[critical]`, because that tag
fires the "Fail on escalations" GHA gate.

**New this run -- the CI-cache-masking hypothesis is disconfirmed.** Run 3's escalation
could not rule out that CI's `Cache _build` step was letting a cached pass be reused, so
that CI was structurally unable to surface the failure. The CI run on PR #2101
(`build-and-test`, job 89861963483) logged:

```
Cache not found for input keys: dune-Linux-edcd0bc2cb3bfd12b44a0196f9db4fb44f146aa8901a990665dfd31566ddfbb8
```

That was a **cold, full build+runtest with no cache restored**, and it still executed
`test_bayesian_opt.exe` without failure. So CI is *not* vacuously green on this suite.
That removes the more alarming of run 3's two readings and leaves host-dependence as the
surviving explanation.

## Note on the Step 6.2 documented path (carried, 4th run)

`.claude/agents/lead-orchestrator.md` Step 6.2 documents the linter path as
`trading/devtools/checks/status_file_integrity.sh` invoked through
`dev/lib/run-in-env.sh`. That wrapper sets `PROJECT_ROOT="${GITHUB_WORKSPACE}/trading"`
and `cd`s there, so the correct relative path from that working directory is
`devtools/checks/...`, not `trading/devtools/checks/...`. From the repo root the scripts
live at `trading/devtools/checks/`. Verified again this run; used the corrected form.
Still a documentation bug, not a code bug.
