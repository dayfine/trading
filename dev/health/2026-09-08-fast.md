# Health Report -- 2026-09-08 -- fast

Deterministic Step 6 checks only (the agentic fast scan was retired; the weekly
deep scan is unaffected and runs on its own cron -- last one succeeded
2026-09-07, run 34138286380).

Main at run start: **`c0630137`**.

## Summary

- Main build: **PASSING** (`dune build` exit **0**)
- Main tests: **PASSING** (`dune runtest` exit **0**, **0** `^FAIL:` lines)
- Status file integrity: **PASSING** (exit **0** -- "all dev/status/*.md files have required fields")
- Index size linter: **PASSING** (exit **0** -- 14095/20480 bytes, all table rows <= 250 chars)
- Action required: **NO**

Every exit code was read **unpiped** (`cmd > log 2>&1; echo $?`). A piped read
captures the pipe's exit status, not the command's, and has produced a false
green in this repo before.

## Step 6.2 invocation -- the doubled-path defect, worked around

`.claude/agents/lead-orchestrator.md` Step 6.2 prescribes:

```
dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
```

which exits **2** (`sh: 0: cannot open ...`) because `run-in-env.sh` already
`cd`s into the inner `trading/`. This has been reported for eleven consecutive
runs and the one-line fix is blocked behind the `.claude/agents/**` write gate.

The working form, used above and confirmed exit 0 for both checks this run, is
the same command with the `trading/` prefix dropped:

```
dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
dev/lib/run-in-env.sh sh devtools/checks/index_size_linter.sh
```

Recorded here so the next run does not re-derive it.

## Scheduled-workflow health

`trading/devtools/checks/scheduled_workflow_health.sh` -- exit **1**
("measured and bad", the correct signal):

```
SUMMARY: active=17 (1 page(s) fetched, full pagination -- a real total, not a floor)
         ok=7 red=3 stale=0 no-schedule=7
SUMMARY: RED workflows: Prune candidates weekly, Weekly track pacer, Weekly start sweep (BAH SPY)
```

Same three names as 2026-09-05 -- but **the timestamps have moved**, so unlike
last run these are fresh firings, not stale rows:

| workflow | 09-05 reading | 09-08 reading | fired since? |
|---|---|---|---|
| Prune candidates weekly | 08-31T16:26Z | **09-07T16:26Z** | yes |
| Weekly track pacer | 08-30T06:20Z | **09-06T06:20Z** | yes |
| Weekly start sweep (BAH SPY) | 08-31T14:06Z | **09-07T14:05Z** | yes |

The 2026-09-05 report said explicitly "do not read the merges as
confirmations" because no cron had fired yet. They have now fired, and the
answer is negative: **all three are still red.** Two of the three had their
failure *cause change underneath a stable-looking streak* -- see the daily
summary for the measurements.

## Metrics

- Deterministic checks run: 4 (build, runtest, status-integrity, index-size)
- Agentic fast scan: not run (retired)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron; 2026-09-07 succeeded)
