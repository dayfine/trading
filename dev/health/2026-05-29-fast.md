# Health Report -- 2026-05-29 -- fast

## Run-3 supplement (PM cron)

- Main build: PASSING (exit 0 on HEAD `d237ca58` — `docs(experiments): systematic experiment-platform program plan` #1368). `dev/lib/run-in-env.sh dune build` exit 0; `dev/lib/run-in-env.sh dune build && dune runtest` exit 0.
- Status file integrity: PASSING (exit 0 both before AND after the run-3 `_index.md` reconcile; `OK: all dev/status/*.md files have required fields.`).
- HEAD progression since run-2 (`a6c0e9b1`): #1362 stage3 exit-margin knob (PR-A merge) → `993acd7b`; #1364 PR-B retraction → `d1c1c7e0`; #1365 WF-CV spec → `1641834a`; #1366 WF-CV verdict → `64a6ba07`; #1367 spec gate.n fix → `51a0238b`; #1368 experiment-platform plan → `d237ca58`. All landed via maintainer-direct merges between 11:01Z (PR #1362) and 14:00Z (PR #1368). No new linter regressions surfaced.
- Action required: NO. Health remains CLEAN this run.

---

## Run-2 supplement (PM session)

- Main build: PASSING (exit 0 on HEAD `a6c0e9b1` — `docs(notes): PM session priorities — autopsy + stage3 hysteresis as new P0` #1361). `dev/lib/run-in-env.sh dune build` exit 0; `dev/lib/run-in-env.sh dune runtest --force` exit 0.
- Status file integrity: PASSING (exit 0; `OK: all dev/status/*.md files have required fields.`).
- HEAD progression since run-1 (`5ddf8b7b`): #1359 Calmar/Sortino gate → `4752e61c`; #1360 trade-autopsy tool → `e0f69965`; #1361 PM priorities doc → `a6c0e9b1`. All landed via maintainer-direct merges. No new linter regressions surfaced.
- Action required: NO. Health remains CLEAN this run.

---

## Run-1 (original — 02:00Z dispatch)

## Summary
- Main build: PASSING (exit 0 on HEAD `52733bf`; advanced to `5ddf8b7b` mid-flight when #1357 retracted P1 — both are doc-only sequences, no source impact)
- Status file integrity: PASSING (exit 0; `OK: all dev/status/*.md files have required fields.`)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan per Step 6 contract)
- Advisory linter output: covered by `dune runtest` exit code (exit 0)
- Deep scan: `dev/health/2026-05-25-deep.md` (0 critical / 22 warnings / 105 info; warnings are non-actionable design-doc drift)

## Notes
- Concurrent GHA-workflow `dune build` + `dune runtest` processes held the build lock briefly at run start (PIDs 645 + 9819); both completed cleanly with exit 0; orchestrator's retry then ran clean.
- One transient sandbox-race log line observed during runtest: `find: '/__w/trading/trading/trading/_build/.sandbox/338516851d0be06fba28b99b638f9ebd': No such file or directory` — per `.claude/rules/pr-merge-gates.md`, sandbox-race messages without `^FAIL:` are admissible infra noise; exit code 0 confirms the gate is GREEN.
