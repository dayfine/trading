# Health Report -- 2026-05-07 -- fast

## Summary
- Main build: PASSING (exit 0 on HEAD `bb72fe5`)
- Status file integrity: 7 advisory FAILs (script exits 1 directly, but dune runtest exit code is 0 — gate is exit code only per orchestrator policy; NOT a gate).
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build/runtest verified live this session pre-dispatch + post-dispatch (both exit 0 on HEAD `bb72fe5`)
- Status integrity advisory FAILs (7): `all-eligible.md` (×2 — `## Status` trailing clause + missing `## Interface stable`), `hybrid-tier.md`, `orchestrator-automation.md`, `tuning.md`, `weekly-snapshot.md` (×2 — `## Status` + `## Interface stable`). One new since 2026-05-05 fast scan: `all-eligible.md` (track schema not yet aligned with the integrity script's strict schema). Recurring schema drift; not blocking dune runtest.
- Deep scan: see `dev/health/2026-05-04-deep.md` (weekly GHA cron — last ran 2026-05-04)
