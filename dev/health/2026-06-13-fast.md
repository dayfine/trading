# Health Report -- 2026-06-13 -- fast (run 3)

## Summary
- Main build: PASSING (`dune build` exit 0 on db23af3d, the run-start HEAD)
- Full test suite: PASSING (`dune runtest` exit 0; **0 `^FAIL:` lines**)
- Status file integrity: PASSING (covered by the green `dune runtest`)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): `dune build` exit 0 + full `dune runtest` exit 0 on run-start
  HEAD db23af3d. `File "."` / `No dune-project in "."` lines are benign
  dune workspace-root notices, not failures.
- Status integrity (6.2): wired into `dune runtest`; green.
- CI cross-check: build-and-test + perf-tier1-smoke + golden-sp500-5y +
  golden-custom-universe all SUCCESS on db23af3d (the warmup flip #1566 re-pinned
  goldens cleanly).
- Post-merge: PR #1567 (NS1) merged with required CI green (build-and-test +
  perf-tier1-smoke SUCCESS on tip 31c94221) → main 3b6d5ced; no-op-default change
  keeps main GREEN. PR #1569 (NS2 docs-only) merged with CI green → main 5ac27fc1.
- Index-size linter (post Step 5.5 reconcile): exit 0 (6433/20480 bytes; longest
  row 228 <= 250).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA Monday cron).

## File-length trend watch (leading indicator, carried)
`portfolio.ml` is at 489 lines after this run's NS1 extraction of
`Portfolio_cash_floor` (the author extracted to stay under the 500 hard limit
rather than bump it -- code-health-discipline honored). Several core/orchestration
modules remain within ~10 lines of the 500 limit; a scheduled proactive extraction
pass is still advisable before the next additive PR breaks CI (carried from
run-1/run-2).
