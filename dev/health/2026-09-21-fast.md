# Health Report -- 2026-09-21 -- fast

## Summary
- Main build: **PASSING** (`BUILD_EXIT=0`)
- Main tests: **PASSING** (`RUNTEST_EXIT=0`)
- Status file integrity: **PASSING** (exit 0)
- Action required: **NO**

## Evidence

Full gate run against `main` @ `f99a1c18`, serialized behind `flock /tmp/dune.lock`
so it could not race the concurrently-dispatched agents on this 15 GB
memory-bound runner (a silent OOM here is indistinguishable from a real
failure — `.claude/rules/container-capacity-scheduling.md`).

```
BUILD_EXIT=0
RUNTEST_EXIT=0
```

`^FAIL:` lines in the full output: **zero**.

The gate is the **exit code**, not `FAIL:` text — but both agree here, so there
is no interpretation to make. Contrast the 09-20 run, where `RUNTEST_EXIT=1`
came from that run's *own* in-progress `_index.md` edit and correctly was **not**
escalated as a main-baseline failure.

Standalone checks, run without dune and with exit codes read **unpiped** (a
pipeline reports the last command's status, which is how a broken check reads as
green):

```
OK: all dev/status/*.md files have required fields.                                   exit 0
OK: index_size_linter — dev/status/_index.md within limits (15748/20480, rows <=250)  exit 0
OK: no-python check -- no *.py files found.                                           exit 0
```

## Metrics
- Checks run: 2 load-bearing (deterministic; no agentic fast scan) + 3 standalone
- Advisory linter output: not separately assessed — covered by the `dune runtest` exit code
- Deep scan: weekly GHA cron; newest is `dev/health/2026-09-14-deep.md`

## Notes

Nothing to escalate from this scan. The two findings this run produced
(issues #2886, #2887) came from *exercising* the harness rather than from
scanning it — neither is a build-health finding, and neither is visible to these
checks. That is worth recording: a clean fast scan is evidence about the build,
not about the tooling around it.
