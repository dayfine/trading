# Health Report -- 2026-04-29 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- Test suite: PASSING (`dune runtest` exit 0)
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; agentic fast scan retired per T3-A+)
- Advisory linter output: present in stdout but not gating (file_length on optimal_strategy_report.ml 442 LOC + screener.ml 636 + price_path.ml 511; nesting on exit_audit_capture.ml; fn_length on entry_audit_capture.ml:make_entry_transition 57 LOC). All pre-existing on main; none new from today's PRs.
- Deep scan: see dev/health/*-deep.md (weekly GHA cron)
