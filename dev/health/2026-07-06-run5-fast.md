# Health Report -- 2026-07-06 -- run5 fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Detail
- `dune build` — exit 0.
- `dune runtest` — exit 0. `fmt_check` OK ("all .ml/.mli files are correctly
  formatted"); `linter_magic_numbers` OK ("no magic numbers found in lib/
  files"); no `FAIL:` lines. The `find: … No such file or directory` lines
  under `_build/.sandbox/**` are the known sandbox-cleanup-race infra noise
  (documented non-FAIL; PR #1117 prune guard) — not a gate failure. The
  advisory `@large-module` / `@large-function` tables printed by the length
  linters are declared-large markers within policy, not violations.
- `status_file_integrity.sh` — exit 0 ("all dev/status/*.md files have
  required fields").
- Weekly deep scan (agentic, GHA Monday cron): last report
  `dev/health/2026-07-06-deep.md` (merged run-4 via #1870, 170 findings —
  0 critical / 30 warnings / 140 info). No new deep scan this run; no
  code-health fn_length / magic-number / dead-code finding outstanding.

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan).
- Advisory linter output: covered by `dune runtest` exit code.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).
