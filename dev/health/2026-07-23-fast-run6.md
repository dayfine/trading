# Health Report -- 2026-07-23 -- fast (run6)

## Summary
- Main build: PASSING (live CI on HEAD `285d1ca7`: build-and-test=success, perf-tier1-smoke=success, golden-sp500-5y=success, golden-custom-universe=success)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (10788/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic: live-CI required checks, status_file_integrity.sh exit 0, index_size_linter.sh exit 0)
- Advisory linter output: covered by the merged CI on HEAD (build-and-test green)
- Deep scan: weekly GHA cron; latest is PR #2022 (`ops/health-deep-2026-07-20`, advisory, human-merged)

## Notes
- The GHA orchestrator container has `opam` + `dune` (under `eval $(opam env)`) and can build; a full local `dune build && dune runtest` was NOT re-run this pass because ZERO code was dispatched/changed this run — main is byte-identical to the CI-verified state on `285d1ca7`. Health assessed via live CI on the exact HEAD.
- Non-required `orchestrator` check shows one prior `completed=failure` on `285d1ca7` (the #2055 docs PR is maintainer-authored, not an orchestrator run); the required merge-gate checks (build-and-test, perf-tier1-smoke) are both green, so main baseline is CLEAN.
