# Health Report -- 2026-08-13 -- fast

Source: `lead-orchestrator` Step 6 (deterministic; no agentic fast scan).
Orchestrator run 31679469222.

## Summary

- **Main build: PASSING** — `dev/lib/run-in-env.sh dune build` **exit 0**,
  measured on the run tree at `ec190b86` (= `main@7a0c8078` plus this run's
  summary commit). Captured via the detached-plus-`.rc`-file pattern, so the
  code read is dune's own and not a pipeline's.
- **Status file integrity: PASSING** — exit 0, `OK: all dev/status/*.md files
  have required fields.`
- **Action required: NO**

## Metrics

- Checks run: 2 (deterministic).
- Advisory linter output: not separately inspected — the gate is the `dune`
  exit code, per Step 6.1.
- Deep scan: latest is `dev/health/2026-08-03-deep.md` (weekly GHA cron). It
  carries no `[high]`/`[medium]` findings, so **Step 2e.1 appended nothing** to
  `dev/status/cleanup.md` §Backlog this run.

## Corroborating CI evidence on `main`

Independent of the local gate, GitHub check-runs were green on every SHA this
run touched:

| SHA | `build-and-test` | `perf-tier1-smoke` | note |
|---|---|---|---|
| `7a0c8078` | success | success | main at run start; `golden-runs-custom-universe` also success |
| `13e61cc3` | success | success | #2265 after `update-branch`, pre-merge |
| `1d2a94fd` | — | — | main after #2265 merged |

Zero open `ci-red` watchdog issues at run start and at run end.

## Step 6.2's documented invocation is still wrong — 7th consecutive run

Measured again this run, both forms:

```
$ dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh: No such file
exit=2                                          <- the form Step 6.2 documents

$ dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
OK: all dev/status/*.md files have required fields.
exit=0                                          <- the correct form
```

The wrapper `cd`s to the dune workspace root (`trading/`), so the path must be
workspace-relative. An orchestrator that followed Step 6.2 literally would read
**exit 2** and report a health-check failure that does not exist. Blocked on
`.claude/**` write access; carried forward in §Escalations.
