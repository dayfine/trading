# Health Report -- 2026-04-16 -- fast

## Summary
- Findings: 1  (critical: 1  warnings: 0  info: 0)
- Main build: FAILING (tests)
- Action required: YES

## Critical (requires immediate action before next orchestrator run)

1. **Test suite failures on main** -- 7 test suites failing with assertion failures
   - `test_sector_map` (analysis/weinstein/data_source) — Hashtbl.length assertion fails, likely due to missing `TRADING_DATA_DIR` env var in test environment
   - `test_screener_e2e`, `regression_test` (analysis/weinstein/screener) — 15 failures
   - `test_macro_e2e` (analysis/weinstein/macro) — 5 failures  
   - `test_portfolio_risk_e2e` (trading/weinstein/portfolio_risk) — 4 failures
   - `test_weinstein_backtest` (trading/simulation) — 3 failures
   - `test_weinstein_strategy_smoke` — 1 error

   **Root cause analysis:** The `test_sector_map.ml` test calls `Data_path.default_data_dir()` which reads the `TRADING_DATA_DIR` env var. Without this var set in the dune test environment, it falls back to `/workspaces/trading-1/data` (Docker workspace path), which does not exist in the GitHub Actions CI environment (`/__w/trading/trading`).
   
   The same path lookup cascades to dependent tests (`screener`, `macro`, `portfolio_risk`, `backtest`, `strategy`), causing assertion failures when trying to load sectors/bars data.
   
   **Status as of last fast-scan (2026-04-14):** Main build was PASSING.
   
   **Recommendation:** Add `(env_vars TRADING_DATA_DIR)` to affected dune test stanzas in analysis/weinstein/data_source/test/dune and trading/simulation/test/dune, and ensure the `dev/run.sh` orchestrator environment exports `TRADING_DATA_DIR=$(pwd)/test_data` before running tests.

## Checks

| Check | Status | Details |
|-------|--------|---------|
| Stale status files | PASS | All status files updated within 14 days; READY_FOR_REVIEW items have reviews dated 2026-04-16 |
| Main build (compile) | PASS | `dune build` succeeds |
| Main tests (runtest) | FAIL | 7 test suites with 38 total failures; root cause: missing TRADING_DATA_DIR env var |
| Magic numbers linter | PASS | Covered by dune runtest; linter_exceptions.conf has no expired entries |
| Status file integrity | PASS | All dev/status/*.md files have required `## Status` and `## Last updated` fields |
| Linter exceptions past review date | PASS | 0 exceptions with expired review_at dates |

## Metrics
- Open follow-up items: not counted (fast scan)
- Linter exceptions past review date: 0
- Stale READY_FOR_REVIEW items: 0
- Status file integrity: PASSING
- Magic numbers linter: PASSING (covered by dune runtest)
