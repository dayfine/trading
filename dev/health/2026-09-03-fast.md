# Health Report -- 2026-09-03 -- fast

Orchestrator GHA run **33778603256**. Baseline measured at main `e4984c5f`
(run start). Every exit code below read **unpiped** — no `| tail; echo $?`,
which captures `tail`'s status rather than the command's.

## Summary

- Main build (`dune build @runtest --force`): **PASSING** (exit **0**)
- `^FAIL:` lines in that output: **0**
- Status file integrity: **PASSING** (exit **0**)
- `index_size_linter`: **PASSING** (exit **0**, 12690/20480 bytes, all rows <= 250 chars)
- Action required: **NO** for the local tree

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- Deep scan: see `dev/health/2026-08-24-deep.md` (weekly GHA cron; `Weekly deep
  health scan` last scheduled run **succeeded** 2026-08-31)

## Scheduled-workflow health (the #2634 check, run manually this run)

The check #2634 proposes does not exist yet, so I ran its logic by hand — one
pass over `/actions/workflows`, then `?event=schedule&per_page=5` per active
workflow, reporting any whose **newest scheduled run failed**. It cost one
round of API calls and reproduced all three known-red workflows without being
told about them:

| conclusion | workflow | newest scheduled run | failure streak |
|---|---|---|---|
| **failure** | Prune candidates weekly | 2026-08-31T16:26 | 2 |
| **failure** | Weekly start sweep (BAH SPY) | 2026-08-31T14:06 | >=5 (page-capped; >=16 per 2026-09-02) |
| **failure** | Weekly track pacer | 2026-08-30T06:20 | 1 |
| success | Build CI image | 2026-08-31T06:39 | — |
| success | Dependency freshness check | 2026-08-31T08:20 | — |
| success | Weekly deep health scan | 2026-08-31T15:26 | — |
| success | golden-runs-sp500-15y | 2026-09-03T13:13 | — |
| success | perf-nightly | 2026-09-03T09:19 | — |
| success | perf-weekly | 2026-08-31T07:07 | — |

**Caveat on the streak column, stated because 2026-09-02 got this exact thing
wrong:** these counts come from a 5-run page and are therefore a **floor**, not
a measurement. The 2026-09-02 summary recorded reading a pagination boundary as
the start of a streak and undercounting the sweep failures by 3x. The same
trap is live here; the real sweep figure is the previously-measured **>=16**.

None of the three has run again since 2026-08-31 — they are weekly, so today's
run reflects the same failures, not new ones. Two now have fixes in flight
(below), which is the first movement on any of them.

## The 15y golden cron fired today

2026-09-02 escalated that the `0 9 * * *` 15y golden produced `total_count: 0`
for that date. Today `golden-runs-sp500-15y` ran at **13:13Z** and **succeeded**.
One silent miss followed by a normal run — consistent with a one-off scheduler
delay rather than a broken schedule. Worth leaving on the watch list rather
than escalating.

## Step 6.2 doubled-path defect -- 9th consecutive run, re-measured

`.claude/agents/lead-orchestrator.md:1908` still prescribes:

```
dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh   -> exit 2
dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh           -> exit 0
```

Measured again this run, both arms:

```
doubled-path EXIT=2
sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh: No such file
```

The wrapper already `cd`s to the inner `trading/` workspace root (confirmed by
reading `dev/lib/run-in-env.sh`'s in-container branch), so the `trading/`
prefix is doubled. An exit 2 is indistinguishable from a check failure at a
glance, which is what makes a one-character defect worth nine mentions.

**Attempted the fix this run rather than only reporting it** — the `Edit` call
against `.claude/agents/lead-orchestrator.md` was **refused for lack of write
permission**. That is measured, not assumed: the file is `-rw-r--r-- root root`
and the refusal came from the harness permission layer, not the filesystem.
7th consecutive run blocked.

## Corroboration of #2633's root-cause hypothesis (unplanned)

2026-09-02 filed #2633 with a *prime suspect*: `actions/checkout` adds
`safe.directory` under a temporarily-overridden `HOME` and restores it, so
later steps running as `HOME=/home/opam` do not have the exception.

**The orchestrator hit this itself, on its very first `git` call this run:**

```
fatal: detected dubious ownership in repository at '/__w/trading/trading'
```

Environment confirmed directly: `whoami=root`, `HOME=/home/opam`. That is the
hypothesised mismatch reproduced in a different workflow, which upgrades #2633's
diagnosis from "prime suspect" to observed behaviour of this container image —
independent of the paired reproduction PR #2651 already carried.

## Disk

Peak **66%** (94G/145G), 51G free at run end. No ENOSPC. Held at a maximum of
**3** concurrent worktrees and removed each QC worktree as soon as its agent
returned, per the 2026-09-02 finding that six concurrent worktrees at ~13 GB
each drove disk to 100% and silently failed a commit while reporting success.
