# Health Report -- 2026-07-15 -- fast (run3)

## Summary
- Main build: PASSING (`dune build` exit 0; cold build ~2m50s)
- Main tests+linters: PASSING (`dune runtest` exit 0; ~4m)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 9686/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 4 deterministic (dune build, dune runtest, status_file_integrity, index_size_linter)
- Advisory linter output: covered by `dune runtest` exit code (0)
- Deep scan: see dev/health/*-deep.md (weekly GHA cron); most recent 2026-07-13

## Note — main-HEAD GHA CI false-red (infra, not a code red)
- Main HEAD `d8bfabb3` (run-2 summary commit #1976) shows GHA
  `build-and-test: completed/failure`. The failing job (run 29400913820,
  job 87305009054) has step #6 `dune build` stuck `in_progress` with
  `dune runtest` / `dune fmt` `pending` — the signature of a cancelled /
  timed-out runner (consistent with the known #1636 CI disk-headroom /
  ENOSPC issue), NOT a `^FAIL:` linter or test failure. Job logs have
  expired (BlobNotFound), so there is no FAIL line to cite.
- Reproduced locally on the same tree: `dune build` exit 0 AND
  `dune runtest` exit 0. The code is clean; this is an infra cancellation.
- Re-running the failed job requires an `actions:write` token scope the
  orchestrator PAT lacks (403 "Resource not accessible"). The run-3 summary
  PR will receive fresh CI on merge, producing a new green main HEAD.
- Classified `[info]`, not `[critical]` (build passes locally; no FAIL line;
  cancelled-step signature).
