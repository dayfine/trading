# Health Report -- 2026-06-11 -- fast (run 2)

## Summary
- Main build: PASSING (`dune build` exit 0)
- Main tests: PASSING (`dune runtest` exit 0; 0 `^FAIL:` lines)
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Index-size linter (post Step 5.5 reconcile): PASSING (5521/20480 bytes, all rows <= 250 chars; longest harvest-rotate row 180)
- Action required: NO

## Evidence
- Build gate (6.1): `dune build` exit 0 on HEAD `c4249670`; full `dune runtest` exit 0, `0` `^FAIL:` lines. The benign `File "."` / "No dune-project in ." line is the dune-workspace-root notice (workspace root carries `dune-workspace`), not a failure.
- GitHub CI on the identical HEAD `c4249670`: `build-and-test` SUCCESS, `perf-tier1-smoke` SUCCESS, `golden-sp500-5y` SUCCESS, `golden-custom-universe` SUCCESS; `watchdog` skipped. 0 open `ci-red` issues.
- Status integrity (6.2): covered by `dune runtest` (wired into `devtools/checks/dune`), exit 0; re-run standalone exit 0.

## Metrics
- Checks run: 3 (deterministic; build/test gate + status integrity + index-size). No agentic fast scan (retired per T3-A+).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).
