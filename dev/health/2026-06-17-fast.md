# Health Report -- 2026-06-17 -- fast

## Summary
- Main build: PASSING (CI authoritative — `CI` + `perf-tier1` COMPLETED SUCCESS on `fc81ca8a`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0; 7120/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status_file_integrity.sh, index_size_linter.sh); both exit 0.
- Build gate: did NOT run a local full `dune build && dune runtest` — CI on `fc81ca8a` is
  authoritative and GREEN (`CI` + `perf-tier1` success; `golden-runs-custom-universe` success;
  `golden-runs-sp500-5y` in_progress = the known long golden). Skipping the local full build
  avoids the 2026-06-15 runner disk-exhaustion flake, consistent with run-3 practice.
- Advisory linter output: not checked here — covered by `dune runtest` exit code in CI.
- Deep scan: see `dev/health/2026-06-15-deep.md` (weekly GHA cron). No new [medium]/[high]
  findings since.
