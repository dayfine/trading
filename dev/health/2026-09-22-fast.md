# Health Report -- 2026-09-22 -- fast

## Summary
- Main build: PASSING (`BUILD_EXIT=0`)
- Main tests: PASSING (`RUNTEST_EXIT=0`), `^FAIL:` lines: **0**
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Gates (exit codes read UNPIPED)

```
BUILD_EXIT=0
RUNTEST_EXIT=0
^FAIL: lines: 0
```

Both gates ran behind `flock /tmp/dune.lock` so they could not race the
dispatched agents. On a 15 GB memory-bound runner a silent OOM is
indistinguishable from a real failure, so serialising is the only way the
exit code means what it says.

Tree measured: `94a2e378` plus this run's uncommitted documentation edits
(daily summary, `dev/status/_index.md`, `dev/status/cleanup.md`). None of
those touch `trading/` or `analysis/`, so the build result is a clean read of
the code at that commit. Main advanced to `4afa2e44` during the run via this
run's own merges (#2898, #2903), both of which passed full CI on their own
tips before merging.

## Standalone checks (run without dune, exit codes read UNPIPED)

```
OK: all dev/status/*.md files have required fields.                                   exit 0
OK: index_size_linter -- dev/status/_index.md within limits (16468/20480, rows <=250) exit 0
OK: no-python check -- no *.py files found.                                            exit 0
```

`index_size_linter` was re-run **after** the Step 5.5 index edit, not before —
the 09-21 run recorded reading this exact check as green through a `| tail`
pipeline when it was in fact exit 1, and the 09-20 run hit the same linter on
the same row. Both readings here are unpiped.

## Metrics
- Checks run: 2 dune gates + 3 standalone (deterministic; no agentic fast scan)
- Deep scan: `dev/health/2026-09-21-deep.md` — merged to `main` this run via
  #2898. **0 critical, 10 warnings, 170 info.** Nine of the ten warnings are
  expired linter exceptions past their `review_at` date; appended to
  `dev/status/cleanup.md` §Backlog this run as a newly-actionable entry.

## Note on what a clean fast scan does and does not mean

Both of this run's substantive findings — the 09-21 run-2 summary loss and the
still-live `prior_summary_iso` defect (#2887) — came from **exercising** the
harness, not from scanning it. Neither is visible to any check in this report.
A clean fast scan is evidence about the build, not about the tooling around it.
