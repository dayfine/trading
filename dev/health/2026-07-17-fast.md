# Health Report -- 2026-07-17 -- fast

## Run-2 addendum (orchestrator run 29597183213)
- Re-verified at new HEAD `ad0b29ab` (docs: resistance-v2 confirmation grid, #1994;
  2 LOCAL commits — #1993 test specs, #1994 docs — landed since run-1's summary #1992).
- `dev/lib/run-in-env.sh dune build` → exit 0. `dev/lib/run-in-env.sh dune runtest`
  → exit 0 (no `FAIL:`; fmt_check OK, magic_numbers OK).
- Main-HEAD GHA required gates (`ad0b29ab`): `build-and-test`, `perf-tier1-smoke`,
  `golden-sp500-5y`, `golden-custom-universe` all **success**.
- Result: **CLEAN** — main GREEN, no action required.

---

## Summary
- Main build: PASSING (`dune build` exit 0, cold)
- Main tests + linters: PASSING (`dune runtest` exit 0; no `FAIL:` lines; fmt_check OK, magic_numbers OK)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 9748/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Evidence
- HEAD: `093c75b9` (ops: daily orchestrator summary 2026-07-16, #1991) — no feature inflow since the 07-16 summary merge.
- `dune build` exit 0 (cold) and `dune runtest` exit 0 (full suite + dune-wired linters). fmt_check OK; magic_numbers OK.
- `devtools/checks/status_file_integrity.sh` exit 0 — all `dev/status/*.md` have required fields.
- `devtools/checks/index_size_linter.sh` exit 0 — within limits, all table rows <= 250 chars.
- Main-HEAD GHA CI (`093c75b9`): `build-and-test`, `perf-tier1-smoke`, `golden-sp500-5y`, `golden-custom-universe`, `perf-tier2-nightly` all **success**.

## Metrics
- Checks run: 2 deterministic (build gate + status integrity) + index-size linter (no agentic fast scan).
- Advisory linter output: covered by `dune runtest` exit code (gate is exit code only).
- Deep scan: latest is `dev/health/2026-07-13-deep.md` (0 medium/high mechanical findings); next weekly deep scan via GHA cron.
