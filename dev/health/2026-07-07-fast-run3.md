# Health Report -- 2026-07-07 -- fast (run3)

## Summary
- Main build: PASSING (`dune build` exit 0)
- `dune runtest`: PASSING (exit 0) — `fmt_check` OK, `linter_magic_numbers` OK, `fn_length`/`file_length` within limits (18 declared-large of 375), no `FAIL:` lines
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (build+runtest gate; status-file integrity) — no agentic fast scan
- Sandbox-race noise: `find: … No such file or directory` on a `.sandbox/` path is the known non-FAIL infra artifact, not a gate failure (exit 0)
- Advisory linter tables (`@large-module` / `@large-function`) are declared-large markers within policy
- Deep scan: no new run this pass; last report `dev/health/2026-07-06-deep.md` (170 findings — 0 critical)
