# Health Report -- 2026-07-19 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0 on HEAD `73fd37a0`)
- Main tests + dune-wired linters: PASSING (`dune runtest` exit 0; no `FAIL:` / `LAPACKE` / `signal ILL` / Fatal-error lines)
- Status file integrity: PASSING (wired into `dune runtest`; standalone exit 0)
- Index size: PASSING (exit 0 — 10200/20480 bytes, all `_index.md` rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: deterministic (Step 6) — build gate + full runtest (linters included) + status_file_integrity + index_size_linter
- GHA CI on HEAD `73fd37a0` (#2007): `build-and-test`=success, `perf-tier1-smoke`=success, `golden-sp500-5y`=success, `golden-custom-universe`=success; `golden-sp500-15y`=in_progress (long-running, non-required); `orchestrator`=in_progress (this run); `watchdog`=skipped
- Deep scan: see `dev/health/2026-07-13-deep.md` (weekly GHA cron; 0 medium/high mechanical findings)

## Note on transient dirty-tree FAILs (not main state)
Mid-run, a dispatched harness-maintainer (GHA, plain-git, no worktree isolation)
ran `git checkout -b` in the SHARED orchestrator checkout, so a first health-gate
pass ran against a contaminated tree (agent's uncommitted `bayesian_opt.ml` edits +
mid-branch-switch state) and reported `FAIL: jj_workspace_smoke` (jj state disturbed
by the branch switch) and `FAIL: file length linter`. After recovering the tree to
clean `main` (agent edits reverted, WIP diff preserved as a patch), the re-run is
green: `dune build` exit 0, `dune runtest` exit 0. The FAILs were dirty-tree
artifacts, not a main regression. See the daily summary §Escalations for the
process fix (Step-6 gate must run strictly after dispatched build-agents finish).
