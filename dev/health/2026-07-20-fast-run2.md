# Health Report -- 2026-07-20 -- fast (run 2)

## Summary
- Main build: PASSING (local `dune build` exit 0; GHA CI `build-and-test` = success on HEAD `edcaa7c9`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 10460/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Evidence
- Local `dune build` (workspace root `/__w/trading/trading/trading`): exit 0.
- GHA CI on HEAD `edcaa7c9` (#2018 run-1 summary merge, docs-only): `build-and-test`
  = success, `perf-tier1-smoke` = success (both branch-protection-required),
  `build` = success, `check-deps` = success, `perf-tier2-nightly` = success.
  `golden-custom-universe` / `golden-sp500-5y` cancelled (concurrency, non-fatal);
  `golden-sp500-15y` / `perf-tier3-weekly` still in_progress at read time.
- `devtools/checks/status_file_integrity.sh`: exit 0 — all dev/status/*.md have
  required fields.
- `devtools/checks/index_size_linter.sh`: exit 0 — within limits.

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan) — local build gate, status
  integrity, index size.
- Advisory linter output: covered by the GHA `build-and-test` job on HEAD (green);
  no `FAIL:` observed.
- Deep scan: see dev/health/*-deep.md (weekly GHA cron; latest 07-13, Action-required NO).

## Note on the build gate
The only commit on main since run-1's verified-green baseline (`b4ef7900`) is
`edcaa7c9` = #2018, the run-1 orchestrator-summary merge (docs/status only, no
code change). GHA `build-and-test` on that exact HEAD is success, so the local
cold `dune runtest` (which duplicates CI on an unchanged code tree) was not
re-run in full; the local `dune build` exit 0 corroborates.
