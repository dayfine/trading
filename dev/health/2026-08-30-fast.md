# Health Report -- 2026-08-30 -- fast

Measured on `950a392f` (main tip at run start, GHA orchestrator run 33311750900).
Every exit code below was captured **unpiped** — no `| tail`, no `| grep` — so
the status is the command's own and not a pipeline's.

## Summary

- Main build: **PASSING** (`dune build` exit **0**)
- Main tests: **PASSING** (`dune runtest` exit **0**, **0** `^FAIL:` lines)
- Status file integrity: **PASSING** (exit **0**, corrected path — see below)
- Index size linter: **PASSING** (exit **0**, 12097/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- `^FAIL:` lines across the whole `dune runtest` output: **0**
- No `test_csv_snapshot_builder_cleanup` SIGTERM flake observed this run
  (`dev/status/cleanup.md` `flaky_test` records ~50% under load; the baseline
  gate here ran while two agent worktrees were building concurrently, so this
  is a genuine non-observation under load, not a quiet box)

## Step 6.2 path defect — 5th consecutive run

The orchestrator spec's literal invocation still fails, and its failure is
indistinguishable from a check failure at a glance:

| invocation | exit | output |
|---|---|---|
| `run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh` | **2** | `sh: 0: cannot open ... No such file` |
| `run-in-env.sh sh devtools/checks/status_file_integrity.sh` | **0** | `OK: all dev/status/*.md files have required fields.` |

`run-in-env.sh` already cds into the inner `trading/`, so the spec's
`trading/` prefix is doubled. An exit **2** reads exactly like a failing
check to anyone scanning output, which is why this keeps costing a
re-measurement. The one-line fix (drop the `trading/` prefix in
`.claude/agents/lead-orchestrator.md` Step 6.2) is drafted and verified in
both directions but remains unapplied: writing `.claude/agents/**` is gated
by a harness tool-permission prompt not granted to this runtime. **Fifth run
reporting it.**
