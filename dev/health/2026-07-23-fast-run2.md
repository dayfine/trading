# Health Report — 2026-07-23 — fast (run 2)

## Summary
- Main build: **in_progress** on `6a2d9b42` (#2047 resistance-v2 bundle
  promotion, merged 05:21Z ~10 min before this run) — NOT red at run time.
  `perf-tier1-smoke` = success; `build-and-test` + `golden-sp500-5y` +
  `golden-custom-universe` still running (maintainer merge; commit message
  reports 313/313 OUnit suites pass locally + CSV/panel bit-identical).
- Status file integrity: **PASSING** (exit 0)
- Index-size linter: **PASSING** (10741/20480 bytes, all rows ≤ 250 chars)
- Action required: **NO** (in-progress CI is not a failure; re-verify at next run)

## Metrics
- Checks run: 2 deterministic (status integrity + index size); no agentic fast scan
- Build gate: not locally runnable in the orchestrator container (no `dune`/`_build`);
  assessed via live CI (in_progress on new HEAD)
- Deep scan: latest on main is `dev/health/2026-07-13-deep.md`
  (0 critical / 32 warnings / 143 info; Action-required NO). The 07-20 deep scan
  is still an OPEN advisory PR (#2022), not yet on main.
