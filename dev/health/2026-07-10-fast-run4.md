# Health Report -- 2026-07-10 -- fast (run 4)

## Summary
- Main build: PASSING (GitHub CI on HEAD `7d14ff75`: build-and-test,
  perf-tier1-smoke, golden-custom-universe, golden-sp500-5y all `success`;
  watchdog skipped; orchestrator in_progress = this run)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 9007/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Notes
- `dune build`/`dune runtest` not runnable in the GHA runner (`dune`/`ounit2`
  absent from PATH). Authoritative main-green signal is GitHub CI on `7d14ff75`.
- The run-3 `golden-sp500-5y` `failure` (an infra runner-cancellation on a
  docs-only commit) has cleared on the newer HEAD — `success` on `7d14ff75`.

## Metrics
- Checks run: 3 (deterministic: GitHub CI status read + status_file_integrity +
  index_size_linter). No agentic fast scan (retired — see lead-orchestrator Step 6).
- Deep scan: latest dev/health/2026-07-06-deep.md; next weekly ~2026-07-13.
