# Health Report -- 2026-07-18 -- fast (run 3)

## Summary
- Main build: PASSING (`dune build` exit 0)
- Main tests + linters: PASSING (`dune runtest` exit 0 — fmt_check OK, magic_numbers OK; no `FAIL:` / `LAPACKE` / `signal ILL` lines)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 10033/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Evidence
- HEAD `010ad196` (#2001, docs-only resistance-v2 promotion inputs).
- Local `dune build && dune runtest` via `dev/lib/run-in-env.sh`: build exit 0,
  runtest exit 0. Tail: `OK: all .ml/.mli files are correctly formatted.`,
  `OK: no magic numbers found in lib/ files.`
- GHA CI on `010ad196`: `build-and-test`=success, `perf-tier1-smoke`=success,
  `golden-sp500-5y`=success, `golden-custom-universe`=success.

## Metrics
- Checks run: 4 deterministic (build, runtest+linters, status-integrity, index-size).
  No agentic fast scan (retired — deterministic checks only).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron; latest 2026-07-13).
