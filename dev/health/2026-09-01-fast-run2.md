# Health Report -- 2026-09-01 -- fast (run 2)

## Summary
- Main build: PASSING (exit 0) -- measured at run start `9fa471a0` and run end `34b4e7c2`
- Main runtest: PASSING (exit 0), **0** `^FAIL:` lines -- both measurements
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0; 12,609 / 20,480 bytes, all rows <= 250 chars)
- Action required: NO

## Measurements

| gate | run start (`9fa471a0`) | run end (`34b4e7c2`) |
|---|---|---|
| `dune build` | 0 | 0 |
| `dune runtest` | 0 | 0 |
| `^FAIL:` lines | 0 | 0 |
| `status_file_integrity.sh` | 0 | 0 |
| `index_size_linter.sh` | 0 | 0 |

Every exit code read **unpiped** (`grep -c '^FAIL:'` on the captured log, exit
code captured directly from the wrapper, never through a `| tail`).

Cross-check, independent of the local measurement: GitHub CI on `9fa471a0`
reported `build-and-test` success, `perf-tier1-smoke` success, and all three
golden workflows (`golden-sp500-15y`, `golden-sp500-5y`,
`golden-custom-universe`) success.

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)
- `test_csv_snapshot_builder_cleanup` SIGTERM flake: **not observed**, despite
  sustained 3-concurrent-agent load (~56 dune processes at peak). Sixth run
  carrying this item without a reproduction.

## Note on the Step 6.2 path defect (7th consecutive run)
`.claude/agents/lead-orchestrator.md` Step 6.2 prescribes
`run-in-env.sh sh trading/devtools/checks/...`, but the wrapper already `cd`s
into the inner `trading/`, so the prefix is doubled and the call exits **2**
(`sh: 0: cannot open`) -- indistinguishable from a real check failure. The
correct form is `run-in-env.sh sh devtools/checks/...`. Used the correct form
here. The one-line spec fix remains blocked on `.claude/agents/**` write
permission.
