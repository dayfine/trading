# Health Report -- 2026-04-20 -- fast (run-5)

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here -- covered by dune runtest exit code
- Deep scan: see `dev/health/2026-04-20-deep.md` (weekly GHA cron)

## Notes
- `dune runtest --force` on `main@origin` (HEAD=366d0e0, post-#466 merge) exits 0.
  The `linter_file_length` dune rule reported `OK: 8 declared-large of 110 total`
  even though `trading/backtest/lib/runner.ml` is 344 lines (>300 soft limit, no
  `@large-module` annotation). Running `linter_file_length.sh` directly inside
  `_build/default/devtools/checks/` FAILs on runner.ml, so the check logic is
  sound — but the dune rule only declares `_check_lib.sh` + `linter_file_length.sh`
  as deps, so changes to `lib/*.ml` do not invalidate the rule's cache and
  sandboxed execution appears to miss the growth. This is a latent dune-deps
  gap, not a current gate failure — #474 (landing now) extracts tiered_runner
  and brings runner.ml back to 287 lines, masking the bug until the next
  large-module drift. Surface as an `[info]` for harness-maintainer follow-up.
