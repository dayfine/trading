# Health Report — 2026-07-13 — fast (run-2)

## Summary
- Main build: **PASSING** — `dune build` exit 0 on main HEAD `d9072efe` (#1949).
- Full test/lint suite: `dune runtest` exit 1 — **dune sandbox-race infra flake,
  NOT a real failure** (see Details). Zero `FAIL:` lines; all linters print `OK:`.
- Status file integrity: **PASSING** — `status_file_integrity.sh` exit 0
  ("all dev/status/*.md files have required fields").
- Index size: **PASSING** — `index_size_linter.sh` exit 0 (9440/20480 bytes,
  all table rows ≤ 250 chars).
- Action required: **NO** (no code action; the runtest flake is a devtools-check
  hardening item for the harness backlog — see Escalations in the daily summary).

## Details — `dune runtest` exit 1 is the sandbox-race flake
Reproduced identically on **three independent machines/runs**, each racing a
**different** dune sandbox dir (proving non-determinism):

1. main-HEAD CI `build-and-test` (run 29236293299, job 86771629244):
   `find: '/__w/.../_build/.sandbox/d67e3f8a…': No such file or directory` → exit 1.
2. local `dune runtest` (this run): find-race at `d67e3f8a…` + `f1771cf9…` → exit 1.
3. local `dune runtest` rerun (this run): find-race at
   `621a5014…/…/snapshot/test/fixtures/ko-2024-divs` + `…/test_data` → same flake.

In every case `fmt_check`, `linter_magic_numbers`, `linter_mli_coverage`, and the
file-length/large-module marker scans printed `OK:`; **no `^FAIL:` line appears**.
The non-zero exit comes from a devtools-check `find` traversing a `_build/.sandbox/`
subtree that dune concurrently tears down — the exact `find: … No such file or
directory` signature `.claude/rules/pr-merge-gates.md` names as an admissible
infra signal. Per Step 6.1 live-evidence rule, this is classified `[info]`, **not**
`[critical]` main-red: the code compiles clean and no test/linter violation exists.

## Metrics
- Checks run: 4 (deterministic): `dune build`, `dune runtest`,
  `status_file_integrity.sh`, `index_size_linter.sh`.
- Deep scan: last `dev/health/2026-07-06-deep.md` (0 actionable [medium]/[high]);
  next weekly deep scan ~2026-07-13 via GHA cron (not orchestrator-dispatched).
