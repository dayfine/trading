# Health Report -- 2026-06-05 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate: `dev/lib/run-in-env.sh dune build` exit 0; `dune runtest` exit 0; no `^FAIL:` lines
- Status integrity: `devtools/checks/status_file_integrity.sh` exit 0 ("all dev/status/*.md files have required fields")
- HEAD: `5e10deaf` (`feat(build-snapshots): accept composition universe snapshots` #1451)
- Advisory linter output: not checked here -- covered by dune runtest exit code
- Deep scan: see dev/health/*-deep.md (weekly GHA cron)
