# Health Report -- 2026-09-13 -- fast

Orchestrator run 34757914354. Main at run start: `8c7d0ada`.

## Summary
- Main build: **PASSING** (`dune build` exit **0**)
- Main tests: **PASSING** (`dune runtest` exit **0**, **0** `^FAIL:` lines across 4,213 output lines)
- Status file integrity: **PASSING** (exit **0**)
- `index_size_linter`: **PASSING** (exit **0**, 13,999 / 20,480 bytes, all rows <= 250 chars)
- Action required: **NO**

Every exit code was read **unpiped** (`cmd > log 2>&1; echo $?`), never through a
pipe to `tail`/`head` — that trap fired on two orchestrator runs in a single day
on 2026-09-09 and both were caught only by luck.

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan)
- Baseline gate wall time: ~35 min on this runner (`dune build` + `dune runtest`
  from a cold `_build`). Consistent with the >25 min measured 2026-09-09 and with
  the D3 finding (issue #2747) that a 21-minute orchestrator step cannot possibly
  have waited for a dispatched agent.
- Deep scan: **stale — last ran 2026-08-31 (13 days).** The 09-07 weekly slot did
  not produce one. See the daily summary's Escalations.

## Step 6.2 doubled-path defect — 15th consecutive run

The form prescribed by `.claude/agents/lead-orchestrator.md` Step 6.2:

```sh
dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
# EXIT=2 -- sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh: No such file
```

fails because the wrapper has already `cd`'d into `trading/`, so the path is
applied twice.

**Measured this run rather than carried forward** — all three forms were executed
here, not quoted from the 2026-09-09 report:

| form | exit |
|---|---|
| `dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh` (prescribed) | **2** |
| `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` | **0** |
| `sh trading/devtools/checks/status_file_integrity.sh` (from repo root, no wrapper) | **0** |

Either of the latter two is a correct one-line Step 6.2 edit. Still behind the
`.claude/agents/**` write gate, re-probed and refused this run.

A note on why this was re-measured: an earlier draft of this very report asserted
the exit-2 result from the prior run's record, and a spot check of the *wrong
form* (bare, from repo root) returned 0 and appeared to contradict it. Both
readings were "right" about different commands. Re-running all three settled it.
That is the same confusion — a claim correct about the outcome, imprecise about
the mechanism — that produced three separate defects on 2026-09-09.

## Scheduled-workflow health (run this pass, not previously wired)

`trading/devtools/checks/scheduled_workflow_health.sh` (#2634/#2663) needs only
API **read** access — it does **not** need the blocked `workflow` scope. Nothing
had ever called it, because the blocked half (wiring it as its own workflow) was
taken to block the whole thing. The orchestrator can just run it, and did:

```
active=17  ok=8  red=2  stale=0  no-schedule=7   (exit 0)
RED: "Prune candidates weekly", "Weekly start sweep (BAH SPY)"
```

Both RED rows last fired **2026-09-07**, i.e. they predate their fix (#2725,
merged overnight 09-08) and cannot produce a new datapoint before their next
weekly cron. No new information; not re-derived further.

**The detector reported `Daily orchestrator` as `OK` while it was in the middle
of a six-run failure streak.** It inspects only the single most-recent scheduled
run and classifies `in_progress` as OK — so the orchestrator's own executing run
masks the orchestrator's own health, unconditionally and by construction. Fix
dispatched this run (`harness/schedhealth-streak`). See issue #2770.
