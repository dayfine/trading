# Health Report — 2026-07-08 — run2 — fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- `dune runtest` (full gate): PASSING (exit 0) — `fmt_check` OK, `linter_magic_numbers` OK, no `FAIL:` lines
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: `@large-module` (16 declared-large coordinator modules) +
  `@large-function` (1, `_ascii_chart`) tables printed — all `no-review-at`, within
  policy, not FAIL lines.
- Sandbox-race `find: … No such file` noise: known non-FAIL infra artifact (not present this run).
- Deep scan: no new run this pass; last report `dev/health/2026-07-06-deep.md` (170 findings, 0 critical).

## Notes
- HEAD verified: `2d683512` (`ops: daily orchestrator summary 2026-07-08 (#1889)`).
- Only inflow since run1's green verification is #1889 (run1's own summary, docs-only `_index.md`) — cannot affect build/tests; re-verified green anyway with live evidence.
- Open PR #1890 (`fix/warmup-364`) is a *branch* CI failure (fmt only), NOT a main-baseline failure — main is green. Diagnosed separately in the daily summary + a PR comment.
