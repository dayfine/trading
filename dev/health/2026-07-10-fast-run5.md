# Health Report -- 2026-07-10 -- fast (run-5)

## Summary
- Main build: PASSING (GitHub CI on HEAD `61702357`, all required checks `success`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 8789/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status_file_integrity.sh, index_size_linter.sh) + GitHub CI read on main HEAD
- Main HEAD `61702357` (#1917 — ops: land 5 orphaned budget records): `build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all `success`;
  `watchdog` skipped; `orchestrator` in_progress (this run).
- `dune build` / `dune runtest`: NOT runnable in the GHA runner (`dune` absent from PATH;
  `ounit2` absent). Authoritative main-green signal is GitHub CI on `61702357`.
- Advisory linter output: not checked here — covered by `dune runtest` exit code in CI.
- Deep scan: last `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13 (GHA cron).
