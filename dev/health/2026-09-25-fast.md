# Health Report -- 2026-09-25 -- fast

## Summary
- Main build: PASSING (dune build exit 0, main @ 2b0b37af)
- dune runtest: PASSING (exit 0, 0 `^FAIL:` lines)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here -- covered by dune runtest exit code
- Deep scan: see dev/health/2026-09-21-deep.md (weekly GHA cron)

## Observation
- The main-tree `dune runtest` left the orchestrator's untracked summary + audit files as
  intent-to-add (` A`) entries -- live reproduction of the defect fixed by PR #2956
  (`jj_workspace_smoke.sh` on a colocated repo). Undone with `git reset -- dev/audit dev/daily`.
