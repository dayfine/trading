# Health Report -- 2026-05-02 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity (standalone): FAILING (8 advisory FAILs on the 4
  new tracks created today; `dune runtest` exit code remains 0 because
  the integrity test wired into `dune runtest` does not gate on these).
- Action required: NO (build green; integrity drifts are advisory and
  trace to the schema mismatch on PLANNED tracks introduced 2026-05-02).

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build: `dune build` exit 0; `dune runtest --force` exit 0 (entire
  suite green, including the previously-flagged
  `test_split_day_stop_exit:1:post_split_exit_no_orphan_equity` which
  was fixed by PR #752 between the prior orchestrator run and now).
- Status integrity (standalone): exit 0 with `FAIL:` text on stdout —
  schema-drift on the four new tracks created in today's manual planning
  session:
  - `data-foundations.md`, `experiments.md`, `tuning.md`,
    `weekly-snapshot.md` — `## Status` value `PLANNED` is not in the
    enum `IN_PROGRESS|READY_FOR_REVIEW|APPROVED|MERGED|BLOCKED|PENDING`.
  - Same four files — `## Interface stable` value
    `NO — track is brand-new.` is not one of `YES|NO|N/A`.
  Either the linter enum should grow `PLANNED` (and accept a free-form
  trailing clause on `Interface stable`), or the four new files should
  be edited to use `PENDING` + `N/A`. Pre-decision; surfacing for the
  next harness slack window.
- Advisory linter output: covered by `dune runtest` exit code (none of
  the wired linters fired non-zero this run).
- Deep scan: most recent at `dev/health/2026-04-27-deep.md` (33
  findings: 0 critical, 6 warnings, 27 info — all already actioned or
  documented). Weekly cron next fires Monday 2026-05-04 15:17 UTC.
