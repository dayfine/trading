# Health Report -- 2026-08-23 -- fast

Produced by the GHA lead-orchestrator (run 32625658534), Step 6.
Deterministic checks only -- the agentic fast scan is retired (it produced
recurring false-positive `[critical]` findings); the weekly deep scan is a
separate GHA cron and is unaffected.

## Summary

- Main build: **PASSING** -- CI `build-and-test` **completed/success** on
  `main@e64f8655`, `perf-tier1-smoke` **completed/success**,
  `golden-custom-universe` **completed/success**.
- Status file integrity: **PASSING** (exit 0).
- Action required: **NO**.

## Measurement

### Build gate (Step 6.1)

A whole-repo local `dune runtest` was **deliberately not launched**, same call as
the 2026-08-21 runs. Two feat/harness agents were building concurrently in their
own worktrees on a 4-core / 15 GB runner, and a prior run was **OOM-killed (exit
137, silently)** doing exactly this. The build gate therefore rests on:

1. CI on `main@e64f8655` -- both required checks completed/success (read from
   `/commits/<sha>/check-runs`, not from a workflow-run listing).
2. Each dispatched agent running `dune build && dune runtest` to exit 0 natively
   on a superset of `main` inside its own worktree.

`golden-sp500-5y` was `in_progress` at scan time. It is **not** a required PR
gate; #2481 (merged 2026-08-22) raised its cap to 240 min after the root cause
was found to be 3 scenarios exceeding a 90-min cap since #1050.

### Shell gates (Step 6.2) -- run natively, exit codes read directly

Never piped: `cmd | tail; echo $?` reports `tail`'s status, not the check's.

| check | exit |
|---|---|
| `status_file_integrity.sh` | **0** |
| `index_size_linter.sh` | **0** |
| `no_python_check.sh` | **0** |
| `tracked_artifact_linter.sh` | **0** |
| `posix_sh_check.sh` | **0** |

### Corroborating signals

- **Zero open `ci-red` issues** (18 open issues scanned, none carries the label).
- **Zero open PRs** at run start -- an empty queue, not a saturated one.

## Metrics

- Checks run: 5 shell gates + 1 CI read (deterministic; no agentic fast scan).
- Advisory linter output: not separately assessed -- covered by the `dune runtest`
  exit code that CI's `build-and-test` already gates on.
- Deep scan: `dev/health/2026-08-17-deep.md` (weekly GHA cron, 6 days old, on
  cadence).

## Carried trap (re-stated for every agent brief this run)

`dune runtest` output on a **green** tree contains **two literal `FAIL:` strings**
that are not failures -- they sit inside `OK:` lines of the
`sete_diagnostics_check` meta-linter. `grep -c 'FAIL:'` returns **2** on a healthy
tree; `grep -c '^FAIL:'` returns **0**. Gate on the exit code, and use the
anchored grep when scanning text.
