# Health Report -- 2026-07-14 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0, main HEAD `7c3518b2`)
- Status file integrity: PASSING (exit 0; re-verified after harness.md + _index.md edits)
- Index size: PASSING (exit 0 — 9462/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (build, status-integrity, index-size); no agentic fast scan
- `dune runtest`: NOT run locally — this GHA runner is non-AVX512, so the owl/OpenBLAS
  tuner tests (`test_bayesian_opt`, `test_bayesian_runner_bin`) SIGILL ("Command got
  signal ILL") on the currently-loaded `trading-ci:latest` image. This is the #1955
  hazard, FIXED this run by #1961 (`b7628dcb`) — a `.devcontainer/Dockerfile` OWL_CFLAGS
  pin that auto-triggered `image.yml` rebuild (run 29300387751). Once the rebuilt image
  propagates, `dune runtest` / `build-and-test` stop false-redding. Until then, main-HEAD
  CI on an AVX512 runner is the runtest authority.
- Deep scan: weekly (`dev/health/2026-07-13-deep.md`, merged this run via #1951) — 0
  critical, 32 warnings, 143 info; Action required: NO.
