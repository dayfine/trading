# Health Report -- 2026-08-12 -- fast

Measured on `main@32a192ac` (GHA orchestrator run 31596975517).

## Summary
- Main build: **PASSING** (`dune build` exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0, full suite, foreground, ~19 min)
- Status file integrity: **PASSING** (exit 0, via the corrected invocation -- see below)
- Action required: **NO**

Result: **CLEAN**.

## Metrics
- Checks run: 2 (deterministic; the agentic fast scan is retired per Step 6)
- `FAIL:` lines in full `dune runtest` output: **0**
- Deep scan: latest is `dev/health/2026-08-03-deep.md` (weekly GHA cron); it carries
  no `[high]` or `[medium]` findings, so Step 2e.1 appended nothing to
  `dev/status/cleanup.md` §Backlog this run.

## GitHub CI cross-check on the same SHA
`main@32a192ac`: `CI` **success**, `perf-tier1` **success**,
`golden-runs-sp500-5y` **success**, `golden-runs-custom-universe` **success**.
Zero open `ci-red` watchdog issues. Local and remote agree.

## Step 6.2's documented invocation is still wrong -- 6th consecutive run

```
$ dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh: No such file
exit=2

$ dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
OK: all dev/status/*.md files have required fields.
exit=0
```

`run-in-env.sh` `cd`s to the **dune workspace** (`<repo>/trading`), so every path
handed to it is already relative to that directory. An orchestrator trusting the
documented form escalates a phantom `exit=2` failure. Blocked on `.claude/**`
being unwritable; carried since 2026-08-04.

## Standing environment traps re-confirmed this run

- `gh` **ABSENT**, `docker` **ABSENT**, `dune` **not on PATH**, `jj`/`jst`
  present but **BROKEN** (`H-JJ-JST-BROKEN-GHA`). Working: `git`, `curl`, `jq`,
  `python3`, `bash`, `dev/lib/run-in-env.sh`.
- `Warning: No dune-project file has been found in directory "."` is emitted on
  every `run-in-env.sh dune ...` invocation and is **cosmetic**
  (`H-RUNENV-DUNE-PROJECT-WARNING`). It has misled agents on 5+ occasions.
- `run-in-env.sh`'s worktree guard was verified empirically this run: from inside
  `/tmp/wt-probe`, the worktree-local `./dev/lib/run-in-env.sh` resolves
  `PROJECT_ROOT=/tmp/wt-probe/trading` (correct), while invoking the main
  checkout's copy by absolute path **refuses loudly** with "PROJECT_ROOT is
  ambiguous". The 2026-06-07 silent-wrong-tree contamination class is closed for
  this wrapper.
- `git`-shelling checks still pass in this container **for the wrong reason**
  (`H-GIT-SHELLING-CHECKS-NEED-SAFE-DIRECTORY`): the orchestrator's own
  `git config --global --add safe.directory` at Step 1 is inherited by every
  agent in this shared container, while CI's dune sandbox has a stripped `HOME`
  and fails with `fatal: detected dubious ownership`. CI remains the only clean
  room for that class.

## Disk
`overlay 145G total, 95G used, 51G free (66%)` at dispatch time -- ample headroom
for three concurrent agent worktrees. No ENOSPC this run.
