# Health Report -- 2026-07-08 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- Status file integrity: PASSING (`devtools/checks/status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- `dune runtest`: PASSING (exit 0) — `fmt_check` OK, `linter_magic_numbers` OK,
  no `FAIL:` lines. The `find: … No such file or directory` lines from the
  sandbox-race are known non-FAIL infra noise; the `@large-module` /
  `@large-function` advisory tables are declared-large markers within policy.
- Advisory linter output: not gated here — covered by the `dune runtest` exit code.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron); last report
  `dev/health/2026-07-06-deep.md` (170 findings — 0 critical). No new run this pass.
