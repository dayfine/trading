# Health Report -- 2026-07-29 -- fast

Orchestrator run 30458563291. Deterministic checks only (Step 6); the agentic
fast scan was retired after it produced recurring false-positive `[critical]`
findings. The weekly deep scan is unaffected -- latest is
`dev/health/2026-07-27-deep.md`, next due ~2026-08-03.

## Summary

- **Main build: PASSING.** CI on the exact starting HEAD `3da122db`:
  `build-and-test` SUCCESS, `perf-tier1-smoke` SUCCESS, `golden-sp500-5y`
  SUCCESS, `golden-custom-universe` SUCCESS. (`golden-sp500-15y` shows
  `cancelled`; it is the long-running optional golden and is not a required
  check.)
- **Status file integrity: PASSING** (exit 0, direct invocation).
- **Action required: NO.**

## Checks run

Each invoked directly and judged on its **exit code**, not on `FAIL:` text in
stdout. Deliberately not piped through `tail` -- `cmd | tail -6; echo $?`
reports `tail`'s status, not the script's, which is the
`H-CHECK-SETE-DIAGNOSTICS` sibling shape the orchestrator itself committed in
run 4.

| check | exit | result |
|---|---|---|
| `status_file_integrity.sh` | 0 | PASS -- all `dev/status/*.md` have required fields |
| `index_size_linter.sh` | 0 | PASS -- 10333/20480 bytes, all table rows <= 250 chars |
| `no_python_check.sh` | 0 | PASS -- no `*.py` anywhere |
| `check_universe_deps.sh` | 0 | PASS -- all `repo_root()`-calling scripts covered by `(universe)` or the exceptions list |
| `budget_rollup_check.sh` | 0 | PASS -- 8/8 assertions (re-run after the budget-record recovery below; still 8/8) |

`dune build` / `dune runtest` were **not** re-run locally. CI ran the full
suite on this exact HEAD and is authoritative; re-running it is the 10-to-20
minute job that `A-FINISH-PROTOCOL-BACKGROUND` identifies as the thing agents
background and then strand their work on.

## Metrics

- Checks run: 5 deterministic, 0 agentic.
- Deep scan: not due (weekly; last 2026-07-27).
- Step 2e.1 backlog refresh: **no-op** -- no `[medium]`/`[high]` findings in
  `2026-07-27-deep.md` or `2026-07-28-fast-run4.md` absent from
  `dev/status/cleanup.md`.

## Note -- budget records recovered, not a code-health finding

Five orchestrator budget records existed only on unmerged `ops/budget-*`
branches and had never reached `main`: run 4 of 2026-07-28 plus all four
2026-07-29 runs. Every one carries `"fallback_branch": "1b"`. They were
recovered into `dev/budget/` in this run's summary commit. This is recorded
here because it changes the measured daily totals the next run will read, not
because it is a health regression. See the daily summary's §Budget.
