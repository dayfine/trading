# Health Report -- 2026-04-26 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- Main test gate: FAILING (`dune runtest` exit 1) — nesting linter on `analysis/data/storage/csv/lib/csv_storage.ml:180:_stream_in_range_prices` (introduced by PR #543, merged 2026-04-24).
- Status file integrity: FAILING (`status_file_integrity.sh` exit 1) — `data-panels.md: '## Last updated' is not YYYY-MM-DD: '2026-04-25 (PR 3.3 pushed)'` (PR #573 merged the malformed timestamp; PR #575 fixes it but is awaiting human merge).
- Action required: YES — both findings surface as `[critical]` in today's daily §Escalations.

## Detailed findings

### 1. Nesting linter — `_stream_in_range_prices` (csv_storage.ml)
```
FAIL: nesting linter — 1 function(s) exceed limits:

  avg    max  else why        location                                            function
  ----------------------------------------------------------------------------------------
  3.61   9    0    avg+max    analysis/data/storage/csv/lib/csv_storage.ml:180    _stream_in_range_prices

Limits: avg > 3.0, max > 5, nested-else > 0.
Fix: refactor or add (* @nesting-ok: <reason> *) before the 'let'.
Nested-else: split the function at the else — the else body is a new fn.

Codebase avg nesting: 1.44  |  884 functions scanned  |  108 files
```

This was introduced by PR #543 (`perf(csv_storage): stream-parse get with in-range filtering (H7)`) merged 2026-04-24. The function avoids materializing the full CSV in memory but the stream-then-filter shape pushed nesting metrics over the linter limits. Two fix shapes:
- Refactor to split inner predicate into a named helper (preserves perf, drops nesting).
- Add `(* @nesting-ok: streaming filter requires inline match on parsed bar *)` annotation before the `let`.

A clean resolution should land as a `cleanup/csv-storage-nesting` dispatch on the next orchestrator run; orchestrator added a Backlog entry to `dev/status/cleanup.md` per Step 2e refresh policy (see §Cleanup-backlog refresh below).

### 2. Status file integrity — data-panels.md `Last updated` malformation
`data-panels.md` on `main@1e921de` carries `## Last updated: 2026-04-25 (PR 3.3 pushed)` — the linter requires bare `YYYY-MM-DD`. PR #573 (Stage 3 PR 3.3) merged with this malformation. The data-panels agent fixed it on PR #575's branch (sets `## Last updated: 2026-04-26`), but PR #575 is awaiting human merge.

This is a recurring drift pattern: human-merged PRs land status-file edits that fail the linter. The integrity check itself exits 1 and gates `dune runtest`, but humans can override the merge gate via admin merge. See `dev/status/harness.md` §Cleanup recurring "status-file integrity drift from human-merged PRs is a [info] follow-up — linter already exits 1 on violation; requires branch-protection config to enforce."

## Cleanup-backlog refresh

Per Step 2e.1, the orchestrator should append the nesting-linter finding to `dev/status/cleanup.md` §Backlog so the next run can dispatch `code-health` on it. Entry shape:

```
- [ ] nesting: trading/analysis/data/storage/csv/lib/csv_storage.ml — `_stream_in_range_prices` (line 180) avg=3.61 max=9 from H7 stream-parse refactor (source: 2026-04-26-fast.md)
```

(Backlog entry added inline by orchestrator this run.)

## Metrics
- Checks run: 2 (deterministic Step 6.1 build gate + Step 6.2 status integrity; no agentic fast scan)
- Both checks RED.
- Deep scan: see `dev/health/*-deep.md` (most recent 2026-04-20-deep.md; weekly GHA cron next runs Monday 2026-04-27).
