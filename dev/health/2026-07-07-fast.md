# Health Report -- 2026-07-07 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- `dune runtest`: PASSING (exit 0) — `fmt_check` OK, `linter_magic_numbers` OK,
  no `FAIL:` lines. The `find: … No such file or directory` line is the known
  sandbox-race non-FAIL infra artifact; the advisory `@large-module` (many) /
  `@large-function` (1) tables are declared-large markers within policy
  (`no-review-at`), not violations.
- Advisory linter output: not gated here — covered by the `dune runtest` exit code.
- Deep scan: see `dev/health/2026-07-06-deep.md` (weekly GHA cron; 170 findings,
  0 critical). No new deep scan this pass.
