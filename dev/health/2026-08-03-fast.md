# Health Report -- 2026-08-03 -- fast

## Summary

- Main build: **PASSING** (`bash dev/lib/run-in-env.sh dune build` exit 0 on `70f6cda9`)
- Main tests: **PASSING** (`bash dev/lib/run-in-env.sh dune runtest` exit 0 on `70f6cda9`)
- Status file integrity: **PASSING** (`OK: all dev/status/*.md files have required fields.`, exit 0)
- Action required: **NO**

## Metrics

- Checks run: 3 (deterministic; no agentic fast scan)
- Gate basis: **exit code**, not `FAIL:` text in stdout, per Step 6.1
- Deep scan: most recent is `dev/health/2026-07-27-deep.md` (7 days old — weekly
  cadence, due imminently but not yet overdue)

## GitHub CI on main tip `70f6cda9`

| Check | Status | Conclusion |
|---|---|---|
| `build-and-test` | completed | **success** |
| `perf-tier1-smoke` | completed | **success** |
| `golden-sp500-5y` | completed | **success** |
| `golden-custom-universe` | completed | **success** |
| `build` | completed | success |
| `perf-tier2-nightly` | completed | success |
| `perf-tier3-weekly` | in_progress | — |
| `orchestrator` | in_progress | — (this run) |

Both required merge-gate checks are green. No baseline breakage.

## Incidental finding (filed, not fixed here)

`.claude/agents/lead-orchestrator.md` Step 6.2 documents the status-integrity
invocation as:

```
dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
```

This **fails with exit 2** (`sh: 0: cannot open ... No such file`). `run-in-env.sh`
cds into the `trading/` dune-workspace root first, so the repo-root-relative path no
longer resolves. The working invocation is from the repo root, without the wrapper:

```
bash trading/devtools/checks/status_file_integrity.sh
```

The wrapper is unnecessary — the script needs no opam environment. Cosmetic but
misleading: a run that trusted the documented form would record a spurious
integrity failure. Logged to `dev/status/cleanup.md` §Backlog.
