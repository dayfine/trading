# Health Report -- 2026-06-04 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): `dune build` exit 0; `dune runtest` exit 0 on merged main `919e10a8` (no `^FAIL:` lines). The pre-existing benign "No dune-project file in directory '.'" warning is the repo's workspace-root layout (a `dune-workspace` marks the root; projects live in subdirs); CI builds identically.
- Status integrity (6.2): exit 0 — "all dev/status/*.md files have required fields" (re-validated; `stage-accuracy.md` added by #1446 has all required fields).
- Advisory linter output: covered by `dune runtest` exit code (0).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).
