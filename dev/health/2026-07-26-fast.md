# Health Report -- 2026-07-26 -- fast

## Summary
- Main build: **PASSING** (`dune build` exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0, zero `^FAIL:` lines)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0)
- Action required: **NO**

Baseline commit at scan time: `a559c1ed` (main, before this run's merges).

## Gate detail

| Gate | Command | Exit | Notes |
|---|---|---|---|
| Build | `dev/lib/run-in-env.sh dune build` | 0 | Only output is the benign root-dir `dune-project` warning. |
| Tests | `dev/lib/run-in-env.sh dune runtest` | 0 | Full suite incl. wired linters (`fmt_check`, `linter_magic_numbers`, `fn_length`, `nesting`, `file_length`, `mli_coverage`, `no_python_check`, `status_file_integrity`). `grep -c '^FAIL:'` = **0**. |
| Status integrity | `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` | 0 | `OK: all dev/status/*.md files have required fields.` |
| Index size | `dev/lib/run-in-env.sh sh devtools/checks/index_size_linter.sh` | 0 | `11276/20480 bytes, all table rows <= 250 chars.` |

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan)
- Gate rule applied: **exit code only**, never `FAIL:` text matching. Both
  `nesting_linter` and `linter_magic_numbers` do exit 1 on violations, so the
  exit code is the authoritative signal.
- Ordering: Step 6 was run **strictly before** any build-agent dispatch, per the
  process fix filed in `harness.md` after the 2026-07-19 dune-lock incident. The
  two QC agents dispatched afterwards were explicitly instructed **not** to run
  `dune`, so no concurrent-lock contention was possible.

## Path corrections carried from prior runs
`lead-orchestrator.md` Step 6.2 documents the integrity linter as
`run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`. That form
exits **2** — the wrapper already `cd`s to `<repo>/trading`, so the correct path
is `devtools/checks/status_file_integrity.sh`. Used the corrected form here
(exit 0). Unchanged from the 2026-07-25 report; still worth a harness fix.

## Deep scan
See `dev/health/2026-07-20-deep.md` (178 findings / 33 warnings). That report had
been stranded on unmerged PR #2022 for 6 days, which is why `dev/status/cleanup.md`
§Backlog read empty. #2022 was picked up for merge this run — see the daily summary.
