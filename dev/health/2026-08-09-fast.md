# Health Report -- 2026-08-09 -- fast

## Summary
- Main build: **PASSING** (exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0, measured post-merge on `main` at `2c84d8c4`)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (12653/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan per Step 6)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here

## Notes

- **Step 6.2's documented invocation is still wrong — 4th consecutive run.**
  `dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`
  exits **2** (`sh: 0: cannot open ...: No such file`) because `run-in-env.sh`
  resolves `PROJECT_ROOT` to `<repo>/trading`. The correct form is
  `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` (exit 0).
  An orchestrator trusting the documented form would escalate a phantom failure.
  Fix is blocked on `.claude/**` write access.

- **New this run — tree-cleanliness after a build is now a live property of `main`.**
  PR #2248 gitignored `trading/compile_commands.json`. Verified post-merge on
  `main`: clean tree -> full `dune build && dune runtest` -> `git status --porcelain`
  still clean (no artifact churn). Previously every build dirtied the tree.

- **New this run — an intermittently-failing test was observed.**
  `test_csv_snapshot_builder_cleanup` (SIGTERM-based) failed for one QC agent
  before any mutation was applied, while three other independent full runs on the
  same SHA were exit 0. Filed to `dev/status/cleanup.md` §Backlog as `flaky_test`.
  Relevant because every merge gate here is an exit-code gate.
