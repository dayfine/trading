# Health Report — 2026-06-11 — fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (covered by `dune runtest`, exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): `dune build` exit 0 + full `dune runtest` exit 0 on HEAD
  `53822998`; `0` `^FAIL:` lines. The "No dune-project file in directory ."
  warning is the benign dune-workspace-root notice (the workspace root carries
  `dune-workspace`; sub-projects carry `dune-project`), not a failure.
- Status integrity (6.2): wired into `dune runtest` via `devtools/checks/dune`,
  exit 0.
- Index-size linter (post Step 5.5 reconcile): exit 0 (5333/20480 bytes, all
  rows ≤ 250 chars).
- Advisory linter output: not separately checked — covered by `dune runtest`
  exit code (fmt_check, magic-numbers, nesting, fn_length all wired into runtest).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here.
