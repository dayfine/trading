# Health Report — 2026-05-14 — fast (run-2)

## Summary
- Main build: PASSING (exit 0; `dune build` clean on HEAD `2189aa1`)
- Main test suite: PASSING (exit 0; `dune runtest --force` clean)
- Status file integrity: PASSING (exit 0; `devtools/checks/status_file_integrity.sh`)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
  - Step 6.1 `dune build && dune runtest --force` — exit 0
  - Step 6.2 `devtools/checks/status_file_integrity.sh` — exit 0
- Advisory linter output: covered by `dune runtest` exit code (nesting / magic-numbers / mli-coverage / fn-length / cc all gated via `dune runtest devtools/checks/`)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron — see `.github/workflows/health-deep-weekly.yml`)
