# Health Report -- 2026-07-23 -- fast (run-5)

## Summary
- Main build: PASSING (live CI on HEAD `90f31964`, #2052)
- Status file integrity: PASSING (exit 0)
- Index-size linter: PASSING (10507/20480 bytes, all rows <= 250)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status-file integrity + index-size linter);
  no agentic fast scan (retired).
- Local `dune build`/`dune runtest` gate: not runnable in the orchestrator
  container (no `dune`/`_build`); assessed via live CI on HEAD `90f31964`:
  `build-and-test`=success, `perf-tier1-smoke`=success, `golden-sp500-5y`=success,
  `golden-custom-universe`=success (`orchestrator`=in_progress = this run).
- Advisory linter output: covered by the green `build-and-test` CI check.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron; latest PR #2022,
  advisory/human-merged).
