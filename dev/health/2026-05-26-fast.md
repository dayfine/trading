# Health Report — 2026-05-26 — fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here — covered by dune runtest exit code
- Deep scan: see dev/health/2026-05-25-deep.md (weekly GHA cron)

## Notes (run-1)
- Main HEAD at scan time: `b607f603` (`feat(orchestrator): 4x daily cron + v7 sweep speedup plan + status entries` #1316).
- Two PRs open + APPROVED awaiting merge gate: #1317 (`harness/sweep-parallel-6`, Win #2), #1318 (`feat/sweep-perf-active-through-prune`, Win #4).

## Run-2 update

- **Red-main caught + fixed at session entry.** Main was actually RED at orchestrator-2 start (`c67b07cf`); `status_file_integrity.sh` failed because run-1's status-file edit added a parenthetical to `## Last updated:`. Shipped one-line fix as PR #1321; re-verification post-merge: GREEN at `af45cfc4`. Re-verification of CI on subsequent merges (`c53bfa7d` Win #3, `a3dcca7c` Win #3 fix): `build-and-test` + `perf-tier1-smoke` both completed/success.
- **Latent regression caught + fixed.** Post Win #3 merge at `c53bfa7d`, the `Build CI image` workflow (NOT a required gate; only `build-and-test` + `perf-tier1-smoke` are) failed because the agent's Dockerfile referenced a non-existent opam package (`ocaml-variants.5.3.0+flambda` for the 5.3 series). Corrected via PR #1324 (verified package existence against opam-repository REST API; the canonical 5.3 flambda form is `ocaml-variants.5.3.0+options,ocaml-option-flambda`).
- **Status file integrity:** PASSING on the working tree at end of run-2 reconcile (`sh trading/devtools/checks/status_file_integrity.sh` exit 0).
- **Action required:** NO (build gates clean; the manual ghcr.io devcontainer image rebuild is a maintainer follow-up, not a CI-gate blocker).
