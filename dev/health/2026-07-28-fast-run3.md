# Health Report -- 2026-07-28 -- fast (run 3)

## Summary

- Main build: **PASSING**
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0)
- No-python check: **PASSING** (exit 0)
- Action required: **NO** (for main's health; see the daily summary's §Escalations
  for the workflow-level defect this run diagnosed, which is not a code-health item)

## Evidence

**Main build gate — CI on the exact starting HEAD `9f50de92`, not a local re-run:**

| workflow | conclusion |
|---|---|
| `build-and-test` | SUCCESS |
| `perf-tier1-smoke` | SUCCESS |
| `golden-sp500-5y` | SUCCESS |
| `golden-custom-universe` | SUCCESS |

CI evidence is used deliberately in preference to a local `dune runtest`, for the
two reasons runs 7, 1 and 2 gave: it is strictly stronger (the full linter suite in
a clean container), and a concurrent OCaml build on this 4-CPU/15-GB box while two
subagents are building is how the 2026-07-27 run 7 manufactured a misleading
`exit 137`. Two agents were building concurrently for most of this run.

**The three cheap deterministic checks were run directly**, outside dune, and their
exit codes read (not their stdout text):

```
bash trading/devtools/checks/status_file_integrity.sh
  OK: all dev/status/*.md files have required fields.
  exit=0

bash trading/devtools/checks/index_size_linter.sh
  OK: index_size_linter -- dev/status/_index.md within limits
      (10048/20480 bytes, all table rows <= 250 chars).
  exit=0

bash trading/devtools/checks/no_python_check.sh
  OK: no-python check -- no *.py files found.
  exit=0
```

Running them **directly rather than through `dune runtest`** is not incidental this
run. All three are among the 23 scripts identified below whose dune rules lack
`(universe)`, so a `dune runtest` invocation of them can be served from a warm
cache without executing. Invoking the scripts directly bypasses that entirely and
is the only form of this gate currently worth trusting.

## Metrics

- Checks run: 4 (3 deterministic scripts + CI evidence for the build gate)
- Agentic fast scan: not run (retired; Step 6 is deterministic)
- Deep scan: see `dev/health/2026-07-27-deep.md` (weekly GHA cron)
- Step 2e.1 backlog refresh: **no-op** -- no new `[medium]`/`[high]` findings in the
  latest deep or fast reports that are absent from `dev/status/cleanup.md` §Backlog

## Measured finding: the cache-blind check class, quantified

Not a main-health failure, but measured during this run's pre-flight and recorded
here because it bounds how much the `dune runtest` half of the build gate is
currently worth. In `trading/devtools/checks/`:

- **25** `*.sh` scripts call `repo_root()` — i.e. they read files above the dune
  workspace root, which no `(deps ...)` entry can name.
- The directory's `dune` has **35 rules**; only **3** mention `(universe)`.
- **23 of the 25** have a rule lacking `(universe)` and are therefore silently
  cache-skippable on a warm `_build`.
- Only `_check_lib.sh` and `run_in_env_root_check.sh` are covered, both as a result
  of PR #2143.

This confirms the "~25" estimate in `H-CHECK-CACHE-BLIND` (`dev/status/harness.md`)
at 23 exposed. The exposed set includes load-bearing CI gates —
`status_file_integrity.sh`, `index_size_linter.sh`, `no_python_check.sh`,
`posix_sh_check.sh`. Dispatched to `harness-maintainer` this run (PR #2148).
