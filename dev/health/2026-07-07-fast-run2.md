# Health Report -- 2026-07-07 -- fast (run 2)

## Summary
- Main build: PASSING (`dune build` exit 0)
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0; also wired into `dune runtest`)
- `dune runtest`: PASSING (exit 0) -- `fmt_check` OK, `linter_magic_numbers` OK, no `FAIL:` lines
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan) -- `dune build`, `dune runtest`, `status_file_integrity.sh`
- Advisory linter output: `@large-module` (24 markers) + `@large-function` (1 marker) tables emitted by the fn/file-length linters -- all declared-large markers within policy (`no-review-at`), not violations
- Sandbox-race `find: … No such file or directory` line: known non-FAIL infra artifact, ignored
- Deep scan: no new run this pass; last report `dev/health/2026-07-06-deep.md` (170 findings, 0 critical)
