# Health Report -- 2026-07-11 -- fast (run-4)

## Summary
- Main build: **PASSING** (`dune build` exit 0)
- Main test+lint suite: **PASSING** (`dune runtest` exit 0)
- Status file integrity: **PASSING** (exit 0)
- Index size: **PASSING** (exit 0)
- Action required: **NO**

## Notable correction this run
`dune` IS available in the GHA runner via `eval $(opam env)` (dune 3.24.0).
The prior ~18 GHA runs' claim that "dune is absent" was wrong — they did not
source the opam env. This run RAN the full Step 6.1 build gate locally
(`dune build && dune runtest`, both exit 0), so main-baseline health is
verified directly, not only inferred from CI. Path note: the dune workspace
root is `${GITHUB_WORKSPACE}/trading` (double-nested repo; the `dune-workspace`
file lives there; `dune-project` files are in subdirs — the "No dune-project
in '.'" warning at the workspace root is benign for a multi-project workspace).

## Metrics
- `dune build`: exit 0 (warm _build cache; ~30s)
- `dune runtest`: exit 0 (~250s) — full OUnit suite + dune-wired linters
  (`fmt_check`, `linter_magic_numbers`, `fn_length`, `nesting`, `mli_coverage`,
  `status_file_integrity`) all pass. `find: ... No such file or directory`
  lines in the log are the known benign sandbox-race artifact, not test
  failures (RUNTEST_EXIT=0, zero `^FAIL:` lines).
- `status_file_integrity.sh`: exit 0 (standalone)
- `index_size_linter.sh`: exit 0 (8862/20480 bytes, all rows <= 250 chars)
- Deep scan: see `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13.

## Result
**CLEAN** — main is unambiguously green, verified by a local full build+test
this run and by GitHub CI (`build-and-test`, `perf-tier1-smoke`,
`golden-custom-universe` `success` on `e086ec5f`; `golden-sp500-5y`
in_progress).
