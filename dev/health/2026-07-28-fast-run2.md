# Health Report -- 2026-07-28 -- fast (run 2)

Gates evaluated on `main` @ `16d28ed4` (the exact HEAD this run started from),
GHA orchestrator run 30380136239.

## Summary

- Main build: **PASSING** — `build-and-test` SUCCESS on `16d28ed4`
- Main tests: **PASSING** — same check (`dune runtest` + full linter suite inside it)
- Status file integrity: **PASSING** (exit 0, run directly in this container)
- Index size linter: **PASSING** (exit 0, 10048/20480 bytes, all rows <= 250 chars)
- Action required: **NO** (no code-health finding); see §Escalations in the daily
  summary for a workflow-level defect found this run that is not a code gate.

## Metrics

- Checks run: 4
- Golden-run workflows on the same HEAD: `golden-sp500-5y` SUCCESS,
  `golden-custom-universe` SUCCESS, `perf-tier1-smoke` SUCCESS
- Open `- [ ]` follow-up items across `dev/status/*.md`: **46**
  (harness 20, cleanup 10, tuning-methods 4, all-eligible 3, +9 across 6 files)
- New `[medium]`/`[high]` findings requiring a `dev/status/cleanup.md` §Backlog
  entry: **none** — Step 2e.1 was a no-op this run.

## Why the build gate is CI evidence rather than a local re-run

Same substitution runs 7 and run 1 made, for the same two reasons, and it is
worth restating rather than letting it become an unexamined habit:

1. **It is strictly stronger evidence.** `build-and-test` on `16d28ed4` runs the
   full linter suite (`nesting_linter`, `linter_magic_numbers`, `fn_length_linter`,
   `file_length_linter`, `linter_mli_coverage`, `no_python_check`,
   `status_file_integrity_linter`) in a clean container. A local `dune runtest`
   here reproduces a subset.
2. **A local re-run would have been actively misleading.** Up to three agents were
   compiling concurrently on a 4-CPU / 15-GB box for most of this run. Run 7's
   local gate was OOM-killed (exit **137**) under exactly this load, and a naive
   reading of 137 reports "main is RED" when nothing is wrong with main.

The two checks that *are* cheap and deterministic (`status_file_integrity.sh`,
`index_size_linter.sh`) were run directly in this container and both exited 0.

## Not a code-health finding, but found by the same sweep

The measured-cost record on `main` was **systematically incomplete**: 31
`ops/budget-*` fallback branches had accumulated since 2026-07-14 holding
$283.31 of measured spend that no PR was ever opened for. Recovered onto main in
this run's summary PR. Root cause and the corrected daily totals are in the daily
summary under §Budget and §Escalations — recorded there rather than here because
it is a workflow defect, not a repository-health signal.
