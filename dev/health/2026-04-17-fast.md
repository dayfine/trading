# Health Report -- 2026-04-17 -- fast (run 2)

## Summary
- Findings: 2  (critical: 1  warnings: 1  info: 0)
- Main build: FAILING
- Action required: YES

## Critical (requires immediate action before next orchestrator run)

1. **Main build FAILING on function length linter** -- `main@origin` (commit `0799b72`)
   - `analysis/scripts/fetch_finviz_sectors/lib/fetch_finviz_sectors_lib.ml:167` function `run` is 102 lines (limit 50)
   - `analysis/scripts/universe_filter/test/test_universe_filter.ml:458` function `test_keep_if_sector_rescues_reits` is 69 lines (limit 50)
   - Recommended action: Refactor these functions to be under 50 lines, or add `(* @large-function: <reason> *)` annotation before the `let` keyword, or add exception entries to `trading/devtools/checks/linter_exceptions.conf` with a clear justification.
   - Note: This is a gate failure (dune runtest exit code 1). These files appear to have been added/modified recently; the previous successful build (2026-04-08) did not have these violations.

## Warnings (should be addressed within 1 week)

1. **Pre-existing nesting linter warnings** -- 45 functions exceed nesting limits across 8 files in `analysis/scripts/` and `trading/weinstein/strategy/`
   - Already flagged in previous scans; remains in `dune runtest` output but does not block the gate (exit 0 for nesting linter).
   - Note: Nesting is separate from function length; the function length violations above are the actual gate failure.

## Info (no immediate action; awareness only)

1. **Status file schema — previous run finding clarified** -- `backtest-scale.md` and `short-side-strategy.md` are COMPLIANT (not violations)
   - Both files have `## Status: PENDING` and `## Interface stable: N/A — not started`, which are valid per the schema (N/A is allowed when Status is PENDING).
   - The previous run's report incorrectly flagged these as violations. This run confirms they pass `status_file_integrity.sh`.

## Check Details

### Step 1: Stale review check
CLEAN. `backtest-infra.md` is READY_FOR_REVIEW with review dated 2026-04-17 20:02 (today). `support-floor-stops.md` is MERGED (no review required).

### Step 2: Main build health
FAILING. `dune runtest` exits with code 1. Function length linter reports 2 violations on main@origin.

```
FAIL: function length linter — functions exceeding 50 lines:
  ../../analysis/scripts/fetch_finviz_sectors/lib/fetch_finviz_sectors_lib.ml:167: 'run' is 102 lines (limit 50)
  ../../analysis/scripts/universe_filter/test/test_universe_filter.ml:458: 'test_keep_if_sector_rescues_reits' is 69 lines (limit 50)
```

### Step 3: New magic numbers
SKIPPED. Function length failure blocks full test suite completion. When function length is resolved, magic numbers check will run as part of standard `dune runtest`.

### Step 4: Status file integrity
PASSING. `status_file_integrity.sh` exits 0. All files have required fields and valid values (including PENDING + N/A pairs).

```
OK: all dev/status/*.md files have required fields.
```

### Step 5: Linter exceptions past review date
CLEAN. All `review_at:` annotations use milestone-based or condition-based triggers. No YYYY-MM-DD date entries. Zero expired entries.

## Metrics
- Open follow-up items: 4 (within threshold of 10)
- Linter exceptions past review date: 0 (all valid)
- Stale reviews: 0 (backtest-infra.md review is fresh today)
- Main build status: FAILING (gate blocker)
