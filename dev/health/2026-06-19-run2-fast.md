# Health Report -- 2026-06-19 -- fast (run 2)

## Summary
- Main build: PASSING (CI authoritative — main GREEN on HEAD `8065c7d9`: build-and-test + perf-tier1-smoke + golden-sp500-5y + golden-custom-universe all SUCCESS)
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0)
- Index size: PASSING (`index_size_linter.sh` exit 0 — 8282/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status-integrity, index-size) + CI build gate (authoritative on HEAD)
- Advisory linter output: not checked here — covered by `dune runtest` exit code on CI (green)
- Deep scan: see `dev/health/2026-06-15-deep.md` (weekly GHA cron; 0 critical, Action-required NO)

## Note (not a main-health finding)
- Open PR #1659 (`feat/short-sleeve`, default-off short-side feature) has CI RED on its tip
  `44eed707`, **solely** on the `dune build @fmt` step — 3 of its files
  (`weinstein_strategy_config.mli`, `weinstein_strategy_screening.ml`,
  `test_weinstein_strategy.ml`) are not `dune fmt`-clean. This is a PR-side mechanical miss,
  not a main-branch regression. The "trade #0 differs" line in `test_determinism.exe` is an
  EXPECTED divergence the test asserts (start-shift cases), and that suite reports OK.
  Pre-existing ocamlformat "Invalid documentation comment / Unpaired ']'" warnings on
  `laggard_cf.mli/ml` + `regression.ml` are NOT from this PR and are non-blocking warnings.
