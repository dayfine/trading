# Health Report -- 2026-08-18 -- fast

Deterministic (Step 6). No agentic fast scan — that was retired after it produced
recurring false-positive `[critical]` findings. The weekly **deep** scan is a
separate GHA cron and is not dispatched here; the most recent is
`dev/health/2026-08-17-deep.md`.

Measured on `main` at `7ce138b9`, run to completion, reading **real exit codes**
(not `| tail` output, which captures `tail`'s status rather than the command's).

## Summary

- **Main build: PASSING** — `dune build` exit **0**
- **Main tests: PASSING** — `dune runtest` exit **0**, **zero** `^FAIL:` lines
- **Format: PASSING** — `dune build @fmt` exit **0**
- **Status file integrity: PASSING** — exit **0**
- **Action required: NO**

## Gates

| gate | exit | note |
|---|---|---|
| `dune build` | **0** | |
| `dune runtest` | **0** | zero `^FAIL:` lines; `fmt_check.sh` and `linter_file_length.sh` both report OK inside it |
| `dune build @fmt` | **0** | see the odoc warning below |
| `status_file_integrity.sh` | **0** | `OK: all dev/status/*.md files have required fields.` |
| `index_size_linter.sh` | **0** | `10792/20480 bytes, all table rows <= 250 chars` |
| `no_python_check.sh` | **0** | `no *.py files found` |

`linter_file_length.sh` scope counts, recorded so a future project-root change
has a canary to compare against: **17 declared-large of 456** `lib/*.ml`,
**0 declared-large of 445** `lib/*.mli`.

## CI cross-check

`main` @ `7ce138b9`: `build-and-test` **completed/success**, `perf-tier1-smoke`
**completed/success**, `golden-custom-universe` **completed/success**,
`golden-sp500-5y` **completed/success**. Zero open `ci-red` watchdog issues.

Local and CI agree. Recording both because they have disagreed before — an
intermittent `test_csv_snapshot_builder_cleanup` failure correlates with **load**
(`dev/status/cleanup.md`, `flaky_test`), so a green measured on an idle runner is
not the same claim as a green measured under three concurrent cold builds.

## Warning observed (non-blocking, exit 0)

`dune build @fmt` emits:

```
Warning: Invalid documentation comment:
File "trading/weinstein/strategy/lib/weinstein_strategy_config.mli", line 1458,
characters 25-26: Paragraph should begin on its own line.
```

Non-fatal — the gate exits 0. Filed rather than fixed: it is adjacent to the
three unresolvable odoc refs #2365 corrected last run in the same file family,
and `weinstein_strategy_config.mli` had reached ~1,450 lines by 2026-08-09,
which is itself the subject of `.claude/rules/experiment-flag-discipline.md`
Rule 4 (retire dead-mechanism docstrings). Logged to `dev/status/cleanup.md`.

## Metrics

- Deterministic checks run: **6** (3 dune targets + 3 shell gates)
- Agentic fast-scan dispatches: **0** (retired by design)
- Advisory-vs-gating: exit code is the only gate. `nesting_linter` and
  `linter_magic_numbers` **do** exit 1 on violations (verified run 24644964113,
  2026-04-20); the CC linter writes a metrics JSON and always exits 0. All are
  wired into `dune runtest`, which exited 0.
