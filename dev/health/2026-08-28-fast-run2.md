# Health Report — 2026-08-28 — fast (run 2)

Deterministic post-run health check (orchestrator Step 6). No agentic fast scan —
that was retired for producing false-positive `[critical]` findings; the weekly
deep scan is unaffected.

**Measured at:** `main` = `5c20fe07` (`ops: daily orchestrator summary 2026-08-28`, #2586)

## Summary

- **Main build:** PASSING — `dune build` exit **0**
- **Main tests:** PASSING — `dune runtest` exit **0**, **0** `^FAIL:` lines
- **Status file integrity:** PASSING — exit **0** ("all dev/status/*.md files have required fields")
- **Index size linter:** PASSING — exit **0** (12071/20480 bytes, all rows ≤ 250 chars)
- **Action required:** NO

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- Gate rule applied: **exit code only**, not `FAIL:` text in stdout. Each check was
  additionally run unpiped so the captured status is the script's, not `tail`'s
  (the `| tail` trap that produced a false `exit=0` reading in run 1 today).
- Independent corroboration: GitHub CI on `5c20fe07` reports `build-and-test`
  **success**, `perf-tier1-smoke` **success**, `golden-sp500-5y` **success**,
  `golden-custom-universe` **success**.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here.

## Carried measurement artifact — Step 6.2 path defect, 4th consecutive run

The orchestrator spec's Step 6.2 snippet invokes
`run-in-env.sh sh trading/devtools/checks/<script>.sh`. `run-in-env.sh` already cds
into the inner `trading/`, so the prefix is doubled. Re-measured both ways this run:

| invocation | exit |
|---|---:|
| `sh trading/devtools/checks/status_file_integrity.sh` (as the spec says) | **2** — `sh: 0: cannot open` |
| `sh devtools/checks/status_file_integrity.sh` (corrected) | **0** |

An exit **2** reads exactly like a check failure at a glance. This run used the
corrected path for all four checks. The doubled prefix remains **correct** in Step 5
Stage 4's `record_qc_audit.sh` / `write_audit.sh` calls, which run under plain `bash`
from the repo root — so the fix is one line, not a global replace.

The one-line fix (`.claude/agents/lead-orchestrator.md`, Step 6.2 snippet) is drafted
and verified both ways but remains **unapplied for a fourth run**: writing to
`.claude/agents/**` is gated by a harness write-permission prompt that has not been
granted. See the daily summary's Escalations.
