# Health Report -- 2026-06-14 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): `dune build` exit 0 + full `dune runtest` exit 0 on `c019db11` (run-start main); 0 `^FAIL:` lines. (`find: ... No such file` lines are the benign sandbox-cleanup race; `File "."` / `No dune-project` are benign workspace-root notices.)
- Status integrity (6.2): covered by green `dune runtest`; re-confirmed via `dune runtest devtools/checks` exit 0 after the Step 5.5 `_index.md` reconcile (nesting / fn-length / file-length / mli-coverage / fmt / magic-numbers all OK; index-size longest row 230 ≤ 250).
- Post-merge: #1574 (Win #4 wiring, opt-in default-off bit-equal) merged with required CI green → `1b7a88c2`; #1575 (NS3 CancelExit, behavior-identical no re-pin) merged with required CI green → `fae3f34a`. Both no-op-default → main stays GREEN.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).

## Result
CLEAN
