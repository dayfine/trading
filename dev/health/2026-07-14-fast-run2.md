# Health Report -- 2026-07-14 -- fast (run-2)

## Summary
- Main build: PASSING (`dune build` exit 0, main HEAD `0a2e4562`)
- Status file integrity: PASSING (exit 0, re-verified after run-2 reconcile edits)
- Index size: PASSING (exit 0 — 9653/20480 bytes, all rows <= 250)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (dune build, status_file_integrity.sh, index_size_linter.sh)
- `dune runtest`: not run locally this run — main-HEAD CI (`build-and-test` +
  `perf-tier1-smoke` + golden-sp500-5y + golden-custom-universe) is GREEN on
  `0a2e4562`, the post-#1961 owl-fixed rebuilt image. Per lead-orchestrator Step 6,
  main-HEAD CI is the runtest authority; the owl/AVX-512 SIGILL lottery that
  previously false-red `dune runtest` on non-AVX512 runners is now closed.
- Advisory linter output: covered by CI `build-and-test` (green on HEAD).
- Deep scan: `dev/health/2026-07-13-deep.md` (merged via #1951; 0 critical,
  32 warnings, 143 info; Action required NO). Next weekly deep scan on cron.
