# Health Report — 2026-06-15 — fast

## Summary
- Main build: RECOVERED — was FAILING on `a05d9479` (#1588) due to a CI-infra
  disk-exhaustion flake; cleared by merging CI-green #1590 → fresh CI on
  `567d7760` PASSING.
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0; 7248/20480 bytes, all rows ≤ 250)
- Action required: NO (red main resolved this run)

## Detail — red-main diagnosis (Step 6.1)
- HEAD `a05d9479` (#1588): `build-and-test` = **failure**. Watchdog issue #1591 open.
- Root cause: `dune_trace_write(): No space left on device` (×3) during the
  native link step → cascade of `collect2: error: ld returned 1 exit status` /
  `Error: Error during linking`. This is a GHA runner **disk-exhaustion** infra
  flake, NOT a code failure:
  - No `FAIL: <linter>` triggered (the single `FAIL:` log line is the AVX-512
    guard's `echo` command text in the step definition, not output).
  - No OUnit test failure.
  - `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all SUCCESS.
  - #1588's own PR-branch CI was green on `cd87d217` (per run-4 summary).
- Matches `pr-merge-gates.md` §"When fail is admissible" (verifiable infra
  signal = "No space left on device"; PR content QC-APPROVED; the merge is the
  unblock).
- Remediation: token lacks `actions:write` (rerun-failed-jobs → 403), so cleared
  via the self-heal path — merged the CI-green, mergeable, docs-only run-4 summary
  PR #1590 → new commit `567d7760` on main → fresh `build-and-test` on a clean
  runner.

## Metrics
- Checks run: 2 deterministic linters (status integrity + index size) — both exit 0.
- Build gate: delegated to GHA CI (the only place the full linter surface runs).
- Deep scan: see dev/health/*-deep.md (weekly GHA cron) — not run here.

## Carried trend watch
- File-length: `panel_runner.ml` `@large-module` (341 lines); ~18 declared-large
  of ~320 lib modules. A proactive extraction pass remains advisable. No source
  landed this run.
- **Infra:** disk-exhaustion on the `build-and-test` runner during the link of
  many `.exe` targets is a recurring capacity risk (seen this run on `a05d9479`).
  Worth a harness follow-up (runner free-disk reclaim before the build step, or a
  larger runner) so a transient full disk stops red-flagging main. Surfaced in
  §Escalations.
