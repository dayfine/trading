# Health Report -- 2026-07-13 -- run3 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (9440/20480 bytes, all rows <= 250)
- Action required: NO (build/integrity green); one repo-wide CI-infra hazard escalated (see below)

## Checks (deterministic; no agentic fast scan)
- `dune build` (build gate): exit 0 — PASSING.
- `dune runtest`: NOT run locally. This orchestrator runner has no `avx512*`
  flags in `/proc/cpuinfo`; running the tuner test exes (which link owl →
  OpenBLAS) would SIGILL and produce a false main-red. Authoritative main-HEAD
  `build-and-test` is GREEN via the GH REST check-runs API (sha 871513dc, ran on
  an AVX-512-capable runner).
- `status_file_integrity.sh`: exit 0 — PASSING.
- `index_size_linter.sh`: exit 0 — PASSING (9440/20480 bytes).

## Notable infra finding (escalated [high], NOT main-red)
- Nondeterministic `build-and-test` SIGILL: `test_bayesian_opt.exe` +
  `test_bayesian_runner_bin.exe` link owl/OpenBLAS, whose AVX-512 kernels
  raise `Command got signal ILL` on non-AVX512 `ubuntu-latest` runners. The
  existing ci.yml AVX-512 guard covers only `libta-lib.so`, not OpenBLAS. Reds
  CI at random depending on runner CPU assignment (main went green because its
  CI landed on a compatible runner; PR #1951's two runs did not). Logged as a
  Follow-up item in `dev/status/harness.md`. Fix is PAT/image-blocked for GHA
  agents (same class as #1636).

## Weekly deep scan
- `dev/health/2026-07-13-deep.md` produced by the weekly cron (open in PR #1951;
  0 critical, 32 warnings — 28 recurring design-doc-drift + follow-up-accumulation
  (54) + 2 manual-review linter-exception entries — 143 info; Action required: NO).
  PR #1951 is HELD (docs-only, but `build-and-test` red via the owl/OpenBLAS
  SIGILL hazard above — not merged per pr-merge-gates.md hard rule).
