# Health Report -- 2026-08-05 -- fast

Measured post-merge on `main` at `6cf8773d` (both this run's PRs landed).

## Summary
- Main build: **PASSING** (`dune build` exit 0)
- Test suite: **PASSING** (`dune runtest` exit 0, zero `FAIL:` lines)
- Status file integrity: **PASSING** (exit 0 -- all `dev/status/*.md` have required fields)
- Index size linter: **PASSING** (11687/20480 bytes, all table rows <= 250 chars)
- Action required: **NO**

## Metrics
- Checks run: 3 deterministic (build+runtest, status integrity, index size). No agentic fast scan -- retired per T3-A+.
- Gate basis: exit code only, never `FAIL:` text. Verified `$?` read directly from dune, not through a pipe.
- Pre-dispatch baseline was measured separately at `04e2c75b` (build 0 / runtest 0) so any red during the run would have been attributable to this run's work.
- Deep scan: see `dev/health/2026-08-03-deep.md` (weekly GHA cron; 0 critical, 4 warnings -- warnings 3 and 4 were closed by #2200 on 2026-08-04).

## Note
`devtools/checks/status_file_integrity.sh` must be invoked as
`dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` -- the path in
`lead-orchestrator.md` Step 6.2 (`trading/devtools/checks/...`) is wrong, because
`run-in-env.sh` already cd's into `trading/`. The documented invocation silently
prints "No such file" while `$?` reads 0 from the pipeline tail. Fix is queued but
blocked on `.claude/**` write access (see the daily summary's Escalations).
