# Health Report — 2026-04-20 — fast

## Summary
- Main build (`dune build`): PASSING (exit 0)
- Main runtest (`dune runtest`): FAILING (exit 1) — pre-existing nesting_linter violations on `analysis/scripts/universe_filter/`, `analysis/scripts/fetch_finviz_sectors/`, and `trading/weinstein/strategy/lib/ad_bars.ml`. **All in files NOT touched by today's PRs.**
- Status file integrity (`status_file_integrity.sh`): PASSING (exit 0)
- Action required: YES (long-running pre-existing nesting violations remain — see harness §Follow-up "Pre-existing nesting linter failures")

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan per Step 6 redesign)
- Build gate exit code: 0 — but `dune runtest` exits 1 due to nesting_linter (and that linter is gating, not advisory, despite previously documented as "advisory" — orchestrator instruction needs updating)
- Status integrity exit code: 0
- Deep scan: see `dev/health/2026-04-20-deep.md` (generated by harness-maintainer this run; will also run weekly via GHA cron)

## Pre-existing nesting violations (deduped — `.formatted/` mirrors counted as one)
- `analysis/scripts/universe_filter/lib/universe_filter_lib.ml` — 5 functions over avg or max threshold (`read_csv` 6.64/13, `load_rows_with_universe` 4.69/8, `_load_universe_metadata` 4.11/9, `load_config` 3.94/7, `_parse_universe_entry` 3.62/6)
- `analysis/scripts/universe_filter/test/test_universe_filter.ml` — 12 test functions over thresholds
- `analysis/scripts/fetch_finviz_sectors/lib/fetch_finviz_sectors_lib.ml` — 3 functions (`load_existing_sectors` 4.90/11, `_fetch_all` 3.42/5, `fetch_one` 2.95/7)
- `analysis/scripts/fetch_finviz_sectors/test/test_fetch_finviz_sectors.ml` — `test_manifest_roundtrip` 3.74/7
- `trading/weinstein/strategy/lib/ad_bars.ml` — `_compose` 3.50/8
- `analysis/technical/indicators/atr/lib/atr.ml` — `true_range_series` 3.30/6, `atr` 1.89/3 (nested-else)
- File avg > 2.5 on 3 files: `test_universe_filter.ml`, `fetch_finviz_sectors.ml`, `universe_filter_lib.ml`

These were on main before this run. PR #457 (merged) cleared the `magic_numbers` and `fn_length` linters; the remaining nesting violations are in scripts/ paths not in the active feature roadmap. Tracked in `dev/status/harness.md` §Follow-up "Pre-existing nesting linter failures".

## Today's PRs are clean of new nesting issues
PR #459 (feat-backtest 3e) and PR #458 (harness drift coverage) introduced **zero new** nesting violations. QC passed on both for the unchanged-by-PR finding rule.
