# Health Report -- 2026-07-04 -- fast

## Summary
- Main build: PASSING (CI authoritative — HEAD `081545d5` `CI` + `perf-tier1` + `golden-runs-sp500-5y` + `golden-runs-custom-universe` all COMPLETED SUCCESS)
- Status file integrity: PASSING (exit 0, after simulation + index reconcile)
- Index size: PASSING (exit 0, 8825/20480 bytes, all rows ≤ 250 chars)
- No-python: PASSING (exit 0)
- Action required: NO (on main). NOTE: open PR #1847 has a real `build-and-test`
  failure (`FAIL: nesting linter` on `metrics.ml:158 _pair_trades_for_symbol`,
  max nesting 6 > 5) — this is a PR blocker, not a main-branch issue. Surfaced to
  the maintainer on the PR (comment 4883620797) + §Escalations.

## Metrics
- Checks run: 3 deterministic (status_file_integrity, index_size_linter, no_python_check) — all exit 0
- Build gate: CI authoritative (main HEAD `081545d5` fully green); no local `dune build` needed this run
- Deep scan: latest 2026-06-29 (#1789) — 0 critical/high/medium; weekly GHA cron owns deep mode
