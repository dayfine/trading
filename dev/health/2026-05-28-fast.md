# Health Report -- 2026-05-28 -- fast

## Summary

- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Run context

- HEAD at run start: `91d0e2ea` (`ops(orchestrator): skip daily PR on no-op, surface via step summary` #1336)
- Build gate: `dev/lib/run-in-env.sh dune runtest --force` → exit 0
- Status integrity: `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` → exit 0 (`OK: all dev/status/*.md files have required fields.`)

## Metrics

- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: covered by `dune runtest` exit code (`linter_magic_numbers` and `nesting_linter` both exit non-zero on violations, verified clean)
- Deep scan: see `dev/health/2026-05-25-deep.md` (weekly GHA cron — most recent Monday slot)
