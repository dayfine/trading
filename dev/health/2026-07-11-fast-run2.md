# Health Report -- 2026-07-11 -- fast (run-2)

## Summary
- Main build: PASSING (CI on `a53a0a4f`; `dune` absent in GHA runner)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 8931/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status_file_integrity, index_size_linter) + CI cross-check
- Main-green signal: GitHub CI on HEAD `a53a0a4f` (#1927) — `build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all `success`.
- `dune build`/`dune runtest`: not runnable in GHA runner (`dune` absent from PATH).
  Per the live-evidence rule this env gap is explicitly NOT a `[critical]`.
- Advisory linter output: covered by CI's build-and-test lint stage (green).
- Deep scan: last `dev/health/2026-07-06-deep.md`; next weekly ~2026-07-13.
