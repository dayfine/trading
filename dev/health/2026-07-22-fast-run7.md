# Health Report -- 2026-07-22 -- fast (run7)

## Summary
- Main build: PASSING (live CI on HEAD `1c38ed31` = #2043 run-6 summary)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (10392/20480 bytes, all rows <= 250)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
  - `status_file_integrity.sh` exit 0 — all dev/status/*.md have required fields
  - `index_size_linter.sh` exit 0 — within limits
  - Build gate: not locally runnable (no `dune`/`_build` in orchestrator
    container) — assessed via live GitHub CI on main HEAD.
- Live CI on HEAD `1c38ed31` (#2043): `build-and-test`=success,
  `perf-tier1-smoke`=success, `golden-sp500-5y`=success,
  `golden-custom-universe`=success.
- Advisory linter output: not checked here — covered by `dune runtest` exit
  code in CI.
- Deep scan: `dev/health/2026-07-16-deep.md` (last); weekly deep scan PR #2022
  (`ops/health-deep-2026-07-20`) open, advisory, 0 critical, Action-required NO.
