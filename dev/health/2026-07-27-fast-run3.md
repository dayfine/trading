# Health Report -- 2026-07-27 -- fast (run 3)

Orchestrator GHA run 30262098532. Gates run on `50112fb9` (main at checkout time,
before this run's merges).

## Summary

- Main build: **PASSING** (`dune build`, exit 0)
- Main tests: **PASSING** (`dune runtest --force`, exit 0) — *on this host*; see §CPU below
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0, 10042/20480 bytes, all rows <= 250 chars)
- no-python check: **PASSING** (exit 0)
- fmt: clean · magic numbers: clean
- Action required: **NO** (main was red at checkout on GHA; cleared this run by merging #2113)

## The OpenBLAS CPU discriminator is now settled

Run 2 asked for exactly this and named the missing evidence: *"no **green** run recorded
its CPU, so this is a confirmed property of the red side and a plausible-but-unproven
discriminator overall. Cheap fix: log `/proc/cpuinfo` model name in every health report --
two more runs would settle it."*

This run supplies the missing cell as a **controlled comparison**, not an inference:

| | commit | container image | CPU | `dune runtest` |
|---|---|---|---|---|
| GHA CI run 30261052917 | `50112fb9` | `ghcr.io/dayfine/trading/ci:latest` | (affected runner) | **exit 1** -- 4 `Error:` lines, all `Tuner.Bayesian_opt:11`/`:17` |
| This orchestrator run | `50112fb9` | same image | **AMD EPYC 9V74** | **exit 0**, full suite, `--force` |

**Identical commit, identical image, different CPU, opposite result.** That removes the last
alternative explanation (a code regression, or a cache artifact) and confirms the run-2 root
cause: the distro OpenBLAS mis-dispatches kernels on certain runner CPUs. The tally across
recorded runs is now:

- **Red:** `Intel Xeon 6973P-C` (run 3 on 2026-07-26, run 2 on 2026-07-27, CI run 30261052917)
- **Green:** `AMD EPYC 9V74` (this run) -- **first green run with a recorded CPU model**

`--force` matters here: run 2 caught itself accepting a cached exit 0 as evidence. This run's
exit 0 is from a forced full traversal, not an up-to-date target.

## Main was RED at checkout, and is green now

`dune runtest` on GHA for `50112fb9` failed with exactly:

```
Error: Tuner.Bayesian_opt:17:BO converges on 2D Branin.
Error: Tuner.Bayesian_opt:17:BO converges on 2D Branin (in the log).
Error: Tuner.Bayesian_opt:11:suggest_next GP phase respects bounds.
Error: Tuner.Bayesian_opt:11:suggest_next GP phase respects bounds (in the log).
```

PR #2113 (the fix for precisely these two tests) was QC-APPROVED on both gates in run 2 but
held because GitHub had never created a check-suite for its branch. That resolved on its own:
the branch's merge commit `e04332d1` **did** trigger both required workflows. Merged this run
as `757abdf2`; **main CI is now `completed/success` on `757abdf2`.**

Two notes on the CI evidence, both about not accepting vacuous greens:

- #2113's CI was verified **non-vacuous** -- the job log shows both
  `./test_bayesian_opt.exe` and `./test_bayesian_opt_cholesky.exe` actually executing, with
  zero `Error:` lines.
- By contrast, the green `build-and-test` on PR #2120's branch (run 30260163088, 10:59Z)
  contains **zero** `Tuner.Bayesian_opt` lines at all -- that target was served from the
  `_build` cache and never ran. It merged, and the same content on main then ran the suite
  cold and went red. **A green PR check is not evidence the suite ran**; the cache key
  differing between PR branch and main is enough to flip it.

## Metrics

- Checks run: 5 deterministic (build, runtest --force, status integrity, index size, no-python)
- Agentic fast scan: not run (retired -- Step 6 is deterministic)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)
- CPU model recorded: **AMD EPYC 9V74 80-Core Processor** (per run 2's recommendation; this
  line is the whole reason the discriminator above could be closed)

## Standing recommendation (unchanged, still blocked)

Set `OPENBLAS_CORETYPE` in CI and turn `ci.yml`'s existing CPU-flag smoke step into a real
numerical self-check. #2113 removes the tuner's exposure, and the only other in-repo
Owl-linalg references are dead aliases, so current blast radius is small -- but nothing stops
the next numerical module from hitting it. Blocked on a `workflow`-scoped PAT
(`dev/status/harness.md` H-BLAS).
