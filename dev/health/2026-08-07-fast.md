# Health Report -- 2026-08-07 -- fast

Measured post-merge at `b54f8880` (after #2231 and #2232 landed), from the repo root
`/__w/trading/trading`, all in the foreground with exit codes read directly.

## Summary
- Main build: **PASSING** (`dev/lib/run-in-env.sh dune build` -> exit 0)
- Main tests: **PASSING** (`dev/lib/run-in-env.sh dune runtest` -> exit 0, zero `FAIL:` lines)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0, 11799/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan -- retired per Step 6)
- GitHub CI on the same SHA lineage: `build-and-test`, `perf-tier1-smoke`,
  `golden-sp500-5y`, `golden-custom-universe` all green.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) -- not run here.

## Note on the Step 6.2 path bug (7th consecutive run, and it bit again today)

`lead-orchestrator.md` Step 6.2 documents
`dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`, but
`run-in-env.sh` already `cd`s into `trading/`, so the correct path is
`devtools/checks/...`. I invoked the linters directly instead.

Separately, this run produced a **live demonstration of the companion `$?` bug** in the
same documented snippet. Running

    bash trading/devtools/checks/index_size_linter.sh 2>&1 | tail -5; echo "exit=$?"

printed `FAIL: index_size_linter -- table rows exceed 250 chars ... line 41: 264 chars`
**and `exit=0`**, because `$?` reads `tail`'s status, not the linter's. Re-run as
`bash ... > /tmp/idx.log 2>&1; echo "exit=$?"` it correctly reported the failure, I
shortened the row, and it went to exit 0. A run following the documented snippet
literally would have committed an over-length index row and reported PASSING.

This is the second consecutive run in which the integrity/size linters caught a defect in
the **orchestrator's own** Step 5.5 output rather than an agent's. Both times the
documented invocation would have missed it. Blocked on `.claude/**` write access.
