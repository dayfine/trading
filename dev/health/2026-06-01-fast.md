# Health Report -- 2026-06-01 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0 via `dev/lib/run-in-env.sh`)
- Status file integrity: PASSING (`trading/devtools/checks/status_file_integrity.sh` exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here -- covered by `dune runtest` exit code
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron). Most recent: `dev/health/2026-05-25-deep.md`.

## Notes
- HEAD at run: `48e4f824` (`docs(notes): neutral_blocks_longs directional read — fragile, do not promote` #1412).
- Main CI workflow conclusion `SUCCESS` on `48e4f824` (`Build CI image` + `perf-nightly` + `golden-runs-custom-universe` + `perf-tier1` + `CI` + `golden-runs-sp500-5y` all completed/success at run start; `Daily orchestrator` + `perf-weekly` in_progress — this run + the weekly cron respectively).
- Run delta vs prior fast report (2026-06-01 deferred this morning per run-1's Step 6 contract; CI conclusion proxied for the build gate): nothing new; build gate exit 0 on this run is a positive cross-check against the CI gate.
