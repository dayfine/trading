# Health Report -- 2026-07-22 -- fast (run 2)

## Summary
- Main build: PASSING (live CI on HEAD `7ef57ed2` / #2038: `build-and-test` = success, `perf-tier1-smoke` = success; both goldens success)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 10358/20480 bytes, all rows <= 250)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status_file_integrity, index_size_linter) + live CI read
- Build gate: not runnable in orchestrator container (no `dune`/`_build`) -- assessed via live GHA CI, which is green on HEAD #2038
- Advisory linter output: covered by `dune runtest` exit code in CI (green)
- Deep scan: latest weekly deep scan is PR #2022 (`ops/health-deep-2026-07-20`, open, advisory, 0 critical, Action-required NO)
