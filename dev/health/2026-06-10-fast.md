# Health Report -- 2026-06-10 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (covered by `dune runtest`, exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- `dune build` exit 0; full `dune runtest` exit 0 on HEAD `d8867b8b` (#1508)
- fmt_check: OK (all .ml/.mli correctly formatted)
- magic_numbers: OK (no magic numbers in lib/)
- No `^FAIL:` lines. The `find: ... No such file or directory` lines in
  runtest output are the known benign sandbox-race noise (per-symbol
  test_data subdirs absent in the dune sandbox), not test failures.
- Index-size linter (post Step 5.5 reconcile): exit 0 (5331/20480 bytes,
  all rows <= 250 chars).
- Cross-check: CI on HEAD `d8867b8b` — `build-and-test` SUCCESS,
  `perf-tier1-smoke` SUCCESS, `golden-sp500-5y` SUCCESS,
  `golden-custom-universe` SUCCESS, watchdog skipped. 0 open `ci-red` issues.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here.
