# Health Report -- 2026-07-06 -- run4 fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Detail
- `dune build` — exit 0.
- `dune runtest` — exit 0. `fmt_check` OK; `linter_magic_numbers` OK
  ("no magic numbers found"); no `FAIL:` lines. The `find: … No such file
  or directory` lines under `_build/.sandbox/**/test_data/**` are the known
  sandbox-cleanup-race infra noise (documented non-FAIL; see
  `no_python_check.sh` prune guard, PR #1117) — not a gate failure.
- `status_file_integrity.sh` — exit 0 ("all dev/status/*.md files have
  required fields").
- Weekly deep scan (agentic, GHA Monday cron): PR #1870 merged this run
  (`dev/health/2026-07-06-deep.md`, 170 findings — 0 critical / 30 warnings
  / 140 info). Warnings are design-doc-drift (26) + follow-up accumulation
  (1) + linter-exception-expiry-milestone-unknown (3); info is TODO count,
  declared-large near-limit files, and QC-audit-trail extraction gaps. No
  code-health fn_length / magic-number / dead-code finding surfaced.

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan).
- Result: CLEAN.
