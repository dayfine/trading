# Health Report -- 2026-07-16 -- fast

## Summary
- Main build: PASSING (`dune build` exit 0, cold)
- Main tests + linters: PASSING (`dune runtest` exit 0; no `FAIL:` lines)
- Format: PASSING (fmt_check.sh OK — all .ml/.mli correctly formatted)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 9882/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 4 deterministic (build gate, runtest gate incl. dune-wired
  linters, status_file_integrity, index_size_linter). No agentic fast scan
  (retired — deterministic checks only per orchestrator Step 6).
- Advisory (non-gating): cc_linter WARNING — 1 function CC>10
  (`analysis/scripts/shiller_weinstein_decades/shiller_weinstein_decades.ml:430`
  `_ascii_chart`, CC=16). Advisory only; runtest exit 0.
- Main-HEAD GHA CI (`7c986a9e`): CI, golden-runs-sp500-5y,
  golden-runs-custom-universe, perf-tier1 all **success**.
- Deep scan: see dev/health/2026-07-13-deep.md (weekly GHA cron; next ~07-20).
  Latest deep: 0 critical, 32 warnings (all design-doc-drift + follow-up
  accumulation + intentional linter-exception notes — no mechanical code-health
  findings), 143 info. Action required: NO.
