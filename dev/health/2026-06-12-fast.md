# Health Report — 2026-06-12 — fast

## Summary
- Main build: PASSING (`dune build` exit 0 + `dune runtest` exit 0 on `c7980165`)
- Status file integrity: PASSING (wired into `dune runtest`, which exited 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): full `dune build && dune runtest` exit 0 on the run-start main
  HEAD `c7980165`; **0 `^FAIL:` lines**. The `find: .../_build/.sandbox/...: No
  such file` + `File "."` lines are benign dune-sandbox/workspace-root notices.
- Status integrity (6.2): `status_file_integrity.sh` covered by the green
  `dune runtest`.
- Post-dispatch: PR #1551 (`short_min_price` no-op-default gate) merged to main
  `a68e5c6c` with full required CI green on its tip `ba6c165b` (`build-and-test`
  SUCCESS, `perf-tier1-smoke` SUCCESS). No-op-default change → main remains GREEN.
- Index-size linter (post Step 5.5 reconcile): exit 0 (5611/20480 bytes, all rows
  ≤ 250 chars; longest short-side-strategy at 237).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).
