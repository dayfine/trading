# Health Report -- 2026-06-20 -- fast

## Summary
- Main build: PASSING (CI authoritative — main GREEN on `f245b1fe`)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build gate (6.1): CI authoritative — main CI GREEN on HEAD `f245b1fe` (#1659):
  build-and-test SUCCESS, perf-tier1-smoke SUCCESS, golden-sp500-5y SUCCESS,
  golden-custom-universe SUCCESS. (`orchestrator` in_progress = THIS run;
  `watchdog` skipped.) 0 open `ci-red` issues.
- Status integrity (6.2): `status_file_integrity.sh` exit 0
  ("all dev/status/*.md files have required fields").
- Index size: `index_size_linter.sh` exit 0 (8282/20480 bytes, all rows <= 250).
- Advisory linter output: not checked here -- covered by CI's `dune runtest` exit code.
- Deep scan: latest is `dev/health/2026-06-15` deep scan (0 critical; Action required: NO);
  next weekly deep scan via GHA cron.
