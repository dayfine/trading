# Health Report -- 2026-07-23 -- fast

## Summary
- Main build: PASSING (live CI on `8a874d98` / #2045: `build-and-test` = success, `perf-tier1-smoke` = success, `golden-sp500-5y` = success, `golden-custom-universe` = success)
- Status file integrity: PASSING (exit 0 — all dev/status/*.md have required fields)
- Index size linter: PASSING (10473/20480 bytes, all table rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (no agentic fast scan) — status-file integrity, index-size linter, live-CI build gate
- Local `dune` build gate: not runnable in the orchestrator container (no `dune`/`_build`); assessed via live CI on main HEAD
- Advisory linter output: not checked here — covered by `build-and-test` exit code in CI
- Deep scan: `dev/health/*-deep.md` (weekly GHA cron; latest is PR #2022 = `ops/health-deep-2026-07-20`, 0 critical, Action-required NO — still open/advisory)
