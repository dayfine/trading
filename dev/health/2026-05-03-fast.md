# Health Report -- 2026-05-03 -- fast

## Summary
- Main build: PASSING (exit 0 on `dune build`, HEAD `825bc0e`)
- Main runtest: PASSING (exit 0 on `dune runtest --force`, HEAD `825bc0e`)
- Status file integrity: ADVISORY FAIL — 8 schema warnings from 4 new tracks (`data-foundations.md`, `experiments.md`, `tuning.md`, `weekly-snapshot.md`); script exits 0; not a gate contributor.
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Build/runtest: clean — yesterday's [critical] linter regressions on `weinstein_strategy.ml` / `screener.ml` / `price_path.ml` etc. are RESOLVED on HEAD.
- Status integrity advisories carried unchanged (pre-decision schema drift; same 8 FAILs as run-2 yesterday).
- Deep scan: see dev/health/*-deep.md (weekly GHA cron; not run this session).

## Recovery from 2026-05-02 [critical]
The run-2 of 2026-05-02 reported `dune runtest --force` exit 1 (file_length 7 / fn_length 4 / nesting 73 / magic_numbers many) on HEAD `cabb6b6`. As of HEAD `825bc0e` (today), `dune runtest --force` exits 0. 11 PRs landed in the interval (#785..#794, plus a few un-numbered docs/perf merges on the same branch); the linter regressions appear to have been resolved by the maintainer-driven merge stream — no orchestrator-side action needed.
