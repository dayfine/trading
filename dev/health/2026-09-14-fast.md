# Health Report -- 2026-09-14 -- fast

Orchestrator run 34853606164. Main at run start: `8055e8db`.

## Summary
- Main build: **PASSING** (`dune build` exit **0**)
- Main tests: **PASSING** (`dune runtest` exit **0**, **0** `^FAIL:` lines across 4,321 output lines)
- Status file integrity: **PASSING** (exit **0** -- "all dev/status/*.md files have required fields")
- Index size linter: **PASSING** (exit **0** -- 14,572 / 20,480 bytes, all table rows <= 250 chars)
- Action required: **NO**

Every exit code read **unpiped** -- the gate is the exit code, never `^FAIL:` text
in stdout (some linters are advisory and exit 0 while printing; `nesting_linter`
and `linter_magic_numbers` do exit 1, so only the code is load-bearing).

## Step 6.2 doubled-path defect: RETIRED

The prescribed Step 6.2 form was wrong for **15 consecutive runs** -- it passed a
repo-root-relative path to a wrapper that has already `cd`-ed into `trading/`,
producing a spurious exit 2. PR **#2779** (merged 2026-09-13 evening) corrected
the agent definition to `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh`
and added a "do not fix this back" note.

Re-measured this run with the **corrected** form:

| form | exit |
|---|---|
| `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` (current prescription) | **0** |

The defect is gone and is **not carried forward**. This is the first run in
sixteen where Step 6.2 is not an escalation item.

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan -- retired per harness T3-A+)
- Baseline gate wall time: ~25 min from the workflow's restored `_build` cache
  (cf. ~35 min from cold on 2026-09-13, >25 min on 2026-09-09).
- Peak container memory during the gate: ~1-2 GB used of 15 GB total
  (`free -g`), 13 GB available throughout. The binding constraint on this runner
  is **concurrent** OCaml links, not a single one -- see the dune-mutex note in
  today's daily summary.
- Deep scan: newest on main is `dev/health/2026-08-31-deep.md`, now **14 days
  old**. The 2026-09-07 weekly run reported every step successful and produced
  no file (issue **#2771**); no deep scan has run since. Deep-scan coverage
  remains stale and is escalated, not resolved here.
