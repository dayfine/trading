# Health Report -- 2026-06-22 -- fast (run 3)

## Summary
- Main build: PASSING (CI authoritative — build-and-test + perf-tier1-smoke + both goldens SUCCESS on HEAD `6c4b4663`; merge of #1697 → `f0147ddd` carries the same PR-green content)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (status_file_integrity, index_size_linter, no_python) — all exit 0
- Build gate (6.1): CI-authoritative; main GREEN. PR #1697 build-and-test + perf-tier1-smoke SUCCESS before auto-merge.
- Status integrity (6.2): `status_file_integrity.sh` exit 0; `index_size_linter.sh` exit 0 (7672/20480 bytes, all rows ≤ 250 after run-3 reconcile); `no_python_check.sh` exit 0.
- Advisory linter output: covered by `dune runtest` exit code (PR #1697 ran the full suite green in-container during QC).
- Deep scan: see weekly GHA cron deep report (`dev/health/*-deep.md`); last deep scan 2026-06-15, 0 actionable [medium]/[high].

## Notes
- Two maintainer-authored PRs open at end of run carry RED/pending CI but are NOT main-health issues (main is GREEN): #1696 (decline-character Build 3, build-and-test FAILURE — active LOCAL flagship, maintainer-driven) and #1698 (opam-weekly, build-and-test in_progress). Surfaced in the daily summary §Escalations / §Pending work.
