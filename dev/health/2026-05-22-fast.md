# Health Report -- 2026-05-22 -- fast

## Summary
- Main build: PASSING (exit 0 on HEAD `e82b90b4`)
- Status file integrity: PASSING (exit 0 — `OK: all dev/status/*.md files have required fields.`)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan per orchestrator §Step 6)
- Advisory linter output (covered by `dune runtest` exit code):
  - magic_numbers: `OK: no magic numbers found in lib/ files.`
  - nesting: PASSING
  - fn_length: PASSING
  - mli_coverage: PASSING
  - status_file_integrity (dune-wired): PASSING
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)

## Invocation

```
$ cd /__w/trading/trading/trading
$ dev/lib/run-in-env.sh dune build           # exit 0
$ dev/lib/run-in-env.sh dune runtest         # exit 0 (full suite + wired linters)
$ dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh   # exit 0
```

## Context

- HEAD at run start: `e82b90b4` (`docs(notes): 11-knob BO sweep int_of_sexp crash + fix path (P3 blocker) (#1249)`)
- Stale ci-watchdog issue #1250 is on superseded SHA `099445a` (fix-forward by #1251). Current main green per live verification above. See `dev/daily/2026-05-22.md` §Escalations.
- No new `[medium]` / `[high]` findings to append to `dev/status/cleanup.md` §Backlog (backlog drained as of #1245).
