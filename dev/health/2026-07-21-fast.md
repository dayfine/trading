# Health Report -- 2026-07-21 -- fast (run-6)

## Summary
- Main build: **PASSING** — the run-5 ENOSPC infra red has CLEARED. When run-5's
  summary (#2033) merged, CI re-ran on the new HEAD and went green.
  - Local deterministic `dune build && dune runtest` gate NOT runnable in the
    orchestrator container (no `dune`/`_build` toolchain here). Assessed via live CI:
    - main HEAD `ed30712b` (#2033, run-5 orchestrator summary): `build-and-test`
      = **success**, `perf-tier1-smoke` = **success**.
    - The prior [high] main-red (on #2030 `17402361`, an ENOSPC disk-space flake
      = tracked #1636) is RESOLVED and NOT carried forward.
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (10357/20480 bytes, all rows <= 250; exit 0)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status integrity + index size) + live CI read.
- Deterministic local build gate: not runnable in orchestrator container;
  substituted with authoritative live-CI evidence (main HEAD `ed30712b` green).
- Deep scan: latest advisory PR #2022 (`ops/health-deep-2026-07-20`) = 0 critical,
  Action-required NO.

## Carried-forward context (still-real, human-gated)
- #1636 ci.yml ENOSPC disk-headroom fix (commit `4cc7d08`) remains BLOCKED on a
  human `workflow`-scoped PAT. It is the durable stop to recurring ENOSPC reds;
  today's HEAD happens to be green, but the flake recurs on docs-only commits.
