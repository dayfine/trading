# Health Report -- 2026-07-21 -- fast (run-5)

## Summary
- Main build: **CODE SOUND, but main-HEAD CI RED on an ENOSPC infra flake**
  - Local deterministic `dune build && dune runtest` gate NOT runnable in the
    orchestrator container (no `dune`/`_build` toolchain here — this is the
    claude-code-action runtime, not the CI build image). Assessed via live CI:
    - main HEAD `17402361` (#2030, run-4 orchestrator summary — **docs-only**):
      `build-and-test` = **failure**, `perf-tier1-smoke` = success.
    - Root cause: **disk-space exhaustion** on the ubuntu runner
      (`##[warning] You are running out of disk space. Free space left: 93 MB`)
      -> `collect2: error: ld returned 1 exit status` linking `trading/backtest/test/*`.
      This is the tracked **#1636 ENOSPC** issue (harness.md line 162), blocked on
      a human `workflow`-scoped PAT to push the ci.yml disk-headroom step.
    - **Code is sound** (a docs-only commit cannot cause a link failure): last
      *code* commit `f5cad903` (#2027) `build-and-test` = success; maintainer PR
      **#2032** (`feat/sketch-v5-threading-cert`) `build-and-test` + `perf-tier1-smoke`
      = success on the full tree this run. The `FAIL: libta-lib` line in the log is
      echoed script text — the ISA check actually PASSED ("OK: ... no AVX-512").
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (10347/20480 bytes, all rows <= 250; exit 0)
- Action required: YES (human) — clear the spurious main red via
  `gh run rerun --failed 29825879856` (orchestrator got 403 on the API
  rerun-failed-jobs endpoint — token lacks `actions:write`), and merge the
  #1636 ci.yml disk-headroom fix to stop recurrence.

## Metrics
- Checks run: 2 deterministic (status integrity + index size) + live CI read.
- Deterministic local build gate: not runnable in orchestrator container;
  substituted with authoritative live-CI evidence (#2032 green on full tree).
- Deep scan: `dev/health/2026-07-13-deep.md` (weekly GHA cron; latest advisory
  PR #2022 = 0 critical, Action-required NO).
