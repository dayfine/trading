# Health Report -- 2026-07-25 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0)
- Action required: NO

## Deterministic gate results (Step 6)

Run against main `46b3a8d0` at 2026-07-25T02:03-02:40Z, before any dispatch.

| Gate | Command | Exit | Result |
|---|---|---|---|
| Build | `dev/lib/run-in-env.sh dune build` | 0 | PASS |
| Test | `dev/lib/run-in-env.sh dune runtest` | 0 | PASS -- zero `^FAIL:` lines |
| Status integrity | `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh` | 0 | PASS -- "all dev/status/*.md files have required fields" |
| Index size | `dev/lib/run-in-env.sh sh devtools/checks/index_size_linter.sh` | 0 | PASS -- 10129/20480 bytes, all rows <= 250 chars |

## Metrics
- Checks run: 4 (deterministic; no agentic fast scan)
- Gate basis: **exit code only**, per the Step 6.1 rule. No `grep FAIL:` substitution.
- Deep scan: `dev/health/2026-07-20-deep.md` remains UNMERGED on PR #2022 (advisory,
  human-owned). Its findings are therefore not yet triaged into
  `dev/status/cleanup.md` -- see the daily summary's Escalations.

## Note on the Step 6.2 documented path (carried from run-6, now confirmed)

`lead-orchestrator.md` Step 6.2 documents the invocation as
`run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`. That path is
**wrong** -- the wrapper already cds to `<repo>/trading`, so the correct form is
`devtools/checks/status_file_integrity.sh`. The documented form exits 2 with
"cannot open ... No such file". Confirmed again this run by using the corrected
path successfully (exit 0). The script is `status_file_integrity.sh`, not
`status_file_integrity_linter.sh`. Harness fix still open.
