# Health Report — 2026-08-26 — fast

Orchestrator run 32943299640. Deterministic checks only (Step 6); no agentic
fast scan — see `.claude/agents/health-scanner.md` §"Deep scan" for the weekly
agentic pass.

## Summary

- Main build: **PASSING** (`dune build` exit 0, `dune runtest` exit 0)
- Status file integrity: **PASSING** (exit 0)
- Action required: **NO**

Gate read on **exit code**, not on `FAIL:` text in output — `linter_file_length`
and `fmt_check` both printed `OK:` lines and the run exited 0.

Measured at `3a51cb6b` (main, after #2557 merged), with this run's docs-only
`dev/status/_index.md` reconcile in the tree.

## Metrics

- Checks run: 2 (deterministic)
- `dune build` exit: 0
- `dune runtest` exit: 0
- `status_file_integrity.sh` exit: 0
- `index_size_linter.sh` exit: 0 (11,597 / 20,480 bytes; all table rows ≤ 250 chars)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron)

## Supporting CI evidence at the same SHA

| workflow | conclusion |
|---|---|
| `CI` (`build-and-test`) | success |
| `perf-tier1` | success |
| `golden-runs-sp500-5y` | success |
| `golden-runs-custom-universe` | success |
| `golden-runs-sp500-15y` | still in progress at run end |

## Note carried from this run's QC

Two findings this run were **invisible to a green `dune runtest`** and are worth
remembering as a class — a green gate is evidence about the code, not about the
data files or the sibling scripts:

1. **#2559** — the RSS digit-fusion bug fixed by #2557 is byte-identical in five
   sibling scripts, including `dev/scripts/perf_tier1_smoke.sh`, which backs the
   **required** `perf-tier1-smoke` PR gate. Nothing in `dune runtest` covers them.
2. **UTF-8 double-encoding in three golden `.sexp` `(description ...)` fields** on
   the `feat/rs-trend-live` branch (found by qc-behavioral, since repaired at
   `88b60dc9`). Full suite exited 0 and CI was green throughout; the corruption
   reached a data field and would have double-encoded again on the next re-pin.
