# Health Report -- 2026-07-06 -- fast (run 3)

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- dune runtest: PASSING (exit 0) — fmt clean, magic-numbers linter clean, no `FAIL:` lines
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
  1. `dune build` — exit 0
  2. `status_file_integrity.sh` — exit 0 ("all dev/status/*.md files have required fields")
  3. `dune runtest` — exit 0 (fmt_check OK, linter_magic_numbers OK; `@large-module`/`@large-function`
     markers are informational, not violations)
- Sandbox-race `find: … No such file or directory` lines observed in runtest output — known infra
  noise (`.claude/rules/pr-merge-gates.md`), not a FAIL.
- Corroboration: GitHub CI on current main HEAD `88383aec` reports build-and-test, perf-tier1-smoke,
  golden-sp500-5y, golden-custom-universe all COMPLETED SUCCESS.
- Deep scan: see dev/health/2026-06-22-deep.md (weekly GHA cron); not dispatched here.
