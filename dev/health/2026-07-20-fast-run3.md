# Health Report -- 2026-07-20 -- fast (run 3)

## Summary
- Main build: PASSING (local `dune build` exit 0 on HEAD `0e6430cb`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 10288/20480 bytes, all table rows <= 250 chars)
- Action required: NO

## Evidence
- Local `dune build` via `dev/lib/run-in-env.sh`: **exit 0** (14:03–14:08).
- GHA CI on HEAD `0e6430cb` (#2019, run-2 summary): `build-and-test` = **success**,
  `perf-tier1-smoke` = **success** (both branch-protection-required),
  `golden-custom-universe` = success (`golden-sp500-5y` cancelled — concurrency,
  non-fatal). This is authoritative: it runs the full build + linter suite + tests
  on the exact HEAD.
- Code tree is byte-identical to run-2's verified-green state: HEAD is #2019 (run-2's
  own daily-summary merge, docs-only), so nothing has changed on main since run 2.
- `status_file_integrity.sh`: exit 0 (all `dev/status/*.md` have required fields).
- `index_size_linter.sh`: exit 0 (10288/20480 bytes after run-3 index reconcile).

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
- Advisory linter output: covered by GHA `build-and-test` exit (success on `0e6430cb`)
- Deep scan: latest is `dev/health/2026-07-13-run3-fast.md` era; weekly deep scan runs
  via GHA cron (unaffected by this run). Latest deep scan Action-required = NO.
