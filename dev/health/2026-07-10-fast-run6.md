# Health Report -- 2026-07-10 -- fast (run-6)

## Summary
- Main build: PASSING (GitHub CI on HEAD `1578e777`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0)
- Action required: NO

## Evidence
- **Main CI on `1578e777`** (run-5 summary #1918): `build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe`, `golden-sp500-5y` all
  `completed success`; `watchdog` skipped; `orchestrator` in_progress (this run).
- `status_file_integrity.sh` exit 0 ("all dev/status/*.md files have required fields").
- `index_size_linter.sh` exit 0 (8698/20480 bytes, all rows ≤ 250 chars).
- `dune build`/`dune runtest` NOT runnable in the GHA orchestrator runner
  (`dune` absent from PATH; `ounit2` absent). This is a runner env gap, not a
  code failure — authoritative main-green signal is the CI run on `1578e777`.

## Metrics
- Checks run: 2 deterministic (status integrity + index size). No agentic fast scan.
- Weekly deep scan: last `dev/health/2026-07-06-deep.md`; next cadence ~2026-07-13
  (not due).
