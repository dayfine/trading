# Health Report -- 2026-09-01 -- fast

## Summary

- Main build: **PASSING** (exit 0)
- Main tests: **PASSING** (exit 0, 0 `^FAIL:` lines)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0)
- Action required: **NO**

Measured on `6da52064` (main at run end, after #2623 merged). Every exit code
read **unpiped**. Re-measured from `c0f40ecb` at run start -- also all 0 --
so main was green throughout the run.

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- Deep scan: `dev/health/2026-08-31-deep.md` (weekly GHA cron, #2609)
- No `test_csv_snapshot_builder_cleanup` flake observed despite sustained
  2-agent load across ~8 subagent dispatches.

## Step 6.2 path defect -- 6th consecutive run

`run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh`
-> exit **2** (`sh: 0: cannot open trading/devtools/checks/... No such file`)

`run-in-env.sh sh devtools/checks/status_file_integrity.sh`
-> exit **0** (`OK: all dev/status/*.md files have required fields.`)

The wrapper already `cd`s into the inner `trading/`, so the path in
`lead-orchestrator.md` Step 6.2 is doubled. Exit 2 reads exactly like a check
failure. Measured live again this run, both ways.

**It cost real time this run**, which is new evidence for prioritising it: a
recovered harness agent's verification run hit it and reported a false failure
before the path was corrected. The one-line fix remains blocked on
`.claude/agents/**` write permission (see the daily summary's Escalations).
