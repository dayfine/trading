# Health Report -- 2026-07-10 -- fast (run-3)

## Summary
- Main build: **PASSING** (authoritative signal = GitHub CI, not local dune —
  `dune` is not on PATH in the GHA runner). Required checks on main HEAD
  `27248f8d` (run-2 summary #1914): `build-and-test` **success**,
  `perf-tier1-smoke` **success**; `golden-custom-universe` **success**.
- `golden-sp500-5y`: shows `failure` on `27248f8d` but the job log
  (run 29080927652 / job 86323333085) is:
    `##[error]The runner has received a shutdown signal. This can happen when the
     runner service is stopped, or a manually started runner is canceled.`
    `##[error]The operation was canceled.`
  → an **infra runner-cancellation** (~17 min in, then killed), NOT a code
  `^FAIL:` (no OUnit/golden assertion, no mismatch, no diff). The commit is a
  docs-only orchestrator-summary PR, which cannot regress a golden. Known-class
  flake (cf. ci.yml ENOSPC/#1636 resource pressure). `[info]`, NOT `[critical]`,
  per the live-evidence rule.
- `golden-sp500-15y`: `in_progress` (nightly, long-running) at scan time.
- Status file integrity: **PASSING** (`status_file_integrity.sh` exit 0).
- Index size: **PASSING** (`index_size_linter.sh` exit 0 after reconcile —
  9007/20480 bytes, all table rows <= 250 chars).
- Action required: **NO** (main effectively green; the one `failure` is an infra
  cancellation on a docs commit).

## Metrics
- Checks run locally: 2 deterministic shell checks (status-integrity,
  index-size); both exit 0.
- Build/test gate: **not runnable in GHA** — `dune` absent from PATH; `ounit2`
  absent (`Unbound module OUnit2`). Authoritative main-green signal is GitHub CI
  on `27248f8d`.
- Deep scan: not due — last `dev/health/2026-07-06-deep.md`; next ~2026-07-13
  (weekly GHA cron).
