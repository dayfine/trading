# Health Report -- 2026-06-21 -- fast

## Summary
- Main build: PASSING (CI-authoritative — build-and-test + perf-tier1-smoke + golden-sp500-5y + golden-custom-universe all SUCCESS on HEAD `a329851d`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 7588/20480 bytes, all rows <= 250)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (status_file_integrity.sh exit 0, index_size_linter.sh exit 0, CI-authoritative build gate). No agentic fast scan (retired — see Step 6).
- `dune` not on PATH in the orchestrator container; build gate relies on the CI check-runs on HEAD, which are GREEN.
- Advisory linter output: not checked here — covered by the CI build-and-test exit code (SUCCESS on HEAD).
- Deep scan: latest is `dev/health/2026-06-15-deep.md` (0 critical, 27 warnings — all design-doc drift / follow-up-accumulation advisories; Action required: NO). Next weekly deep scan runs via GHA Monday cron.
