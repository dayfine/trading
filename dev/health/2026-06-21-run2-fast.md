# Health Report -- 2026-06-21 -- run2 -- fast

## Summary
- Main build: PASSING (CI-authoritative — build-and-test + perf-tier1-smoke + golden-sp500-5y + golden-custom-universe all SUCCESS on the PR tips that merged this run; main HEAD `8fea11a4` CI re-running post-merge, expected GREEN since both PR tips were clean + up-to-date)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 7592/20480 bytes, all table rows <= 250)
- No-python check: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 4 deterministic (status_file_integrity.sh exit 0, index_size_linter.sh exit 0, no_python_check.sh exit 0, CI-authoritative build gate). No agentic fast scan (retired — see Step 6).
- `dune` not on PATH in the orchestrator container; build gate relies on CI check-runs, which were GREEN on both merged PR tips and on prior main HEAD `5774b0a5`.
- Advisory linter output: covered by the CI build-and-test exit code (SUCCESS on the merged PR tips).
- Deep scan: latest is `dev/health/2026-06-15-deep.md` (0 critical, 27 warnings — all design-doc drift / follow-up-accumulation advisories; Action required: NO). Next weekly deep scan runs via GHA Monday cron.

## This run
- Merged 2 docs-only maintainer PRs (CI-green, idle ~2.5h) per the pr-merge-gates.md docs-only carve-out: #1681 (4 status-header refreshes) + #1680 (weekly track-pacer report). Each was `behind` main → update-branch → CI re-run GREEN → squash-merge.
- Reconciled `dev/status/_index.md`: barbell-overlay → MERGED (#1683 gate-#2 overlay landed default-off since run 1; status file had not been flipped).
- 0 open PRs after merges. 0 open ci-red issues.
