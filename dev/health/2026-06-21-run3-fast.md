# Health Report -- 2026-06-21 -- fast (run 3)

## Summary
- Main build: PASSING (CI authoritative). HEAD `1edd7621` (#1689 squash-merge):
  perf-tier1-smoke SUCCESS; build-and-test + both goldens re-running post-merge,
  expected GREEN — the squashed content is identical to PR #1689's QC'd tip
  `10e10512`, which was build-and-test SUCCESS + perf SUCCESS.
- Status file integrity: PASSING (exit 0 — all dev/status/*.md have required fields).
- Index size: PASSING (exit 0 — 7483/20480 bytes, all rows <= 250 chars).
- No-python: PASSING (exit 0 — no *.py files).
- Action required: NO.

## Metrics
- Checks run: 3 deterministic linters (status-integrity, index-size, no-python) +
  CI cross-reference. No agentic fast scan (retired — Step 6 is deterministic).
- Advisory linter output (fn_length / nesting / magic_numbers): not run here;
  covered by `dune runtest` exit code in CI (build-and-test).
- Deep scan: see dev/health/*-deep.md (latest 2026-06-15; weekly GHA cron).
- This run's only feature change (#1689) passed its own qc-structural run with the
  dune linters green (runtest exit 0 on trading/backtest/barbell/).
