# Health Report -- 2026-07-22 -- fast (run 5)

## Summary
- Main build: PASSING (assessed via live CI on main HEAD `15f3eee4` — `build-and-test` = success, `perf-tier1-smoke` = success; `dune` not runnable in the orchestrator container)
- Status file integrity: PASSING (exit 0 — ran `status_file_integrity.sh` standalone this run)
- Index size: PASSING (exit 0 — `index_size_linter.sh`: 10282/20480 bytes, all table rows ≤ 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (no agentic fast scan): live-CI gate read + status_file_integrity.sh + index_size_linter.sh
- Advisory linter output: not checked here — covered by `dune runtest` exit code on CI (green on `15f3eee4`)
- Deep scan: latest weekly is `dev/health/2026-07-20-deep.md` (PR #2022, advisory/human-merged, 0 critical, Action-required NO)

## Notes
- Intervening scheduled orchestrator run 29926293662 (13:57Z) FAILED at the
  "Initialize containers" step — a transient GHA infra flake (container init),
  not a code/baseline failure. It never entered orchestrator logic and produced
  no summary. Main HEAD gating checks remain green.
