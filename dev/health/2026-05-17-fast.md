# Health Report -- 2026-05-17 -- fast

## Summary
- Main build: PASSING (exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Evidence
- `dev/lib/run-in-env.sh dune build` → exit 0 (HEAD `4a201f16`).
- `dev/lib/run-in-env.sh dune runtest` → exit 0 (advisory linters wired
  via dune all PASS — final line `OK: no magic numbers found in lib/
  files.`).
- `dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh`
  → exit 0 (`OK: all dev/status/*.md files have required fields.`).

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not separately surfaced -- covered by
  `dune runtest` exit code.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron).
