# Health Report -- 2026-06-22 -- fast (run 2)

## Summary
- Main build: PASSING (CI authoritative — main GREEN on HEAD `7b25d5d7`: build-and-test + perf-tier1-smoke + golden-sp500-5y + golden-custom-universe all SUCCESS)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (status_file_integrity exit 0; index_size_linter exit 0 — 7529/20480 bytes, all rows ≤ 250; no_python_check exit 0). No agentic fast scan (retired).
- Advisory linter output: not checked here — covered by CI's `dune runtest` exit code (main GREEN).
- Deep scan: last 2026-06-15-deep.md; weekly GHA cron owns it (not dispatched here). 0 actionable [medium]/[high] outstanding.

## Notes
- One open PR (#1692, `feat/decline-character`) carries a RED-CI `nesting_linter` failure on its branch — NOT a main-health issue (main is GREEN). Surfaced in the daily summary §Escalations / §Integration Queue. Unchanged from run 1 (same tip `96ebd49c`).
