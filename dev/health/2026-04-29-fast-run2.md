# Health Report -- 2026-04-29 -- fast (run 2)

## Summary

- Main build (`dune build @runtest` on orchestrator HEAD 1bdda6d): PASSING (exit 0)
- Status file integrity (wired into `dune runtest`): exit 0 (advisory; the standalone script reports a violation on `dev/status/optimal-strategy.md` `## Last updated: 2026-04-28 (post run-2)` on this HEAD, but the dune wiring is non-gating). On origin/main (af85921, post-#670 merge) the same line reads `2026-04-29 (PR-4 follow-up B)` — a similar violation, addressed by orchestrator-opened PR #673.
- Action required: NO from build gate; cleanup in flight via PR #673.

## Notes (run-2 specific)

- Run-1's `dev/health/2026-04-29-fast.md` already recorded a CLEAN build state plus advisory linter `FAIL:` lines on `optimal_strategy_report.ml` (442 LOC), `screener.ml` (636 LOC), `price_path.ml` (511 LOC), `exit_audit_capture.ml` (nesting), `entry_audit_capture.ml:make_entry_transition` (57 LOC). None of those gate. This run does not change that picture.
- Between run-1 and run-2, five PRs merged on main (#666, #667+#668 daily summary, #669 harness A2, #670 follow-up B, #671 macro persistence). Of those, only #670 reintroduced a new `## Last updated` schema violation on `dev/status/optimal-strategy.md`. PR #673 fixes it (and also addresses a P6 test-pattern finding from qc-structural).

## Metrics

- Checks run: 1 deterministic (`dune build @runtest` exit 0); the standalone `status_file_integrity.sh` advisory was inspected manually (informs PR #673 but not a separate gate this run).
- Advisory linter output: unchanged from run-1 (same five sites).
- Deep scan: see most recent `dev/health/*-deep.md` (weekly GHA cron).
