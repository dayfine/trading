# Health Report -- 2026-08-04 -- fast

## Summary
- Main build: **PASSING** (`dune build` exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0)
- Status file integrity: **PASSING** (`status_file_integrity.sh` exit 0)
- Index size linter: **PASSING** (12012/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

Result: **CLEAN**

## Measurement basis

Full cold `dune build` + `dune runtest` run directly on the checkout at `206cea4b`
(pre-dispatch), both exit **0**. Gate is the **exit code**, not `FAIL:` text in output.

Post-run, main advanced to `f6333f84` via three merges (#2199, #2187, #2200), each of
which had `build-and-test` and `perf-tier1-smoke` COMPLETED SUCCESS on its own tip before
merge. GitHub `CI` on main is green at `206cea4b` and every subsequent SHA.

## Metrics
- Deterministic checks run: 4 (build, runtest, status integrity, index size)
- No agentic fast scan (retired -- deterministic checks replaced it)
- Weekly deep scan: `dev/health/2026-08-03-deep.md`, merged this run as #2187.
  **0 critical, 4 warnings, 150 info.** Warnings 3 and 4 (the two expired/malformed
  `linter_exceptions.conf` entries) were **closed this run** by #2200. Warning 1
  (design-doc drift, `tax_lens/` missing from the subdir inventory) and warning 2
  (open-item accumulation) remain in `dev/status/cleanup.md` §Backlog.

## Note on the open-item tally (deep-scan warning 2)

The deep scan reported 52 open `- [ ]` items across `dev/status/*.md`. That count was
inflated by at least one literal template stub (`- [ ] <finding type>: <file path> --
...`) sitting in `dev/status/cleanup.md` as documentation-by-example. #2200 deleted it.
Worth knowing that the tally counts any `- [ ]` line, including illustrative ones, so the
threshold breach is somewhat overstated.
