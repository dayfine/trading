# Health Report -- 2026-08-14 -- fast (run 2)

## Summary
- Main build: PASSING (`dune build` exit 0, on `4bda0ae0`)
- Full test suite: PASSING (`dune runtest` exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) -- not run here

## Notes

**`FAIL:` text appeared twice inside an exit-0 `dune runtest`.** Both occurrences
are prose *inside passing `OK:` assertion lines* from `sete_diagnostics_check.sh`,
which self-tests that a linter emits a `FAIL:` line:

```
OK: sete_diagnostics_check - negative control: pre-fix shape dies (exit=1) with NO FAIL: line ...
OK: sete_diagnostics_check - fix: jj_workspace_smoke.sh emits a FAIL: line naming the 'jj workspace list' failure (exit=1)
```

A `grep -c 'FAIL:'` gate would have reported main as RED here. The gate is the
**exit code**, never the presence of `FAIL:` text -- this run is a live instance
of why that distinction is written down.

**Step 6.2 invocation path (9th consecutive run confirming the same defect).**
Measured both forms this run:

| form | exit |
|---|---|
| `sh trading/devtools/checks/status_file_integrity.sh` (as documented) | **2** (`sh: 0: cannot open ... No such file`) |
| `sh devtools/checks/status_file_integrity.sh` (workspace-relative) | **0** |

`run-in-env.sh` `cd`s to the dune workspace root, so the path must be
workspace-relative. The orchestrator spec still documents the failing form.

**Linter populations (informational, from the green run):**
`17 declared-large of 455` `lib/*.ml`; `0 declared-large of 444` `lib/*.mli` --
both well under the 11% per-population cap that fixture E (#2314) now pins.
