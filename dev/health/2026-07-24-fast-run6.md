# Health Report -- 2026-07-24 -- fast (run 6)

## Summary
- Main build: PASSING (no local red; see evidence below)
- Status file integrity: PASSING (exit 0)
- Index size linter: PASSING (exit 0)
- Action required: NO

## Evidence

Deterministic checks run this run, from the repo root via `dev/lib/run-in-env.sh`
(note the in-container CWD is `<repo>/trading`, so the script paths are
`devtools/checks/...`, not `trading/devtools/checks/...` — the latter is what
prior runs' documented invocation used and it silently exits 2 with
`cannot open ... No such file`):

```
dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
  OK: all dev/status/*.md files have required fields.
  exit=0

dev/lib/run-in-env.sh sh devtools/checks/index_size_linter.sh
  OK: index_size_linter — dev/status/_index.md within limits
      (10129/20480 bytes, all table rows <= 250 chars).
  exit=0
```

### Build gate

No local `dune build && dune runtest` was run against main HEAD at report time:
main HEAD `dd70c6bb` (#2075, maintainer docs-only picks backfill) had CI still
`in_progress` for `build-and-test` / goldens, with `perf-tier1-smoke`=success.
That is a pending state, **not** a red — no `[critical]` is warranted.

Stronger live evidence available this run: **both orchestrator PRs went through
full CI green before merge**, on the exact reviewed SHAs —
- #2073 @ `9fcf3d19`: `build-and-test`=success, `perf-tier1-smoke`=success (merged as `ddb6255d`)
- #2074 @ `1eaa6806`: `build-and-test`=success, `perf-tier1-smoke`=success

`build-and-test` runs `dune build @fmt`, `dune build`, `dune runtest` plus the
full linter suite (`fn_length_linter`, `nesting_linter`, `linter_magic_numbers.sh`,
`file_length_linter`, `linter_mli_coverage.sh`, `status_file_integrity_linter`,
`no_python_check.sh`) — strictly stronger than a local build. The full suite also
passed locally inside both feat-agent sessions (exit 0, foreground-verified).

## Metrics
- Checks run: 2 deterministic (no agentic fast scan — retired, see Step 6)
- Deep scan: last on main 2026-07-13; PR #2022 (2026-07-20 deep scan) still open/advisory
- New `[medium]`/`[high]` findings this run: none
- `dev/status/cleanup.md` §Backlog: 0 open `[ ]` (all `[x]`) — no cleanup dispatch
