# Health Report -- 2026-07-05 -- fast

## Summary
- Main build: **FAILING → FIXED**. HEAD `66141546` (#1850 run-2 summary merge) was
  RED: `build-and-test` `dune runtest` exit 1 via the status_file_integrity linter.
  Root cause: the prior run's Step 5.5 reconcile wrote
  `## Last updated: 2026-07-04 (run 2 — #1847 MERGED)` in `dev/status/simulation.md`,
  violating the strict `YYYY-MM-DD` schema; it merged via the run-2 summary bundle.
  Fixed by `[main-fix]` PR **#1853** (`e80b2169`, docs-only, CI-gated) — merged this run.
- Status file integrity: **PASSING** (exit 0) on fresh main `e80b2169`.
- Index size: PASSING (8397/20480 bytes; all rows ≤ 250 chars).
- No-python: PASSING (exit 0).
- Action required: **NO** (main green after #1853).

## Evidence
```
# Failed run 28722389155 (build-and-test), HEAD 66141546:
FAIL: status file integrity linter — malformed or missing fields in dev/status/:
simulation.md: '## Last updated' is not YYYY-MM-DD: '2026-07-04 (run 2 — #1847 MERGED)'
##[error]Process completed with exit code 1.

# After #1853, fresh main e80b2169:
OK: all dev/status/*.md files have required fields.   exit=0
OK: index_size_linter — within limits (8397/20480 bytes, all rows <= 250 chars).  exit=0
OK: no-python check -- no *.py files found.  exit=0
```
The `trade #0 differs` lines in the failed log are labeled
`(expected — see test docstring)` — informational prints from `test_determinism`
start-shift scenarios, NOT a test failure. The sole hard `FAIL:` was the
status-integrity linter.

## Metrics
- Checks run: 3 (deterministic; status-integrity, index-size, no-python). No agentic fast scan.
- Build gate (6.1): #1853's `build-and-test` completed SUCCESS with the identical
  fixed content; post-merge main CI run in progress (perf-tier1-smoke SUCCESS).
- Deep scan: 2026-06-29 (#1789) — 0 critical/high/medium; weekly GHA cron owns deep mode.
