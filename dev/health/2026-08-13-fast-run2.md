# Health Report -- 2026-08-13 -- fast (run 2)

Source: `lead-orchestrator` Step 6 (deterministic; no agentic fast scan).
Orchestrator run 31700595897.

## Summary

- **Main build: PASSING** — `dev/lib/run-in-env.sh dune build` **exit 0** and
  `dune runtest` **exit 0**, measured on the shared checkout at `a7de3723`
  (= `main@13bae1ff` plus the `harness/mli-file-length-coverage` change).
  Captured via the detached-plus-`.rc`-file pattern, so the exit code read is
  dune's own and not a pipeline's.
- **Status file integrity: PASSING** — exit 0,
  `OK: all dev/status/*.md files have required fields.`
- **Action required: NO**

## Metrics

- Checks run: 2 (deterministic).
- The gate is the `dune` exit code, per Step 6.1 — advisory linter text is not
  separately inspected.
- **Deep scan: STALE — the weekly cron has been failing since 2026-08-10.**
  Latest report on disk is `dev/health/2026-08-03-deep.md` (10 days old).
  `health-deep-weekly.yml` run 31404464033 failed with `is_error: true` after
  615 ms / 1 turn / $0 — the agent never scanned. So Step 2e.1 appended nothing
  to `dev/status/cleanup.md` §Backlog this run, **but that is an absence of
  input, not an absence of findings.** See §Escalations in
  `dev/daily/2026-08-13-run2.md`.

## Deliberate sequencing note

The local build gate was **not** run concurrently with the writer agents. Run 1
established that concurrent `dune` invocations contend on a single lock
(`Another Dune instance is currently running. Aborting...`) and that a QC agent
read exactly that message as a failing test gate. Serialising the gate removes
the false-red class entirely.

## Corroborating CI evidence on `main`

| SHA | `build-and-test` | `perf-tier1-smoke` | note |
|---|---|---|---|
| `f56ad877` | success | success | main at run start; both goldens also success |
| `47438acd` | success | success | #2306 after `update-branch`, pre-merge |
| `13bae1ff` | success | success | main after #2306 merged |

Zero open `ci-red` watchdog issues at run start and at run end.

## Step 6.2's documented invocation is still wrong — 8th consecutive run

Measured again this run, both forms:

```
$ dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh: No such file
exit=2                                          <- the form Step 6.2 documents

$ dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh
OK: all dev/status/*.md files have required fields.
exit=0                                          <- the correct form
```

The wrapper resolves `PROJECT_ROOT` to the dune workspace root (`trading/`) and
`cd`s there, so the path must be **workspace-relative**. An orchestrator that
followed Step 6.2 literally would read **exit 2** and report a health failure
that does not exist. Blocked on `.claude/**` write access (confirmed again this
run by direct probe); carried forward in §Escalations.
