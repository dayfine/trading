# Health Report -- 2026-06-22 -- fast

## Summary
- Main build: PASSING (CI build-and-test SUCCESS on HEAD `8f4ff67f`; perf-tier1-smoke + both goldens SUCCESS)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0; 7529/20480 bytes, all rows <= 250 chars)
- No-python: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 3 deterministic (status_file_integrity, index_size_linter, no_python_check) — all exit 0
- Build gate: CI authoritative — main GREEN on `8f4ff67f` (build-and-test SUCCESS, which runs the full linter suite: nesting, magic-numbers, file-length, fn-length, mli-coverage, status-integrity, index-size, no-python)
- Deep scan: latest is dev/health/2026-06-15-deep.md (weekly GHA cron; 0 actionable [medium]/[high]; ~27 advisory). No new findings to refresh into cleanup backlog.

## Note — open PR with RED CI (not a main-health issue)
- PR #1692 (`feat/decline-character`, maintainer LOCAL) has build-and-test FAILURE on a
  REAL `nesting_linter` violation: 3 functions in the new
  `analysis/weinstein/macro/lib/decline_character.ml` exceed limits
  (`_drawdown_from_trailing_high` L49 max-nesting 6 > 5; `classify` L115 nested-else;
  `_trailing_drawdown_pct` L35 nested-else). This is on the PR branch, not main — main is GREEN.
  Surfaced in the daily summary §Escalations + §Questions. Not dispatched (collision-avoidance).
