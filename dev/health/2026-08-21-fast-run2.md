# Health Report -- 2026-08-21 -- fast (run 2)

## Summary

- Main build: **PASSING** — CI green on `main@d60b057e` (`build-and-test`,
  `perf-tier1-smoke`, `golden-custom-universe` all completed/success).
- Status file integrity: **PASSING** (exit 0)
- Action required: **NO**

## Metrics

- Checks run: **5 deterministic shell gates**, all natively, exit codes read
  directly (no piping — a piped `| tail` reports `tail`'s exit code, not the
  gate's):

  | gate | exit |
  |---|---|
  | `status_file_integrity.sh` | **0** |
  | `index_size_linter.sh` | **0** |
  | `no_python_check.sh` | **0** |
  | `tracked_artifact_linter.sh` | **0** |
  | `posix_sh_check.sh` | **0** |

- Independent corroboration: **zero open `ci-red` issues** (21 open issues
  scanned, none carrying the watchdog label).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here.

## Why no local full `dune runtest`

Deliberate, same call as run 1. Three agents were building concurrently on a
**4-core / 15 GB** runner, and a prior run was **OOM-killed (exit 137,
silently)** doing exactly this. The build gate rests on CI at `d60b057e` plus
each dispatched agent verifying `dune build && dune runtest` to exit 0 on its
own branch (a superset of `main`).

## Carried trap — still live

`dune runtest` output contains **two `FAIL:` strings that are not failures**,
inside `OK:` lines of the `sete_diagnostics_check` meta-linter:

- `grep -c 'FAIL:'` → **2** on a green tree
- `grep -c '^FAIL:'` → **0**

The exit code is the gate. Every agent brief this run states this explicitly.
