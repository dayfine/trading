# Health Report -- 2026-09-04 -- fast

Orchestrator run 33894722318. Tree at `20ed1788`.

## Summary
- Main build: **PASSING** — `dune build @runtest --force` exit **0**, **0** `^FAIL:` lines
- Status file integrity: **PASSING** — exit **0** ("all dev/status/*.md files have required fields")
- Index size linter: **PASSING** — exit **0** (12967/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

Every exit code above was read **unpiped** (`cmd > log 2>&1; echo $?`), not via
`| tail; echo $?` — the latter captures the pipe's status, not the command's, and
has produced a false green in this repo on two prior runs.

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan — retired per T3-A+)
- Open `- [ ]` follow-up items across `dev/status/*.md`: **69** (unchanged from
  2026-09-02 and 2026-09-03). Concentration: `harness.md` 32, `trade-audit.md` 9,
  `orchestrator-automation.md` 7, `cleanup.md` 5.
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron; last success
  2026-08-31T15:26Z).

## Scheduled-workflow health (ran by hand again — #2634 still not wired in)

One pass over `/actions/workflows` plus one `?event=schedule&per_page=1` query
each. Reproduces the known-red set without being told about it:

| conclusion | workflow | newest scheduled run |
|---|---|---|
| **failure** | Prune candidates weekly | 2026-08-31T16:26:33Z |
| **failure** | Weekly start sweep (BAH SPY) | 2026-08-31T14:06:21Z |
| **failure** | Weekly track pacer | 2026-08-30T06:20:58Z |
| success | Dependency freshness check | 2026-08-31T08:20:01Z |
| success | golden-runs-sp500-15y | 2026-09-04T13:07:45Z |
| success | Weekly deep health scan | 2026-08-31T15:26:55Z |
| success | Build CI image | 2026-08-31T06:39:02Z |
| success | perf-nightly | 2026-09-04T09:12:48Z |
| success | perf-weekly | 2026-08-31T07:07:34Z |

**All three red workflows last ran on their crons BEFORE the fixes landed** —
`prune-candidates-weekly` last ran 08-31, and its script-side fix (#2651) merged
09-03; `weekly-start-sweep` last ran 08-31, and its crash fix (#2654) merged
09-04T02:41Z. **Neither fix has been exercised yet.** Their next scheduled runs
are the first real test, and the orchestrator cannot trigger them early
(`POST /actions/workflows/<f>/dispatches` → 403, re-measured 2026-09-03).

`Weekly track pacer` (red since at least 2026-08-30) is the one with **no fix in
flight and no diagnosis**. Its failing step is step 5, "Run track-pacer" — the
agent invocation itself, not a setup step.

**Caveat, repeated deliberately:** any *streak* figure derived from a
`per_page`-bounded page is a **floor, not a measurement**. This table reports
only each workflow's newest scheduled run, which is a fact; it reports no streak
counts, which would not be.

## Post-merge state (appended at end of run)

`#2663` merged to main as **`2310b6b8`**. Its post-merge checks at write time:

| check | state |
|---|---|
| `perf-tier1-smoke` | **success** |
| `golden-sp500-5y` | **success** |
| `golden-custom-universe` | **success** |
| `build-and-test` | **still in progress** |

**Main is therefore NOT verified green at `2310b6b8` as of this report.** Three
of four checks passed; the longest one had not finished when the run ended.
Recorded as unfinished rather than assumed — the PR's own `build-and-test` was
green at `a142243f` and branch protection is `strict: true` (up-to-date required),
so the expectation is green, but an expectation is not a measurement. The next
run's Step 1c should verify `2310b6b8` before treating it as a clean baseline.

Main also advanced past the merge during the run (`a4387436`, #2666, from a
local session).
