# Health Report -- 2026-07-15 -- fast (run 5)

## Summary
- Main build: PASSING (`dune build` exit 0)
- Main tests + linters: PASSING (`dune runtest` exit 0)
- Status file integrity: PASSING (exit 0)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (Step 6.1 build gate + Step 6.2 status-integrity)
  plus the full `dune runtest` linter surface (fmt check, magic-numbers,
  fn-length, nesting, mli-coverage, cc-linter, large-module markers).
- `dune build` exit 0; `dune runtest` exit 0 on HEAD `8142d945`.
- `dune fmt` check: "OK: all .ml/.mli files are correctly formatted".
- Magic-numbers linter: "OK: no magic numbers found in lib/ files."
- No `FAIL:` lines in runtest output.
- Main-HEAD GHA CI (`8142d945`): `build-and-test`, `perf-tier1-smoke`,
  `golden-sp500-5y`, `golden-custom-universe` all **success**.
- Advisory note: the `dune runtest` exit=1 seen in an intermediate run this
  session was a self-inflicted parallel-`dune`-invocation collision artifact
  (a stray background `dune` still held the build lock); a clean sequential
  re-run returned exit 0. Not a real failure.
- Deep scan: see `dev/health/2026-07-13-deep.md` (weekly GHA cron).
