# Health Report -- 2026-09-09 (run 2) -- fast

Deterministic checks only (Step 6.1 / 6.2). No agentic fast scan — that was
retired for producing false-positive `[critical]` findings. The weekly deep scan
is unaffected (`Weekly deep health scan`, last run 2026-09-07, conclusion
`success`).

Main at check time: `df7922fb`.

## Summary

- Main build: **PASSING** (`dune build` exit **0**)
- Test suite: **PASSING** (`dune runtest` exit **0**, **0** `^FAIL:` lines across 4,187 output lines)
- Status file integrity: **PASSING** (exit **0** — "all dev/status/*.md files have required fields")
- Index size linter: **PASSING** (13,494 / 20,480 bytes; all table rows <= 250 chars)
- Action required: **NO**

Every exit code read **unpiped** (`cmd > log 2>&1; echo $?`). A piped read
(`cmd | tail; echo $?`) captures the pipe's status, not the command's, and has
produced false greens in this repo repeatedly — most recently in run 1 this
morning, where `jj git fetch 2>&1 | head -5; echo $?` reported 0 for a command
whose real exit was 1.

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan)
- Wall time for the baseline gate: **> 25 minutes** for `dune build` +
  `dune runtest` on this runner. Recorded because it is load-bearing evidence in
  today's summary: run 1's entire `Run lead-orchestrator` step lasted 20m 57s,
  which is less than one verification gate — so the three agents it reported as
  "in flight" could not possibly have returned before the process ended.
- Peak baseline `trading/_build` size: **15 GB**. Reclaimed after the gate to make
  room for a third concurrent agent worktree (62 GB free after reclaim).

## Step 6.2's doubled-path defect — 14th consecutive run

The form prescribed by `.claude/agents/lead-orchestrator.md` Step 6.2:

```
dev/lib/run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh
```

exits **2** with `sh: 0: cannot open trading/devtools/checks/status_file_integrity.sh:
No such file` — the wrapper has already `cd`'d into `trading/`, so the path is
doubled. Both working forms, re-measured this run:

```
(cd trading && sh devtools/checks/status_file_integrity.sh)   # exit 0
dev/lib/run-in-env.sh sh devtools/checks/status_file_integrity.sh   # exit 0
```

One-line fix, behind the `.claude/agents/**` write gate (12th consecutive run
blocked; re-probed and refused this run).

## Not a finding, but worth recording

`dune runtest` emits `Warning: No dune-project file has been found in directory
"."` on every invocation. This is cosmetic and **expected** — see
`dev/status/harness.md` `H-RUNENV-DUNE-PROJECT-WARNING`, which documents a
2026-08-18 attempt that was deliberately abandoned because adding the file
silently converts 45 `devtools/checks` runtest rules from sandboxed to
unsandboxed execution. Noted here only because it has repeatedly read as a
path-resolution failure to agents diagnosing `run-in-env.sh`.
