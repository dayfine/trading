# Health Report -- 2026-08-24 -- fast

Deterministic (Step 6). No agentic fast scan — retired after recurring
false-positive `[critical]` findings; the weekly deep scan is unaffected.

## Summary

- Main build: **PASSING** — CI green on `main@2b11c60d`
- Status file integrity: **PASSING** (exit 0)
- Action required: **NO**

## Gate results — exit codes read directly, never through a pipe

Run natively on the orchestrator runner at `2b11c60d`:

| gate | exit |
|---|---:|
| `status_file_integrity.sh` | 0 |
| `index_size_linter.sh` | 0 |
| `no_python_check.sh` | 0 |
| `tracked_artifact_linter.sh` | 0 |
| `posix_sh_check.sh` | 0 |
| `backtest_appendix_drift_check.sh` | 0 |
| `record_qc_audit_test.sh` | 0 (**60 passed, 0 failed**) |

## CI on `main@2b11c60d`

Read from `/commits/<sha>/check-runs`, not from a workflow listing:

| check | status | conclusion |
|---|---|---|
| `build-and-test` | completed | **success** |
| `perf-tier1-smoke` | completed | **success** |
| `golden-sp500-5y` | completed | **success** |
| `golden-custom-universe` | completed | **success** |
| `perf-tier2-nightly` | completed | success |
| `perf-tier3-weekly` | in_progress | — (not a required PR gate) |

Zero open `ci-red` issues (20 open issues scanned).

## Notable measurement — #2440

`record_qc_audit_test.sh` scenario 22, reported in **#2440** as failing in CI
while passing locally, **passes here: 60/60, exit 0**. The orchestrator
runner is a third environment distinct from both `trading-1-dev` and the
`build-and-test` job image, and it agrees with local. This narrows #2440 to
something specific to the `build-and-test` image or to cache state, and it
argues against the "pre-existing failure the cache is hiding" reading — the
suite genuinely passes on an uncached native run. Dispatched this run for
diagnosability hardening, not for a fix to a failure that does not reproduce.

## What was deliberately NOT run

A whole-repo local `dune runtest` — same call as the 08-21 and 08-23 runs.
Three agents were building concurrently in their own worktrees on a 4-core /
15 GB box where a prior run was **OOM-killed (exit 137, silently)**. The build
gate rests on CI plus each agent's own foreground exit-0 run inside its own
worktree.

## Carried trap — still live

`dune runtest` output contains **two `FAIL:` strings that are not failures**,
inside `OK:` lines of `sete_diagnostics_check`:

```
grep -c 'FAIL:'   -> 2   on a green tree
grep -c '^FAIL:'  -> 0
```

Anchor the pattern, or read the exit code. Related trap, also stated in every
brief this run: `cmd | tail; echo $?` reports `tail`'s status, not `cmd`'s.

## Metrics

- Checks run: 7 deterministic (no agentic fast scan)
- Findings: none
- Deep scan: `dev/health/*-deep.md` (weekly GHA cron; last 08-17, on cadence)
