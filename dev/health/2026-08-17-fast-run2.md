# Health Report -- 2026-08-17 -- fast (run 2)

Orchestrator run 32030003678. Baseline SHA at gate time: `5250d4a7`
(main advanced to `ba3d4cc1` during the run when the prior run's stranded
summary PR #2361 was merged).

## Summary

- Main build: **PASSING**
- Status file integrity: **PASSING**
- Action required: **NO**

## Step 6.1 -- build gate (measured locally, not substituted)

Run in the foreground via `eval $(opam env)` from `trading/`, detached with a
sentinel and polled to completion:

```
BUILD_EXIT=0
RUNTEST_EXIT=0
FMT_EXIT=0
SENTINEL_DONE
```

`grep -cE '^FAIL:'` over the whole log: **0**.

This is the second consecutive run to execute the real local gate rather than
substitute CI's check-run for it. The "`dune` not on PATH" claim that fourteen
prior summaries carried is wrong -- the binary lives in the opam switch and
needs `eval $(opam env)` first (`dune 3.24.2`).

### Capacity note -- the orchestrator's own gate is a third cold build

The 2026-08-16 run measured this 4-core runner's ceiling at **2 concurrent cold
OCaml builds**. This run dispatched two build-heavy writer agents and *then*
started the Step 6.1 gate, which is itself a cold build -- three in total.
Measured load average peaked at **8.89** on 4 cores.

Nothing failed and no agent starved, but the accounting was wrong: the cap is on
**cold builds**, and the orchestrator's own Step 6.1 gate counts against it. The
correct budget is "2 cold builds total, of which the gate may be one", not "2
dispatched build-heavy agents plus whatever the orchestrator does".

## Step 6.2 -- status file integrity

```
status_file_integrity.sh   exit 0   OK: all dev/status/*.md files have required fields.
index_size_linter.sh       exit 0   OK: 10490/20480 bytes, all table rows <= 250 chars
no_python_check.sh         exit 0   OK: no *.py files found
```

## CI corroboration on main's tip

Check-runs on `5250d4a7` at run start:

```
build-and-test          completed/success
perf-tier1-smoke        completed/success
golden-custom-universe  completed/success
golden-sp500-5y         completed/success
golden-sp500-15y        completed/cancelled   <- superseded, not a failure
watchdog                completed/skipped
```

Zero open `ci-red` watchdog issues.

## Metrics

- Checks run: 3 shell gates + the full 3-command dune gate (deterministic; no
  agentic fast scan).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) -- not run here.
