# Health Report — 2026-05-01 — fast

## Summary
- Main build (`dune build`): PASSING (exit 0)
- Test suite (`dune runtest`): PASSING (exit 0)
- Status file integrity (`status_file_integrity.sh`): PASSING (exit 0)
  after orchestrator fixes this run (was advisory-FAIL on
  `screener.md` `## Status` prose suffix and `short-side-strategy.md`
  `## Last updated` prose suffix; both repaired before the report)
- Action required: NO

## Metrics
- Checks run: 2 (deterministic; no agentic fast scan)
- Advisory linter output: not checked here — covered by `dune runtest`
  exit code (per `lead-orchestrator.md` §Step 6.1: "trust only the
  exit code, not stdout `FAIL:` strings")
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron, next Monday
  2026-05-04 at 15:17 UTC)

## Notes
- `linter_magic_numbers.sh` continues to emit ~3 known false-positives
  on docstring-embedded numerals (`stops_runner.ml:77.49,76`,
  `optimal_strategy_runner.ml:671`); none gate the build per the exit-code
  rule.
- 2 schema drifts on `dev/status/*.md` were repaired in-line before this
  report. Drifts originated in maintainer-driven PRs that updated the
  `## Status` value to include a parenthetical narrative
  (`MERGED (with one optional knob in flight: ...)` on `screener.md`)
  and shifted the `## Last updated` value to a non-ISO form
  (`2026-04-30 (evening)` on `short-side-strategy.md`). Worth surfacing
  upstream — recurring schema drift from maintainer-driven PRs is a
  known harness follow-up.
