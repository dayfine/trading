# Health Report -- 2026-07-21 -- fast (run 3)

## Summary
- Main build: PASSING (GHA CI `build-and-test` + `perf-tier1-smoke` = success on HEAD `f5cad903`)
- Status file integrity: PASSING (exit 0)
- Index size: PASSING (exit 0 — 10309/20480 bytes, all rows ≤ 250 chars)
- Action required: NO

## Metrics
- Checks run: 2 deterministic (status_file_integrity, index_size_linter) + CI build gate cited from GHA on the exact HEAD.
- Advisory linter output: not checked here — covered by `dune runtest` exit code in CI (`build-and-test` = success).
- Deep scan: latest committed `dev/health/2026-07-13-deep.md` (0 critical, Action-required NO); open advisory PR #2022 (`ops/health-deep-2026-07-20`) also 0 critical / Action-required NO.
