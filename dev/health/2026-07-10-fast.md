# Health Report -- 2026-07-10 -- fast (run-2)

## Summary
- Main build (HEAD `bb54b267`): PASSING — GitHub CI `build-and-test` + `perf-tier1-smoke` + `golden-custom-universe` + `golden-sp500-5y` + `perf-tier2-nightly` all `completed success` on this SHA.
- Status file integrity: PASSING (`status_file_integrity.sh` exit 0).
- Index size: PASSING (`index_size_linter.sh` exit 0 — 8994/20480 bytes, all rows ≤ 250 chars).
- Action required: NO for main. One actionable finding on the **open PR #1913** (not main) — see below.

## Open-PR finding (not a main-baseline regression)
- **PR #1913** (`feat/breaker-spy-sleeve`, maintainer LOCAL) CI `build-and-test` FAILS:
  `FAIL: magic number linter — bare numeric literals in lib/ files.`
  Exact site: `trading/backtest/lib/runner.ml:364` — a bare literal `364`
  (the `warmup=364` value for the new `Breaker_spy_sleeve` arm). Route it
  through a named constant / config, or add a path exception. The maintainer's
  LOCAL qc-structural APPROVED but explicitly noted the linter gate "could not
  run due to concurrent dune lock (sweep)" — CI enforced it and caught the one
  literal the manual review missed. `perf-tier1-smoke` is green on the same tip.

## Metrics
- Checks run: 3 deterministic (main-CI status via API, status_file_integrity, index_size_linter).
- `dune runtest` not runnable in GHA runner (`ounit2` absent — ENVFAIL, not code-fail); main-green authority is CI on `bb54b267`.
- Deep scan: last report `dev/health/2026-07-06-deep.md`; next cadence ~2026-07-13 (not due).
