# Health Report — 2026-04-18 — fast (run 3)

## Summary
- Findings: 1 (critical: 0  warnings: 1  info: 1)
- Main build: PASSING
- `dune build @runtest` on `origin/main`: **exits 0** (verified by orchestrator)
- `dune build @runtest` on `origin/feat/short-side-strategy`: **exits 0** (verified by orchestrator)
- `dune build @runtest` on `origin/feat/backtest-phase-tracing`: **exits 0** (verified by qc-structural)
- Action required: NO (no gate failures)

## Provenance note (orchestrator-rewritten)

The original run-3 fast scan was dispatched by the orchestrator after the qc-behavioral
agent for #420 left its working tree on branch `qc-420-beh` (= `feat/short-side-strategy`)
with an uncommitted health-scanner stash. The fast-scan subagent then ran on that
contaminated working tree and reported two `critical` items that were actually
non-gating: (a) `weinstein_strategy.ml` 320 lines exceeds 300 — but `linter_file_length`
uses a soft limit + 11% cap and the binary exits 0 on this branch, AND (b) "11 magic
numbers" — `linter_magic_numbers.sh` exits 0 per qc-structural's verification. The
orchestrator re-verified `dune build @runtest` on three clean checkouts (main + both
feature branches) and all exited 0. This file is the corrected report.

## Critical (requires immediate action before next orchestrator run)

None. Both feature branches and main pass `dune build @runtest` with exit 0.

## Warnings (should be addressed within 1 week)

1. **Status file schema violation on `feat/short-side-strategy`** — `dev/status/short-side-strategy.md` on the branch tip (sha 26ff33ed):
   - Line 6: `## Status\nREADY_FOR_REVIEW (MVP slice)` — schema requires exactly `READY_FOR_REVIEW`. Move the `(MVP slice)` qualifier into a comment or scope section.
   - Line 9: `## Interface stable\nYES — <prose>` — schema requires exactly `YES`. Move the explanation into the file body.
   - `status_file_integrity.sh` exits 0 (advisory) but prints two FAIL lines.
   - Recommendation: feat-weinstein follow-up commit to the same branch — small one-line fix per field. Do NOT block #420 merge on this; surface as a follow-up post-merge if not addressed first.

## Info (no immediate action; awareness only)

- Nesting linter prints `FAIL: 49 function(s)` and `FAIL: 6 file(s)` advisory output for `analysis/scripts/universe_filter/` and `analysis/scripts/fetch_finviz_sectors/` — same on main and on both feature branches. The `nesting_linter` rule under dune returns exit 0 (advisory). This is the same pattern documented in `dev/status/orchestrator-automation.md` §Resolved escalations log › 2026-04-18 §1 — not a gate, do not promote to `[critical]`.
- No stale reviews detected.
- No linter exceptions past their `review_at:` annotation.
- Main baseline (origin/main) at commit b44cf72 passes all tests (exit 0).

## Metrics

- Stale reviews: 0
- Linter exceptions past review date: 0
- `dune build @runtest` exit on main: 0
- `dune build @runtest` exit on feat/short-side-strategy: 0
- `dune build @runtest` exit on feat/backtest-phase-tracing: 0 (per qc-structural)

## Process gap caught this run

The fast-scan subagent inherited a contaminated working tree from a sibling agent
(qc-behavioral on #420). Mitigation candidates for the next harness pass:
1. health-scanner agent definition: explicitly checkout `origin/main` before running.
2. Orchestrator: clean working tree (or run on a fresh checkout) before health-scan
   dispatch when sibling QC agents may have left state behind.
3. health-scanner verdict promotion: cross-check `[critical]` claims against
   `dune build @runtest` exit code on `origin/main` before propagating.

Tracked under `dev/status/harness.md` §Follow-up.
