# Health Report -- 2026-07-24 -- fast (run 5)

## Summary
- Main build: PASSING (CI-green on exact HEAD `940c3eab`: `build-and-test`=success,
  `perf-tier1-smoke`=success, `golden-sp500-5y`=success, `golden-custom-universe`=success;
  `orchestrator`=in_progress = this run)
- Status file integrity: PASSING (exit 0, ran via run-in-env.sh)
- Index size linter: PASSING (12473/20480 bytes, all rows <= 250 chars)
- Action required: NO

## Metrics
- Checks run: 3 (deterministic; no agentic fast scan)
- Main build gate: relied on live CI on exact HEAD `940c3eab` (build-and-test=success);
  no code changed post-HEAD this run (only status-file reconcile + summary/health docs).
- Status file integrity: exit 0 after Step 5.5 reconcile (tax-lens.md Status enum fixed
  MERGED; margin/tax-lens index rows within 250-char cap).
- Deep scan: latest on main = dev/health/2026-07-13-deep.md (no [medium]/[high] findings);
  2026-07-20 deep scan still on unmerged advisory PR #2022. Weekly GHA cron owns deep mode.
