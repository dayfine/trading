# Health Report -- 2026-08-28 -- fast

Orchestrator run 33165247888. Measured on `main` = **`b1f10412`** (after
#2584 merged), not on the run-start commit `cbac374f`.

## Summary

- Main build: **PASSING** (`dune build` exit 0)
- Main tests: **PASSING** (`dune runtest` exit 0, **0** `^FAIL:` lines)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0, 11811/20480 bytes, all rows <= 250 chars)
- Action required: **NO**

Result: **CLEAN**.

## Metrics

- Checks run: 4 (deterministic; no agentic fast scan, per Step 6)
- `dune build` emits the known cosmetic `No dune-project file has been found`
  warning (`H-RUNENV-DUNE-PROJECT-WARNING`, `dev/status/harness.md`). Exit code
  is 0; this is not a finding. A 2026-08-18 attempt to fix it was correctly
  abandoned — adding `trading/dune-project` silently un-sandboxes 45
  `devtools/checks` runtest rules and loses the hermeticity guarantee.
- Independent confirmation: GitHub CI on `cbac374f` was green on **all six**
  checks (`build-and-test`, `perf-tier1-smoke`, `perf-tier2-nightly`,
  `golden-sp500-5y`, `golden-custom-universe`, watchdog skipped).
- Deep scan: see `dev/health/*-deep.md` (weekly GHA cron) — not run here.

## Step 6.2 path defect — RESOLVED AS MEASURED, fix blocked

The 2026-08-27 report flagged that the orchestrator spec's Step 6.2 snippet
invokes `sh trading/devtools/checks/<script>.sh`, which cannot resolve because
`run-in-env.sh` has already cd'd into the inner `trading/`. **Re-measured both
ways on `cbac374f` this run:**

| invocation | exit | output |
|---|---:|---|
| `run-in-env.sh sh trading/devtools/checks/status_file_integrity.sh` | **2** | `sh: 0: cannot open ...: No such file` |
| `run-in-env.sh sh devtools/checks/status_file_integrity.sh` | **0** | `OK: all dev/status/*.md files have required fields.` |

Confirmed: exit **2** reads exactly like a check failure at a glance. This run
used the corrected path for both Step 6.2 checks.

The one-line fix to `.claude/agents/lead-orchestrator.md:1870` was drafted but
**could not be applied — writing `.claude/agents/**` is refused by a
tool-permission gate in this runtime.** Note the refusal is at the *harness
tool* layer, not the token or the filesystem: a `touch .claude/.__probe`
succeeds. This is a sharper diagnosis than the prior record's "editing
`.claude/agents/**` currently refused", which did not distinguish the two. See
today's daily summary §Escalations.

Note the doubled prefix is correct in Step 5 Stage 4's `record_qc_audit.sh` /
`write_audit.sh` calls, which run under plain `bash` from the repo root and are
not wrapped by `run-in-env.sh`. Only the `run-in-env.sh`-wrapped call sites are
affected.
