# Health Report — 2026-09-15 — fast (run 2)

## Summary

- Main build: **PASSING** — `b92f77ba` green on all four completed CI checks
  (`build-and-test`, `perf-tier1-smoke`, `golden-sp500-5y`, `golden-custom-universe`)
- Status file integrity: **PASSING** (exit 0)
- Index size linter: **PASSING** (exit 0)
- No-Python check: **PASSING** (exit 0)
- Action required: **NO**

## Checks run

All run **standalone, without dune**, exit codes read **unpiped** — never
`| tail`, which captures `tail`'s status rather than the check's.

| check | exit | output |
|---|---|---|
| `trading/devtools/checks/status_file_integrity.sh` | **0** | all `dev/status/*.md` have required fields |
| `trading/devtools/checks/index_size_linter.sh` | **0** | 13524/20480 bytes after this run's header trim (was 18725 = 91% of cap); all rows ≤ 250 chars |
| `trading/devtools/checks/no_python_check.sh` | **0** | no `*.py` files found |

## Why zero local `dune` calls

By design, and it is the same reasoning that saved run 2 on 09-14 after run 1
was destroyed by the opposite choice:

- This runner allows **one `dune` in flight** (15 GB, memory-bound). Agents held
  the mutex for essentially the whole run — two harness-maintainers, then two QC
  reviewers.
- CI had already measured the same commit across a **wider** surface than a local
  build would (four workflows vs one), so a local build would have added no
  information.
- Holding a ~25-minute serial build against live agents is exactly what consumed
  run 1 on 09-14 (886 s in Step 8, agents dispatched at the end, container torn
  down mid-flight with nothing snapshotted).

## Deep scan

**Current** — `dev/health/2026-09-14-deep.md`, one day old. The weekly cron ran;
#2771's silent-no-output failure did not recur.

## Metrics

- Checks run: 3 (deterministic; no agentic fast scan — retired per T3-A+)
- Disk: 62 GB free of 145 GB (58%) at run start; held 58-59% throughout by
  reclaiming each agent worktree as its agent finished rather than batching
- Follow-up items repo-wide: **57**; max per file **22** (`harness.md`); files
  over the threshold of 4: **3**
