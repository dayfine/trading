# Health Report -- 2026-07-12 (run-2) -- fast

## Summary
- Main build: PASSING — `dune build` exit 0 on merged main `8a834791`; full
  `dune build && dune runtest` exit 0 live-verified on `468aea7d` earlier this
  run; both merged PRs (#1941, #1942) had full `dune runtest` exit 0 in QC and
  green CI on their tips; the merges are disjoint additive default-off/bit-identical
  changes.
- Status file integrity: PASSING (exit 0 — all dev/status/*.md have required fields)
- Index size: PASSING (exit 0 — dev/status/_index.md 9056/20480 bytes, all rows <= 250)
- Action required: NO

## Note — main HEAD CI (#1940 `468aea7d`) build-and-test RED = ENOSPC infra flake
- The `build-and-test` check on main HEAD `468aea7d` (#1940) shows
  `completed/failure`, but this is the **known recurring ENOSPC linker infra
  flake**, not a code failure:
  - Failing log lines: `/usr/bin/ld: final link failed: No space left on device`
    + `##[error]No space left on device` during `ld` — NOT a linter/OUnit failure.
  - `grep -cE '^\S*FAIL:'` on the job log = 0 real FAIL lines (the one `FAIL:`
    match is an `echo` inside the AVX-512 guard step).
  - Live evidence: `dune build && dune runtest` exit 0 on `468aea7d` in this run
    (this runner has 68 GB free).
- This is the documented `dev/status/harness.md` `[~]` item (PR #1636 ci.yml
  disk-headroom fix, commit `4cc7d08`), BLOCKED on a `workflow`-scoped PAT.
  Escalated as `[info]`, not `[critical]`.

## Metrics
- Checks run: 3 (deterministic; dune build + status integrity + index size)
- Deep scan: last dev/health/2026-07-06-deep.md; next weekly ~2026-07-13 (GHA cron)
