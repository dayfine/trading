---
name: stop-buffer-by-macro-state-axis
description: Item 3 landed 2026-09-08 (#2718, e7dde095a) as the default-off axis `initial_stop_buffer_by_macro_state` (five breadth-state slots; 0/non-positive = use the scalar initial_stop_buffer; one read site Entry_walk._initial_stop_buffer threading the same float to the ticket builder and the narrow-stops ordering); candidate map to SWEEP (not promote) = Bullish/Recovering 0.9167 (12%), Neutral 8–10%, Deteriorating/Bearish 1.0 (4–6%); with enable_breadth_direction off only Bullish/Neutral/Bearish slots can fire.
metadata:
  type: project
---

**What shipped (#2718):** `Weinstein_strategy.Stop_buffer_by_state` (flat five-float record over `breadth_state`, `0.0`/non-positive = unset), config field `initial_stop_buffer_by_macro_state [@sexp.default default]` in the .ml and both .mli copies (the `sexp_default_drift` linter caught the drift — run the FULL suite post-push), ONE read site `Entry_walk._initial_stop_buffer` threading the resolved float to the ticket builder AND to `Entry_stop_width_order.prefer_narrow_stops ?initial_stop_buffer` (ordering-vs-gate contract pinned). Override spellings: dot-path `initial_stop_buffer_by_macro_state.deteriorating=1.0`; full sexp `((initial_stop_buffer_by_macro_state ((bullish 0.9167) (recovering 0.9167))))`. R1: goldens bit-identical (paired sp500-2019-2023 digit-for-digit, `paired-run-done`). W2: an ADAPTATION of the §5.3 4–6% width dial (the book does not condition width on breadth — checked in the book, no write-back owed), motivated by the 09-04 yearly review (Deteriorating entries −$604k, Recovering +$627k).

**Next (item 3's surface, then item 4):** sweep the candidate map on the 26y record spec (clean 2000-vintage `_v7mark` warehouse, salts 0–2) against the record band (263% median, 181–562%, maxDD 42–45%; `project_record_rebase_2026_09_07`); `enable_breadth_direction` must be ON for the Recovering/Deteriorating slots to fire (default-off, #2685) — an arm with it off tests only Bullish/Neutral/Bearish. Caveat carried from the 09-05 stop-width surface: re-read every width verdict on the CLEAN basis (the 12%-weekly promotion candidate was measured pre-#2672). The 09-05 writeup's own caveat ("fix #2672 before any promotion PR") is now discharged by the data-layer program. Related: [[record-rebase-2026-09-07]], [[stop-width-cadence-surface-2026-09-05]], [[warehouse-vintage-coverage]].

**Null read 09-08 11:01:** a0 (breadth read ON, map empty, build e7dde095a) = the 09-07 record salt-0 cell DIGIT-FOR-DIGIT (263.155 / 707 / 0.3854 / 42.372). The breadth read is inert; #2705/#2708/#2709 are inert on the _v7mark 2000 warehouse at salt 0 — the record band stands on the current build.

**a1 salt 0 (09-08 11:26):** map (12%/8%/4%) = 692% / 784 / Sharpe 0.55 / DD 37.9 vs null 263 / 707 / 0.39 / 42.4 — shared 359 trades +$1.1M→+$2.3M (CLS +$987k alone), a1-only 425 trades +$3.0M (AEIS, NLS, BFX, KTOS, MOS), stop_loss 450→377, rotation 242→387. Tail lever, same shape as the 09-05 width surface; ONE DRAW — salts 1–2 running; never quote 692.

**QC correction (09-08 12:20):** the a1 salt-0 level is INFLATED — V6 twin violations 6 on a1 (the null and the record have 0 twin violations; their 3 failing invariants are V7/V12/V13): NLS/BFX and BB/BBRY are one instrument each, ≈$764k (27% of the realised delta) double-counted; six movers = 86% of the +429pp; 34% of it is MTM on 9 open positions; ex-movers the map is realised-negative vs the null. AWRE 2014-07-25 force liquidation = a special distribution (unadjusted bars gap through the stop), not news. Mechanism: the null never entered the twin partners; the map's smaller tickets fund both (sizing/funding, not a stop-out). The `_v7mark` warehouses carry twins (#2730) — DEDUPE AT BUILD before quoting any salted read of this surface. What survives: the exit-mix shift and DD 42.4→37.9 as a shape.
