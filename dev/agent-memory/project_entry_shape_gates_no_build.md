---
name: project-entry-shape-gates-no-build
description: "Both W12 entry gates KILLED by screens (07-12): overhead-headroom blocks momentum leaders not crash-bounces (Class-A hr 27-133% ≈ monsters 168-590%); base-quality/recent-S4 IS the standard entry (64-84% of all) — FARM≡TFX twins; losses are the premium, monsters the payout, SAME ticket. Twin-dedup haircut ~12% realized."
metadata: 
  node_type: memory
  type: project
  originSessionId: 3d8cc658-807d-4d9b-bbe3-8da048a636d9
---

Two do-no-harm proxy screens (2026-07-12, read-only agents; scratch in
session log) killed both entry-gate hypotheses from the visual trade audit
before any build:

1. **Overhead-resistance gate (prior-top within X% above entry): NO-BUILD.**
   Median entry headroom is 16.5% — the strategy routinely buys near highs
   (momentum leaders AMD/DY/REGN). X∈{15,25,40}: blocks 22-39% of ALL
   entries, net_pnl_avoided NEGATIVE everywhere (−$3.4M at X=25), blocked
   cohort has BETTER pnl% than kept. The 8 Class-A losses sit at headroom
   27-133% — same side of the knob as the monsters (SKYW 261%, FARM 590%);
   no X separates. Robust to basis convention.
2. **Base-quality / recent-Stage4 gate: NO-BUILD.** The V-bounce-after-S4
   "defect" shape is the strategy's STANDARD entry: ws4≤8 for 84% of all
   1,140 entries; basing<4 for 78%. Feature-identical twins: FARM(2/1) ≡
   TFX/STRA/ASTE/BF-B; DDD(6/5) ≡ OLED; BCSI(3/2) ≡ COO/ANF. Every variant
   net −$4.2M..−$17.4M, blocks 64-94%. Classifier tags deep bases Stage4
   until the MA catches down, so "long flat base" isn't expressible in
   stage features; price-geometry features would be next but the powered
   entry-selection null ([[project-entry-selection-closed-powered]])
   stands against the class.

**LAW re-derived at entry-shape level (with power): the big losses and the
monsters are the SAME ticket** — stops cap the losses at −5..−15% (worst
−$590k COO) while twins pay +$2.9-5.2M. "Never make these trades again" is
structurally equivalent to "never buy the monsters".

What SURVIVED as real fixes (correctness, not alpha): resistance-mapper
data starvation (520-bar spec vs ~52-110-bar panels — COO/CWST print false
Virgin_territory; fix label, no gate); **rename-twin double-count = ~$2.14M
of $18.0M realized (11.9%) in the record run** (10 confirmed groups
NLS/BFX, ISIS/IONS, WLY triple, COR/ABC…; live universe clean; fix =
twin-dedup in snapshot builders + re-pin, haircut headline until then);
CEF/asset-type universe leak (~55 clear non-equities incl. FTHY/PHYS/PSLV;
EODHD exchange-symbol-list mislabels; fix at universe build via
fundamentals General::Type or blocklist); trades.csv exit_trigger/kind
export-join misalignment on re-traded symbols (key both by position_id).
Validator harness (#TBD): V9/V10 stay report-only statistics FOREVER —
measured harmful as gates.
