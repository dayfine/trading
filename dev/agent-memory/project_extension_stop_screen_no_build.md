---
name: project-extension-stop-screen-no-build
description: "Extension-stop screen (07-11) = NO-BUILD decision; extension events ~1%/26y (WF-CV powerless), tight trails tax the on-ramp (9th fat-tail confirmation, first event-level); parabola give-back CLOSED as build target"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3d8cc658-807d-4d9b-bbe3-8da048a636d9
---

Extension-episode event-level screen 2026-07-11
(`dev/backtest/extension-screen-2026-07-11/FINDINGS.md`; tool
`analysis/scripts/extension_screen/`, reusable): counterfactual trail-stop
replay of every held LONG episode of the two current-basis deep runs,
K ∈ {1.5..3.0} × trail ∈ {10..25}%, strategy's own WMA-30 basis.

- **NO-BUILD decision** for an `extension_stop` performance axis (calibrated
  per [[project_mechanism_validation_rigor]] — not a mechanism rejection).
- **Rarity kills testability**: ~0.6-1% of episodes trigger 2.0×; 2 events in
  1730 reach 2.5×. WF-CV would have zero power (most folds have no event).
  AXTI's true max extension on WMA-30 is 2.41× — the "2.5-3×" from the 07-10
  reflection was an SMA-flavored eyeball; MA-type changes threshold meaning.
- **Tight trails (10-20%) tax the on-ramp**: NLS exits 2020-05 @5.44 then
  rides to 21; AXTI trail-10 would exit Nov-2025 @8.93 vs $70 mark. Trigger
  (price≫MA) looks identical on the on-ramp and at the top — no real-time
  discriminator. 9th confirmation of [[project_edge_is_the_fat_tail]], first
  measured directly on the extension-event subclass.
- Trail 25% ≈ never fires; its positive sum is single-event (DDD +$2.96M
  closed; AXTI +$21.5M pure mark-timing on the June 2026 slide).
- **USER DIRECTED THE INSURANCE BUILD (07-11 PM)**: "no way we sit through
  140→70, even if it takes manual intervention" — encoding the intervention
  beats an untested panic exit. Build = default-off `extension_stop_config
  {trigger_ratio ~2.0 (WMA-30 basis), trail_pct 0.25}`, weekly-close (L3),
  tighten-only (L2), W2 = trader exit-aggressiveness dial. Acceptance via
  sanctioned-insurance path (left-tail/event-audit, NEVER fold Sharpe).
  25% trail = the right width (survives AXTI April shakeout + Jan chop,
  fires May-29 @ $103); 10-20% = on-ramp killers, do NOT build tight.
  Queued as P0a in next-session-priorities-2026-07-12.
- Capture-monster #4 (parabola give-back) CLOSED as build target; capture
  levers stay ranked universe-freshness > capacity > early-cut.
- Screen hygiene notes: price-basis guard needed vs post-run splits (GME 4:1
  2022 made run-prices 4× the store's rebased adjusted closes; ~20% of
  episodes excluded — bias runs AGAINST the tax finding, which held anyway).
  Deep run held NLS/BFX rename-twins as two simultaneous positions —
  universe dedupe follow-up.
