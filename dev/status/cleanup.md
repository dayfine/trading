# Status: cleanup

## Last updated: 2026-08-14

## Status
IN_PROGRESS

## Interface stable
NO

Cleanup track has no public interface — it absorbs small mechanical fix-ups surfaced by `health-scanner` (deep + fast scans) so feature agents stay focused on feature work. The "interface" here is the `dev/status/cleanup.md` Backlog schema, which is maintained by the orchestrator and consumed by `code-health`.

## Ownership
`code-health` agent — see `.claude/agents/code-health.md`. Dispatched by `lead-orchestrator` Step 2e on health-scan findings, one finding per dispatch, ≤200 LOC, no behavior change.

## Backlog

- [x] schema_drift: trading/trading/weinstein/snapshot/gen/lib/live_portfolio.ml — **REAL, not stale** despite PR #2125 (merged after this finding was filed) rewriting `header` to add `stop_state` coverage and a follow-up test (`test_header_documents_stop_state`) hardcoding that one field name as a substring check — that pins the single instance, not the general mechanical tie the finding asked for (a rename or another new field would still slip through undetected). Added `test_header_names_every_schema_field` in `test_live_portfolio.ml`: walks the actual field names out of `sexp_of_t`/`sexp_of_position` (Sexplib renders a record as a list of `(field_name value)` pairs) and asserts every one is named in `header`, without touching `live_portfolio.ml`/`.mli` or adding any public symbol. Verified as a real change-detector: mutated the record twice (added an undocumented field; renamed `stop_state`) — both turned the new test RED (`Expected size 0 but got 1`), reverted, back to 13/13 GREEN. PR #2138, branch cleanup/live-portfolio-header-schema-pin (2026-07-27)
- [x] citation_gap: trading/trading/weinstein/stops/lib/stop_types.mli — cited both `min_correction_pct` (line 63) and `support_floor_lookback_bars`'s "Weinstein's 8% rule" cross-reference (line 88) to `§5.2` (STATE: INITIAL / FIRST_CORRECTION: "WAIT for first substantial correction (8-10%+)"); grepped the whole book-reference doc for every `%`-qualified line and confirmed §5.2's three "8-10%+" instances are the only correction-depth matches — §5.1's ">15% risk" (a different quantity, entry-risk cap, already cited elsewhere) and §5.3's "4-6%" (trader-method initial stop size, not this investor-method correction-depth threshold) were ruled out. Branch cleanup/stop-types-citation (2026-07-27)
- [x] doc_precision: dev/plans/lapacke-gp-cholesky-2026-07-27.md + PR #2113 body — re-verified all three defects independently before touching anything, per code-health-discipline. **B** (PR body only — the plan file did not have this defect, contra a loose reading of the finding, but it did carry defect D below in the same sentence): the body's rework-iteration-1 paragraph said correcting the gemm-shape probe "also closes the flagged-but-defused `triangular_solve` question" — confirmed still present verbatim and confirmed wrong: `dtrtrs` (`Linalg.triangular_solve`) is a different LAPACK routine from `dgemm` (`Mat.dot`); a probe of one says nothing about the other. The conclusion ("nothing besides `dpotrf` needs to move") holds on two *separate* direct measurements instead — `triangular_solve` was independently probed clean at ≤6e-16 up to n=120, and the corrected inner-product `gemm` probe is clean at ≤2.7e-15 up to n=400 — neither implies the other. **C**: grepped `bayesian_opt.ml`'s `fit_gp` (lines 145-181) for every `Mat.dot` call site — exactly two, `Mat.dot (transpose k_star) alpha` and `Mat.dot (transpose v) v`, both `(1×n)·(n×1)` since `k_star`/`v`/`alpha` are all `n×1` (built by `_kernel_vector`'s `Mat.init_2d n 1` and `_solve_lower_triangular`/`_solve_upper_triangular`). No `(n×n)·(n×1)` matrix-vector call exists anywhere in `fit_gp`; the plan file's diagnosis table (line 58) and Approach-section justification (lines 95-101) both listed the fictitious shape — removed from both. **D**: confirmed the plan file's Approach section merged two distinct measurements into one wrong bound — it claimed `triangular_solve` AND the inner-product `Mat.dot` calls were "all correct to ≤6e-16 at every n up to 200+", but the table two paragraphs above shows `triangular_solve` was only tested to n=120 (at ≤6e-16) and the inner-product shapes were tested to n=400 (at ≤2.7e-15, not ≤6e-16) — split the merged sentence into its two accurate, independently-sourced bounds. Fixed the plan file (durable record) and, since the PATCH succeeded (HTTP 200), the merged PR #2113 body itself — verified post-PATCH that neither defective string remains and the corrected text is present. No `.ml`/`.mli` touched; conclusion (OpenBLAS Cooperlake mis-detection on Xeon 6973P-C) unchanged and unweakened. Branch cleanup/lapacke-evidence-precision (2026-07-27)
- [x] test_pin: trading/trading/backtest/tuner/test/test_bayesian_opt_cholesky.ml — re-verified independently with a pure-OCaml (no-BLAS) script rather than trusting the finding's numbers: diagonal dominance actually breaks starting at **n=32** (9/32 rows violate it), not just n=100 as the finding implied — at n=100 it's 61/100 rows, off-diagonal row sum ≈3.51 in the middle rows (matches the finding). The matrix is genuinely PD throughout; measured λ_min ≈ **1.83** (power iteration, cross-checked across 3 shifts × 3 starting vectors, converged), not the finding's ≈1.69. Docstring on `_well_conditioned_spd` rewritten with the measured justification. Comment-only, no test/behavior change. PR #2152, branch cleanup/cholesky-spd-justification (2026-07-28)

- [x] test_pin_position: trading/trading/weinstein/snapshot/test/test_html_report_renderer.ml — **STALE, closed as already-fixed, no code change needed.** The finding was filed against PR #2105's table-based `_held_row` (`<td class="">…</td>` cells) in `html_report_renderer.ml`. PR #2114 ("Picks Phase C v2 — HTML report to the committed design reference", 2026-07-27) rewrote that renderer from scratch: `_held_row`/table `<td>` cells no longer exist there (a same-named `_held_row` survives only in the unrelated Markdown renderer `report_renderer.ml`, which has no chart column at all). Held and candidate cards now go through one shared `Report_card.render` (`report_card.ml:70-77`) — there is no longer a separate per-arm HTML assembly that could drift independently, so the whole bug *class* is structurally closed, not just this instance. The same PR also landed the body-only golden the finding asked for (`test_body_matches_golden`, `test_html_report_renderer.ml:226`). Verified empirically rather than by inspection alone: applied the closest analogous mutation (swapped the chart `<div>`'s position in `Report_card.render`'s format string from last to first) — `dune runtest weinstein/snapshot/test/` went RED, exactly `body_matches_golden` failing ("Rendered body differs from fixtures/html_report_body.golden"), all other 37 cases still green; reverted the mutation (`git diff --stat` empty) and the suite returned to 38/38 green. No `_held_chart_td` exists anywhere in the repo (`grep -rn _held_chart_td` empty) to apply the literal mutation from the original finding. (source: 2026-07-27 run 1 qc-behavioral re-review of PR #2105, FLAG F4; closed by cleanup/held-row-tr-anchor 2026-07-27, superseded by PR #2114)
- [x] citation_defect: trading/trading/weinstein/stops/lib/support_floor.mli — split the doc-comment citation: long rule now cites `Ch. 6 §5.1 ("Initial Stop Placement")`, short rule now cites `Ch. 7 §6.3 ("Short Stop (Buy-Stop) Rules")`; independently verified against docs/design/weinstein-book-reference.md that §5.1 contains only the long rule and §6.3 the short rule. Branch cleanup/support-floor-citation (2026-07-26)
- [x] flake_root_cause: trading/trading/backtest/tuner/lib/bayesian_opt_cholesky.ml — **ROOT-CAUSED AND FIXED 2026-07-27** (branch `fix/lapacke-gp-cholesky`). Not the data: at the failure point the kernel matrix had 0 non-finite entries, exact symmetry, and a minimum Cholesky pivot of 1.18e-2, and a pure-OCaml factorisation of that exact matrix succeeded — so the surviving "non-finite entries" hypothesis is **eliminated**. The cause is the surviving BLAS hypothesis, confirmed by direct measurement: the vendored OpenBLAS (`/lib/x86_64-linux-gnu/libopenblas.so.0`) mis-detects the `Intel Xeon 6973P-C` runner as `Cooperlake` (`OPENBLAS_VERBOSE=2` → `Core: Cooperlake`) and dispatches AVX-512 kernels that are wrong on that part. `dpotrf ~upper:false` spuriously fails for every `33 <= n <= 63` (info exactly `n/4 + 1`) including on `4*I`; `dpotrf ~upper:true` returns `info = 0` with a silently wrong factor (5-7% relative for `n >= 33`); `dgemm` is wrong at `n = 120`; `Linalg.inv` aborts with `double free or corruption`. `OPENBLAS_CORETYPE=Haswell|SkylakeX|Zen|Nehalem` repairs all of them. Fix = the GP Cholesky is now a pure-OCaml unblocked factorisation (switching `uplo` was rejected: it would have made the tests green while silently corrupting GP posteriors); the non-finite guard and a regression test pinning the degenerate class (matrix order in 33..63) both landed. The **wider** OpenBLAS defect (square `Mat.dot` at `n >= ~120`, `Linalg.inv`) is NOT fixed here — it is a distro binary. In-repo exposure is small: `bayesian_opt.ml` is the only live Owl-linalg consumer and its remaining calls were probed at their real shapes and are clean; the `Owl.Linalg.S` aliases in `analysis/technical/trend/lib/{segmentation,visualization}.ml` are **dead — declared but never called** (an earlier draft of this entry wrongly listed them as affected). See `dev/status/backtest-infra.md` §Follow-up; owner `harness-maintainer`. Evidence: `dev/plans/lapacke-gp-cholesky-2026-07-27.md`. Superseded original entry follows for provenance: `Tuner.Bayesian_opt:11`/`:17` fail with `Failure("LAPACKE: 9")` on SOME runner hosts, not all. **Host-dependent, not a regression** — run 3 saw exit 1 reproduced 3/3; runs 1, 2 and 4 saw exit 0, run 4 with 44/44 passing and 3/3 clean direct re-runs on the same image. Two root-cause hypotheses were ELIMINATED by reading in run 4: (a) "a failed `potrf` leaves `k` partially overwritten so the retry jitters a corrupted matrix" is FALSE — `Owl.Linalg.D.chol` (`owl_linalg_generic.ml:288`) does `let x = M.copy x in` before `potrf`, so each attempt sees a clean copy; (b) "`max_added_jitter = 1e-2 *. signal_variance` collapses to ~0" is FALSE — `bayesian_opt.ml:219` fixes `_signal_variance = 1.0`, so the cap is `1e-2` absolute and cumulative jitter reaches ~1.1% of a ~1.0 diagonal, ample for merely-duplicated points. Surviving suspects: non-finite (`NaN`/`inf`) entries in the kernel matrix, against which no jitter helps and which would produce exactly the observed "exhausts all 6 attempts"; or a genuine BLAS-kernel difference across runner CPU generations (run 3's host was an AVX-512 `Xeon 6973P-C`; `ci.yml` already carries a CPU-flag smoke step). Needs (a) a guard/assert on non-finite kernel entries with a clear error, and (b) a regression test pinning the degenerate input class. See dev/health/2026-07-26-fast-run4.md. (source: 2026-07-26 run 4, supersedes the run-3 entry which asserted a deterministic main-is-red regression)
- [x] citation_gap: trading/trading/weinstein/stops/lib/support_floor.mli — added `(§5.2)` citation to the `min_pullback_pct` docstring. Verified §5.2's STATE: INITIAL block explicitly pairs "stop = below prior correction low" (the §5.1 rule this function implements) with the qualifying depth "WAIT for first substantial correction (8-10%+)" in the same state — same quantity, not a coincidental number match; searched the whole book-reference doc for other percentage-qualified "correction"/"pullback" language and found none elsewhere, so §5.2 is the only and correct citation; [0.08] is the range's lower bound. Branch cleanup/min-pullback-citation (2026-07-27)
- [x] citation_precision: trading/trading/weinstein/stops/lib/support_floor.mli — the Short stop line's "BEFORE the breakdown" is not stated in §6.3; it is a two-hop inference from "**Initial** buy-stop" + §6's "Mirror image of buying" preamble + §5.1's "BEFORE the breakout". Sound and structurally licensed (§6.2 enumerates the differences from buying and stop *timing* is not among them), but derived rather than quoted. `harness_gap: ONGOING_REVIEW` — telling a faithful paraphrase from drift needs both sections read in context; not mechanizable. (source: 2026-07-26 run 4 qc-behavioral on PR #2099) **CLOSED, PR #2285 (2026-08-13)**: added `(§6.3; timing per §6 mirror-image preamble)` clause.
- [x] test_pin: trading/trading/weinstein/snapshot/gen/test/test_rename_detector.ml — the `prefilter_rel_tol = Float.infinity` correctness precondition (without it `Twin_detector`'s prefilter can drop a genuine rename pair before scoring) is pinned only **incidentally**, by the B2 `_trunc_*` fixture whose anchor-key gaps happen to be 0.041–0.050 > the stock 2e-2. The pre-existing OLDCO/NEWCO pair has min gap 0.0057 and would leave it unpinned. Nothing records the dependency, so a future fixture edit could silently unpin a stated precondition. Fix = a one-line comment on `_trunc_zombie_indices` noting the 4-session predecessor gap is load-bearing. (source: 2026-07-26 run 4 qc-behavioral F7 on PR #2100) **CLOSED, PR TBD (2026-08-13)**: all three numbers independently re-measured (replicating the fixture's price path) and confirmed: stock `Twin_detector.Config.default.prefilter_rel_tol` = 2e-2 exact; `_trunc_*` anchor-key gaps at its 5 valid overlap dates = 0.0435/0.0495/0.0491/0.0489/0.0407 (matches the claimed 0.041–0.050); OLDCO/NEWCO min gap = 0.005715 at date index 195 (matches the claimed 0.0057). Mutation-verified the mechanism directly: temporarily set `prefilter_rel_tol` to the stock 2e-2 in `rename_detector.ml` — `test_bars_after_as_of_are_ignored` (the `_trunc_*` fixture test) went RED with exactly the predicted failure (candidate pair dropped by the prefilter, `List length (0) does not match matchers length (1)`), all other 25 cases stayed green; reverted, back to 26/26. Added the load-bearing comment to `_trunc_zombie_indices`. Comment-only change: `dune runtest trading/weinstein/snapshot/gen/test` exit 0 both before and after, `test_rename_detector.exe` 26/26 OK unchanged.
- [ ] linter_coverage: trading/devtools/checks/linter_file_length.sh — scope is `-path "*/lib/*.ml"` (and, as of the P0.1 `.mli` fix below, `*/lib/*.mli`), so **test files are entirely exempt** from the 300-soft / 500-hard limits. `test_rename_detector.ml` reached 600 lines in PR #2100 with no linter signal. Either the exemption is intentional (then say so in the script header, since its absence reads as an oversight) or test files should carry a higher explicit limit. Not urgent; flagged so the gap is a decision rather than an accident. **Measured 2026-08-13 (harness/mli-file-length-coverage, P0.1 rework)**, so this is a decision with real numbers behind it now: `find` over `*/test/*.ml` finds **401 files**, of which **154 exceed 300 lines and 70 exceed 500 lines**; the largest is `test_screener.ml` at **2687 lines**. `*/test/*.mli` files are essentially nonexistent (test modules are almost never given `.mli`s), so this gap is `.ml`-only in practice. Deliberately left out of the P0.1 PR — that PR closed the `.mli` blind spot (a linter-mechanics bug: raw-vs-signature line counting), this is a policy question (what limit, if any, should apply to test files, which are legitimately more repetitive than lib code) that needs its own decision before a linter change. (source: 2026-07-26 run 4 orchestrator verification; measurements refreshed 2026-08-13)
- [x] linter_expiry: docs/design/weinstein-trading-system-v2.md — added a `Current milestone: M5` marker (§7 Milestones) that `check_11_linter_expiry.sh` parses; verified the parse warning is gone and `CURRENT_MILESTONE=M5` resolves. Consequence: the one milestone-pinned exception (`linter_exceptions.conf:24`, segmentation.ml magic_numbers, `review_at: M5`) newly evaluates as `[EXPIRED]` — not retired here, see follow-up entry below. M5 chosen on evidence from `dev/status/_index.md` (WF-CV/tuner/experiment-ledger live); M6/M7 only partially built. PR TBD (2026-07-28)
- [x] linter_exception_expired: trading/devtools/checks/linter_exceptions.conf:24 (analysis/technical/trend/lib/segmentation.ml magic_numbers) — **retired, not re-dated.** Measured empirically: removing the exception and running the exact linter body against the file surfaced exactly 2 real violations, both `0.2` on the `score` computation line (`left_penalty *. 0.2` / `right_penalty *. 0.2`); the `0.5` trend-change-bonus weight on the adjacent line was already exempt under the linter's own always-allowed list. Hoisted both into two new documented `segmentation_params` fields, `trend_bonus_weight` (default 0.5) and `penalty_weight` (default 0.2) — same values, no behavior change — which also resolves the in-file `TODO(screener/segmentation-weights)` comment that named exactly these two weights. Re-ran the linter against the file post-fix: clean. PR #TBD, branch cleanup/linter-exception-hygiene (2026-08-04)
- [x] linter_exception_format: trading/devtools/checks/linter_exceptions.conf — **retired, not re-dated.** Measured empirically: removing the exception and running the exact linter body against `price_path.ml` surfaced zero violations — every numeric literal in the file sits in a doc comment, a `= <num>` record-default line, a `let x = ...` binding, or a `->` match arm, matching the exception's own (now-verified-accurate) claim. Per `code-health-discipline.md`, an unparseable `review_at` that the underlying claim no longer needs re-verifying against a moving target is better retired than re-dated with a fresh guess. Deleted the exception line entirely; `dune runtest` (magic_numbers linter) confirms the file still passes clean with no exception. PR #TBD, branch cleanup/linter-exception-hygiene (2026-08-04)
- [x] design_doc_drift: dev/plans/backtest-scale-optimization-2026-04-17.md — **full reconcile, not just `tax_lens/`.** Enumerated the actual `trading/trading/backtest/` subdir list (`ls -1`, 30 entries) against the 23-row appendix and found 7 discrepancies, all missing rows (no stale entries pointing at deleted dirs): `faithfulness_report/` (added #2230, after the appendix pin), `tax_lens/` (added #2006/#2066, predates the pin — the finding's own case), `trade_audit_basis/` (added #2251, after the pin), `scripts/` (predates the pin, `bah_baseline_aggregator` + a dated diagnostic, simply omitted originally), plus `lib/`, `test/`, `scenarios/` — the last three are the foundational package layout (April 2026, pre-dates the plan itself) and are **intentionally** out of the table's scope, same reasoning that already excluded them from the original 23-row appendix; added a scope note saying so explicitly. Added rows for the 4 real gaps (faithfulness_report, tax_lens, trade_audit_basis, scripts), descriptions derived from each dir's `.mli`/`README`. Re-pinned the "accurate as of" commit to `d60b057e` (2026-08-21). Filed a follow-up below for the recurrence itself (mechanical derivation vs. re-pinning by hand again). PR TBD, branch cleanup/backtest-subdir-inventory (2026-08-21)
- [ ] design_doc_drift_mechanization: dev/plans/backtest-scale-optimization-2026-04-17.md — the subdirectory-inventory appendix has now gone stale twice (PR #2096 → this fix, 2026-08-21) purely from new feature PRs not adding a row. A hand-maintained snapshot table will keep recurring; worth a cheap CI check that diffs `ls -1 trading/trading/backtest/*/` (minus the documented `lib/`/`test/`/`scenarios/` exclusion) against the row names in the appendix table and fails the build when a subdir has no row. This is a **new linter**, not a doc fix — likely `harness-maintainer` scope (`.claude/rules/qc-structural-authority.md`'s "Linter rule changes... that's harness-maintainer", not `code-health`), so may need re-routing rather than dispatching to `code-health` as-is. (source: recurrence of 2026-07-27-deep.md warning 1, filed 2026-08-21)
- [x] adjusted_basis_guard_asymmetry: trading/analysis/weinstein/snapshot_pipeline/lib/adjusted_basis.ml — **already documented, was unpinned; now pinned, no code change.** Verified by `git blame` that the exact three-failure-mode + silent-downstream-consequence documentation the finding asks for was already added in `790d23a0`, PR #2145's own merge commit (the same PR the qc-behavioral finding was raised on) — `adjusted_basis.mli` already says "the guard is one-sided... nan -> all-NaN, 0.0 -> zeroed, negative -> negative prices... degrades silently downstream." Considered guarding symmetrically instead, per the finding's other option, and rejected it: per the support-floor-stops B2 precedent (2026-08-05 rework), a per-bar fallback inside a windowed/histogram measurement built from adjacent properly-adjusted bars creates a worse mixed-basis result than the documented failure — same failure shape as that precedent. What was missing was a test pinning the implementation to the documented contract; added 3 tests in `test_adjusted_basis.ml` (nan/0.0/negative `adjusted_close`) exercising the unguarded path, mirroring the existing corrupt-raw-close guard tests. No `.ml`/`.mli` touched, `dune build @fmt` / `dune build` / `dune runtest` all exit 0, zero golden diffs. Branch cleanup/adjusted-basis-guard-symmetry (2026-08-10)
- [x] test_pin: trading/analysis/technical/trend/lib/segmentation.ml -- `default_params`'s two newly-hoisted score weights (`trend_bonus_weight = 0.5`, `penalty_weight = 0.2`, landed #2200) are only **one-sidedly** pinned. Mutation-tested by qc-behavioral: swapped values, wrong-field wiring, sign flip and dropped term all go RED on `segmentation_test.test_complex_segmentation`, but a 10x typo in the *reinforcing* direction (`penalty_weight` -> 0.02, or `trend_bonus_weight` -> 5.0) ships **GREEN** on both the trend and stage suites. Root cause generalizes: the score is consumed only by `find_best_split`, which takes an **argmax**, so an argmax-pinning fixture is sensitive only to moves that *dislodge the incumbent* split -- moves that reinforce it cannot flip anything. Fix is ~4 lines and needs no new fixture: `segmentation_params` already derives `show, eq`, so a direct equality assertion on `default_params` pins all 13 fields exactly and is immune to the argmax one-sidedness. Worth doing because the archived A/B experiments (`dev/experiments/segmentation-ab-2026-05-10/`, `rolling-5y-segmentation-ab-2026-05-11/`, 8 scenario sexps with `stage_method Segmentation`) are only comparable against future re-runs if these defaults are byte-stable -- silent drift would invalidate the comparison with no signal. (source: 2026-08-04 run 1 qc-behavioral on PR #2200, finding F2) **CLOSED 2026-08-07.** The fix (a 13-field `equal_to` pin on `default_params`, +31 lines in `segmentation_test.ml`, `matchers` added to the test `dune`) was written by `code-health` on 2026-08-06 and pushed to `cleanup/segmentation-default-params-pin` at 13:14Z, but **no PR was ever opened** and the 2026-08-06 daily summary recorded the track as `skipped -- deferred for wall clock`. The 2026-08-07 orchestrator run opened #2228 under the Step 4.5 recovery path, at which point qc-behavioral found the PR had a **zero-byte net diff against main**: the content had already landed via **`5abfaf58` = #2224, `ops(budget): record 2026-08-06-...`**, an auto-merged ops PR whose body reads *"Additive observational data only"* but whose diff carried `segmentation_test.ml`, the test `dune`, and this backlog line -- **bypassing both QC gates**. #2228 was closed as superseded and the review posted there is the gate the content never got: mutation-verified against both the pre- and post-fix fixtures, `penalty_weight 0.2->0.02` and `trend_bonus_weight 0.5->5.0` GREEN before / RED after, plus `min_r_squared` and `max_segments` mutations F2 never anticipated also caught. No revert warranted. See dev/daily/2026-08-07.md §Escalations for the ops-PR sweep-in.

- [x] docstring_caveat: trading/analysis/technical/trend/lib/segmentation.mli -- add one sentence each to the `trend_bonus_weight` / `penalty_weight` docstrings recording two caveats a future reader will otherwise get wrong: (a) these weights affect only **which** split point `find_best_split` selects (an argmax), not any reported segment statistic -- as written a reader could reasonably think they scale a meaningful output score; (b) they are **estimator constants, not Weinstein dials**, and are not currently reachable from any spec (`stage.ml:294` calls `segment_by_trends` without `~params`). (b) matters because #2200 moved them from implementation into a params record, and config in this repo reads as "sweepable and Weinstein-legitimate" -- but per `.claude/rules/weinstein-faithful-core.md` they are NOT among the documented dials (MA period, entry mode, sizing, exit aggressiveness, numeric thresholds) and any future *tuning* PR would have to justify them as regression-estimator parameters, not as faithful adaptations. (source: 2026-08-04 run 1 qc-behavioral on PR #2200, finding F3) **CLOSED, PR #2324 (2026-08-14)**: both claims re-verified before writing — (a) the weights appear in exactly one expression, `evaluate_split_point`'s `score` (`segmentation.ml:236-241`), whose sole caller `find_best_split` (`:255-257`) binds `let score, _, _ =` (discarding both stats) and uses it only in `if score > !best_score`, returning an `int option`; the returned `segment` records are built independently from `Reg.calculate_stats`, so no reported field depends on either weight; (b) still unreachable from any spec, but the call site drifted two lines — it is now `stage.ml:292` (`let segments = Trend.Segmentation.segment_by_trends ma_series in`, no `~params`), not `:294`, and a repo-wide grep finds no `segmentation_params` reference outside `trading/analysis/technical/trend/` (the only `~params` call sites are the trend module's own tests and the dev binary `trend/bin/main.ml:63`, which overrides neither weight). Docstring names the module rather than a line number so it cannot drift again. Comment-only: full `dune build @fmt && dune build && dune runtest` exit 0 both with the change and with `segmentation.mli` reverted to main.

- [ ] doc_precision: dev/status/cleanup.md (this file) -- the completed `price_path.ml` retirement note claims every literal there sits in "a doc comment, a `= <num>` record-default line, a `let x = ...` binding, or a `->` match arm". Two do not: `trading/trading/engine/lib/price_path.ml:96` and `:103` (`Float.min (...) 2.0`) are bare literals in code that pass only because `2.0` is on the linter's always-allowed list -- the very mechanism correctly cited for segmentation's `0.5`. The substance of the retirement is right (the linter passes with the exception removed, verified); the wording is incomplete. One-line amendment next time the file is touched. (source: 2026-08-04 run 1 qc-behavioral on PR #2200, finding F4)

- [ ] record_size: trading/analysis/technical/trend/lib/segmentation.ml -- `segmentation_params` is now **13 fields** (up from 11 after #2200), against CLAUDE.md's "does a record contain more than 7-9 fields?" readability guidance. Not actioned: #2200 was APPROVED by structural on the merits and the growth was two documented, previously-hardcoded weights. Recorded so the trend is visible if a third hoist is ever proposed -- at that point grouping (e.g. a nested `score_weights` sub-record) becomes the better move. (source: 2026-08-04 run 1 qc-behavioral on PR #2200, finding F5)
- [~] odoc_malformed_comment: trading/trading/weinstein/strategy/lib/weinstein_strategy_config.mli:1458 -- `dune build @fmt` emits `Warning: Invalid documentation comment: ... Paragraph should begin on its own line` (characters 25-26). Non-fatal: the gate exits **0**, so nothing is red. Filed rather than fixed because it is adjacent to the three unresolvable odoc refs #2365 corrected in this same file family, and because the file itself is the standing subject of `.claude/rules/experiment-flag-discipline.md` Rule 4 -- it had reached ~1,450 lines by 2026-08-09, much of it dead-mechanism docstrings, so a one-line comment fix and a Rule-4 retirement pass want to be the same piece of work rather than two. Fix shape: break the paragraph onto its own line; if a retirement pass is scheduled, fold it in there instead. (source: 2026-08-18-fast.md) **SCOPE CORRECTED 2026-08-19 (orchestrator run 32228074121): this is 1 of 26, not 1 of 1.** Measured on main `184f563f`: `dune build @fmt` (still exit **0**, still non-gating) emits **26** `Invalid documentation comment` warnings across **11** files, of which **9** are in `weinstein_strategy_config.mli` alone. Other affected files: `analysis/technical/trend/lib/regression.ml`, `analysis/weinstein/resistance/lib/resistance_supply.mli`, `analysis/weinstein/snapshot_pipeline/lib/resistance_sketch.mli`, `trading/backtest/decision_grading/lib/laggard_cf.{ml,mli}`, `trading/backtest/test/test_runner_hypothesis_overrides.ml`, `trading/data_panel/snapshot/lib/snapshot_schema.mli`, `trading/simulation/test/test_metrics.ml`, `trading/weinstein/strategy/lib/weekly_sidetable_reader.mli`, `trading/weinstein/strategy/test/test_reserve_resting_ticket_cash.ml` (the last is new as of #2378 the same day, so the population is still growing). Four recurring mechanical shapes: `End of text is not allowed in '[...]'`, `Blank line is not allowed in '[...]'`, `Unpaired ']'` (fix: `\]`), and `{N ...}` misparsed as a section heading (what a literal set like `{13, 26, 52}` in a docstring does). The single-line framing would have sent a cleanup agent to fix 1 of 26; corrected in place rather than filed as a second entry. Full measurement: `dev/health/2026-08-19-fast.md`. (source: 2026-08-19-fast.md)
- [x] rescale_duplication: trading/trading/weinstein/snapshot/lib/svg_series.ml (finding's stated `gen/lib` path doesn't exist; `svg_series.ml` lives in the sibling `lib/` — fixed the real file) — **switchover BLOCKED by A2 + an explicit local design constraint; documentation fixed instead.** Equivalence check: the `_factor`/rescale formula is byte-identical (same NaN/non-positive-close guard, same `adjusted_close /. close_price`), but the two implementations are NOT byte-equivalent end-to-end as claimed — `Svg_series._to_adjusted_basis` builds its result via `Types.Daily_price.make` without `~active_through`, silently defaulting it to `None`, while `Adjusted_basis.to_adjusted_basis` uses `{ b with ... }` and preserves the input's `active_through`. This has no observable effect *today* only because `Svg_series._fold_week` (the sole consumer) also drops `active_through` on every path — but it is a real latent divergence, not "byte-equivalent," and would bite the moment `weekly_bars` is changed to preserve `active_through`. Dependency-direction check (item 3): wiring `Svg_series` to call `Adjusted_basis.to_adjusted_basis` would add a `weinstein.snapshot_pipeline` (analysis/) dependency to `trading/trading/weinstein/snapshot/lib/dune` (the `weinstein_snapshot` library) — a NEW analysis/ → trading/trading/ edge outside `trading/trading/backtest/**`, i.e. an A2 FAIL. This isn't just the generic A2 boundary either: `svg_series.mli`'s own docstring already states "the snapshot library deliberately carries no [analysis/] dependency" as a deliberate design choice (distinct from the sibling `weinstein_snapshot_gen` library, which already carries the full analysis dependency graph including `weinstein.snapshot_pipeline`). Per dispatch instructions, stopped rather than forcing the dependency or inverting it. Fixed what's in scope: rewrote the plain-comment "odoc ref" in `adjusted_basis.ml` (`{!Weinstein_snapshot.Svg_series._to_adjusted_basis}`, targeting a private symbol) to prose explaining the duplication is intentional and permanent given the layering boundary, and added a matching comment in `svg_series.ml` pointing back at `Adjusted_basis.to_adjusted_basis` so both sides tell the same story instead of reading as "forgot to switch over." No behavior change; `dune build`/`dune runtest` unaffected. **Follow-up for a future decision-item (not cleanup-owned, needs feat-weinstein or a design call):** either accept the duplication as permanent (and maybe add a cross-file test pinning the two formulas stay identical) or relocate `Adjusted_basis` to a location `weinstein_snapshot` can legally depend on. Also logged: `_fold_week`'s current disregard for `active_through` means the latent divergence above is dormant, not fixed — worth a note if `weekly_bars` semantics around `active_through` ever change. PR #TBD, branch cleanup/rescale-duplication (2026-07-29)
- [x] adjusted_basis_sync_pin: trading/trading/weinstein/snapshot/lib/svg_series.ml + trading/analysis/weinstein/snapshot_pipeline/lib/adjusted_basis.ml — resolution (a): added `weinstein_trading.snapshot` to `snapshot_pipeline/test/dune` (legal under A2, no cycle) and two cross-file pin tests in `test_adjusted_basis.ml` comparing `Svg_series.weekly_bars [b]` against `Adjusted_basis.to_adjusted_basis b` (equal on all fields except `active_through`, which is separately pinned as a documented divergence). Mutation-verified: perturbing `_factor`'s rescale ratio by 1% turned the new pin test RED (`Expected ... open_price = 24.24 ... not equal`) alongside 4 pre-existing tests in the same/adjacent suites; reverted, all green again. Also fixed the `adjusted_basis.mli` docstring's inaccurate "semantics match" claim to name the `active_through` exception precisely. PR #2166 (2026-08-03). **Rework iter 1 (qc-behavioral CP1):** the corrected docstring claimed `active_through` preservation was pinned, but the split-sync test overwrote `via_pipeline.active_through` before comparing, so the `Some d`-carries-through half was asserted nowhere — added `assert_that via_pipeline.active_through (is_some_and (equal_to _as_of))`; mutation-verified (switching `to_adjusted_basis` to build via `Daily_price.make` without `~active_through` turned it RED, reverted, green). Also closed two of three non-blocking FLAGs: added a guard-branch (`f = 1.0`) cross-file case so deleting `Svg_series`'s guard now also goes RED (mutation-verified), switched the split-sync factor to a non-power-of-two ratio (10.0/70.0), and added `size_is 1` assertions ahead of every `List.hd_exn` in the cross-file section.
- [x] design_doc_drift: docs/design/backtest-scale-optimization-2026-04-17.md — added a 23-subdir inventory appendix to `dev/plans/backtest-scale-optimization-2026-04-17.md` (the finding's stated path doesn't exist; no `docs/design/` file by this name — fixed the real one). PR #2096 (2026-07-26)
- [x] design_doc_drift: docs/design/eng-design-2-screener-analysis.md — added a "Added after the initial design" bullet block to §2.1 Components documenting all 6 missing subdirs (support, continuation, laggard_rotation, stage3_force_exit, snapshot_pipeline, snapshot_runtime), descriptions derived from each module's .mli. PR TBD (2026-07-27)
- [x] nesting: garch.ml + snapshot_writer.ml + split_detector.ml — verified clean 2026-05-22; nesting_linter no longer flags any of these files (likely subsumed by PR #977 / #978 which extracted helpers for the same files)
- [x] nesting: pipeline.ml+daily_panels.ml+optimal_strategy_runner_helpers.ml+optimal_portfolio_filler.ml — extracted _build_rows, update_bench, _insert_rows, _neutral_ctx, _compare_by_r_multiple, _compare_by_score; all 5 dispatch violations cleared (branch cleanup/nesting-pipeline-batch-2, 2026-05-08)
- [x] nesting: 8 final violations — garch.ml, snapshot_writer.ml, split_detector.ml, split_event.ml, runner.ml, reconciler_writer.ml — extracted _run_garch_loop, _version_mismatch_error, _mkdir_error, _snap_if_split, _replace_if_symbol, _pos_symbol, _held_symbols_of_last_step, _write_split_row, _position_symbol; all 8 cleared. PR #978 (2026-05-08)
- [x] nesting: trading/trading/weinstein/snapshot/lib/pick_diff.ml + trading/analysis/weinstein/snapshot_pipeline/lib/snapshot_manifest.ml — verified clean 2026-05-22; nesting_linter no longer flags either file
- [x] nesting: analysis/data/synthetic/garch.ml + regime_hmm.ml + data/types/split_detector.ml + wiki_sp500/changes_parser.ml + stock_analysis.ml — extracted _validate_sample_inputs, _garch_step, _sample_step, _classify_ratio, _date_of_groups, _split_factor_of_bar; all 5 violations cleared. PR #977 (2026-05-08)
- [x] nesting: trading/analysis/data/sources/wiki_sp500/lib/ticker_aliases.ml — verified clean 2026-05-22; nesting_linter no longer flags this file (file avg below 2.5)
- [x] fn_length: trading/trading/simulation/lib/simulator.ml — verified clean 2026-05-22; fn_length_linter says "no functions exceed 50 lines" (step fn was extracted in subsequent simulator-fix PRs)

- [x] nesting: trading/trading/backtest/optimal/lib/optimal_strategy_report_sections.ml — extracted _cmp_actual_by_date, _actual_date_break, _label_actual_group, _cmp_optimal_by_date, _optimal_date_break, _label_optimal_group, _ratio_narrative; all 3 violations cleared (branch cleanup/nesting-report-sections, 2026-05-08)
- [x] nesting: trading/trading/backtest/optimal/lib/outcome_scorer.ml — extracted _step_stage3, _make_initial_state, _resolve_exit; both violations cleared. PR #950 (2026-05-08)
- [x] nesting: trading/trading/backtest/optimal/lib/optimal_run_artefacts.ml — extracted _rejection_pair_of_alternative + _pairs_of_audit_record; nesting linter clean. PR #949 (2026-05-08)
- [x] fn_length: trading/trading/backtest/optimal/lib/optimal_strategy_runner.ml — extracted 7 helpers (calendar, analysis, sector, forward, scan) to Optimal_strategy_runner_helpers; 413→282 LOC (branch cleanup/optimal-runner-split-2, 2026-05-08)
- [x] nesting: trading/trading/data_panel/snapshot/lib/snapshot_format.ml — extracted _check_all_hashes_equal, _build_manifest, _flush_to_channel, _try_write_file, _check_payload_length, _check_payload_md5, _check_row_count, _check_schema_hash; all violations cleared (branch cleanup/nesting-snapshot-format, 2026-05-08)
- [x] nesting: analysis/data/synthetic/garch.ml + regime_hmm.ml + data/types/split_detector.ml + wiki_sp500/changes_parser.ml + stock_analysis.ml — extracted _validate_sample_inputs, _garch_step, _sample_step, _classify_ratio, _date_of_groups, _split_factor_of_bar; all 5 violations cleared. PR #977 (2026-05-08)
- [x] nesting: trading/analysis/data/sources/wiki_sp500/lib/ticker_aliases.ml — verified clean 2026-05-22; nesting_linter no longer flags this file (file avg below 2.5)
- [x] fn_length: trading/trading/simulation/lib/simulator.ml — verified clean 2026-05-22; fn_length_linter says "no functions exceed 50 lines" (step fn was extracted in subsequent simulator-fix PRs)
- [x] nesting: trading/trading/simulation/lib/{antifragility,return_basics,distributional}_computer.ml — extracted _add_ols_pair, _zero_sums, _add_sq_dev, _add_moments, _bucket_loop helpers; all 5 dispatch violations cleared. PR #967 (2026-05-08)
- [x] magic_numbers: trading/trading/backtest/optimal/lib/optimal_strategy_report_sections.ml — extracted _strong_outperform_threshold=3.0 and _moderate_outperform_threshold=1.5; linter clean (branch cleanup/optimal-report-sections-magic, 2026-05-08)
- [x] nesting: trading/analysis/scripts/build_snapshots/build_snapshots.ml — extracted 5 helpers (_entry_is_current, _write_and_checksum, _file_metadata, _build_or_log, _load_benchmark_bars, _make_progress, _last_symbol, _fold_symbol); all 78 fns pass nesting linter (branch cleanup/nesting-build-snapshots-2, 2026-05-08)
- [x] nesting: trading/trading/weinstein/strategy/lib/entry_audit_capture.ml — extracted 5 helpers; classify_candidate/emit_entries/alternatives_of_decisions all pass; file avg now under 2.5 (branch cleanup/nesting-entry-audit-capture, 2026-05-08)
- [x] fn_length + magic_numbers: trading/analysis/weinstein/snapshot_runtime/lib/snapshot_bar_views.ml — condensed comments −15 lines (297); restructured to avoid bare literals on mid-comment lines. PR #924 (2026-05-07)
- [x] nesting: trading/analysis/weinstein/snapshot_runtime/lib/snapshot_bar_views.ml — extracted 9 helpers (_make_daily_price, _match_ohlcv, _fetch_and_build_weekly_view, _fetch_weekly_bars, _to_weekly_bars, _daily_view_from_idx, _find_and_build_daily_view, _low_buf_from_idx, _check_and_fetch_low); all 5 violations cleared. PR #966 (2026-05-08)
- [x] file_length: trading/analysis/weinstein/snapshot_runtime/lib/snapshot_bar_views.ml — 338->295 LOC; extracted 5 OHLCV helpers to snapshot_bar_views_helpers. PR #972 (2026-05-08)
- [x] tracked_artifact_linter: trading/devtools/checks/ — **no check anywhere in the repo would notice a build artifact being re-tracked.** Filed by qc-behavioral on PR #2248 as `harness_gap: LINTER_CANDIDATE`, verified by enumeration: all **31** scripts in `trading/devtools/checks/` were read and none checks tracked-vs-gitignored file lists, git-status cleanliness after a build, or any "no build output is tracked" property; the 3 `.github/workflows/**` hits for `git ls-files`/`gitignore`/`git status --porcelain` (`track-pacer-weekly.yml`, `iwv-scrape-once.yml`, `health-deep-weekly.yml`) all check whether specific review/health files were generated, not whether artifacts got tracked. So #2248 closed one *instance* (`trading/compile_commands.json`) but not the *class*: `git add -f` silently bypasses the new `.gitignore` entry, and a different future dune output tracked the same way would go unnoticed. This matters beyond tidiness — a tracked artifact that re-dirties the tree on every build is a contributing mechanism for the blanket-`git add` sweep-in that is the repo's top open escalation (#2224, #2235 each carried unrelated production code into an auto-merged ops PR past both QC gates). Fix shape: a check asserting either "after a clean `dune build`, `git status --porcelain` is empty" or "no path matching known dune-generated-artifact patterns appears in `git ls-files`". Note `harness-maintainer` owns `devtools/checks/` per §Out of scope, so this dispatches there, not to `code-health`. (source: 2026-08-09 qc-behavioral residual on PR #2248)

  **CLOSED 2026-08-09.** Built `trading/devtools/checks/tracked_artifact_linter.sh`, wired into `dune runtest` via `trading/devtools/checks/dune`. Chose neither fix shape as originally proposed verbatim: instead of the dynamic "`git status --porcelain` after a build" (awkward inside a dune sandbox rule) or a hand-maintained artifact-pattern list (misses unenumerated future artifact kinds), it runs `git ls-files -i -c --exclude-standard` (tracked-AND-ignored) against the real working tree (via `repo_root()`) and defaults to DENY: any hit is a FAIL unless allow-listed. This reuses `.gitignore` itself as the source of truth for "what counts as an artifact," so it automatically covers every current and future `.gitignore` pattern with zero duplicated pattern-list maintenance — closing the *class*, not just enumerated instances. Verified empirically before committing to this shape: a naive "fail on any output" gate does NOT work — on main today that primitive returns ~1334 hits, 1285 of them `trading/test_data/**/data.csv|data.metadata.sexp` (deliberately committed golden bars; `.gitignore`'s own comment says the pattern protects only *new* fetches) plus 49 pre-existing accidental commits under `dev/backtest/scenarios-2026-04-14-*/` and 4 `dev/experiments/*/` run dirs. Added an 8-prefix allow-list to `linter_exceptions.conf` under a new `tracked_artifact` key (same convention as `magic_numbers`/`mli_coverage` entries); confirmed the 8 prefixes account for exactly 0 unaccounted hits out of the 1334. The 7 non-test_data prefixes are dated `review_at: 2026-09-09` (real debt, not addressed here — filed as `H-TRACKED-SCENARIO-DEBT` in `dev/status/harness.md` §Follow-up); the `trading/test_data/` prefix is `review_at: never` (documented, permanent convention). Verified both directions: positive control (`git add -f trading/compile_commands.json` after rebuilding it) → RED, exit 1, names the exact path, reverted with `git restore --staged`; negative control on the clean tree → exit 0. Wired check confirmed actually running as part of `dune runtest devtools/checks/` (not just present on disk) — its `check_universe_deps.sh` self-guard also confirms the owning rule declares `(universe)`, required because `git ls-files` state can change independent of any dune-tracked source file. Known scope gap, stated in the script header: a build-artifact kind that has never been added to `.gitignore` at all stays invisible to this check until someone adds the ignore pattern — closing that residual needs the dynamic `git status --porcelain` property, judged not worth the dune-sandbox complexity with zero known instances today. Branch `harness/tracked-artifact-linter`.
- [ ] flaky_test: trading/analysis/.../test_csv_snapshot_builder_cleanup — a SIGTERM-based cleanup test that **failed for qc-behavioral before any mutation was applied**, while the PR author, qc-structural (in an isolated `git worktree`) and the orchestrator each measured `dune runtest` **exit 0** on the same SHA (`4912c609`), and a post-merge full run on `main` was also exit 0. So it is intermittent, not broken. Worth pinning down precisely because **every merge gate in this pipeline is an exit-code gate**: an intermittently-failing test in the shared `dune runtest` target can red a good PR or, worse, train agents to discount a real failure. Likely a timing/signal-delivery race in the cleanup path. Fix shape: reproduce under load (run the scoped suite in a loop), then either make the SIGTERM wait deterministic or quarantine it out of the shared target until it is. (source: 2026-08-09 qc-behavioral residual on PR #2249) **Second independent observation 2026-08-15 (orchestrator run 31872019203), and it adds the missing correlate — LOAD.** Same run, same day, opposite results: the orchestrator measured whole-repo `dune runtest` **exit 0** on `4b7c410e` with nothing else building, while a harness agent measured **exit 1** on a *pristine `origin/main` worktree with zero changes applied* — at a moment when 2-3 concurrent cold builds were running on the same 4-core runner. That matches the suspected timing/signal-delivery race and gives the reproduction recipe a concrete knob: **run the scoped suite in a loop while saturating the box**, rather than in a loop alone. Note the shape of the risk this time — the agent correctly identified it as pre-existing by re-running on pristine main, but an agent that did not think to do that would have read it as its own regression and burned a rework cycle chasing it. **Third independent observation 2026-08-18 (orchestrator run 32111680652), and it is the first with a measured rate rather than a single hit.** A harness agent investigating `H-RUNENV-DUNE-PROJECT-WARNING` ran `dune runtest` four times on a clean worktree and saw the SIGTERM-cleanup fail in **2 of 4** runs -- crucially, **both with and without its candidate change applied**, so the change is exonerated and the flake is confirmed independent of the diff. The runner was under concurrent load throughout (three other agents active). That is a **~50% failure rate under load**, which upgrades the reproduction recipe from a hypothesis to something directly actionable: the existing entry asks for "run the scoped suite in a loop while saturating the box", and the saturation half is now known to be sufficient on its own at 4 cores. Note the compounding risk this observation adds -- the agent that hit it was *also* the agent evaluating a build-config change, i.e. exactly the situation where an intermittent red is most likely to be misattributed to the change under test.

Orchestrator populates this from `dev/health/<date>-{fast,deep}.md`. Items here are eligible for next dispatch.

## Completed
- [x] mechanism_retirement (Rule 4): `enable_continuation_buys` + `continuation_config` — **NOT ELIGIBLE; no code removed.** Screened per `.claude/rules/experiment-flag-discipline.md` Rule 4 (dispatched as the P1 retirement row in `next-session-priorities-2026-08-14.md`). Determination: **REJECT-as-default-but-legitimate-axis**, so KEEP default-off. (1) The RETIRE row was never seeded — the 08-09 priorities §P1 graveyard list names *"scale-in v2 continuation-add"*, i.e. the scale-in mechanism retired by #2299; the inventory split one seed item into two rows. (2) One of its two cited ledger REJECTs, `2026-07-05-continuation-add-v2-surface`, armed `enable_scale_in` + `Scale_in_detector.Consolidation_breakout` (#1855, code #2299 already deleted) — not this flag; #2299 already spent that verdict. (3) No do-not-revive is recorded anywhere; `project_continuation_combined_rejected` + `dev/experiments/continuation-combined-2026-05-14/report.md` instead name a regime-gated follow-up and make retirement *conditional on it failing* — it was never run. This is the deciding asymmetry vs #2299, whose named revival path *was* recorded as cancelled (`project_envelope_knobs_dead`, #1861). (4) Entry mode (base vs continuation) is a Weinstein-documented dial per `.claude/rules/weinstein-faithful-core.md` — which states the #1366 rejection tested a one-at-a-time graft, not the mechanism — and this flag is the Trader preset's config home in the open `weinstein-trader-investor-presets-2026-05-31.md`. The one on-point REJECT (`2026-05-14-continuation-combined-axis`) is two single-run windows on one universe, pre-WF-CV, with a self-declared approximate config hash. Actions: reclassified the row RETIRE → KEEP-AXIS in `dev/notes/mechanism-flag-inventory-2026-08-09.md` (+ correction-log entry, tallies, step-3 sequencing), evidence in `dev/plans/continuation-retirement-2026-08-13.md`. Re-enters the graveyard only if the trader-preset program is *recorded* as closed, or a regime-gated WF-CV surface REJECTs. Docs-only diff, no `trading/` file touched. Branch feat/continuation-retirement (2026-08-13)
- [x] tracked_build_artifact: trading/compile_commands.json — a `dune build` output was tracked in git and not gitignored, so every build regenerated it with host-specific opam paths and dirtied the tree; accidentally committed at least 3 times (#1299, #2085, #2129). Grepped the repo (dune files, dune-project, CI workflows, devtools/checks, .vscode, .devcontainer, Makefiles): **no consumer** — only prose about the same nuisance plus dune's own `_build/` bookkeeping. Gitignored (root `.gitignore:149`) + `git rm --cached`. Churn quantified: committed copy 508 B vs regenerated 548 B; the 40-byte delta is exactly `5.3` → `5.3.0+flambda` in 4 include paths (10 chars × 4). Acceptance verified live on main post-merge: clean tree → full `dune build && dune runtest` → **still clean**. PR #2248, merged `b8388074` (2026-08-09)
- [x] nesting: trading/trading/weinstein/snapshot/lib/round_trip_verifier.ml — extracted _stop_check/_carryover_stop/_stop_adjusted_ok/_stop_unchanged_ok helpers; all 3 violations cleared. PR #964 (2026-05-08)
- [x] nesting: trading/analysis/weinstein/screener/lib/screener.ml — extracted _filter_and_cap helper; _evaluate_longs/_evaluate_shorts nesting violations cleared. (branch cleanup/nesting-screener-eval-2, 2026-05-08)
- [x] file_length: trading/trading/engine/lib/price_path.ml (511→498) + trading/trading/weinstein/strategy/lib/entry_audit_capture.ml (305→297) — condensed private-fn docstrings; both files now within limits. PR #958 (2026-05-08)

- [x] nesting: trading/trading/weinstein/strategy/lib/exit_audit_capture.ml — extracted _pct_distance_from_callbacks, _make_exit_event, _handle_trigger_exit helpers; nesting linter now passes. (branch cleanup/nesting-exit-audit-capture, 2026-05-07)
- [x] fn_length + file_length: trading/trading/backtest/lib/runner.ml — extracted `Runner_metrics` module + `_filter_steps`/`_extract_filtered_logs` helpers; runner.ml 528→468 lines, `run_backtest` 83→49 lines. Branch cleanup/runner-fn-length (2026-05-07)
- [x] fn_length: trading/trading/backtest/optimal/lib/optimal_strategy_runner.ml — extracted 8 helpers to Optimal_friday_helpers; 413→270 lines (branch cleanup/optimal-strategy-runner, 2026-05-07)
- [x] file_length: trading/trading/backtest/optimal/lib/optimal_strategy_report.ml — extracted divergence + missed-trades sections to Optimal_strategy_report_sections; 488→281 lines (source: dispatch 2026-05-07, PR #928)
- [x] file_length: trading/trading/backtest/lib/panel_runner.ml — extracted step-loop helpers to Panel_step_loop module; 309→237 lines (source: dispatch 2026-05-07, branch cleanup/file-length-panel-runner)
- [x] file_length: trading/trading/backtest/lib/result_writer.ml — extracted trades-CSV cluster to Trades_writer module; 372→245 lines (source: dispatch 2026-05-07, PR #929)
- [x] fn_length + file_length: trading/trading/weinstein/strategy/lib/entry_audit_capture.ml — extracted entry construction + debug trace helpers to entry_audit_helpers.ml; 383→272 lines; make_entry_transition 54→31 lines (PR #930, 2026-05-07)
- [x] nesting: trading/analysis/data/storage/csv/lib/csv_storage.ml — extracted `_parse_and_accumulate` and `_read_next_line` helpers; nesting linter now passes (835 fns, all OK). (source: 2026-04-26-fast.md, PR #578 merged 2026-04-26T16:04Z)
- [x] fn_length / file_length: weinstein_strategy.ml — added @large-module annotation; file length linter now passes. (source: 2026-04-19-fast.md, PR #453, 2026-04-19)
- [x] file_length: trading/analysis/weinstein/screener/lib/screener.ml — extracted sector_rating/scoring_weights/grade_thresholds types + signal helpers + price helpers to Screener_scoring; 708→485 lines (under 500 hard limit). (source: dispatch 2026-05-07, branch cleanup/screener-large)

## Out of scope

- Behavior changes — escalate to the relevant feat-agent.
- Linter rule changes — `harness-maintainer` owns `devtools/checks/`.
- Multi-file refactors crossing module boundaries — feat-agent.

## How findings get here

`lead-orchestrator` Step 2e parses the most recent `dev/health/<date>-deep.md` and `<date>-fast.md`, picks `[medium]` or `[high]` findings (skips `[info]`), and appends one Backlog entry per finding it doesn't already see. The entry shape is:

```
- [ ] <finding_type>: <path/to/file.ml> -- <one-line context: what is wrong and what the fix shape is> (source: <basename of dev/health report>)
```

**This block is an ILLUSTRATIVE TEMPLATE, not a live backlog item.** It is
fenced and lives under `## How findings get here`; the real queue is
`## Backlog` above. Do not tick it, do not rewrite it with a concrete
finding, and do not dispatch from it. (Restored 2026-08-09: PR #2248's
author flipped this example to `[x]` and rewrote it with that PR's
specifics, and the orchestrator's own Step 2e scan had already selected it
as a dispatch target because a bare `grep '^- \[ \]'` over this file
matches inside the fence. The underlying finding was real and is recorded
in `## Completed`; only the bookkeeping was wrong.)

Subsequent runs may dispatch `code-health` to work the top item; agent flips to `[~]` on start, `[x]` on completion.
